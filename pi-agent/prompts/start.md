---
description: Detect the OS/shell and apply platform assumptions before acting (platform-env)
---
Load the `platform-env` skill (`skills/platform-env/SKILL.md`), detect the OS and
shell, state the environment note (platform, shell, python launcher, temp/home,
path & coding conventions), and apply its rules for this session or task.
