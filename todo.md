# Homelab TODO Backlog

> Restructured **2026-08-17** (and trimmed 2026-08: struck rows removed; deploy-gated live-verify moved to [`deployment-tasks.md`](deployment-tasks.md) per-phase blocks). Human decisions moved to the front, work organized into domain modules, deferred items parked. Done items live in the owning `docs/*.md` + git history (frozen `reports/changelog.md` is archive-only); conventions → [`CONVENTIONS.md`](CONVENTIONS.md). Single source for planned work + open decisions (HD-XX).

---

## 0. How this backlog works

- **One row = one outcome.** New work gets `HD-<next>` and links its owning `docs/*.md`.
- **A row is not a changelog.** It says **what is still missing** and, usefully, **how to do it** — the command, the gotcha, the measurement you would otherwise re-take. Everything *durable* (a finding, a root cause, how a subsystem behaves, what was shipped and verified) goes into the owning `docs/*.md` and the commit, and the row keeps only a link to it. So a healthy edit that lands a discovery makes the row **shorter**, not longer, and a row whose `⏳` list is empty is deleted, not decorated with a ✅. Litmus test before saving: does this sentence help whoever finishes the task, or does it only describe what already happened? The first goes in the row, the second in the doc, and neither twice.
- **Priority (P):** 1 = highest (how hot), module = what domain it touches. Priority is per-row, modules group.
- **Executors:** `AI` · `AI + gate` (human checkpoint) · `AI + Human` (joint) · `Human` (blocks).
- **Lifecycle:** open → (decided) → done — ONE pattern (CONVENTIONS §4): a **fully-done** row is DELETED from this file (record lives in the owning doc + git history); an **IaC-done-but-deploy-gated** row STAYS with a `⏳` tail (trimmed to pending items) until the live verify closes it. Deferred → park section. **Decisions:** a decided item is written **once** to its owning `docs/*.md` (+ the `<domain>-rejected.md` log where applicable) and the full rationale is **removed** from `todo.md` — §1 shows only still-open decisions/purchases (compact pointer, no struck duplicate). Historical decisions + done rows live in the frozen `reports/changelog.md` (archive-only).
- **Conventions / onboarding:** consolidated rule index + 10-step service-onboarding checklist in [`CONVENTIONS.md`](CONVENTIONS.md).
- **Service stages:** service-onboarding rows (per-checklist tasks) carry **`Stage: N/10`** in the bold title = current Service-onboarding checklist step ([CONVENTIONS.md](CONVENTIONS.md) §5). `10/10` = deployed + verified; only then does a row close. Non-service tasks (network/fix/decision) carry no stage marker.

## 1. Human decisions & purchases — review first

### Decisions (blocking / waiting on a human call)

> **Open as of 2026-09-21 — after the owner answer round, NO decision blocks AI work.** What is left on the owner's side is hands and eyes, not calls: the **HD-147** browser logins (plus the Forgejo-vs-GitHub question that lives inside it), the **HD-411** APK sideload + verdict, and the physical/bench windows listed in [todo-table.md](todo-table.md) §A2.
>
> **Owner round 2026-09-25 (two sittings) — answered, logged once, do not re-ask.** Where each answer lives:
> the SSH-grant rulings, the `domen` account shape and the Pi `admin` retirement order →
> [deployment-secrets.md](docs/deployment-secrets.md) §Who is authorized where; the Pi's tailnet admission and its
> `tcp/443` grant → [network-vpn.md](docs/network-vpn.md) §Tailnet boundary; the Metabase retirement tail →
> [deployment-secrets.md](docs/deployment-secrets.md) §Item titles are load-bearing; oldsrv's git push path →
> [deployment-ansible.md](docs/deployment-ansible.md) §Runner placement; the immich `foto` fix →
> [deployment-oidc.md](docs/deployment-oidc.md) §Immich. The 1Password rename to `domen_ssh` was executed the same
> day and the script references + doc rows moved in the same change. ⚠ **What that round changed is the blocker
> list, not the decision list:** `oldsrv` is off the network and the WoL path is spent (HD-455 → HD-454), so every
> oldsrv leg is parked — do not plan oldsrv work as if it were runnable.
>
> **Open as of 2026-09-22 — three memory-plane calls, measured and waiting (no HD row until you answer):**
> **OQ-12** is `agentmemory` the ONE central memory plane (not as specified on 0.9.29: loopback-forced REST, one
> shared bearer, `TEAM_*` inert in the search path ⇒ central means a proxy + `project` tags); **OQ-13** where
> Hermes' memory lives (the owner ruling "not its own files" **is** implementable — `memory_enabled: false`;
> `agentmemory` can never target Qdrant, so a Qdrant Hermes means two planes); **OQ-14** whether the `bge-m3` leg
> is needed (keyless clears every gate; the leg is a measured +2). One line each in
> [services-ai.md](docs/services-ai.md) §9b, evidence [`reports/probe-agentmemory-20260921.md`](reports/probe-agentmemory-20260921.md).
> Nothing was installed, nothing was decided, and **no AI work waits on them**.
>
> **Decided 2026-09-21** — each written once to its owning doc + its `<domain>-rejected.md` row, so do not re-ask any of them: the cockpit surface + which account runs it (**HD-409**), the exit-node host (**HD-408** → row deleted, stays on the Pi), the family-support onboarding model (**HD-412**), the git-credential shape on the control node (**HD-407 / HD-409**), the scoped-consumer grant (**HD-384**), the second OWUI instance (**HD-248**, deferred), the tailnet resolver chain (**HD-415**, redesigned rather than trimmed), the HA trusted-proxy fix (**HD-418**) and the jellyfin publish (**HD-419**). Note for the next session: four items previously described as "only a human can place these" turned out to be AI-runnable (**HD-347** reads its own group id, **HD-350** appends its own pubkey, **HD-407**'s `op read` is non-interactive) or owner-minted into the vault already — so the human-gate wording in a row is a claim to test, not a fact to obey.
>
> Resolved decisions are logged once in their owning `docs/*.md` + the `<domain>-rejected.md` decision log and are **not** duplicated here.

## 2. Active work — by module

### 2.1 Network & DNS — VLANs, firewall, DNS, VPN, router/switch

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-318 | 2 | AI + gate | 1 | **oldsrv Phase-3 provision — ✅ PHASE-3 COMPLETE + LIVE (2026-09-08):** full `home_servers.yml` converge **failed=0**, 17 enabled `docker_services` Up + healthy. ✅ First contact + office/amd_rocm/ollama + 1P items done — original blockers all cleared: **① ONLYOFFICE repo/key fixed (official repo), ② ROCm pins MOOT — spark = sole inference tier (HD-335), `amd_rocm` kept as Debian-trixie-native host userland only, ③ `check-vault-items.sh --strict` green (`pihole_password`/`sonarr_api`/`radarr_api` present)**. ✅ **Converge-driven fixes (live-verified, 2026-09-08):** media-library layout, pihole `DNSMASQ_USER: root` + avahi-daemon stopped/masked (was blocking pihole's 5353 port), gluetun `custom` PrivadoVPN endpoint (owner-supplied, qbit compose), docker-orphan-bridge cleanup (6 stale `br-*` stole routes to container IPs — removed; host→container routing restored), Technitium secondary admin-align + seed (**HD-340 DONE — all 3 DNS legs seeded**). ✅ **RO-path fixes batch (commit `320257b`..`aae0182`, branch `session/hd318-ro-fixes-20260908`):** dropped `read_only` on qbittorrent/seerr/profilarr(+parser)/actual-budget/kopia-agent/signal-cli; per-image root causes fixed live — linuxserver s6 (`/run:exec` + SETGID/SETUID), seerr `bind_dirs: ['config']` (uid 1000), actual-budget data mount `/app/data`→`/data` + `bind_owner_uid: 1001` (image's real dir), profilarr/signal-cli CHOWN+SETGID+SETUID (su-exec/setpriv), signal-cli DAC_OVERRIDE+FOWNER (jsonrpc2-helper persist), kopia-agent `entrypoint: []` + URL-encode server password (fold-based, compose-`$$`-safe) + `KOPIA_PASSWORD`. **LIVE RESULT (2026-09-08):** qbittorrent ✅ seerr ✅ profilarr+parser ✅ jellyfin ✅ pihole ✅ actual-budget ✅ signal-cli ✅ all UP + healthy. ⏳ **Remaining:** (a) `kopia-agent` — ✅ **CLOSED + LIVE (HD-318a, 2026-09-08):** VPS leg converged (fingerprint seeded from the live cert), oldsrv re-render + auth fix landed — agent **Up + verified** (HTTPS + fingerprint pin; repo user `oldsrv-agent@oldsrv.kogler.si` provisioned with the REPO MASTER password — kopia quirk, `access denied` otherwise; htpasswd carries server-admin + agent entries; compose command-block `$$`-escaping fixed — single-`$` was mangling the credential at parse time); first snapshots created (/opt 1.3 GB 5648 files + signal-cli-data, retention labels offsite). Commits `94f6231` + `98d4720`. (b) `recyclarr` quality-profile sync — stacks live (all *arr + recyclarr Up 2026-09-08); verify profile sync at the next @daily run. (c) ~~home-assistant standby keepalived render~~ **✅ CLOSED + LIVE 2026-09-08 (HD-318c):** standby role fixed (cold-standby dir pre-create, active keepalived.conf bootstrap from BACKUP, `/etc/ha-failover` prereq, `home_assistant_version` on oldsrv) + rendered `/opt/home-assistant-standby` (compose + BACKUP keepalived + failover variant + secrets + config) + ha-failover-api active. (d) `signal-cli` phone registration — **the owner-manual link is done** ([observability.md](docs/observability.md) §Alerting records the container linked to Domen's personal number + the dedicated alerting group (its name is recorded in observability.md §Alerting)); what is still missing is the group UUID in SSOT → tracked under **HD-347**, not here. · [hardware-oldsrv.md](docs/hardware-oldsrv.md) · [services-downloads.md](docs/services-downloads.md) · [observability.md](docs/observability.md) §Alerting |
| HD-373 | 2 | AI | 2 | **litellm `/ui` SPA-fallback — `/ui/login` deep-link 404s (upstream nginx gap); workaround `/fallback/login` (intended flow)** — **OPEN (2026-09-15).** litellm `v1.83.10-stable` serves `/ui/` (index.html) but NOT the client-side SPA routes (`/ui/login` → 404) because the container has **no nginx SPA fallback** (upstream docker image normally ships `ui/nginx.conf` `try_files /$1.html /$1/index.html /index.html`; our container = `NO_NGINX`). Same as upstream issue #29340 (pip 1.85.1, `/fallback/login` works). **Workaround (live):** `https://litellm.kogler.si/fallback/login` — litellm's intended fallback login app route + UI "Try Fallback Login"; master-key (`litellm_api`) login works → admin UI reachable. Authentik SSO/forward-auth NOT recommended (heavy; would break the same-host OpenAI-API bearer consumers unless route-scoped to `/ui/` only). ⏳ **Fix options (pick one):** (a) litellm via nginx SPA fallback (`ui/nginx.conf` in the image, or Traefik `PathPrefix(/ui/)` → rewrite to `/ui/`); (b) route-scope Authentik forward-auth on `/ui/*` only (not the API). · [services-ai.md](docs/services-ai.md) · [network-vpn.md](docs/network-vpn.md) · 📋 [`prompt-384.md`](prompt-384.md) |




| HD-406 | 2 | AI | 2 | **WireGuard road-warrior peer on the RB4011 — the dev path that depends on neither the VPS nor headscale (⏳ decided 2026-09-20)** — the "site-to-site only, no road-warrior" strategy was taken for the *family's* sake (an app toggle beats a config file); one admin device is a different cost, and the router is already a kernel-fast WG server on a DDNS endpoint (the same shape as the older travel-AP design in `brainstorming/`). A full-tunnel variant also answers the Slovenian-egress need with **no exit node at all**. Decision logged in [network-rejected.md](docs/network-rejected.md). ✅ **DECIDED 2026-09-21 (owner): build AFTER HD-414 is measured; the phone peer is SCOPED (one admin device), with the full-tunnel variant as an optional second allowed-IPs profile — that profile is what answers the Slovenian-egress need and is what makes the Pi's exit node redundant.** The AI mints the peer keypair in IaC vars (no owner credential hand remains). ⏳ **Then AI:** render the peer in the router role + `rb4011_converge.rsc.j2` (never a hand-applied delta — the 3-tier rule in [network-ops.md](docs/network-ops.md)), import, and the forward/NAT rules for the egress variant. ⚠ Gates: known-peer-only, allowed-IPs scoped to the ONE host, nothing advertised into any other VLAN range, rate-limit + log the listener. ⏳ Verify: the same reach matrix as HD-405, run twice (tunnel up / tunnel down) so the two paths are proven independent, not merely both configured. · [network-vpn.md](docs/network-vpn.md) · [network-ops.md](docs/network-ops.md) · 📋 [`prompt-414.md`](prompt-414.md) ⏳ **2026-09-22 re-check (HD-414 lane), row stays open, and one new fact sharpens it:** the *home* side now has inbound-capable v6, but the **VPS side cannot**: the VPS has a GUA + v6 default route and **no working IPv6** (both `ping6` targets 100 % loss, `curl -6` no connect, no ip6 nft ruleset). Also measured: the wg-s2s tunnel carries v6 but **nothing routes the Home prefix into it**, and `/interface wireguard servers` has **no `vrf=`** in 7.24 → a v6 road-warrior pool on that server would collide with oldsrv's advertised prefix rather than being isolated by a VRF. ✅ **DECIDED 2026-09-22 (owner): this path becomes MikroTik **Back To Home** — but not now.** Verified from MikroTik's docs so the next lane does not re-derive it: BTH is a RouterOS feature on ARM/ARM64/**TILE** (the RB4011 is TILE → supported), it is **WireGuard**-based, it works with **no public IP** by falling back to MikroTik **relay servers**, enabling it creates `/interface wireguard name=back-to-home-vpn` (listen-port 45415) and its peers automatically, and a plain WireGuard client can import the config from the `/ip cloud` menu. ⚠ Two things to settle BEFORE implementing: (1) **the converge collision** — BTH writes interfaces, firewall rules and `/ip cloud` state outside `roles/router`, while the RB4011 converge rebuilds filter rules device-wide, so the two will fight unless BTH's artifacts are managed in IaC or explicitly exempted and documented; (2) **it puts a vendor cloud and a vendor relay into the remote-dev path** — structurally the DERP shape this thread just measured at 70–240 ms on cellular; the *direct* case should hold here though, because the router holds the public IPv4/GUA and the **phone initiates**, which is exactly why the carrier block that killed Tailscale punching does not apply to it. Row stays open on owner timing. |
| HD-418 | 2 | AI + gate | 1 | **HA rejects anything oldsrv proxies — `ha_trusted_proxies` omits `oldsrv_home_ip`** — any request carrying `X-Forwarded-For` from a peer outside `ha_trusted_proxies` gets `400 Bad Request` (proven 2026-09-20: `Host: ha.kogler.si` + XFF → 400, without → 200), which means the standby edge's `ha` router has **never worked** and would fail the same way in a takeover; the tailnet router only sidesteps it by stripping the forwarding headers. **✅ DECIDED 2026-09-21 (owner): add `oldsrv_home_ip` to `ha_trusted_proxies`, in a planned window — it restarts the smart-home controller.** ⏳ AI: render the var, converge `home_assistant` on the ACTIVE HA host inside the window, then prove the standby edge proxies (XFF present → 200). Registered from the [network-vpn.md](docs/network-vpn.md) fault note, which had no row. · [network-vpn.md](docs/network-vpn.md) · [smart-home.md](docs/smart-home.md) · 📋 [`prompt-357.md`](prompt-357.md) |
> ✅ **HD-301 CLOSED 2026-09-22 (row deleted)** — the hardening floor was measured on the live RB4011 (7.24.4), which is what its tail asked for: `ftp`/`telnet`/`www`/`api-ssl`/`reverse-proxy` **disabled**; `ssh`/`www-ssl`/`winbox`/`api` enabled and each bound `available-from=<the Mgmt subnet>`. Nothing to change; re-verify after any factory reset. · [network-ops.md](docs/network-ops.md) §Service Binding & INPUT Firewall.
> ✅ **HD-419 CLOSED 2026-09-22 (row deleted)** — jellyfin publishes `127.0.0.1:8096` (**loopback**, not the Home IP: the edge is host-networked so loopback reaches it, and a Home-VLAN publish would have exposed Jellyfin's login + its CrowdSec/HSTS bypass to every device at home) and `media-backend` follows it. Proven: `media.kogler.si` was **502**, now **302 → 200 on `/web/`** through the home edge; siblings `ha-ts`/`immich-ts` 307. ⚠ **Found while verifying: the same 502 shape still stands for `seerr`/`sonarr` (measured 502 — 5055/8989 published on no interface)** and by extension the rest of the "home-hosted" route group; this row was written believing those already published. Fixing them is the identical one-line-per-service change — **no row exists for it yet**. · [services-traefik.md](docs/services-traefik.md) §The home edge · [services-media.md](docs/services-media.md).
> ⛔ **HD-09 VOID — deleted 2026-09-22, do not re-add.** The ask (open Home→Mgmt 80/443 for the UPS's old Mgmt address) cannot be implemented as written and should not be: the UPS management NIC left VLAN 99 with **HD-338** (moved to `fritzbox-lan`, the VLAN-20 no-WAN segment), and the plan of record in [hardware-ups.md](docs/hardware-ups.md) is that its **web UI and Modbus TCP stay unused with the NIC isolated** (NUT/USB + serial only). ⚠ Measured: **no such rule ever existed** — neither in IaC nor on the live device — despite the row's "✅ IaC done". ⚠ Drift found while checking it, **not touched here**: the `ups` row in the address SSOT reserves one VLAN-20 static while live ARP shows that same NIC answering on a different address in that segment — whoever touches the UPS plane next should reconcile the two. · [network-vlans.md](docs/network-vlans.md) §Inter-VLAN · [hardware-ups.md](docs/hardware-ups.md).
| HD-448 | 2 | AI | 2 | **The VPS's IPv6 was dead in both directions — and the cause was our own firewall, not the provider** — found 2026-09-23, fixed + live 2026-09-24. Mechanism: the `vps-hardening` input chain is an `inet` table whose only ICMP accept was `ip protocol icmp` — the IPv4 upper-protocol field, which matches NOTHING for v6 — so under `policy drop` the chain discarded the provider router's neighbour/router advertisements, `ip -6 -s neigh show` sat pinned at `fe80::1 … FAILED` (2.66M hits), and the v6 default route could never resolve a nexthop. **Attribution measured, not read off the template:** a counter-only rule (`nft insert rule inet filter input ip6 nexthdr icmpv6 counter` — no verdict, so it cannot change behaviour) counted nine inbound packets while `nstat Icmp6InNeighborAdvertisements` stayed 0, because the netfilter verdict precedes the ICMPv6 handler; arrive + not delivered = ours. Same instrument as the home punch discriminator (`a7fb1c6`), opposite verdict — and note the VPS's own drop-logger was the WRONG instrument there (rate-limited 5/minute, saturated at 50 lines/10 min): prove the logger before believing its silence. ✅ **SHIPPED + LIVE:** NDP (link-local-scoped, per RFC 4861) + the ICMPv6 error types (deliberately NOT source-scoped — PMTUD `packet-too-big` arrives from upstream with a GLOBAL address) + v6 echo parity with v4 + MLD (rides `nexthdr 0`, not 58), converged `playbooks/vps.yml --tags hardening` `ok=29 changed=2 failed=0`. Measured after: `fe80::1 … REACHABLE`, `Icmp6InNeighborAdvertisements` 0→5, TLS over v6 to an external resolver, sub-ms `ping6`, v4 unchanged — and the v4 check is what exonerated the change (nas/pi/spark all answer through the same jump and tunnel), which is proof rather than reasoning. ⚠ **Two corrections to this row's original text, kept because both are the class of error that survives a green converge:** the v6 default route **already existed** (statically, from the netcup image; `accept_ra=0`/`autoconf=0`, so SLAAC was never in play), which made this row's own acceptance line (`ip -6 route` must show `default via fe80::1`) **pass on a broken box**; and the fix therefore touches no addressing, no RA and no `sysctl` — input-chain rules only. ⛔ Acceptance is reachability, never route presence. ⏳ **Inbound half still open:** outbound v6 and NDP are measured, but no external v6 peer has connected *to* this box yet — one `curl -6 https://vps.kogler.si` from an off-net v6 host (a phone on LTE counts) closes it. ⏳ **OPEN owner question, deliberately NOT decided inside the change:** the `tcp dport 22/443`, `udp dport 51820` and RustDesk accepts are family-agnostic in an `inet` table, so they were IPv4-only only because NDP never resolved; they are reachable over v6 now. Confirm the parity or scope them `meta nfproto ipv4` (the `ip saddr …` rules — the DNS allow-set, the wg-s2s scoping — stay v4-only by the shape of the match). ⏳ The Cloudflare allowlist's VPS /64 is no longer decoration — keep it deliberately, or say why it stays. ⏳ Now-cheap follow-ups this unblocks, neither of which has a row: `blackbox-exporter`'s `preferred_ip_protocol: ip4` pin (whose comment still claims the container has no IPv6 route) and the `tailscale-sidecar` listen-port fix — the only remaining lever for a direct tailnet path. · [network-ops.md](docs/network-ops.md) §IPv6 · [security.md](docs/security.md) §8 · [services-vps.md](docs/services-vps.md) §VPS-Specific Firewall |

### 2.1a Consolidated work packages — what can be done together (2026-09-04)

> **Batching rationale:** nearly every row below is an edit to the SAME SSOT set (`network_static_hosts`, `rb4011_converge.rsc.j2`, `roles/router`) applied by ONE render → ONE `/import`; DNS rows share the `technitium-seed` role + `technitium_login`. Run each package as one SSOT render + one apply + an immediate tagged-99 membership verify (two full-converge mgmt-plane incidents recorded in `network-ops.md`) — never many small touches. **Swept 2026-09-22 (CONVENTIONS §4(a)):** packages **A, B, C, D and F are closed** — every row they folded is deleted (HD-326/312, HD-304/302/313/89/03/182, HD-324/330/340, HD-311, HD-331-334/349-352), so the packages carry no open work and their record is the owning docs + git history (`services-traefik.md` §Edge model, `network-vpn.md` §HD-333, `network-dns.md` §HD-334, `network-vlans.md` §Kids controls). Only **Pkg E** remains, and only because its owner failover test waits inside the **HD-04** row.

| Pkg | Rows folded | Runs together | Gates |
|-----|-------------|---------------|-------|
| **E — HA/owner tail** | HD-04 umbrella | Owner: `external_url` set (DONE 2026-09-07), Authentik-OIDC NOT wanted (owner decision), **failover test** (prepare in progress 2026-09-09 — button/runbook) | owner-only, not AI-runnable |

### 2.1b Human / owner decisions & work — what blocks what (2026-09-04)

> **Swept 2026-09-22:** items 1, 2, 5, 6 and 7 are DONE or SUPERSEDED and left this file (record: `network-vlans.md` §Kids controls + [network-rejected.md](docs/network-rejected.md) 2026-09-09 / 2026-09-21, `hardware-oldsrv.md` for the Phase-3 blockers). The one thing still owed is the `flash/` re-upload below.

| # | Owner task | Status | Blocks |
|---|------------|--------|--------|
| 3 | MikroTik admin password rotation | ✅ DONE (HD-321, 2026-09-03); ⏳ next-reset `flash/` re-upload | Pkg B close-out |

### 2.2 Storage, ZFS & UPS — NAS datasets, NFS, UPS/NUT

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-452 | 2 | AI | 1 | **`nut`'s ACL task reports `changed` on every converge of every tag, so the changed count is permanently yellow.** `roles/nut/tasks/main.yml`'s 2026-09-09 `upssched-cmd` task is `changed_when: true` under `tags: always` → any converge touching the role (`--tags cockpit` included) reports `changed=1` forever. Harmless individually, corrosive in bulk: the one number that means "this converge did something you did not ask for" stops being readable. Fix: read the real state back (`getfacl`/`stat`) and let `changed_when` judge it; acceptance = a second consecutive converge of the same tag reporting `changed=0` on a compliant host. ⚠ **Same class, measured 2026-09-25 on the VPS `docker_services` lane:** six `Sync postgres role password with vault` tasks reported `changed` in one green run (`ok=326 changed=11 failed=0`) on hosts whose passwords were already correct — an unconditional `ALTER ROLE`, not a compare-first. Take the same-commit-twice measurement as your first step and name every task that still reports `changed` the second time. · [hardware-nas.md](docs/hardware-nas.md) |
| HD-06 | 3 | AI | 1 | **NUT master on nas** — `usbhid-ups`, `upsd` :3493, `nut_exporter` :9199, `upssched-cmd` notify. ✅ **LIVE 2026-09-03 (nas provision, HD-314):** `upsc powerwalker@localhost` returns battery 100% / Innova Unity; `upsd` :3493 + `nut_exporter` :9199 active. ✅ **Client auth fixed 2026-09-08:** `gen_pw` excluded `#` (NUT comment char — a `#` in the password truncated the MONITOR line to 5 args → NUT 2.8.1 refused nas/oldsrv/pi upsmon); passwords rotated `#`-free, POWERDOWNWAIT removed (gone in 2.8.1); all three legs active (`nut-monitor` nas/oldsrv/pi active, `upsc powerwalker@nas.kogler.si` → OL 100%). ✅ **Live battery-pull drill 2026-09-09 found + fixed 3 latent defects** (the guaranteed-notify/shutdown layer was fully non-functional): (1) stray ACL on `upssched-cmd` (`group::r--`) stripped group-execute despite mode 0750 → upssched exec 126, no notify AND no shutdown on ANY host; (2) SMTP creds never rendered — template used shell `${nut_smtp_*}` not Jinja, so the master script carried literal `${...}` and would send empty SMTP auth (**also fixed + `smtp_login` password rotated + propagated 2026-09-09** — nas re-converged, script embeds the NEW value, verified hash match vs vault); (3) nut cannot run `/sbin/shutdown` (`Interactive authentication required` — no sudoers entry). Fixes deployed nas+oldsrv+pi 2026-09-09: ACL cleared + chmod 0750, Jinja-rendered SMTP creds (master only), `/etc/sudoers.d/nut` NOPASSWD poweroff+shutdown, all-poweroff (not halt), charge-threshold policy: **oldsrv ≤75% charge, nas/pi critical-only (LOWBATT)**, restart-upsmon handler for client config reloads, **wake-after-recharge via persistent systemd timer** (nut-wake.timer on nas → WoL at ≥80% recharge; BIOS settings verified on oldsrv Z270 Extreme4). ⚠ **Open defect in this lane, found 2026-09-24:** that ACL task is `changed_when: true` under `tags: always`, so every converge of every tag reports `changed=1` forever — the number that is supposed to mean 'this converge did something you did not ask for' is permanently yellow. Fix: read the real state back (`getfacl`/`stat`) and let `changed_when` decide, instead of asserting change. ⏳ **Remaining (owner):** the short-pull re-test → poweroff + WoL wake end-to-end. (HD-08 — UPS metrics/alerts/dashboard — is CLOSED: live since 2026-09-04 with `nut_exporter` pinned; that drill is the last open thing in the UPS lane and HD-08's row is deleted.) · [hardware-ups.md](docs/hardware-ups.md) |
| HD-207 | 3 | AI + gate | 1 | **Execute the NAS Pool-Creation Runbook + migrated-data final home (landing-zone redistribution)** — ✅ **Runbook EXECUTED 2026-08-23** (SMART pre-check → bulk raidz2 ashift=12 → legacy `new-pool` sent into `bulk/migrate` → tank mirror 2×4 TB → export both before installer boots — installer-ready; deltas: second source pool `backup` declared disposable + destroyed; explicit `bulk/migrate` parent create needed; preseed refreshed same day, stick re-keyed nas-ready). ⏳ **Remaining:** redistribute the landing zone slowly, then destroy it — personal documents → OpenCloud/live Box (HD-151 user-files tier), interim park `/tank/data/users/<name>/` Samba per-user drive, media → `bulk/media/*`; landing zone gone before the tree equals the storage-role SSOT at `playbooks/storage.yml` (final media rename vs personal-files decision per storage.md). · [hardware-nas.md](docs/hardware-nas.md), [deployment-tasks.md](deployment-tasks.md), [storage.md](docs/storage.md) |
| HD-360 | 3 | AI | 2 |  **Enable Samba Authentik-as-LDAP (VPS-side) — split from HD-132 (2026-09-14)** — the deploy-gated remainder of the Samba↔LDAP story, now its own outcome: (1) **declare the Authentik LDAP provider** (`authentik_providers_ldap.ldapprovider`, base DN `DC=home,DC=kogler,DC=si`, bind_mode DIRECT) **+ search-group svc_samba** (+ service user) in the `ks-oidc.yml` Blueprint (IaC), (2) **create the LDAP outpost** + mint a **fresh `authentik-ldap_bind` token** into 1P (the current item is an expired outpost token from 2026-09-01 — `authentik-ldap` unhealthy), (3) redeploy `authentik-ldap` compose (vps.yml), (4) **flip `storage_samba_passdb: ldapsam`** in `host_vars/nas.kogler.si.yml` + converge nas, (5) **live-verify a family drive mounts** with Authentik credentials (the D7 pull model). Gates/context: Samba (nas) reaches the outpost over WG at the VPS's WG-side address — no public publish (HD-186); smbd fails HARD on startup if ldapsam is enabled while the outpost is unreachable (2026-09-14 live hit) — do NOT flip the var before steps 1–3 are live. ⛔ do NOT flip the var before steps 1–3 are live.🔎 **Re-probed live 2026-09-21 (read-only) — TWO blockers, not one:** Authentik holds **no LDAP provider, no LDAP source and no LDAP outpost object** (the only `Outpost` row is the proxy one) while `authentik-ldap` crash-loops `403 Forbidden (Token invalid/expired)`, so minting a token alone yields an outpost serving zero providers. Ordered gates, the `ldapsearch` proof (WG side only) and the read-only sweep that proves it: [deployment-compose.md](docs/deployment-compose.md) §HD-132 and [services-authentik.md](docs/services-authentik.md). ⛔ **Parked at the owner gate:** the token write (the runner's ambient `op` token does have write — measured — but the flip itself is the family-drive change) and the one-shot Blueprint apply (HD-386 class). ⚠ Kept lesson: an expiry-bearing infra token with no rotator decays silently — same class as `grafana_automation_api`. ⚠ **Owed to** `prompt-407.md` (**closed 2026-09-23, brief deleted**): `scripts/ak-shell.sh` reports every failure as "runner key / VPS unreachable" (`2>/dev/null` on the remote command), so an `ImportError` inside `ak shell` looks like an ssh/auth fault. · [deployment-compose.md](docs/deployment-compose.md) §Samba↔Authentik-as-LDAP · [storage.md](docs/storage.md) |

### 2.3 Platform & Deploy — Ansible, compose conventions, GitOps, Renovate, Forgejo Actions

| ID | D | Exec | P | Item |
|----|---|------|---|------|

| HD-454 | 1 | AI + Human | 1 | **The only remote power path for oldsrv is undocumented.** `docs/hardware-oldsrv.md` names the GL.iNet Comet KVM (GL-RM1, PoE from the CRS328, BIOS-level power/reset) but records no address, no network (VLAN 10? 99? tailnet?), no account and no reachability check — so "use the KVM" is not actionable by the next session, or by the owner from a phone. Measured 2026-09-24: WoL from the nas (3 packet shapes × 2 bursts, same L2) drew no L2 response, so the KVM is now the primary path for a box that has been dark since 2026-09-23 21:39:55. Do: record address + network + how to reach it off-site, confirm it can actually power-cycle the box, and decide whether it joins the tailnet/VLAN with an ACL of its own (a device that can hard-power a production host should not sit un-gated on a user VLAN). · [hardware-oldsrv.md](docs/hardware-oldsrv.md) |
| HD-230 | 2 | AI | 1 | **Phase-1 wave-2 corrective batch: pairdrop PUBLIC + blueprint one-shot-apply + onlyoffice cap_add + db-backup boot-race delay + nftables scoped flush + renovate latent-fix + disabled-service teardown** — ✅ Done (IaC, all (a)–(g)); pairdrop/drop live public (services.md). ✅ homelab repo on internal Forgejo (HD-220/264). ⏳ **Remaining (deploy-gate + owner):** surgical converge + verifies; kopia source-wiring decision; HD-360 LDAP enable (split from HD-132, 2026-09-14); HD-211 kopia/grafana password items. · [services-traefik.md](docs/services-traefik.md) · [services-authentik.md](docs/services-authentik.md) · [deployment-renovate.md](docs/deployment-renovate.md) |
| HD-159 | 2 | AI | 2 | **blackbox liveness — home↔VPS link tunnel-down as a first-class alert** — ✅ IaC done (`wg_icmp` scrape job probing `wg_s2s_vps.router_ip` + `wg-s2s-down` Grafana rule). ⏳ live-verify at Phase 1 (tunnel up) that the alert fires on a `wg down` test. · [observability.md](docs/observability.md) · 📋 [`prompt-414.md`](prompt-414.md) ⏳ **2026-09-22 (HD-414 lane): not run — no window was ever stated, and this is the one item in that lane that cannot be measured without one.** ~3 min of home↔VPS link downtime, owner-present: from oldsrv run `while sleep 2; do ping -c1 -W1 100.101.99.1; done` across `/interface wireguard set [find name=wg-s2s] disabled=yes` and back, and confirm the `wg-s2s-down` Grafana rule + alert route actually fire (nothing but UDP 51820 from the VPS may cross while it is down — that is the router-side half of the same test). |
| HD-160 | 2 | AI | 2 | **services-internal sibling auth** — ✅ IaC done + **1P items `immich-ml-internal_api` + `openclaw-opencloud_api` created** (catalog-generated, 2026-09); Immich v3 ML-auth env names deploy-verified. ✅ **`immich-app→immich-ml` round-trip LIVE + VERIFIED 2026-09-08** (**VPS** server health-checks the oldsrv ML endpoint over WG S2S — app log "Machine learning server became healthy", /ping 200; [services-ai.md](docs/services-ai.md) Status). ⏳ **Remaining:** `openclaw onboard`/WebDAV round-trip — rolls into HD-104 (Table AI). · [deployment-compose.md](docs/deployment-compose.md), [security.md](docs/security.md) |

| HD-404 | 2 | AI | 3 | **Strings that outlived the thing they name — IaC comments and provision wording still describing the pre-Victoria / pre-#27 world** — ✅ the six collected items are fixed (2026-09-25): the `home_servers.yml` header + the `storage.yml` header + the `vps.yml` wg-s2s comment now name the **Victoria** backend, `raspberry_pi.yml`'s `technitium-secondary` says the live tier is VPS primary + oldsrv secondary + **Pi tertiary** (HD-332), the `llm-backend` purpose string in `group_vars/all/main.yml` describes LiteLLM ↔ its oldsrv backends instead of "LLM backend (Ollama)" and `docs/network-addresses-generated.md` was **re-rendered** from it, the Pi `first-boot-config.sh` + `scripts/README.md` now say official Raspberry Pi OS Lite 64-bit (the raspi.debian.net rebuild boots this Pi 4 to a rainbow screen), and `traefik-tailnet` states that the `tailnet-apps` overlay's live user is `litellm` (HD-372) with the HD-296 dsh/pi-dev wording labelled as the overlay's origin. Also landed: `scripts/check_iac_backend_strings.py`, a **ratchet** on those names (see its docstring for why a per-line lie-detector and an allowlist were both measured to be worse). ⏳ **Deploy-gated** (CONVENTIONS §4(b)): `roles/amd_rocm` no longer writes `OLLAMA_KEEP_ALIVE` to `/etc/environment` — the task now removes it, and the **container env in `docker_services/ollama` is the real consumer** — but oldsrv has been off the network since 2026-09-23, so the stale line is still on the box until an oldsrv converge runs (**HD-455**). ⏳ A seventh, found while sweeping: `group_vars/vps.yml` still points the HA token at `/etc/prometheus/ha_token` and the gate var is named `prometheus_ha_exporter` — that is a **var + path rename** (and the Alloy `ha_exporter` block reads it), not a comment fix, so it needs its own change with the HD-04 HA-token seeding it waits on. · [deployment-ansible.md](docs/deployment-ansible.md) · [observability.md](docs/observability.md) · [network-dns.md](docs/network-dns.md) · [deployment-preseed.md](docs/deployment-preseed.md)


| HD-445 | 2 | AI | 2 | **The coding cockpit runs on oldsrv with no owning role — two hand-installed daemons under `domen` on a production host, and a port var nobody reads** — state left by the cockpit seeding (HD-409, closed 2026-09-23). `pi-web` is a systemd **user** unit + a `tailnet-bind.conf` drop-in + a 0600 env file + `loginctl enable-linger`, all written out as imperative procedure in the manual (Phase 4c) and rendered by **nobody**: a rebuild of oldsrv loses the cockpit silently, and `cockpit_pi_web_port` / `paseo_port` in `group_vars/all/main.yml` are reservations that no template consumes — which is the honest description, not "the cockpit is configured by the var". ✅ **Half-closed 2026-09-23:** `cockpit_pi_web_port` is now consumed by the `traefik-internal` `pi-oldsrv-backend`, so a port edit is one change instead of a silent disagreement between three files; `paseo_port` stays an unconsumed reservation (HD-411 parked), and the unit/drop-in/env are still hand-installed and owned by nobody — that half is still exactly as this row describes it. ⏳ Decide the home: `docker_services` is **wrong** (these are not containers, and the row that made them native was an owner decision), the closest fit is a host-level role; the unit, drop-in, env-file path and linger all need rendering from the vars. ⏳ Must be `--check`-safe on the first try — that requirement has already cost this fleet two rows (HD-399, and `roles/tailscale-node`'s `tailscale ip -4` under check mode). ⏳ `tmux`: the cockpit-must-not-be-the-only-door principle currently holds only because the **laptop** still has a TUI; `tmux` is absent on oldsrv, and putting it there is a package decision for the owning role, not a hand `apt install` on a production box. · [deployment-manual.md](deployment-manual.md) Phase 4c · [services-ai.md](docs/services-ai.md) §9b-1 |
| HD-446 | 3 | AI | 3 | **`scripts/install-pi-debian.sh` does not exist — a bare-Debian pi installer is the missing sibling, and the trap it must encode is already measured** — the 2026-09-23 lane corrected its own HD-407 row, which had claimed this script as delivered. `scripts/` holds `install-pi-wsl.sh` only. What the sibling must carry (all measured on oldsrv the same night): pi declares `engines.node >= 22.19.0` while Debian 13's `apt` candidate is Node **20.19.2**, so an apt-based install produces a harness that cannot start — the pinned official tarball under `~/.local/share/pi-node/` is the shape both the laptop and oldsrv actually use; then `npm install -g @earendil-works/pi-coding-agent`; then the model contract and provider auth come from `scripts/render-pi-config.py` (**never** a copied laptop file — that is the defect HD-388 existed to kill); and if the web cockpit is wanted, it installs as a **pi package** whose binary exits 1 unless `~/.pi/agent/sessions` already exists. ⛔ Sibling, not fork: the WSL script's `/mnt` and drive-letter assumptions are exactly what make forking tempting and wrong. ⏳ Worth writing before the **third** pi host, not before — one host plus the manual is currently adequate. · [pi-harness.md](docs/pi-harness.md) §1/§4 · [deployment-manual.md](deployment-manual.md) Phase 4c |
| HD-449 | 2 | AI | 2 | **Two loose ends on the oldsrv control node, both parked behind the box (HD-455)** — the decision half is closed: "push by seat, pull by runner" with the repo-scoped `github-homelab-deploy_ssh` deploy key is the owner ruling of 2026-09-25 and lives in [deployment-ansible.md](docs/deployment-ansible.md) §Runner placement. ⏳ AI once oldsrv answers: install the key for the **`domen` seat clone only**, switch that remote to `git@github.com:…` with `IdentitiesOnly` pinned to it, add github.com's host key deliberately, then `git push --dry-run`. ⚠ A deploy key is **read-only unless write was ticked**, so acceptance is the dry-run and never the key's existence, and the **private** half must actually be in the vault item under that exact name/case. ⛔ The write key never goes to the `ansible-admin` runner clone — that is the tree every converge executes and it keeps the read-only HTTPS `github-homelab-deploy_api` `--ff-only` path; two git paths on one box is the decision, not an accident to tidy away. ⏳ **(b)** `restore-runner-key.sh --force-throwaway` left `~/.ssh/id_ed25519.pre-restore-20260922-230051` under `ansible-admin`: confirm nothing in any `authorized_keys` still accepts it, then delete it deliberately — a private key left behind by omission is how the next audit finds something nobody can explain. · [deployment-ansible.md](docs/deployment-ansible.md) §Runner placement · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-440 | 2 | AI | 1 | **Every `docker_services` converge restarts a healthy service: the HD-268c stub-dir guard deletes the rendered file it exists to protect** — minted by the `prompt-407.md` lane session 2026-09-23, found by running HD-386's converge **twice**. `roles/docker_services/tasks/deploy-service.yml:82` pre-removes `{{ docker_compose_dir }}/{{ svc.name }}/<extra>` with `state: absent` before templating, to clear the empty **directory** docker compose auto-creates for a missing bind-mount source — the task's own comment says "pre-remove any such *stub*". But `state: absent` does not care that the path is a regular file: on oldsrv `/opt/lan-litellm/config.yaml` is a regular file (`0644 root:root`), so **every converge deletes it**, `Template extra .j2 files` recreates it (⇒ `changed`), and the `extra_templates_result` guard then restarts the stack "to apply changed extra config". Measured, same commit twice: `--tags docker_services,lan-litellm` → `ok=83 changed=5 failed=0`, and again → `ok=83 changed=5 failed=0`, with `lan-litellm` reading `Up 2 minutes` then `Up 3 minutes`. A live LLM gateway is restarted for no reason on every converge, indefinitely, and no run ever reports a failure. ⏳ **Fix is a type test, not a heuristic:** `stat` the path and remove it only when it is a **directory**, then let the template task's own checksum comparison decide — and prove it the way this was found: two converges from one commit, the second must be `changed=0`. ⏳ Audit the other repeat in the same pass: `nut : Clear stray ACL on upssched-cmd and restore mode 0750` also reported `changed` on both runs, so it is very likely the same class (an unconditional `setfacl`/`mode` fix instead of a converge-gated one). ✅ **FIXED + PROVEN 2026-09-23 (the spark-edge lane, on spark):** the pre-remove is now a `stat` + remove-**only-if-directory** pair, and the `HD-162` compose-validation shell task got `changed_when: false` (it was the whole of the residual churn — a bare `shell` task reports `changed` forever, which is the same reporting class as the stub bug). Proof on spark, same commit twice: `--tags docker_services,spark-dashboard` → `changed=3` then **`changed=0 failed=0`**, no restart handler, `traefik-spark` kept its id and `StartedAt`. ⏳ **Still owed** (⚠ the oldsrv slot was used 2026-09-23 for `--tags monitoring` only — HD-345’s relabel, `failed=0` — so nothing here was re-proven): (a) re-prove the pair on **oldsrv** (`lan-litellm` was the original witness — its `config.yaml` must now survive a converge), (b) audit the second repeat the row named: `nut : Clear stray ACL on upssched-cmd and restore mode 0750` reported `changed` on both runs (same class: an unconditional `setfacl`/`mode` fix instead of a converge-gated one). · [deployment-ansible.md](docs/deployment-ansible.md) §Long converges · [services-ai.md](docs/services-ai.md) §4a |

### 2.4 Services & Edge — Traefik, SSO, service catalog, Matrix, VPS edge

| ID | D | Exec | P | Item |
| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-353 | 2 | AI | 2 | **SeerrNG — ✅ DEPLOYED + LIVE 2026-09-14 (oldsrv converge).** Container Up (internal-only :5055). ⏳ **Remaining (owner):** verify own Jellyfin login at seerrng route. · [services-media.md](docs/services-media.md) |
| HD-354 | 2 | AI | 2 | **Navidrome on VPS + Storage Box (IaC IMPLEMENTED 2026-09-10)** — template `templates/docker_services/navidrome/docker-compose.yml.j2` + `navidrome_version` pin (`deluan/navidrome:0.63.2`, registry-verified 2026-09-10); VPS docker_services entry (subdomain music, INTERNAL-ONLY, library on Storage Box CIFS, app+SQLite on VPS NVMe Kopia-backed); music moved OFF nas (Lidarr hardlink→copy-import); SSO web UI optional/deploy-gated + local logins + `/rest/*` local (Subsonic). ✅ **DEPLOYED + LIVE (verified on the box 2026-09-18):** a later VPS converge completed — `navidrome` is Up, `/opt/navidrome` exists, and the Storage Box library bind is live (`/mnt/storagebox/music` → `/music` ro over the `//u653411.your-storagebox.de/backup` CIFS mount); the scanner ran and finished. ⏳ **Remaining:** the Box library is **empty** (owner: put music on the Storage Box), **no admin user exists yet** (navidrome logs that playlist import stays disabled until one does), and the Subsonic/ExtAuth verify at the pin. · [services-media.md](docs/services-media.md) · [storage.md](docs/storage.md) **✅ OWNER FACT 2026-09-21: there is NO music on local disks (the family listens from the cloud) — the Box-library and admin-user halves of this row are VOID until a real library exists; do NOT mint an admin for an empty library. What is left: the Subsonic/ExtAuth verify at the pin, and a placement decision if a local library ever appears.** |
| HD-357 | 2 | AI | 2 | **Homepage tiles fix (bug #6, 2026-09-10 handoff)** — no working Jellyfin/Seerr tiles on the Homepage; Immich stuck in “Soon” though live. Wire the layout/widgets to the verified live endpoints (new `traefik-internal` home edge + VPS edge paths); verify the tile probe set points at reachable backends now that route tables are final. · [services.md](docs/services.md) accessibility SSOT · [homepage templates](IaC/ansible/templates/homepage_services.yaml.j2) · 📋 [`prompt-357.md`](prompt-357.md) |
| HD-358 | 2 | AI | 2 | **Seerr → Sonarr/Radarr link (bug #7, 2026-09-10 handoff)** — the Seerr↔*arr link is an undocumented post-deploy step. Record the API-key + URL wiring as an owner step in the runbook (deployment-manual.md §media / services-media.md) once the home edge is live; consider automating the API-key hand-off in IaC. · [services-media.md](docs/services-media.md) · 📋 [`prompt-357.md`](prompt-357.md) |
| HD-362 | 2 | AI | 2 | **Music pillar on oldsrv — ✅ DEPLOYED + LIVE 2026-09-14 (converge); crash-loop fixes landed 2026-09-15.** slskd (+gluetun PrivadoVPN sidecar) healthy · aurral Up · lidarr-ydl UP (binds /home/appuser — image has no home on its RO layer → `.profile` EACCES was FATAL, now healthy) · seerrng Up · ~~Tube Archivist~~ **⚠️ DISABLED + TORN DOWN 2026-09-15** (owner: “I don't need it right now” — TA looped on the `path.repo` ES snapshot env-check, driving oldsrv constant CPU + alternating RAM; row `enabled: false`, converge tore the stack down + unit disabled so it won't resurrect on reboot). ⏳ **Remaining:** before re-enabling TA, apply the `path.repo` fix in the ES container's `elasticsearch.yml` directly (env-var and -E forms both destabilize ES bootstrap); Lidarr download client wiring in UI + Navidrome Box refresh (owner); PLACEHOLDER 1P overwrites (slskd_login, soulseek_api, lidarr_api, tube-archivist_login/_ui). · [services-media.md](docs/services-media.md) §Music Pillar |
| HD-366 | 2 | AI | 2 | **DGX Dashboard JupyterLab on the LAN (:11002)** — the dashboard's INTEGRATED JupyterLab assigns per-user ports in `/opt/nvidia/dgx-dashboard-service/jupyterlab_ports.yaml` (admin = :11002); NVIDIA's remote path is a second SSH tunnel per port. The `spark-dashboard` Traefik edge publishes admin's JupyterLab the same way (HD-364 rework: second entrypoint + route → `spark_home_ip:11002` → `127.0.0.1:11002`; add more ports = more entrypoint+route pairs). JupyterLab spawns ON DEMAND from the dashboard's JupyterLab panel (healthcheck intentionally stays on :11000 — a stopped lab would otherwise mark the bridge unhealthy). ✅ **the edge half is already LIVE (measured 2026-09-23):** `:11002` answers **502** with no lab running = Traefik listens on the `jupyter` entrypoint and proxies to a loopback backend with no listener (the lab is on-demand), so the template re-render this row was waiting on has already happened. ⏳ **Remaining (no converge needed):** start a lab from the dashboard's JupyterLab panel → `curl http://spark.kogler.si:11002` → 200 from a LAN client. · [hardware-spark.md](docs/hardware-spark.md) §Remote management · [network-dns.md](docs/network-dns.md) §Per-Instance Split-Horizon |


| HD-141 | 4 | AI + gate | 1 | **(epic) Authentik OIDC provisioning — Blueprint + secret-egress glue** — declares OIDC providers/apps in a Blueprint; a glue copies generated client creds → 1Password `lookup()`. Sub-epic A–E, G done (IaC + VPS-live: ai/vpn Forward-Auth verified, glue mints ephemeral tokens per HD-211); live-verify tail = sub-epic F **HD-147**. · [services-authentik.md](docs/services-authentik.md) |
| HD-147 | 2 | AI + gate | 1 | **Live deploy-verify (OIDC epic tail)** — every verified service and the autopsies live in [deployment-oidc.md](docs/deployment-oidc.md) §Per-service SSO login ledger; do not re-derive them here. ⏳ **Left:** (1) **`claw` + `cloud` desktop/mobile OAuth** (HD-144 CSP, HD-145 JIT-vs-preseed) — the web logins work, the native clients are unwired; (2) **`ai` is broken** → HD-458; (3) **Forgejo's real gap is content, not auth** — nothing has ever been pushed, so “Forgejo primary + GitHub mirror” is a plan rather than a fact (owner call, HD-141 tail), and git transport is undecided while `22/tcp` is exposed but published on no host port (a push needs a PAT, or SSH gets published); (4) `vpn` was verified in the HD-219 era and has never been re-checked — re-prove it rather than carrying the ✅; (5) re-render the service inventory. ⏳ Matrix **inbound federation** is HD-47's outcome and now carries a named blocker there. · [services-vps.md](docs/services-vps.md) · [services-authentik.md](docs/services-authentik.md) · [deployment-oidc.md](docs/deployment-oidc.md) |
| HD-457 | 2 | AI + gate | 1 | **The Immich family library is owned by the break-glass seat, not the SSO seat** (measured 2026-09-25, right after the first SSO login) — `Auto Register` did exactly what [deployment-oidc.md](docs/deployment-oidc.md) §Immich warns it does: the first `domen` OIDC login created a **second** user (`domen@kogler.si`, **0 assets**) while the pre-existing native `admin@kogler.si` seat keeps the only assets (**4**). The OIDC flow is fine (the owner is logged in); the open question is which seat owns the family library. Pick ONE: **(a)** `domen` is the family seat → move/replace the 4 assets there (Immich v3 has no cross-user asset transfer, so: download from `admin`, delete there, re-upload as `domen`); or **(b)** the native admin seat is the family seat → link the OIDC identity to it (`POST /api/oauth/link`, reached from user-settings — that redirect URI exists for this), then measure which user the login resolves to and delete the empty `domen` user only once it resolves to the linked one. ⚠ Do it BEFORE the phones start uploading — the asset count is the entire reason this is cheap today. Either way re-check role + quota afterwards: `immich_role`/`immich_quota` are creation-only and never re-synced, and the break-glass password seat must stay reachable (`passwordLogin` is deliberately still true). ⏳ **Owner decides (a) vs (b); AI executes once decided.** · [deployment-oidc.md](docs/deployment-oidc.md) §Immich · [services-media.md](docs/services-media.md) |
| HD-458 | 2 | AI | 1 | **`ai.kogler.si` SSO dies on the return leg — the callback path moved and our redirect URI still says the old one on BOTH sides of the wire.** Root cause, the file:line reading of the deployed 0.11 build and the fix procedure are in [deployment-oidc.md](docs/deployment-oidc.md) §Open WebUI native-OIDC note; what is left is the execution: change `OPENID_REDIRECT_URI` (compose) **and** `provider_openwebui.redirect_uris` (`ks-oidc.yml`) to `/oauth/oidc/callback`, recreate OWUI, re-apply `playbooks/authentik-blueprints.yml`, then verify `grant_types`/`property_mappings` survived the upsert (HD-231 — pinned, but check). ⚠ Acceptance is **a login that lands logged in**, not a 200 on the callback. · [deployment-oidc.md](docs/deployment-oidc.md) · [services-ai.md](docs/services-ai.md) |
| HD-459 | 2 | AI + Human | 1 | **OpenCloud native Android app — ✅ LIVE + owner-verified on device 2026-09-25** (`domen` logs in through sso and sees the file list; the check that actually counted was server-side: a `RefreshToken` row whose scope still carries `offline_access`). Two server-side causes, both durable, both in [services-authentik.md](docs/services-authentik.md) facts 7 + 9: native clients **discover** their client_id from WebFinger and Authentik binds one client_id to one issuer ⇒ all platforms share the ONE `web` client (`WEBFINGER_{ANDROID,IOS,DESKTOP}_OIDC_CLIENT_ID` + the `oc://android.opencloud.eu` redirect); and a refresh token exists only if `offline_access` is a property mapping ON the provider. Verification row: [deployment-oidc.md](docs/deployment-oidc.md) §SSO login verification matrix; family steps: [docs/manual/opencloud.md](docs/manual/opencloud.md). ⏳ **Owed:** ⚠ **Nothing left on the phone:** doc-open was exercised by the owner 2026-09-25 and really does hand a browser tab to the WOPI editor; photo backup is NOT owed — Immich is the photo SSOT and the owner proved that path the same day (picture + video uploaded from the phone to `foto` as `domen`, [deployment-oidc.md](docs/deployment-oidc.md) matrix); ⏳ desktop/iOS inherit the shared id but each owes its own scheme-callback redirect entry at onboarding (HD-52 desktop leg); ⏳ a device whose app predates webfinger client-id discovery (first release carrying it: v1.2.5, 2026-06-27) still arrives as `OpenCloudAndroid` → flip the SHARED id (`WEBFINGER_WEB_OIDC_CLIENT_ID` + ORM-set provider `client_id`), never add a provider. · [services-authentik.md](docs/services-authentik.md) · [services-office.md](docs/services-office.md) |

| HD-122 | 2 | AI | 2 | **Matrix federation default (open + hardening)** — ✅ decided + IaC (profile/auth + room-dir hardening; trusted_servers = key-notary not permit-list). ✅ **profile auth live-verified 2026-09-25** (the wait this row carried for years is over): unauthenticated `GET /_matrix/client/v3/profile/%40domen%3Akogler.si` → **401 `M_MISSING_TOKEN`**, and `user_directory/standard/query` → 404 `M_UNRECOGNIZED` (the query form is not exposed at all). ⏳ **One item left, and it is HD-47's outcome, not new work:** inbound federation, now blocked on a named defect (the apex `/.well-known/matrix/*` is behind Forward-Auth). Verify with an external-room join, not with our own two servers. · [services-matrix.md](docs/services-matrix.md), [security.md](docs/security.md) |
| HD-47 | 2 | AI | 3 | **Matrix public records + Federation** — publish `matrix`/`chat` (Cloudflare DNS-only) + `_matrix` well-known/SRV delegation; WAN 443 (8448 optional); no Forward-Auth on `/_matrix/*`. ✅ **Records are published** (measured from public DoH 2026-09-25: `matrix.kogler.si` and `kogler.si` both → `159.195.111.66`); `matrix.kogler.si/.well-known/matrix/{client,server}` → **200 + correct JSON**; `/_matrix/federation/v1/version` → 200 (Tuwunel 1.9.0). ⛔ **The named blocker, found while measuring SSO 2026-09-25:** the **apex** well-knowns — `https://kogler.si/.well-known/matrix/client` and `.../server` — answer **302 → `sso.kogler.si`**. A remote homeserver resolves the apex, because `server_name = kogler.si`, so it gets a login redirect instead of the delegation JSON. Fix = a public (no-Forward-Auth) route for `/.well-known/matrix/*` on the apex, the same rule class this row already states for `/_matrix/*`; there is no `_matrix._tcp`/`_matrix._https` SRV anywhere (DoH: NXDOMAIN), so the well-known path is the only delegation we have. ⏳ Then **prove inbound from an external server** (an external-room join is the test) — nothing about federation is proven until that happens. · [services-matrix.md](docs/services-matrix.md), [services-traefik.md](docs/services-traefik.md), [network-dns.md](docs/network-dns.md) |
| HD-112 | 4 | AI | 2 | **Zipline public bin + private OIDC file share** — Stage 9/10; ✅ decided + IaC done (v4.7.0 pin, `bin.kogler.si` on `crowdsec-only@file`, native-OIDC `provider_zipline` + glue-seeded `zipline_oidc`, guestbin quota + 6h TTL dropzone, DB05). ✅ **DEPLOYED (verified on the box 2026-09-18):** `zipline` + `zipline-db` are Up/healthy on the VPS — the human-gated first deploy has happened, so this row is post-up work only. ⏳ **Post-up seeding:** then local admin → OIDC login verify → flip bypass-local-login → create `guestbin` user + `dropzone` folder; anonymous upload→short-URL→viewer round-trip + 6h sweep verify; family drop script (capability secret → 1Password); family guide entry. · [services-utilities.md](docs/services-utilities.md) · [storage.md](docs/storage.md) |
| HD-288 | 1 | AI + Human | 3 | **`sunshine` deployment tail (Phase 3 oldsrv)** — enable decision resolved in IaC (`enabled: "{{ homelab_mode == 'desktop' }}"`, home_servers.yml; sunshine = oldsrv GPU tier per HD-135 split). ✅ **security.md §3 reconciled 2026-09-08** — the stale "restrict 47989-48010 to Home VLAN IP" note now matches the in-template HD-62 decision: host ports kept on all interfaces BY DESIGN (loopback/drop would break Moonlight streaming); exposure time-limited (`restart: "no"` manual-start gaming timer); narrower binding = LAN/Headscale interface if wanted. ⏳ **Remaining (owner/Phase-3):** live-verify at oldsrv Phase-3 bring-up (manual-start gaming timer + Moonlight round-trip from LAN/VPN). · [home_servers.yml](IaC/ansible/group_vars/home_servers.yml) · [sunshine/docker-compose.yml.j2](IaC/ansible/templates/docker_services/sunshine/docker-compose.yml.j2) · [services-admin.md](docs/services-admin.md) · [hardware-oldsrv.md](docs/hardware-oldsrv.md) · [security.md](docs/security.md) §3 |


| HD-412 | 2 | AI + gate | 2 | **RustDesk — server on the VPS for family support, direct-IP over the tailnet for our own machines (Stage: 9/10)** — 🟢 **LIVE on the VPS 2026-09-21**: `rustdesk_login` seeded (44 / 88 base64 chars, length-verified) → `enabled: true` → converged `--tags hardening,docker_services,rustdesk-server` (green) → verified `Up (healthy)`, hbbs on 21115 + 21116 tcp/udp, hbbr on 21117, `id_ed25519` + `.pub` at 0600 in a 0700 `/data`, hbbr logging its **effective** caps `48/24/8 Mb/s` + `0.66` / `1800s`, and from the public internet **21117 OPEN / 21118 REFUSED**. Owner confirmed the caps the same day. Design, exposure/auth decision, consent posture, bandwidth arithmetic and both enrolment paths: [services-admin.md](docs/services-admin.md) §RustDesk; procedure: [deployment-manual.md](deployment-manual.md) §1.11. ⏳ **Remaining (needs a human at both ends):** enrol one Path-B family machine (read the Key with `op read op://Homelab-ansible/rustdesk_login/username`) and run **one relay session** (phone on mobile data) plus **one Path-A tailnet direct-IP session with the relay provably uninvolved** (hbbr's loopback `ls` console on 127.0.0.1:21117 stays empty while it runs). ⏳ **Owed, named (does not block):** the blackbox `tcp_connect` probe of 21116/21117 lives in `roles/monitoring/**` = [`prompt-420.md`](prompt-420.md). · [services-admin.md](docs/services-admin.md) · [security.md](docs/security.md) |
| HD-447 | 3 | AI + Human | 3 | **Finish Metabase's retirement — the registry switch and the egress glue are done; the live Authentik objects, the leftover dir and the vault item are not** (owner 2026-09-23: retire it, keep the IaC; owner 2026-09-25: delete the live objects — ruled once, in [deployment-secrets.md](docs/deployment-secrets.md) §Item titles are load-bearing). ⏳ AI: delete `provider_metabase` + `app_metabase` and the `edge-sec{,_ts}` providers/apps + their `outpost_embedded` entries through the Authentik API / `ak shell`, then the `/opt/metabase` leftovers on the VPS. ⚠ Acceptance is **proving the blueprint does not re-mint them while `enabled: false`**: `blueprints/*.yml` are `copy:`d unrendered and `!KeyOf` resolves per file while `outpost_embedded` is one shared object, so a dangling reference fails the whole import — an **IdP** outage, not a Metabase one, and the reason commenting them out was never the fix. ⏳ Owner, one line: delete or keep the dormant `metabase_oidc` vault item. ⏳ Worth doing once, and general: give the blueprint files a render path (or a per-service blueprint-file convention whose shared-object references cannot dangle) so `enabled:` reaches cross-service artifacts at the *next* retirement instead of this being re-litigated. · [deployment-secrets.md](docs/deployment-secrets.md) §Item titles are load-bearing · [group_vars/vps.yml](../IaC/ansible/group_vars/vps.yml) `metabase` entry |

### 2.5 Interfaces & Dashboards — Homepage launchpad + Grafana boards

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-316 | 3 | AI + gate | 1 | **Homepage family launchpad — real app dashboard (`home.kogler.si`) + Domen technical section (Grafana stays separate)** — ✅ **IaC + deployed live 2026-09-03** (structured Family/technical/Soon groups, siteMonitor on public FQDNs only; widgets: VPS resources + Site Monitor; `homepage_show_technical`/`homepage_show_soon` gates; `https://home.kogler.si/` → 302 Forward-Auth). ⏳ **Owner verify:** family sees live apps green; Domen-only technical section renders. · [services.md](docs/services.md), [interfaces.md](docs/interfaces.md) |
| HD-315 | 2 | AI | 1 | **Author Grafana technical dashboards (v1) — make the live Prometheus data usable** — ✅ **Done (IaC, 2026-09-04).** Five new provisioned dashboards under `IaC/ansible/roles/monitoring/files/dashboards/` (valid JSON, uid unique, datasource uid `prometheus` — the API-seeded Prometheus datasource): `homelab-host-overview` (node resources: CPU/mem/load/disk//network/uptime on `job="alloy"`, multi-instance template var), `homelab-service-reachability` (blackbox probe tables HTTP+ICMP, latency, TLS cert expiry), `homelab-wan-tunnel` (wg_icmp liveness, Traefik requests, CrowdSec decisions, stack up), `homelab-selfmonitoring` (up{} per component incl. alloy), `homelab-overview` (entry point: down-counts + VPS CPU/mem + links). `docs/observability.md` got a **Dashboards** subsection (table + metric-name gap notes). ✅ **DEPLOYED + GRAFANA FILE-PROVIDER LIVE 2026-09-03:** VPS `monitoring` converge copied them into `/srv/docker/grafana/provisioning/dashboards/` (6 JSON incl. ups; file-provider hot-reload, no restart). ✅ **Traefik metrics LIVE (2026-09-04):** main-edge compose enables `--metrics.prometheus=true` + `--entryPoints.metrics.address=:8082` + joins `db-internal` so Prometheus resolves `traefik:8082`; dashboards use the v3 name `traefik_entrypoint_requests_total`; live-verified `up{job="traefik"}=1` + series flowing. ✅ **Render round-2 (2026-09-08, committed `e30159f` + deployed live to `/srv/docker/grafana/provisioning/dashboards/`):** CrowdSec WAN panels fixed `crowdsec_decisions`→**`cs_active_decisions`** (real metric, labels `origin`/`action`/`reason`; live-verified 10 series) + UPS got **Load/Runtime/Battery-charge over-time** timeseries panels. ⚠ **Deferred to VictoriaMetrics migration (HD-341/342) — RESOLVED 2026-09-08 (HD-346 closeout):** the earlier "Host Overview still empty / connection refused / ZERO node_*" deferral is fixed — Alloy's `unix` host exporter is wired + all 4 hosts (vps/oldsrv/nas/pi) now ship node_* to VM; the blackbox probe family (Service Reachability) also fixed (container egress + IPv4 + target pattern). ⏳ **Remaining (owner):** render-verify panels with data (CrowdSec/ups/network-clients verified now; host overview + probe tables have data). · [observability.md](docs/observability.md) §Dashboards · [IaC/ansible/roles/monitoring/files/dashboards/](IaC/ansible/roles/monitoring/files/dashboards/) |
| HD-343 | 2 | AI | 2 | **Grafana “Network Clients” dashboard (`stats.kogler.si`) — all clients grouped per VLAN — DESIGN SSOT + EXPORTER + DASHBOARD AUTHORED 2026-09-07/08; ✅ EXPORTER LIVE 2026-09-08 (oldsrv converge).** Design SSOT in [observability.md](docs/observability.md) §Network Clients Dashboard (5-source join: DHCP leases / ARP / bridge FDB / wifi-reg / SSOT `network_static_hosts`, per-source VLAN classifiers; VPS→router direct + Loki-derived + SNMP-primary all rejected). `network-clients-exporter.py.j2` host binary + systemd unit + Alloy scrape + `homelab-network-clients.json` dashboard in `IaC/ansible/roles/monitoring/`. **LIVE (2026-09-08):** exporter runs as `networkclients` (0750 root:networkclients — 0700 root broke the unprivileged unit), pinned `routeros-api==0.21.0` in a dedicated venv (trixie has no `python3-routeros-api`), fixed `ROUTER_TLS` lowercase-`false` NameError; unit active + `/metrics` serves real `mikrotik_client` union (DHCP/ARP/FDB/wifi from the RB4011 logpipe user). ⏳ **Remaining (owner/verify steps):** verify live wifi path (`/interface/wifi/registration-table` vs legacy); render-verify panels owner step; dashboard visible on `stats.kogler.si` after the VPS Grafana provisioning converge (VPS-side, post-Victoria — in-lane with HD-341/342). · [observability.md](docs/observability.md) §Network Clients Dashboard · [monitoring role](IaC/ansible/roles/monitoring/) |
| HD-450 | 2 | AI | 2 | **A cert consumer can age out silently: nothing watches the pair's age at a consumer, and a failing systemd timer reaches no alert** — found closing HD-350: oldsrv's `traefik-cert-pull.timer` had been failing every 15 min for 4 days (2026-09-19 → 23) with the wildcard pair sitting on a 26-day-old copy, and the event appears in NO series Grafana can see — systemd unit results are not scraped at all, and the cert pair a consumer holds is not exported anywhere, so the failure is invisible until TLS actually expires (this pair expires 2026-11-20). ⏳ **Next:** instrument one of the two, then alert on it — (a) the last-result of the pull units (`traefik-cert-pull` on oldsrv + spark, `ha-cert-sync` on the Pi) as a series with an alert on any non-success result, or (b) a per-consumer cert-age gauge with WARN 30 d / CRIT 14 d, emitted for every consumer edge (`traefik-internal`, Pi `traefik-ha`, `spark-dashboard`, VPS `traefik-tailnet`). **Acceptance = the alert actually fires end-to-end** (stop one consumer's timer, or back-date its pair, and see the message in the Signal alert group — HD-347's router is the leg that must exist first). ⛔ Do not mute the class by tolerating a non-zero rc in the pull script: rc 90 is the HD-450 self-pull guard and must stay loud. · [observability.md](docs/observability.md) §Alerting · [services-traefik.md](docs/services-traefik.md) §Certification |

### 2.6 Smart Home — HA primary/standby, Homematic, voice, devices

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-04 | 5 | AI + gate | 1 | **Pi redo: HAOS → Debian + HA Container + Technitium secondary — ✅ DONE + LIVE 2026-09-03** (Phase 4: HA 2026.8.1 on `pi.kogler.si`, keepalived MASTER on `ha-vip`, traefik-ha, Technitium secondary — as-built runbook: [deployment-pi-provision.md](docs/deployment-pi-provision.md)). ⏳ **Remaining: the owner failover test — drill EXECUTED LIVE 2026-09-09, forward takeover PROVEN** (VIP Pi→oldsrv, standby HA up at VIP:8123 after the 8123-publish fix `a3a11c0`, Shelly control verified; record + gaps in [smart-home-failover.md](docs/smart-home-failover.md) §Drill verification record). ⏳ **Two tails:** (a) **KNX on standby broken** — GIRA (`knx-ip` VLAN 20) drops all KNXnet/IP from oldsrv (device-side: tunnel-client allowlist or power-cycle; owner step); (b) **restore oldsrv standby keepalived conf (BACKUP 100)** after the drill's failover (120) variant — do before the next standby cold boot; (c) **`ha.kogler.si` HTTPS on Pi-down has no edge until HD-350/351** (HD-349 re-decision tracks it). Homematic-local deferred: HmIP-HAP stays cloud mode, no HmIP-RFUSB (HD-13 parked/rejected HD-18). · [home-assistant-current.md](docs/home-assistant-current.md) |
| HD-434 | 3 | AI + owner | 2 | **KNX on the HA standby is still unproven — and its only published cause has been withdrawn.** The standby's KNX failure was attributed to the GIRA router; that inference was built on probes that were never going to be answered (the integration authenticates with a `user_id`, so unauthenticated `SEARCH`/`CONNECT` from ANY host, including the healthy primary, returns nothing). The real bug found tonight was ours: `trusted-ha` omitted the HA primary host (**HD-438**, fixed). oldsrv has always been in that list, so it does not explain the standby. **Remaining work: one owner-present takeover drill retest**, now that the primary is a known-good KNX reference — start the standby, check `hass` comes up with a live KNX tunnel (an entity that changes when the bus does), and if it fails, read `xknx` lines for authorization vs no-channel vs no-route. Do not infer from raw UDP probes again. · [docs/smart-home-failover.md](docs/smart-home-failover.md) Gap 1 |
| HD-438 | 3 | AI + owner | 2 | **HA's KNX has been dead since 2026-09-20 10:16; the firewall hole is closed but the service is not proven back.** Cause found: Home→IoT (VLAN 20) is gated on `src-address-list=trusted-ha`, which held `oldsrv`+`ha-vip` and **not the Pi** — HA tunnels from its own address (`local_ip: null`), so the primary's KNXnet/IP was dropped. Fixed live 2026-09-23 00:00:40 with a widening-only delta folded into `rb4011_converge.rsc.j2`; reachability verified (ICMP up, `tcp/3671` open, accept counter advancing). **Open, in priority order:** (a) end-to-end KNX unconfirmed — the telegram store's newest row is 2026-09-20 10:16:51 and xknx timeouts continued after the fix (53 in 8 min); settle it with a positive instrument (an entity changing in the HA UI, or the HA API), never with log absence again; (b) explain the 09-20 10:16 boundary — the gate entered the template 2026-09-10 (`4f0604d`), the live rows date 2026-09-15, HA has been up since 09-07, so what took effect on 09-20 is unexplained; (c) 19 of 154 group addresses answer nothing (`Kabinet Zaluzije Z6` family) — possible offline device, separate defect; (d) HD-436 validator: derive `trusted-ha` from the hosts that actually run HA. **Owner asked for:** one glance at a KNX light/shutter entity in HA, and which automations keyed off KNX state since 09-20. · [docs/network-vlans.md](docs/network-vlans.md) §Firewall policy, [docs/smart-home-failover.md](docs/smart-home-failover.md) Gap 1 |
| HD-439 | 3 | AI | 2 | **KNX leftovers from HD-438 — (a) closed by measurement, and the premise was wrong; (b) still real.** ✅ **(a) 2026-09-25:** the row claimed the generator emits ~36 `state_address` values that "never answer on the bus". Measured against the ETS project (`docs/assets/references/knx/StanovanjeKogler_v1_0.knxproj`): it publishes **246** group addresses, the render emits **154**, and **0 of the 154 are outside the project** — including all **19** hand-typed sensor-appendix addresses, which were the only place an address could be invented. The committed artifact is byte-identical to a fresh render, and the file deployed on the Pi is md5-identical to the artifact (in place since 2026-09-07), so there was nothing to delete. The ~36 figure describes neither the file nor the generator today and is **not repeated as fact anywhere**; what was missing was the thing that would have made the claim checkable, so that shipped instead: `scripts/knx-hass-gen.py --check` re-renders and refuses to pass if any emitted address is not published in the project (canary: retyping one appendix address to `12/1/99` turns it red and names it as hand-typed), and `--self-test` — the stdlib-only half — runs in `validate-all.sh` because the runner has no `xknxproject` and **no file in this repo declares that dependency** (recorded in `scripts/README.md`, not quietly worked around); `--check` itself is therefore a manual pre-commit step and cannot be wired while the dependency is undeclared and no CI runner exists (HD-396). ⏳ **(b) the detection half, unchanged and still the actual risk:** the interface that stopped forwarding for three days while HA kept reporting everything OK is still undetected. It cannot be wired yet for a measured reason — HA's Prometheus-exporter scrape is switched off (`prometheus_ha_exporter: false`, `group_vars/vps.yml`) until the HA long-lived token exists (**HD-04**), so an alert today would be green-by-absence. Exact action when HD-04 lands: enable the exporter job, then alert on growth of the KNX interface telegram counters, with the 2026-09-20 half-open interface as the incident it must catch. ⏳ **Live acceptance:** a HA reload with eyes on Settings → Devices to confirm no entity answers `did not respond` — an owner-at-the-keyboard step on a live smart home, deliberately not taken unattended. · [smart-home.md](docs/smart-home.md) §KNX address audit · [scripts/README.md](scripts/README.md) |
| HD-435 | 3 | AI + owner | 2 | **HA's tailnet answer must survive either home box — the Pi joins the tailnet and `ha.kogler.si` gets two A records.** Decided 2026-09-22 with the owner; today the tailnet's answer for HA is oldsrv's node alone (`websecure-ts` exists only there), so if the box that happens to be a tailnet node dies, the house's most critical service is unreachable from away even though it is up — proven the other way at the 2026-09-22 drill, where the standby answered `VIP:8123` but no tailnet listener existed to reach it. Steps: join the Pi to tailnet (new node, own address + tag); bind `traefik-ha`'s tailnet listener; publish `ha.kogler.si` with **both** node addresses; ACL grant scoped to the new node address (owner authorizes the widening). **Acceptance must prove the unproven part, not assume it:** MagicDNS returns both A records, and with one box powered off a real client still reaches HA (expect a timeout before it falls to the second answer — measure the delay, do not assert instant failover). **✅ Owner approved 2026-09-25** — the new node and the ACL grant are authorised, so the row is AI work now, with two honest limits written into it. ⏳ AI: the boundary now permits **two** home nodes (`oldsrv` + `pi`), both with no advertised routes and no LAN bridge; the Pi's grant is **`tcp/443` to its own node address from the tailnet only**, and it must **not** inherit the HD-415 `udp 53` rule (that rule is address-scoped precisely so a second `tag:dev` node cannot pick up a resolver grant — and the Pi already runs Technitium tertiary). ⏳ Order: strengthen `scripts/guarded-converge.sh`'s post-converge liveness probe **before** touching the extra-record settings (HD-436 step 1 — the two settings are mutually exclusive in the pinned headscale and a crash-looping container still reports `Running`). ⏳ **Acceptance splits into what a remote session can prove and what it cannot:** MagicDNS returning both A records + HA answering on each node address = measurable from away; **the fall-over itself needs one box powered off** and stays an owner-present leg (expect a timeout before the second answer — measure the delay). Stopping the standby's listener is a weaker proxy, and must be labelled as one, not recorded as the drill. · [docs/network-vpn.md](docs/network-vpn.md) §Tailnet boundary, [docs/smart-home-failover.md](docs/smart-home-failover.md) |
| HD-436 | 3 | AI + owner | 3 | **`zone_kogler_si` — one derived list for the whole namespace, replacing four separately-maintained sources.** Today the zone lives in the Technitium seed's inline record list, headscale's `tailnet_subdomains` + `tailnet_ts_only_subdomains`, the public Cloudflare set, and an unversioned hosts file on a workstation. Derive from what already exists — the `docker_services` entries carrying `name`/`subdomain`/`public`/`enabled` — adding `internal:`, `tailnet: none\|edge\|node\|dual`, `ts_router:`; render the seed, the watched `extra_records_path` JSON, the public set and the tailnet listener's router list; ship a **generated** workstation alias file so no hosts line is ever typed (owner rule: generate or don't). Validators are the point: tailnet-only names must not appear in the LAN-facing view (the `stats`/`logs`/`csui` wart), an enabled service must have a name somewhere (`music` has none), a disabled one must not keep one (`sec` still resolves), and `ts_router` must match its `tailnet` flag. Order of execution matters: strengthen guarded-converge's liveness probe before the watched-file swap, since a bad render there crash-loops the control plane and `docker … running` reports true for a container in a crash loop. Measured drift table + decisions: · [docs/network-dns.md](docs/network-dns.md) §The answer-plane model |

| HD-14 | 2 | AI | 2 | **Export HA entity list** — enable HA Prometheus exporter; needed for HA Dashboard `lovelace` + Grafana. Wait for observability. · [smart-home.md](docs/smart-home.md) |
| HD-17 | 3 | AI | 2 | **Single failover button + `ha-failover.sh`** — ✅ IaC done + committed; `ha-failover-api` **active** on oldsrv (2026-09-08 converge). ⏳ deploy: render the Homepage failover button (gate `homepage_failover_button`; RFUSB path is obsolete — HD-18 rejected the stick; the active path is the **IP-only** button: VIP move + standby start). · [smart-home-failover.md](docs/smart-home-failover.md) · 📋 [`prompt-357.md`](prompt-357.md) |
| HD-217 | 1 | AI | 2 | **Homepage failover-button gating (`homepage_failover_button`)** — ✅ **Done (IaC; owner sign-off 2026-08-22).** ⏳ Deploy-gated: next green vps.yml run renders homepage with the gate off. · [smart-home-failover.md](docs/smart-home-failover.md) · 📋 [`prompt-357.md`](prompt-357.md) |

| HD-319 | 3 | AI | 1 | **HA prep: KNX render-from-.knxproj + modernized Stanovanje dashboard** — ✅ IaC done + **live (2026-09-03)**: `knx-entities.yaml` generated from the ETS .knxproj (192 GAs) + `.knxproj` deployed for panel import; KNX integration set up in HA (UDP tunneling to GIRA IP router, entities load); dashboard modernized (`lovelace-stanovanje` sections/tiles). ⏳ **Remaining (owner/verify):** confirm the 3 rekuperator GAs (12/1/*) answer on the bus (some GroupValueRead warnings). · [smart-home.md](docs/smart-home.md) · [home-assistant-current.md](docs/home-assistant-current.md) |

### 2.7 AI & Office — LLM stack, Open WebUI, office MCP, vector DB

| ID | D | Exec | P | Item |
| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-359 | 2 | AI | 1 | **spark engine-neutral benchmark plan + deploy kit (Qwen3.8-Flash-Next on GB10; research COMPLETE, IaC MOSTLY DONE, bench PENDING)** — engine **not pre-assigned**: vLLM ↔ SGLang benched apples-to-apples via [spark/BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md) (S1–S5 profiles, bench matrix, engine tuning ladder, post-bench SSOT promotion §10). R1–R6 research DONE: [spark/resources/RESEARCH-VERDICTS.md](spark/resources/RESEARCH-VERDICTS.md) (bf16 dropped; NVFP4 tables exist; 262k native/524k YaRN ok; TRT-LLM = future row). ✅ **IaC FOLDED IN + CONFIG AUTHORED 2026-09-14:** first-class IaC host (`inventory` `spark` group, `host_vars/spark.kogler.si.yml` SSOT-derived statics, `roles/spark` = NVMe p2→XFS with 1TB headroom assert + models/overlays on XFS, `group_vars/spark.yml` = registry + all `spark_vllm_*` render vars) **+ dockerized proven config** (`templates/docker_services/spark-ai/` = B1 AWQ W4A16 + PLE INT4 + MTP3 + llm-d; `spark_vllm_image` digest-pinned; `spark-internal` net). ✅ **Artifact store role (HD-359) 2026-09-14:** `roles/spark-artifacts/` = general, config-agnostic download layer (manifest `spark_artifacts:` — B1 weights + PLE overlays + INT4 tables → XFS; idempotent per repo, `hf` CLI + `hf_transfer`, runnable via `--tags spark-artifacts`). **STAGED LIVE on the box 2026-09-14** (weights 169G + overlays 3×.py + tables 30G/129 files; 296G free; 4 live-run defects fixed — hf CLI swap, XFS child-dir perms + HF_HOME, .cache, tables-flatten, compose overlay paths). Remaining: bench S1–S5 on the live box → flip `spark-ai.enabled: true`. ✅ **First boot DONE 2026-09-14:** DGX OS wizard complete (EN / Europe/Ljubljana, admin → 1P `spark_login`, analytics off), updates + reboot; post-update L2 blackout root-caused to headless auto-suspend → **all 4 sleep targets masked** (power-cycle recovered; runbook `deployment-manual.md` §Phase 5); GPU verified GB10 / driver 580.173.02 / CUDA 13.0 / 121 Gi; MAC `38:A7:46:78:13:97` learned → `network_static_hosts` VLAN-10 row. ⏳ **Deploy-gated (remaining):** router static-lease flip to the reserved spark Home static, first-contact key/user (common role), `spark_partition_enable: true`, converge → **`spark-artifacts` downloads (~155 GB)** → flip `spark-ai.enabled: true` → vLLM+router up. ⏳ **Bench-prep (after live):** engine-neutral harness (SGLang metrics/CLI param), A-cal, then the §4 bench matrix; promote winner per profile + flip `spark.enabled`. · [hardware-spark.md](docs/hardware-spark.md) §Benchmark/engine selection + §Bring-up · [BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md) · [RESEARCH-VERDICTS.md](spark/resources/RESEARCH-VERDICTS.md) · 📋 [`prompt-376.md`](prompt-376.md) |
| HD-367 | 2 | AI | 1 | **spark mgmt-leg dual-home on the SAME NIC (VLAN-99 tagged) + llm-d REMOVED + S1 bench (IaC DONE + boot DONE; bench NOT yet certified — flip committed on session branch, NOT on main)** — ✅ **mgmt-leg IaC DONE + LIVE:** single enP7s7 carries Home untagged + Mgmt VLAN-99 tagged (same MAC, both SSOT rows; the "mgmt NIC not cabled" note is obsolete — no second NIC). `roles/network` NM dual-home generalized (`network_nm_dualhome_hosts` = pi+spark, `nm_parent_iface`; spark parent = DHCP via the router static-lease `dhcp-client-id=mac` + tagged `spark-mgmt` mgmt IP/24 + route1 + never-default; `nmcli connection reload` + explicit `load` before `up` — fresh-keyfile live hit `unknown connection`). Converge failed=0 → **mgmt leg UP, ping to the router mgmt IP 0.5ms OK**, default route stays Home. Router converge render proves the `dhcp-mgmt` spark static — apply = standard `render-converge.yml`→/import. ✅ **llm-d REMOVED (owner):** the real llm-d is a Kubernetes inference-pool stack; `ghcr.io/llm-d/router:latest` was never public (GHCR 401). LiteLLM (existing) fronts vLLM :8000; `spark-ai` compose = vLLM engine only (`llm-backend`), `spark-internal` net + `spark_llm_router_*` vars removed, `spark-ai` dropped from validator ALLOWED_LATEST. ✅ **S1 bench BLOCKERS fixed (2026-09-14 close-out + 2026-09-14 TRACK-1 session):** (a) numactl `--membind=0` → `get_mempolicy: Operation not permitted` under the hardened container → entrypoint now plain `python3 -m vllm...`; (b) torchinductor `OSError [Errno 30]` → compose now mounts **the WHOLE `/root/.cache`** (`{{ spark_xfs_mount }}/vllm_cache:/root/.cache`); (c) **first real boot surfaced + fixed 4 more (commit 66873c2, branch `session/spark-s1-converge-20260914-2322`, NOT merged — validate-all green):** `--swap-space` removed in this vLLM build (`0.1.dev20073+g8e685d198`) → dropped; `--quantization awq` mismatched weights' `compressed-tensors` quant_method → dropped (vLLM reads packed format); `disable_by_batch_size` dropped from SpeculativeConfig → B1 = plain MTP-3; `gpu_memory_utilization` 0.95→0.93 (DGX dashboard co-resident, free 114.76<115.54 GiB). **Boot LIVE at close-out:** engine passed init, **PLE offload table load ~33% (2/6 shards)**. ⏳ **Deploy-gated (next session):** confirm :8000 health (was loading at close) → warmup → accuracy gate → C1/C2/C3 sanity ×2 + warm → record in [BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md) §9 → **merge session branch (certifies `spark-ai.enabled: true`)** → verify iofficiency; then full ladder = later. ✅ **Dashboard tails CLOSED 2026-09-15:** `dgx-dashboard-socat` removed; `spark-dashboard` converged (lands corrected healthcheck `traefik healthcheck --ping`, no `--api.insecure`); `traefik-spark` **healthy**; `http://spark.kogler.si:11000` → 200 (HD-364 close-out). Pi DNS verified resolve: `spark.kogler.si → spark Home-VLAN IP` (tertiary OK). · [hardware-spark.md](docs/hardware-spark.md) §Benchmark/engine selection · [services-ai.md](docs/services-ai.md) · [BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md)  ✚ **(2026-09-15 S1 bench session):** B1-on-GB10 memory-fit fix COMMITTED + LIVE on session branch `session/spark-b1-memfit-20260915-0050`: gpu_memory_utilization 0.93→0.70 (unified 121.62 GiB pool; 0.93 demanded ~113 GiB GPU + ~57 GiB CPU PLE mirror ≈ 170 GiB → guaranteed global OOM at load), memory cage 126G→105G (relapse kills container only), max_model_len 173400→65536 (64k, KV pool ~112k tok fits). **Engine stable LIVE: boots past PLE-load, /health 200, warmup OK.** **B1 sanity partial: C1x2 + C2x2 certified** (harness fixed for the vllm bench serve CLI — module has no __main__; float-safe deltas; kv_cache_usage_perc rename; docker exec cat for results) → [BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md) §9 row B1 filled (C1 TTFT 42.7/48.5s, C2 0.83-1.95s, 0 preempts, MTP ~35-44%); accuracy gate accuracy/B1 saved (code-fib/state-tracker/logic correct; tool-call EMPTY = no tools registered). ⏳ **C3 (12x8k@c3) OOM d the HOST at 02:50** (NVRM NV_ERR_NO_MEMORY memdesc + global OOM killed sshd/NetworkManager/polkitd; vLLM container survived the cage) → harness now defaults C3→6x8k@c2 + MEM_FLOOR_GB preflight; C3x2+warm re-run at safe size PENDING. ⏳ next: finish safe C3/warm → record §9 → merge session branch (certifies spark-ai enabled) → verify iofficiency → ladder (BENCHMARK-PLAN §6). ✚ **(2026-09-15 S1 sweep DONE + PRODUCTION-READY-for-casual, owner+agent):** stability sweep complete — max servable util **0.82**, max ctx **262,144** (native; 300k rejected at startup), live config 0.82/262k (SSOT group_vars/spark.yml). Certified: 250k@95% 0 preemptions, acc-gate 8/10 serial + 8/10 concurrent-3. ⚠️ **Pending before full agent/max-ctx trust:** needle-test @262k, tool-call EMPTY fix, true 3×87k fill stress, NVFP4/SGLang (S2/S3) lane. Verdict: **usable for casual/chat now** — details: [BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md) §6a/§9. · [network-ops.md](docs/network-ops.md) · 📋 [`prompt-376.md`](prompt-376.md) |
| HD-101 | 4 | AI | 2 | **AI stack: Open Web UI** — Stage 4/10; ✅ IaC done. ⏳ Deploy-gated: surgical converge; verify SSO → Authentik round-trip → local-admin linked; then LiteLLM completion + RAG. · [services-ai.md](docs/services-ai.md) |
| HD-103 | 3 | AI | 2 | **AI stack: Docling OCR** — Stage 4/10; ✅ IaC done. ⏳ Live-verify a **real** Slovenian scan conversion (a synthetic render is not a scan). Corrected premise, measured 2026-09-21: the multi-GB model pull is a **non-event** — the weights are baked into the pinned image (871 MB) and the `/models` bind is decorative, so `sl` needs no download (`latin_g2.pth` is present). Any in-container CLI work must go through the served API: the CLI dies on the unwritable bind — see **HD-421**. · [services-ai.md](docs/services-ai.md) §Where the model weights actually live |
| HD-104 | 4 | AI | 2 | **AI stack: OpenClaw agent + integrations** — Stage 4/10; ✅ IaC done. ⏳ run `openclaw onboard` → `openclaw.json`; live-verify round-trip. · [services-ai.md](docs/services-ai.md) |

| HD-111 | 4 | AI | 2 | **Office MCP via Open WebUI** — Stage 1/10; `ppt-mcp` first then Word/Excel; register MCP servers in Open WebUI; retires AnythingLLM + LocPilot (HD-107 path). · [services-office.md](docs/services-office.md), [services-ai.md](docs/services-ai.md) |
| HD-249 | 2 | AI | 3 | **n8n internalization + LiteLLM integration** — ⚡ **public route DROPPED + tailnet edge LIVE (HD-135b follow-up, 2026-08-28)**: `auto.kogler.si` labels removed from the main edge; n8n reached over the tailnet via the `traefik-tailnet` edge (`auto.kogler.si` / `auto.ts.kogler.si`, forward-auth on the plain name). Remaining: audit external webhook deps + WEBHOOK_URL (was a precondition to dropping public — verify no inbound public webhook breaks), scoped LiteLLM key with budget cap for AI workflow nodes · [services-utilities.md](docs/services-utilities.md) · 📋 [`prompt-384.md`](prompt-384.md) |
| HD-251 | 2 | INFRA | 3 | **Fleet exposure rework phase-2 (tailscale-first)** — Headplane → tailnet-only; review Dozzle/Metabase/Grafana/Traefik-dash tiers; document Patterns A/B in network-vpn.md + capability-tiering policy in security.md (**doc part landed 2026-08-26 with this commit**); rollout pending. · [network-vpn.md](docs/network-vpn.md), [security.md](docs/security.md) |
| HD-28 | 3 | AI | 3 | **Office AI stack** — ONLYOFFICE desktop + MS Office via Open WebUI MCP. ⛔ **Nothing here is unblocked (2026-09-18):** the "Ollama models" half is superseded twice over (spark = generation tier #24, pinned-AI legs #25 → shipped as HD-391, closed) and the MCP half waits on HD-111; AnythingLLM + LocPilot superseded. Do not start this row. · [services-office.md](docs/services-office.md) |
| HD-268  | 3  | AI  | 1  | **IaC for the HD-267 modular AI: Qdrant swap + Forgejo OKF wiki repos + dual pi.dev/DSH harness** — ✅ IaC authored + full converge live 2026-08-27 (Qdrant, pi-dev, dsh up on services-internal); ✅ tailnet sidecar IaC closed (HD-268c: Pattern-A sidecars, opt-in flags, 2 new 1P items). ⏳ **Remaining (re-scoped 2026-09-18):** the **harness half is parked** — `dsh` + `pi-dev` are `enabled: false` (HD-386) and decision #26 routes harnesses direct to the engine, so the HD-268c tailnet sidecar enable-flow for those two UIs is parked with them (their tailnet names + the `dsh-backend`/`pi-backend` routes still answer 502 — owner call to drop, tracked under HD-386). **Still live work:** Qdrant embed/rerank + re-index after the bge-m3/1024 cutover (⚠ the HD-391 Vulkan embed move does **not** trigger that re-index — same 1024 dims, cosine 0.9996 vs the live vectors), the OKF wiki repo skeletons (HD-268a), and **HD-268b = `rag-mcp` itself: still a STUB, not a service** — `templates/docker_services/rag-mcp/docker-compose.yml.j2` is a TODO comment block with no `services:` section, so its `enabled: false` in `vps.yml` is not a flag to flip (flipping it fails `docker compose config`). Owner call 2026-09-19: implement it as its own lane; the HD-391 reranker leg therefore **ships dormant** (≈327 MiB idle) until 268b gives it a caller. · [docs/services-ai.md](docs/services-ai.md), [docs/network-vpn.md](docs/network-vpn.md), [deployment-compose.md](docs/deployment-compose.md), [deployment-secrets.md](docs/deployment-secrets.md), [group_vars/vps.yml](IaC/ansible/group_vars/vps.yml) |
| HD-335 | 2 | AI | 2 | **spark (Lenovo ThinkStation PGX / NVIDIA GB10) headless Triton node + Mem0 + OpenHands** — the new Phase-2 hardware (supersedes HD-42 Ryzen/R9700 build). Headless NVIDIA GB10 (128 GB unified, 1 PFLOP FP4) node `spark.kogler.si`; Triton Inference Server serving the NVFP4 generation set (nemotron-30B, qwen3-next-80B, llama-3.3-70B, qwen3-coder-next-80B); **Mem0** long-term memory for OWUI (Qdrant-backed, per-user/proj scoping) + **OpenHands** agentic coding harness. **(2026-09-06) spark = sole inference tier; (2026-09-15, decision #24) refined → spark = big-model generation tier** — the small pinned services (Whisper STT + bge-m3 embed + bge-reranker) move to the oldsrv RX 7600; no Ollama/ROCm on oldsrv; Cohere retired (bge-m3/1024). SSOT: [`hardware-spark.md`](docs/hardware-spark.md) + [`services-ai.md`](docs/services-ai.md). ✅ **Bring-up is DONE (2026-09-14/15):** DGX OS, `spark.yml`, XFS carve, static placement, and the generation tier is **vLLM serving `spark/qwen3.8-flash-next`** — unified-memory-budgeted and stress-certified 2026-09-18. The Triton/NVFP4 serving plan is history (decisions #24/#25). ⏳ **Remaining on this row:** Mem0 + OpenHands onboarding, the embedding-cutover tail (bge-m3/1024 + Qdrant re-index → HD-268), and the S2/S3 NVFP4×SGLang bench lane. · [services-ai.md](docs/services-ai.md) · [hardware-gpu.md](docs/hardware-gpu.md) |
| HD-336 | 3 | AI | 2 | **Coding plane — agent memory (`agent-memory.dev`, per project) is not built.** Spec is the SSOT: [services-ai.md](docs/services-ai.md) §9b (shape, ports 3111+N, `~/.agentmemory/<project>`, MCP `@agentmemory/mcp`, consolidation via a LiteLLM scoped key; OpenViking rejected outright 2026-09-21). ⏳ One project instance wired end-to-end + its scoped key + the MCP hookup on oldsrv. ⏳ The owner call inside §9b on the wording of the memory rule is in front of the cross-project part; the per-project half is not blocked by it. ⚠ `oldsrv` is currently off the network ([hardware-oldsrv.md](docs/hardware-oldsrv.md) §Reachability & wake), which gates any live verify here. · [services-ai.md](docs/services-ai.md) §9b · [hardware-spark.md](docs/hardware-spark.md) · [hardware-oldsrv.md](docs/hardware-oldsrv.md)|
| HD-336b | 2 | Human | 2 | **CrewAI epic-orchestration pilot — OWNER DECISION ONLY (no AI work until answered)** — pilot (2-week vertical slice, human-signaling at every decision point) starts only when the owner rules: **run the 2-week pilot now / later / never**. Kill criteria: 2-week vertical slice or abandon. Spec: [services-ai.md](docs/services-ai.md) §9b. Does NOT gate the agent-memory/ZeroClaw row (HD-336). · [services-ai.md](docs/services-ai.md) §9b **PARK REAFFIRMED 2026-09-21 (owner): still parked — the owner is evaluating alternatives (including Hermes); no pilot until they rule.** |
| HD-337 | 2 | AI | 2 | **spark bring-up — execute the HD-335 node plan (pointer row; bring-up DONE 2026-09-14/15)** — spec in [`hardware-spark.md`](docs/hardware-spark.md), NOT here. ✅ DGX OS → Ansible role + `network_static_hosts` placement → the wg-s2s leg + router forward-accept → engine live as **vLLM** (the Triton/`triton-backend` overlay plan is superseded by decisions #24/#25) → pinned STT/embed/rerank moved to the oldsrv RX 7600. ⏳ **Remaining tails only:** embedding cutover bge-m3/1024 + Cohere retirement + **Qdrant re-index** (→ HD-268), Mem0 + OpenHands, and the S2/S3 lane. · [hardware-spark.md](docs/hardware-spark.md) · [services-ai.md](docs/services-ai.md) |

| HD-376 | 2 | AI | 2 | **pi.dev harness (workstation) → spark: full 262k context + thinking/timeout tuning (APPLIED on the workstation + DOCUMENTED 2026-09-16)** — the harness ran on pi's implicit `contextWindow: 128000` default, so it compacted at ~112k and **left more than half of the certified 262,144-token engine window unused** (the S1 stability sweep 2026-09-15 + the HD-374 KV-pool governor certify 262k — [hardware-spark.md](docs/hardware-spark.md)); it also sent **no thinking control**, which let the vLLM Qwen template reason on EVERY turn (measured 23–121 hidden `reasoning_tokens`/turn) with no way to switch it off. ✅ **Measured the live endpoint first** (engine `vllm-0.1.dev20073+g8e685d198`): `max_model_len 262144` = the servable per-request ceiling — the oft-quoted "273k" is the KV-pool ~268–275k **token slots** (aggregate across sequences) and the GB10's 273 GB/s bandwidth, neither of which is servable context, so putting 273k in the harness would only produce engine `400`s; `chat_template_kwargs.enable_thinking` honored; `thinking_token_budget` honored (96 → 82 reasoning tok vs 121 uncapped); `reasoning_effort` accepted-but-ignored; `qwen3_coder` tool calls, thinking replay (`reasoning`/`reasoning_content`) and `stream_options.include_usage` all 200; ~11 tok/s decode. ✅ **Applied** to `~/.pi/agent/models.json` (`contextWindow: 262144`, `reasoning: true`, `compat.thinkingFormat: qwen-chat-template`, `compat.thinkingTokenBudgetField: thinking_token_budget`, `supportsReasoningEffort: false`, binary `thinkingLevelMap`, sampling parity with the engine's `--override-generation-config`) + `~/.pi/agent/settings.json` (`compaction.reserveTokens: 16384` → compaction now fires at **245,760**, `keepRecentTokens: 32768`, `httpIdleTimeoutMs: 900000` + `retry.provider.timeoutMs: 1800000` so a cold large-prompt prefill is not killed mid-flight, `showCacheMissNotices: true`, `defaultThinkingLevel: off`). ✅ **Verified:** `pi --list-models` → `spark  spark/qwen3.8-flash-next  262.1K  16.4K  yes` (was 128K / thinking no); `pi -p` smoke OK through the new compat block. The two files are workstation-only (they carry the `spark-llm_api` bearer key → never in git), so the **reference copy + the measured probe table now live in** [pi-harness.md](docs/pi-harness.md), including the **parallel-lane rule**: the KV pool is ONE shared ~262k-token budget (`max_num_seqs: 4`), so a parent at the full window + subagent lanes preempt each other — lanes need a smaller `contextWindow` profile. ⏳ **Tails:** needle test at 262k still gates agentic max-context work; the `spark-lane` 64k profile is authored-on-paper only; ✅ re-point `pi-dev` at the same window — **CLOSED 2026-09-17 as moot**: the LiteLLM entries did land the window (HD-382: `max_input_tokens 245760` + `max_output_tokens 16384` = 262,144 minus the reserve) but `pi-dev`/`dsh` are PARKED (HD-386) and decision #26 routes harnesses direct-to-engine, so there is no re-point left to do. · [pi-harness.md](docs/pi-harness.md) · [hardware-spark.md](docs/hardware-spark.md) · [services-ai.md](docs/services-ai.md) · 📋 [`prompt-376.md`](prompt-376.md) |
| HD-400 | 2 | AI | 2 | **spark: text-only engine mode — drop the vision stack from the engine (⏳ proposed, bench-gated; NOT applied)** — the staged checkpoint is multimodal (`Qwen4ExpForConditionalGeneration`) and the live `spark-ai` compose sets **no** `--limit-mm-per-prompt`, so vLLM loads the ViT + the mm processor cache (default **4 GiB host RAM** = the same 121.62 GiB pool) for consumers that are all text-only. Proposed: `--limit-mm-per-prompt '{"image": 0, "video": 0}'` (≡ `--language-model-only`), optionally `--mm-processor-cache-gb 0`. ⚠ **This is not a speed change** — with no image tokens the tower never executes, so **C2 decode is expected FLAT**; the gain is pool memory on the side that actually binds (boot-side headroom +5.4 GiB), where **1 GiB ≈ 32.2k KV tokens**. ⚠ Syntax: the `image=0,video=0` comma form errors on current vLLM (#39687) — JSON form only. ⚠ Module-skipping on disable is per-model-implementation upstream (issue #21943) — **measure on the pinned fork**, don’t assume. **Acceptance (one variable, BENCHMARK-PLAN §6 step 11):** boot per-pid MiB ↓ + host usable ↑ + `C2` tok/s within noise + accuracy gate 10/10 unchanged; the freed GiB is spent as a **separate** row (KV or margin), never the same change, or the ledger cannot attribute either. Consequence accepted: `image:0` ⇒ an image part is a hard **400** (weigh in HD-384 if OWUI joins the scoped tier). · [hardware-spark.md](docs/hardware-spark.md) §Text-only engine mode · [spark/BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md) §6 #11 · 📋 [`prompt-376.md`](prompt-376.md) |
| HD-401 | 2 | AI + Human | 2 | **Workstation (admin laptop, Strix Halo) = client-side inference tier: local FIM autocomplete + Qwen3-VL visual judgment; NPU out** — new owning doc **[hardware-workstation.md](docs/hardware-workstation.md)** (decision #28 in [services-ai.md](docs/services-ai.md) §9 records the placement). Two legs, both local by necessity: **(1) FIM** — typing-time latency budget (~150–400 ms, prefill paid per trigger), a 7B-class FIM model does not fit the 7600 ledger, source stays on the laptop; the model **must be FIM-trained** (`Qwen2.5-Coder-1.5B/3B/7B` or `StarCoder2-3B/7B`) — instruct/VL variants do not do it. **(2) Vision = judgment, as a text cascade** (`Qwen3-VL-30B-A3B-Instruct`, iGPU): token-level split inference into spark is **rejected as unbuildable** (embeddings must live in the target model’s own visual-token space; image tokens cost *context* anyway). Seam rules are load-bearing: describe-once frozen by image-byte hash (keeps the prompt prefix warm), **pass-through** rewrite (never a key-dropping proxy — the decision-#26 class), loud rewrite counting. **XDNA/NPU explicitly out** (no VL-encoder path; buys watts not bandwidth; repack+driver pinning). ⏳ **Pre-flight gates before any config:** ROCm/`gfx1151` stack pick + version (community break: 7.2.1, not 7.0; Vulkan/RADV fallback) · VL `mmproj` actually runs on GPU (measure `--image` prefill) · FIM trigger latency on the real Continue prompt shape · BIOS iGPU carve-out/GTT check. ⚠ Platform figures in the doc are owner-stated/community, **not yet measured on this box**. · [hardware-workstation.md](docs/hardware-workstation.md) |
| HD-402 | 2 | AI | 2 | **Docling OCR engine: bench EasyOCR → RapidOCR-ONNX BEFORE applying (Slovenian first)** — ⛔ **not "one measurement away" (measured 2026-09-21, pinned `docling-serve-cpu:2.118.0`):** the adapter was never the risk (`RapidOcrOptions` exposes `rec_model_path` / `rec_keys_path` / `lang` / `backend`), the **`rapidocr` package is not installed** in the image (EasyOCR 1.7.2 is; the container sets no OCR env ⇒ default resolution = EasyOCR), while the **PP-OCRv6 det/rec weights ARE baked in** — the swap is one dependency away, not a download away. Ahead of it sits **HD-421** (no model can be fetched at runtime: `/models` is empty + root-owned vs uid 1001). Quality on `č ć š ž` stays **unanswered**: a synthetic render is not a scan and the served API rejected an ad-hoc multipart POST — both recorded rather than presented as a result. Free lever meanwhile: `do_ocr=false` **per request** for born-digital PDFs (call-site, not service env). Full evidence + bench protocol: [services-ai.md](docs/services-ai.md) §Docling OCR engine selection. · [services-ai.md](docs/services-ai.md) · HD-421 · HD-103 |
| HD-421 | 3 | AI | 2 | **Docling: make the model cache writable before any engine change (prerequisite of HD-402)** — the service's `/models` bind is empty and root-owned while the container runs as uid 1001, so **no model can ever be fetched at runtime** (reproduced: `PermissionError: /models/hub` from the CLI); the served pipeline works only because its weights are baked into the pinned image. Two-line shape: `bind_owner_uid: "1001"` on the registry entry (or repoint `HF_HOME` / `DOCLING_SERVE_ARTIFACTS_PATH` at the bind) + verify a runtime fetch actually lands on the bind. Until then any engine change (HD-402's RapidOCR bench, or a pin bump that drops a baked weight) is dead on arrival. Measured facts + the proof: [services-ai.md](docs/services-ai.md) §Where the model weights actually live |

| HD-403 | 2 | AI | 2 | **Home Assistant has no LiteLLM path at all — Assist/voice's LLM leg has nothing to call** — found by reading the render during the 2026-09-20 docs SSOT sweep: `home-assistant-primary/docker-compose.yml.j2` gives HA **only `TZ`** in `environment:`, and `roles/home_assistant/` carries no LiteLLM/`llm:` wiring at all, there is **no `home-assistant` alias in either `litellm_scoped_keys`**, and no vault item exists — yet [services-ai.md](docs/services-ai.md) named HA a gateway consumer and [smart-home-voice.md](docs/smart-home-voice.md) described the voice LLM as "a gateway model". Both docs now state the gap. ⏳ Ship with HD-384's consumer work: mint the scoped key vault-first, give HA `LITELLM_BASE_URL` + key in the compose env, point the Assist pipeline at it, then prove one intent turn end-to-end. · [services-ai.md](docs/services-ai.md) · [smart-home-voice.md](docs/smart-home-voice.md) · [HD-384](#27-ai--office--llm-stack-open-webui-office-mcp-vector-db) · 📋 [`prompt-384.md`](prompt-384.md) |

| HD-411 | 2 | AI + Human | 3 | **Native Android cockpit trial — Paseo daemon on oldsrv, direct-over-tailnet, relay OFF (time-boxed)** — Paseo is client-server (a daemon on the machine that runs the agents; native iOS/Android app + web) with a first-class `pi` integration, so it drives the **same** harness with the same credentials instead of replacing it. The posture-safe mode is the one its own docs recommend for VPN use: bind the daemon to the **tailnet address** only, set a password (bcrypt), keep `daemon.hostnames` allowlisted, leave the optional E2E relay **off** — nothing third-party enters the path. The alternative app is declined for now and logged: `remote-pi`'s relay is mandatory and its payload is **not E2E encrypted in the current version** ([services-rejected.md](docs/services-rejected.md)), which fails the [security.md](docs/security.md) §10 tiering invariant. ⚠ **Sequence changed 2026-09-21 (owner): the trial runs SIMULTANEOUSLY with HD-409, not after it** — one comparison window, two surfaces (each gets its own `*_port` var; both tailnet-bound, relay off). **Owner hand remains:** sideload the APK + pair + the 2-week verdict. ⏳ **Then AI:** the deployment (systemd unit or a scoped container with tightly-pinned mounts), the tailnet bind + password + hostnames, one 1P item. ⏳ Verdict lands in [services-ai.md](docs/services-ai.md) §9b either way: accepted → full §5 onboarding; abandoned → tombstone the row and leave the decision row standing as history. ⚠ The browser cockpit (HD-409) stays **primary** by design — an app someone else maintains cannot be a dependency, a URL can. ⏳ **PARKED 2026-09-23 — deliberately not installed, and here is exactly why plus exactly how to resume.** Installed instead: the browser cockpit (HD-409), which is verifiable from the server side. Paseo's acceptance needs a hand on the phone, and standing up a second unattended third-party daemon on a production host at 02:30 that nothing can end-to-end-test is worse judgment than parking. **Facts that save the next person an hour:** `npm i paseo` is a **name-squatted Next.js scaffold** (v0.1.0, unrelated to this project) and `@paseo/cli`/`paseo-ai`/`@getpaseo/daemon` do not exist — the real install is **`npm install -g @getpaseo/cli` then `paseo`** ([`getpaseo/paseo`](https://github.com/getpaseo/paseo), Apache-2.0), default port **6767** (its own Docker example maps 6767 and `paseo run --host <h>:6767` is the documented direct form), auth via **`PASEO_PASSWORD`**, and the **relay is an interactive startup prompt** — decline it and you get direct-over-tailnet, which is the posture this row requires. **Pre-flight already done:** port 6767 measured free on oldsrv; `paseo_port: 6767` reserved in `group_vars/all/main.yml` (the earlier 31416 reservation is explicitly freed — do not reuse it, or the two files disagree about what was allocated); Node 22.23.2 + **pi 0.87.1 already installed under `domen`** by HD-409, and Paseo lists `pi` as a first-class provider, so the comparison client starts from the **same rendered credentials** (HD-388 spec) rather than a second config surface. **Resume sequence:** (1) as `domen`, `npm install -g @getpaseo/cli` with the pinned Node on PATH; (2) decline the relay, bind the daemon to the derived `tailscale0` address on `paseo_port` — same drop-in pattern and the same *never-0.0.0.0* rule as pi-web's `tailnet-bind.conf`; (3) set `PASEO_PASSWORD` in a 0600 env file and keep `daemon.hostnames` allowlisted; (4) systemd **user** unit + `loginctl enable-linger domen` (already enabled, shared with HD-409); (5) **owner hand**: install the Android APK, Settings → host → Pair Device, and confirm the direct-over-tailnet path — which is also the first real test of the tailnet path the browser cockpit could not verify. ⚠ Two shared blockers, both inherited: no role owns these unit files (hand-converged until an owning lane exists), and the daemon password cannot be minted into the vault while oldsrv's `op-write_api` token is a **deleted** service account — see [deployment-secrets.md](docs/deployment-secrets.md). · [services-ai.md](docs/services-ai.md) §9b · [security.md](docs/security.md) · 📋 closed runner/cockpit lane (2026-09-23; brief deleted) |
| HD-444 | 2 | Owner | 2 | **Nobody has driven the oldsrv cockpit from the phone — the tailnet path it exists for has never been traversed by a real peer** — the browser cockpit is live under `domen` (HD-409, closed 2026-09-23): bound to the **derived** `tailscale0` address on `cockpit_pi_web_port`, `401` without the token and `302` with it, model contract and provider auth rendered from the HD-388 spec. **Changed by owner decision the same day:** the seat gets a real URL — **`https://pi-oldsrv.ts.kogler.si`**, tailnet-only, served by oldsrv's own `websecure-ts` listener with the daemon on loopback, so the phone needs no new oldsrv ACL grant beyond reaching that node, and `pi.kogler.si` was never an option (that FQDN is the RPi4 node). IaC authored + `validate-all` green; ⚠ the URL **404s until Phase 4c step 6 re-binds the daemon to loopback and the `traefik-internal` converge deploys the now-authored `pi-oldsrv-ts` router** — and this row's acceptance is now exactly one thing: cross it from the phone. What is *not* proven is the only part that matters for the owner's actual goal: every node reachable from that session is itself ACL-scoped away from oldsrv — the VPS `tailscale-sidecar` is a genuine tailnet peer and answers `tailscale ping <oldsrv>` with `no matching peer`, which is the ACL working correctly, not a fault — so no test has crossed the headscale policy the phone will cross. ⏳ **Owner hand:** open the PWA on the phone over the tailnet, confirm the token gate, the session list, and one chat round-trip **with the laptop shut**; that is also HD-445's premise ("the phone is a remote control, not a laptop replacement"). ⏳ If the headscale policy does not yet allow phone → oldsrv on the cockpit port, that is the first thing this finds, and the policy is the transport lane's file — record the exact denied pair rather than editing it here. ⏳ This row also gates HD-411's comparison window, which stayed parked for the same reason. · [services-ai.md](docs/services-ai.md) §9b-1 ⏳ Both remaining steps are blocked on the box, not on config: [docs/hardware-oldsrv.md](docs/hardware-oldsrv.md) §Reachability & wake. · [pi-harness.md](docs/pi-harness.md) §1 |

### 2.8 Security & Secrets — secrets hygiene, privilege, firewall, preseed hardening

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-361 | 2 | AI | 1 | **Cockpit break-glass login (`maint`) on the second cockpit host, oldsrv.** Design, the PAM-gate finding, exact grants, the `cockpit-nas.ts.kogler.si` tailnet name and both vault items are in [docs/security.md](docs/security.md), [docs/services-traefik.md](docs/services-traefik.md) §Cockpit Routes and [docs/deployment-secrets.md](docs/deployment-secrets.md). ⏳ From the laptop (`cockpit` is in the HD-413 lockout set): `bash scripts/ansible-run.sh playbooks/home_servers.yml --limit oldsrv.kogler.si --tags cockpit` — one converge lands the account, the gate and `cockpit.yml`, and the file provider hot-reloads Traefik (no restart, so no home-edge blip). Blocked on the box: [docs/hardware-oldsrv.md](docs/hardware-oldsrv.md) §Reachability & wake. ⏳ Then prove the authenticated pane in a browser per host. Clue for both steps, learned the hard way: `GET /` answers 200 **unauthenticated**, `POST /cockpit/login` is 405, and only `GET /cockpit/login` with Basic auth yields a truth-telling 401/200 — and even 200 leaves cockpit's real Origin check (the WebSocket handshake) unproven, which is what the browser load is for. · [docs/hardware-nas.md](docs/hardware-nas.md)|
| HD-194 | 1 | AI + gate | 3 | **`sso` route edge middleware (S17a)** — ✅ **Done.** Decided (HD-204): enforce, exception only if callbacks break. Reasoned through the flow: nothing breaks — the bouncer filters by SOURCE IP only (path-agnostic); OIDC `/application/o/<slug>/callback/` + forward-auth `/outpost.goauthentik.io/*` arrive as ordinary browser requests from user IPs; outpost↔server API traffic runs container-direct on services-internal, never through the edge router. `crowdsec-only@file` enforced on the authentik router (reasoning documented in-template); security.md §1 map + middlewares.yml.j2 comment updated; fail2ban http-auth jail stays as layered compensating control. ⏳ Deploy-gated: confirm a login + one OIDC callback end-to-end at VPS deploy. Closes audit S17(a). · [security.md](docs/security.md) §1, [services-traefik.md](docs/services-traefik.md) |
| HD-280 | 1 | AI | 2 | **fail2ban fix: add traefik `accesslog` + a traefik-aware filter (option a chosen)** — ✅ **IaC DONE + DEPLOYED + LIVE VERIFIED 2026-09-04:** `http-auth` jail reads the real Traefik accesslog (`/opt/traefik/logs/access.log`), jails `http-auth` + `sshd` both active (re-verified 2026-09-08). ⏳ **Remaining:** confirm a repeated 401/403 actually triggers a ban (observation job — waits on natural SSO brute-force; no work product until then). · [IaC/ansible/roles/vps-hardening/tasks/main.yml](IaC/ansible/roles/vps-hardening/tasks/main.yml), [docs/deployment-compose.md](docs/deployment-compose.md) |
| HD-287 | 1 | AI | 2 | **Add `cap_drop: ALL` + targeted `cap_add` to immich-ml (hardening)** — **RESCOPED 2026-09-08:** **Ollama removed 2026-09-07** (inference consolidated on spark/Triton, HD-335) — the split-HD-287 ollama leg is moot (service no longer deploys; template kept on the removed-services shelf). Remaining = **immich-ml**: GPU/ML leg is **LIVE 2026-09-08** (container healthy + ML round-trip verified, HD-160) → the HD-204 keep-in-`ALLOWED_NO_CAP_DROP` gate is met: AI can now **encode `cap_drop: ALL` + ROCm-init `cap_add` set → oldsrv converge → live-verify cap verification** (container Up + ML inference still works). · source: full-audit-2026-08-29 (MERGE-13) · [IaC/ansible/templates/docker_services/immich-ml/docker-compose.yml.j2](IaC/ansible/templates/docker_services/immich-ml/docker-compose.yml.j2), [docs/security.md](docs/security.md) §4, [scripts/validate-docker-services.py](scripts/validate-docker-services.py) |
| HD-442 | 1 | AI | 1 | **oldsrv's rendered `/etc/op/provision-token` is a stale copy, so every vault write from that host fails — and one secret is still homeless** (the dead-SA false trail, why a home host had no refresh path in code at all, and the fix are written once in [deployment-secrets.md](docs/deployment-secrets.md) `op-write_api`; no owner step remains — the service account is alive). ⏳ oldsrv `docker_services` converge **from the laptop** (HD-413 lockout set) to land `op-provision-token`, then the live re-probe. ⏳ Mint `cockpit-pi-web_api` carrying the **same** value already in `/home/domen/.config/pi-web/env` — a new value re-pairs every client. ⏳ Damage audit: what the oldsrv-side glue and key-provisioning passes were supposed to write since the file went stale and never did, verified one by one. ⛔ Any consumer that treats a token file's *existence* as its *validity* must fail loud on 403 — a provisioner that ignores rc is how a dead credential survived eight green converges. **All of it is parked behind HD-455** (oldsrv L2-dead since 2026-09-23 21:39:55). · [deployment-secrets.md](docs/deployment-secrets.md) · [1password.md](docs/1password.md) |
| HD-443 | 1 | AI + gate | 1 | **Grant plumbing — the rulings live in the doc; this row is the work that makes the live boxes match it** (`scripts/check_ssh_grants.py`, HD-416, reds on three placements). The verdicts — the Pi's admin surface, the fleet-wide `ai-debug`, the `domen` account shape, the Pi `admin` retirement, the `domen_ssh` rename — are recorded once in [deployment-secrets.md](docs/deployment-secrets.md) §Who is authorized where; do not re-ask or restate them here. ⏳ AI, in this order: own the layout in IaC (a role that converges users + `authorized_keys`, `--check`-safe on the first try — **today no role owns it**: preseed / `first-boot-config.sh` write it once imperatively, so sanctioning in prose alone leaves SSOT = drift, the HD-445 class) → converge `ansible-admin` + `domen` + `ai-debug` on the reachable nodes → re-run the auditor. ⏳ The Pi's `admin`: ⛔ **the order is the safety property** — prove `ansible-admin` logs in → remove the key from `admin` → only then lock/remove the account; never reverse it (HD-413 lockout set). ⏳ Owner, laptop only, 30 s: rename the SSH-agent stub `~/.ssh/laptop-domen.pub` → `~/.ssh/domen_ssh.pub` and repoint `IdentityFile` (the agent matches item names). ⏳ HA guests (`haos-vm-1`, `haos-mininta`) stay outside the audit set — record that as a declared scope limit, never as "clean". ⛔ The auditor must never touch an `authorized_keys`; ⛔ a gate that turns green by describing reality as intentional is not a gate. · [deployment-secrets.md](docs/deployment-secrets.md) §Who is authorized where |
| HD-455 | 3 | AI + Human | 1 | **`oldsrv` is off the network, and the defect worth naming is that this carried no row** — the fault was measured and written to its owning doc on 2026-09-24 ([hardware-oldsrv.md](docs/hardware-oldsrv.md) §Reachability & wake) but registered nowhere, so no lane could pick it up: headscale (authoritative, ACL-blind) has node 11 `Connected: offline`, last seen **2026-09-23 19:39:55 UTC** — the same second as the nas's NFS lease release for that client; `ip neigh` on the nas is `INCOMPLETE` and the MAC is absent from L2 entirely, so it is **not** a DHCP re-assign, the box is gone. ⛔ `tailscale ping` answering `no matching peer` from the VPS sidecar proves nothing here (the sidecar is ACL-scoped away from `tag:dev` by design). **Why it is P1 — the fan-out:** the home edge, `cockpit-oldsrv`, the nas cockpit's tailnet name (rendered ONTO oldsrv by the cockpit role), the control node + runner, the pinned-AI tier, and the rows HD-361 · HD-442 · HD-443 (its oldsrv placements) · HD-444 · HD-445 · HD-409/411 · HD-450 and the HD-419-class 502s. ⛔ **The WoL path is spent** (2026-09-24: three packet shapes × two bursts, same L2, no ARP afterwards) — silence there means no standby power or an unarmed wake path, so do not spend another evening on magic packets. ⏳ **What unblocks it is HD-454, not this row:** the Comet KVM's access path has never been written down (no address, no network, no account, no off-site reachability check), and the conditional deciding whether an away path even exists is which network it sits on — **VLAN 99 is sealed to same-site by HD-398 A**, VLAN 10 or a tailnet node is not. Until that is measured, this row is owner-present-or-HD-454 and **no AI step substitutes**. ⏳ AI once it answers: land HD-442's converge + the live re-probe, the oldsrv half of the grant sweep (HD-443), and re-read whether the drop explains HD-393's bounded `amdgpu` episode. · [hardware-oldsrv.md](docs/hardware-oldsrv.md) §Reachability & wake |

### 2.9 Backup & DR — ZFS snapshots, Kopia, off-site, restore drills

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-34 | 2 | AI + Human | 4 | **Assess Kopia Web GUI vs CLI** at the first restore drill (agent assesses during the human-run yearly drill). · [backup.md](docs/backup.md) |
| HD-191 | 2 | AI | 2 | **oldsrv Kopia agent (S18/J5)** — ✅ **Done + LIVE 2026-09-08 (HD-318a close-out):** agent Up + **first snapshots in repo** (`kopia snapshot list` → opt 1.3GB/5648f, dumps, immich-upload, signal-cli-data; all 4 sources, full retention). ✅ Auth fixed live (agent presents `oldsrv-agent@oldsrv.kogler.si` htpasswd identity, not the server admin user). ⏳ **Remaining:** restore drill from a snapshot; volume-name pin verified at restore. Closes audit S18/J5/W12 (restore drill = W12 tail). · [backup.md](docs/backup.md) — ⏳ **Folded in 2026-09-21 (owner: fold, do not spawn rows): the two Kopia legs the retired backup work left behind, measured on the box.** (a) **there is no backup client on the VPS at all** — no binary, no client config, no `kopia-agent` container; the host runs only `kopia-server`, the endpoint the other agents push through — so **no `/srv/docker/*` path and none of the `db-backup` SQL dumps reach any off-site snapshot**; (b) **oldsrv has no `db-backup` job** and `/srv/dumps` has been empty since it was created 2026-09-03 while the agent dutifully snapshots that empty directory ⇒ `lan-litellm-db` has no dump anywhere. Shape of the fix is this row's own precedent: an agent container + an include list (`/opt`, the dump volume, the `/srv/docker/*` state dirs). Evidence + the coverage-table corrections: [backup.md](docs/backup.md) §VPS-side coverage gap |
| HD-238 | 2 | AI | 3 | **oldsrv→VPS DR runbook for non-GPU services** — ✅ **written 2026-09-21**: [backup.md](docs/backup.md) §Runbook — restore the non-GPU tier on a replacement VPS (7 imperative steps: bootstrap → address plane BEFORE data plane, because a changed public IPv4 moves `dns_primary_ip`, the Cloudflare loopback origin, Headscale's advertised routes and Technitium's conditional forwarder in ONE commit → registry-ordered converge → IdP-first restore with the `docker inspect` bind mounts as the path map → home-side re-point including the kopia-server / `kopia_password` / trust-fingerprint chain → two-sided verify → write down what the restore cost) + §Restore drill (yearly) with a real pass criterion, and a VPS-loss row in §Recovery Paths. ⏳ **Owner-scheduled only (left for now on instruction):** run the drill and tick it with a dated result; fill the per-service fail-over-vs-accept-loss table (media re-download and spark model re-serve are already decided). ⚠ What the runbook cannot promise until the HD-191 legs land: the VPS's own state has no off-site copy. · [backup.md](docs/backup.md) |

### 2.10 Docs & Family — family guides, docs, manual

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-32 | 2 | AI | 4 | **Write family guides `docs/manual/*`** — Slovenian guides per [`manual/README.md`](docs/manual/README.md) (count derived from the index table, never hand-entered), `status: wip`, not yet written; content well-specified; deferred until services live. · [manual/README.md](docs/manual/README.md) |
### 2.11 Finance & Subscriptions — budget apps, banks, billing

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-57 | 3 | AI + Human | 3 | **Finance stack: Actual Budget / Enable Banking** — Stage 8/10: `docker_services/actual-budget/` (stable pin `26.8.1`) + group_vars registry + decisions landed 2026-08-24 (oldsrv placement · stable+n8n EB bridge, nightly rejected · CSV-first card · seed-before-sync — see [services-finance.md](docs/services-finance.md) Decisions). ⏳ **Deploy-gated:** human tokens + EB app creation ([services-finance.md](docs/services-finance.md) Open Questions), n8n import/categorization workflows (Phase 1), WG AllowedIPs scope for the :5006 API leg, first deploy + live verify at Phase 3. · [services-finance.md](docs/services-finance.md) |
| HD-133 | 3 | AI | 3 | **Subscription renewal reminders (Homepage + calendar + n8n)** — SSOT = `subscriptions.yml`; drives Homepage Subscriptions/calendar + n8n renewal notify; `subscription.md` derived table. · [subscription.md](docs/subscription.md), [automation-renewals.md](docs/automation-renewals.md) |

### 2.12 Observability / Alerting

> Victoria* migration (single-stack decision, 2026-09-08). Owner request: replace Prometheus/Loki with a single Victoria stack on the VPS (reliable tier); multiple writers (Alloy/exporters on oldsrv/Pi/VPS) remote-write into one VictoriaMetrics/VictoriaLogs on the VPS; **MCP AI-debugging servers live on oldsrv** (much more RAM there) but **logs + metrics stay on the VPS** (infra more reliable). Decision + prior research in [`brainstorming/recent/Prometheus-Vs-victoriastack.md`](brainstorming/recent/Prometheus-Vs-victoriastack.md).

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-342 | 2 | AI | 2 | **Observability: Victoria stack implementation + LIVE CUTOVER (VPS)** — ✅ **DONE + LIVE 2026-09-08 (HD-342 leg + this session's cutover):** VictoriaMetrics (:8428) + VictoriaLogs (:9428) replace prometheus/loki on the VPS (topology B — Alloy is the single scrape tier; VM/VL pure storage/query; basic-auth via `victoria-metrics_api`/`victoria-logs_api`; retention 365d/90d Kopia-backed host binds `/srv/docker/victoria-*/data`; self-monitoring rules/dashboards retargeted). **Live-verified this session:** seeded both 1P items (+ `kopia-server_fingerprint`); converged VPS (`docker_services,monitoring`); Grafana datasources API-seeded to VM/VL (uid `prometheus`→VM default, `loki`→VL via `victoriametrics-logs-datasource`; removed the stale **read-only Loki datasource** — the file-provisioning leftover that blocked the API seed — via sqlite row delete + grafana restart); **fixed VictoriaLogs healthcheck** (tool-less `victoriametrics/victoria-logs` image ships only `/victoria-logs-prod` — `wget` CMD healthcheck failed to exec → false `unhealthy`; removed the healthcheck — covered by Grafana datasource health + `up{job="victoria-logs"}`); **Alloy restarted** to load Victoria remote_write targets (was running the Sep-3 config → VM had no series); old `prometheus` + `loki` containers **stopped**; datasource healths OK, VM shows 13 `up` series, alert rules evaluate with 0 errors (no more DatasourceNoData flood — HD-345). ⏳ **Remaining tails:** kopia client wiring for `/srv/docker/victoria-*/data`; oldsrv MCP (HD-344). · [observability.md](docs/observability.md) · [IaC/ansible/roles/monitoring/](IaC/ansible/roles/monitoring/) · 📋 [`prompt-420.md`](prompt-420.md) · **2026-09-22, the kopia tail: not monitoringly fixable — there is no VPS backup client to wire.** The IaC-side precondition is already true (`/srv/docker/victoria-{metrics,logs}/data` are host binds under `/srv/docker`, which is what a client needs), but [backup.md](docs/backup.md) §VPS-side coverage gap records the state: the VPS runs `kopia-server` (the repository endpoint) and has **no client of its own** — no binary, no client config, no `kopia-agent` in the registry. Adding two paths to an include list with no client is the fiction that file warns about; the client itself is **HD-191’s** change, so this row keeps its tail and names HD-191 instead of pretending the data is off-site. |
| HD-344 | 3 | AI | 3 | **MCP AI-debugging servers on oldsrv — ✅ ENABLED + DEPLOYED 2026-09-14.** `mcp-victoriametrics` + `mcp-victorialogs` live, Basic-auth via victoria-*_api, backend `victoria_backend_host` (wg-s2s). **Ports are `mcp_metrics_port` 8083 / `mcp_logs_port` 8084**, bound to `oldsrv_home_ip` — NOT :8080/:8081 (8080 belongs to pi-dev; moved 2026-09-15). Use the vars, never a literal. ⏳ **Remaining (AI):** register endpoints in pi / Open WebUI / OpenClaw; tailnet redo = owner. · [observability.md](docs/observability.md) §MCP |
| HD-347 | 2 | AI | 1 | **Observability false-alarm cleanup + alert-router wiring (owner triage 2026-09-08/09)** — actioned all 5 owner asks from the Grafana warning-mail triage: ① **SNMP `ifName` labels fixed** (`snmp.yml.j2`: add ifName OID to `walk` + `type: DisplayString`; was rendering hex `0x65746865…` + no label → alerts said "interface [no value]"; live-proven vs RB4011+CRS328) + **per-device `instance` labels** (router/switch) so the two devices no longer collapse into one flat series. ② **`homelab-alerts` n8n webhook workflow created + ACTIVATED** via the n8n public API (`n8n_api` 1P item → `POST /api/v1/workflows` + `/activate`); versioned JSON in `roles/monitoring/files/n8n/`; verified webhook 200 (was 404); `signal-cli-rest-api` published on `oldsrv_home_ip:8080` for VPS n8n over WG (the old "no host ports" comment was aspirational — n8n couldn't resolve signal-cli on oldsrv's overlay). ⏳ **Signal delivery = owner, and it is STILL open (2026-09-18 re-check):** the device is linked, but `signal_alert_recipients` is empty in `group_vars/all/main.yml` — capture the alerting group's UUID — non-secret SSOT, like `grafana_alert_email`, group name in [observability.md](docs/observability.md) §Alerting — or the workflow has no recipient. A prior handoff claimed this closed; the SSOT says otherwise. ③ **`mikrotik-link-down` alert removed** (empty switch ports are not real incidents — the 10 firing instances were all no-cable ports). ④ **stale `prometheus-down`/`loki-down` removed** (pre-Victoria leftovers, live-orphans). ⑤ **self-monitoring rules actually arm**: per-rule `eval_type: lt`+`eval_params: [1]` for `== 0` exprs (old `gt 0` made them dead) + VM/VL self-scrape `basic_auth` (was 401 → `up=0`). ✅ **CONVERGED + LIVE 2026-09-09** (VPS `docker_services,n8n`): n8n `SIGNAL_*` env live (webhook 200 "Workflow was started"), `signal_alert_recipients` fallback renders the quoted signal-group name; three follow-on fixes in the same commit — `_template_vault_items["n8n"]` + Signal items (was missing → render crash), `SIGNAL_RECIPIENTS` double-quote YAML bug, `tuwunel_version` `1.9.0`→`v1.9.0` (nonexistent tag, 404 on docker.io). · [observability.md](docs/observability.md) · [IaC/ansible/roles/monitoring/](IaC/ansible/roles/monitoring/) **✅ OWNER ANSWER 2026-09-21: alerts go to the WHOLE Signal alerting group (its name lives in [observability.md](docs/observability.md) §Alerting), not the personal number — and the AI sources the value itself: the group's invite LINK is not a recipient, the group ID is. Read it from the linked signal-cli on oldsrv (read-only), write `signal_alert_recipients`, converge n8n, prove delivery. Pure AI.** |
| HD-377 | 2 | AI | 2 | **Unified **LLM dashboard** (`homelab-llm`) — merge the three HD-368 vLLM dashboards into one, + spark CPU/RAM/GPU + engine readiness (AUTHORED 2026-09-16 · ✅ PROVISIONED 2026-09-22 · ❌ FIRST OWNER RENDER 2026-09-23 FAILED IT — 15 of 53 panels were blank · ✅ FIXED + EVERY QUERY REPLAYED 2026-09-23, live version 3 · ⏳ owner renders it again)** — owner: "instead of many dashboards, I want 1 … include CPU, GPU, RAM usage from spark … include indicator for vLLM/SGLang readiness (health)"; RX 7600 comes later. ✅ **Generated, not hand-edited** — [`scripts/build-llm-dashboard.py`](scripts/build-llm-dashboard.py) merges the three HD-368 exports into `homelab-llm.json` (10 rows / 48 panels: Health & Readiness · spark node · Scheduling · Latency · Throughput · Cache · Reliability · Request shape · per-engine detail · forward-look). Chain is **upstream → `adapt-vllm-dashboards.py` → this script**; never hand-edit the merged JSON. Merging ≠ concatenating: **53 of the 53 source panels are either carried once or listed in `DROPPED` with a reason** (the three exports triple-cover TTFT/KV/E2E/prefix-cache; 100 %-cut dashboard = gnet 25043, fully superseded) and **every carried query is re-joined `by (job, instance)` + scoped to `$instance`**, because a `sum()` across two engines is not a metric — a guard fails the build if any query still ignores the picker. 5 fail-loud guards (`--check` = CI form, byte-idempotent): reference resolution, empty/`$model_name`-outside-matcher queries, `$instance` coverage, layout-vs-`DROPPED` consistency (caught two of my own doc errors), non-English operator strings, HD-368 stale-metric creep into queries. ✅ **Live-verified against VM before writing** (43/47 query metric names present in VM today; the 3 absent are the intentional DCGM panels) + all label sets confirmed (`finished_reason`/`reason`/`engine`/`le`/`mountpoint`) — the panel list is derived from live data, not from the upstream templates. **New panels:** readiness = `up{job="vllm"}` + `up{job="sglang"}` + **independent** blackbox `http_2xx` on the engine `/health` (three panels = three different failures, explained in-panel) + engine count + served-model sanity; spark row = CPU %, RAM % of the **GB10 unified pool**, RAM absolute (**the OOM signal is the `MemAvailable` floor, not utilisation %** — live 9.5 GiB avail / 121.6 GiB, already inside the HD-375 warn band), load, engine RSS vs the 105 GiB cage (HD-374), disk capacity (`/` + the `/mnt/spark_nvme` XFS weights/KV mount) **and disk work** (throughput B/s, IOPS, device busy % — added 2026-09-16 per the owner's "DISK io/throughput" ask; `node_disk_*` on `nvme0n1`, already live in VM, see HD-378). **GPU finding (owner-visible):** GB10 exposes **no VRAM counter at all** — live `nvidia-smi -q -d MEMORY` → Total/Used/Free `N/A`, `dmon -s um` → fb/bar1 `-`, no `dcgm`/`dcgmi` package installed. So DCGM panels (SM active / tensor pipe / DRAM active) are **authored empty-on-purpose** and NOT faked from host CPU/RAM (a different quantity); a live DCGM-free "GPU work" panel (vLLM's `estimated_flops/read_bytes_per_gpu_total` + KV %) keeps the row informative today. ~~DCGM is the right source anyway — SM/tensor/power are on no other exporter.~~ **⚠ Corrected the same day by a live DCGM probe (HD-378 follow-up, [observability.md](docs/observability.md) §LLM Dashboard → GPU):** the `nvidia/dcgm-exporter` image already on spark DOES run and gives util/temp/power/energy/SM-clock/XID — but its profiling module refuses to load (`Skipping line 21..25 … metric not enabled`; NVIDIA: no plans to support DCGM on Spark), so **SM active / tensor pipe / DRAM active can never fill and those 3 panels must be DELETED, not wired**, and `FB_*` (VRAM) stays impossible per NVIDIA's own unified-memory statement. Wiring the exporter is still worth doing for the 6 `DCGM_FI_DEV_*` signals nothing else carries. ⚠ **Honest limits, recorded in-panel, not hidden:** every `sglang:*` sibling is written from SGLang's **documented** names + the gnet 25502 author's pairing, never from a live scrape (we have never run SGLang) — expect HD-368-style name corrections; the SGLang prefix-cache denominator divides by total prompt tokens (SGLang has no queries counter) = an upper bound; joins are applied **only** where both engines expose the same metric — where they don't (`realtime_tokens_total` vs `prompt_tokens_total`) the two upstream panels are kept side by side rather than summed into a number that means neither. ✅ **PROVISIONED + IMPORTED (2026-09-23, read-only verify over SSH — no converge):** the VPS copy of `files/dashboards/homelab-llm.json` is **byte-identical** to the repo (sha256 prefix `7fea0c37cde03d1a`) and Grafana serves it (`GET /api/dashboards/uid/homelab-llm` → 200, title `Homelab — LLM`, **53 leaf panels** / 63 panel objects incl. 10 rows, version 2, `prometheus` datasource health OK); the file provider re-scans every 30 s. **The three HD-368 dashboards stay provisioned** until the owner signs off on this one, then deleting the three JSONs + re-converging `--tags monitoring` retires them (file-provider state follows the dir). ⏳ Not in scope here: the `/health` blackbox probe (needs a spark-reachable exporter — spark loopback is unreachable from the VPS blackbox) and DCGM wiring; both are specified in [observability.md](docs/observability.md) §LLM Dashboard. · [observability.md](docs/observability.md) §LLM Dashboard · [scripts/README.md](scripts/README.md) · 📋 [`prompt-420.md`](prompt-420.md) · **❌ (a) WAS RENDERED ON 2026-09-23 AND DID NOT PASS** — owner: "Throughput & Workload", "Per-engine / TP-rank detail" mostly No data, "this is not good enough yet", and the views they want are **"Engine Internal & Cache"** (from `vLLM Monitoring V2`) and **"Token Throughput (Prefill / Decode)" + "Decode Throughput"** (from `LLM Inference (vLLM/SGLang)`). All four were already inside the merged board — dead or scattered — and the causes were three bug classes, none of them a missing metric: **a picker behind exact `=`** (All substitutes `.*`, and `=` is equality → `model_name=".*"` matches nothing: measured 0 series vs 1 for `=~`), **a joined `{__name__=~"vllm:x\|sglang:y"}` selector carrying a SGLang-only label** (`cache_source`, `stage`) which filtered the *running* engine out of its own panel, and **empty-by-design panels rendering as `No data`**. Before the fix **51 of 100 targets returned zero series and 15 panels were fully blank** (Latency 4/4, Per-engine 4/4, Throughput 3/4). All three are now generator guards (`guard_picker_operators`, `guard_joined_selectors`) + a `noValue` for the honest empties, the two preferred groupings are real rows ("Engine internals & cache"; the prefill/decode pair moved into Throughput), and all 101 queries were replayed against live VM — every panel returns series except the two that now say "not deployed" / "probe not wired". Method + numbers: [observability.md](docs/observability.md) §The first owner render. ⏳ **(a) is now: render it again** at `https://stats.kogler.si` → **Homelab — LLM**, picker on All. On sign-off say so and the three HD-368 JSONs get deleted + `--tags monitoring` re-converged (that also retires the hard-coded `[5m]` prefix-cache gauge, which lives only on `vllm-master-v2.json` — rebuilding it on a board scheduled for deletion is not the fix). (b) is **DONE 2026-09-22**: the three `DCGM_FI_PROF_*` panels are deleted (they can never load on GB10), the six real DCGM signals + `DCGM_FI_DEV_XID_ERRORS` readiness replace them, **48 → 53 panels**, re-derived by `build-llm-dashboard.py` with `--check` green and provisioned. |
| HD-380 | 2 | AI | 2 | **spark unified-memory governor term 2 + OOM forensics watchdog (HD-380 — code LIVE, docs DONE, alert tail OPEN)** — HD-374's KV-only governor did NOT hold: two more global OOMs within hours (#4 **no client attached**, #5 two sessions at 18% window combined) and its predicted 32.5 GiB reserve **never existed** (measured 16.7 GiB; engine per-pid 103,449→105,477 MiB ratcheting idle vs 83.4 GiB accounted). Root defect found by measurement: this build captures CUDA graphs for **[1,2,4,8,16,24,32]** while `max_num_seqs=4` caps decode at 4 → unreachable captures strand pool memory (HD-374 assumed graphs=0.22 GiB — wrong by ~40×). ✅ **LIVE 2026-09-16 (converge rc=0, health_ok=1, RestartCount=0):** `--max-cudagraph-capture-size 4` → per-pid **105,477→96,235 MiB**, host `MemAvailable` **16.7→22.2 GiB**. ✅ **`spark-oom-watchdog`** (roles/spark, sampler 15 s + KILL/CRIT/WARN/RESTART forensic bundles, OOM-immune `OOMScoreAdjust=-1000` because the killer took alloy+dcgm in #2/#3, evidence on XFS) — **proved itself same night** (CRIT bundle captured the 22:15Z event incl. pre-event curve). ✅ **Bench harness de-noised:** `vllm bench serve` 401s every request against the `--api-key` engine **and still exits 0** with `Successful requests: 0` — the first HD-380 run "passed" 21 steps at **zero load**; both runners now read the key from container argv and assert on the counters ⇒ **any bench number taken after `spark-llm_api` landed is invalid until re-run**. ✅ Guard **self-heals** (`docker stop` is intentional ⇒ `unless-stopped` never restarts it; the first fire cost a 14-min outage). Ruled out with numbers: dcgm leak (peak 0.19 GiB / cap 256 MiB / never tripped), more swap (killer fired with swap half free), zram (AnonPages 0.55 GiB, refault file:anon 42:1), concurrency-as-cause (#5). ⚠ **OPEN (trimmed 2026-09-18 after the certifying run):** (1) ~**13 GiB** non-KV/non-weight allocation is still **unprofiled and its mechanism is NOT removed** — `kv_cache_memory_bytes` skips memory profiling; C1+C2 raised the floor **and** (now measured) bounded the *observed* peak, but only `--enforce-eager` removes the graph term, and that trades decode throughput on every turn. Certified state: **+1,278 MiB / ~1.5 h of the pessimal 2×240k shape** (was +21,012 MiB / ~30 min pre-C1/C2) → the C3 trigger in [`spark/stability-test.md`](spark/stability-test.md) §4 is **not** met, so C3 stays off **on measurement**; re-open it only if a future curve grows >2 GiB/h under traffic and does not return at idle. (2) **agent sessions are a first-class memory term** (incident #6: ~162k tok = 56 % of the OLD 283k pool; prefix hit 0 % → 17,008 tok/s full-window re-prefill) — never benchmark with a large session attached; long agent sessions need the `spark-lane` context cap or off-box routing (**HD-376**, still open). ✅ **CLOSED 2026-09-18, do not re-open:** open item 3 (KV raise) — pool is **16 GiB LIVE + stress-certified**, and the same run found the **ceiling** (boot-side +5.4 GiB binding) so the next byte is fp8's job; open item 4 (HD-375 rules) — **LIVE 2026-09-17 22:33C**; open item 5 (idle ratchet) — **retracted by HD-381** (flat overnight at 88,773 MiB, traffic-cumulative under load); open item 6 (re-run A/B with no agent attached) — **DONE**, the 3-rung chain ran with in-flight 0 and passed 3/3. · **⏳ Next on this lane (the only open stability work):** watch for a curve that grows **> 2 GiB/h under traffic and does not return at idle** (`/mnt/spark_nvme/oom-watchdog/state/samples.csv`, `gpu_top_mib`) — that and only that re-arms C3 `--enforce-eager`; otherwise the remaining spark items are the HD-376 needle test + `spark-lane` profile and the S2/S3 NVFP4×SGLang lane, not memory safety. ⚠ **Registered 2026-09-18 — the one pre-condition of the 16 GiB raise that was never measured:** the raise is justified by FEWER EVICTIONS, not a bigger number, yet the prefix-cache hit rate and the preemption counter were never sampled across 8.2 → 16 GiB ([`spark/stability-test.md`](spark/stability-test.md) §6 caveat (a) asks for exactly this). Cheap and non-invasive: read both off the engine's `/metrics` across one normal working day and compare with the pre-raise numbers in the incident records — if the hit rate did not improve, the 7.8 GiB bought nothing and `usable` paid for it. · **2026-09-23: the watchdog’s idle-recycle term is now OFF by owner decision** (`spark_oom_watchdog_recycle: false`) — which makes the >2 GiB/h growth curve above the ONLY re-arm trigger for the recycle term as well as for C3; decision, measured rationale and the CRIT-path reassurance are in [hardware-spark.md](docs/hardware-spark.md) §Unified-memory budget. · [spark-incidents.md](docs/spark-incidents.md) #4/#5/#6 · [hardware-spark.md](docs/hardware-spark.md) §Unified-memory budget (corrections + the certified 2026-09-18 baseline) · [spark/stability-test.md](spark/stability-test.md) · 📋 [`prompt-376.md`](prompt-376.md) |
| HD-397 | 2 | AI | 2 | **Off-LAN access parity — ✅ LANDED + live-verified off-LAN 2026-09-19; ⏳ Deploy-gated: the ON-SITE pass** — the VPS jump now travels IN THE INVENTORY: `ansible_ssh_common_args: "-o ProxyJump=vps"` in `group_vars/{home_servers,storage,raspberry_pi,spark}.yml` (`storage.yml` created — the `storage` group had no group_vars file at all, which is why `nas` had no jump anywhere), `host_vars/oldsrv.kogler.si.yml` `ansible_host` moved to the **Home** leg (HD-398 → owner decision A: the Mgmt plane is a same-site plane), and the laptop alias contract is documented as the SSOT for both `~/.ssh/config` files (WSL + Windows rebuilt: `nas` gained the jump, `oldsrv` moved mgmt→Home, `pi99` lost a `ProxyJump` that could never work, `oldsrv99`/`nas99` re-added as on-site-only). Measured 2026-09-19 from a mobile hotspot with **no `-e` override**: `ssh <alias>` AND `ansible <host> -m ping` green on all four behind-NAT hosts; the group var alone suffices (pong with a stub ssh config holding only `Host vps`); `home_servers.yml --limit oldsrv --check` `unreachable=0` (`failed=1` = the known check-mode breaker, now **HD-399**); the recorded `delegate_to: pi` victim task returns `ok: [oldsrv.kogler.si -> pi.kogler.si]`; `rotate-spark-llm-key.sh`'s `lan-litellm` leg prints a real hash on the default `ssh oldsrv` target. ⏳ **Remaining needs presence, not knowledge:** re-run the matrix from the LAN and with `Mgmt99` link — prove the jump is a harmless no-op for a LAN-attached runner and that `router`/`switch`/`ap-*`/`*99` answer again (no on-site half was taken this cycle: the station was abroad, vNIC Disconnected). Record: [network-vpn.md](docs/network-vpn.md) §Reaching LAN nodes when away |
| HD-369 | 2 | AI | 2 | **RX 7600 = pinned-services tier (STT/embed/rerank); spark = big-model generation tier (decision #24, 2026-09-15)** — reverses decision #23's "sole inference tier" for the *small pinned services*: **Whisper STT (~2 GB) + bge-m3 embed (~1–2 GB) + bge-reranker (~1–2 GB) move to the oldsrv RX 7600** (container-bundled ROCm, ≈5–6 GB of 8 GB, pause-able for gaming — same pattern as immich-ML). **Piper TTS stays CPU** (CPU-only engine, ~0.5 GB, instant). **spark (GB10, 128 GB unified) keeps the big-model generation tier** — one large model with the largest context (Qwen3-Coder-Next-80B, 262k ctx via SGLang). immich-ML stays on oldsrv GPU as **lowest priority** (paused when voice/embed active). Rationale: pinned services are small + latency-sensitive (voice); spark is a 128 GB monster that should do the heavy lifting, not babysit a 2 GB Whisper. Risk accepted: ROCm-on-RDNA3 container-bundled path (immich-ML precedent). ✅ **IaC AUTHORED 2026-09-15 (SSOT updated, NOT converged):** implementation = **ONE Ollama container on oldsrv** (the proven `:rocm` RDNA3 image, `ollama_version: 0.32.15-rocm` verified 2026-08-21) serving **all three pinned services** on the isolated `llm-backend` network — the same pattern the repo already validated (removed-Ollama template re-armed). No custom ROCm images needed (faster-whisper-server has no ROCm tag; TEI ROCm targets Instinct MI200/MI300, not RDNA3; 2sophia/bge-m3-service 404). Models: `whisper-large-v3-turbo` (STT), `bge-m3` (1024-dim embed, Qdrant), `bge-reranker-v2-m3` (rerank). LiteLLM scoped keys `owui-public-rag` + `rag-int-svc` re-pointed from retired Cohere to `ollama/bge-m3,ollama/bge-reranker-v2-m3`. Cohere refs swept from litellm/lan-litellm/qdrant/rag-mcp templates + vault-item map + deployment-secrets. **Ollama entry flipped `enabled: true` 2026-09-15 (gate-clear: spark stable / 262k ctx, big-model tier live)** — oldsrv converge **failed=0, ollama container Up + healthy on `llm-backend`**. ⏳ **Remaining (2026-09-22 sweep — every other item in this row is done, superseded, or measured by the pinned-AI lane):** (d) voice pipeline re-point — Whisper → the **whisper.cpp Vulkan leg on oldsrv (NOT Ollama)**, Piper → CPU, LLM → spark (the STT leg has been live since 2026-09-19); (e) priority glue (Sunshine pause/unpause pinned-AI + immich-ML). **Closed here, never re-run:** the `enabled: true` flip + oldsrv converge and the first-boot model pull (2026-09-15) — the pull is still a **documented first-boot step with no Ansible task**, and the spec's `whisper-large-v3-turbo` + `bge-reranker-v2-m3` **do not exist in the ollama library** (both the pull names and their 2026-09-19 deletion are logged in [services-rejected.md](docs/services-rejected.md)); the ollama-era catalog recreate was **replaced 2026-09-19** by [services-ai.md](docs/services-ai.md) **§4a** (under decision #27 rerank and STT leave Ollama entirely; `ollama/bge-m3` survives only as the embed **fallback rung**); the VRAM/concurrency verify is in the Vulkan tier's record, not here. **⚠ Defect blocking the rerank + STT legs:** ollama `0.32.15-rocm` has **no `/api/rerank`** (**CORRECTED 2026-09-17: this is NOT fixed by a version bump — the earlier “landed in ollama ≥ 0.5.x” claim was wrong**; `v0.34.1` tree grep = 0 rerank hits, issue #3368 still open, PR #11389/#7219 closed-not-merged) and whisper STT cannot be served on this build. **Resolved by decision #25 (2026-09-17), then restacked again by decision #27 (2026-09-18): rerank AND STT move to the ggml/Vulkan family on the same card** — see the pinned-AI research record ([services-ai.md](docs/services-ai.md) §9c) → **HD-391 (authored + shipped 2026-09-19, closed)**. Embed (bge-m3 on Ollama) remains the one verified leg **and is now the fallback rung** under the Vulkan embed leg. ✅ **2026-09-19: the Vulkan tier is LIVE** (HD-391 closed — `whisper`/`reranker`/`embed` Up+healthy on `llm-backend`, gateway rows `local-rerank`/`bge-m3-vk`/`local-stt` answering through `lan-litellm`, tier measured 2475 MiB of 8 GiB, 0 `amdgpu` hang/reset). ✅ **2026-09-19: the two retired ollama blobs are deleted — 1.5 GB freed.** · [services-ai.md](docs/services-ai.md) §9 decision #24 · [hardware-gpu.md](docs/hardware-gpu.md) · [smart-home-voice.md](docs/smart-home-voice.md) |
| HD-387 | 2 | AI | 2 | **Re-measure the thinking control THROUGH the LiteLLM gateway on the pinned image (settles a live contradiction between a recorded measurement and the pinned source)** — HD-382 recorded from a live run: *"`chat_template_kwargs` thinking control survives `drop_params: true` (measured `reasoning_tokens: 0)`"*. Reading the LiteLLM tree on 2026-09-17 contradicts it for the `openai/` provider: `OpenAIUnknownModelConfig` (the class an alias like `openai/spark/…` selects) = `OpenAIGPTConfig.get_supported_openai_params` **+ `reasoning_effort` only**, and that base list carries neither `chat_template_kwargs` nor `thinking_token_budget` nor `top_k` (repo-wide: `chat_template_kwargs` handled by `hosted_vllm`/`together`/`fireworks`, `thinking_token_budget` by Bedrock only). Under `drop_params: true` an unsupported key is **dropped with no error**, so the likely failure mode is thinking silently **ON** — which is precisely the class of defect this repo keeps paying for (engine default thinks; pi-harness.md §2 measured 23–121 hidden `reasoning_tokens`/turn). ⚠ **The source read is NOT authoritative for us:** the pinned image is `v1.83.10-stable`, not `main` — hence measure, don't argue. ⏳ **Probe protocol (run against the LAN instance, master key, no client change needed):** (1) `POST /v1/chat/completions` with `messages` only → record `completion_tokens_details.reasoning_tokens` = **BASELINE**; (2) same + `chat_template_kwargs: {enable_thinking: false}` → if `reasoning_tokens` == BASELINE, the kwarg was **dropped** (gateway NOT thinking-controllable); if ~0, it survives; (3) `thinking_token_budget: 96` with thinking on vs off → compare against the direct-engine numbers in pi-harness.md §2 (96 → 82 reasoning tok vs 121 uncapped) — those two are the control pair that proves the budget, not just the toggle; (4) `top_k: 20` → confirm whether it reaches the engine (pi's `samplingParams` parity claim depends on it); (5) read `litellm`'s own dropped-params log line for the same request as the corroborating evidence. **Interpretation:** dropped ⇒ record it in services-ai.md §9d and require `hosted_vllm` as the provider prefix for any future gateway consumer that needs thinking control; survives ⇒ §9d gets the mechanism that explains it (so a future LiteLLM bump does not silently break it). **Either way the harness route does NOT change** — decision #26 keeps pi.dev direct; what this decides is whether the SIMPLE QUERIER tier (HomeAssistant/Docling/OWUI) may rely on thinking being off through the gateway. · [services-ai.md](docs/services-ai.md) §9d · [pi-harness.md](docs/pi-harness.md) §1b · [hardware-spark.md](docs/hardware-spark.md) · 📋 [`prompt-384.md`](prompt-384.md) |


| HD-383 | 2 | AI | 1 | **LAN LiteLLM bootstrap-keys glue ABORTed rc2 on a stale `dsh` secret — PARKED 2026-09-17 by HD-386 (the consumer it guarded is gone); remediation still owed if the record is ever restored** — `Run LiteLLM bootstrap-keys glue (lan-litellm)` retries 12× then fails: `ABORT(rc2): STALE SECRET for 'dsh'. Clear credential of 'dsh_api' + delete server key 'dsh', re-run.` Meaning: the 1P `dsh_api` credential probes 401 against the **LAN** DB (it is a VPS-DB-era value left in the same item when the records moved hosts in HD-356/370), so `dsh`'s LAN LiteLLM auth is broken today and the vault-first glue deliberately refuses to auto-overwrite a secret (rc-2 design). ⏳ **Owner remediation (documented in the script header, do NOT automate):** clear the `credential` field of 1P `dsh_api` (and `pi-harness_openai_api` if it reports the same) → delete the server-side key with alias `dsh` in the LAN Admin UI (`llitellm.kogler.si`, `/fallback/login`) → re-run `--tags docker_services,lan-litellm` (the glue then mints fresh LAN keys vault-first) → restart `dsh`/`pi-dev` so they re-read the new key. Verify: `/v2/model/info` unaffected; glue exits 0; dsh reaches `lan-litellm:4000`. Note: this failure was invisible before 2026-09-17 because a scope-only run (`-e docker_services_scope=lan-litellm` WITHOUT the service tag) tag-filters the whole per-service task list — see HD-382/scripts-README. **STATE 2026-09-17 (HD-386):** the abort is now UNREACHABLE rather than fixed — the `dsh`/`pi-harness` records are commented out of `group_vars/home_servers.yml` with their removed consumers and `bootstrap_keys: false` on `lan-litellm`, so the glue neither mints nor probes. The stale 1P `dsh_api` credential is therefore still standing in the vault (item deliberately NOT deleted — a parked consumer is not a deleted secret); restore the record only together with the remediation above, in the same change as `bootstrap_keys: true`. · [services-ai.md](docs/services-ai.md) · [IaC/ansible/roles/docker_services/templates/litellm-bootstrap-keys.sh.j2](IaC/ansible/roles/docker_services/templates/litellm-bootstrap-keys.sh.j2) · [home_servers.yml](IaC/ansible/group_vars/home_servers.yml) **✅ AUTHORIZED 2026-09-21 (owner; the service token has write): remediate now — clear the stale `dsh_api` credential, delete the `dsh` key alias in the LAN Admin UI, and drop the item's row in [deployment-secrets.md](docs/deployment-secrets.md) in the SAME change. The glue stays `bootstrap_keys: false` until HD-384's first real consumer record lands.** · 📋 [`prompt-384.md`](prompt-384.md) |
| HD-384 | 2 | AI | 1 | **Grant `spark/*` to the SIMPLE-QUERIER tier through the gateway (the harness half of this question was closed by decision #26)** — `spark/qwen3.8-flash-next` exists in both LiteLLM DBs (HD-382) but `litellm_scoped_keys` allowlists are still `ollama/*,openrouter/*`, so no scoped consumer can reach it. The LAN `litellm_scoped_keys` comment explicitly parks this ("spark/* access for the dev harnesses is a later decision, NOT decided here"), so it was NOT granted by side-effect. Options: (a) add `spark/*` to `dsh` + `pi-harness` in `group_vars/home_servers.yml` (dev-side only, cheap, but every harness turn lands on the single shared ~262k KV pool — see the HD-376 parallel-lane rule + HD-380 incident #6: one 162k-token session ate 56 % of the pool); (b) keep harnesses direct-to-engine (`pi-harness.md` today) and use the gateway only for new consumers; (c) add a per-model budget/rate cap on the allowlist entry. Needs the same-change rule: a synced model's allowlist and its key cleanup move together (`hardware-spark.md` §Model-catalog sync). ⏳ **Re-scoped 2026-09-17 by decision #26:** the harnesses are OUT of this question (they go direct — services-ai.md §9 row 26 + §9d), so what remains is the consumer set that actually uses the gateway: **HomeAssistant + Docling + OWUI-class** — which models each (`spark/*` for generation, `ollama/*` for embed/STT), and `max_budget`/`rpm` per key. Two mechanical prerequisites ride with it: flip `bootstrap_keys: true` back on `lan-litellm` (it is off while the list is empty, HD-386) and mint those items vault-first. Plus the tail decision #26 opened: **harden `spark-llm_api` at the edge** — the credential is currently triple-used (engine `--api-key`, both LiteLLMs' `OPENAI_API_KEY` env, and every direct client), so give the `llm` router its own client credential or an allow-list. ⏳ Ask: which consumers get `spark/*`, with what budget? · [services-ai.md](docs/services-ai.md) §4 · [pi-harness.md](docs/pi-harness.md) · [hardware-spark.md](docs/hardware-spark.md) ⏳ **Prerequisite inventory 2026-09-20 (docs SSOT sweep, read-only):** the LAN instance has **zero scoped consumers today** (`litellm_scoped_keys: []` in `home_servers.yml` — only the VPS DB has them), and Home Assistant has no compose env, no alias and no vault item (HD-403). So the consumer set this row names is built from zero on the LAN side, not trimmed. Read the roster from [services-ai.md](docs/services-ai.md) §4a rather than re-measuring it: the three pinned rows live in the **`lan-litellm`** DB and `spark/qwen3.8-flash-next` exists in **both** — that section is the catalog SSOT. What this row still owes is which of those rows each consumer may use, and at what budget. **✅ DECIDED 2026-09-21 (owner): grant the simple-querier tier the `spark/qwen3.8-flash-next` ROW ONLY — not a `spark/*` wildcard — for HomeAssistant, Docling and OWUI, each with its own scoped key. `max_budget` is DROPPED (owner: owned devices, no metered cost); small `rpm` caps STAY, as contention protection on the single 262k KV pool, not as money. Flip `bootstrap_keys: true` on `lan-litellm` in the SAME change, and mint a separate client credential for the `llm` router so `spark-llm_api` is no longer triple-used. Pure AI from here.** · 📋 [`prompt-384.md`](prompt-384.md) |
| HD-393 | 2 | AI | 1 | **`amdgpu: init_user_pages: Failed to get user pages: -1` on oldsrv — triage ONE bounded episode, not a running leak (re-measured 2026-09-19)** — the box was up **9 d 21 h** and `dmesg` now carries **1,296** of the lines, but **every one of them sits inside a single ~1.8 h window** (`[774316.7]` → `[780804.8]`, i.e. ≈ 21–22 h before the probe), **0 hang/reset lines** exist anywhere in the buffer, and nothing new arrived in the ~21 h since. The ring has **wrapped**, so the 641 lines counted on 2026-09-18 (dated Sep 15→18) were evicted — which is why the raw count grew while the symptom is not actually continuing. Origin is still the existing KFD/compute holders (`ollama :rocm`, `immich-ml`) under memory pressure or a bad GEM handle leak, not Vulkan compute (the probes produced 0 hang/reset over ~25 min sustained). ⏳ **Attribute that one episode** per-process (journal timestamps vs container restarts inside that window), decide benign-vs-OOM-adjacent, and fix or record as accepted noise **before** the pinned-AI tier adds more GPU residents. ⚠ **Method correction:** `rocm-smi` is **not on the host PATH** (ROCm is container-bundled by design — `amd_rocm` is Debian-trixie-native userland only, no `rocm-smi`), so `rocm-smi --showpids` must run **inside** `immich-ml`/`ollama`, not on the host. Capture `dmesg` before the next wrap if the episode is worth keeping as evidence. ✅ **2026-09-19 (HD-391 deploy boundary): this row now has its attribution evidence — ZERO new lines.** The last occurrence is still `[780804.8]` while `/proc/uptime` had passed **875842 s** (~26 h after the episode ended), across the whole pinned-AI deploy + a three-way concurrency load, with **0 hang/reset** anywhere. ⚠ **Method corrections for whoever finishes this:** reading `dmesg` on oldsrv needs `sudo` (`dmesg: read kernel buffer failed: Operation not permitted` otherwise — and an unsprivileged count silently returns **0**, which looks like recovery but is nothing), and the raw count can **fall** (1296 → 1226) purely from ring eviction, so compare **timestamps**, never counts. Remaining work is attribution of the one episode to a process (journal vs container restarts inside 774316–780805 s), not watching for growth. · [hardware-gpu.md](docs/hardware-gpu.md) · [todo.md HD-391](todo.md) |


## 3. Park — deferred / optional / Phase 2

> Items here are not actively worked. They stay visible for planning.

| ID | D | Exec | Item |
|----|---|------|------|

| HD-248 | 3 | AI | **OWUI two-instance split + `msg.`/`chat.` remap (v2) — parked by owner 2026-09-21: no second instance until a second audience exists.** Resume from [services-ai.md](docs/services-ai.md) §Open WebUI, which owns the spec, the live-state contradiction and the banner the old row asked to fix — not from this row. If it is ever resumed: parametrize `open-webui/docker-compose.yml.j2` to render twice, add the `owui-int`/`owui-public` registry entries, and move `element-web` off `chat.`.|
| HD-45 | 3 | AI | **Network dashboard + Homelable — LIVE implementation (re-scoped 2026-09-09, owner):** the owner wants (i) who-is-on-network, (ii) topology map, (iii) a combined dashboard. **Homelable (Pouzor/homelable, MIT, v3.4.1 — network discovery + live health + MCP) to run on oldsrv**; baseline = the existing Grafana Network Clients dashboard (HD-343). A parallel lane is implementing this — do not re-plan. · [observability.md](docs/observability.md) |
| HD-264 | 2 | AI + gate | *(test)* **Renovate sandbox `domen/test` — prove end-to-end tracer bump + PR path before pointing at the real repos** — ⏳ **(Parked 2026-09-03, owner steps.)** (b) run-model CLOSED 2026-08-29 (IaC: `restart: on-failure:5` + `RENOVATE_SCHEDULE` 6h nightly; live-verified RestartCount=0). **Remaining (owner):** external re-launch mechanism (host systemd timer / Ansible cron); commit a real versioned manifest + `renovate.json` to `domen/test` (currently README only) + verify Dependency Dashboard issue + PR lands; then flip `RENOVATE_REPOSITORIES` to the real repo. **Superseded note (2026-09-03):** the GitHub-migration framing is OBSOLETE — the homelab repo lives on internal **Forgejo** (HD-230/220: renovate endpoint `http://forgejo:3000`, `forgejo_api` token); the final flip targets the Forgejo homelab repo, not GitHub. · [deployment-renovate.md](docs/deployment-renovate.md) · [renovate docker-compose.yml.j2](IaC/ansible/templates/docker_services/renovate/docker-compose.yml.j2) |
| HD-250 | 3 | AI + gate | **DeepSeek Harness (DSH) onboarding — PARKED 2026-09-18 with the service** (`dsh` is `enabled: false` per HD-386 and decision #26 routes generation harnesses DIRECT to the engine, so a harness container on oldsrv is no longer the plan). The spec below stays valid if it is ever revived: own thin image, non-root unique uid, `cap_drop: ALL` / `read_only`+tmpfs, netns tailscale serve → 127.0.0.1:3080 (**no socat bridge**), 443 egress open (recorded risk), Forgejo service-account PAT (PR-only, no merge) + branch protection, Kopia scope. ⛔ **Re-open only if** the owner asks for a server-side DevOps cockpit again — and re-check it against decision #26 first. · [services-ai.md](docs/services-ai.md) |

## 3b. Deploy path — Ansible-only (HD-150, supersedes HD-02 Doco-CD)

> **Doco-CD is DROPPED (HD-150).** Deployment + upgrades use **Ansible only** for BOTH VPS and
> oldsrv. The single upgrade path: **Renovate** opens Forgejo PRs → the **Forgejo Actions deploy
> button** (`workflow_dispatch`) runs Ansible (idempotent `docker_compose_v2` re-render/applies the
> merged image-tag) → post-deploy renders Homepage config + inventory. Homepage is a dashboard link to
> the Dependency Dashboard + deploy button (no deploy role). Old HD-02 activation notes removed.

## 3c. Deploy-gated verification — lives in `deployment-tasks.md`

> **Per-phase `Deploy-gated verification` checklists are the authoritative home** for every `⏳` (IaC-done, not-live-verified) row, ordered by the deployment phase (1/1.5/2/3/4/6/8) where each live-verify runs.
> See [`deployment-tasks.md`](deployment-tasks.md) → each phase's `Deploy-gated verification` block. Row detail stays in the owning doc. This section is now a pointer, not a second SSOT.

## 4. Status & dependency notes

> **Swept 2026-09-22:** the dependency notes that named only closed work are gone (HD-50/16/43-46 forward-auth spine, HD-03/07/08 UPS chain, HD-29/31/131 off-site storage, HD-40A/40B/135 Phase-1 VPS edge, HD-13 Homematic cloud). Each is DONE and its record is the owning doc + git history — off-site storage in [storage.md](docs/storage.md) + [backup.md](docs/backup.md), the VPS edge in [services-vps.md](docs/services-vps.md), the forward-auth spine in [services-authentik.md](docs/services-authentik.md).

- **HD-04** is the one dependency still live: the Pi runs Debian + HA Container since 2026-09-03 and the only open half is the **owner failover test** (VIP move Pi→oldsrv + failback) = Pkg E above. Its Homematic leg is **parked/rejected (HD-18, 2026-09-08)** — HmIP-HAP stays in cloud mode.
- 'Implemented, not deployed' rows (any row carrying a ⏳ marker — enumeration deliberately not maintained here; see the module tables) stay open until a live deploy happens — closing requires a deploy/verify pass, not just IaC.

## 5. Conventions quick-reference

> Full, consolidated rule set: [`CONVENTIONS.md`](CONVENTIONS.md) — this section is only the index.

| Area | Rule | Owning doc |
|------|------|-----------|
| Hostnames | single `kogler.si` namespace, flat subdomains | `docs/index.md` Conventions
| IPs | `docs/network-addresses-generated.md` is the SSOT, generated, never hand-edit | `scripts/check_doc_ips.py`
| Secrets | 1Password `Homelab-ansible` vault, `<service>_<type>` naming | `docs/deployment-secrets.md`
| Compose | conventions & port binding policy | `docs/deployment-compose.md`
| Ansible | roles/templates/conventions | `docs/deployment-ansible.md`, `IaC/README.md`
| Service catalog | `group_vars/home_servers.yml` + `docs/services.md` | `docs/services.md`
| Validation | `bash scripts/validate-all.sh` before commit | `scripts/`

## 6. Service onboarding

> Uniform 10-step path for adding a service (exposure/auth → secrets → compose → registry → edge →
> state/backup → observability → validation → deploy gate → docs). Canonical copy:
> **`CONVENTIONS.md` §5 — Service-onboarding checklist**. Reproduced there; new services must clear all 10 steps.
