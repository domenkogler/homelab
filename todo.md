# Homelab TODO Backlog

> Restructured **2026-08-17** (and trimmed 2026-08: struck rows removed → [changelog.md](changelog.md); deploy-gated live-verify moved to [`deployment-tasks.md`](deployment-tasks.md) per-phase blocks). Human decisions moved to the front, work organized into domain modules, deferred items parked. Done items live in [changelog.md](changelog.md); conventions → [`CONVENTIONS.md`](CONVENTIONS.md). Single source for planned work + open decisions (HD-XX).

---

## 0. How this backlog works

- **One row = one outcome.** New work gets `HD-<next>` and links its owning `docs/*.md`.
- **Priority (P):** 1 = highest (how hot), module = what domain it touches. Priority is per-row, modules group.
- **Executors:** `AI` · `AI + gate` (human checkpoint) · `AI + Human` (joint) · `Human` (blocks).
- **Lifecycle:** open → (decided) → done → changelog — ONE pattern (CONVENTIONS §4): a **fully-done** row is DELETED from this file (changelog-only record); an **IaC-done-but-deploy-gated** row STAYS with a `⏳` tail (trimmed to pending items) until the live verify closes it, and still gets its Done changelog row. Deferred → park section. **Decisions:** a decided item is written **once** to `changelog.md` (decision-log SSOT) and the full rationale is **removed** from `todo.md` — §1 shows only still-open decisions/purchases (compact pointer, no struck duplicate).
- **Conventions / onboarding:** consolidated rule index + 10-step service-onboarding checklist in [`CONVENTIONS.md`](CONVENTIONS.md).
- **Service stages:** service-onboarding rows (per-checklist tasks) carry **`Stage: N/10`** in the bold title = current Service-onboarding checklist step ([CONVENTIONS.md](CONVENTIONS.md) §5). `10/10` = deployed + verified; only then does a row close. Non-service tasks (network/fix/decision) carry no stage marker.

## 1. Human decisions & purchases — review first

### Decisions (blocking / waiting on a human call)

> **None open.** Resolved decisions are logged once in [`changelog.md`](changelog.md) (decision-log SSOT) and are **not** duplicated here.
> Recent: HD-22 (meteoblue weather), HD-24 (HA Dashboard), HD-25 (wake word), HD-29 (Hetzner boxes), HD-39 (no watchtower),
> HD-52 (OpenCloud OIDC), HD-122 (Matrix federation), HD-131 (storage architecture), HD-132 (Authentik-LDAP), HD-204 (public-record SSOT incremental publication),
> HD-228 (cloud appliances on VLAN 21), HD-232 (CAPsMAN modern wifi-qcom-ac / wAP retired), R-1 (home-server VLAN Option A — pi/oldsrv dual-homed, nas single-Home) — all in `changelog.md`.


### Purchases

| ID | D | Exec | Item |
|----|---|------|------|
| HD-30 | 1 | Human | *(buy)* **Sign up Infomaniak kSuite** — email, CalDAV, catch-all aliases; ~€3–5/mo; secrets → 1Password `Homelab-ansible`. · [subscription.md](docs/subscription.md) |

## 2. Active work — by module

### 2.1 Network & DNS — VLANs, firewall, DNS, VPN, router/switch

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-03 | 5 | AI + gate | 1 | **Network redo: VLAN segmentation** — flat Home /24 → VLANs 10/20/21/30/40/50/99 + inter-VLAN firewall + CAPsMAN. ✅ IaC committed (`39b9f02`); ⏳ **cutover Phase 1.5** — not live; deploy via Ansible in WSL; open switch bridge-VLAN membership + CAPsMAN SSID secrets; WG S2S VPS peer (IaC) — provision pubkeys + tunnel; reconcile at cutover: Home→IoT new-connections gated by `trusted-admin` (incl. nas) while the narrower `trusted-ha` list gates nothing — audit J8/S20, decide narrow-IaC vs widen-doc when the matrix is verified live. · [network-vlans.md](docs/network-vlans.md) |
| HD-89 | 1 | AI | 3 | **Disable/move unused AP ethernet ports off Mgmt VLAN** — wired devices on AP ports currently get full Mgmt access (KOPS-046). · [network-vlans.md](docs/network-vlans.md) |
| HD-182 | 3 | AI | 2 | **Kids VLAN firewall (HD-179: implement at 1.5)** — ✅ **Done (IaC).** Real rules replace the debug placeholder in `roles/router`: (1) bedtime 22:00–07:00 WAN block for VLAN 40 via the RouterOS **native time-based filter rule** (`time=` param) — deliberately not a `/system scheduler` enable/disable toggle, which a declarative `api_modify` re-run would reset (a 23:00 run would silently lift the block until 07:00) and which would not survive a reboot mid-window; (2) forced filtered DNS — Kids `:53` udp/tcp dst-nat'ed to the Technitium primary (Kids-Group / Cloudflare-Families policy per network-dns.md) + a DoT `853` bypass drop (DoH/443 residual accepted); (3) explicit Kids→Home drop row. DNS-parity fixes in the same change: secondary-resolver (Pi) forward rule mirroring the primary, DHCP now hands out primary → secondary → router tertiary, input-chain accept + `ip dns allow-remote-requests=yes` for that tertiary. Structural fix bundled: the default-deny forward rule merged into the single ordered chain block — the old second `chain=forward` api_modify (`handle_absent_entries: remove`, same restrict) would have deleted every rule above it on each live run. ⏳ **Deploy-gated (Phase 1.5):** live-verify the overnight `time=` span on RouterOS 7, the forced-DNS hijack and the Kids→Home drop; `ip dns` settings-path write needs community.routeros ≥ 2.17 (fails loud on older). Closes audit J6/J7/S19 before Phase 1.5 verification. · [network-vlans.md](docs/network-vlans.md), [network-dns.md](docs/network-dns.md) |
| HD-198 | 2 | AI + gate | 3 | **Public-record SSOT migration (B2)** — ✅ **Decided (HD-204, 2026-08-21): incremental publication (each record added as its service lands).** `roles/cloudflare_dns/vars/main.yml` manages only `vps` + SMTP2Go CNAMEs; the service records (root/home, sso, foto, file, office, ai, git, ha, vpn, matrix, chat per services.md) exist only as comments → live zone and file are dual SSOTs. Extend the list incrementally — one record per service going live (gate: applies go LIVE on Cloudflare) — and render the human mirror as it grows. Closes audit B2/W7. · [services.md](docs/services.md), [network-dns.md](docs/network-dns.md) |

### 2.2 Storage, ZFS & UPS — NAS datasets, NFS, UPS/NUT

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-06 | 3 | AI | 1 | **NUT master on nas** — `usbhid-ups`, `upsd` :3493, `nut_exporter` :9199, `upssched-cmd` notify. ✅ IaC done; ⏳ live deploy on nas (Phase 2) + battery-pull test; feeds HD-07/08. · [hardware-ups.md](docs/hardware-ups.md) |
| HD-08 | 3 | AI | 2 | **UPS metrics + alerts → Prometheus/Grafana** — Critical/Warning/Info tiers. Depends HD-06/07. ✅ Monitoring IaC implemented; ⏳ live-verify `nut_ups_status` bitmask + metric names + Grafana alert-rule provisioning (blocks on HD-06). · [hardware-ups.md](docs/hardware-ups.md) |
| HD-09 | 1 | AI | 2 | **UPS web-UI firewall rule** — open 80/443 Home→Mgmt for the UPS Mgmt address (`ups`, address SSOT) only; Modbus 502 retired. ✅ IaC done; ⏳ not deployed (Phase 1.5). · [hardware-ups.md](docs/hardware-ups.md) |
| HD-26 | 1 | AI | 3 | **Confirm UPS SNMP UDP (161/udp)** — confirmantional only (no consumer; NUT/USB is the monitor). Probe must run from a Mgmt-VLAN (99) host. · [hardware-ups.md](docs/hardware-ups.md) |
| HD-132 | 3 | AI | 2 | **Authentik-as-LDAP for Samba self-service password** — ✅ IaC done (pull-model, `passdb backend=ldapsam`); ⏳ create LDAP provider + outpost, seed `authentik-ldap_bind`, live-verify a family drive mounts (Phase 2). Samba (nas, the client) reaches the VPS outpost over WG at the VPS's WG-side address — no public publish (HD-186). · [deployment-compose.md](docs/deployment-compose.md), [storage.md](docs/storage.md) |
| HD-207 | 3 | AI + gate | 1 | **Execute the NAS Pool-Creation Runbook + migrated-data final home (landing-zone redistribution)** — ✅ **Runbook EXECUTED 2026-08-23** (SMART all-pass pre-check → bulk raidz2 ashift=12 → legacy `new-pool` sent `-R` into `bulk/migrate` → tank mirror 2×4 TB → export both BEFORE installer boots — done, installer-ready; reality deltas: TWO source pools existed, `backup` (61.9 G gen8 dumps) declared disposable by owner and destroyed post-verification; explicit `bulk/migrate` parent create required — receive doesn't make intermediates; full record: changelog + deployment-journal §Phase 1a). ⏳ Remaining: **redistribute the landing zone slowly, then destroy it** — personal documents → OpenCloud/live Box (HD-151 user-files tier), interim park `/tank/data/users/<name>/` Samba per-user drive, media → `bulk/media/*`; landing zone gone before the tree equals the storage-role SSOT at `playbooks/storage.yml` (final media rename vs personal-files decision per storage.md). Preseed refreshed for nas same day (early_command by-id resolver + iwlwifi blacklist); stick re-keyed and nas-ready. · [hardware-nas.md](docs/hardware-nas.md), [deployment-tasks.md](deployment-tasks.md), [storage.md](docs/storage.md) |


### 2.3 Platform & Deploy — Ansible, compose conventions, GitOps, Renovate, Forgejo Actions

| ID | D | Exec | P | Item |
|----|---|------|---|------|

| HD-258 | 3 | AI | 1 | *(speed)* **Bulk 1Password pre-pass — kill the ~150 `op` CLI spawns per VPS converge** — ✅ **Done (IaC 2026-08-26; surgical live-verified).** Replaced per-template `community.general.onepassword` lookups (all docker_services compose/config `.j2`) with one pre-pass that derives the per-host needed item set from its ENABLED docker_services' `template_dir` -> `_template_vault_items` map and fetches each item ONCE concurrently via `scripts/op-vault-export.py --derive` (~34 items in 2.4s, one `op item get --reveal` each). Builds a `vault: {<name> -> {username,password,credential,bcrypt_hash}}` dict; templates read `vault['NAME'].F`. **Fail-closed (HD-91/HD-65): no `default('')`, missing/down item aborts loudly.** Static set before the authentik lane; litellm scoped keys fetched post-litellm-bootstrap. Byte-identical render proven offline (A/B vs HEAD) + live surgical converge on `vps` (scope=headscale) RC=0 (ok=28 changed=4 failed=0), containers healthy. `deploy-service.yml` DB-sync guards stay on direct lookup (rare, dynamic `svc.db_item`). ⏳ **Deploy-gate (full):** a full 28-service `--tags docker_services` converge renders every template from the `vault` dict (op-spawn count recorded via profile_tasks, no container churn vs the prior lookup render). · [deployment-secrets.md](docs/deployment-secrets.md), [deployment-ansible.md](docs/deployment-ansible.md) (Bulk 1Password pre-pass) · depends on: HD-257 (measure first, done 2026-08-26) |
| HD-259 | 3 | AI | 1 | *(speed)* **Run Ansible from WSL ext4 (primary repo source = Debian)** — relocate the session/worktree into WSL home (`/home/domen/source/homelab`, ext4) instead of `/mnt/d` drvfs, killing the 9p I/O tax on var parse/template copy-render/temp writes. **✅ In effect 2026-08-26 (this session runs from the ext4 primary).** `git-bootstrap.sh` + `ansible-run.sh` (derives `$REPO` from own path, sets `ANSIBLE_CONFIG`/`ANSIBLE_ROLES_PATH`) make the ext4 worktree the canonical runner; `deployment-manual.md` + script headers refreshed off the stale `/mnt/d`/9P-sync-gate refs (HD-212 gate demoted to `/mnt/d`-residual only). Watch-outs remain for any Windows-native tool hardcoded to a `D:/…` path. · [deployment-manual.md](deployment-manual.md), [scripts/README.md](scripts/README.md), [scripts/ansible-run.sh](scripts/ansible-run.sh) · depends on: HD-257 (measure before/after) |

| HD-260 | 2 | AI | 1 | *(bug, blocker)* **Fix the restart-on-config-change guard `when` that aborts surgical runs (`dict has no attribute 'item'`)** — ✅ **Done (IaC 2026-08-26; deploy-gated live verify).** Root cause: the extra-.j2 loop uses `loop_var: extra`, so the registered loop results carry the key `extra` (template filename), NOT `item`; `map(attribute='item')` raised `object of type 'dict' has no attribute 'item'` at the tail of a surgical run (live `ok=117 changed=21` then FAILED). Fixed in `deploy-service.yml` to `map(attribute='extra')` + `(extra_templates_result.results | default([]))`. Folded into **HD-255** (same change: the `docker_services_scope` var + guard hardening) — see that row + changelog for the full record. ⏳ live-verify rides HD-255's surgical-converge gate. · [deployment-ansible.md](docs/deployment-ansible.md), [deployment-compose.md](docs/deployment-compose.md) · depends on: none |
| HD-261 | 3 | AI | 2 | *(speed)* **Mitogen trial (only AFTER profile_tasks shows a large executor/transport floor)** — do NOT adopt on a "3–5x" claim; the dominant costs in this repo are op-lookups and drvfs I/O, neither SSH-transport-bound (ControlPersist/pipelining already active). Run as `strategy: mitogen_ssh` in a branch (`playbooks/vps.yml`), measure with profile_tasks (HD-257), watch `become`/community.docker/jsonfile-cache compatibility, keep a one-commit fallback. Gate: only pursue if HD-257/HD-258/HD-259 still leave a big executor floor. · [deployment-ansible.md](docs/deployment-ansible.md) · depends on: HD-257, HD-258, HD-259 |
| HD-262 | 1 | AI | 4 | *(parked)* **Yacht web UI — re-evaluate AFTER the speedups land** — Yacht is an ops-comfort tool, not a speedup; it's a symptom of slow redeploys. Repo-fit concerns (drift + attack surface: an extra VPS web surface binding 127.0.0.1:8001 needing Authentik integration and unaware of `docker-compose@.service` / restart-on-config-change guards). Verdict from the audit: revisit only after HD-257/258/259 make surgical converges ~30–60 s; cheaper alternate is `scripts/docker-restart.sh <service>` or existing `cockpit` container views. · [deployment-ansible.md](docs/deployment-ansible.md), [deployment-compose.md](docs/deployment-compose.md) · depends on: HD-257, HD-258, HD-259 (after speedups) |
| HD-263 | 1 | AI | 3 | *(cleanup)* **Delete the `ansible-enhancements.md` audit deliverable (all items consumed + folded)** — once HD-257 through HD-262 are handled and the audit's durable content has been folded into the owning docs (the Bulk 1Password design → `docs/deployment-ansible.md` §docker_services, the WSL-ext4 workflow → `scripts/git-bootstrap.sh` + `docs/deployment-manual.md`), remove the stand-alone audit file so the speedup work lives only in the backlog/IaC/owning docs, not as a parallel doc that can drift. **Precondition before deleting:** confirm no remaining todo row or committing doc hard-links `ansible-enhancements.md`; the file lives in the primary checkout (`/d/source/domenkogler/homelab/ansible-enhancements.md`, currently untracked/never committed — do NOT commit it as a fixture, just delete it). Apply the repo's own cleanup discipline so no `-enhancements` artifact lingers. · depends on: HD-257, HD-258, HD-259, HD-260, HD-261, HD-262 (LAST — run after all items) |
| HD-230 | 2 | AI | 1 | **Phase-1 wave-2 corrective batch: pairdrop goes PUBLIC (supersedes HD-113) + blueprint one-shot-apply convention + onlyoffice cap_add + db-backup boot-race delay + nftables scoped flush + renovate latent-config fix + disabled-service teardown** — six parallel read-only diagnostics (renovate debug one-shot, onlyoffice recheck, ldap token harvest, db-backup verify, blueprint auto-apply root-cause, nftables IaC fix) then one serial editing pass. **(a)** PairDrop PUBLIC on BOTH `pairdrop.kogler.si`+`drop.kogler.si` (owner decision 2026-08-23), crowdsec-only tier, no forward-auth, traefik-public-only isolation; forward-pairdrop/edge-pairdrop removed from ks-forward-auth.yml (server-side ORM deletes post-converge); stale `pairdrop` CF CNAME KEPT + IaC-tracked in cloudflare_dns vars. **(b)** Blueprint auto-apply gap ROOT CAUSE: discovery never registered /blueprints/custom/* as BlueprintInstances (28 instances, 0 custom) → hash-reapply never fires → deterministic `apply-authentik-blueprints.yml` one-shot step wired into docker_services main.yml (upsert-only; deletions stay ORM). **(c)** onlyoffice-docs: `cap_add` CHOWN/DAC_READ_SEARCH/FOWNER/SETUID/SETGID (supervisord setuid 105 EPERM under bare cap_drop → docservice/converter never spawned → edge 502; rabbitmq class). **(d)** db-backup: `DEFAULT_BACKUP_BEGIN=120` (image default +0 fired dumps same-second-as-boot → mktemp EACCES ×4, zero dumps ever); COMPRESSION kept (deprecated but honored by pinned 4.1.100); kopia source wiring still OPEN (owner decision). **(e)** nftables scoped flush closes the HD-223 residual: ruleset replaces ONLY `table inet filter` (declare+delete+recreate), unconditional docker-restart task removed. **(f)** renovate: repo-root `"platform": "forgejo"` REMOVED (unsupported by pinned 35.x, latent blocker), deployment-renovate.md refreshed to SSOT, service `enabled: false` until owner creates domen/homelab on Forgejo (instance EMPTY — install wizard HD-219 pending; token verified 200). **(g)** New teardown loop for `enabled: false` services (skip-only semantics would leave stacks running outside IaC). ⏳ Deploy-gated: surgical converge + verifies pending. Owner actions surfaced: create/migrate homelab repo on Forgejo; kopia source wiring decision; LDAP HD-132 authoring decisions (base DN/bind mode/TLS/UIDs + authentik-ldap_bind.password decoupling); HD-211 additions: kopia-server htpasswd + repo password (probe-exposed via .Args), grafana contactpoint + datasource passwords. · [deployment-journal.md](deployment-journal.md), [docs/services-traefik.md](docs/services-traefik.md), [docs/services-authentik.md](docs/services-authentik.md), [docs/deployment-renovate.md](docs/deployment-renovate.md) |
| HD-220 | 2 | AI | 1 | **Phase-1 post-convergence incident batch: rotation-drift guard + grafana↔prometheus auth gap + renovate internal endpoint** — 🔨 **IaC in session branch, ⏳ deploy-gated.** Overnight convergence (green ok=264) re-rendered compose with the HD-211-rotated vault values and exposed three live gaps: **(a)** postgres images apply `POSTGRES_PASSWORD` ONLY at first cluster init → rotated `forgejo_db` left the persisted cluster on the OLD role password; forgejo crash-looped (1007 restarts, SASL 28P01) and db-backup DB02/DB04 dumps would fail the same way → new opt-in `db_role_sync/db_item/db_pg_container` metadata (authentik/immich-app/forgejo/pgvector) + `deploy-service.yml` post-up sync task (`docker exec psql ALTER ROLE` from the SAME vault item, no_log, IaC-only — no hand-SQL). **(b)** prometheus enforces basic auth (HD-59) but `grafana-datasources.yml.j2` shipped the Prometheus datasource with NO credentials → every grafana query/alert 401 since first deploy → datasource now carries `basicAuthUser`+`secureJsonData` from `prometheus-internal_api`. **(c)** renovate hit `https://git.kogler.si` which sits behind authentik-forward-auth (SSO redirect ≠ token check) AND forgejo was down anyway → endpoint switched to internal `http://forgejo:3000` (shared services-internal network; Phase-5 Actions runner should reuse the pattern). Same change: role-level tags in vps.yml (surgical `--tags docker_services,monitoring,…` runs — gap noted in deployment-manual How-to-use), services-vps.md vsock known-noise note (decision A), stale "not yet live" banner fix. Owner-side remains: renovate token validity check once forgejo is green (Forgejo audit log); kopia seed (config dir still empty). · [deployment-journal.md](deployment-journal.md), [deployment-ansible.md](docs/deployment-ansible.md), [services-vps.md](docs/services-vps.md) |
| HD-219 | 2 | AI | 1 | **Edge forward-auth wiring (`ks-forward-auth.yml`) + headscale tier correction** — ✅ **Done (IaC + applied live).** Root cause of fleet-wide route 404s: the `authentik-forward-auth@file` chain queried an outpost with NO applications mapped - only OIDC providers existed; the authentik side of the edge was never authored because the chain was dead until the plugin fixes landed. Blueprint declares 9 proxy providers (forward_single; git/stats/sec/auto/pairdrop/pdf/kogler.si/home.kogler.si/traefik-dash + internal backends) + edge applications + embedded-outpost binding. Headscale middleware corrected to `crowdsec-only` (native OIDC; unauthenticated client control traffic /ts2021/DERP must pass; owner-approved). Verified live: all 9 routes 302 -> sso authorize; vpn + ai 200 (crowdsec-only tiers intact). ⏳ Remaining: forgejo install wizard (owner, browser) + renovate/kopia owner steps. · [network-vpn.md](docs/network-vpn.md), [services-traefik.md](docs/services-traefik.md) |
| HD-218 | 3 | AI | 1 | **Phase-1 crash-loop triage — Wave 1 (18 read-only diagnostics) + Wave 2 (batched fixes)** — ✅ **Done (IaC, Wave 1+2 of the owner-approved wave pattern).** All 18 crash-looping containers diagnosed with live log/inspect evidence; fixes applied in one orchestrator pass. Root causes: crowdsec `crowdsecurity/matrix` collection nonexistent upstream (dropped from `crowdsec_collections`; matrix log parsing traded away until a collection exists) + `/staging` tmpfs for its symlink farm · opencloud config bind `:ro` dropped (+ csp.yaml finally MOUNTED, HD-144 gap) · **Class A**: root-owned auto-created bind dirs vs non-root image uids (grafana 472, n8n/openclaw 1000, opencloud storage_uid) → generic `bind_owner_uid`/`bind_dirs` metadata on docker_services entries + pre-create/chown task in deploy-service.yml · **Class B** HD-202 hardening vs image init systems: element-web tmpfs conf.d/nginx pair, pairdrop+db-backup `/run` tmpfs (s6), forgejo app `read_only` removed (s6 lock in /etc/s6), metabase cap_drop+read_only relaxed w/ validator ALLOWED_NO_CAP_DROP entry, stirling-pdf read_only→cap_add CHOWN/SETUID/SETGID · **Class C drift**: matrix tuwunel.toml `[global]` header, immich `DB_HOST`→`DB_HOSTNAME`, headscale `command: serve` + dns/policy schema migration to ≥0.24 keys, renovate platform `forgejo`→`gitea` (image 35.x predates native support), prometheus URL-targets→bare host:port + `prometheus_ha_exporter: false` gate (no HA/token before Phase 4) · kopia-server `entrypoint: []` + /app/logs tmpfs (.env rework verified GOOD) · traefik-certs-dumper: wrapper rewritten into rendered `certs-rename.sh` (post-hook now shelled, dead args gone, idles instead of looping pre-issuance) + wildcard `*.kogler.si` REQUEST added on the dash router issuer-only (nothing ever asked LE for it). Tooling: check-vault-items.sh scans shared top-level templates + VPS group_vars now (blind-spot class fixed; kopia miss had self-resolved via same-day .env rework). ⏳ **Deploy-gated:** Wave-3 combined vps.yml run + container-state re-sample; CF token IP-filter owner action DONE same day (VPS egress whitelisted). HD-211 additions from diagnostic env exposure: GF admin+smtp, N8N encryption+basic-auth, RENOVATE_TOKEN, OPENCLAW_GATEWAY_TOKEN, OPENCLOUD_WEBDAV_PASSWORD, db-backup DB01–04. Follow-up suggestion: -rootless variants for s6 services (re-hardening path). · [deployment-journal.md](deployment-journal.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-211 | 2 | AI + Human | 1 | **Phase-1 secret hygiene: rotation batch + replace placeholder API keys** — the write-scoped provision token transited chat logs on 2026-08-22; the persisted `authentik-provision_api` item has since been RETIRED (HD-143 Option A — glue mints ephemeral tokens; tombstone in [deployment-secrets.md](docs/deployment-secrets.md)), so the old "rotate SA + redeploy token file" step is moot for it; owner rotation now applies only if `vps-op-write_api` (the `/etc/op/provision-token` service account) is ever exposed. **✅ Placeholder swap DONE (owner, 2026-08-26):** real provider keys confirmed in place for `openrouter_api` (LiteLLM LLM generation), `cohere_api` (Cohere embed-v4 multilingual) and `forgejo_api` (renovate token) — all three verified present in the `Homelab-ansible` vault with live values. **HD-216 tail:** every PERSISTED Authentik API token created or touched in this batch MUST be `expiring=False` (recipe: [services-authentik.md](docs/services-authentik.md) *API-token auto-rotation*); include the one-time `expiring=False` verification of `authentik-api_token` (sync-authentik-users glue read token). **HD-252 follow-up (2026-08-26): `headscale_api` rotation COMPLETED** — ✅ done 2026-08-26 (journal Phase 1): `headscale_api.credential` (OIDC client_secret) was exposed in a live log/transcript dump during the HD-252 Lane-D recovery → Authentik pk 13 `client_secret` regenerated → persisted to 1P `headscale_api.credential` → headscale + headplane re-rendered + restarted on the new secret → token-endpoint replay `invalid_grant` + `/health` 200. **headscale+headplane share this OIDC client** — note headplane reads the same `headscale_api` credentials. · [deployment-secrets.md](docs/deployment-secrets.md), [docs/services-authentik.md](docs/services-authentik.md), deployment-journal.md Phase 1 |
| HD-212 | 2 | AI | 2 | **/mnt/d 9P stale-cache risk on the WSL runner** — twice on 2026-08-22 WSL served minutes-old file contents to the runner (provisioner ran an old catalog; a render used an old template) → phantom failures. ✅ Decision (2026-08-22): Phase 1 completion stays on `/mnt/d` (Option A) behind a **mandatory pre-run sync gate** — whole-tree `git ls-files -z \| xargs -0 md5sum --text \| md5sum` compare Windows↔WSL before every playbook run; on mismatch force-invalidate (① drop_caches, ② drvfs remount); if both fail to clear, migrate the runner clone natively into WSL WITHOUT re-asking (pre-authorized fallback, Option B). ⏳ close when Phase 1 completes with no staleness incident (gate proven sufficient) or on Option-B migration. · [deployment-manual.md](deployment-manual.md) How-to-use, [scripts/README.md](scripts/README.md) ansible-run.sh |
| HD-134 | 3 | AI | 2 | **Pin all unpinned image tags + validator guard** — ✅ Done (`_version` pins in all.yml, Renovate-tracked; `validate-docker-services.py` fails on bare `latest` w/ `ALLOWED_LATEST`). ⏳ fluid/obscure services still documented `latest` — pin at first deploy when registry-verified. · [deployment-compose.md](docs/deployment-compose.md), [CONVENTIONS.md](CONVENTIONS.md) §3 |
| HD-159 | 2 | AI | 2 | **blackbox liveness — home↔VPS link tunnel-down as a first-class alert** — ✅ IaC done (`wg_icmp` scrape job probing `wg_s2s_vps.router_ip` + `wg-s2s-down` Grafana rule). ⏳ live-verify at Phase 1 (tunnel up) that the alert fires on a `wg down` test. · [observability.md](docs/observability.md) |
| HD-160 | 2 | AI | 2 | **services-internal sibling auth** — ✅ IaC done (immich-app→immich-ml key; OpenClaw→OpenCloud WebDAV app-password); ⏳ create `immich-ml-internal_api` + `openclaw-opencloud_api` 1P items; verify Immich v3 ML-auth env names; live-verify Phase 1/3. · [deployment-compose.md](docs/deployment-compose.md), [security.md](docs/security.md) |
| HD-205 | 3 | AI | 4 | **`provision-secrets.py` hygiene nits (scripts.md F8)** — pass the bcrypt hash via env/stdin instead of CLI argv (process-list exposure); auto-detect or document the `BCRYPT_PY` launcher for WSL (`py -3` default fails there); delete or expose the dead `gen_wg_key()`; abort `--create` loudly when `op item list` fails (auth/scope) instead of failing per-item; reconcile `network-snmp_api` classification (auto-generated catalog vs human-gated in deployment-tasks §0). Matters at first real secret-provisioning run. · [deployment-secrets.md](docs/deployment-secrets.md), [scripts/README.md](scripts/README.md) |
| HD-229 | 3 | AI + gate | 2 | **Smart-home cross-VLAN firewall delta + CAPsMAN bring-up** — ✅ IaC done 2026-08-23: CoAP reverse exception in `roles/router` forward chain (`src=vlan20 dst-address-list=trusted-ha udp dst-port=5683 accept`, placed after established/related) — required by the four Gen1 RGBW2 units for HA push state/buttons (live-probed 2026-08-23); `trusted-ha` address list gains its first consumer (was created-but-unused drift vs smart-home-failover.md). ⏳ Deploy-gated (Phase 1.5): live-verify CoAP push latency post-cutover + implement/validate CAPsMAN api_modify tasks against the live manager per capsman.yml header (guarded scaffold ships today; five wifi-kogler* items must be created first). · [docs/network-vlans.md](docs/network-vlans.md), [docs/smart-home.md](docs/smart-home.md), [docs/deployment-secrets.md](docs/deployment-secrets.md) |
| HD-255 | 2 | AI | 1 | **`docker_services` surgical-tag filtering broken + extras restart-on-change guard aborts (found in Lane D / HD-252)** — ✅ **Fixed (IaC 2026-08-26); deploy-gated live verify.** **(b)** the extras restart-on-change guard abort: the extra-.j2 loop uses `loop_var: extra`, so registered loop results carry the key `extra` (template filename), NOT `item`; the guard's `map(attribute='item')` raised `Error evaluating conditional: object of type 'dict' has no attribute 'item'` (same defect as HD-260). Fixed to `map(attribute='extra')` + `(extra_templates_result.results | default([]))`, so a CHANGED non-`.env` extra still fires the restart while an absent/empty registration never crashes. **(a)** the inner per-service tags in `deploy-service.yml` DO narrow the task list, but the direct-role `main.yml` tasks (networks, teardown, homepage, cert-pull, authentik-lane) carry `tags: docker_services` and ran on every filtered run (union semantics). Added **`docker_services_scope`** var (role default `all`); a named scope (`playbooks/vps.yml --tags docker_services -e docker_services_scope=<svc>`) gates those platform tasks OFF and collapses the `Deploy each enabled service` loop to that one service -> genuinely single-service converge. Docs rewritten (`§Tags & surgical runs` + canonical invocation + Targeted Mode). Validated (no live deploy): harness reproduced the old `item` crash exact error; corrected guard restarts headscale-style extras (2 non-`.env` changed -> fires). `ansible-playbook --syntax-check` on vps/home_servers/raspberry_pi green; `validate-all.sh` green. ⏳ **Deploy-gate (live):** a fresh surgical converge of ONE service must run only that service's tasks and complete green (e.g. `headscale`). ⚠ Follow-up (HD-252): Authentik ScopeMapping for headscale provider (pk 13) emitting `email_verified: True` in ks-oidc.yml. · [deployment-ansible.md](docs/deployment-ansible.md), [deployment-manual.md](deployment-manual.md), [deployment-journal.md](deployment-journal.md), [changelog.md](changelog.md) |

| HD-264 | 2 | AI + gate | 2 | *(test)* **Make `domen/test` a real Renovate sandbox — prove end-to-end tracer bump + PR path BEFORE migrating GitHub** — Owner decision 2026-08-26: GitHub migration to Forgejo (`domen/homelab`) is the LAST step, deferred until this sandbox is stable + tested. **Current gap (live-probed 2026-08-26):** renovate container is in an exit-0 restart churn (`RestartCount=3392`, policy `restart: unless-stopped`) — it runs once, finds `domen/test` EMPTY (only `README.md`, no manifests), logs `Repository is disabled - skipping` every pass, then exits 0 and is restarted instantly. That's not by design (no `RENOVATE_CRON`/schedule configured); it is a run-once job image mismatched with an auto-restart policy. Also note HD-211 confirmed `forgejo_api` real token in vault = Renovate auth gate already satisfied. **Scope:** (a) commit a real versioned manifest into `domen/test` (e.g. `package.json`/`requirements.txt`/`Dockerfile` with a lagging dep AND a `renovate.json` at repo root so `RENOVATE_ONBOARDING=false` is not a no-op) → proves repo-read + dependency extraction + **Dependency Dashboard issue** + **PR creation + merge** end-to-end (the auth+extraction+PR-permission test we want before GitHub migration; note Renovate needs NO push webhook — it polls on its own cadence, a schedule/cron is optional *when* policy only); (b) fix the run-model: choose long-running sidecar vs systemd-timer/on-demand vs proper `RENOVATE_CREATE_ONBOARD_CONFIG` — kill the per-minute restart churn; (c) verify a PR actually lands + survives a stability hold; (d) only after all green, flip `RENOVATE_REPOSITORIES` back to `domen/homelab` (IaC one-liner, converge) as the final migration step. Own cloud doc: `docs/deployment-renovate.md`. · [docs/deployment-renovate.md](docs/deployment-renovate.md), [IaC/ansible/templates/docker_services/renovate/docker-compose.yml.j2](IaC/ansible/templates/docker_services/renovate/docker-compose.yml.j2)

### 2.4 Services & Edge — Traefik, SSO, service catalog, Matrix, VPS edge

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-252 | 1 | AI + gate | 1 | **Fix Headscale↔Authentik OIDC token exchange (stale client secret in running container; all browser enrollments die 403)** — ✅ Diagnosed 2026-08-26 (read-only probes + upstream source verified): EVERY interactive login fails at the final step — `/oidc/callback` returns 403 `invalid_client` ("Client authentication failed") on all six phone attempts (00:26–00:52 Aug 26) AND the laptop attempt (00:26); zero successful callbacks since the container first came up (Aug 23) — the "Forbidden / You are not authorized..." page users see is Headscale's OWN error template (`handlers.go` renders exactly that heading+message pair for browser-facing 403s). Root cause = rotation-propagation gap (CONVENTIONS §2): `headscale_api` vault credential was rotated/corrected in the HD-233 lesson wave → config re-rendered Aug 24 10:38 with the GOOD new secret (live probe: token-endpoint replay with on-disk creds ACCEPTED — `invalid_grant` on dummy code = client auth OK) → headplane restarted Aug 24 10:47 (shares the same `headscale_api` client; its login works) → **headscale itself was NEVER recycled** (started Aug 23 22:36 CEST, pre-render) → the running process still signs token exchanges with the STALE pre-rotation secret; Authentik rejects. Live work-arounds (both bypass OAuth entirely): laptop manually attached via headplane under hand-created user `domen` (⚠ carries NO provider_identifier link — a future device enrolled under his Authentik identity would auto-create a colliding second account), phone via pre-auth key. ✅ **① FIXED live 2026-08-26 (Lane D session):** root cause confirmed — headscale container started 2026-08-23T20:36Z (pre-rotation, `RestartCount 8`, stale secret in process memory) while the on-disk config.yaml already carried the good 128-char rotated secret (block scalar correct); `docker compose up -d --no-deps headscale` was a NO-OP (spec unchanged) so the container was force-recreated (`up -d --no-deps --force-recreate headscale`) → `Started 2026-08-26T14:05:00Z, RestartCount 0`; **verified live:** token-endpoint replay with on-disk creds now returns `invalid_grant` on a dummy code (was `invalid_client` — client auth OK), `/health` 200, `/api/v1/node`+`/user` 200 (headplane polling), zero log errors, laptop node `Domen_P14s` reconnected `online` at 14:05:03 right after the recycle. ⚠ New finding (why the Aug-24 render never recycled it + blocker for the documented surgical converge): `--tags "docker_services,<service>"` does **NOT** filter per-service as documented — a `docker_services,headscale` run executes ALL 40 services' tasks (include-tag inheritance beats inner per-service tags), and the extras restart-on-change guard (`deploy-service.yml` `Restart ... to apply changed extra config`) aborts on `selectattr('item')` when a service in the loop has no registered template result (dict without `item`) — tracked as new HD-255. ② **DONE (2026-08-26): first-ever successful OIDC callback + phone enrolled.** Owner added the phone via the Tailscale app; it landed as node 3 `Naprava A54` under the OIDC-linked user `domen@kogler.si` (logs: `POST /register/confirm/… hskey-authreq… 200` then `node connected machine.key=[ciDr+] node.name=“Naprava A54” user.name=domen@kogler.si` at 17:44:06Z). The earlier fix path: after the recycle fixed `invalid_client`, the attempt hit `401 unverified email` — Authentik's default `email` scope mapping hardcodes `email_verified: False` × headscale v0.29.3 default `oidc.email_verified_required: true` → **FIXED live** by `oidc.email_verified_required: false` in `/opt/headscale/config.yaml` + the IaC template (family-trusted users behind `allowed_domains: kogler.si`; strict-default-preserving Authentik-side fix tracked in HD-255). Headscale restarted, `/health` 200. ③ **RESOLVED (owner action):** owner deleted the hand-created user ID 1 in Headplane (had no email/provider_identifier) → the OIDC-linked user ID 2 `domen@kogler.si` is now the only user. ⚠ **Cleanup side-effect to redo:** deleting the old user ALSO removed the laptop node (node 1 `Domen_P14s`) and the owner separately deleted the just-added phone (node 3) — **nodes list is currently EMPTY**, both devices must be re-enrolled via the (now-working) OIDC flow and they'll land under `domen@kogler.si`. ④ **DONE (2026-08-26): ACL tightened at first enrolment.** `policy.hujson.j2` interim `*:*` → deny-by-default user-based ACL: `{"action":"accept","src":["domen@kogler.si"],"dst":["domen@kogler.si:*"]}` (single family user; add one rule per member as they join). Applied to live `/opt/headscale/policy.hujson`, validated `headscale policy check --bypass-grpc --file` = "Policy is valid" (resolves domen@kogler.si against the live DB), headscale restarted; `policy get` + `/health` 200, **0 denials**, phone node reconnected **online** under the tightened policy (laptop currently client-disconnected, not a policy effect). `tagOwners` stays empty (headscale v2 needs tags declared before ACL use; no autogroup:admin), so ACLs are user-email-based; tag-based `tag:kogler` documented as the later model. All Lane-D objectives (①②③④) closed. · [network-vpn.md](docs/network-vpn.md), [deployment-oidc.md](docs/deployment-oidc.md) |
| HD-241 | 2 | AI + gate | 3 | **Metabase post-setup housekeeping + SMTP via compose env (smtp2go)** — ✅ **IaC done 2026-08-25.** First-boot wizard walked manually 2026-08-24 and recorded in services-admin.md Notes (admin `admin@kogler.si` alias, `metabase_login` vault item, tracking OFF, HTTPS-redirect OFF, forward-auth logout verified). SMTP is env-driven, NOT manual: `MB_EMAIL_SMTP_HOST/PORT/SECURITY(starttls)/USERNAME/PASSWORD` + `MB_FROM_ADDRESS notify@kogler.si` in the compose template — shared `smtp2go_host/port` SSOT (HD-54) + shared `smtp_login` creds via 1Password lookups in `>-` scalars; env-set settings override+lock the UI fields (IaC = single SSOT). Ride-along landed: interfaces.md stale "OIDC" wording fixed to Forward-Auth per HD-148; first-boot runbook captured in services-admin.md Notes. ⏳ **Deploy-gate:** surgical converge (`--tags "docker_services,metabase"`) → Admin → Email → *Send test email* → invite flow works. Known accepted gap: `metabase-data` H2 volume has NO backup coverage until the HD-238 DR tail — keep sandbox data disposable. · [services-admin.md](docs/services-admin.md), [interfaces.md](docs/interfaces.md) |
| HD-242 | 2 | AI + gate | 2 | **Metabase data sources — CrowdSec dashboard + sandbox feeds** — ✅ **IaC done 2026-08-25 (①②).** ① CrowdSec SQLite: `/srv/docker/crowdsec/db` bind mounted into the metabase container (RW deliberately — SQLite WAL/journal recovery must write after unclean crowdsec shutdown; Metabase only SELECTs; official-dashboards pattern) → connection type SQLite at `/var/lib/crowdsec/data/crowdsec.db`. ② Forgejo Postgres: metabase joins `db-internal`; NEW generic opt-in `db_ro_sync/db_ro_item/db_pg_container` in deploy-service.yml (sibling of the HD-220 drift guard) idempotently CREATEs a READ-ONLY role then re-applies password + schema-public SELECT grants incl. default privileges EVERY converge → vault rotation propagates automatically; registry entry wired to forgejo-db with new vault item `metabase-forgejo_ro` (username `metabase_ro`, added to provision-secrets.py catalog, rotation-safe by design); contract documented in deployment-ansible.md. ③ Zipline Postgres: same pattern, ⏳ sub-gated on HD-112 first deploy (DB05). ⏳ **Deploy-gate:** owner seeds `metabase-forgejo_ro` FIRST (`provision-vault.sh --create` or manual 1P — fail-loud render otherwise) → converge → connect both sources → import official CrowdSec dashboard JSONs → verify SELECT-only (write attempt must fail). · [services-admin.md](docs/services-admin.md), [deployment-compose.md](docs/deployment-compose.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-243 | 2 | AI + gate | 4 | *(option, parked)* **Metabase LDAP auth vs Authentik outpost** — OSS-included LDAP would let Authentik users log in with directory creds, but NOT real SSO: second login form after the forward-auth edge, no MFA on the LDAP leg, superuser stays local-first. Also needs an outpost-binding decision (HD-186: outpost deliberately WG-S2S-bound for Samba; metabase container reaching it = nftables input allow or outpost network extension). Wiring sketch (when triggered): dedicated non-expiring bind service account per HD-132 lesson, new `metabase-ldap_bind` item, `MB_LDAP_*` compose env, group sync → Metabase groups. **Trigger to revisit: second regular human user needs Metabase access.** Owner decision 2026-08-25: keep local-admin-only until then. · [services-admin.md](docs/services-admin.md), [docs/services-authentik.md](docs/services-authentik.md) |
| HD-181 | 3 | AI + gate | 1 | **Single cert issuer (HD-178): `acme_issuer` flag + doc alignment** — ✅ **Done (IaC).** Decided (HD-204): oldsrv pulls its cert pair from the VPS via its own timer (second consumer, direct pull, no relay). `traefik_acme_issuer` flag (role default false; true ONLY in group_vars/vps.yml): ACME env/flags/`./acme` volume + certs-dumper render on the VPS only; consumers mount the synced pair read-only and serve TLS from the DEFAULT store (new `traefik/dynamic/tls.yml.j2`, renders comment-only on the issuer). All 30 `certresolver` label refs wrapped (`{% if traefik_acme_issuer is defined and traefik_acme_issuer %}`); file-provider routes fixed: traefik ha route conditional (consumer `tls: {}`), cockpit routes dropped the dead resolver (latent Pi-edge bug class), traefik-ha already consumer-style (comments updated). Cert-sync retargeted: Pi `ha-cert-sync` SRC → VPS `ansible_host` (hostvars SSOT); NEW oldsrv `traefik-cert-pull` script+units in docker_services role (gated to non-issuer traefik hosts). Validator BASE_CTX pins the issuer path; both paths YAML-verified by hand-render. Docs de-⏳: services-traefik, services-vps, smart-home-failover, deployment-tasks ×2, IaC/README, all.yml comments. ⏳ **Deploy-gated:** VPS first issuance; authorize consumer pull keys on the VPS (Pi ha-sync + oldsrv traefik-cert-sync pubkeys); verify Pi/oldsrv sync timers deliver a valid pair. Closes audit J3/C1/W11. · [services-traefik.md](docs/services-traefik.md), [services-vps.md](docs/services-vps.md) |
| HD-183 | 3 | AI + gate | 1 | **Homepage → VPS (HD-180)** — move `homepage` from `group_vars/home_servers.yml` to `vps.yml`; add public root+`home` route on the VPS edge; replace the oldsrv docker.sock health-check with widget/probe-based reachability; fix `HOMEPAGE_ALLOWED_HOSTS` to include the `home.` alias. — ✅ **Done (IaC).** homepage moved to the VPS edge tier (after authentik; Forward-Auth consumer, not native OIDC). Route satisfied by the move itself via the existing Docker-provider labels — audit J10's stated mechanism; `routes.yml.j2` deliberately untouched (file provider stays non-container-only; duplicate routers would be a defect; interpretation supervisor-approved 2026-08-21). `HOMEPAGE_ALLOWED_HOSTS` now carries the `home.` alias; docker.sock mount dropped → widget/probe-based reachability (siteMonitor/blackbox — HD-180 consequence); homepage_services.yaml.j2 header re-pointed at `vps.yml`; services.md + interfaces.md aligned. ⏳ **Deploy-gated:** first VPS deploy renders homepage there and serves `kogler.si`/`home.kogler.si` through the VPS edge. Closes audit J10/A6/W9. · [services.md](docs/services.md), [interfaces.md](docs/interfaces.md) |
| HD-184 | 2 | AI | 1 | **Fix immich-app→immich-ml URL (cross-host)** — ✅ **Done (IaC).** `immich_ml_bind`/`immich_ml_url` derived from the SSOT (`oldsrv_home_ip` pattern) in `group_vars/all.yml`; immich-app env now targets the oldsrv Home IP over WG (was unresolvable container name); immich-ml publishes `:3003` bound to that Home IP (WG AllowedIPs + RouterOS ACL scope, never 0.0.0.0); `IMMICH_HOST: 0.0.0.0` inside the container confirmed; validator render context gained the matching mocks. ML key auth untouched (HD-160). ⏳ **Deploy-gated:** live-verify ML round-trip from the VPS over WG (Immich smart-search job completes). Closes audit J1/D1. · [services-ai.md](docs/services-ai.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-141 | 4 | AI + gate | 1 | **(epic) Authentik OIDC provisioning — Blueprint + secret-egress glue** — declares OIDC providers/apps in a Blueprint; a glue copies generated client creds → 1Password `lookup()`. Sub-epics: A HD-142 · B HD-143 · C HD-144 · D HD-145 · E HD-146 · F HD-147. · [services-authentik.md](docs/services-authentik.md) |
| HD-142 | 4 | AI | 1 | **Authentik Blueprint `ks-oidc.yml` + `blueprints/` volume** — ✅ Blueprint written (OpenWebUI/Headscale/Matrix/OpenClaw/OpenCloud, Confidential + authz-code; creds not pre-seeded); volume mounted. ⏳ deploy-verify (HD-147): version-specific attrs. · [services-authentik.md](docs/services-authentik.md) |
| HD-143 | 3 | AI | 1 | **Secret-egress glue + `authentik-provision_api` token** — ✅ Glue written (harvests per-provider client_id/secret → 1Password, idempotent, fail-loud); wired into `deploy-service.yml`. ⏳ needs write token; NOT the opencloud-service_api job (HD-145). · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-144 | 4 | AI | 1 | **OpenCloud native-OIDC switch (HD-52)** — ✅ IaC done (OIDC env uncommented, Forward-Auth → `crowdsec-only@file`, CSP overlay). ⏳ deploy-verify (HD-149): CSP + desktop/mobile OAuth. Depends HD-142+143. · [deployment-compose.md](docs/deployment-compose.md), [services.md](docs/services.md) |
| HD-145 | 3 | AI | 2 | **`sync-authentik-users.sh` rework — Authentik-driven OpenCloud lifecycle** — ✅ IaC done (least-priv `opencloud-service_api`, best-effort ensure). ⚠ JIT-vs-preseed deploy-verify (HD-149). · [storage.md](docs/storage.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-146 | 2 | AI | 2 | **Wire `vps.yml` deploy order + IaC comments** — ✅ done (authentik precedes OIDC consumers — constraint comment; stale comments → Blueprint+glue). · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-147 | 2 | AI + gate | 1 | **Live deploy-verify** — run VPS playbook; confirm Blueprint + glue seeds all 8 OIDC items; OIDC login verify for ai/vpn/matrix/claw/cloud/foto; Forgejo register (HD-148); re-render inventory. · [services-vps.md](docs/services-vps.md) |
| HD-148 | 4 | AI + gate | 1 | **Extend OIDC Blueprint → Immich + Forgejo + Metabase** — ✅ IaC done (immich switched native; forgejo kept Forward-Auth w/ OIDC-source pending; Metabase reverted — OSS has no OIDC). ⏳ deploy-verify (HD-149). · [services-authentik.md](docs/services-authentik.md) |
| HD-149 | 2 | AI | 1 | **Verify Authentik `ks-oidc.yml` blueprint attrs vs source** — ✅ Verified (flow slugs, signing_key, sub_mode, app binding via `!KeyOf`; scripts/validate_blueprints.py added). ⏳ deploy-gated (Phase 1, HD-40A): live-apply on 2026.5.6 + glue harvest. · [services-authentik.md](docs/services-authentik.md) |
| HD-122 | 2 | AI | 2 | **Matrix federation default (open + hardening)** — ✅ decided + IaC (profile/auth + room-dir hardening; trusted_servers = key-notary not permit-list). ⏳ live-verify profile auth at first deploy (HD-46). · [services-matrix.md](docs/services-matrix.md), [security.md](docs/security.md) |
| HD-40A | 3 | AI + Human | 1 | *(Phase 1)* **Provision VPS + public Traefik edge** — ✅ Provisioned (IP `159.195.111.66`, specs); edge IaC enabled (`b070f7d`). ⏳ remaining deploy-gated: run `vps.yml`, wildcard cert (DNS-01), `sso` record, CIFS mount; WG S2S home↔VPS. · [services-vps.md](docs/services-vps.md), [deployment-tasks.md](deployment-tasks.md) |
| HD-40B | 3 | AI | 1 | *(Phase 1)* **Migrate public-facing services to VPS** — Authentik/OpenCloud/Forgejo/Grafana + AI stack; DBs stay LAN over WG; storage per HD-135. Folds into `enabled:` split. · [services-vps.md](docs/services-vps.md) |
| HD-135 | 4 | AI + gate | 2 | **VPS/oldsrv service split (+ storage/backup transport)** — ✅ IaC matrices applied (`b070f7d`/`d805f6d`); WG S2S role complete (VPS `77833f1`/router `85ba6dc`). **HD-135b (2026-08-28): VPS self-observability folded here** — VPS runs its own Alloy (loopback) + Dozzle (`logs.kogler.si` moved from oldsrv; old entry disabled tombstone); SPOF narrowed to *home* telemetry only. **HD-135b follow-up: observability is INTERNAL-ONLY + tailnet-only** — public CNAMEs for stats/sec/traefik/auto removed from `cloudflare_dns/vars/main.yml` (deploy-gated: owner deletes the live records in Cloudflare), `tailscale-sidecar` Pattern-B skeleton added (disabled; seed `tailscale-sidecar_api` + enable + converge), headscale ACL `tag:sidecar` added. ⏳ remaining live: provision both peer pubkeys, live-Box CIFS mount, cross-host wiring, `foto`/`file`/`git`/`ai` round-trip over tunnel; **HD-135b deploy-verify**: scoped converge `--tags docker_services -e docker_services_scope=dozzle` + `--tags monitoring` on VPS → `logs.kogler.si` serves + VPS Alloy ships VPS container logs to Loki (labels appear). · [services-vps.md](docs/services-vps.md), [storage.md](docs/storage.md), [backup.md](docs/backup.md), [observability.md](docs/observability.md), [network-vpn.md](docs/network-vpn.md) |
| HD-43 | 3 | AI | 3 | **Deploy/verify Media *arr stack** — Stage 4/10; templates in `home_servers.yml` (jellyfin, *arr, qbit+gluetun, …); deploys oldsrv bulk/media NFS. · [services.md](docs/services.md) |
| HD-44 | 2 | AI | 3 | **Deploy/verify new ops services** — Stage 3/10; `dozzle` + `traefik-ha` VIP edge on the Pi (added but not verified). · [services.md](docs/services.md) |
| HD-46 | 4 | AI | 3 | **Implement Matrix IaC (native-only)** — Stage 4/10. ✅ IaC done + committed (`62e0045`; Tuwunel + Element Web, Authentik OIDC, closed registration, RocksDB). ⏳ hosts unprovisioned; needs HD-47 records + OIDC provider/redirect. No bridges in Phase 1 (HD-48). · [services-matrix.md](docs/services-matrix.md) |
| HD-47 | 2 | AI | 3 | **Matrix public records + Federation** — publish `matrix`/`chat` (Cloudflare DNS-only) + `_matrix` well-known/SRV delegation; WAN 443 (8448 optional); no Forward-Auth on `/_matrix/*`. · [services-traefik.md](docs/services-traefik.md), [network-dns.md](docs/network-dns.md) |
| HD-58 | 3 | AI | 3 | **Stirling PDF service** — Stage 4/10; ✅ IaC done (pinned `stirling_pdf_version`, OCR `slv`, Forward-Auth, internal-only). ⏳ re-render `services-inventory-generated.md`; live-verify OCR + Forward-Auth chain. · [services.md](docs/services.md) |
| HD-112 | 4 | AI | 2 | **Zipline public bin + private OIDC file share** — Stage: 9/10. ✅ Decided + IaC done 2026-08-24 (v4.7.0 pin; local datasource VPS NVMe; ONE public host `bin.kogler.si` on `crowdsec-only@file`; dashboard native-OIDC via `ks-oidc.yml` `provider_zipline` + glue-seeded `zipline_oidc`; guestbin Zipline-local user owns `dropzone` folder for anonymous no-login uploads w/ hardcoded `x-zipline-deletes-at: 6h`; NO type blockers, generous global cap — public bounded by guestbin QUOTA + TTL; uploads Kopia-excluded, DB = db-backup DB05; CNAME `bin` tracked). ⏳ **Deploy-gated (Phase-1 wave):** first deploy human-gated; post-up seeding per the compose-header runbook (local admin → OIDC login verify → flip bypass-local-login in Server Settings → create `guestbin` user w/ small quota + `dropzone` folder `allowUploads=true`); anonymous upload→short-URL→viewer round-trip logged-out + 6h sweep verify; family drop script (folder-id capability secret → 1Password); family guide entry (`docs/manual/`). · [services-utilities.md](docs/services-utilities.md), [storage.md](docs/storage.md) |
| HD-113 | 2 | AI | 3 | **PairDrop P2P file share** — Stage 4/10; ✅ IaC done (pinned `pairdrop_version`, Forward-Auth, internal-only). ⏳ re-render inventory; live-verify WebRTC/signaling through Traefik (may need STUN/TURN). · [services.md](docs/services.md) |

### 2.6 Smart Home — HA primary/standby, Homematic, voice, devices

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-04 | 5 | AI + gate | 1 | **Pi redo: HAOS → Debian + HA Container + Technitium secondary** — in-use migration; approved direction, not yet applied. ⏳ Homematic-local deferred: keep HmIP-HAP cloud mode, no HmIP-RFUSB buy yet (HD-13 parked). · [home-assistant-current.md](docs/home-assistant-current.md) |
| HD-185 | 3 | AI + gate | 1 | **Fix Pi first-deploy ordering (docker_services vs home_assistant)** — ✅ **Done (IaC).** Decided HD-204: option A render-first — `home_assistant` now runs BEFORE `docker_services` in `playbooks/raspberry_pi.yml` (supersedes KOPS-063/HD-117 on this host; supersession recorded in the playbook + role comments). Belt-and-braces regular-file guard in `deploy-service.yml`: before every `compose up`, asserts `config/configuration.yaml`, `keepalived.conf`, `secrets.yaml` exist as regular files under `/opt/home-assistant-primary/` and fails loud with the empty-directory remediation path. deployment-ansible.md §Implementation Order step 7 aligned. ⏳ **Deploy-gated:** first real Pi deploy (Phase 4) renders configs before `compose up` and passes the guard; `secrets.yaml` render task is still pending (tracked) — the guard fails loud until it lands. Closes audit J2/D2. · [smart-home-failover.md](docs/smart-home-failover.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-14 | 2 | AI | 2 | **Export HA entity list** — enable HA Prometheus exporter; needed for HA Dashboard `lovelace` + Grafana. Wait for observability. · [smart-home.md](docs/smart-home.md) |
| HD-17 | 3 | AI | 2 | **Single failover button + `ha-failover.sh`** — ✅ IaC done + committed (`ha-failover.sh` fwd/reverse, keepalived failover configs, trigger API + systemd, Homepage buttons). ⏳ not deployed; needs `ha-failover_api` + HmIP-RFUSB moved at runbook. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-217 | 1 | AI | 2 | **Homepage failover-button gating (`homepage_failover_button`)** — ✅ **Done (IaC; owner sign-off 2026-08-22, Phase 1 resume).** The monitoring role's homepage services template hard-referenced `failover_api_url` (undefined anywhere) + a vault lookup of `ha-failover_api` (HD-17/Phase-4 scope, not in the vault) -> hard halt at the first post-docker_services playbook run. Decided: the two HA cards render ONLY when group_var `homepage_failover_button: true` (group_vars/vps.yml); stays **false** until HD-17 goes live (`ha-failover_api` seeded + HmIP-RFUSB stick moved), then flips true - unblocks monitoring without pretending Phase-4 features are live. ⏳ Deploy-gated: next green vps.yml run renders homepage with the gate off. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-18 | 2 | Human | 2 | **Once: test HmIP-RFUSB pairing transfer** + entity reconstruction across stick move. Hands-on; **blocked** until the HmIP-RFUSB is bought (HD-13 parked). · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-124 | 3 | AI | 2 | **Keepalived hardening** — ✅ IaC done + constraint documented (pinned `keepalived_version: 2.3.4`; `auth_type PASS` is the protocol MAXIMUM — accepted w/ Home-VLAN isolation). ⏳ deploy-gated (hosts unprovisioned). · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-21 | 1 | AI + Human | 3 | **Confirm ESPHome / Guition ESP32-S3 status** — `esphome` not loaded; 2026-08-21 HAOS harvest: `lsusb` = hub-only (no USB serial device), no esphome add-on among the 3 installed — remaining check is network-side pairing / owner memory. · [home-assistant-current.md](docs/home-assistant-current.md) |
| HD-23 | 1 | AI | 3 | **Confirm HmIP-SWO-B channels** — no rain / wind-direction on this sensor; verification only. · [smart-home.md](docs/smart-home.md) |
| HD-27 | 4 | AI + Human | 3 | **Voice pipeline build-out** — Whisper → Ollama → Piper containers, flash ESP32-S3 (ESPHome + microWakeWord), HA Assist on phones; GPU + physical flashing. · [smart-home-voice.md](docs/smart-home-voice.md) |

### 2.7 AI & Office — LLM stack, Open WebUI, office MCP, vector DB

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-100 | 4 | AI | 2 | **AI stack: LiteLLM spine** — Stage 4/10; ✅ IaC done (compose on services-internal+llm-backend; config model_list = Ollama/OpenRouter/Cohere; SQLite for keys/spend). ⏳ create `litellm_master_key`/`openrouter_api`/`cohere_api`; MUST pin `litellm_version`; live-verify completion + embed. · [services-ai.md](docs/services-ai.md) |
| HD-101 | 4 | AI | 2 | **AI stack: Open Web UI** — Stage 4/10; ✅ IaC done (compose, `ai.kogler.si` route, Authentik OIDC + crowdsec, LiteLLM backend); secrets seeded; owner admin account live (local login, creds in 1P). **OIDC dead-on-arrival root cause FIXED (2026-08-25):** the compose template shipped invented env names (`ENABLE_OIDC`/`OIDC_ISSUER`/`OIDC_CLIENT_*`/`OIDC_REDIRECT_URI`) that open-webui never reads (verified against v0.11.0 `backend/open_webui/config.py` — provider registers only from `OPENID_PROVIDER_URL` + `OAUTH_CLIENT_ID/SECRET`) → no SSO button ever rendered; template reworked to the real vars + posture hardened (`ENABLE_SIGNUP=false` closes public local signup on the public host, `ENABLE_OAUTH_SIGNUP=true` + `DEFAULT_USER_ROLE=pending` default for family self-service, `OAUTH_MERGE_ACCOUNTS_BY_EMAIL=true` links the existing local admin). deployment-ai-stack-secrets.md var names aligned. ⏳ **Deploy-gated:** surgical converge of `open-webui`; verify SSO button → Authentik round-trip → local-admin linked by email; then LiteLLM completion + RAG. · [services-ai.md](docs/services-ai.md) |
| HD-102 | 3 | AI | 2 | **AI stack: PGVector DB + backup** — Stage 4/10; ✅ IaC done (compose + DB04 backup block). ⏳ create `pgvector_db`; live-verify extension init + db-backup DB04. · [services-ai.md](docs/services-ai.md) |
| HD-103 | 3 | AI | 2 | **AI stack: Docling OCR** — Stage 4/10; ✅ IaC done (CPU-only compose, v1 API, backend-only). ⏳ first start downloads HF models; live-verify Slovenian scan conversion. · [services-ai.md](docs/services-ai.md) |
| HD-104 | 4 | AI | 2 | **AI stack: OpenClaw agent + integrations** — Stage 4/10; ✅ IaC done (compose, gateway; wire to LiteLLM + OpenCloud WebDAV at deploy). ⏳ run `openclaw onboard` → `openclaw.json`; live-verify round-trip. v2 (2026-08-26): register as LiteLLM model on the INTERNAL OWUI instance only — public keys exclude agents. · [services-ai.md](docs/services-ai.md) |
| HD-105 | 2 | AI | 2 | **AI stack: secrets + wiring** — Stage 1/10; ⏳ **pre-deploy gate:** create the 7 1Password items + Authentik OIDC providers per [deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md) — blocks HD-100→104. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-111 | 4 | AI | 2 | **Office MCP via Open WebUI** — Stage 1/10; `ppt-mcp` first then Word/Excel; register MCP servers in Open WebUI; retires AnythingLLM + LocPilot (HD-107 path). · [services-office.md](docs/services-office.md), [services-ai.md](docs/services-ai.md) |
| HD-246 | 3 | AI | 2 | **AI stack: RAG retrieval wiring (decided 2026-08-25)** — implement the services-ai.md §5 decided-parameter table (decision record: changelog HD-246): **(a)** `litellm/config.yaml.j2` — fix the embed entry to `cohere/embed-v4.0` @1536 (template currently mis-pins `embed-english-v3.0` under the name `cohere/embed-v4`) + add the rerank entry (`cohere/rerank-v4.0-pro`; verify the exact id resolves against the pinned litellm_version at render); **(b)** `open-webui/docker-compose.yml.j2` — `ENABLE_PERSISTENT_CONFIG=false` + env pins: docling extraction (`CONTENT_EXTRACTION_ENGINE=docling`, `DOCLING_SERVER_URL=http://docling:5001`), embedding via LiteLLM openai-compat (`RAG_EMBEDDING_MODEL=cohere/embed-v4.0` → `http://litellm:4000/v1`, key = `litellm_master_key`), external reranker (`RAG_RERANKING_ENGINE=external`, `RAG_EXTERNAL_RERANKER_URL=http://litellm:4000/rerank`, same key, timeout set), hybrid ON (`ENABLE_RAG_HYBRID_SEARCH=true`, `RAG_HYBRID_BM25_WEIGHT=0.5`), `RAG_TOP_K_RERANKER=20` → `RAG_TOP_K=5`, threshold 0, `RAG_TEXT_SPLITTER=token` + `CHUNK_SIZE=512` / `CHUNK_OVERLAP=64`; secret-valued envs as folded `>-` scalars (HD-233/166 rule); **(c)** idempotent PGVector HNSW index task (`vector_cosine_ops`, 1536 dims; `no_log`; no hand-SQL per HD-220) gated on OW schema init; **(d)** live-verify: Slovenian scan JPG → Docling markdown → hybrid+rerank answer round-trip + one rerank-endpoint curl probe. v2 addition (2026-08-26): Family Manuals KB ingested on BOTH instances (same source folder; dual re-index step folds into (c)); public instance corpus RESTRICTED to that one KB. ⏳ all steps pending (converge + live verify). · [services-ai.md](docs/services-ai.md), [deployment-journal.md](deployment-journal.md) |
| HD-247 | 3 | AI | 1 | **LiteLLM spine hardening cutover — scoped keys + models-in-DB** - ✅ **Done (IaC 2026-08-26).** Registry-verified pin `litellm_version: v1.83.10-stable` + `litellm_db_version: 16.15-alpine`; bundled litellm-db Postgres sidecar (db-internal only) + `STORE_MODEL_IN_DB=true` (models become Admin-UI-managed; git drift accepted by owner decision); `model_list` removed from config.yaml.j2; DATABASE_URL folded `>-` w/ urlencode; db-backup DB06 + backup.md critical-state row (keys/spend/models live in litellm-db); `bootstrap-keys` glue vault-first idempotent (empty item → POST /key/generate → sk into 1P; non-empty → live `/v1/models` probe with 401⇒abort stale-secret; unique-alias conflict ⇒abort; docker-exec python3 @127.0.0.1:4000, no new host ports; no_log; runs before open-webui/openclaw render like prepass-authentik); scoped-key SSOT = `litellm_scoped_keys` in vps.yml (7 specs: owui-public-chat_api / owui-public-rag_api / owui-int-wife_api / owui-int-owner_api / dsh_api / openclaw-litellm_api / rag-int-svc_api); master_key demoted to admin/bootstrap ONLY (zero consumer-template hits); scanner sees all 8 new items (self-test 20/20); deviations recorded: openclaw-litellm_api name (collision with existing openclaw-* convention), rag keys carry rerank-v4.0-pro because HD-246 routes /rerank through the same key (embed-only would break the leg). ⏳ **Deploy-gated:** `litellm_db` seeded 2026-08-26 (HD-211 keys present) → converge vps.yml (glue mints the 7 sks) → Admin-UI model recreation (compose-header runbook step) → live-verify scoped keys + open-webui/openclaw against them. Unblocks HD-250/HD-248. · [services-ai.md](docs/services-ai.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-248 | 3 | AI | 2 | **OWUI two-instance split + subdomain remap (v2 architecture)** — parametrize `open-webui/docker-compose.yml.j2` (container_name/volume/env derive from svc entry) so it renders twice; deploy `owui-int` @ `ai.kogler.si` via tailscale-sidecar Pattern A (**public DNS record for `ai.` DROPPED/repointed — nothing answers externally**); rename element-web → `msg.kogler.si` and owui-public → `chat.kogler.si` (Traefik Host rules, Authentik blueprint redirect URIs + secret-egress glue, OPENID_REDIRECT_URI env, homepage links); public instance knowledge pinned **Family Manuals ONLY** (admin-only ingestion; user workspace-knowledge permission disabled). Depends HD-247 (scoped keys). · [services-ai.md](docs/services-ai.md) |
| HD-249 | 2 | AI | 3 | **n8n internalization + LiteLLM integration** — drop public route (`auto.kogler.si`; audit external webhook deps + WEBHOOK_URL first), Pattern-B tailnet sidecar for editor UI, scoped LiteLLM key with budget cap for AI workflow nodes · [services-utilities.md](docs/services-utilities.md) |
| HD-250 | 3 | AI | 2 | **DeepSeek Harness (DSH) onboarding — DevOps/IaC cockpit (replaces pi.dev)** — image decision (own thin build from official npm pkg vs runzhliu/deepseek-harness-docker; digest-pinned; NO smanx without provenance vetting), non-root unique uid, cap_drop ALL/read_only+tmpfs, netns tailscale serve → 127.0.0.1:3080 (**NO socat bridge — UI must not listen on services-internal**), 443 egress open (accepted risk, recorded), Forgejo service-account PAT (homelab repo only, PR-only, NO merge rights) + branch protection on main, Kopia scope; depends HD-247. Research live-verified 2026-08-26 (MIT, TS/Cordis plugins, custom OpenAI-compatible provider supported). · [services-ai.md](docs/services-ai.md) |
| HD-251 | 2 | INFRA | 3 | **Fleet exposure rework phase-2 (tailscale-first)** — Headplane → tailnet-only; review Dozzle/Metabase/Grafana/Traefik-dash tiers; document Patterns A/B in network-vpn.md + capability-tiering policy in security.md (**doc part landed 2026-08-26 with this commit**); rollout pending. · [network-vpn.md](docs/network-vpn.md), [security.md](docs/security.md) |
| HD-28 | 3 | AI | 3 | **Office AI stack** — Ollama models, n8n, ONLYOFFICE desktop + MS Office via Open WebUI MCP; depends oldsrv GPU; AnythingLLM + LocPilot superseded. · [services-office.md](docs/services-office.md) |
| HD-267 | 3 | AI | 2 | **AI modular architecture decided (owner 2026-08-27): Qdrant instead of PGVector + Forgejo-OKF wiki as knowledge SSOT + dual pi.dev+DSH harness** — the brainstorm (`ai-brainstorming.md`) was ~80% re-stating the already-locked v2 architecture; the three genuine deltas are now **decided** and supersede the prior lock where they conflict: ① **Vector store = Qdrant** (standalone Rust DB, hybrid dense+sparse BM25, independent of OWUI's built-in RAG lock-out) — **re-opens locked HD-246 (PGVector)**: a changelog re-decision + migration/backtest plan (vector-dimension locks at first ingest, cohere/embed-v4.0 @ 1536) + NEW Qdrant backup seam are required before any live swap. ② **Forgejo = OKF llm-wiki knowledge SSOT**: git is the SSOT of `.md` (per-owner private repos `wiki-druzina`/`wiki-osebno-moj`/`wiki-osebno-zena` + public KB), each record = one `.md` with OKF YAML front-matter (`title`/`type`/`tags`/`generated_at`), `*-generated.md` outside the corpus; Qdrant index = a rebuildable cache over the git floor; OpenCloud stays raw-asset ingress only. ③ **Dual harness pi.dev + DSH** — supersedes HD-250's "DSH replaces pi.dev" wording → DSH *alongside* pi.dev (C# + IaC, both TUI + WebUI, feature-keyed bake-off); agents reach git/wiki via MCP, not filesystem. Does NOT change the committed LiteLLM v2 spine (HD-247 scoped keys + models-in-DB) — Qdrant only swaps the *store* consumer of LiteLLM embed/rerank. **Next**: do the IaC via HD-268. · [docs/services-ai.md](docs/services-ai.md), [docs/storage.md](docs/storage.md), [todo.md](todo.md) |
| HD-268 | 3 | AI | 1 | **IaC for the HD-267 modular AI (docs updated): Qdrant swap + Forgejo OKF wiki repos + dual pi.dev/DSH harness** — implements the decided `docs/services-ai.md` (2026-08-27 rewrite). **(a) Qdrant replaces PGVector:** add `qdrant` service entry in `group_vars/vps.yml` `docker_services` + a `qdrant/docker-compose.yml.j2` template on `db-internal`; new `qdrant_db` 1P item (db → password/username) replacing `pgvector_db`; remove/retire the PGVector template + switch RAG vector-store references (services-ai §5, deployment-compose, deployment-secrets, backup) to Qdrant; wire Qdrant snapshots into `db-backup` + Kopia; re-verify the vector-dimension lock (1536) at first ingest. **(b) OKF wiki repos:** create the OKF repo skeletons (`wiki-druzina`/`wiki-osebno-moj`/`wiki-sluzba-moj`/`wiki-osebno-zena` + public KB) with OKF YAML front-matter template; rag-mcp / Forgejo MCP service stubs on `services-internal`. **(c) Dual harness:** bring up pi.dev + DSH as separate `services-internal` entries, each consuming a scoped LiteLLM key (`dsh` + a pi key); verify tailnet serve (Pattern A) for their UIs. Keeps the LiteLLM v2 spine (HD-247) unchanged. ✅ IaC authored + full converge **live 2026-08-27** (`ok=311 failed=0`; Qdrant, pi-dev, dsh all up on `services-internal`; dsh image corrected to `runzhliu/deepseek-harness`, crash-loop fixed with `dsh-home` volume). ⏳ Deploy-gated remaining: tailnet sidecar wiring (Pattern-A serve + `tailscale_dsh`/`tailscale_pi_dev` items) + live-verify Qdrant embed/rerank + re-index. · [docs/services-ai.md](docs/services-ai.md), [deployment-compose.md](docs/deployment-compose.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-269 | 2 | OPS | 2 | **Ansible deploy runtime optimisation (VPS) — scoped + multi-service deploy surgery + glue parallelization + base tier + runbook DONE (2026-08-28)** — baseline full converge ~204s, ~35s = 3 serial `op`/`docker exec` glue loops (Authentik 21-22s, LiteLLM 8s, op-vault-export ~5s). **Completed under HD-269 (Steps a-f):** (a) single-service surgical run measured/fixed — scope=<svc> deploys in ~5-6s wall; (b) **glue parallelization** — authentik per-provider egress shipped 21-22s → **11.94s** (live 2026-08-28), litellm parallel **FAILED live** (`docker exec -i` heredoc probe breaks under `xargs -P`) → **reverted to serial** (lesson in scripts/README.md §Parallel-1P); (c) **multi-service scope** (`docker_services_scope="forgejo,traefik"`); (d) **scoped op pre-pass** + authentik-first gate; (e) **Step-4 rare `base` tier** — additive tag on the six bootstrap roles (commit 6606670); (f) **measured-timing runbook, Step f** — `docs/deployment-ansible.md` §Deploy Timing Runbook (commit 3fd166f). Full converge `ok=311 changed=45 failed=0`, wall ~3:13. **Follow-through (separate rows):** HD-270 silent compose `$`-truncation bug, HD-271 ansible-core 2.24 deprecations. · [deployment-ansible.md](docs/deployment-ansible.md), [deployment-journal.md](deployment-journal.md) |
| HD-270 | 2 | OPS | 1 | *(bug — silent secret truncation)* **Docker-compose `$`-interpolation mangles 1Password secret values mid-`$` — ✅ IaC DONE (authored + validated 2026-08-28, commit 96f0043)** — root cause confirmed from `fullconverge2.log`: compose interpolates `${...}`/`$VAR` over the whole rendered file **BEFORE YAML parse** (quoting/`>-`/block scalars don't protect; `--quiet` validate only warns+blanks). **Fix:** Layer 1 — `provision-secrets.py` `gen_pw()` drops `$` from its alphabet (every future rotation natively compose-safe); Layer 2 — `| replace('$','$$')` on every standalone vault-value expr in the 24 compose templates (82 exprs; `DATABASE_URL`/`AMQP_URI` exempt — already `urlencode`'d); convention in `docs/deployment-secrets.md`. ⏳ ~~**Deploy-gated (re-render needed):** the 6 live services…~~ ✅ **LIVE VERIFIED 2026-08-28** (scoped converge `docker_services_scope="authentik,zipline,litellm,pi-dev,openclaw"`, `ok=58 changed=23`; kopia re-rendered in the qdrant/kopia fix): `docker inspect` env == vault (length + tail) for all — authentik 32 each, zipline OIDC 128, litellm 32/73/40, pi-dev PR-token 19→32, openclaw 28→32; disk compose `$$`-escaped; kopia non-JSON warning cleared. (litellm had no literal `$` → never truly truncated; the `$`-bearing ones — authentik/zipline/pi-dev/openclaw/kopia — are all correct now.) Journal: `deployment-journal.md` Phase 1 2026-08-28. · [deployment-compose.md](docs/deployment-compose.md), [deployment-secrets.md](docs/deployment-secrets.md), [scripts/provision-secrets.py](scripts/provision-secrets.py) |
| HD-271 | 2 | OPS | 3 | *(version-clock hygiene)* **Prepare for ansible-core 2.24 deprecations (3 warnings in fullconverge2.log) — ✅ DONE (IaC, 2026-08-28)** — ① `inject_facts_as_vars: false` set in `ansible.cfg` + both `ansible_service_mgr` refs in `deploy-service.yml` prefixed to `ansible_facts['service_mgr']` (magic facts stay namespaced); ② the two internal-`vars` membership guards **replaced with `lookup('vars', '<key>', default='')`** — verified live in core 2.21.3 that even a parenthesized `'key' in vars` still emits `[DEPRECATION WARNING] The internal "vars" dictionary is deprecated…removed from ansible-core version 2.24`; the deprecation prescribes the `vars` lookup instead. `default=''` preserves the fail-closed fallback; the wireguard/router runtime asserts still fail closed on a blank peer. Owning-doc note updated (`deployment-ansible.md` §File Layout). `validate-all.sh` green (syntax-check 2.21.3); confirmed the `localhost` group+host clash warning is pre-existing cosmetic (tracked in row). ⏳ **Deploy-gated (next converge, none today):** real 2.24-core run not possible on this host without upgrading ansible-core; live converge re-verify the fact-prefixed refs (compose-validate + systemd-enable gated by `ansible_facts['service_mgr']`). **Live-found follow-up (2026-08-28, commit `d90f0e2`):** the deploy-health rollup exposed two crash-loop bugs fixed in the same worktree — qdrant (compose `read_only: true` + missing writable snapshots path → `QDRANT__STORAGE__SNAPSHOTS_PATH` added, `Up (healthy)` live) + kopia-server (first-run gate checked the wrong file `config.json` instead of `repository.config` → re-created an existing repo every boot; gate fixed, server `Up` live). See `deployment-journal.md` Phase 1 2026-08-28. · [deployment-ansible.md](docs/deployment-ansible.md) |

### 2.8 Security & Secrets — secrets hygiene, privilege, firewall, preseed hardening

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-59 | 2 | AI | 3 | **Internal service auth on flat Docker networks** — ✅ IaC applied (Ollama→`llm-backend` isolation; Kopia `--htpasswd-file` [no `--password`]; Prometheus bcrypt; Signal done). ⏳ create `kopia-server-internal_api` + `prometheus-internal_api`; wire consumers. · [deployment-compose.md](docs/deployment-compose.md), KOPS-001/016/002 |
| HD-154 | 3 | AI + gate | 1 | **Harden the public VPS host (SSH/container/firewall)** — ✅ `vps-hardening` role enforces checklist (SSH 3/no/no, fail2ban, nftables default-deny `:443`+`:51820`, Docker daemon). ⏳ deploy-gated Phase 1 (HD-40A): verify `fail2ban-client status sshd` + `nft list ruleset` + `sshd -T` + `docker info`. · [security.md](docs/security.md) §8, [services-vps.md](docs/services-vps.md) |
| HD-155 | 3 | AI + gate | 1 | **VPS→home tunnel blast radius (least-access ACL + fail-loud WG gate)** — ✅ IaC done (AllowedIPs scoped both sides, RouterOS forward ACL, fail-loud pubkey gate). ⏳ deploy-gated Phase 1/1.5 (HD-03): provision peer pubkeys + live-verify scoped access / DROP of non-scoped target. · [network-vpn.md](docs/network-vpn.md), [security.md](docs/security.md) §9 |
| HD-186 | 3 | AI + gate | 1 | **Close the published-port firewall bypass (S1)** — ✅ **Done (IaC).** Decided (HD-204): option A. The authentik all-interfaces LDAP `3389` publish is gone; the outpost binds ONLY the WG S2S address (prometheus/loki precedent, peer-pubkey-conditional) so Samba (nas, the client) pulls over the tunnel and the WAN path refuses; `storage_samba_ldap.host` retargeted oldsrv Home-IP → `wg_s2s_vps.ip` (stale "outpost on oldsrv" comments fixed); option B (DOCKER-USER chain) documented as future hardening in `docs/security.md` §8; S1 verification plan moved into the `services-vps.md` §VPS-Specific Firewall checklist (row 7); HD-132 "firewall 3389→nas" wording corrected (todo + deployment-tasks). ⏳ **Deploy-gated:** external `nc -vz <vps-public-ip> 3389` must REFUSE; `ldapsearch` over WG at the VPS WG address must connect; `nft list ruleset` shows input drop + forward `oifname docker* accept`. Closes audit S1. · [security.md](docs/security.md) §8, [services-vps.md](docs/services-vps.md) |
| HD-194 | 1 | AI + gate | 3 | **`sso` route edge middleware (S17a)** — ✅ **Done.** Decided (HD-204): enforce, exception only if callbacks break. Reasoned through the flow: nothing breaks — the bouncer filters by SOURCE IP only (path-agnostic); OIDC `/application/o/<slug>/callback/` + forward-auth `/outpost.goauthentik.io/*` arrive as ordinary browser requests from user IPs; outpost↔server API traffic runs container-direct on services-internal, never through the edge router. `crowdsec-only@file` enforced on the authentik router (reasoning documented in-template); security.md §1 map + middlewares.yml.j2 comment updated; fail2ban http-auth jail stays as layered compensating control. ⏳ Deploy-gated: confirm a login + one OIDC callback end-to-end at VPS deploy. Closes audit S17(a). · [security.md](docs/security.md) §1, [services-traefik.md](docs/services-traefik.md) |

### 2.9 Backup & DR — ZFS snapshots, Kopia, off-site, restore drills

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-266 | 2 | AI | 3 | **Hetzner StorageBox identifiers + locations recorded (docs-SSOT)** — ✅ **Done (docs+IaC, 2026-08-27).** Canonical identifiers/locations of the two Hetzner Storage Boxes captured across the owning docs + IaC SSOT. **Mapping:** SB-Data (live originals) = `FSN1-BX2190` · Falkenstein, Germany · `eu-central` (`u653411`); SB-Backup (Kopia off-site) = `HEL1-BX186` · Helsinki, Finland · `eu-central` (`u653424`). Added `location` field to both entries in `IaC/ansible/group_vars/subscriptions.yml` + a **Location column** to `IaC/ansible/templates/subscription-table.md.j2` (rendered into `docs/subscriptions-table-generated.md`); annotated the boxes in `docs/backup.md` (3-2-1 table + Box-identifiers callout + Kopia flow), `docs/storage.md` (Kopia Policy heading + live-Box principle), `docs/subscription.md` (Location rows), `docs/services-vps.md` (Bulk Storage row). validate-all green. · [backup.md](docs/backup.md), [storage.md](docs/storage.md), [subscription.md](docs/subscription.md), [group_vars/subscriptions.yml](IaC/ansible/group_vars/subscriptions.yml)
| HD-49 | 3 | AI | 3 | **Backup Matrix identity + media** — signing/identity keys (critical — reissue breaks rooms), homeserver DB (db-backup/Kopia), media store; add to backup policy. · [services-matrix.md](docs/services-matrix.md), [backup.md](docs/backup.md) |
| HD-34 | 2 | AI + Human | 4 | **Assess Kopia Web GUI vs CLI** at the first restore drill (agent assesses during the human-run yearly drill). · [backup.md](docs/backup.md) |
| HD-191 | 2 | AI | 2 | **oldsrv Kopia agent (S18/J5)** — ✅ **Done (IaC; decided HD-204: containerized agent).** New `kopia-agent` service in `group_vars/home_servers.yml` + `templates/docker_services/kopia-agent/`: snapshot loop (daily default, env-tunable) connecting to kopia-server over the WG S2S tunnel (`wg_s2s_vps.ip:51515` SSOT-derived), auth via the existing `kopia-server-internal_api` item (fail-loud, no new 1Password item). Sources read-only + NAS-independent per backup.md: `/opt/*` configs, `/srv/dumps`, immich upload/thumbs dir, signal-cli state volume (`name:`-pinned external ref). kopia-server :51515 now published ONLY on the VPS WG address (fail-loud guard mirrors the authentik LDAP bind — unprovisioned router pubkey → loopback). Shared `kopia_version` pin; validator `wg_s2s_vps` mock added. backup.md aligned in the same change. · prompt-hd191.md deleted (A3 lifecycle). ⏳ **Deploy-gated:** first snapshot from oldsrv lands in the repo; restore drill picks it up; volume-name pin verified at deploy. Closes audit S18/J5/W12. · [backup.md](docs/backup.md) |
| HD-238 | 2 | AI | 3 | **oldsrv→VPS DR runbook for non-GPU services** — imperative procedure: if oldsrv breaks/is disposed, recreate its non-GPU state services (actual-budget, *arr configs, signal-cli identity, DNS primary) on the VPS from Kopia/NAS backups; per-service scope = DR-fail-over vs accept-loss (media re-download, ollama models re-pull); covers WG AllowedIPs/router ACL changes + Traefik route moves. Owning doc: new DR section in backup.md (runbook itself → deployment-manual.md when hosts are live). Triggered by HD-57 placement decision 2026-08-24. · [backup.md](docs/backup.md) |

### 2.10 Docs & Family — family guides, docs, manual

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-32 | 2 | AI | 4 | **Write family guides `docs/manual/*`** — Slovenian guides per [`manual/README.md`](docs/manual/README.md) (count derived from the index table, never hand-entered), `status: wip`, not yet written; content well-specified; deferred until services live. · [manual/README.md](docs/manual/README.md) |
| HD-33 | 1 | AI | 4 | **Export live router config `rb4011_live.rsc`** — one-time RouterOS export; docs-only. · [network-ops.md](docs/network-ops.md) |
| HD-195 | 2 | AI | 2 | **deployment-tasks phase sweep** — ✅ **Done.** Header decision date 2025→2026; Phase 2 chain + `storage` role (matches playbooks/storage.yml exactly); Phase 3 service list rewritten to the real oldsrv subset from group_vars/home_servers.yml (ollama, immich-ml, technitium-primary, pihole, homepage→VPS per HD-180/183, dozzle, signal-cli, sunshine gated, HA-standby, jellyfin+*arr) with the applied-HD-135 "remove/downgrade" sentence dropped; Phase 3 verify Grafana/Forgejo wording moved to the VPS-edge note; Phase 4 chain = KOPS-063 order (docker_services BEFORE home_assistant), Pi services = home-assistant-primary/technitium-secondary/traefik-ha, Homematic step ⏳ parked (HD-13); Phase 6 TileBoard → HA Dashboard lovelace; Phase 7 item 7 → Office MCP path (HD-108/111); Phase 9 resolved HD-35/HD-39 rows deleted; Host→Playbook table chains match the playbook files byte-for-byte (nas += storage; vps += vps-hardening/cifs/wireguard-cond; pi reordered); 2025-08-16 typos swept in all five evidence-named docs (deployment-tasks, security, services-ai, services-vps, hardware-oldsrv; changelog rows left as written). Every changed claim cross-checked against the playbook/group_vars file it names. Closes audit I2/A3/A4/A7 + tracking §2. · [deployment-tasks.md](deployment-tasks.md) |
| HD-199 | 2 | AI | 4 | **Docs structure pass** — ✅ **Done.** (a) `deployment-compose.md` split: the Authentik OIDC-provisioning content (Blueprint + secret-egress-glue contract, deploy ordering, OpenCloud/Immich/Forgejo/Metabase native-OIDC recipes) moved verbatim to new **`docs/deployment-oidc.md`** (`role: design-spec`, ★ authoring spec); pointer stub left behind; cross-references updated (services-authentik, deployment-ansible, deployment-secrets, deployment.md hub map, index.md Document Map + OIDC dispatch row, IaC `prepass-authentik.yml` header comment); stale `metabase/metabase:latest` tag dropped in the moved text. (b) Single-public-record-mirror sub-item **deferred** — gated on HD-198 (incremental publication, open); network-dns already points at the services.md mirror per HD-196. (c) services-office/services-ai boundary trim per docs-changes §2: office keeps ONLYOFFICE/WOPI, MS fonts, client matrix, MCP topology; local-model recommendations table moved to services-ai §4 (platform SSOT), Strategy de-duplicated, "AI queries go directly to Ollama" corrected to the LiteLLM-spine path. (d) Deploy-gate marker standardized to the CONVENTIONS §3 two-sided wording `🟢 **IaC done, not yet live — ⏳ deploy-gated**` across 15 docs (observability, 9 services-* stack docs incl. merged duplicate block in finance, smart-home, smart-home-failover, hardware-ups, services-vps). ✅ validate-all green (doc map 73 docs, all links resolve). · `docs-changes.md` (fold+deleted, HD-203) |
| HD-239 | 1 | AI | 1 | **Docs liveness sweep** — ✅ **Done (docs-only).** Evidence base = deployment-journal Phase 1 (2026-08-22/23 waves; VPS tier live, 32/35 Up). README §2 rewritten: stale "nothing is deployed live yet" (2026-08-20) → Phase-1-live statement + a three-step live-vs-authored check order (`deployment-journal.md` > `deployment-tasks.md` ⏳ checklists > doc banners as hints only). Eight stale banners corrected to services-vps.md's "**Live since 2026-08-22**" pattern: observability (backend live; collectors/alerting/SNMP stay ⏳), services-traefik (VPS edge live), services-authentik (live; OIDC chain proven via HD-230b one-shot), services-matrix (containers up; federation HD-47 gated), services-admin (Forgejo healed per HD-220a etc.; kopia-agent gated), services-utilities (n8n/pairdrop/stirling-pdf live; signal-cli gated), services-office (OpenCloud+ONLYOFFICE chain owner-verified, HD-166 pts.1–6; desktop/MCP parts future), services-ai ("Planning—nothing deployed" → VPS platform live; frontmatter `status: active`). Home-host-scoped docs verified accurate, untouched (media/downloads/dns/smart-home*/hardware-ups; finance post-HD-57). ✅ validate-all green. · [deployment-journal.md](deployment-journal.md) |


### 2.11 Finance & Subscriptions — budget apps, banks, billing

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-57 | 3 | AI + Human | 3 | **Finance stack: Actual Budget / Enable Banking** — Stage 8/10: `docker_services/actual-budget/` (stable pin `26.8.1`) + group_vars registry + decisions landed 2026-08-24 (oldsrv placement · stable+n8n EB bridge, nightly rejected · CSV-first card · seed-before-sync — changelog). ⏳ **Deploy-gated:** human tokens + EB app creation ([services-finance.md](docs/services-finance.md) Open Questions), n8n import/categorization workflows (Phase 1), WG AllowedIPs scope for the :5006 API leg, first deploy + live verify at Phase 3. · [services-finance.md](docs/services-finance.md) |
| HD-133 | 3 | AI | 3 | **Subscription renewal reminders (Homepage + calendar + n8n)** — SSOT = `subscriptions.yml`; drives Homepage Subscriptions/calendar + n8n renewal notify; `subscription.md` derived table. · [subscription.md](docs/subscription.md), [automation-renewals.md](docs/automation-renewals.md) |

## 3. Park — deferred / optional / Phase 2

> Items here are not actively worked. They stay visible for planning.

| ID | D | Exec | Item |
|----|---|------|------|
| HD-36 | 3 | AI | **Internal AAAA records** — deferred/optional; needs stable per-host global addressing + IPv6 firewall mirroring. · [network-dns.md](docs/network-dns.md) |
| HD-37 | 3 | AI | **Long-term metric retention** — remote-write/downsampling (Thanos/VictoriaMetrics) only if ever needed. · [observability.md](docs/observability.md) |
| HD-38 | 2 | AI | **Prometheus Alertmanager** — only if Grafana-outage resilience demanded; Grafana Alerting covers Phase 1. · [observability.md](docs/observability.md) |
| HD-41 | 4 | AI | *(Phase 2)* **Proxmox role + VM lab** — bridges, storage, VMs; implementation order step 10. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-42 | 3 | Human | *(Phase 2)* **Phase-2 hardware build** — Ryzen 9, open-frame chassis; only if Phase 1 insufficient; physical. · [hardware-phase2.md](docs/hardware-phase2.md) |
| HD-13 | 3 | Human | **Homematic full-local (HmIP-RFUSB + RaspberryMatic)** — ⏳ **Parked (2026-08-18, decision A):** keeping the **HmIP-HAP in cloud mode** for now; **no HmIP-RFUSB purchase yet**. Full-local Homematic (`homematic` XML-RPC 2001/2010) resumes only after the stick/buy. Until then HD-04's Pi redo proceeds with **cloud HmIP-HAP** (no USB placeholder step, no local pairing); HD-18 (stick-move test) is blocked on this. · [smart-home.md](docs/smart-home.md) |
| HD-45 | 3 | AI | *(Phase 2)* **Re-evaluate Homelable (topology/rack visualizer)** — Pouzor/homelable, MIT, young project; network + rack canvas + nmap scan + live health + MCP; potential successor to `Rack.canvas` visual/Homepage reachability widget. Keep deferred until services are live; re-check maturity. Noted in `observability.md` + `network-rack.md`. · [observability.md](docs/observability.md) |
| HD-48 | 3 | AI + Human | **Requested-only bridges (deferred, Phase 2 best-effort)** — WhatsApp/Messenger/Signal bridges are **out of Phase 1 scope** (every bridge risks a real external account). Revisit **only if family asks**, and then only against **dedicated** numbers, accepting re-pairing/ban. · [services-matrix.md](docs/services-matrix.md) |
| HD-129 | 2 | AI | **Router DHCP → use internal resolver** — bootstrap assigns 1.1.1.1 not the internal resolver (KOPS-028); same outcome as HD-03 DNS setup, so fold in at live DNS. · [network-vlans.md](docs/network-vlans.md) |
| HD-130 | 2 | AI | **Low-severity opportunistic fixes** — Homepage docker.sock ro visibility, Seerr SQLite failure domain, pi edge cert expiry (KOPS-058/059/061). ✅ **Applied (2026-08-18):** KOPS-058 already `:ro` on the docker.sock mount (verified); KOPS-059 Seerr config+`seerr.db` added to `backup.md` Service-state scope + documented in the compose; KOPS-061 Grafana SSL-cert-expiry alert rule added (warning tier, fires <14 days out) per the KOPS-061 recommended fix. Live dashboard/cert check still at deploy. Do remaining opportunistically during service deployment. · [services.md](docs/services.md) |

## 3b. Deploy path — Ansible-only (HD-150, supersedes HD-02 Doco-CD)

> **Doco-CD is DROPPED (HD-150).** Deployment + upgrades use **Ansible only** for BOTH VPS and
> oldsrv. The single upgrade path: **Renovate** opens Forgejo PRs → the **Forgejo Actions deploy
> button** (`workflow_dispatch`) runs Ansible (idempotent `docker_compose_v2` re-render/applies the
> merged image-tag) → post-deploy renders Homepage config + inventory. Homepage is a dashboard link to
> the Dependency Dashboard + deploy button (no deploy role). Old HD-02 activation notes removed.

## 3c. Deploy-gated verification — lives in `deployment-tasks.md`

> **Per-phase `Deploy-gated verification` checklists are the authoritative home** for every `⏳` (IaC-done, not-live-verified) row, ordered by the deployment phase (1/1.5/2/3/4/6/8) where each live-verify runs.
> See [`deployment-tasks.md`](deployment-tasks.md) → each phase's `Deploy-gated verification` block. Row detail stays in the owning doc. This section is now a pointer, not a second SSOT.
## 4. Status & dependency notes

- **HD-50 done** → blocks all `docker_services` deployments; **HD-16 done** (Authentik + Forward-Auth middleware) unblocks Forward-Auth services (HD-43/44/46).
- **HD-03 → HD-04** (network redo feeds Pi redo). **HD-04 → HD-13** was the Homematic-full-local path, but **HD-13 is parked (2026-08-18)** until an HmIP-RFUSB is bought — HmIP-HAP stays in cloud mode; HD-04's Pi redo proceeds without local-Homematic.
- **HD-06/07 done** → feeds HD-08. **HD-29 → HD-31** done (off-site = **two 1 TB Hetzner Storage Boxes**: live next-DC CIFS + backup far-DC Kopia-over-SSH/SFTP; iDrive dropped — HD-131).
- **HD-40A/40B/135 = deployment Phase 1 (VPS edge)** — the VPS is the independent public tier, **needed before Phase 2** (network redo is Phase 1.5); see `deployment-tasks.md`. **HD-135** (service split) blocks on **HD-03 WG S2S peer** + live-box CIFS mount wired.
- 'Implemented, not deployed' rows (any row carrying a ⏳ marker — enumeration deliberately not maintained here; see the module tables) stay open until a live deploy happens — closing requires a deploy/verify pass, not just IaC.

## 5. Conventions quick-reference

> Full, consolidated rule set: [`CONVENTIONS.md`](CONVENTIONS.md) — this section is only the index.

| Area | Rule | Owning doc |
|------|------|-----------|
| Hostnames | single `kogler.si` namespace, flat subdomains | `docs/index.md` Conventions
| IPs | `docs/network-addresses-generated.md` is the SSOT, generated, never hand-edit | `scripts/check_doc_ips.py`
| Secrets | 1Password `Homelab-ansible` vault, `<service>_<type>` naming | `docs/deployment-secrets.md`
| Compose | conventions & port binding policy | `docs/deployment-compose.md`
| Ansible | roles/templates/conventions | `docs/deployment-ansible.md`, `IaC/README.md`
| Service catalog | `group_vars/home_servers.yml` + `docs/services.md` | `docs/services.md`
| Validation | `bash scripts/validate-all.sh` before commit | `scripts/`

## 7. Service onboarding

> Uniform 10-step path for adding a service (exposure/auth → secrets → compose → registry → edge →
> state/backup → observability → validation → deploy gate → docs). Canonical copy:
> **`CONVENTIONS.md` §5 — Service-onboarding checklist**. Reproduced there; new services must clear all 10 steps.

