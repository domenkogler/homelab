# Homelab TODO Backlog

> Restructured **2026-08-17** (and trimmed 2026-08: struck rows removed; deploy-gated live-verify moved to [`deployment-tasks.md`](deployment-tasks.md) per-phase blocks). Human decisions moved to the front, work organized into domain modules, deferred items parked. Done items live in the owning `docs/*.md` + git history (frozen `reports/changelog.md` is archive-only); conventions → [`CONVENTIONS.md`](CONVENTIONS.md). Single source for planned work + open decisions (HD-XX).

---

## 0. How this backlog works

- **One row = one outcome.** New work gets `HD-<next>` and links its owning `docs/*.md`.
- **Priority (P):** 1 = highest (how hot), module = what domain it touches. Priority is per-row, modules group.
- **Executors:** `AI` · `AI + gate` (human checkpoint) · `AI + Human` (joint) · `Human` (blocks).
- **Lifecycle:** open → (decided) → done — ONE pattern (CONVENTIONS §4): a **fully-done** row is DELETED from this file (record lives in the owning doc + git history); an **IaC-done-but-deploy-gated** row STAYS with a `⏳` tail (trimmed to pending items) until the live verify closes it. Deferred → park section. **Decisions:** a decided item is written **once** to its owning `docs/*.md` (+ the `<domain>-rejected.md` log where applicable) and the full rationale is **removed** from `todo.md` — §1 shows only still-open decisions/purchases (compact pointer, no struck duplicate). Historical decisions + done rows live in the frozen `reports/changelog.md` (archive-only).
- **Conventions / onboarding:** consolidated rule index + 10-step service-onboarding checklist in [`CONVENTIONS.md`](CONVENTIONS.md).
- **Service stages:** service-onboarding rows (per-checklist tasks) carry **`Stage: N/10`** in the bold title = current Service-onboarding checklist step ([CONVENTIONS.md](CONVENTIONS.md) §5). `10/10` = deployed + verified; only then does a row close. Non-service tasks (network/fix/decision) carry no stage marker.

## 1. Human decisions & purchases — review first

### Decisions (blocking / waiting on a human call)

> **None open.** Resolved decisions are logged once in their owning `docs/*.md` + the `<domain>-rejected.md` decision log and are **not** duplicated here.
> Recent: HD-22 (meteoblue weather), HD-24 (HA Dashboard), HD-25 (wake word), HD-29 (Hetzner boxes), HD-39 (no watchtower),
> HD-52 (OpenCloud OIDC), HD-122 (Matrix federation), HD-131 (storage architecture), HD-132 (Authentik-LDAP), HD-204 (public-record SSOT incremental publication),
> HD-228 (cloud appliances on VLAN 21), HD-232 (CAPsMAN modern wifi-qcom-ac / wAP retired), R-1 (home-server VLAN Option A — pi/oldsrv dual-homed, nas single-Home) — recorded in the frozen `reports/changelog.md` (archive-only).


### Purchases

| ID | D | Exec | Item |
|----|---|------|------|
| HD-30 | 1 | Human | *(buy)* **Sign up Infomaniak kSuite** — email, CalDAV, catch-all aliases; ~€3–5/mo; secrets → 1Password `Homelab-ansible`. · [subscription.md](docs/subscription.md) |
## 2. Active work — by module

### 2.1 Network & DNS — VLANs, firewall, DNS, VPN, router/switch

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-310 | 2 | AI | 1 | **Pi/oldsrv/laptop port model: untagged Home (10) primary + tagged Mgmt (99) — SSOT done, live-apply-gated (fixes the Pi's dead Home leg, HD-307/HD-308 class)** — ✅ **SSOT committed 2026-09-01 (branch `homelab-wt-20260901-2337`, 6b08cd5):** the port model flips from "Mgmt-access + single-VLAN" to **untagged = primary/access VLAN (Home 10), tagged = secondary/admin backplane (Management 99)**. A single untagged port maps to ONE VLAN (pvid) — untagged frames all go to pvid — which is why the old model left the Pi's Home leg dead. `router_port_map` now encodes `pvid`/`tagged` per port (old-srv + laptop-domen + rpi = `pvid:10` + `tagged:[99]`; nas-ilo + homematic-ap = `pvid:99`); `rb4011_converge.rsc.j2` bridge section rewritten (ether2/3 → pvid 10; VLAN 10 `untagged=ether2,3,10`; VLAN 99 `tagged=…,ether2,3,10` + `untagged=ether7,9`); `rb4011_pi_delta.rsc.j2` aligned (guarded VLAN 10 + new 2b VLAN 99 tagged/untagged); `docs/network-vlans.md` Port Type Reference + port-model note updated. Template renders green (mock-secret render passed) + `validate-all.sh` green. ✅ **LIVE-APPLIED + VERIFIED 2026-09-02 (final port model):** **ether2 = untagged 10 + tagged 99 · ether3 = untagged 10 ONLY (laptop, mgmt via Pi hop) · ether10 = untagged 10 + tagged 99 (Pi)** — applied via idempotent delta through the Pi's tagged-Mgmt leg + folded into converge template; CPU/laptop/Pi confirmed. **Mgmt strict again:** services `available-from` back to the **Mgmt subnet only** ([network-addresses-generated.md](docs/network-addresses-generated.md)) (Home→Mgmt forward DROPPED — defense-in-depth; Pi `eth0.99` tagged sub-interface is the mgmt-plane client, `~/.ssh/config` `pi99`/`router99` ProxyJump preconfigured). ✅ **Physical cable move DONE 2026-09-02:** Pi → ether10, laptop → ether3 (owner moved the cables; mini-switch freed). Live end-to-end re-verified after the move: Pi Home leg reachable (`pi` SSOT row in [network-addresses-generated.md](docs/network-addresses-generated.md)), `router99` via the Pi's tagged-99 hop works, router bridge ports pvid=10 (ether2/3/10) + pvid=99 (ether7/9) match SSOT. ✅ **Phase 4 provision LIVE 2026-09-03 (HD-307):** `raspberry_pi.yml` full run `failed=0` — HA primary + technitium-secondary + traefik-ha up; `ha.kogler.si`→VIP verified (keepalived MASTER, TLS Verify 0). ⏳ **Remaining:** owner steps (KNX UI import, Authentik OIDC, DNS cutover, failover runbook) — HD-04 umbrella. · [router.yml](IaC/ansible/group_vars/router.yml) `router_port_map` · [rb4011_converge.rsc.j2](IaC/router/templates/rb4011_converge.rsc.j2) · [network-vlans.md](docs/network-vlans.md) |
| HD-311 | 2 | AI | 2 | **IaC follow-ups for the untagged-Home/tagged-Mgmt port model (router role + host stacks): dual-home needs the OS to speak tagged VLAN 99 — router role tasks, Pi NM sub-interface, oldsrv/laptop sub-interfaces** — the SSOT/port-model change (HD-310) is the L2 fix; these are the **host-side + role-side IaC** that actually make dual-home work. ✅ **(a)+(b)+(d) DONE + LIVE-VERIFIED 2026-09-02 (Pi + router role):** router role now enforces bridge-port pvid/tagged membership from `router_port_map` (`Enforce bridge port pvid + tagged memberships`, before VLAN-filtering enable); the Pi's Mgmt IP moved OFF the untagged parent ONTO the tagged `eth0.99` sub-interface — new `pi-mgmt.nmconnection.j2` (tagged VLAN, never-default, route via the tagged leg) + `pi-eth0.nmconnection.j2` is now Home-only (`address2` removed); both templates render green + live-applied to the Pi + verified (`router99` → Pi tagged-99 hop → router mgmt WORKS; live state post-NM-reload: eth0=Home only, eth0.99=Mgmt, `mgmt_subnet` routes via eth0.99 only; live router bridge ports pvid=10 on ether2/3/10 + pvid=99 on ether7/9 match SSOT). Route-metric 200 on the tagged leg keeps `mgmt_subnet` on eth0.99. ✅ **(c) oldsrv/nas IaC DONE 2026-09-02 (parallel lane):** networkd units rendered by the `network` role (`netd-phys.network.j2` / `netd-vlan.netdev.j2` / `netd-vlan.network.j2`). ⏳ **REMAINING (deploy-gated on host provision):** apply the oldsrv tagged-99 sub-interface + laptop (stays untagged-Home only per decision — no tagged-99 on Windows). · [network-vlans.md](docs/network-vlans.md) · [pi-eth0.nmconnection.j2](IaC/ansible/roles/network/templates/pi-eth0.nmconnection.j2) · [pi-mgmt.nmconnection.j2](IaC/ansible/roles/network/templates/pi-mgmt.nmconnection.j2) · [roles/network/tasks/main.yml](IaC/ansible/roles/network/tasks/main.yml) · [roles/router/tasks/main.yml](IaC/ansible/roles/router/tasks/main.yml) |
| HD-312 | 2 | AI | 2 | **3-SSID WiFi consolidation + per-MAC control (Kogler / IOT / guest) + static-IP prerequisite + n8n firmware automation** — **DECIDED 2026-09-03** (supersedes per-purpose SSIDs; decision row in [network-rejected.md](docs/network-rejected.md)). **Architecture (target):** ① **Kogler** (VLAN 10, 2.4+5GHz) = family + kids — kids devices join here and can see family (kids controls via firewall MAC-address-list, NOT VLAN 40); ② **IOT** (VLAN 20, 2.4GHz, client-isolation) = all smart devices — cloud-IoT (Bosch/LG/HAP) get WAN via `iot-wan-allow` MAC list; ③ **Kogler guest** (VLAN 30, 5GHz, client-isolation + drop-to-LAN) = visitors. **Prerequisite:** every controlled device gets a **static DHCP reservation** in `network_static_hosts` (SSOT) so MAC-based firewall lists + CAPsMAN `access-list` vlan-id + n8n all reference stable identity. **Mechanism:** CAPsMAN `/interface wifi access-list` per-MAC `vlan-id` (wifi-qcom-ac 7.24.1 verified) + router firewall MAC-address-lists (`iot-wan-allow`, `kids-*`) rendered from SSOT; `iot-wan-allow` permanent members = cloud-IoT, temporary = n8n firmware window (add → update → remove). **n8n automation (owner idea, VPS-hosted n8n):** monthly schedule → for each Shelly: add to `iot-wan-allow` via router API (over wg-s2s tunnel) → trigger update (local Shelly RPC/CoAP) → poll/verify → remove from list; fail-closed (remove on any error). **Implementation phases:** (1) static-IP sweep in SSOT; (2) CAPsMAN 3-SSID + per-MAC ACL IaC; (3) firewall MAC-lists + kids-control migration (VLAN 40 → MAC-list); (4) n8n workflow + scoped router API user. ✅ **Phase 1 (static-IP sweep) DONE 2026-09-03 (SSOT + render, not yet live):** 4× Shelly RGBW2 static on VLAN 20 (the n8n firmware targets) + 4 family WiFi clients static on Home (`tablet-valentina`, `phone-domen` A54, `phone-martina` A55, `tablet-ipad` — kids-control MAC-list references these) in `network_static_hosts`; UPS + iLO rows gained their MACs so they finally bind their reserved SSOT addresses (were dynamic); generated SSOT re-rendered; router-role + `rb4011_converge.rsc.j2` pick the rows up automatically. ⏳ **Deploy-gated (router converge, Phase 1.5 / next `router.yml`):** apply the new reservations live + verify each device binds its reserved IP on lease turn-over. ✅ **Phase 2a (CAPsMAN 3-SSID) DONE 2026-09-03 (live):** `Kogler guest` (VLAN 30, 5GHz-only) turned up via idempotent delta (config enabled + 5GHz provisioning slave + CAP bridge VLAN 30 on both APs + switch AP-port VLAN 30 tagged) — broadcast live on ap-spalnica/ap-dnevna. **IOT-WAN + Kids SSID configurations DELETED** live + from SSOT (configs + security profiles removed; 1P items kept, unreferenced). · docs updated (network-vlans.md LIVE line, secrets rows). ✅ **Phase 3 (per-MAC cloud-IoT WAN egress) LIVE 2026-09-03 (router converge):** `src-mac-address` forward-accept per cloud-IoT device (`vlan: 21` rows: 2× LG, 3× Bosch, HAP) above the IoT→WAN drop (separate `handle_absent_entries: ignore` task — never touches the HD-182 single-remove forward chain). ⚠ **Live fix (2026-09-03):** RouterOS `/ip firewall address-list` is IP/CIDR-only — MACs are NOT valid there; the earlier `iot-wan-allow` MAC-address-list task was removed (broken) and the mechanism is the `src-mac-address` forward rule. `kids-*` list sherds reserved `when: false`. ⏳ **Verify (LIVE):** confirm LG/Bosch/HAP regain WAN (their DHCP leases turn over to the reserved VLAN 20 pool). ⏳ **Remaining phases:** (2b) CAPsMAN per-MAC `access-list` vlan-id ACL; (3b) `kids-*` MAC-lists active + kids-control migration off VLAN 40 (owner: define the kids device set); (4) n8n workflow — needs the scoped router API user, now provided by **[HD-313](todo.md) (created 2026-09-03)**. Live SSIDs = Kogler + Kogler IOT + Kogler guest. · [network-vlans.md](docs/network-vlans.md) §3-SSID + §Static-IP, [network-migration-inventory.md](docs/network-migration-inventory.md), [network-rejected.md](docs/network-rejected.md), [todo.md](todo.md) |
| HD-313 | 2 | AI | 2 | **RouterOS → central monitoring (Loki) + CrowdSec failed-login defense (RB4011/switch/AP remote RFC5424 syslog)** — **IaC DONE 2026-09-03 (new task, owner request).** The router (and switch/APs) forward logs over `wg-s2s` → VPS CrowdSec (failed-login/auth + port-scan scenarios) and the same stream is searchable in **Loki**. **What's in place:** (router role `router-logging`) `/system logging action centralsyslog` → `remote=<ssot wg_s2s_vps.remote_ip>:514`, `src-address=wg-s2s` + topic rules `error`/`firewall`/`critical`/`warning` → remote; scoped **read-only `logpipe` API user** (`read` group, `mikrotik-logpipe_api` 1P item — **seeded 2026-09-03**) — reused by HD-312d n8n; (monitoring role `routeros-syslog`, VPS) rsyslog UDP/514 on the wg address (accepts the router peer only) → `/var/log/remote-syslog/routeros.log`; (docker_services crowdsec) `acquis-routeros.yml.j2` extra template + `crowdsec_collections` += `a1ad/mikrotik` (upstream parser + `mikrotik-bf` + `mikrotik-scan-multi_ports`); crowdsec compose mounts `/var/log/remote-syslog` (ro). ⚠ **Live fix (2026-09-03):** RouterOS logging action names are letters+numbers only — `central-syslog`/`central_syslog` rejected → renamed `centralsyslog`. Docs: network-ops.md §Central log shipping, observability.md SSOT row, services-traefik.md §CrowdSec, deployment-secrets.md item. ✅ `validate-all.sh` green. ⏳ **Deploy-gated (next provider/MikroTik converge + crowdsec service deploy):** live-verify router `/system logging action print` + topic rules + `logpipe` user; restart crowdsec with the new collection + acquis; the `Ensure logging rules` task still errors at rule-creation on converge (action-ref validation) — pending VPS CrowdSec/Loki deploy; confirm alerts (`cscli alerts list`) + Loki search find RouterOS lines. · [network-ops.md](docs/network-ops.md) §Central log shipping · [observability.md](docs/observability.md) · [services-traefik.md](docs/services-traefik.md) §CrowdSec · [IaC/ansible/roles/router/tasks/main.yml](IaC/ansible/roles/router/tasks/main.yml) · [IaC/ansible/roles/monitoring/](IaC/ansible/roles/monitoring/) · [IaC/ansible/templates/docker_services/crowdsec/](IaC/ansible/templates/docker_services/crowdsec/) |
| HD-314 | 2 | AI | 2 | **nas Phase-2 provision (storage.yml failed=0) + trixie provisioning fixes** — ✅ **DONE + LIVE-VERIFIED 2026-09-03.** `storage.yml` converges **failed=0** on nas (first Ansible contact after Phase 1.5). Root-cause fixes along the way (all IaC, validated green): ① **DHCP client-id = MAC** — nas/oldsrv sent RFC4361 DUIDs that didn't match RouterOS MAC reservations → they took dynamic `.100/.101`; added `dhcp_client: mac` SSOT marker + `network` role tasks (dhcpcd `clientid mac` on nas; NM `ipv4.dhcp-client-id=mac` on oldsrv) → nas now binds its **reserved Home static** (SSOT; verified). ② **trixie apt sources** — minimal/preseed install had NO sources (only commented cdrom) → `common` now writes deb822 `.sources` (deb822_repository no-ops on this ansible-core; direct copy). ③ `linux-headers-<running-kernel>` + `zfs-dkms` (stock kernel lacked the zfs module). ④ `syncoid` not a package (ships with `sanoid`); removed from apt list. ⑤ `ansible_facts['os_family']` (inject_facts_as_vars=False) + `cockpit-zfs`→`cockpit-storaged` (trixie). ⑥ NUT: `retrycount` invalid for `usbhid-ups` 2.8.1 → removed; `/etc/nut/*` → `root:nut` (ACL denied the nut user); udev reload (ups USB group perms). ⑦ `exports.j2` `{%- endfor %}` joined lines (exportfs syntax error) → fixed; `bulk/data/immich-thumbs` explicit mountpoint (parent `bulk/data` is `mountpoint=none`). ⑧ zfs_exporter symlink + nut_exporter long-flag unit. ⏳ **Remaining:** battery-pull test (owner, HD-06), `bulk/migrate` landing-zone redistribution (HD-207), oldsrv Phase 3 now unblocked (NFS + NUT master live). · [hardware-nas.md](docs/hardware-nas.md) · [hardware-ups.md](docs/hardware-ups.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 2 · [network-vlans.md](docs/network-vlans.md) §DHCP · [IaC/ansible/roles/network/](IaC/ansible/roles/network/) · [IaC/ansible/roles/common/](IaC/ansible/roles/common/) · [IaC/ansible/roles/storage/](IaC/ansible/roles/storage/) · [IaC/ansible/roles/nut/](IaC/ansible/roles/nut/) · [IaC/ansible/roles/cockpit/](IaC/ansible/roles/cockpit/) |
| HD-304 | 2 | AI | 1 | **Phase 1.5: bootstrap `.rsc` pub-key imports must live in `flash/` (switch + APs) + Ansible roles must enforce the service lockdown / INPUT firewall from the templates (HD-301 parity)**
| HD-03 | 5 | AI + gate | 1 | **Network redo: VLAN segmentation** — flat Home /24 → VLANs 10/20/21/30/40/50/99 + inter-VLAN firewall + CAPsMAN. ✅ IaC committed (`39b9f02`); 2026-08-30 prep: ap-garage retired, dnevna MAC → hAP ac², 5 `wifi-kogler*` 1P items created, WG S2S pubkeys provisioned, `render-routeros.yml` fixed. ⏳ **cutover Phase 1.5** — not live; deploy via Ansible in WSL; open switch bridge-VLAN membership + CAPsMAN SSID secrets; WG S2S VPS peer (IaC) — provision pubkeys + tunnel; reconcile at cutover: Home→IoT new-connections gated by `trusted-admin` (incl. nas) while the narrower `trusted-ha` gates nothing — audit J8/S20, decide narrow-IaC vs widen-doc when the matrix is verified live. · [network-vlans.md](docs/network-vlans.md) |
| HD-89 | 1 | AI | 3 | **Disable/move unused AP ethernet ports off Mgmt VLAN** — wired devices on AP ports currently get full Mgmt access (KOPS-046). · [network-vlans.md](docs/network-vlans.md) |
| HD-182 | 3 | AI | 2 | **Kids VLAN firewall (HD-179: implement at 1.5)** — ✅ **Done (IaC).** Real rules in `roles/router`: bedtime 22:00–07:00 WAN block for VLAN 40 (native `time=` rule, deliberately NOT a scheduler toggle), forced filtered DNS (Kids :53 dst-nat → Technitium primary Kids/Cloudflare-Families + DoT 853 drop), Kids→Home drop; DNS-parity fixes (secondary-resolver forward rule mirroring primary, DHCP primary→secondary→router tertiary, input accept for tertiary); default-deny forward merged into the single ordered chain. ✅ **`time=` overnight render confirmed on RouterOS 7 (live 2026-09-01)** — the earlier `invalid=true`/`inactive time` read was the rule being **outside** its 22:00–07:00 active window (normal RouterOS display for a time-restricted rule, not a defect; clock was already NTP-synced at 14h uptime — a controlled probe proved an active-window rule reads `invalid=false`). ⏳ **Deploy-gated (Phase 1.5):** live-verify the forced-DNS hijack and the Kids→Home drop (the bedtime drop itself is confirmed working); `ip dns` settings-path write needs community.routeros ≥ 2.17 (fails loud on older). **NAT dedup (follow-up ②, 2026-09-01):** converge `.rsc` + role both added the same 3 NAT rows (2× masquerade + 2× Kids DNS) → live 6 rows; fixed by (a) converge NAT reset-first + (b) role NAT task now a full reconcile (`handle_absent_entries: remove` + restrict srcnat/dstnat), live-deduped to 3. Closes audit J6/J7/S19. · [network-vlans.md](docs/network-vlans.md), [network-dns.md](docs/network-dns.md) |
| HD-198 | 2 | AI + gate | 3 | **Public-record SSOT migration (B2)** — ✅ **Decided (HD-204, 2026-08-21): incremental publication (each record added as its service lands).** `roles/cloudflare_dns/vars/main.yml` manages only `vps` + SMTP2Go CNAMEs; the service records (root/home, sso, foto, file, office, ai, git, ha, vpn, matrix, chat per services.md) exist only as comments → live zone and file are dual SSOTs. Extend the list incrementally — one record per service going live (gate: applies go LIVE on Cloudflare) — and render the human mirror as it grows. Closes audit B2/W7. · [services.md](docs/services.md), [network-dns.md](docs/network-dns.md) |
| HD-274 | 3 | AI + Human | 2 | **Technitium split-horizon A records for the plain tailnet dashboards (HD-273 follow-up)** — when the **first Technitium instance comes up** (none running in the homelab yet), add **A records on BOTH instances (primary + secondary) → the `vps-obs` tailnet IP (`tailnet_sidecar_ip`, group_vars/vps.yml)** for the plain `stats/logs/csui/sec/traefik/auto.kogler.si`. ⏳ **Deploy-gated on Technitium being up** (Phase 2/3 hosts). **Why:** Tailscale client MagicDNS only ANSWERS its `base_domain` (`ts.kogler.si`); a FQDN matching search_domain `kogler.si` is queried against the tailnet's configured nameserver — Technitium — which must serve these records or the plain names NXDOMAIN on tailnet devices while the `.ts` twins work (verified live 2026-08-28; docs already updated: `network-dns.md` static-records, `network-vpn.md` Laptop access). Value is a tailnet IP → only tailnet clients can connect (LAN-only resolves but can't reach — correct, tailnet-only). If records are ever IaC-tracked, add a Technitium API role. · [network-dns.md](docs/network-dns.md), [network-vpn.md](docs/network-vpn.md) |
| HD-303 | 2 | AI | 1 | **`/user add` requires `password=` on RouterOS 7 (template fix)** — ✅ Done (IaC, deploy-gated on the next reset). `/user add` merged to one line with `password=""` in rb4011/crs328/ap templates (live-failed on the 3rd cutover reset — RouterOS 7 strict-validates `add`); regression-guard comment added. 3rd RouterOS-7-vs-6 schema bug this cutover (NTP `server=`/service `interface=` = HD-301) — a `validate-routeros7-syntax` linter HD is the right post-cutover follow-up. ⏳ **Deploy-gated:** re-render required before the next reset; the new line lands on the next `/import` or `run-after-reset=`. · [rb4011_initial.rsc.j2](IaC/router/templates/rb4011_initial.rsc.j2), [crs328_initial.rsc.j2](IaC/router/templates/crs328_initial.rsc.j2), [ap_initial.rsc.j2](IaC/router/templates/ap_initial.rsc.j2) |
| HD-302 | 2 | AI | 1 | **Comtrend modem mgmt access from homelab (no cable swap)** — ✅ **DONE + LIVE-VERIFIED 2026-09-02** (live fix: `/32`→`/24` on ether1 restores the connected route to the modem subnet; the consumer ONT only answers its web UI to its directly-connected LAN peer, so a **srcnat masquerade** out ether1 (dst=modem mgmt `tcp/80,443`, trusted-admin) presents LAN traffic as the router's own modem-LAN address — laptop → modem UI returns 401 (auth page)). IaC + converge SSOT updated (`comtrend_modem.router_on_modem` = `/24`; SNAT rule in `rb4011_converge.rsc.j2`; generated SSOT re-rendered). ⏳ **Remaining (deploy-gated on the first Technitium):** private split-horizon DNS record `modem.kogler.si` → `comtrend_modem.modem_mgmt_ip` (must NEVER go to Cloudflare) — Technitium (oldsrv) currently offline. · [router.yml](IaC/ansible/group_vars/router.yml) `comtrend_modem`, [rb4011_converge.rsc.j2](IaC/router/templates/rb4011_converge.rsc.j2), [network.md](docs/network.md) §Comtrend modem management path |
| HD-301 | 2 | AI | 1 | **Phase 1.5 bootstrap hardening (RB4011/CRS328/APs)** — ✅ Done (IaC, deploy-gated). Three template fixes: (1) NTP `server=` → `address=`; (2) service binding `interface=` → `available-from=` (live-validated 2026-08-31 — first cutover attempt left services unbound incl. WAN → 72.47.208.90 telnet brute-force post-reset); (3) explicit disabled-from-first-line block for telnet/ftp/www/api-ssl/reverse-proxy + minimal INPUT firewall in the RB4011 template. All three templates scope services to `{{ mgmt.subnet }}` (SSOT). ⏳ **Deploy-gated (Phase 1.5):** the floor is IaC-only — the next operator (or the post-cutover `validate-routeros7-syntax` linter, TBD HD) verifies firewall rules + service state on the live device after each reset. · [rb4011_initial.rsc.j2](IaC/router/templates/rb4011_initial.rsc.j2), [crs328_initial.rsc.j2](IaC/router/templates/crs328_initial.rsc.j2), [ap_initial.rsc.j2](IaC/router/templates/ap_initial.rsc.j2), [deployment-manual.md](deployment-manual.md) §Phase 1.5, [network-ops.md](docs/network-ops.md) |
| HD-285 | 1 | AI + Human | 2 | **Provision router WG pubkey + bring up wg-s2s (Phase 1.5 cutover)** — ✅ **RESOLVED + LIVE 2026-09-02 (two-key design fix).** Root cause: the SSOT used ONE shared `wg_password` on BOTH ends → router interface pubkey == its VPS peer pubkey (`gwXAk+…`) → WireGuard refuses to handshake with your own key (no handshake ever fired). **Fix:** distinct per-side keys — router uses `wg_password` (`wg_s2s_router_public_key`), VPS uses NEW 1P item `wg_password_vps` (`wg_s2s_vps_public_key`); each side peers with the OTHER's pubkey. Also: networkd 257 netdev quirks (networkd applies the .netdev key but NEVER `[WireGuardPeer]`, and strips manual peers while owning the iface) → VPS `wg-ensure-s2s-peer` oneshot now OWNS wg-s2s (networkd = create-only + Unmanaged .network; oneshot = create-if-missing → address → key → peer → verify). And the router HD-155 forward accept was BELOW the Default deny (shadowed) → moved above it (rule 29 · before 30). **Live-verified:** VPS `wg show` fresh handshake (keepalive), peer = router key, router peer `vps-s2s` = VPS key `NeEeWi…` with rx/tx moving; VPS → router mgmt (`router` host) 0% loss over tunnel. `nas` (host) not answering = Phase-2 gated, unrelated. · [IaC/ansible/group_vars/all/main.yml](IaC/ansible/group_vars/all/main.yml), [IaC/ansible/roles/wireguard/](IaC/ansible/roles/wireguard/), [network-vpn.md](docs/network-vpn.md) |
| HD-308 | 2 | AI | 1 | **RB4011 converge must trunktag every VLAN on sfp-sfpplus1 (missing 20/21/30/40/50 dropped them at the trunk → Shield on Media(50) had no gateway, no WAN)** — ✅ **Done + LIVE-APPLIED + VERIFIED 2026-09-01 (this is the L2 root cause — CLOSED).** `rb4011_converge.rsc.j2` + router role only trunktagged VLANs 99/10; VLANs 20/21/30/40/50 were `tagged: bridge-lan` only, so the router's vlan-filtering bridge dropped their frames at the SFP+ uplink (empty router ARP per VLAN, static leases `waiting/never`) — Shield on Media(50), **nvidia-shield** static reservation, had no gateway reach, no WAN. Fix: converge template + role trunk-loop every `network_vlans` id (`vlan-ids=X tagged=bridge-lan,sfp-sfpplus1`, VLAN 10 keeps `untagged=ether10`, VLAN 99 keeps mgmt untagged members); transient delta `rb4011_bridge_vlan_delta.rsc` applied via SSH `/import` (`Script file loaded and executed successfully`); live `/interface/bridge/vlan` confirms every VLAN trunktagged on `sfp-sfpplus1`. ⏳ **End-device (verified later this session):** Shield obtained its static reservation (bound, `nvidia-shield` ARP reachable) — L2 confirmed working. **HD-309 (Media→WAN forward-ACCEPT + router DNS tertiary upstreams) — CLOSED + LIVE 2026-09-01** (owning docs + git history). · [rb4011_converge.rsc.j2](IaC/router/templates/rb4011_converge.rsc.j2), [roles/router/tasks/main.yml](IaC/ansible/roles/router/tasks/main.yml), [crs328_converge.rsc.j2](IaC/router/templates/crs328_converge.rsc.j2), [network-ops.md](docs/network-ops.md), [network-vlans.md](docs/network-vlans.md), [network-addresses-generated.md](docs/network-addresses-generated.md)
| HD-318 | 2 | AI + gate | 1 | **oldsrv Phase-3 provision — IN PROGRESS (first Ansible contact done; mgmt plane + ZFS + NFS + docker + cockpit + nut live; blocked on office/amd_rocm + 3 vault items)** — ✅ **2026-09-03 oldsrv static IP + tagged Mgmt leg applied** (pre-Ansible first-contact prep): NM profile `Wired connection 1` → `manual` (SSOT `oldsrv` Home row + [`network-addresses-generated.md`](docs/network-addresses-generated.md) gateway, DNS 1.1.1.1/1.0.0.1, `ipv4.dhcp-client-id=mac` — HD-314 client-id) + new NM VLAN profile `VLAN 99 Mgmt` (`enp0s31f6.99`, SSOT `oldsrv` Mgmt row, `never-default`). ✅ **mgmt-plane outage RESOLVED (session #66):** the vlan-99 bridge entry had `ether2`/`ether10` in **UNTAGGED** instead of **TAGGED** (a prior full `router.yml` converge flipped them) → tagged clients (Pi, oldsrv) dark. Fixed via re-render + `/import rb4011_pi_delta.rsc` (sets `vlan-99 tagged=…,ether2,ether10 untagged=ether7,ether9`) — verified oldsrv→router99/pi99 0% loss, ARP REACHABLE. ✅ **First Ansible contact (`home_servers.yml` on oldsrv, Home leg) — provision reached `office` role:** docker ✅ (installed+enabled), network ✅ (netd units + /etc/hosts), storage ✅ (**`nvme` ZFS pool CREATED** on the 970 EVO — wiped the old NTFS per owner OK, data backed up on nas; datasets + NFS mounts to nas live), nut ✅ (client), cockpit ✅ (+ `/opt/traefik/dynamic/cockpit.yml`), desktop ✅, office ⚠. **4 IaC bugs fixed + committed (all signed, merged to main):** ① `linux-headers-amd64` in storage role (DKMS zfs module for running kernel — `7a084ef`); ② fresh-build pool create never reached (import aborts on missing pool — `5c3c1f5`); ③ same, iterate presence results + skip import when allow_create (`6ee6b76`); ④ `/etc/hosts` from `network_static_hosts` SSOT not `hostvars.ansible_host` (the `-e ansible_host` override leaked to every entry → nas resolved to oldsrv's own IP → NFS 'Connection refused' — `66dd629`); ⑤ cockpit creates `/opt/traefik/dynamic` before writing the route (`4e79bbe`). ⚠ **BLOCKED (remaining):** ① `office` role — ONLYOFFICE apt repo key mismatch (stale `squeeze` suite + wrong GPG key `E09C…DE8E5` missing) — needs the office role repo/key fix; ② `amd_rocm` — ROCm noble-on-trixie dep conflict (rocm-cmake/hipcc distro versions vs ROCm 7.2.4 pins) — needs pinning/removal decision; ③ **3 oldsrv 1P items still missing** (`pihole_password`, `sonarr_api`, `radarr_api` — owner-created; `privado-vpn_api`/`signal_api`/`ha_api` now present) — `docker_services` (the big service deploy) is gated on these. ⚠ **`switch_port_map` gap:** no `oldsrv` entry — but oldsrv is wired to **router ether2** (verified live: MAC on ether2, NOT the switch), so the gap is moot for oldsrv (resolve only if the wiring changes). · [network-ops.md](docs/network-ops.md) incident 2026-09-03 · [hardware-oldsrv.md](docs/hardware-oldsrv.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 3 · [switch.yml](IaC/ansible/group_vars/switch.yml) |
### 2.2 Storage, ZFS & UPS — NAS datasets, NFS, UPS/NUT

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-06 | 3 | AI | 1 | **NUT master on nas** — `usbhid-ups`, `upsd` :3493, `nut_exporter` :9199, `upssched-cmd` notify. ✅ **LIVE 2026-09-03 (nas provision, HD-314):** `upsc powerwalker@localhost` returns battery 100% / Innova Unity; `upsd` :3493 + `nut_exporter` :9199 active. ⏳ battery-pull test (owner, manual); feeds HD-07/08. · [hardware-ups.md](docs/hardware-ups.md) |
| HD-08 | 3 | AI | 2 | **UPS metrics + alerts → Prometheus/Grafana** — Critical/Warning/Info tiers. Depends HD-06/07. ✅ Monitoring IaC implemented; ⏳ live-verify `nut_ups_status` bitmask + metric names + Grafana alert-rule provisioning (blocks on HD-06). · [hardware-ups.md](docs/hardware-ups.md) |
| HD-09 | 1 | AI | 2 | **UPS web-UI firewall rule** — open 80/443 Home→Mgmt for the UPS Mgmt address (`ups`, address SSOT) only; Modbus 502 retired. ✅ IaC done; ⏳ not deployed (Phase 1.5). · [hardware-ups.md](docs/hardware-ups.md) |
| HD-26 | 1 | AI | 3 | **Confirm UPS SNMP UDP (161/udp)** — confirmantional only (no consumer; NUT/USB is the monitor). Probe must run from a Mgmt-VLAN (99) host. · [hardware-ups.md](docs/hardware-ups.md) |
| HD-132 | 3 | AI | 2 | **Authentik-as-LDAP for Samba self-service password** — ✅ IaC done (pull-model, `passdb backend=ldapsam`); ⏳ create LDAP provider + outpost, seed `authentik-ldap_bind`, live-verify a family drive mounts (Phase 2). Samba (nas, the client) reaches the VPS outpost over WG at the VPS's WG-side address — no public publish (HD-186). · [deployment-compose.md](docs/deployment-compose.md), [storage.md](docs/storage.md) |
| HD-207 | 3 | AI + gate | 1 | **Execute the NAS Pool-Creation Runbook + migrated-data final home (landing-zone redistribution)** — ✅ **Runbook EXECUTED 2026-08-23** (SMART pre-check → bulk raidz2 ashift=12 → legacy `new-pool` sent into `bulk/migrate` → tank mirror 2×4 TB → export both before installer boots — installer-ready; deltas: second source pool `backup` declared disposable + destroyed; explicit `bulk/migrate` parent create needed; preseed refreshed same day, stick re-keyed nas-ready). ⏳ **Remaining:** redistribute the landing zone slowly, then destroy it — personal documents → OpenCloud/live Box (HD-151 user-files tier), interim park `/tank/data/users/<name>/` Samba per-user drive, media → `bulk/media/*`; landing zone gone before the tree equals the storage-role SSOT at `playbooks/storage.yml` (final media rename vs personal-files decision per storage.md). · [hardware-nas.md](docs/hardware-nas.md), [deployment-tasks.md](deployment-tasks.md), [storage.md](docs/storage.md) |
### 2.3 Platform & Deploy — Ansible, compose conventions, GitOps, Renovate, Forgejo Actions

| ID | D | Exec | P | Item |
|----|---|------|---|------|

| HD-265 | 2 | AI | 2 | *(tooling)* **Include git/GitHub auth in `scripts/git-bootstrap.sh`** — ✅ Core authored 2026-08-27: opt-in `--ssh-auth`/`OP_SSH_AUTH=1` mode — human `op` sign-in, pulls `GitHub sign`/`GitHub auth` keys into `~/.ssh`, ssh-add, `~/.ssh/config` (github.com → auth key), origin HTTPS→SSH, ssh signing key/format/gpgsign set; `~/.gitconfig-debian` cleaned. Win11 companion `git-bootstrap-win11.sh` (desktop-app path — SSH agent over named pipe, `core.sshCommand` → Windows OpenSSH). ⏳ **deploy-gated:** run `bash scripts/git-bootstrap.sh --ssh-auth` in the live ext4 worktree (interactive human `op` sign-in) + verify SSH push + signed commit to `origin/main`. ⏳ **Win11 deploy-gate:** run `git-bootstrap-win11.sh --ssh-auth` from git-bash; `ssh -T git@github.com` → `Hi domenkogler!` + a signed commit. · [scripts/git-bootstrap-win11.sh](scripts/git-bootstrap-win11.sh), [scripts/git-bootstrap.sh](scripts/git-bootstrap.sh), [deployment-manual.md](deployment-manual.md), [scripts/README.md](scripts/README.md) |
| HD-261 | 3 | AI | 2 | *(speed)* **Mitogen trial — optional, gate NOT triggered** — the premise ("only if profile_tasks shows a large executor floor after the speedups") never fired: the Bulk 1Password pre-pass (HD-258) removed the op-lookup cost and VPS converges are live; no executor floor was demonstrated. Revisit only if a profile_tasks trace shows SSH-transport-bound time dominating. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-262 | 1 | AI | 4 | *(parked)* **Yacht web UI — re-evaluate only if surgical converges stay slow** — ops-comfort tool, not a speedup; drift + attack-surface concerns (extra VPS web surface, Authentik integration, unaware of `docker-compose@.service` guards). Cheaper alternate: `scripts/docker-restart.sh <service>` or cockpit container views. · [deployment-ansible.md](docs/deployment-ansible.md) · [deployment-compose.md](docs/deployment-compose.md) |
| HD-263 | 1 | AI | 3 | *(cleanup)* **Remove stale `ansible-enhancements.md` references (file already deleted)** — the audit deliverable itself is GONE (never committed; not in the tree). **Remaining:** `scripts/install-pi-wsl.sh:11`, `scripts/git-bootstrap.sh:30`, `scripts/README.md` §63–65 (and the `../ansible-enhancements.md` link in the §63 table row) still hard-link the deleted file → replace each with the folded owning-doc location (`docs/deployment-ansible.md` §docker_services / `docs/deployment-manual.md` / CONVENTIONS §6) so no dead `-enhancements` link lingers. · [scripts/install-pi-wsl.sh](scripts/install-pi-wsl.sh) · [scripts/git-bootstrap.sh](scripts/git-bootstrap.sh) · [scripts/README.md](scripts/README.md) · depends on: HD-258/260 (done — rows removed) |
| HD-230 | 2 | AI | 1 | **Phase-1 wave-2 corrective batch: pairdrop PUBLIC + blueprint one-shot-apply + onlyoffice cap_add + db-backup boot-race delay + nftables scoped flush + renovate latent-fix + disabled-service teardown** — ✅ Done (IaC, all (a)–(g)); pairdrop/drop live public (services.md). ✅ homelab repo on internal Forgejo (HD-220/264). ⏳ **Remaining (deploy-gate + owner):** surgical converge + verifies; kopia source-wiring decision; HD-132 LDAP authoring; HD-211 kopia/grafana password items. · [services-traefik.md](docs/services-traefik.md) · [services-authentik.md](docs/services-authentik.md) · [deployment-renovate.md](docs/deployment-renovate.md) |
| HD-220 | 2 | AI | 1 | **Phase-1 post-convergence incident batch: rotation-drift guard + grafana↔prometheus auth gap + renovate internal endpoint** — ✅ (a) `db_role_sync` post-up `ALTER ROLE` (forgejo crash-loop SASL 28P01 fixed, live); ✅ (b) Grafana datasource credentials from `prometheus-internal_api` (401 fixed); ✅ (c) renovate → internal `http://forgejo:3000` (forgejo live). ⏳ **Remaining (owner):** renovate token validity check once forgejo green; kopia seed (config dir still empty). · [deployment-ansible.md](docs/deployment-ansible.md) · [services-vps.md](docs/services-vps.md) |
| HD-219 | 2 | AI | 1 | **Edge forward-auth wiring (`ks-forward-auth.yml`) + headscale tier correction** — ✅ Done + applied live (9 proxy providers + embedded-outpost binding; headscale `crowdsec-only`; live-verified all 9 routes 302 → sso authorize, vpn/ai 200). ⏳ **Remaining (owner):** forgejo install wizard (browser); renovate/kopia owner steps. · [network-vpn.md](docs/network-vpn.md) · [services-traefik.md](docs/services-traefik.md) |
| HD-218 | 3 | AI | 1 | **Phase-1 crash-loop triage — Wave 1 + Wave 2 (18 containers)** — ✅ Done (IaC): all 18 crash-looping containers diagnosed + fixed (Class A/C drift fixes, Class B tmpfs/cap relaxations, crowdsec matrix collection dropped, kopia-server entrypoint, certs-dumper wildcard request, check-vault-items scope). ✅ Wave-3 combined vps.yml runs happened in later live converges (HD-219/317). ⏳ container-state re-sample + confirm no crash-loop residue. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-211 | 2 | AI + Human | 1 | **Phase-1 secret hygiene: rotation batch + replace placeholder API keys** — ✅ Placeholder swap DONE (owner 2026-08-26): real provider keys confirmed for `openrouter_api`, `cohere_api`, `forgejo_api` (renovate token). ✅ `headscale_api` rotation COMPLETED 2026-08-26 (OIDC client_secret exposed in a HD-252 Lane-D log dump → regenerated → persisted → headscale+headplane re-rendered/restarted → token-endpoint replay `invalid_grant` + `/health` 200; headplane reads the same credentials). Retired: persisted `authentik-provision_api` (HD-143 Option A — glue mints ephemeral tokens); owner rotation now applies only if `vps-op-write_api` is ever exposed. ⏳ HD-216 tail: every PERSISTED Authentik API token touched in this batch MUST be `expiring=False` (recipe [services-authentik.md](docs/services-authentik.md) *API-token auto-rotation*); include one-time `expiring=False` verification of `authentik-api_token`. · [deployment-secrets.md](docs/deployment-secrets.md), [docs/services-authentik.md](docs/services-authentik.md), [deployment-tasks.md](deployment-tasks.md) Phase 1 |

| HD-134 | 3 | AI | 2 | **Pin all unpinned image tags + validator guard** — ✅ Done (`_version` pins in all.yml, Renovate-tracked; `validate-docker-services.py` fails on bare `latest` w/ `ALLOWED_LATEST`). ⏳ fluid/obscure services still documented `latest` — pin at first deploy when registry-verified. · [deployment-compose.md](docs/deployment-compose.md), [CONVENTIONS.md](CONVENTIONS.md) §3 |
| HD-159 | 2 | AI | 2 | **blackbox liveness — home↔VPS link tunnel-down as a first-class alert** — ✅ IaC done (`wg_icmp` scrape job probing `wg_s2s_vps.router_ip` + `wg-s2s-down` Grafana rule). ⏳ live-verify at Phase 1 (tunnel up) that the alert fires on a `wg down` test. · [observability.md](docs/observability.md) |
| HD-160 | 2 | AI | 2 | **services-internal sibling auth** — ✅ IaC done (immich-app→immich-ml key; OpenClaw→OpenCloud WebDAV app-password); ⏳ create `immich-ml-internal_api` + `openclaw-opencloud_api` 1P items; verify Immich v3 ML-auth env names; live-verify Phase 1/3. · [deployment-compose.md](docs/deployment-compose.md), [security.md](docs/security.md) |
| HD-205 | 3 | AI | 4 | **`provision-secrets.py` hygiene nits (scripts.md F8)** — pass the bcrypt hash via env/stdin instead of CLI argv (process-list exposure); auto-detect or document the `BCRYPT_PY` launcher for WSL (`py -3` default fails there); delete or expose the dead `gen_wg_key()`; abort `--create` loudly when `op item list` fails (auth/scope) instead of failing per-item; reconcile `network-snmp_api` classification (auto-generated catalog vs human-gated in deployment-tasks §0). Matters at first real secret-provisioning run. · [deployment-secrets.md](docs/deployment-secrets.md), [scripts/README.md](scripts/README.md) |

### 2.4 Services & Edge — Traefik, SSO, service catalog, Matrix, VPS edge

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-252 | 2 | AI + gate | 1 | **Headscale↔Authentik OIDC — re-enroll the 2 tailnet devices after the token-exchange fix** — ✅ token-exchange resolve closed 2026-08-26 (stale pre-rotation secret in the running container; OIDC flow works, phone enrolled, ACL deny-by-default — record: [network-vpn.md](docs/network-vpn.md), [deployment-oidc.md](docs/deployment-oidc.md)). ⏳ **Owner cleanup:** nodes list EMPTY — re-enroll both devices (laptop + phone) via the now-working OIDC flow and confirm they appear in `headscale nodes list`. · [network-vpn.md](docs/network-vpn.md) · [deployment-oidc.md](docs/deployment-oidc.md) |
| HD-241 | 2 | AI + gate | 3 | **Metabase SMTP/post-setup housekeeping (smtp2go)** — ✅ IaC done 2026-08-25 (SMTP env-driven `MB_EMAIL_SMTP_*` + `MB_FROM_ADDRESS`, first-boot wizard walked, interfaces.md OIDC wording fixed). ⏳ **Deploy-gate:** surgical converge (`--tags "docker_services,metabase"`) → Admin → Email → *Send test email* → invite flow works. Known gap: `metabase-data` H2 volume has NO backup coverage until the HD-238 DR tail — keep sandbox data disposable. · [services-admin.md](docs/services-admin.md), [interfaces.md](docs/interfaces.md) |
| HD-242 | 2 | AI + gate | 2 | **Metabase data sources — CrowdSec + Forgejo (and Zipline) SELECT-only feeds** — ✅ IaC done 2026-08-25 (① CrowdSec SQLite RW bind-mount for WAL recovery + SELECT-only; ② Forgejo Postgres read-only role via `db_ro_sync`, `metabase-forgejo_ro` in catalog, grants re-applied every converge; ③ Zipline same pattern, sub-gated on HD-112). ⏳ **Deploy-gate:** owner seeds `metabase-forgejo_ro` FIRST (fail-loud render otherwise) → converge → connect both sources → import official CrowdSec dashboard JSONs → verify SELECT-only (write must fail). · [services-admin.md](docs/services-admin.md), [deployment-compose.md](docs/deployment-compose.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-243 | 2 | AI + gate | 4 | *(option, parked)* **Metabase LDAP auth vs Authentik outpost** — OSS-included LDAP would let Authentik users log in with directory creds, but NOT real SSO: second login form after the forward-auth edge, no MFA on the LDAP leg, superuser stays local-first. Also needs an outpost-binding decision (HD-186: outpost deliberately WG-S2S-bound for Samba; metabase container reaching it = nftables input allow or outpost network extension). Wiring sketch (when triggered): dedicated non-expiring bind service account per HD-132 lesson, new `metabase-ldap_bind` item, `MB_LDAP_*` compose env, group sync → Metabase groups. **Trigger to revisit: second regular human user needs Metabase access.** Owner decision 2026-08-25: keep local-admin-only until then. · [services-admin.md](docs/services-admin.md), [docs/services-authentik.md](docs/services-authentik.md) |
| HD-181 | 3 | AI + gate | 1 | **Single cert issuer (HD-178): `acme_issuer` flag + doc alignment** — ✅ Done (IaC; decided HD-204: oldsrv pulls its pair from the VPS via its own timer). `traefik_acme_issuer` flag (true only in vps.yml) gates ACME env/flags/volume + certs-dumper render on the VPS only; consumers mount the synced pair RO and serve from the DEFAULT store (new `tls.yml.j2`); all 30 `certresolver` refs wrapped; cert-sync retargeted (Pi `ha-cert-sync` → VPS `ansible_host`; NEW oldsrv `traefik-cert-pull` script+units); validator BASE_CTX pins the issuer path; docs de-⏳. ⏳ **Deploy-gated:** VPS first issuance; authorize consumer pull keys on the VPS (Pi ha-sync + oldsrv cert-sync pubkeys); verify Pi/oldsrv sync timers deliver a valid pair. Closes audit J3/C1/W11. · [services-traefik.md](docs/services-traefik.md), [services-vps.md](docs/services-vps.md) |
| HD-183 | 3 | AI + gate | 1 | **Homepage → VPS (HD-180)** — move `homepage` from `home_servers.yml` to `vps.yml`; public root+`home` route; widget/probe-based reachability (no docker.sock); `HOMEPAGE_ALLOWED_HOSTS` includes `home.` — ✅ Done (IaC; homepage on VPS edge tier after authentik, Forward-Auth consumer; route via existing Docker-provider labels; `routes.yml.j2` untouched; docker.sock mount dropped; homepage_services.yaml.j2 header → vps.yml; services.md + interfaces.md aligned). ⏳ **Deploy-gated:** first VPS deploy renders homepage there + serves `kogler.si`/`home.kogler.si` through the VPS edge. Closes audit J10/A6/W9. · [services.md](docs/services.md), [interfaces.md](docs/interfaces.md) |
| HD-316 | 3 | AI + gate | 1 | **Homepage family launchpad — real app dashboard (`home.kogler.si`) + Domen technical section (Grafana stays separate)** — ✅ **Done (IaC 2026-09-03, template generators).** `homepage_services.yaml.j2` rewritten into structured groups: **Family** (Photos→`foto`·Files→`file`·Docs→`office`+`pdf`·Chat→`chat`·Git→`git`·AI→`ai`·Tools→`drop`+`bin`·Accounts→`sso`+1Password) each with `siteMonitor` health dot on PUBLIC FQDNs only; **Domen · technical** (tailnet-only: `stats`/`logs`/`traefik`/`csui`/`sec`/`auto`/`vpn`/`dsh`/`pi-dev` — **no siteMonitor on tailnet-only names**, gated by new `homepage_show_technical` default true; Grafana = technical dashboard, never family); **Soon** (Phase 3/4 deploy-gated: HA/Homematic/Media-GPU as description-only `href:'#'` tiles, new `homepage_show_soon` default true). Subscriptions group kept + sorted by renewal (renewing→ongoing→planned, HD-133). `homepage_widgets.yaml.j2`: VPS **resources** CPU/RAM/disk gauges + **Site Monitor** reachability widget (public set only) + renewals-calendar block unchanged (calendar_url-gated). `group_vars/vps.yml`: +`homepage_show_technical`/`homepage_show_soon`. docs: interfaces.md §Homepage updated. ✅ **DEPLOYED + LIVE 2026-09-03:** full VPS converge rendered services.yaml/widgets.yaml + restarted homepage; `https://home.kogler.si/` → 302 Authentik Forward-Auth. ⏳ **Owner verify:** family sees live apps green; Domen-only technical section. · [services.md](docs/services.md), [interfaces.md](docs/interfaces.md) |
| HD-184 | 2 | AI | 1 | **Fix immich-app→immich-ml URL (cross-host)** — ✅ Done (IaC). `immich_ml_bind`/`immich_ml_url` derived from SSOT (`oldsrv_home_ip` pattern); immich-app env targets the oldsrv Home IP over WG; immich-ml publishes `:3003` bound to that Home IP (never 0.0.0.0); validator mocks added. ML key auth untouched (HD-160). ⏳ **Deploy-gated:** live-verify ML round-trip from the VPS over WG (smart-search job completes). Closes audit J1/D1. · [services-ai.md](docs/services-ai.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-141 | 4 | AI + gate | 1 | **(epic) Authentik OIDC provisioning — Blueprint + secret-egress glue** — declares OIDC providers/apps in a Blueprint; a glue copies generated client creds → 1Password `lookup()`. Sub-epic A–E, G done (IaC + VPS-live: ai/vpn Forward-Auth verified, glue mints ephemeral tokens per HD-211); live-verify tail = sub-epic F **HD-147**. · [services-authentik.md](docs/services-authentik.md) |

| HD-147 | 2 | AI + gate | 1 | **Live deploy-verify (OIDC epic tail)** — Blueprint + glue seeded + deployed on the live VPS (ai/vpn OIDC verified, Forward-Auth 302s live per HD-219). **Remaining (owner/Phase-3-gated):** OIDC login verify for **matrix** (still needs HD-47 records + provider/redirect URI per deployment-tasks §Phase 1), **claw/cloud** (incl. HD-144 CSP + desktop/mobile OAuth, HD-145 JIT-vs-preseed), **foto/immich** native OIDC; **Forgejo register** (kept Forward-Auth w/ OIDC-source pending, HD-148); re-render inventory. · [services-vps.md](docs/services-vps.md) · [services-authentik.md](docs/services-authentik.md) · [deployment-compose.md](docs/deployment-compose.md) |

| HD-122 | 2 | AI | 2 | **Matrix federation default (open + hardening)** — ✅ decided + IaC (profile/auth + room-dir hardening; trusted_servers = key-notary not permit-list). ⏳ live-verify profile auth at first deploy (HD-46). · [services-matrix.md](docs/services-matrix.md), [security.md](docs/security.md) |
| HD-40A | 3 | AI + Human | 1 | *(Phase 1)* **Provision VPS + public Traefik edge** — ✅ Provisioned (IP `159.195.111.66`, specs); edge IaC enabled (`b070f7d`). ⏳ remaining deploy-gated: run `vps.yml`, wildcard cert (DNS-01), `sso` record, CIFS mount; WG S2S home↔VPS. · [services-vps.md](docs/services-vps.md), [deployment-tasks.md](deployment-tasks.md) |
| HD-40B | 3 | AI | 1 | *(Phase 1)* **Migrate public-facing services to VPS** — Authentik/OpenCloud/Forgejo/Grafana + AI stack; DBs stay LAN over WG; storage per HD-135. Folds into `enabled:` split. · [services-vps.md](docs/services-vps.md) |
| HD-135 | 4 | AI + gate | 2 | **VPS/oldsrv service split (+ storage/backup transport)** — ✅ IaC matrices applied (`b070f7d`/`d805f6d`); WG S2S role complete (VPS `77833f1`/router `85ba6dc`). ⏳ remaining live: provision both peer pubkeys, live-Box CIFS mount, cross-host wiring, `foto`/`file`/`git`/`ai` round-trip over tunnel. · [services-vps.md](docs/services-vps.md), [storage.md](docs/storage.md), [backup.md](docs/backup.md) |
| HD-43 | 3 | AI | 3 | **Deploy/verify Media *arr stack** — Stage 4/10; templates in `home_servers.yml` (jellyfin, *arr, qbit+gluetun, …); deploys oldsrv bulk/media NFS. · [services.md](docs/services.md) |
| HD-44 | 2 | AI | 3 | **Deploy/verify new ops services** — Stage 3/10; `dozzle` + `traefik-ha` VIP edge on the Pi (added but not verified). · [services.md](docs/services.md) |
| HD-46 | 4 | AI | 3 | **Implement Matrix IaC (native-only)** — Stage 4/10. ✅ IaC done + committed (`62e0045`; Tuwunel + Element Web, Authentik OIDC, closed registration, RocksDB). ⏳ hosts unprovisioned; needs HD-47 records + OIDC provider/redirect. No bridges in Phase 1 (HD-48). · [services-matrix.md](docs/services-matrix.md) |
| HD-47 | 2 | AI | 3 | **Matrix public records + Federation** — publish `matrix`/`chat` (Cloudflare DNS-only) + `_matrix` well-known/SRV delegation; WAN 443 (8448 optional); no Forward-Auth on `/_matrix/*`. · [services-traefik.md](docs/services-traefik.md), [network-dns.md](docs/network-dns.md) |
| HD-58 | 3 | AI | 3 | **Stirling PDF service** — Stage 4/10; ✅ IaC done (pinned `stirling_pdf_version`, OCR `slv`, Forward-Auth, internal-only). ⏳ re-render `services-inventory-generated.md`; live-verify OCR + Forward-Auth chain. · [services.md](docs/services.md) |
| HD-112 | 4 | AI | 2 | **Zipline public bin + private OIDC file share** — Stage: 9/10. ✅ Decided + IaC done 2026-08-24 (v4.7.0 pin; local datasource VPS NVMe; ONE public host `bin.kogler.si` on `crowdsec-only@file`; dashboard native-OIDC via `ks-oidc.yml` `provider_zipline` + glue-seeded `zipline_oidc`; guestbin Zipline-local user owns `dropzone` folder for anonymous no-login uploads w/ hardcoded `x-zipline-deletes-at: 6h`; NO type blockers, generous global cap — public bounded by guestbin QUOTA + TTL; uploads Kopia-excluded, DB = db-backup DB05; CNAME `bin` tracked). ⏳ **Deploy-gated (Phase-1 wave):** first deploy human-gated; post-up seeding per the compose-header runbook (local admin → OIDC login verify → flip bypass-local-login in Server Settings → create `guestbin` user w/ small quota + `dropzone` folder `allowUploads=true`); anonymous upload→short-URL→viewer round-trip logged-out + 6h sweep verify; family drop script (folder-id capability secret → 1Password); family guide entry (`docs/manual/`). · [services-utilities.md](docs/services-utilities.md), [storage.md](docs/storage.md) |
| HD-113 | 2 | AI | 3 | **PairDrop P2P file share** — Stage 4/10; ✅ IaC done (pinned `pairdrop_version`, Forward-Auth, internal-only). ⏳ re-render inventory; live-verify WebRTC/signaling through Traefik (may need STUN/TURN). · [services.md](docs/services.md) |

| HD-284 | 1 | AI | 2 | **Add inline `default()` clarifying comments (3 of 4 linked templates done)** — kopia-server compose already carries the CONVENTIONS §3 non-secret comment; **remaining:** loki, authentik, prometheus `docker-compose.yml.j2` (structural WG-bind / compose-schema defaults, NOT secrets) still lack the inline note. · source: full-audit-2026-08-29 (MERGE-10 in `reports/merged-audit-2026-08-29.md`) · [loki/docker-compose.yml.j2](IaC/ansible/templates/docker_services/loki/docker-compose.yml.j2) · [authentik/docker-compose.yml.j2](IaC/ansible/templates/docker_services/authentik/docker-compose.yml.j2) · [prometheus/docker-compose.yml.j2](IaC/ansible/templates/docker_services/prometheus/docker-compose.yml.j2) · [docs/deployment-compose.md](docs/deployment-compose.md) |
| HD-288 | 1 | AI + Human | 3 | **`sunshine` deployment tail (Phase 3 oldsrv)** — enable decision resolved in IaC (`enabled: "{{ homelab_mode == 'desktop' }}"`, home_servers.yml; sunshine = oldsrv GPU tier per HD-135 split). ⏳ **Remaining:** live-verify at oldsrv Phase-3 bring-up (manual-start gaming timer + Moonlight round-trip from LAN/VPN); reconcile `docs/security.md` §3 note ("restrict 47989-48010 to Home VLAN IP") vs the in-template HD-62 decision that host ports stay on ALL interfaces (loopback/drop would break streaming) — the security.md line is stale after HD-289. · [home_servers.yml](IaC/ansible/group_vars/home_servers.yml) · [sunshine/docker-compose.yml.j2](IaC/ansible/templates/docker_services/sunshine/docker-compose.yml.j2) · [services-admin.md](docs/services-admin.md) · [hardware-oldsrv.md](docs/hardware-oldsrv.md) · [security.md](docs/security.md) §3 |


| HD-292 | 1 | Human | 3 | **`docker image prune` for stale `traefik:v3.5.2` on VPS (operational)** — docker image prune (operational). · source: full-audit-2026-08-29 (MERGE-18 in `reports/merged-audit-2026-08-29.md`) · [VPS host: docker images], [services-traefik.md](docs/services-traefik.md), [security.md](docs/security.md) |

| HD-315 | 2 | AI | 1 | **Author Grafana technical dashboards (v1) — make the live Prometheus data usable** — ✅ **Done (IaC, 2026-09-04).** Five new provisioned dashboards under `IaC/ansible/roles/monitoring/files/dashboards/` (valid JSON, uid unique, datasource uid `prometheus` — the API-seeded Prometheus datasource): `homelab-host-overview` (node resources: CPU/mem/load/disk//network/uptime on `job="alloy"`, multi-instance template var), `homelab-service-reachability` (blackbox probe tables HTTP+ICMP, latency, TLS cert expiry), `homelab-wan-tunnel` (wg_icmp liveness, Traefik requests, CrowdSec decisions, stack up), `homelab-selfmonitoring` (up{} per component incl. alloy), `homelab-overview` (entry point: down-counts + VPS CPU/mem + links). `docs/observability.md` got a **Dashboards** subsection (table + metric-name gap notes). ✅ **DEPLOYED + GRAFANA FILE-PROVIDER LIVE 2026-09-03:** VPS `monitoring` converge copied them into `/srv/docker/grafana/provisioning/dashboards/` (6 JSON incl. ups; file-provider hot-reload, no restart). ⏳ **Live-verify (owner):** render each panel with data; `traefik_requests_total` empty until Traefik `--metrics.prometheus=true` on `:8082` (scrape exists); `crowdsec_decisions`/`node_*` live names confirm on first scrape (display-only gaps, alerts untouched). · [observability.md](docs/observability.md) §Dashboards · [IaC/ansible/roles/monitoring/files/dashboards/](IaC/ansible/roles/monitoring/files/dashboards/) |
### 2.6 Smart Home — HA primary/standby, Homematic, voice, devices

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-04 | 5 | AI + gate | 1 | **Pi redo: HAOS → Debian + HA Container + Technitium secondary** — in-use migration; approved direction, not yet applied. ⏳ Homematic-local deferred: keep HmIP-HAP cloud mode, no HmIP-RFUSB buy yet (HD-13 parked). · [home-assistant-current.md](docs/home-assistant-current.md) |
| HD-307 | 3 | AI | 1 | **Pi provisioning continuation (Phase 4): configure the Pi's own network dual-homed + install Docker + HA Container/Technitium + provision.** Debian 13 trixie aarch64 installed (Raspberry Pi OS Lite, hostname `pi`, user `admin`, SSH key `ansible-admin_ssh`), router `ether10` dual-homed (VLAN-10 access pvid+ untagged + mgmt-99 tagged), Pi static reservations (mgmt `dhcp-mgmt` + Home `dhcp-10`, per SSOT) applied live via idempotent delta rsc import. **✅ 2026-09-01 (HD-307 discovery session):** Pi + laptop (new `laptop-domen` SSOT entry on the Mgmt VLAN — see [network-addresses-generated.md](docs/network-addresses-generated.md) `laptop-domen` row) static DHCP reservations, and a whole-Mgmt-VLAN `Mgmt(99)->Home(10)` FORWARD accept applied via idempotent delta `rb4011_hd307_delta.rsc.j2` (+ folded into `roles/router` inter-VLAN block + `rb4011_converge.rsc.j2`); the stale Pi dynamic `.97` lease is removed so its static Mgmt reservation binds on renewal — owner decision 2026-09-01 (whole-Mgmt scope). **Remaining:** (1) Pi network role → static dual-homed IPs (mgmt + Home, per SSOT) on `eth0` — ✅ **implemented via the `network` role NetworkManager keyfile (`pi-eth0.nmconnection`, Home + Mgmt static per SSOT, default via Home — single gateway, no `never-default`; DNS via `bootstrap_dns_servers`; Pi-uses-NM decision `network-rejected.md` 2026-09-01)** — ⚠ **⏳ deploy-gated + LIVE-UNSAFE: the current role does an NM profile-switch `up` that deactivated the DHCP connection and dropped the interface (Pi offline 2026-09-01, only router-pingable on Home; Mgmt not up). Rework to the additive pattern (add Mgmt address to the existing DHCP connection) BEFORE any live run, then power-cycle the Pi.** (2) create `ansible-admin` — ✅ **DONE on the Pi 2026-09-01** (user created, NOPASSWD + `ansible-admin_ssh` key; verify before Ansible runs); (3) `raspberry_pi.yml` (render-first HD-185): `common → ai_diag → network → nut → docker → home_assistant → docker_services → monitoring` — installs Docker + HA-Container + technitium-secondary + traefik-ha; (4) verify `ha.kogler.si`→VIP `ha_vip` (SSOT) (keepalived MASTER), Technitium secondary resolves, HA native-OIDC login, failover runbook. ✅ **FULLY PROVISIONED + LIVE 2026-09-03 (HD-307/310/04):** full `raspberry_pi.yml` run `failed=0`; home-assistant-primary (HA 2026.8.1 + keepalived MASTER on VIP `ha_vip` (SSOT)), technitium-secondary (DNS :53 alive), traefik-ha (TLS `*.kogler.si` valid, Verify 0) all up + verified; HA boots 0 config errors; `https://ha.kogler.si/` → 302. Live-fixed: docker_services enabled-set, first-boot guard loop_var, technitium read_only/cap_add, knx-entities `knx:` wrapper, meteoblue-not-in-2026.8 (disabled), trusted_proxies + stale `.storage/http`, ha-cert-sync `dump/` + VPS rsync/key auth. ⏳ **Remaining (owner-gated):** KNX UI config-flow import (tunneling `knx_router_ip`), Authentik OIDC on the `ha` route, `ha.kogler.si` DNS cutover → VIP, failover runbook (HD-04 umbrella). · [deployment-pi-provision.md](docs/deployment-pi-provision.md) (status banner = fully provisioned) · [deployment-manual.md](deployment-manual.md) §Phase 4 · [deployment-tasks.md](deployment-tasks.md) §Phase 4 · [home-assistant-current.md](docs/home-assistant-current.md) · [network-rejected.md](docs/network-rejected.md) |
| HD-185 | 3 | AI + gate | 1 | **Fix Pi first-deploy ordering (docker_services vs home_assistant)** — ✅ **Done (IaC).** `secrets.yaml` renderer LANDED in the `home_assistant` role (`secrets.yaml.j2`, rendered to `/opt/home-assistant-primary/secrets.yaml` on the Pi + `{{ ha_standby_path }}/secrets.yaml` on the standby; inline `community.general.onepassword` lookups — same convention as `configuration.yaml.j2` — fail-closed per HD-91, block-scalar per HD-233; `ha_api` intentionally NOT in HA's secrets.yaml, the monitoring role owns that token under `prometheus_ha_exporter`). ⏳ **Deploy-gated:** first real Pi deploy (Phase 4) renders configs before `compose up` and passes the guard; the regular-file guard closes (guard fails loud until the renderer). Closes audit J2/D2. · [smart-home-failover.md](docs/smart-home-failover.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-14 | 2 | AI | 2 | **Export HA entity list** — enable HA Prometheus exporter; needed for HA Dashboard `lovelace` + Grafana. Wait for observability. · [smart-home.md](docs/smart-home.md) |
| HD-17 | 3 | AI | 2 | **Single failover button + `ha-failover.sh`** — ✅ IaC done + committed. ⏳ not deployed; needs `ha-failover_api` + HmIP-RFUSB moved at runbook. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-217 | 1 | AI | 2 | **Homepage failover-button gating (`homepage_failover_button`)** — ✅ **Done (IaC; owner sign-off 2026-08-22).** ⏳ Deploy-gated: next green vps.yml run renders homepage with the gate off. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-18 | 2 | Human | 2 | **Once: test HmIP-RFUSB pairing transfer** + entity reconstruction across stick move. Hands-on; **blocked** until the HmIP-RFUSB is bought (HD-13 parked). · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-124 | 3 | AI | 2 | **Keepalived hardening** — ✅ IaC done + constraint documented (pinned `keepalived_version: 2.3.4`; `auth_type PASS` is the protocol MAXIMUM — accepted w/ Home-VLAN isolation). ⏳ deploy-gated (hosts unprovisioned). · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-313 | 3 | AI | 1 | **HA prep: KNX render-from-.knxproj + modernized Stanovanje dashboard (HD-04/HD-307 continuation)** — ✅ **Done (IaC 2026-09-03).** The ETS project `assets/references/knx/StanovanjeKogler_v1_0.knxproj` (192 GAs, main group 0) is the SSOT: `scripts/knx-hass-gen.py` (xknxproject) generates `knx-entities.yaml` (24 lights / 8 covers / 7 switches / 10 binary_sensors / 15 sensors incl. rekuperator 12/x ported from legacy), deployed to `/config/` on Pi + standby; the `.knxproj` is also deployed to `/config/knx/` for the KNX panel import (Group Monitor names — the UI import does NOT auto-create control entities). `configuration.yaml.j2`: `knx: !include knx-entities.yaml` + Lovelace `lovelace-stanovanje` (YAML mode) + radiator-timer scripts (ported from old-ha scripts.yaml); dashboard modernized to `sections`/tiles: view_pregled (status + per-room), view_luci, view_zaluzije, view_ogrevanje. ⏳ **Deploy-gated + owner steps:** KNX config-flow UI — tunneling to GIRA IP router `knx_router_ip` (`knx-ip` (SSOT)) + upload the .knxproj in the KNX panel; first Pi provision (Phase 4). · [smart-home.md](docs/smart-home.md), [home-assistant-current.md](docs/home-assistant-current.md), [deployment-pi-provision.md](docs/deployment-pi-provision.md) (Phase-4 provision runbook, lands 2026-09-03) |
### 2.7 AI & Office — LLM stack, Open WebUI, office MCP, vector DB

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-100 | 4 | AI | 2 | **AI stack: LiteLLM spine** — Stage 4/10; ✅ IaC done. ⏳ create `litellm_master_key`/`openrouter_api`/`cohere_api`; MUST pin `litellm_version`; live-verify completion + embed. · [services-ai.md](docs/services-ai.md) |
| HD-101 | 4 | AI | 2 | **AI stack: Open Web UI** — Stage 4/10; ✅ IaC done. ⏳ Deploy-gated: surgical converge; verify SSO → Authentik round-trip → local-admin linked; then LiteLLM completion + RAG. · [services-ai.md](docs/services-ai.md) |
| HD-102 | 3 | AI | 2 | **AI stack: PGVector DB + backup** — Stage 4/10; ✅ IaC done. ⏳ create `pgvector_db`; live-verify extension init + db-backup DB04. · [services-ai.md](docs/services-ai.md) |
| HD-103 | 3 | AI | 2 | **AI stack: Docling OCR** — Stage 4/10; ✅ IaC done. ⏳ first start downloads HF models; live-verify Slovenian scan conversion. · [services-ai.md](docs/services-ai.md) |
| HD-104 | 4 | AI | 2 | **AI stack: OpenClaw agent + integrations** — Stage 4/10; ✅ IaC done. ⏳ run `openclaw onboard` → `openclaw.json`; live-verify round-trip. · [services-ai.md](docs/services-ai.md) |
| HD-105 | 2 | AI | 2 | **AI stack: secrets + wiring** — Stage 1/10; ⏳ **pre-deploy gate:** create the 7 1Password items + Authentik OIDC providers per [deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md) — blocks HD-100→104. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-111 | 4 | AI | 2 | **Office MCP via Open WebUI** — Stage 1/10; `ppt-mcp` first then Word/Excel; register MCP servers in Open WebUI; retires AnythingLLM + LocPilot (HD-107 path). · [services-office.md](docs/services-office.md), [services-ai.md](docs/services-ai.md) |
| HD-246  | 3  | AI  | 2  | **AI stack: RAG retrieval wiring (decided 2026-08-25)** — ✅ Decided; ⏳ implement the services-ai.md §5 decided-parameter table (embed/rerank entries + OWUI RAG env pins + HNSW index task) + live-verify Slovenian-scan → Docling → hybrid+rerank round-trip; Family Manuals KB on both instances, public corpus restricted to that one KB. Decision record: [services-ai.md](docs/services-ai.md) §5 (note: superseded by Qdrant swap — HD-267). ·
| HD-247 | 3 | AI | 1 | **LiteLLM spine hardening cutover — scoped keys + models-in-DB** — ✅ **Done (IaC 2026-08-26).** ⏳ Deploy-gated: `litellm_db` seeded → converge vps.yml (glue mints 7 sks) → Admin-UI model recreation → live-verify scoped keys + open-webui/openclaw against them. · [services-ai.md](docs/services-ai.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-248 | 3 | AI | 2 | **OWUI two-instance split + subdomain remap (v2 architecture)** — parametrize `open-webui/docker-compose.yml.j2` (container_name/volume/env derive from svc entry) so it renders twice; deploy `owui-int` @ `ai.kogler.si` via tailscale-sidecar Pattern A (**public DNS record for `ai.` DROPPED/repointed — nothing answers externally**); rename element-web → `msg.kogler.si` and owui-public → `chat.kogler.si` (Traefik Host rules, Authentik blueprint redirect URIs + secret-egress glue, OPENID_REDIRECT_URI env, homepage links); public instance knowledge pinned **Family Manuals ONLY** (admin-only ingestion; user workspace-knowledge permission disabled). Depends HD-247 (scoped keys). · [services-ai.md](docs/services-ai.md) |
| HD-249 | 2 | AI | 3 | **n8n internalization + LiteLLM integration** — ⚡ **public route DROPPED + tailnet edge LIVE (HD-135b follow-up, 2026-08-28)**: `auto.kogler.si` labels removed from the main edge; n8n reached over the tailnet via the `traefik-tailnet` edge (`auto.kogler.si` / `auto.ts.kogler.si`, forward-auth on the plain name). Remaining: audit external webhook deps + WEBHOOK_URL (was a precondition to dropping public — verify no inbound public webhook breaks), scoped LiteLLM key with budget cap for AI workflow nodes · [services-utilities.md](docs/services-utilities.md) |
| HD-250 | 3 | AI | 2 | **DeepSeek Harness (DSH) onboarding — DevOps/IaC cockpit (replaces pi.dev)** — image decision (own thin build from official npm pkg vs runzhliu/deepseek-harness-docker; digest-pinned; NO smanx without provenance vetting), non-root unique uid, cap_drop ALL/read_only+tmpfs, netns tailscale serve → 127.0.0.1:3080 (**NO socat bridge — UI must not listen on services-internal**), 443 egress open (accepted risk, recorded), Forgejo service-account PAT (homelab repo only, PR-only, NO merge rights) + branch protection on main, Kopia scope; depends HD-247. Research live-verified 2026-08-26 (MIT, TS/Cordis plugins, custom OpenAI-compatible provider supported). · [services-ai.md](docs/services-ai.md) |
| HD-251 | 2 | INFRA | 3 | **Fleet exposure rework phase-2 (tailscale-first)** — Headplane → tailnet-only; review Dozzle/Metabase/Grafana/Traefik-dash tiers; document Patterns A/B in network-vpn.md + capability-tiering policy in security.md (**doc part landed 2026-08-26 with this commit**); rollout pending. · [network-vpn.md](docs/network-vpn.md), [security.md](docs/security.md) |
| HD-28 | 3 | AI | 3 | **Office AI stack** — Ollama models, n8n, ONLYOFFICE desktop + MS Office via Open WebUI MCP; depends oldsrv GPU; AnythingLLM + LocPilot superseded. · [services-office.md](docs/services-office.md) |
| HD-267  | 3  | AI  | 2  | **AI modular architecture decided (owner 2026-08-27): Qdrant instead of PGVector + Forgejo-OKF wiki as knowledge SSOT + dual pi.dev+DSH harness** — ✅ Decided (3 deltas supersede the prior lock; re-opens HD-246 PGVector — re-decision + migration/backtest + Qdrant backup seam before any live swap). ⏳ IaC via HD-268. · [docs/services-ai.md](docs/services-ai.md), [docs/storage.md](docs/storage.md) |
| HD-268  | 3  | AI  | 1  | **IaC for the HD-267 modular AI: Qdrant swap + Forgejo OKF wiki repos + dual pi.dev/DSH harness** — ✅ IaC authored + full converge live 2026-08-27 (Qdrant, pi-dev, dsh up on services-internal); ✅ tailnet sidecar IaC closed (HD-268c: Pattern-A sidecars, opt-in flags, 2 new 1P items). ⏳ Deploy-gated remaining: HD-268c owner enable-flow (mint preauth keys → seed 1P → flip flags → scoped converge → verify UIs) + Qdrant embed/rerank + re-index + OKF wiki repo skeletons (HD-268a/b). Owner 4-step enable flow: `group_vars/vps.yml`. · [docs/services-ai.md](docs/services-ai.md), [docs/network-vpn.md](docs/network-vpn.md), [deployment-compose.md](docs/deployment-compose.md), [deployment-secrets.md](docs/deployment-secrets.md), [group_vars/vps.yml](IaC/ansible/group_vars/vps.yml) |
| HD-296  | 3  | AI + Human  | 1  | *(HD-268c sub-task)* **Add `dsh` + `pi-dev` tailnet subdomains to headscale `dns.extra_records`** — ✅ (a) IaC CLOSED 2026-08-29 (group_vars `tailnet_subdomains` + `tailnet_dsh_ip`/`tailnet_pi_dev_ip` + config.yaml.j2; validate green). ⏳ (b) DEPLOY (owner): scoped converge `-e docker_services_scope=headscale` → MagicDNS picks up the A-records. ⏳ (c) DEPLOY-VERIFY (owner): `dig dsh.kogler.si`/`dsh.ts.kogler.si`/`pi-dev.kogler.si`/`pi-dev.ts.kogler.si` resolve to the tailnet IPv4s (edge `tailnet_sidecar_ip`); reach `vps-dsh.ts.kogler.si:3080` + `vps-pi-dev.ts.kogler.si:8080`. IPs assigned sequentially at node join — if sidecars rejoin, refresh group_vars from `headscale nodes list`. CLI-instruction detail: [docs/network-vpn.md](docs/network-vpn.md) §Pattern A. · [group_vars/vps.yml]
| HD-297  | 3  | AI + Human  | 1  | *(HD-296 follow-up)* **Route dsh + pi-dev through `traefik-tailnet` — clean URLs (`https://dsh.ts.kogler.si` / `https://pi-dev.ts.kogler.si` / plain `*.kogler.si` → SSO)** — ✅ (a) IaC CLOSED 2026-08-29 (`tailnet-apps` overlay + routers + backends). ✅ (b) DEPLOY mostly DONE 2026-08-31 (SESSION #45): DNS fixed (dsh/pi-dev extra_records → EDGE `tailnet_sidecar_ip`, NOT the Pattern-A sidecars); Authentik `forward-dsh` + `forward-pi-dev` ProxyProviders created **AND bound to the embedded outpost** (`outpost_embedded.providers` in ks-forward-auth.yml — unbinding is why the outpost never loaded them); **pi-dev subdomain rename** (`pi.kogler.si` is the Pi DEVICE hostname; docker UI is now `pi-dev.*`). **LIVE-VERIFIED:** `https://dsh.ts.kogler.si` + `https://pi-dev.ts.kogler.si` → **200** (via the vps-obs `tailscale serve` TCP:443 edge). 🔴 **(c) BLOCKED — plain `*.kogler.si` → 302 SSO does NOT work**: traefik-tailnet logs `middleware "authentik-forward-auth@file" does not exist` — `/opt/traefik-tailnet/dynamic/middlewares.yml` is NEVER RENDERED (only `.yml.j2` exists; routes.yml/tls.yml DO render). The "Template extra .j2" loop includes `dynamic/middlewares.yml.j2` (`_extra_templates['traefik-tailnet']` line 80) but the rendered file never lands. **Fix directions:** ① re-render manually to prove the .j2 is valid; ② find why the loop skipped middlewares (dest path / template_dir / bind-mount `:ro` interplay); ③ render middlewares as a static file if needed; then restart traefik-tailnet + `wget -S --header="Host: dsh.kogler.si" https://127.0.0.1/` from tailscale-sidecar netns → expect **302 Location: sso.kogler.si**; regression-check plain `stats.kogler.si` → 302. Owner final verify from the laptop. · [middlewares.yml.j2](IaC/ansible/templates/docker_services/traefik-tailnet/dynamic/middlewares.yml.j2), [_extra_templates](IaC/ansible/roles/docker_services/defaults/main.yml) line 74-82, [ks-forward-auth.yml](IaC/ansible/templates/docker_services/authentik/blueprints/ks-forward-auth.yml), [docs/network-vpn.md](docs/network-vpn.md) §Pattern A · [middlewares.yml.j2] |
| HD-299  | 3  | AI + Human  | 1  | **Move Technitium primary to the VPS (new `dns-servers` overlay) + oldsrv + Pi as secondaries (3-instance DNS HA) + split-horizon static A records so `*.kogler.si` (plain) works from any tailnet device** — design change (VPS-primary + home-LAN-secondaries, per `docs/network-dns.md` line 18). ✅ **IaC EXECUTED 2026-09-03 via [HD-317](todo.md) (VPS-primary / oldsrv-secondary / Pi-tertiary, Option A = VPS public IP resolver + nftables source-allow).** ⏳ **Remaining (owner + next VPS/router converge):** (f) seed split-horizon A records on ALL 3 instances; router RB4011 DHCP/resolver ordering (VPS→oldsrv→Pi); owner verify: `dig +short pi-dev.kogler.si` → edge IP (`tailnet_sidecar_ip`) + `https://pi-dev.kogler.si` on the pi web UI; S2S-pull 30s → still resolves via oldsrv secondary. Full spec: network-dns.md line 67 (static records) — this row is the pointer. · [docs/network-dns.md](docs/network-dns.md), [HD-317](todo.md), [IaC/ansible/group_vars/vps.yml](IaC/ansible/group_vars/vps.yml), [IaC/ansible/templates/docker_services/technitium/](IaC/ansible/templates/docker_services/technitium/), [IaC/ansible/templates/docker_services/headscale/config.yaml.j2](IaC/ansible/templates/docker_services/headscale/config.yaml.j2) nameservers, [deployment-manual.md?](docs/network-vpn.md) §MagicDNS |
| HD-317 | 3 | AI + gate | 1 | **Technitium DNS PRIMARY on the VPS (HD-299 executed: 3-instance DNS HA, VPS-primary / oldsrv-secondary / Pi-tertiary)** — ✅ **IaC DONE 2026-09-03 (HD-317; HD-315=Grafana, HD-316=Homepage).** Implemented per HD-299 rows (a)-(g) + **supervisor Option A decision**: single resolver address SSOT `dns_primary_ip` = **VPS public IP 159.195.111.66** (LAN hairpins via HD-309 WAN egress; tailnet resolves it too; survives a WG-tunnel drop). `dns_secondary_ip` = oldsrv (SSOT-derived), `dns_tertiary_ip` = Pi; `network_ranges` += `dns-servers` overlay (CIDR in [network-addresses-generated.md](docs/network-addresses-generated.md) §Infrastructure networks) + docker_services allowlist; vps.yml technitium PRIMARY (subdomain dns, host :53, **no host 5380**, web UI dns.kogler.si Forward-Auth); home_servers technitium → `instance: secondary`, raspberry_pi → `secondary-pi` (resolver-only, no labels); compose template generalized to 3-instance via `svc.instance` (container_name per instance, primary joins dns-servers pinned `tchnitium_dns_overlay_ip` (SSOT var, [network-addresses-generated.md](docs/network-addresses-generated.md)) + traefik-public; home instances join traefik-public + services-internal); headscale nameservers = VPS-public first, oldsrv second, Pi third; router.yml now references the group_vars/all.yml resolver SSOT. **Security control (Option A):** VPS nftables (vps-hardening) source-allow UDP/TCP 53 ONLY from the **tailnet headscale overlay** (CGNAT range, [network-addresses-generated.md](docs/network-addresses-generated.md) §Infrastructure networks) + a `dns-allow-home` set refreshed by a new systemd timer (`dns-allow-refresh.service/.timer`/script, resolves home DDNS `s.kogler.si` every 5 min) — the abuse/amplification surface. Docs: network-dns.md (line 18/flow/static-records/firewall) + network-vpn.md §MagicDNS rewritten to VPS-primary + every-instance static records. Validator: BASE_CTX mocks + `svc` render-context injection + web-label law gated to primary instances (needed for green validate). ✅ `validate-all.sh` green. ✅ **VPS CONVERGE + LIVE 2026-09-03 (this session):** vps.yml full + `--tags monitoring` + surgical crowdsec all green; Technitium primary UP (`:53/udp+tcp` listening, container on the `dns-servers` overlay pin (`tchnitium_dns_overlay_ip`, SSOT) + `traefik-public`, web UI serves on overlay); nftables DNS source-allow + `dns-allow-refresh` timer deployed. ⏳ **Remaining (owner + next converges):** (1) seed split-horizon static A records on ALL 3 Technitium instances — **BLOCKED: no `technitium_*` admin credential in 1Password `Homelab-ansible` (checked, none exists) — the Technitium API needs the admin token/pw; create the 1P item + wire an Ansible seed role (currently a manual/API step per network-dns.md); then `dns.kogler.si`/`ha`/`dns-pi`/dashboards resolve**; (2) RB4011 step (router.yml converge → DHCP/resolver ordering VPS→oldsrv→Pi); (3) owner verify: `dig @159.195.111.66 dns.kogler.si` + tailnet `stats.kogler.si` + LAN resolv.conf ordering + WAN-out failover. · [network-dns.md](docs/network-dns.md) · [network-vpn.md](docs/network-vpn.md) §MagicDNS · [vps.yml](IaC/ansible/group_vars/vps.yml) · [technitium/docker-compose.yml.j2](IaC/ansible/templates/docker_services/technitium/docker-compose.yml.j2) · [vps-hardening](IaC/ansible/roles/vps-hardening/) · [all/main.yml](IaC/ansible/group_vars/all/main.yml) |
### 2.8 Security & Secrets — secrets hygiene, privilege, firewall, preseed hardening

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-59 | 2 | AI | 3 | **Internal service auth on flat Docker networks** — ✅ IaC applied. ⏳ create `kopia-server-internal_api` + `prometheus-internal_api`; wire consumers. · [deployment-compose.md](docs/deployment-compose.md), KOPS-001/016/002 |
| HD-154 | 3 | AI + gate | 1 | **Harden the public VPS host (SSH/container/firewall)** — ✅ `vps-hardening` role enforces checklist. ⏳ deploy-gated Phase 1 (HD-40A): verify `fail2ban-client status sshd` + `nft list ruleset` + `sshd -T` + `docker info`. · [security.md](docs/security.md) §8, [services-vps.md](docs/services-vps.md) |
| HD-155 | 3 | AI + gate | 1 | **VPS→home tunnel blast radius (least-access ACL + fail-loud WG gate)** — ✅ IaC done (AllowedIPs scoped both sides, RouterOS forward ACL, fail-loud pubkey gate). ⏳ deploy-gated Phase 1/1.5 (HD-03): provision peer pubkeys + live-verify scoped access / DROP of non-scoped target. · [network-vpn.md](docs/network-vpn.md), [security.md](docs/security.md) §9 |
| HD-186 | 3 | AI + gate | 1 | **Close the published-port firewall bypass (S1)** — ✅ **Done (IaC).** Decided (HD-204): option A. The authentik all-interfaces LDAP `3389` publish is gone; the outpost binds ONLY the WG S2S address (prometheus/loki precedent, peer-pubkey-conditional) so Samba (nas, the client) pulls over the tunnel and the WAN path refuses; `storage_samba_ldap.host` retargeted oldsrv Home-IP → `wg_s2s_vps.ip` (stale "outpost on oldsrv" comments fixed); option B (DOCKER-USER chain) documented as future hardening in `docs/security.md` §8; S1 verification plan moved into the `services-vps.md` §VPS-Specific Firewall checklist (row 7); HD-132 "firewall 3389→nas" wording corrected (todo + deployment-tasks). ⏳ **Deploy-gated:** external `nc -vz <vps-public-ip> 3389` must REFUSE; `ldapsearch` over WG at the VPS WG address must connect; `nft list ruleset` shows input drop + forward `oifname docker* accept`. Closes audit S1. · [security.md](docs/security.md) §8, [services-vps.md](docs/services-vps.md) |
| HD-194 | 1 | AI + gate | 3 | **`sso` route edge middleware (S17a)** — ✅ **Done.** Decided (HD-204): enforce, exception only if callbacks break. Reasoned through the flow: nothing breaks — the bouncer filters by SOURCE IP only (path-agnostic); OIDC `/application/o/<slug>/callback/` + forward-auth `/outpost.goauthentik.io/*` arrive as ordinary browser requests from user IPs; outpost↔server API traffic runs container-direct on services-internal, never through the edge router. `crowdsec-only@file` enforced on the authentik router (reasoning documented in-template); security.md §1 map + middlewares.yml.j2 comment updated; fail2ban http-auth jail stays as layered compensating control. ⏳ Deploy-gated: confirm a login + one OIDC callback end-to-end at VPS deploy. Closes audit S17(a). · [security.md](docs/security.md) §1, [services-traefik.md](docs/services-traefik.md) |
| HD-280 | 1 | AI | 2 | **fail2ban fix: add traefik `accesslog` + a traefik-aware filter (or drop the http-auth jail)** — (a) add traefik accesslog + write a traefik-aware filter, or (b) drop the http-auth jail. · source: full-audit-2026-08-29 (MERGE-6 in `reports/merged-audit-2026-08-29.md`) · [IaC/ansible/roles/vps-hardening/tasks/main.yml](IaC/ansible/roles/vps-hardening/tasks/main.yml), [docs/deployment-compose.md](docs/deployment-compose.md) |
| HD-286 | 1 | AI | 2 | **Document vault-escape context (TOML/env/yaml don't need `| replace('$','$$')`) in template headers** — add one-line comments in TOML/env/yaml template headers to make this explicit for future readers. · source: full-audit-2026-08-29 (MERGE-12 in `reports/merged-audit-2026-08-29.md`) · [IaC/ansible/templates/docker_services/](IaC/ansible/templates/docker_services/) (TOML/env/yaml), [docs/deployment-compose.md](docs/deployment-compose.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-287 | 1 | AI | 2 | **Add `cap_drop: ALL` + targeted `cap_add` to ollama + immich-ml (hardening)** — add cap_drop: ALL + targeted cap_add (e.g., SYS_RAWIO) for amdkfd; hardening opportunity. **Deferred 2026-08-30 — needs live cap verification**: picking the wrong targeted cap (SYS_RAWIO vs SYS_ADMIN vs none) bricks the LLM/ML service on first deploy; safer path = scoped `--cap-drop=ALL --cap-add=SYS_RAWIO --device /dev/kfd` on a single container, confirm ROCm init, THEN encode the verified cap set in IaC + remove from `ALLOWED_NO_CAP_DROP`. Audit itself notes "Mitigated by overlay isolation (HD-59) + HD-160 sibling auth" — defense-in-depth, not a fix. · source: full-audit-2026-08-29 (MERGE-13 in `reports/merged-audit-2026-08-29.md`) · [IaC/ansible/templates/docker_services/ollama/docker-compose.yml.j2](IaC/ansible/templates/docker_services/ollama/docker-compose.yml.j2), [IaC/ansible/templates/docker_services/immich-ml/docker-compose.yml.j2](IaC/ansible/templates/docker_services/immich-ml/docker-compose.yml.j2), [docs/security.md](docs/security.md) §4, [scripts/validate-docker-services.py](scripts/validate-docker-services.py) |

### 2.9 Backup & DR — ZFS snapshots, Kopia, off-site, restore drills

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-49 | 3 | AI | 3 | **Backup Matrix identity + media** — signing/identity keys (critical — reissue breaks rooms), homeserver DB (db-backup/Kopia), media store; add to backup policy. · [services-matrix.md](docs/services-matrix.md), [backup.md](docs/backup.md) |
| HD-34 | 2 | AI + Human | 4 | **Assess Kopia Web GUI vs CLI** at the first restore drill (agent assesses during the human-run yearly drill). · [backup.md](docs/backup.md) |
| HD-191 | 2 | AI | 2 | **oldsrv Kopia agent (S18/J5)** — ✅ Done (IaC; containerized agent). ⏳ Deploy-gated: first snapshot from oldsrv lands in repo; restore drill picks it up; volume-name pin verified at deploy. Closes audit S18/J5/W12. · [backup.md](docs/backup.md) |
| HD-238 | 2 | AI | 3 | **oldsrv→VPS DR runbook for non-GPU services** — imperative procedure: if oldsrv breaks/is disposed, recreate its non-GPU state services (actual-budget, *arr configs, signal-cli identity, DNS primary) on the VPS from Kopia/NAS backups; per-service scope = DR-fail-over vs accept-loss (media re-download, ollama models re-pull); covers WG AllowedIPs/router ACL changes + Traefik route moves. Owning doc: new DR section in backup.md (runbook itself → deployment-manual.md when hosts are live). Triggered by HD-57 placement decision 2026-08-24. · [backup.md](docs/backup.md) |
### 2.10 Docs & Family — family guides, docs, manual

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-32 | 2 | AI | 4 | **Write family guides `docs/manual/*`** — Slovenian guides per [`manual/README.md`](docs/manual/README.md) (count derived from the index table, never hand-entered), `status: wip`, not yet written; content well-specified; deferred until services live. · [manual/README.md](docs/manual/README.md) |
| HD-33 | 1 | AI | 4 | **Export live router config `rb4011_live.rsc`** — one-time RouterOS export; docs-only. · [network-ops.md](docs/network-ops.md) |
### 2.11 Finance & Subscriptions — budget apps, banks, billing

| ID | D | Exec | P | Item |
|----|---|------|---|------|
| HD-57 | 3 | AI + Human | 3 | **Finance stack: Actual Budget / Enable Banking** — Stage 8/10: `docker_services/actual-budget/` (stable pin `26.8.1`) + group_vars registry + decisions landed 2026-08-24 (oldsrv placement · stable+n8n EB bridge, nightly rejected · CSV-first card · seed-before-sync — see [services-finance.md](docs/services-finance.md) Decisions). ⏳ **Deploy-gated:** human tokens + EB app creation ([services-finance.md](docs/services-finance.md) Open Questions), n8n import/categorization workflows (Phase 1), WG AllowedIPs scope for the :5006 API leg, first deploy + live verify at Phase 3. · [services-finance.md](docs/services-finance.md) |
| HD-133 | 3 | AI | 3 | **Subscription renewal reminders (Homepage + calendar + n8n)** — SSOT = `subscriptions.yml`; drives Homepage Subscriptions/calendar + n8n renewal notify; `subscription.md` derived table. · [subscription.md](docs/subscription.md), [automation-renewals.md](docs/automation-renewals.md) |
## 3. Park — deferred / optional / Phase 2

> Items here are not actively worked. They stay visible for planning.

| ID | D | Exec | Item |
|----|---|------|------|
| HD-36 | 3 | AI | **Internal AAAA records** — deferred/optional; needs stable per-host global addressing + IPv6 firewall mirroring. · [network-dns.md](docs/network-dns.md) |
| HD-37 | 3 | AI | **Long-term metric retention** — remote-write/downsampling (Thanos/VictoriaMetrics) only if ever needed. · [observability.md](docs/observability.md) |
| HD-38 | 2 | AI | **Prometheus Alertmanager** — only if Grafana-outage resilience demanded; Grafana Alerting covers Phase 1. · [observability.md](docs/observability.md) |
| HD-41 | 4 | AI | *(Phase 2)* **Proxmox role + VM lab** — bridges, storage, VMs; implementation order step 10. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-42 | 3 | Human | *(Phase 2)* **Phase-2 hardware build** — Ryzen 9, open-frame chassis; only if Phase 1 insufficient; physical. · [hardware-phase2.md](docs/hardware-phase2.md) |
| HD-13 | 3 | Human | **Homematic full-local (HmIP-RFUSB + RaspberryMatic)** — ⏳ **Parked (2026-08-18, decision A):** keeping the **HmIP-HAP in cloud mode** for now; **no HmIP-RFUSB purchase yet**. Full-local Homematic (`homematic` XML-RPC 2001/2010) resumes only after the stick/buy. Until then HD-04's Pi redo proceeds with **cloud HmIP-HAP** (no USB placeholder step, no local pairing); HD-18 (stick-move test) is blocked on this. · [smart-home.md](docs/smart-home.md) |
| HD-45 | 3 | AI | *(Phase 2)* **Re-evaluate Homelable (topology/rack visualizer)** — Pouzor/homelable, MIT, young project; network + rack canvas + nmap scan + live health + MCP; potential successor to `Rack.canvas` visual/Homepage reachability widget. Keep deferred until services are live; re-check maturity. Noted in `observability.md` + `network-rack.md`. · [observability.md](docs/observability.md) |
| HD-48 | 3 | AI + Human | **Requested-only bridges (deferred, Phase 2 best-effort)** — WhatsApp/Messenger/Signal bridges are **out of Phase 1 scope** (every bridge risks a real external account). Revisit **only if family asks**, and then only against **dedicated** numbers, accepting re-pairing/ban. · [services-matrix.md](docs/services-matrix.md) |
| HD-129 | 2 | AI | **Router DHCP → use internal resolver** — bootstrap assigns 1.1.1.1 not the internal resolver (KOPS-028); same outcome as HD-03 DNS setup, so fold in at live DNS. · [network-vlans.md](docs/network-vlans.md) |
| HD-264 | 2 | AI + gate | *(test)* **Renovate sandbox `domen/test` — prove end-to-end tracer bump + PR path before pointing at the real repos** — ⏳ **(Parked 2026-09-03, owner steps.)** (b) run-model CLOSED 2026-08-29 (IaC: `restart: on-failure:5` + `RENOVATE_SCHEDULE` 6h nightly; live-verified RestartCount=0). **Remaining (owner):** external re-launch mechanism (host systemd timer / Ansible cron); commit a real versioned manifest + `renovate.json` to `domen/test` (currently README only) + verify Dependency Dashboard issue + PR lands; then flip `RENOVATE_REPOSITORIES` to the real repo. **Superseded note (2026-09-03):** the GitHub-migration framing is OBSOLETE — the homelab repo lives on internal **Forgejo** (HD-230/220: renovate endpoint `http://forgejo:3000`, `forgejo_api` token); the final flip targets the Forgejo homelab repo, not GitHub. · [deployment-renovate.md](docs/deployment-renovate.md) · [renovate docker-compose.yml.j2](IaC/ansible/templates/docker_services/renovate/docker-compose.yml.j2) |
## 3b. Deploy path — Ansible-only (HD-150, supersedes HD-02 Doco-CD)

> **Doco-CD is DROPPED (HD-150).** Deployment + upgrades use **Ansible only** for BOTH VPS and
> oldsrv. The single upgrade path: **Renovate** opens Forgejo PRs → the **Forgejo Actions deploy
> button** (`workflow_dispatch`) runs Ansible (idempotent `docker_compose_v2` re-render/applies the
> merged image-tag) → post-deploy renders Homepage config + inventory. Homepage is a dashboard link to
> the Dependency Dashboard + deploy button (no deploy role). Old HD-02 activation notes removed.

## 3c. Deploy-gated verification — lives in `deployment-tasks.md`

> **Per-phase `Deploy-gated verification` checklists are the authoritative home** for every `⏳` (IaC-done, not-live-verified) row, ordered by the deployment phase (1/1.5/2/3/4/6/8) where each live-verify runs.
> See [`deployment-tasks.md`](deployment-tasks.md) → each phase's `Deploy-gated verification` block. Row detail stays in the owning doc. This section is now a pointer, not a second SSOT.
## 4. Status & dependency notes

- **HD-50 done** → blocks all `docker_services` deployments; **HD-16 done** (Authentik + Forward-Auth middleware) unblocks Forward-Auth services (HD-43/44/46).
- **HD-03 → HD-04** (network redo feeds Pi redo). **HD-04 → HD-13** was the Homematic-full-local path, but **HD-13 is parked (2026-08-18)** until an HmIP-RFUSB is bought — HmIP-HAP stays in cloud mode; HD-04's Pi redo proceeds without local-Homematic.
- **HD-06/07 done** → feeds HD-08. **HD-29 → HD-31** done (off-site = **two 1 TB Hetzner Storage Boxes**: live next-DC CIFS + backup far-DC Kopia-over-SSH/SFTP; iDrive dropped — HD-131).
- **HD-40A/40B/135 = deployment Phase 1 (VPS edge)** — the VPS is the independent public tier, **needed before Phase 2** (network redo is Phase 1.5); see `deployment-tasks.md`. **HD-135** (service split) blocks on **HD-03 WG S2S peer** + live-box CIFS mount wired.
- 'Implemented, not deployed' rows (any row carrying a ⏳ marker — enumeration deliberately not maintained here; see the module tables) stay open until a live deploy happens — closing requires a deploy/verify pass, not just IaC.

## 5. Conventions quick-reference

> Full, consolidated rule set: [`CONVENTIONS.md`](CONVENTIONS.md) — this section is only the index.

| Area | Rule | Owning doc |
|------|------|-----------|
| Hostnames | single `kogler.si` namespace, flat subdomains | `docs/index.md` Conventions
| IPs | `docs/network-addresses-generated.md` is the SSOT, generated, never hand-edit | `scripts/check_doc_ips.py`
| Secrets | 1Password `Homelab-ansible` vault, `<service>_<type>` naming | `docs/deployment-secrets.md`
| Compose | conventions & port binding policy | `docs/deployment-compose.md`
| Ansible | roles/templates/conventions | `docs/deployment-ansible.md`, `IaC/README.md`
| Service catalog | `group_vars/home_servers.yml` + `docs/services.md` | `docs/services.md`
| Validation | `bash scripts/validate-all.sh` before commit | `scripts/`

## 7. Service onboarding

> Uniform 10-step path for adding a service (exposure/auth → secrets → compose → registry → edge →
> state/backup → observability → validation → deploy gate → docs). Canonical copy:
> **`CONVENTIONS.md` §5 — Service-onboarding checklist**. Reproduced there; new services must clear all 10 steps.

