# Changelog — Kogler Homelab

> Append-only log of completed/decided work. Migrated out of `todo.md` on 2026-08-17.
> Dates are best-effort git attributions (module/commit dates); verify against the owning doc before
> relying on them — they are advisory, not SSOT.

**Done items migrated: 19** — HD-01, HD-05, HD-07, HD-10, HD-106, HD-107, HD-108, HD-109, HD-11, HD-110, HD-12, HD-16, HD-19, HD-35, HD-50, HD-51, HD-56, HD-92, HD-93

## Decisions & research

| ID | Date | Item |
|----|------|------|
| HD-131 | 2026-08-18 | *(decision)* **Storage & family-access architecture (S3-proof; D1–D6)** — decided (read-only design, locked 2026-08-18): **D1** S3 backend from day one **only for Immich** (MinIO-on-NAS now → Hetzner Storage Box S3 later); OpenCloud + drives stay filesystem. **D2** per-app interface: Immich→S3, OpenCloud→WebDAV/filesystem, Win11/Linux drives→SMB/CIFS. **D3** minimal photo retention — only backup needed (Kopia S3-originals→box + the Immich metadata DB; no deep version tier). **D4** per-user isolation is app-level (Authentik↔OpenCloud); family drives via **Samba per-user shares**; login manual username+password, **no 1Password SSO** (1Password only stores the passwords). **D5** build Authentik→NAS/OpenCloud provisioning **glue** (new member → NAS user+uid+dataset+Samba share+OpenCloud account). **D6** encode D1–D6 into owning docs when those files are next edited. Note: ZFS snapshots/instant-rollback are traded for Kopia+box-retention; ZFS is retained for OpenCloud shared files but Immich originals go S3. Frame + re-scope of HD-94. · [deployment-compose.md](docs/deployment-compose.md), [storage-zfs.md](docs/storage-zfs.md), [backup.md](docs/backup.md) |
| HD-53 | 2026-08-18 | *(decision)* **MikroTik SNMP community = dedicated RO + Mgmt-VLAN ACL (Option A)** — dedicated read-only community (`network-snmp_login`, in 1Password) replaces the snmp_exporter default `public`; SNMP (161/udp) restricted to the Management VLAN via router INPUT-chain ACL. Device-side `/snmp` enable + RO community = HD-03 deploy step (RouterOS commands in `snmp.yml.j2` header). KOPS-034 closed. · [observability.md](docs/observability.md) |
| HD-54 | 2026-08-18 | *(decision)* **SMTP relay = SMTP2Go (Option B)** — dedicated transactional relay `mail.smtp2go.com:587` (STARTTLS), free-tier 1000/mo, chosen **independently** of the Infomaniak kSuite decision (HD-30) so the alert fail-safe isn't coupled to personal email. Shared by Grafana (`grafana-smtp_login`) + NUT (`nut-smtp_login`). · [observability.md](docs/observability.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-05 | 2026-08-08 | **VIP address / notation / firewall IP-set** — decided: `10.10.1.200/32` (`ha-vip`), DHCP pool ≤ `.199`, router lists `trusted-ha` + `trusted-admin`. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-106 | 2026-08-17 | *(decision)* **Office MCP bridges = native Windows per-client apps (no Docker)** — Office COM requires a licensed, installed Office **natively on the Windows host**; Docker/Linux can't reach host COM automation. The bridge (Node MCP/SSE wrapper + pinned PyWin32 COM worker) runs natively per client. Version-pinned (Flaw-B supply-chain discipline). Distributed from a repo-owned `client/office-bridge/` folder served read-only over the Headscale tunnel (independent of Ansible/GitOps, which manages server Docker, not Windows endpoints). · [llm-office.md](docs/llm-office.md) |
| HD-107 | 2026-08-17 | *(decision)* **Linux clients = server-side Office tools only** — `python-docx`/`python-pptx`/`openpyxl` on the server act on files in OpenCloud (file SSOT); results appear in the synced OpenCloud folder on any host (Linux opens them in ONLYOFFICE). No live COM on Linux — file-level sync latency, **not** in-app live editing. · [llm-office.md](docs/llm-office.md) |
| HD-108 | 2026-08-17 | *(decision)* **OpenCloud = file SSOT · Open WebUI = chat/UX SSOT** — Office files live/round-trip in OpenCloud; chat + RAG + all tools live in Open WebUI (+PGVector). **AnythingLLM and LocPilot retired** once the Office MCP path lands. · [llm-office.md](docs/llm-office.md), [ai-stack.md](docs/ai-stack.md) |
| HD-109 | 2026-08-17 | *(decision)* **Office MCP bridges exposed over Headscale-only, token-auth** — bind to the Headscale interface only (no public, no host `0.0.0.0`), token-auth, matching the repo exposure convention. · [network-vpn.md](docs/network-vpn.md) |
| HD-110 | 2026-08-17 | **Research: unified vs per-app Office MCP bridge + client topology** — ✅ **Complete (2026-08):** one unified bridge (PPT+Excel+Word) behind a single Headscale endpoint + token is feasible, but **needs a thin STDIO→HTTP wrapper** we own — Open WebUI's MCP client is `streamablehttp_client` (remote HTTP), while every native COM server (`ppt-mcp`, `word-mcp-live`, `office-mcp`) is stdio-local. **Recommendation:** unified bridge v1 on the shared family desktop; use mature per-app backends (`ppt-mcp` 1.7.0 + `word-mcp-live` + an added Excel) or adopt immature unified `office-mcp` (0★) for PoC. Excel-live server gap is an open risk. Gates HD-111. · [hd110-office-mcp-research.md](docs/hd110-office-mcp-research.md) |
| HD-51 | 2026-08-16 | *(decision)* **Family users & shared-data ownership (multi-axis identity model)** — ✅ **Decided (2025-08-16):** keep three **separate** ownership axes — ① **persons** = one Authentik identity each (4 family + `guest`); ② **shared bytes** = a neutral `media`/`media` **system** account (dedicated uid/gid, e.g. `1005`, stored once as `storage_uid`/`storage_gid` — **not** `domen`, **not** 1000) owning the shared NAS datasets + local shared files; ③ **processes** = least-privilege containers using `storage_uid`/`storage_gid` (→ HD-94 / KOPS-060). oldsrv: each human = own Linux login (4 + guest), auto-login = primary family account (**not** domen), family users **not** in the `docker` group. **nas: no interactive human logins** — only `ansible-admin` + `ai-debug` (ops) + the neutral system owner. **OpenCloud: Authentik OIDC per-person accounts** (native SSO, not Forward-Auth) so each user gets real per-user file isolation; storage on a NAS dataset owned by the neutral uid. · [services-authentik.md](docs/services-authentik.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-56 | 2026-08-16 | *(decision)* **Host network config-manager: systemd-networkd vs netplan** — ✅ **Decided: systemd-networkd.** Rationale: native Debian server tooling, no Python dependency (netplan pulls python3+libnetplan), Ansible `community.general.systemd_network` module is mature, consistent with existing systemd timer usage (sanoid/syncoid/db-backup). Netplan rejected — Ubuntu desktop default, extra translation layer on Debian server. Blocker cleared for network role's static-IP/VLAN-trunk provisioning. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-92 | 2026-08-16 | *(decision)* **oldsrv stays bare-metal Debian — no local Proxmox / no GPU passthrough on the single Phase-1 box** — ✅ **Decided (2025-08-16):** keep Phase 1 as bare-metal Debian + Docker on oldsrv. Rationale: one shared dGPU (RX 7600) is needed by **both** the family desktop and the AI stack (Ollama/immich-ML) — VM/GPU-passthrough is mutually exclusive and would force choosing one; a single host gains no HA/migration from VMs; hypervisor + ~40 containers is RAM-heavy and fights the "works without Domen" priority. **Proxmox deferred to HD-41/HD-42** (Phase 2) once a real 2nd local node exists. If desktop isolation ever becomes a hard requirement → dedicated 2nd local desktop box, **not** a VM on oldsrv. · [hardware-oldsrv.md](docs/hardware-oldsrv.md), [IaC/README.md](IaC/README.md) |
| HD-93 | 2026-08-16 | *(decision)* **Buy Contabo VPS before go-live; public edge on VPS from day one** — ✅ **Decided (2025-08-16):** purchase the VPS pre-go-live and fold **HD-40A/HD-40B forward into Phase 1** — public Traefik + CrowdSec + Authentik and public apps (Forgejo, Grafana, OpenCloud web) terminate TLS on the VPS (WG S2S → oldsrv backends); oldsrv becomes an internal/LAN box (leaner, less exposed → mitigates the audit's SPOF critique). Does **not** change HD-92 — the VPS is remote/GPU-less, hosts no desktop or AI, and is not a substitute for a 2nd local node. · [services-vps.md](docs/services-vps.md) |

## Implementation & tooling

| ID | Date | Item |
|----|------|------|
| HD-01 | 2026-08-15 | **Create remaining `docker_services` compose templates** — 24 created ✅ (pre-existing), 17 TODO stubs implemented ✅ (bazarr, dozzle, immich-app, immich-ml, jellyfin, lidarr, ollama, pihole, profilarr, prowlarr, qbittorrent, radarr, recyclarr, sabnzbd, seerr, sonarr, sunshine). 18+ compliance fixes applied across all 43 templates. Immich v3 updated (microservices merged, postgres/valkey images). All validators pass. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-07 | 2026-08-15 | **NUT clients on `oldsrv` + `pi`** — upsmon slave, per-host shutdown delay 60/0/0. ✅ **IaC done:** host_vars wired (oldsrv 60 / pi 0), client upsmon + secret-free upssched-cmd deployed on clients, **fixed deferred-shutdown bug** (`shutdown -h +60` = minutes not seconds → now owned by the upssched ONBATT timer `AT onbatt-delay * EXECUTE powerdown`; LOWBATT stays immediate). ⏳ Not deployed (hosts unprovisioned). · [hardware-ups.md](docs/hardware-ups.md) |
| HD-10 | 2026-08-15 | **Generate `oldsrv/preseed.cfg`** — created: ext4 root on 960 EVO 500 GB, 970 EVO 1 TB left raw for ZFS pool `nvme`, XFCE desktop, GRUB on NVMe. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-11 | 2026-08-15 | **Create Pi first-boot config** — `first-boot-config.sh` writes cloud-init `user-data` + fallback SSH enable on boot partition for raspi.debian.net images. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-12 | 2026-08-15 | **Implement `inventory.md` render pipeline** — enhanced `inventory.md.j2` template (proper frontmatter, 5-column table, render instructions), created initial `docs/inventory.md` from group_vars data. Render hook already wired in `render-docs.yml` + `docker_services` post-deploy. · [interfaces.md](docs/interfaces.md) |
| HD-16 | 2026-08-15 | **Implement Authentik compose template + Traefik Forward-Auth middleware** — server+worker+postgres+redis compose (115 lines), `authentik-forward-auth@file` middleware (42 lines), `subdomain: sso` entry. All implemented in `db51854`. · [services-authentik.md](docs/services-authentik.md), [services-traefik.md](docs/services-traefik.md) |
| HD-19 | 2026-08-15 | **Pi SD-card wear: trim HA recorder + log strategy** — recorder `purge_keep_days: 2` / `commit_interval: 5` / `exclude` (not disabled: keeps Logbook/Energy-LTS/history_stats) + Docker log driver `local` (10m×2) + `journald Storage=volatile` + `/var/log` tmpfs. Implemented as HA config template + docker role daemon.json + home_assistant role tasks (all gated to Pi only). · [observability.md](docs/observability.md) |
| HD-35 | 2026-08-15 | **Fix broken `network-devices.md` reference** — link was already removed from network-vlans.md (resolved). The content lives in `assets/Network-Devices.canvas`. · [network-vlans.md](docs/network-vlans.md) |
| HD-50 | 2026-08-15 | **Implement `docker_services` Ansible role** — ✅ **Done (`eadf9d4`).** Role creates external Docker networks, copies templates, renders Jinja2 with 1Password lookups, runs `docker compose up -d`. Added: systemd auto-start per service (`docker-compose@.service` template unit + `systemctl enable`), post-deploy Homepage reload + `inventory.md` render. Fixed pre-existing path bugs (templates at playbook level, not in role). All 43 templates validated. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-61 | 2026-08-18 | **Pin image tags — Traefik first** — ✅ **Done (`85d387c`).** `traefik_version: latest` → `v3.5.2` in `group_vars/all.yml` (shared by oldsrv edge + Pi `traefik-ha`); matches existing `v`-prefixed pin style (`certs_dumper_version: v2.8.3`); Renovate docker datasource follows. Other mutable tags tracked separately. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-64 | 2026-08-18 | **Fix Loki schema `from:` date** — ✅ **Done (`85d387c`).** `2026-01-01` → `2025-01-01` in `loki.yaml.j2` so the TSDB schema is active for any deployment from Jan 2025 (also covers KOPS-065; avoids silently dropping logs). · [observability.md](docs/observability.md) |
| HD-114 | 2026-08-18 | **Drop dead Alertmanager scrape + phantom alerting block** — ✅ **Done (`85d387c`).** Removed obsolete `alertmanager` scrape job and `alerting.alertmanagers` block from `prometheus.yml.j2` (`alertmanager:9093` not deployed — Grafana Alerting covers Phase 1 per observability.md). Comment notes Phase-2 re-add. · [observability.md](docs/observability.md) |
| HD-62 | 2026-08-18 | **Remove / unbind host ports** — ✅ **Done.** Dropped signal `8080` host binding (n8n via services-internal overlay); loopback-bound prometheus `9090` + loki `3100` (host Alloy agent reaches via localhost); dropped redundant technitium `5380` (web UI via Traefik overlay), kept `53` (network-wide DNS by design); sunshine streaming ports kept w/ justification (manual-start gaming, LAN/VPN clients). · [deployment-compose.md](docs/deployment-compose.md) |
| HD-65 | 2026-08-18 | **Fail-loud on missing secrets** — ✅ **Done.** Removed `default('')` from pihole `WEBPASSWORD` so a failed 1Password lookup aborts the render instead of deploying an unprotected Pi-hole; added a comment documenting the fail-loud policy. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-91 | 2026-08-18 | **Fail-closed guards on missing secrets** — ✅ **Done.** Removed `default=''` on nut SMTP creds (`nut-smtp_login`); added a fail-closed `assert` guard in `docker_services` role that aborts with a clear `fail_msg` if `op_vault` is unset or the 1Password CLI is missing before any compose render. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-82 | 2026-08-18 | **Grafana single auth path** — ✅ **Done.** Added `GF_AUTH_DISABLE_LOGIN_FORM: "true"` to grafana compose so every login goes through Authentik Forward-Auth; the admin account stays reachable for provisioning via API/basic auth (break-glass). · [observability.md](docs/observability.md) |
| HD-116 | 2026-08-18 | **Alloy/Prometheus probe correctness** — ✅ **Done.** Added per-host `instance` label (`{{ inventory_hostname }}`) to the Alloy host-metrics scrape so series don't collide across hosts (KOPS-036); removed 401/403 from blackbox `http_2xx` valid_status_codes so auth-walled/broken endpoints are flagged, not counted up (KOPS-035). · [observability.md](docs/observability.md) |
| HD-117 | 2026-08-18 | **Playbook role-order fixes** — ✅ **Done.** Documented KOPS-050 dependency (network→storage→docker_services) in `home_servers.yml` (already correctly ordered); reordered `raspberry_pi.yml` so `docker_services` runs before `home_assistant` (KOPS-063). · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-60 | 2026-08-18 | **crowdsec-only middleware chain** — ✅ **Done.** Added `crowdsec-only@file` (chain = crowdsec-bouncer only) to `middlewares.yml.j2`; applied it to every self-auth'd route that can't use Forward-Auth: ha (oldsrv route + standby), jellyfin, matrix, seerr, chat. · [services-traefik.md](docs/services-traefik.md) |
| HD-118 | 2026-08-18 | **Apply `crowdsec-only` to HA route** — ✅ **Done (KOPS-004).** oldsrv `traefik` ha route + `home-assistant-standby` compose label get `crowdsec-only@file`; Pi `traefik-ha` edge documented as intentionally CrowdSec-free (CrowdSec is on oldsrv, down on failover — bouncer would fail the very route it must survive). · [services-traefik.md](docs/services-traefik.md) |
| HD-119 | 2026-08-18 | **Seerr behind CrowdSec** — ✅ **Done (KOPS-047).** Applied `crowdsec-only@file` (own login; KOPS-047 recommends CrowdSec-only, not Forward-Auth, since family non-admins submit requests directly). · [services-traefik.md](docs/services-traefik.md) |
| HD-120 | 2026-08-18 | **Element Web `chat.kogler.si` middleware gap** — ✅ **Done (KOPS-025/054).** Applied `crowdsec-only@file`; Matrix-native SSO means no Forward-Auth. · [services-traefik.md](docs/services-traefik.md) |
| HD-121 | 2026-08-18 | **Matrix homeserver `:latest` → pin** — ✅ **Done (KOPS-030).** Added `tuwunel_version` group_var + template reference (matches traefik/certs convention); kept `latest` default with an explicit MUST-pin comment since the obscure single-maintainer tag can't be safely blind-pinned before a registry check. · [services-matrix.md](docs/services-matrix.md) |
| HD-79 | 2026-08-18 | **Pin RaspberryMatic USB path per host** — ✅ **Done (KOPS-040).** Replaced the non-resolving wildcard `/dev/serial/by-id/*` with a per-host `homematic_usb_by_id` host_var (Pi + oldsrv) referenced by the template; placeholder filled at deploy. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-123 | 2026-08-18 | **RaspberryMatic compose hygiene** — ✅ **Done (KOPS-038/049).** Remapped CCU admin UI host port off `80` (Traefik conflict) to per-instance `8085`/`8086` primary/standby via `rmat_ui_port`; duplicates/conflicts resolved. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-77 | 2026-08-18 | **Split `n8n_password`** — ✅ **Done (KOPS-031).** `n8n_password` stays `N8N_ENCRYPTION_KEY` (immutable); new `n8n-webhook_api` → `N8N_BASIC_AUTH_PASSWORD` so webhook/API auth is independently rotatable; Grafana contact point sends basic auth (`grafana`/token). Updated deployment-secrets.md master list. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-125 | 2026-08-18 | **Signal CLI REST API auth** — ✅ **Done (KOPS-002).** Added `SIGNAL_CLI_API_TOKEN` (from `signal-internal_api`) requiring an `X-Api-Key` header, so no container on services-internal can send Signal as Domen's number; host port already unbound in HD-62. n8n sends the token. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-126 | 2026-08-18 | **OpenCloud edge-terminated TLS decision** — ✅ **Done (KOPS-006).** Documented that `OC_INSECURE: true` is CORRECT behind the Traefik edge (forcing false would break the HTTP backend); plaintext confined to the isolated traefik-public Docker network; internal mTLS deliberately not adopted (no gain vs edge TLS + CrowdSec for a single trusted host). · [deployment-compose.md](docs/deployment-compose.md) |
| HD-63 | 2026-08-18 | **Uncomment immich DB backup + OpenCloud tar note** — ✅ **Done (KOPS-026).** Enabled DB03 block for `immich-postgres` (fixed stale `immich-db` name, on db-internal); added note that OpenCloud uses an embedded DB → its backup is a data-dir tar (covered by service-state/Kopia scope), not a DB dump. · [backup.md](docs/backup.md) |
| HD-80 | 2026-08-18 | **Disable root login in preseed** — ✅ **Done (KOPS-044).** `d-i passwd/root-login` set to `false` on nas + oldsrv (removes the identical placeholder-hash + password-root attack surface; `ansible-admin` has key-only SSH + NOPASSWD sudo). deployment-preseed.md updated. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-86 | 2026-08-18 | **Stop persisting OP token in `~/.bashrc`** — ✅ **Done (KOPS-011).** `bootstrap.sh` now stores `OP_SERVICE_ACCOUNT_TOKEN` in `~/.config/op/homelab-sa-token` (chmod 600) and sources it, instead of embedding the plaintext secret inline in the world-readable `~/.bashrc`. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-87 | 2026-08-18 | **Pin CrowdSec bouncer plugin version** — ✅ **Done (KOPS-029).** Added `crowdsec_bouncer_plugin_version: v0.4.0` to group_vars/all.yml; removed the hardcoded `default('v0.4.0')` from the traefik compose. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-88 | 2026-08-18 | **Dedup sshd_config append** — ✅ **Done (KOPS-012).** `post_install.sh` now guards the sshd hardening append with a marker check so a re-run never accumulates duplicate directives. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-127 | 2026-08-18 | **Deploy scripts not root on live** — ✅ **Done (KOPS-056).** Added a dedicated non-root `svc-backup` system user (in docker group, owns the staging dir) and set `User=`/`Group=` on the `push-services` systemd unit so docker exec backups don't run as root; added post-backup integrity checks (`unzip -t` on the Forgejo dump, `PRAGMA quick_check` on the n8n sqlite) so corruption is caught at backup time, not restore. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-81 | 2026-08-18 | **Shrink HA `trusted_proxies`** — ✅ **Done (KOPS-039).** Replaced the blanket traefik-public `/16` trust with an explicit `ha_trusted_proxies` group_var list of specific Traefik edge container IPs (both primary + standby render the same config). Prevents a compromised sibling container from spoofing client IPs. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-78 | 2026-08-18 | **Router INPUT-chain firewall** — ✅ **Done (KOPS-003/009).** Added `chain: input` rules to the router role: management service ports (`22,8728,8729,8291,80,443`) reachable only from the Management VLAN (99) and `trusted-admin` (nas/oldsrv/ha-vip), dropped from all other sources; established/related + DHCP input accepted so the control plane and clients keep working. · [network-ops.md](docs/network-ops.md), [network-vlans.md](docs/network-vlans.md) |
| HD-83 | 2026-08-18 | **Restrict router API in bootstrap .rsc** — ✅ **Done (KOPS-003/042).** `rb4011_initial.rsc.j2` binds `api`/`www-ssl`/`ssh` to the Management VLAN interface (`vlan{mgmt}-mgmt`) so none listens on WAN/other VLANs in the bootstrap window; plain `www` and `api-ssl` (no TLS cert) left disabled. · [network-ops.md](docs/network-ops.md) |
| HD-84 | 2026-08-18 | **Headscale ACL policy** — ✅ **Done (KOPS-022).** Added a real `policy.hujson` (mount + `_extra_templates` + compose volume); OIDC auto-approved nodes need the admin-applied `tag:kogler` to reach the mesh (`tag:kogler:*`), admin plane always reachable; corrected the misleading "requires admin approval" comment. · [network-vpn.md](docs/network-vpn.md) |
| HD-90 | 2026-08-18 | **Renovate managers: ansible-galaxy + pip** — ✅ **Done (KOPS-062).** Added `ansible-galaxy` + `pip_requirements` managers (with the same 3-day stability delay as Docker) so Renovate tracks `IaC/ansible/requirements.yml` collections (community.general/routeros/docker) and `client/office-bridge/requirements.txt`, not just Docker images. · [deployment-renovate.md](docs/deployment-renovate.md) |
| HD-94 | 2026-08-18 | **Centralize shared-data owner + Samba + MinIO (HD-131 D1/D2/D4/D5)** — ✅ **Done (KOPS-060).** `storage_uid`/`storage_gid`=`1005` (`media`), neutral owner provisioned on nas+oldsrv; 9 filesystem/SMB templates (sonarr, radarr, lidarr, prowlarr, bazarr, sabnzbd, qbittorrent, jellyfin, opencloud) centralized to the vars; Samba shared `media` + per-user shares with idempotent provisioning; MinIO S3 service + Immich→S3 originals; Authentik→NAS/OpenCloud glue (`sync-authentik-users.sh`, D5). Remaining: populating `storage_samba_users` once family accounts exist (HD-51). · [deployment-compose.md](docs/deployment-compose.md) |
| HD-85 | 2026-08-18 | **Add CrowdSec collections** — ✅ **Done (KOPS-041).** Moved the hardcoded `COLLECTIONS` env into a `crowdsec_collections` group_var (`all.yml`) and extended it to home-assistant, matrix, grafana parsers (on top of traefik + linux) so the exposed auth-surfaces are parsed by the WAF. Added the var to the render validator mock. · [observability.md](docs/observability.md), [services-traefik.md](docs/services-traefik.md) |

> Decisions were moved into their owning docs; the changelog row keeps the decision summary for audit trail. Items marked `*(decision)*` are `Decided` rather than `Implemented`.

