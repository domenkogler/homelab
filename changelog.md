# Changelog — Kogler Homelab

> Append-only log of completed/decided work. Migrated out of `todo.md` on 2026-08-17.
> Dates are best-effort git attributions (module/commit dates); verify against the owning doc before
> relying on them — they are advisory, not SSOT.

**Done items migrated: 27** — HD-01, HD-05, HD-07, HD-10, HD-106, HD-107, HD-108, HD-109, HD-11, HD-110, HD-12, HD-16, HD-19, HD-22, HD-24, HD-25, HD-29, HD-31, HD-35, HD-39, HD-50, HD-51, HD-52, HD-55, HD-56, HD-92, HD-93

## Decisions & research

> Item names inside rows reflect naming at decision time; current canonical names per the
> [deployment-secrets.md](docs/deployment-secrets.md) rename map (e.g. HD-53's `network-snmp_login` → `network-snmp_api`). Rows are never edited (append-only).

| ID | Date | Item |
|----|------|------|
| HD-259B | 2026-08-26 | *(tooling)* **pi.dev bootstrap for the WSL Debian runner — `scripts/install-pi-wsl.sh` + portable extensions in-repo — ✅ Done (authored, not yet run)** — installs pi natively in Debian WSL (official [`pi.dev/install.sh`](https://pi.dev/install.sh), standalone Node 22.19+ since Debian 13 apt ships nodejs 20.x and no native node/nvm exists in the distro — live-verified `NATIVE=none`), then deploys the **repo SSOT → `~/.pi/agent`** mirroring `update_pi.cmd`: `skills/` (HD-254 artifact-ignore: `net.json`, `__pycache__/**`, zero-byte skill-name markers), `pi-agent/` (`AGENTS.md` + `prompts/start.md`), the tracked pi package set (`npm:@ogulcancelik/pi-ssh-tools`, `npm:pi-subagents`, `npm:@season179/pi-worktree`, `npm:pi-deepseek-optimized` — mirror of Windows `settings.json` packages), and portable extensions from the new `pi-agent/extensions/` (autotalk settings). **`remote-bash.ts` intentionally NOT copied** (hardcodes Windows `sshpass.exe`/`C:\…` — not WSL-portable). Modes `--pi-only` / `--config-only` / `--reload` (info); env-overridable `REPO`/`PI_PACKAGES`; secrets/models NOT synced (auth.json, models.json, settings.json hold API keys + machine-local lmstudio endpoint — per-side `/login` + `/model` guidance printed). Registered in `scripts/README.md`; `bash -n` clean; `validate-all.sh` green; `--reload` smoke-tested inside the live Debian distro. Owner: user (pi.in-WSL companion to `git-bootstrap.sh`, HD-259). · [scripts/install-pi-wsl.sh](scripts/install-pi-wsl.sh), [scripts/README.md](scripts/README.md), [pi-agent/extensions/](./pi-agent/extensions/) |
| HD-247–251 | 2026-08-26 | *(decisions)* **AI stack v2 architecture locked (owner brainstorm): two-instance capability-tiering + LiteLLM v2 spine + DSH replaces Hermes** — **(a)** Two-instance OWUI split: `chat.kogler.si` PUBLIC family instance holding ONLY limited-capability credentials (family-chat + embed keys, no agents; knowledge = **Family Manuals KB ONLY**, admin-only ingestion, user knowledge-creation disabled); `ai.kogler.si` becomes the INTERNAL full-power instance served tailnet-only via Pattern-A sidecar (**public DNS record for `ai.` dropped/repointed**); Element Web renamed to `msg.kogler.si`. **(b)** Capability-tiering posture codified: internet-facing surfaces hold limited-capability credentials only; agent-capable keys exist solely behind tailnet membership; new admin/UI surfaces default tailscale-first — mechanism documented as Patterns A/B in network-vpn.md §Tailnet-exposed services, invariant in security.md §10. **(c)** LiteLLM v2 spine: Postgres-backed runtime (`STORE_MODEL_IN_DB`) so models are Admin-UI-managed at runtime (git drift accepted by owner; DB under db-backup/Kopia scope); per-consumer scoped virtual keys minted once by bootstrap glue into 1Password (`owui-public-chat`, `owui-public-rag` embed-only, `owui-int-wife`, `owui-int-owner`, `dsh`, `openclaw`, `rag-int-svc`); master_key retires from every consumer template. **(d)** n8n → internal tier with scoped LiteLLM key + budget cap; external webhook dependency audit prerequisite. **(e)** DeepSeek Harness (DSH) replaces the earlier Hermes placeholder as DevOps/IaC cockpit: verified live (MIT, TS/Cordis plugin architecture, custom OpenAI-compatible provider supported), own/digest-pinned image, netns tailscale serve → 127.0.0.1:3080 (no bridge on services-internal), 443 egress open (accepted risk), Forgejo service-account PAT PR-only/no-merge + branch protection. **(f)** Embeddings stay cohere/embed-v4 @1536 everywhere; public corpus restricted to Family Manuals; wife's public-work corpora internal-only (HD-246 amendment). Implementation registered as **HD-247–HD-251**. · [docs/services-ai.md](docs/services-ai.md), [docs/network-vpn.md](docs/network-vpn.md), [docs/security.md](docs/security.md), [todo.md](todo.md) |
| HD-246 | 2026-08-25 | *(decision)* **RAG retrieval stack locked (brainstorm session): Docling-only OCR ingestion + Cohere embed/rerank via LiteLLM** — owner-approved; every knob source-verified against open-webui `config.py` (pinned 0.11.0) and Cohere docs. **Docling-only ingestion:** SPDF/Tesseract deliberately OUT of the RAG path (pdf.kogler.si stays the searchable-PDF artifact tool; JPEG/PNG/TIFF/BMP/WEBP are Docling first-class inputs; chat-time image Q&A = LiteLLM vision models, not an ingestion OCR path). Params: cohere/**embed-v4.0 @ 1536 dims** (dimension locks at first ingest — change later = full re-ingest + re-index) called direct-Cohere via LiteLLM (OpenRouter carries no embeddings endpoint); Cohere **rerank v4.0-pro** as EXTERNAL reranker through LiteLLM `/rerank` (never the in-container sentence-transformers engine — read_only rootfs + VPS RAM + spine rule); hybrid BM25+vector search ON (weight 0.5 start); two-stage retrieval 20 candidates → rerank → top 5; **token-based** chunking 512/64 (`RAG_TEXT_SPLITTER=token` mandatory — default splitter counts CHARACTERS, so a plain "512" would silently become ~120-token chunks); `ENABLE_PERSISTENT_CONFIG=false` so Ansible-rendered env vars stay SSOT (persistent-config default lets the live DB silently win over later env changes); PGVector index = HNSW `vector_cosine_ops`@1536 as an IaC task (no hand-SQL per HD-220). Found during research: litellm template mis-pins `embed-english-v3.0` (English-only v3) under the name `cohere/embed-v4` — fix folds into the implementation task. Implementation registered: **HD-246**. · [docs/services-ai.md](docs/services-ai.md), [todo.md](todo.md) |
| HD-247 | 2026-08-26 | **LiteLLM spine hardening cutover — scoped keys + models-in-DB** - ✅ **Done (IaC 2026-08-26).** Registry-verified pin `litellm_version: v1.83.10-stable` + `litellm_db_version: 16.15-alpine`; bundled litellm-db Postgres sidecar (db-internal only) + `STORE_MODEL_IN_DB=true` (models become Admin-UI-managed; git drift accepted by owner decision); `model_list` removed from config.yaml.j2; DATABASE_URL folded `>-` w/ urlencode; db-backup DB06 + backup.md critical-state row (keys/spend/models live in litellm-db); `bootstrap-keys` glue vault-first idempotent (empty item → POST /key/generate → sk into 1P; non-empty → live `/v1/models` probe with 401⇒abort stale-secret; unique-alias conflict ⇒abort; docker-exec python3 @127.0.0.1:4000, no new host ports; no_log; runs before open-webui/openclaw render like prepass-authentik); scoped-key SSOT = `litellm_scoped_keys` in vps.yml (7 specs: owui-public-chat_api / owui-public-rag_api / owui-int-wife_api / owui-int-owner_api / dsh_api / openclaw-litellm_api / rag-int-svc_api); master_key demoted to admin/bootstrap ONLY (zero consumer-template hits); scanner sees all 8 new items (self-test 20/20); deviations recorded: openclaw-litellm_api name (collision with existing openclaw-* convention), rag keys carry rerank-v4.0-pro because HD-246 routes /rerank through the same key (embed-only would break the leg). ⏳ **Deploy-gated:** owner seeds `litellm_db` (+ HD-211 real provider/forgejo keys) → converge vps.yml (glue mints the 7 sks) → Admin-UI model recreation (compose-header runbook step) → live-verify scoped keys + open-webui/openclaw against them. Unblocks HD-250/HD-248. · [docs/services-ai.md](docs/services-ai.md), [docs/deployment-secrets.md](docs/deployment-secrets.md), [docs/deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md), [docs/backup.md](docs/backup.md), [todo.md](todo.md) |
| hd244+hd245 | 2026-08-26 | **check-vault-items.sh: `*_item` registry-key visibility + `--strict`/self-test fixture — ✅ Done** Scanner now parses scalar `*_item:` values per ENTRY RECORD from enabled service entries in group_vars/vps.yml + home_servers.yml (keys sit on continuation lines; a record runs from its `- {` line to the first comment-stripped `}`), closing the HD-244 blind spot class (`db_item`/`db_ro_item` consumed dynamically as `svc.<key>`, ungreppable); literal `enabled: false` skipped, glue-seeded exclusions untouched; scratch moved to mktemp+trap. HD-245: `--strict` exits non-zero on MISSING-and-not-glue (default stays informational); committed self-test fixture + runner under scripts/testdata/check-vault-items/ (--root/--fake-vault, no op CLI) wired into validate-all.sh. Regression proof: old logic needed=39 WITHOUT metabase-forgejo_ro, fixed scanner =40 with exactly that one-item delta; self-test later extended to 20 asserts covering the HD-247 scoped-key class. Live-confirm of the real-vault run rides HD-242's deploy gate. · [scripts/check-vault-items.sh](scripts/check-vault-items.sh), [scripts/testdata/check-vault-items/run.sh](scripts/testdata/check-vault-items/run.sh), [scripts/README.md](scripts/README.md), [todo.md](todo.md) |
| HD-240 | 2026-08-24 | **stats.kogler.si SSO login dead-end fixed (live)** — ✅ **Done (IaC + live ops).** Four stacked causes, each live-probed (④ found in round 2 after owner retest still bounced): ① traefik edge container IP drifted off the whitelisted `traefik_edge_ips` /32 (Docker dynamic assignment) → all forwarded headers rejected → bare `/login`; fixed by pinning the edge IP via compose `ipv4_address` = new `traefik_edge_ip_pin` group_var (validator SSOT-loader extended); ② grafana datasource never sent basic-auth (missing `basicAuth: true` flag — wire-capture proven) AND grafana 13.2.0 file-provisioning stores a secureJsonData blob it cannot resolve at use time → Prometheus datasource moved to idempotent API seeding in the monitoring role (create-only + until-looped health verify; template keeps Loki only; rotation = delete + re-run); ③ UPS alert rules used invalid PromQL bitwise-& (masked by ② since birth) → modulo bitmask tests; ④ (round 2 — the actual browser blocker) traefik silently DROPPED the map-form `authResponseHeaders` field (schema wants a list) → identity headers never reached grafana; fixed to list form, decode-verified live. Owner identity mapped into Grafana as Admin via break-glass API (email matches Authentik). Verified live: forged-header rejection from non-whitelisted container (HD-190 deploy-gate CLOSED), ds-health 200, real query data through the proxy, zero eval errors. ✅ **Owner browser round-trip verified 2026-08-25** — sso dashboard → Grafana UI lands directly; task FULLY closed (todo row deleted per §4 housekeeping). · [docs/observability.md](docs/observability.md), [deployment-journal.md](deployment-journal.md), [deployment-tasks.md](deployment-tasks.md), [todo.md](todo.md) |
| HD-112 | 2026-08-24 | **Zipline public bin + private OIDC file share — ✅ Done (IaC, deploy-gated)** — compose `docker_services/zipline/` (zipline **v4.7.0** + bundled postgres 16-alpine sidecar on db-internal only; read_only + cap_drop ALL; healthcheck-gated depends_on; local datasource `/srv/docker/zipline/uploads`); registry `vps.yml` entry with `db_role_sync` rotation-drift guard; `ks-oidc.yml` `provider_zipline`/`app_zipline` (strict callback `https://bin.kogler.si/api/auth/oauth/oidc` — path verified from v4 source; grant_types + property_mappings pinned per HD-231); secret-egress glue PROVIDERS += `zipline:zipline_oidc`; vault catalog rows (`zipline_oidc`, `zipline_db`, `zipline_password`); Cloudflare CNAME `bin` → vps; versions pins (`zipline_version`, `zipline_db_version`); db-backup **DB05**; backup.md payload row (uploads Kopia-excluded, DB dumped); services.md allowlist row `bin`; utilities catalog + design section flipped 🟢 IaC done ⏳. ⏳ Deploy-gate = the compose-header checklist: human-gated first deploy → local admin (`/auth/setup`) → OIDC verify → flip bypass-local-login in Server Settings → seed `guestbin` user (small quota, no password) + `dropzone` folder (`allowUploads=true`) → anonymous upload→short-URL→viewer round-trip logged-out → 6h sweep verify → family drop script + manual entry. · [docs/services-utilities.md](docs/services-utilities.md), [IaC/ansible/templates/docker_services/zipline/docker-compose.yml.j2](IaC/ansible/templates/docker_services/zipline/docker-compose.yml.j2), [todo.md](todo.md) |
| HD-112 | 2026-08-24 | *(decision)* **Zipline public-bin architecture locked — S3ng tension resolved; single public host + native-OIDC dashboard + guestbin quota-split for anonymous 6h drops** — decided (owner brainstorm; every mechanism source-verified against the Zipline v4 trunk and release **v4.7.0**). Storage: **local filesystem datasource** on VPS NVMe (HD-131-compliant; supersedes the original OpenCloud-S3ng-bucket assumption). Exposure: ONE public host `bin.kogler.si` on the `crowdsec-only@file` tier (security.md §1 never-zero-edge-protection; PairDrop precedent) — viewer/shortener routes and the folder-guest upload API stay anonymous while the dashboard is gated by Zipline NATIVE OIDC (provider declared in the `ks-oidc.yml` blueprint per convention, family-group binding only, `FEATURES_USER_REGISTRATION=false` + local-login bypass; Forward-Auth deliberately NOT stacked — double-auth avoided, Immich/OpenCloud precedent). Anonymous-upload mechanics (v4): FOLDER-scoped — dedicated Zipline-local `guestbin` user (no Authentik identity, never logs in) owns a `dropzone` folder with `allowUploads=true`; guests POST unauthenticated with `x-zipline-folder` + hardcoded `x-zipline-deletes-at: 6h` (global default expiration stays UNSET); uploads attribute to guestbin and draw ITS small quota → global `FILES_MAX_FILE_SIZE` stays generous and NO type/extension blockers ship (owner stores executables privately); public abuse radius = guestbin quota + 6h TTL + rate limit + CrowdSec. Shortener/QR native (dashboard QR modal, `POST /api/user/urls`) — Level-1 static glue page DEFERRED to Phase 2 as a Traefik PathPrefix-priority router (same-origin, no CORS). Ephemeral 6h data EXCLUDED from Kopia scope; persistent private files included (backup/storage doc rows land with the IaC change per the same-change rule). Runtime-editable Server Settings make starting values non-binding. · [docs/services-utilities.md](docs/services-utilities.md), [docs/services.md](docs/services.md), [todo.md](todo.md) |
| HD-216 R2 | 2026-08-24 | **HD-216 close-up: knowledge folded into ALL owning docs; stale provision-token references purged** — ✅ **Done (docs-only).** Audit greps confirmed the rotation mechanism itself is fully documented in services-authentik.md; this pass closed three remaining gaps: ① `deployment-secrets.md` `authentik-provision_api` tombstone now carries the identified root cause and points at the `expiring=False` recipe; ② `deployment-oidc.md` contract de-staled — the blueprint-apply step now names the deterministic `apply-authentik-blueprints.yml` one-shot (HD-230b) instead of the retired provision token, and the fail-closed paragraph describes the ephemeral per-run mint with a pointer to the auto-rotation section; ③ `todo.md` HD-211 row corrected — its "rotate the `authentik-provision_api` SA" step is moot (item retired 2026-08-22), batch refocused on `openrouter_api`/`cohere_api`/`forgejo_api` keys plus the HD-216 tails (`expiring=False` mandatory for any persisted Authentik token in the batch; one-time verification of `authentik-api_token`). Historical `.update(expires=None)` mentions in changelog/journal are records and intentionally untouched (append-only). · [docs/services-authentik.md](docs/services-authentik.md), [docs/deployment-secrets.md](docs/deployment-secrets.md), [docs/deployment-oidc.md](docs/deployment-oidc.md), [todo.md](todo.md) |
| HD-216 | 2026-08-24 | **Authentik background token rotator IDENTIFIED — upstream auto-rotation of expiring API tokens; durable-token recipe documented** — ✅ **Done (offline source research; NO live probing).** The Phase-1 mystery (persisted Bearer credentials rewritten server-side within minutes) is a legitimate upstream feature: `clean_expired_models` runs on an ≈every-5-min crontab (`2-59/5 * * * *`) sweeping rows with `expiring=True AND expires<=now()`, and `Token.expire_action()` ROTATES api-intent tokens — new key + fresh `tenant.default_token_duration` expiry (upstream default `days=1`) + a `SECRET_ROTATE` audit event — while recovery/verification/app_password intents are deleted at expiry. Bare ORM creates carry `expiring=True`, so even an `expires=NULL` token is rotated on the first sweep; the previously documented ak-shell `.update(expires=None)` advice was ineffective and is FIXED in the owning doc. ONLY `expiring=False` is exempt → codified as THE durable-persisted-token rule (set user attribute `goauthentik.io/user/token-expires: false` BEFORE creating via UI/API); the tenant `default_token_duration` knob only slows rotation, never prevents it. Consequences applied: glue keeps its ephemeral mint (immune by construction); HD-211's scoped persisted `authentik-provision_api` re-issue is unblocked via the recipe; `authentik-api_token` gets a one-time `expiring=False` verification at its next live touch; app-password vanish-at-expiry flagged for future LDAP bind users (HD-132). Source read at upstream branch `version-2026.5` (pinned image 2026.5.6 = same minor). · [docs/services-authentik.md](docs/services-authentik.md), [deployment-journal.md](deployment-journal.md) |
| HD-233 | 2026-08-24 | **Headplane admin UI for Headscale at `vpn.kogler.si/admin` — co-deployed service + OIDC via shared headscale client** — ✅ **Done (IaC + DEPLOYED + live-verified 2026-08-24).** Root cause of the blank page: stock headscale serves `BlankPage()` at `/` by design — no built-in admin UI. Added Headplane (`ghcr.io/tale/headplane:0.7.0` — GHCR tags bare semver), Traefik `Host(vpn.kogler.si) && PathPrefix(/admin)` (longer rule wins priority; crowdsec-only), `server.base_url` = public root. OIDC reuses the **same Authentik client as headscale** (identical client_id; `ks-oidc.yml` gained redirect `…/admin/oidc/callback`, both pinned). Vault items `headplane_api` (Headscale API key, minted) + `headplane_password` (Password category → lookup `field='password'`). Deploy hit 3 crash-loop bugs fixed live: duplicate `_extra_templates` key (dir-vs-file EISDIR), block-scalar indentation, wrong 1P field. **YAML-block-scalar `>-` convention made the default** for secrets rendered into YAML configs (rotated `headscale_api` contains `:` `"` `'` `?` — broke headscale+headplane on restart); applied to headscale/headplane/recyclarr/prometheus/traefik-middlewares, TOML uses escaping (`tuwunel`). LIVE: headplane `Up (healthy)`, owner SSO login ✓, `/admin/` 302, `disable_api_key_login: true`; crash-log secrets scrubbed. · [docs/network-vpn.md](docs/network-vpn.md), [docs/deployment-secrets.md](docs/deployment-secrets.md), [CONVENTIONS.md](CONVENTIONS.md), [deployment-journal.md](deployment-journal.md) | *(decision)* **CAPsMAN flavor flips LEGACY → MODERN `wifi-qcom-ac`; garage wAP ac declared DEAD** — **decided (owner):** the wAP ac (AP-garaza, 6C:3B:6B:7D:B9:C5) is hardware-dead — live-diagnosed 2026-08-24 via RouterOS API + CRS328 logs: PHY/PoE healthy, board boot-loops then goes totally silent after network init (no DHCP/MNDP/ARP); config reset + Netinstall/Etherboot failed (one BOOTP reply at 00:40, then mute; Netinstall never listed it even with Npcap/firewall fixes). Its death removes one of the two MIPSBE blockers that forced the 2026-08-23 legacy pin. Conditions for the modern fleet: ① dnevna swap per owner plan (spare hAP ac² takes dnevna; classic MIPSBE unit retires), ② garage replacement must be wifi-qcom-ac-capable. Same change: capsman.yml header re-pinned (modern flavor + HD-232 deltas: `/interface/wifi` cap syntax in ap_initial.rsc.j2 now stale until cutover-prep rewrite; VLAN tagging moves to configuration datapath — validate live), migration-inventory rows updated (dnevna swap = hard prerequisite; spalnica wifi-qcom-ac note relevant again; garage lease `10.10.1.4` kept reserved for replacement), journal Phase 1.5 entry added. · [IaC/ansible/roles/router/tasks/capsman.yml](IaC/ansible/roles/router/tasks/capsman.yml), [docs/network-migration-inventory.md](docs/network-migration-inventory.md), [deployment-journal.md](deployment-journal.md) |
| HD-03 R1-editor | 2026-08-24 | **ONLYOFFICE/OpenCloud editor — PARTIAL: .txt works, .docx BROKEN (HD-166 tail)** — owner round-trip test: `.txt` preview+edit+save WORK; `.docx` fails with “No preview available … download instead” — the ONLYOFFICE editor does NOT open office docs. Root cause (high confidence): `.txt` uses OpenCloud's native text preview; office docs fall through because the `frontend.app_handler`/app-provider isn't authoring office MIME → OnlyOffice. Suspect: `COLLABORATION_STORE=nats-js-kv` but **no NATS broker container** (no registry/store to announce office authoring); also rendered `collaboration.app.insecure: true` vs env false. `OC_ADD`+`OC_EXCLUDE` both list `collaboration` (ADD wins; EXCLUDE inert). ⏳ **Needs real fix + re-test; NOT closed.** · [deployment-journal.md](deployment-journal.md), [docs/services-office.md](docs/services-office.md) |
| HD-166-pt.2 | 2026-08-24 | **HD-03 R1-editor CORRECTED — root cause #1: the `collaboration` service never starting; “no NATS” theory DISPROVEN (append-only correction row)** — live `opencloud list` shows **no `collaboration` service** while `nats` (embedded), app-registry, gateway, frontend all run. Compose env pulls BOTH `OC_ADD_RUN_SERVICES=collaboration` AND `OC_EXCLUDE_RUN_SERVICES=collaboration,idp`; in OpenCloud 7.4 running set = defaults − EXCLUDE + ADD, so EXCLUDE nets `collaboration` out even though ADD also lists it → the WOPI app-provider never registers → `.docx` “No preview available … download instead”. The prior “no NATS broker / app-provider not authoring” DIAGNOSIS (see R1-editor row above) was DISPROVEN — embedded NATS runs, ADD does not win over EXCLUDE. **Fix DEPLOYED (Lane A):** `OC_EXCLUDE_RUN_SERVICES` → `"idp"`; `collaboration` now runs. · [deployment-journal.md](deployment-journal.md) HD-166 pt.2, [docs/services-office.md](docs/services-office.md) |
| HD-166-pt.3 | 2026-08-24 | **HD-166 root cause #2: ONLYOFFICE DS WOPI disabled → `/hosting/discovery` 404; FIXED + verified live (append-only row)** — after pt.2, `collaboration` ran but still logged `WopiDiscovery … /hosting/discovery … 404`. Root cause: the DS image defaults `WOPI_ENABLED=false` (`run-document-server.sh:127`), so `local.json` had `wopi.enable:false` and DS never served the discovery XML. **Fix DEPLOYED (Lane A2):** `WOPI_ENABLED: "true"` on the onlyoffice-docs compose → DS regenerates and serves `/hosting/discovery` **200** with the full Word/Excel/PPT discovery XML + auto-generated WOPI keypair (regenerable, no literal secret). Live-verified: onlyoffice-docs recreated healthy, opencloud logs show no new discovery errors. **Owner browser `.docx` round-trip re-test = closing gate — PENDING.** Secondary open: `collaboration.app.insecure: true` env→schema mapping (7.4 rolling). · [deployment-journal.md](deployment-journal.md) HD-166 pt.3, [docs/services-office.md](docs/services-office.md) |
| HD-166-pt.4 | 2026-08-24 | **HD-166 “Invalid access token” 401: REVA-leg JWT secret split-brain — DIAGNOSED; fix PARTIAL/IN-FLIGHT (append-only row)** — pt.3 got `.docx` past “download instead” into the editor, but DS shows 401 “Invalid access token”. OpenCloud collaboration log `wopicontext.go` fails at `DismantleToken` (`failed to dismantle reva token manager … token signature is invalid`) — the REVA leg, not the WOPI leg. Cause: `OC_JWT_SECRET` env is ABSENT, so auth mints the REVA token with yaml `token_manager.jwt_secret` (`eefff1c1…`), while collaboration verifies with `COLLABORATION_JWT_SECRET` env (`169e85ad…`) → mismatch. Also confirmed: `opencloud.yaml` is a persistent bind mount (Aug 22) and `opencloud init` doesn't regenerate it; envdecode (7.4) is the intended override. **Partial fix DEPLOYED (Lane A3, branch `laneA3-hd166-wopisec` commit `4976540`, NOT merged):** `COLLABORATION_WOPI_SECRET`=shared. **Remaining:** set `OC_JWT_SECRET`=same shared value on opencloud compose, converge, then user re-login + `.docx` retry. · [deployment-journal.md](deployment-journal.md) HD-166 pt.4, [docs/services-office.md](docs/services-office.md) |
| HD-166-pt.5 | 2026-08-24 | **HD-166 final fix #1: REVA-leg JWT split-brain CLOSED — `OC_JWT_SECRET` = shared value, chain hash-verified live** — added `OC_JWT_SECRET` (= 1P `opencloud-collab_password`, same as `COLLABORATION_JWT_SECRET`/`COLLABORATION_WOPI_SECRET`) to the opencloud compose; env names re-verified against the v7.4.0 source (`OC_JWT_SECRET;AUTH_SERVICE_JWT_SECRET` mint leg vs `OC_JWT_SECRET;COLLABORATION_JWT_SECRET` dismantle leg). All four collab-chain envs now render via folded block scalar `>-` (owner-directed: `"` in a 1P value breaks the RENDERED compose at docker-compose parse time). Converge after dry-run + 9P gate ✓; verified live: three secret envs hash-identical (`eb63ab…`), collaboration running, discovery 200, zero signature errors; owner re-login → editor OPENS (401 gone). · [deployment-journal.md](deployment-journal.md) HD-166 pt.5, [docs/services-office.md](docs/services-office.md) |
| HD-166-pt.6 | 2026-08-24 | **HD-166 final fix #2: mixed-content "Prenos ni uspel" empty-doc layer CLOSED — Traefik CSP `upgrade-insecure-requests`; task DONE (owner round-trip PASS incl. live co-editing)** — after pt.5 DS still failed document delivery: it emits browser-facing cache URLs (`http://office.kogler.si/cache/files/data/<id>/Editor.bin…`) over the WOPI session websocket, ignoring `X-Forwarded-Proto` (upstream [ONLYOFFICE/DocumentServer#2186](https://github.com/ONLYOFFICE/DocumentServer/issues/2186) class; no server-side knob). Forensics: single successful `/contents` GET then cache reuse, valid `Editor.bin` (`DOCY`), all server logs clean → failure isolated to browser-side fetch; owner DevTools showed the blocked mixed-content XHR. Fix: file middleware `onlyoffice-csp` (`Content-Security-Policy: upgrade-insecure-requests`) attached to the office router next to `crowdsec-only@file`; DS-native TLS route considered and rejected (wildcard-key sprawl, WAF loss/double-TLS, entrypoint fragility). Header verified live post-converge; **owner verified `.docx` open + edit + save + two-browser live sync — HD-166 fully closed** (six root-cause layers total). · [deployment-journal.md](deployment-journal.md) HD-166 pt.5, [docs/services-office.md](docs/services-office.md) |
| HD-03 R8-capsman | 2026-08-24 | **CAPsMAN steady-state as modern wifi-qcom-ac rsc (task 8 — authoring only, no deploy)** — 🔨 **Done (IaC).** Owner decision: steady-state WiFi = **rendered rsc** (NOT ansible api_modify); whole fleet MODERN `wifi-qcom-ac` (HD-232). Created `IaC/router/templates/capsman_steady-state.rsc.j2` (`/interface wifi` security profiles WPA2-PSK passphrases from the 5 wifi-kogler* 1P items at render + datapath VLAN tagging per SSID + provisioning rule master cfg-kogler/slaves), quoted SSIDs; extended `render-routeros.yml` (renders capsman_steady-state.rsc, import-after-bootstrap not-a-reset); updated `capsman.yml` header + `group_vars/router.yml` + `network-vlans.md` to rsc-upload; `ap_initial.rsc.j2` legacy `/interface wireless cap` → modern `/interface wifi cap` with validate-live TODO. **Human-gated at cutover:** ① dnevna swap, ② garage replacement wifi-qcom-ac-capable. TODOs mark wifi-qcom-ac field validation live (fail-loud). NOT deployed. · [IaC/router/templates/capsman_steady-state.rsc.j2](IaC/router/templates/capsman_steady-state.rsc.j2), [IaC/ansible/playbooks/render-routeros.yml](IaC/ansible/playbooks/render-routeros.yml), [deployment-journal.md](deployment-journal.md) |
| HD-03 R6 | 2026-08-24 | **Migration-inventory unknowns refresh (task 6) — read-only live lease dump** — 🔨 **Done (reference).** Live RouterOS `/ip/dhcp-server/lease` probe (mikrotik skill, MSYS_NO_PATHCONV) captured 32 leases. Resolved: `0003B5F29AFDC36` (00:1A:22:1E:F7:FD) = **HMIP-HAP HomeMatic AP** (matches router.yml ether9 + Rack.canvas + rack-connections.json — was a mislabeled lease, WIRED to VLAN 21 IoT-Internet per network-vlans Port Type Reference (cloud-bound HAP needs internet)); NVIDIA Shield confirmed hostname-less. Remaining PENDING-IDENTIFY (owner): `deblab` (00:15:5D = Hyper-V vNIC — which Windows host?) and `truenas` (92:47:15 = locally-administered MAC, no vendor doc). Also logged confirmed Bosch hostnames + family tablet/Domen device MACs. No IP literals in prose (check_doc_ips OK); no secret values; no live mutation. · [docs/network-migration-inventory.md](docs/network-migration-inventory.md), [deployment-journal.md](deployment-journal.md) |
| HD-03 R2-switch | 2026-08-24 | **CRS328 switch role access-port VLAN coverage: add IoT(20)/IoT-Internet(21) tasks; nas-eno ports -> Home(10)** — 🔨 **Done (IaC, task 2).** Added `Configure access port for IoT VLAN (20)` + `Configure access port for IoT-Internet VLAN (21)` blocks in `roles/switch/tasks/main.yml` (mirroring the 99/10/50 blocks); `switch_port_map` nas-eno1/nas-eno2 blank `vlan:` -> `10` (Home, per R-1) and removed the stale duplicate `# NAS eno1` comment. Effect: reolink/knx device ports no longer fall through to the unconfigured->mgmt(99) default. Validators green (YAML, validate-docker-services, check_doc_ips). · [roles/switch/tasks/main.yml](IaC/ansible/roles/switch/tasks/main.yml), [group_vars/switch.yml](IaC/ansible/group_vars/switch.yml), [deployment-journal.md](deployment-journal.md) |
| HD-03 R1 | 2026-08-24 | *(decision)* **Reconcile home-server VLAN/IP/DNS model to Option A (R-1, HD-03 cutover gap closed)** |
| HD-235 | 2026-08-24 | **Rotate shared `headscale_api` OIDC client secret + encode “never a secret VALUE in output” hygiene rule** — 🔨 **Done.** The headscale Authentik OAuth provider regenerated its client_secret (Authentik ORM, `authentik.lib.generators.generate_key`; `client_id` stable — no `generate_client_id` symbol exists) and the new value was persisted into `headscale_api` (item `k6ehl…`, field lengths verified, value never printed). Same change: CONVENTIONS §2 gained a “Secret output hygiene” row + §6 bullet (probes show lengths/prefixes/IDs/hashes only; rotate a shared Authentik client = regenerate in Authentik → persist to 1P → re-render headscale + headplane → verify); docs/1password.md gained the Output-hygiene note + a troubleshooting row for a leaked-value incident. · [CONVENTIONS.md](CONVENTIONS.md), [docs/1password.md](docs/1password.md), [deployment-journal.md](deployment-journal.md) |
| HD-232 | 2026-08-24 | *(decision)* **CAPsMAN flavor flips LEGACY → MODERN `wifi-qcom-ac`; garage wAP ac declared DEAD** — **decided (owner):** the wAP ac (AP-garaza, 6C:3B:6B:7D:B9:C5) is hardware-dead — live-diagnosed 2026-08-24 via RouterOS API + CRS328 logs: PHY/PoE healthy, board boot-loops then goes totally silent after network init (no DHCP/MNDP/ARP); config reset + Netinstall/Etherboot failed (one BOOTP reply at 00:40, then mute; Netinstall never listed it even with Npcap/firewall fixes). Its death removes one of the two MIPSBE blockers that forced the 2026-08-23 legacy pin. Conditions for the modern fleet: ① dnevna swap per owner plan (spare hAP ac² takes dnevna; classic MIPSBE unit retires), ② garage replacement must be wifi-qcom-ac-capable. Same change: capsman.yml header re-pinned (modern flavor + HD-232 deltas: `/interface/wifi` cap syntax in ap_initial.rsc.j2 now stale until cutover-prep rewrite; VLAN tagging moves to configuration datapath — validate live), migration-inventory rows updated (dnevna swap = hard prerequisite; spalnica wifi-qcom-ac note relevant again; garage lease `10.10.1.4` kept reserved for replacement), journal Phase 1.5 entry added. · [IaC/ansible/roles/router/tasks/capsman.yml](IaC/ansible/roles/router/tasks/capsman.yml), [docs/network-migration-inventory.md](docs/network-migration-inventory.md), [deployment-journal.md](deployment-journal.md) |
| HD-229 | 2026-08-23 | **Smart-home cross-VLAN firewall delta + CAPsMAN scaffold — ✅ IaC done (deploy-gated to Phase 1.5 live bring-up)** — CoAP reverse exception added to `roles/router` forward chain (`src=vlan20 → dst-address-list=trusted-ha, udp/5683 accept`, directly after established/related): required by the four Gen1 Shelly RGBW2 units (live-probed 2026-08-23, fw v1.14.0) for HA push state/button latency under the IoT→Home drop-all default; also gives the previously created-but-unused `trusted-ha` address list its first consumer (closes the smart-home-failover.md drift). CAPsMAN ships as a GUARDED SCAFFOLD: SSID/VLAN/item SSOT map in group_vars/router.yml (`routeros_capsman_ssids`, flag off), documented implementation plan in roles/router/tasks/capsman.yml header (security-profiles / configurations / access-list ssid→vlan tagging / provisioning), include-gate in main.yml — real api_modify tasks get written against the LIVE manager during cutover prep (speculative ROS7 field guesses deliberately not shipped). · [IaC/ansible/roles/router/tasks/main.yml](IaC/ansible/roles/router/tasks/main.yml), [docs/network-vlans.md](docs/network-vlans.md), [docs/smart-home.md](docs/smart-home.md), [todo.md](todo.md) |
| HD-193 | 2026-08-23 | **Bootstrap & guard hardening bundle (S3/R1/R2/R3) — ✅ Done** — (S3) `vps` added to the site.yml pre-flight guard host pattern (`home_servers:raspberry_pi:storage:vps`) now that the VPS is provisioned + public; (R1) crs328_initial.rsc.j2 api/www-ssl/ssh now bind to `interface=vlan{{ mgmt.id }}-mgmt` from first boot (B4 binding floor, rb4011 pattern); (R2) ap_initial.rsc.j2 ssh binds to `interface=bridge` (AP = L2-only, no WAN egress per HD-165); (R3) rb4011 bootstrap DHCP DNS literal 1.1.1.1 parameterized to new group_var `bootstrap_dns_servers` (all/main.yml) — Technitium arrives only at Phase 3. Closes audit S3/R1/R2/R3 ahead of the Phase 1.5 cutover as network-ops.md requires. · [docs/network-ops.md](docs/network-ops.md), [deployment-tasks.md](deployment-tasks.md) |
| HD-228 | 2026-08-23 | *(decision)* **Cloud appliances (LG ThinQ ACs, Bosch Home Connect) placed on VLAN 21; migration runbook documented; mDNS reflection rejected** — **decided:** both families are strictly cloud-to-cloud (appliance ⇄ vendor cloud outbound TLS; HA ⇄ vendor API via PAT/OAuth) → VLAN 21 IoT-Internet tier, zero port-forwardings, zero cross-VLAN rules (no mDNS reflection — first entry in network-rejected.md; no multicast routing needed); VLAN 21→WAN stays allow-all deliberately (Bosch needs TCP/8080, both need UDP/123 NTP + DNS — clock drift breaks TLS validation). CAPsMAN constraint recorded: IoT SSIDs must serve 2.4 GHz and keep alphanumeric passphrases (QCA4002 + Gen1 Shelly chips). LG WiFi migration runbook (remote-button pairing reset + ThinQ app re-provision) written into smart-home.md §Cloud Appliances; intended vault item `lg-thinq_api`. Same change: device-table rows (LG/Bosch), network-vlans Port Type row, network-rejected entry. · [docs/smart-home.md](docs/smart-home.md), [docs/network-vlans.md](docs/network-vlans.md), [docs/network-rejected.md](docs/network-rejected.md), [todo.md](todo.md) |
| HD-227 | 2026-08-23 | *(decision)* **Authentik human-user policy — new users are INTERNAL type** — **decided (owner):** human accounts are Internal type (real name/email, active, `family` group membership, WebAuthn+TOTP at first login); External reserved for federated sources, Service account for machine/API identities; superuser capability stays group-based ("authentik Admins", `akadmin` = break-glass) — daily-driver users never join. Codified in deployment-manual §1.5 (runbook SSOT for from-scratch rebuilds), cross-referenced from services-authentik RBAC. Same-change execution record: `Family` group + `domen` user (Internal, domen@kogler.si, MFA enrolled) created on sso.kogler.si; forgejo `domen` admin via §1.7 installer; n8n owner account. · [deployment-manual.md](deployment-manual.md) §1.5, [docs/services-authentik.md](docs/services-authentik.md), [deployment-journal.md](deployment-journal.md) |
| HD-231 | 2026-08-24 | **OIDC login hotfix chain: grant_types + property_mappings wipe (all native-OIDC providers), opencloud CSP wiring triple-fault, external-IdP service set** — ✅ **Done (ORM one-shots live + IaC pins; owner-confirmed logins on git.kogler.si AND first-ever file.kogler.si).** authentik 2026.5's per-provider `grant_types` allowlist was EMPTY on every ks-oidc.yml-created provider (blueprint upsert writes [] for absent array attrs) → authorize rejected `authorization_code`; after fixing grants, UserInfo 403'd (`insufficient_scope`) because `property_mappings` was wiped the same way → tokens carried no scopes. Fix: ORM one-shots set `[authorization_code, refresh_token]` + openid/email/profile ScopeMappings on all 8 providers; BOTH fields pinned per entry in ks-oidc.yml; post-apply regression check proves they survive. Separately file.kogler.si needed THREE more fixes: CSP env is `PROXY_CSP_CONFIG_FILE_LOCATION` (not CSP_CONFIG_FILE — name verified from image binary); csp.yaml schema wraps everything under top-level `directives:` (flat map parses into nothing); proxy reads CSP once at startup so bind-mount content changes need an explicit restart. Plus external-IdP service set per docs: exclude built-in `idp`, KEEP `idm` (stale exclude-idm left LDAP :9236 dead → graph/v1.0/me 500), and `PROXY_AUTOPROVISION_ACCOUNTS=true` so first OIDC login creates the account. Forgejo CLI verbatim recorded (provider key `openidConnect`, no --active flag, delete via `admin auth delete --id N`). · [deployment-journal.md](deployment-journal.md) HD-231 pt.1–7, [docs/services-authentik.md](docs/services-authentik.md), [docs/services-traefik.md](docs/services-traefik.md), [todo.md](todo.md) |
| HD-230 | 2026-08-23 | **Phase-1 wave-2 corrective batch: pairdrop PUBLIC + blueprint one-shot convention + onlyoffice caps + db-backup boot race + nftables scoped flush + renovate latent fix + disabled-service teardown** — 🔨 **Done (IaC, session branch; ⏳ deploy-gated until the surgical converge).** Six parallel diagnostics → one serial editing pass. PairDrop now PUBLIC on both `pairdrop.kogler.si`+`drop.kogler.si` (owner decision; supersedes HD-113 LAN-only): crowdsec-only tier, no forward-auth, traefik-public-only isolation, forward-pairdrop/edge-pairdrop blueprint entries removed (ORM deletes post-converge), stale `pairdrop` CNAME kept + IaC-tracked. Blueprint auto-apply gap root-caused: discovery never registers `/blueprints/custom/*` as BlueprintInstances (28/0 found live) → new `apply-authentik-blueprints.yml` one-shot step in docker_services main.yml. onlyoffice-docs gained the supervisord cap_add set (setuid-105 EPERM class, rabbitmq precedent) — was edge-502 since deploy. db-backup first-run delayed 120s (image default +0 raced its own init: mktemp EACCES ×4, zero dumps ever landed). vps-hardening ruleset now replaces ONLY its own inet filter table (HD-223 residual: 'flush ruleset' wiped docker NAT on every restart); unconditional docker-restart task removed. renovate.json lost the unsupported `"platform": "forgejo"` key (pinned 35.x); service disabled until Forgejo has domen/homelab (instance empty — owner wizard step HD-219 outstanding); token verified valid. New teardown loop removes containers of `enabled: false` services. · [todo.md](todo.md), [docs/services-traefik.md](docs/services-traefik.md), [docs/services-authentik.md](docs/services-authentik.md), [docs/deployment-renovate.md](docs/deployment-renovate.md), [deployment-journal.md](deployment-journal.md) |
| HD-206 | 2026-08-23 | *(execution close-out)* **nas OS reinstalled FULLY AUTOMATED via preseed — Phase 1a complete (all ledger checkboxes), preseed path re-proven** — the Automated grub entry ran end-to-end with ZERO interactive questions (early_command resolved the Crucial by-id; GRUB landed on the Generic_Flash_Disk permanent carrier via the static pin; keyed late_command applied hardening + keys); SanDisk booted explicitly via iLO one-time menu and pulled pre-reboot (HD-226 two-stick roles). Verified: `ansible-admin` key-only SSH, `domen` refused (AllowUsers), hostname `nas`, both Crucial by-id forms resolve, six data-disk ZFS labels intact (pools untouched, stay exported until the Phase-2 import-only storage role), sshd drop-in live. Findings: zfsutils absent on fresh OS → definitive `zpool list/import` check folds into Phase 2; stale whole-disk `rpool` zfs_member label on the OS SSD flagged as optional owner-gated Phase-2 wipefs candidate. Same change: deployment-manual §Phase 1a official-path note flipped (automated = proven official, interactive = fallback), hardware-nas.md current-state banner, ledger tick, journal Phase 1a entry, todo HD-207 tail trimmed. · [deployment-manual.md](deployment-manual.md) §Phase 1a, [docs/hardware-nas.md](docs/hardware-nas.md), [deployment-tasks.md](deployment-tasks.md), [deployment-journal.md](deployment-journal.md), IaC/host/nas/preseed.cfg |
| HD-225 | 2026-08-23 | *(decision)* **Git-worktree convention hardened into CONVENTIONS §6 — every session isolates in `../homelab-wt-<date>-<HHMM>` before touching any file** — **decided (owner, after a live near-miss this session):** an `rm -rf` aimed at an already-existing worktree path hit ANOTHER live session's worktree dir (`session-20260823-phase1-verify`) — survived only because a file lock stopped the delete mid-flight (verified zero loss: 0 deleted tracked files, clean porcelain status). Rule set now owned by CONVENTIONS §6 (SSOT; prompt.md points to it): date AND time in worktree names (same-day collisions impossible), isolation is the DEFAULT for single-session work too, manage ONLY your own worktree via `git worktree add`/`git worktree remove` — never rm/move/rename any worktree directory; on `path already exists`, abort and pick a new name instead of cleaning up. · [CONVENTIONS.md](CONVENTIONS.md) §6, [prompt.md](prompt.md) |
| HD-226 | 2026-08-23 | *(decision)* **nas boot chain finalized — Generic_Flash_Disk (C3EB7FE7) stays PERMANENT GRUB carrier; SanDisk = installer medium only** — **decided (owner):** the Gen8 cannot boot from the internal SATA port carrying the Crucial SSD, so GRUB lives on USB permanently (extends HD-206). The preseed pins `grub-installer/bootdev` STATICALLY to the Generic by-id — an interim runtime usb-* resolver was reverted because with two sticks attached it would be ambiguous. Install roles documented: SanDisk boots the installer and is removed afterwards; Generic stick must be plugged DURING the install so the pin resolves, then stays in the box forever. Docs: preseed §3b/§8 comments (roles + rationale), deployment-manual §1a.2 nas two-stick exception (wrong-medium-boot warning), hardware-nas.md Boot Drive bullet; stick synced byte-identical to the repo copy. · [deployment-manual.md](deployment-manual.md) §1a.2, [hardware-nas.md](docs/hardware-nas.md), IaC/host/nas/preseed.cfg |
| HD-207 | 2026-08-23 | **Pool-Creation Runbook EXECUTED on nas/gen8 — bulk RAIDZ2 + tank mirror created, legacy payload landed in `bulk/migrate`, pools exported installer-ready** — ✅ **Done (execution half; redistribution still ⏳).** SMART pre-check all-pass (per-serial hours now attributed: WDC 45,903 / Toshibas 6,885·8,560·8,497 / ST4000NT001 399 / HGST 60,452; `reports/smart-report-nas-20260823T105831.txt`) — closes the deferred HDD-hour re-read early. Reality deltas vs runbook: TWO source pools existed — legacy payload in single-disk `new-pool` on ST4000NT001 (~1.96 T: `uvoz-zpool/storage/data` 1.49 T + `uvoz-data/NAS` 460 G + shares) plus second single-disk pool `backup` (61.9 G gen8 Proxmox dumps); *(decision)* owner declared `backup` DISPOSABLE — destroyed after migration verification, not migrated. Sequence: recursive `@migrate` snapshot (26 snaps, est. 2.35 T stream) → wipefs stale GPT labels ×4 → bulk raidz2 ashift=12 → `send -R \| receive -s` into `bulk/migrate/new-pool` (explicit parent `zfs create -p bulk/migrate` required — receive doesn't create intermediates; ~2.33 T logical landed, ≈1.67× physical parity+padding, peak ~450 MiB/s, transfer started by owner at iLO console) → tree-shape diff identical + full new-share subtree diff clean + zero kernel I/O faults (only benign Gen8 DMAR PTE spam — hardware-nas.md note added) → owner-gated close: export new-pool, destroy backup, wipefs sde+sdh, tank mirror ONLINE, export BOTH → installer-ready. Same change: hardware-nas.md (runbook EXECUTED banner + reality deltas, per-serial SMART tables, Gen8 boot-port quirk line — SSD port can't boot so GRUB stays on USB per HD-206), deployment-tasks.md ledger tick, deployment-journal §Phase 1a as-built entry, todo HD-207 tails trimmed to redistribution only; nas preseed refreshed with oldsrv lessons (early_command by-id resolver + iwlwifi blacklist) and the SanDisk stick re-keyed/verified NAS-ready (keyed post_install.sh reused after label/placeholder verification). · [hardware-nas.md](docs/hardware-nas.md), [deployment-journal.md](deployment-journal.md), [deployment-tasks.md](deployment-tasks.md), [todo.md](todo.md) |
| HD-223 | 2026-08-23 | **nftables forward chain drops container↔container traffic (fleet-wide isolation)** — ✅ **Done (IaC + applied live).** vps-hardening forward chain allowed `iifname "docker*"` but docker custom bridges are `br-<hash>`; with br_netfilter loaded + bridge-nf-call-iptables=1 ALL bridged peer traffic traverses host FORWARD → policy drop (7533 drops counted). Triggered into effect by a nftables restart during recovery; survived reboot + network rebuild (on-disk conf = source of truth). Fix: `br-*` prefix accepts. Verified: authentik-server healthy, peer paths restored fleet-wide. Residual: `flush ruleset` hazard on nftables restart (wipes docker NAT until docker re-programs) — scoped-flush follow-up. |
| HD-221 | 2026-08-23 | **pairdrop route moved to drop.kogler.si** — ✅ **Done (IaC + live).** group_vars subdomain, CF CNAME `drop` created via dns.yml (verified changed=1), forward-auth blueprint URLs, traefik Host label, inventory re-render. Server-side activation needed a manual Authentik ORM nudge (ProxyProvider.external_host via ak-shell) because the renamed blueprint did not auto-re-apply — blueprint auto-apply gap logged as follow-up. Stale `pairdrop` CNAME remains in Cloudflare (role has no absent-state) — owner deletes manually. |
| HD-224 | 2026-08-23 | **chat (element-web) never served — tmpfs ownership bug** — ✅ **Done (IaC + applied live).** Image runs as uid 101(nginx); docker mounts short-form tmpfs root:root 0755 → envsubst could not write /etc/nginx/conf.d (“not writable”) → no server block → nothing on :80 → unhealthy + edge 404 since first deploy. Fix: volumes `type: tmpfs` with `mode: 01777` (long `tmpfs:` block rejected by compose 5.5.0). Verified: chat healthy, edge 200. |
| HD-128 | 2026-08-23 | **Phase 1a oldsrv disk-facts captured live; NVMe by-id verified; collector + media-secrets tooling added** — ✅ **Done.** Ran the new read-only `scripts/collect-disk-facts.sh` (POSIX sh, d-i-console-safe) from a Debian 13.6 live USB on oldsrv: auto-wrote `disk-facts-oldsrv-20260822-191419.txt` to the stick root (archived in `reports/`, alongside `DOMENPC-cpuz.txt` moved there from docs/assets/references/). Result: both Windows-derived EUI64 by-ids **verified on Linux** — `nvme-eui.0025385b0143f12e` → nvme1n1 = 970 EVO 1 TB S/N `S5H9NS1NB12680T` (data pool), `nvme-eui.0025385c61b048c2` → nvme0n1 = 960 EVO 500 GB S/N `S3EUNX0HC06971Z` (OS/preseed disk); blkid shows NTFS signatures on all four NVMe partitions (expected pre-wipe input for the HD-207 runbook step 0); SMART hours NOT captured (smartmontools absent in the live env — nas HDD hour re-read still pending at Phase-2 deploy check per hardware-nas.md). Same change: `storage_nvme_data_by_id` comment updated to verified status + fail-loud-guard wording fixed; HD-128 todo row DELETED per lifecycle (fully-done → changelog-only record); hardware-oldsrv.md by-id note gained verification + serials; oldsrv/preseed.cfg VERIFY comment flipped to VERIFIED. Tooling added in the same session: `scripts/collect-disk-facts.sh` (by-id reverse map, SMART key attrs + full dump when smartmontools present, blkid/lsblk context, writable-stick-root output with loud fallback; scripts/README.md row) and `IaC/host/gen-media-post-install.sh` (media-build injector of the three real 1Password pubkeys into git-ignored `post_install_with_secrets.sh`, HD-209 guards — homelab sibling of vps/gen-custom-script.sh; wired into deployment-preseed.md media-layout rule + Files tree, .gitignore, check_placeholders.py ALLOWLIST generator class). Session close-out: official host-install path codified in **deployment-manual.md §Phase 1a** (interactive install + catch-up bootstrap = canonical; full preseed automation DEFERRED with open items: file= mount-timing on FAT32 media / initrd-injection unproven — two-sticks incident); oldsrv ledger checkbox ticked (hold rule until 1.5); deployment-journal Phase 1a section created; hardware-oldsrv.md current-state header → installed 2026-08-23, BIOS/CSM, live NIC map; storage.md gained the 970-EVO-NTFS-signatures caveat for the future pool-create; reports/ relocation of disk-facts + DOMENPC-cpuz.txt with reference sweep. · [hardware-oldsrv.md](docs/hardware-oldsrv.md), [deployment-preseed.md](docs/deployment-preseed.md), [deployment-manual.md](deployment-manual.md), [storage.md](docs/storage.md), [scripts/README.md](scripts/README.md), [todo.md](todo.md) |
| HD-217 | 2026-08-22 | *(decision)* **Homepage HA-failover buttons gated behind `homepage_failover_button` until HD-17** — **decided (owner sign-off, Phase 1 resume):** the monitoring role's homepage services template hard-referenced `failover_api_url` (undefined anywhere) + a `onepassword` lookup of `ha-failover_api` (Phase-4/HD-17 scope, not in the vault) -> hard halt of vps.yml at the monitoring role. The two HA Forward Takeover / Reverse Failback cards now render ONLY when group_var `homepage_failover_button: true` (group_vars/vps.yml, default **false**); flip to true when HD-17 goes live (`ha-failover_api` seeded + HmIP-RFUSB stick moved at runbook time). Implemented same change (template `{% if %}` gate + pinned var); ⏳ deploy-gated until the next green vps.yml run. · [smart-home-failover.md](docs/smart-home-failover.md), [todo.md](todo.md) |
| HD-212 | 2026-08-23 | *(decision)* **9P stale-cache handling for the Phase 1 completion runs = forced-sync gate on `/mnt/d` (Option A), native-WSL-clone fallback pre-authorized** — **decided (owner Q/A session):** stay on the `/mnt/d` runner, guarded by a mandatory whole-tree hash compare (`git ls-files -z \| xargs -0 md5sum \| md5sum`, Windows↔WSL) before EVERY playbook run; on mismatch force-invalidate (mechanism ① `drop_caches`, mechanism ② drvfs remount); if both fail to clear a mismatch, migrate the runner clone natively into WSL (Option B / HD-212 proper) autonomously without re-asking. Protocol documented in [deployment-manual.md](deployment-manual.md) How-to-use + [scripts/README.md](scripts/README.md) ansible-run.sh row; next-session execution parameters (full `vps.yml` re-run first → `--start-at-task` fast iterations; autonomous provisioning of missing generated vault items via `provision-vault.sh`; HD-159 stays deploy-gated until Phase 1.5) recorded in [prompt.md](prompt.md) §3. Same change: the two-vault model (`Homelab` = human/break-glass, `Homelab-ansible` = automation) written into [deployment-secrets.md](docs/deployment-secrets.md) — closes the deployment-journal's `netcup-vps_login` placement open question (Table B annotation 2026-08-22 had already recorded the consolidation decision). |
| HD-215 | 2026-08-23 | *(decision)* **Phase 1a parallel track — NAS pool bootstrap + host installs run BEFORE Phase 1.5** — **decided:** the destructive human-gated long-pole disk work (Pool-Creation Runbook, HD-207) and the nas/oldsrv OS reinstalls carry zero network dependency (preseeds are DHCP-based; today's flat LAN IS the future VLAN-10 Home subnet, so planned Home IPs survive the cutover) and start in parallel with Phase 1 completion. Hard line: both hosts stay OUT of the Ansible loop until the 1.5 cutover (SSOT carries not-yet-existing Mgmt-VLAN 99 addresses). Recorded as new **Phase 1a** section in deployment-tasks.md (between Phases 1 and 1.5, with ledger checkboxes + hold rule); Phase 2 Depends-on line gains Phase 1a; todo HD-207 tail re-pointed at Phase 1a prep; hardware-oldsrv.md gained a Current-state note (Windows 10, rarely used — doc text is target state). · [deployment-tasks.md](deployment-tasks.md) Phase 1a, [hardware-oldsrv.md](docs/hardware-oldsrv.md), [hardware-nas.md](docs/hardware-nas.md) |
| HD-178 | 2026-08-21 | *(decision)* **Wildcard `*.kogler.si` cert issuer = the VPS Traefik** — **decided:** exactly one ACME (DNS-01) issuer, and it is the **VPS** edge (always-on, public, independent of home power/WAN). oldsrv's internal Traefik and the Pi `traefik-ha` edge become cert **consumers** (synced pair). Supersedes the oldsrv-issuer wording in `group_vars/all.yml` comment, `smart-home-failover.md`, `IaC/README.md`, `deployment-tasks.md` Phase 3. Implementation = **HD-181** (issuer flag + doc alignment). Convention encoded in CONVENTIONS §1 (Cert issuer). · [services-traefik.md](docs/services-traefik.md), [services-vps.md](docs/services-vps.md) |
| HD-179 | 2026-08-21 | *(decision)* **Kids VLAN firewall = implement at Phase 1.5** — **decided:** the documented Kids controls (bedtime 22:00–07:00 WAN block, forced filtered DNS, Kids→Home drop) are **in scope for the network redo**, not deferred — closing the audit gap where `network-vlans.md` documents them as matrix rows while `roles/router` carries only a `debug:` placeholder. Implementation = **HD-182** (before Phase 1.5 verification). · [network-vlans.md](docs/network-vlans.md) |
| HD-180 | 2026-08-21 | *(decision)* **Homepage moves to the VPS** — **decided:** the family launchpad (`kogler.si` root + `home`) runs on the **VPS** next to the public edge (was: oldsrv, which left the public root without a VPS-side route — audit J10/W9). Consequence to solve in implementation: the docker.sock health-check only sees VPS containers — oldsrv reachability moves to Homepage widgets/probes (or blackbox). Implementation = **HD-183**. · [services.md](docs/services.md), [interfaces.md](docs/interfaces.md) |
| HD-204 | 2026-08-21 | *(decision)* **Implementation-option decisions for audit tasks (batch)** — **decided:** HD-185 = option A (render-first reorder on the Pi; supersedes KOPS-063/HD-117 order for this host) · HD-186 = option A (remove the authentik `3389` publish; DOCKER-USER chain documented as future hardening in security.md §8) · HD-194 = enforce `crowdsec-only@file` on `sso` (documented exception only if outpost callbacks break) · HD-198 = incremental public-record publication (record per service going live) · HD-202 = roll `cap_drop`/`read_only` into templates (GPU/VPN/gluetun exempt; validator backstop) · HD-189 = delete the dead `NETWORK_MAP` · HD-191 = containerized Kopia agent on oldsrv · HD-192 = `tuwunel`/`litellm` stay MUST-pin exceptions (not pinned this pass) · HD-181 = oldsrv pulls its cert pair from the VPS via its own timer. Decisions also carried in the todo rows + prompt handoffs; they ride into each task's closing changelog entry. |
|----|------|------|
| HD-165 | 2026-08-20 | *(decision)* **Per-gear RouterOS credentials** — **decided: keep the single shared `mikrotik-admin_login` across router + switch + APs** as an **accepted** risk, do **not** split into per-gear 1Password items. Rationale (verified from IaC): every RouterOS management surface binds to the **Management VLAN (99) only** — router `api`/`www-ssl`/`ssh` (8728/443/22) are `interface=vlan{mgmt}-mgmt` in `rb4011_initial.rsc.j2` with `api-ssl`/`www` disabled; switch + APs are L2-only with **no WAN egress** at all; the steady-state Mgmt-VLAN INPUT chain drops management ports off everything outside VLAN 99/`trusted-admin`. So the shared credential **never crosses an internet boundary**. Revisit per-gear items only if a gear gains WAN-exposed management or the Mgmt-VLAN ACL changes. Justification documented in `deployment-secrets.md`, `network-ops.md`, `security.md` §6. · [deployment-secrets.md](docs/deployment-secrets.md), [network-ops.md](docs/network-ops.md), [security.md](docs/security.md) |
| HD-158 | 2026-08-23 | *(resolved without change)* **`network-addresses.md` = IP-SSOT only until `switch_port_map` verified** — closed: the switch port map is **already human-verified** (commit `d1685f9`, 2026-08-20: “Physical port positions on the CRS328 is confirmed (hand edited)”), and the generated doc never rendered `switch_port_map` — `network-addresses-generated.md` is pure IP-SSOT (template/renderer have no `switch_port_map` reference, `git log -S` confirms). Removed the **stale WIP comment block** in `group_vars/switch.yml` (lines: “work-in-progress / estimates / verify by human”) + the stale “verify by human / TODO” debug task in `roles/switch/tasks/main.yml` (port-map is confirmed; unlisted ports default to Mgmt VLAN 99). The physical-port view lives in the separate `network-rack-generated.md` pipeline (hand-verified `rack-connections.json`, not `switch_port_map`) — no gate flag needed. Live re-verify of real ports stays deploy-gated under **HD-03** (network cutover). · [switch.yml](IaC/ansible/group_vars/switch.yml), [network-addresses-generated.md](docs/network-addresses-generated.md) |
| HD-93B | 2026-08-18 | *(purchase)* **VPS host = netcup RS 2000 G12 (supersedes Contabo)** — bought **263,52 €/12 mo** (21,96 €/mo, 2026-08-18), account/access pending. Replaces the Contabo Storage VPS 30 named in HD-93; both host identity (`group_vars/vps.yml`) and billing (`group_vars/subscriptions.yml`) updated, prose references fixed. Root server (already virtualized) → no nested Proxmox. Specs TBD at provisioning. Todo HD-40A (provision + public Traefik edge) is now un-gated on the buy, waiting on netcup access. · [services-vps.md](docs/services-vps.md), [subscription.md](docs/subscription.md) |
| HD-139 | 2026-08-18 | *(decision)* **VPS/oldsrv service residency + cross-host tunnel topology** — **decided (HD-135 IaC applied, `b070f7d`/`77833f1`/`d3cc0aa`):** (1) **MinIO retired** — Immich originals = live Hetzner Box (CIFS), not S3/MinIO → MinIO removed from `home_servers.yml`. (2) **Public/edge/GitOps/observability tier consolidated on the VPS**: Matrix+Element (public/federated), Headscale (public coordination), Metabase (CrowdSec dashboard), PairDrop, Stirling PDF, Renovate (co-located with Forgejo) all move to the VPS; the observability **backend** (Prometheus/Loki/Grafana) also moves to the reliable netcup tier — VPS `monitoring` role + oldsrv Alloy forwarding. (3) **Cross-host tunnel = VPS↔router `wg-s2s`** (home RB4011 = L3 gateway for all VLANs), **not** a separate VPS↔oldsrv tunnel — one link reaches every home target + cross-host app backends over `wg-vps-services`; old-srv needs no WG of its own. oldsrv keeps the GPU/LAN/storage-bound core (ollama, immich-ml, jellyfin/*arr, sunshine, DNS, ha-standby, homepage, dozzle, signal-cli). Deploy-gated: router-side WG peer + live-verify. · [services-vps.md](docs/services-vps.md), [network-vpn.md](docs/network-vpn.md), [observability.md](docs/observability.md) |
| HD-150 | 2026-08-19 | *(decision)* **Drop Doco-CD — single Ansible-only deployment/upgrade path** — decided: **Doco-CD is removed entirely** (it was a 2nd deployment path that could not safely cover public VPS services — `docker.sock:rw` = root-equivalent on a public host, security.md). The single path for BOTH VPS and oldsrv is **Ansible** (idempotent `docker_compose_v2` re-run applies image-tag upgrades); **Renovate** opens Forgejo PRs; the **Forgejo Actions deploy button** (`workflow_dispatch`) runs Ansible; **Homepage** is dashboard/launcher only (links to the Dependency Dashboard + deploy button). Purges: `.doco-cd.yml`, `doco-cd/` compose template, `doco_cd_metrics_port`, Prometheus `doco-cd` scrape job, `doco-cd_password` secret, and Doco-CD prose across deployment/renovate/interfaces/services/observability/security/hardware docs. `op_api` (1Password SA token) is KEPT — Forgejo Actions still uses it. Supersedes HD-02 (Activate Doco-CD) → closed as dropped. · [deployment.md](docs/deployment.md), [deployment-renovate.md](docs/deployment-renovate.md), [interfaces.md](docs/interfaces.md), [security.md](docs/security.md) |
| HD-140 | 2026-08-18 | *(decision)* **1Password Service Account token lives in a Private vault, not `Homelab`** — **decided:** the ansible runner authenticates its `community.general.onepassword` lookups via the item **`Service Account Auth Token: ansible`** (`ansible`) stored in a **Private vault** (kept out of `Homelab` to limit blast radius). The `op_api` Service Account token item that `deployment-tasks.md`/`deployment-secrets.md` assume in `Homelab` is **intentionally NOT created**. The token is installed on the WSL runner at `~/.config/op/homelab-sa-token` (0600, sourced from `~/.bashrc`). Runner auth documented in [docs/1password.md](docs/1password.md). |
| HD-39 | 2026-08-18 | *(decision)* **No watchtower for Pi HA container; keep Renovate + `stable`** — **decided:** do **not** add watchtower. HA updates stay **Renovate + `stable` tag** (controlled/gated: Renovate → PR → review → deploy), not unattended auto-update. Rationale: HA is the failover-managed smart-home controller; watchtower would bypass review, break **primary/standby version parity** (the two nodes should run the same version for clean manual failover), contradict the repo's deliberate Renovate gating, and add a resident container on the Pi. Revisit only if HA ever runs single-node (no standby). · [smart-home-failover.md](docs/smart-home-failover.md), [deployment-renovate.md](docs/deployment-renovate.md) |
| HD-29 | 2026-08-18 | *(decision)* **Off-site storage = two Hetzner Storage Boxes; iDrive dropped** — **decided:** two 1 TB Storage Boxes — **live** (nearest DC, Immich S3 + family SMB/WebDAV drives) + **backup** (far DC Helsinki/Falkenstein, Kopia repo). iDrive e2 **dropped** (Hetzner cheaper/TB + SMB/WebDAV; single-provider risk accepted). **Sizing (1 TB × 2):** live fits 300 GB Immich library + headroom; backup holds dedup/encrypted Kopia fine; boxes are live-upgradable in-place → safe start. **Backup-space add-on:** live box **minimal** (copy-protection = ZFS + far-DC Kopia, geo-separation is the point); backup box **carries** the backup-space (protects the Kopia repo). The **buy** (HD-31) stays open. · [backup.md](docs/backup.md), [subscription.md](docs/subscription.md) |
| HD-25 | 2026-08-18 | *(decision)* **Wake word = "Assistant"** — **decided:** use **"Assistant"** (English, single word) on all wake-word voice devices (ESP32-S3 kitchen + phone satellites). **Changeable later** — wake words are per-device configurable in ESPHome / HA Assist, so the family can re-decide after trying it with no re-architecture. · [smart-home.md](docs/smart-home.md), [smart-home-voice.md](docs/smart-home-voice.md) |
| HD-52 | 2026-08-18 | *(decision)* **OpenCloud sync client = manual AppImage; OIDC via Authentik** — **decided:** official `opencloud-eu/desktop` installed **manually** per client (AppImage → chmod → `/opt` or `~` + `.desktop`); Ansible preps only the runtime dep **`libfuse2t64`** (FUSE), **Debian 13 (trixie) only** (Deb 12 removed). Auth = **native OpenCloud OIDC → Authentik** with a **multi-redirect provider** (web + desktop + mobile callbacks), plus **`csp.yaml`** adding `sso.kogler.si` to `connect-src`/`frame-src`. **Prefer native OIDC over Traefik Forward-Auth** on the `file` route so the desktop client's OAuth/WebDAV isn't blocked by a browser-redirect layer. ⏳ *Deploy-gated:* uncomment OIDC block (commented in compose) → create Authentik provider → add CSP → drop `authentik-forward-auth` label. · [llm-office.md](docs/llm-office.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-24 | 2026-08-18 | *(decision)* **Smart-home UI = native HA Dashboard; TileBoard retired** — **decided:** TileBoard is **obsolete/unmaintained** → **retire it** and consolidate smart-home control on the **native Home Assistant Dashboard** (maintained, PWA-installable, declarative YAML that fits the `home_assistant` role). **No purchase** — surface on **existing devices only**: iPad (A16) + Android RT8 (both 80%-battery-capped) as wall/dash panels + family phones via PWA/bookmark. HA Dashboard runs identically on iOS/Android (browser PWA); `card-mod` still available for CSS. Loose nothing vs TileBoard (lights/blinds/RGBW/media all covered); removes an obsolete dependency (hardening/minimalism-aligned). · [interfaces.md](docs/interfaces.md) |
| HD-22 | 2026-08-18 | *(decision)* **Weather in HA = core `meteoblue` only (Maribor)** — **decided:** drop the third-party HACS "Weather 2000" **and** core `met` in favor of a **single** authoritative source, HA core **`meteoblue`** (models Slovenia well; hourly + 7-day forecast). Key = `meteoblue_api` (1Password Homelab, `api`/`credential`); coords = `home_latitude`/`home_longitude` += Maribor (46.5596/15.6355) added to `all.yml`. Configured declaratively in `configuration.yaml.j2` (IaC). Removes a third-party surface + a duplicate weather provider (hardening-aligned). · [home-assistant-current.md](docs/home-assistant-current.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-122 | 2026-08-18 | *(decision)* **Matrix federation default = open + hardening (KOPS-033 closed)** — open federation **kept** (affirms the recorded acceptance; a federated homeserver interoperates with the wider Matrix world). Mitigated WITHOUT breaking federation via `require_auth_for_profile_requests=true` (stops anonymous MXID/profile scraping) + `allow_public_room_directory_over_federation=false` (blocks `/publicRooms` enumeration) in `tuwunel.toml.j2`. Key correction: `trusted_servers` is a **key-notary** list, not an ingress permit-list — Conduwuit/Tuwunel has no per-server inbound federation allow-list, so the original "explicit permit-list" framing was not technically viable; unsolicited DMs are handled client-side (per-user ignore/block in Element). · [services-matrix.md](docs/services-matrix.md), [security.md](docs/security.md) |
| HD-55 | 2026-08-18 | *(decision)* **Alloy per-host `instance` label** — **superseded/implemented by HD-116 (KOPS-036)**: the per-host `instance` label (`{{ inventory_hostname }}`) was added to the Alloy host-metrics scrape, so series no longer collide across hosts. Row closed — the decision it tracked is already done. · [observability.md](docs/observability.md) |
| HD-131 | 2026-08-18 | *(decision)* **Storage & family-access architecture (S3-proof; D1–D6)** — decided (read-only design, locked 2026-08-18): **D1** S3 backend from day one **only for Immich** (MinIO-on-NAS now → Hetzner Storage Box S3 later); OpenCloud + drives stay filesystem. **D2** per-app interface: Immich→S3, OpenCloud→WebDAV/filesystem, Win11/Linux drives→SMB/CIFS. **D3** minimal photo retention — only backup needed (Kopia S3-originals→box + the Immich metadata DB; no deep version tier). **D4** per-user isolation is app-level (Authentik↔OpenCloud); family drives via **Samba per-user shares**; login manual username+password, **no 1Password SSO** (1Password only stores the passwords). **D5** build Authentik→NAS/OpenCloud provisioning **glue** (new member → NAS user+uid+dataset+Samba share+OpenCloud account). **D6** encode D1–D6 into owning docs when those files are next edited. Note: ZFS snapshots/instant-rollback are traded for Kopia+box-retention; ZFS is retained for OpenCloud shared files but Immich originals go S3. Frame + re-scope of HD-94. · [deployment-compose.md](docs/deployment-compose.md), [storage.md](docs/storage.md), [backup.md](docs/backup.md) |
| HD-132 | 2026-08-18 | *(decision)* **Authentik as LDAP provider for Samba self-service password (D7)** — decided (read-only, 2026-08-18): Samba authenticates against **Authentik as an LDAP provider** (`passdb backend = ldapsam`, LDAPS/TLS), so each family member changes their own password in the **Authentik self-service portal** (no admin). Supersedes the D5 local-`smbpasswd` approach for password ownership (D5 glue still creates dataset+share+OpenCloud). Passkey-only users set one-time workspace/LDAP password for the drive via the portal. **Status: decision recorded; implementation left for a later session.** · [deployment-compose.md](docs/deployment-compose.md), [storage.md](docs/storage.md) |
| HD-53 | 2026-08-18 | *(decision)* **MikroTik SNMP community = dedicated RO + Mgmt-VLAN ACL (Option A)** — dedicated read-only community (`network-snmp_login`, in 1Password) replaces the snmp_exporter default `public`; SNMP (161/udp) restricted to the Management VLAN via router INPUT-chain ACL. Device-side `/snmp` enable + RO community = HD-03 deploy step (RouterOS commands in `snmp.yml.j2` header). KOPS-034 closed. · [observability.md](docs/observability.md) |
| HD-54 | 2026-08-18 | *(decision)* **SMTP relay = SMTP2Go (Option B)** — dedicated transactional relay `mail.smtp2go.com:587` (STARTTLS), free-tier 1000/mo, chosen **independently** of the Infomaniak kSuite decision (HD-30) so the alert fail-safe isn't coupled to personal email. Shared by Grafana (`grafana-smtp_login`) + NUT (`nut-smtp_login`). · [observability.md](docs/observability.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-05 | 2026-08-08 | **VIP address / notation / firewall IP-set** — decided: `10.10.1.200/32` (`ha-vip`), DHCP pool ≤ `.199`, router lists `trusted-ha` + `trusted-admin`. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-106 | 2026-08-17 | *(decision)* **Office MCP bridges = native Windows per-client apps (no Docker)** — Office COM requires a licensed, installed Office **natively on the Windows host**; Docker/Linux can't reach host COM automation. The bridge (Node MCP/SSE wrapper + pinned PyWin32 COM worker) runs natively per client. Version-pinned (Flaw-B supply-chain discipline). Distributed from a repo-owned `client/office-bridge/` folder served read-only over the Headscale tunnel (independent of Ansible/GitOps, which manages server Docker, not Windows endpoints). · [llm-office.md](docs/llm-office.md) |
| HD-107 | 2026-08-17 | *(decision)* **Linux clients = server-side Office tools only** — `python-docx`/`python-pptx`/`openpyxl` on the server act on files in OpenCloud (file SSOT); results appear in the synced OpenCloud folder on any host (Linux opens them in ONLYOFFICE). No live COM on Linux — file-level sync latency, **not** in-app live editing. · [llm-office.md](docs/llm-office.md) |
| HD-108 | 2026-08-17 | *(decision)* **OpenCloud = file SSOT · Open WebUI = chat/UX SSOT** — Office files live/round-trip in OpenCloud; chat + RAG + all tools live in Open WebUI (+PGVector). **AnythingLLM and LocPilot retired** once the Office MCP path lands. · [llm-office.md](docs/llm-office.md), [ai-stack.md](docs/ai-stack.md) |
| HD-109 | 2026-08-17 | *(decision)* **Office MCP bridges exposed over Headscale-only, token-auth** — bind to the Headscale interface only (no public, no host `0.0.0.0`), token-auth, matching the repo exposure convention. · [network-vpn.md](docs/network-vpn.md) |
| HD-110 | 2026-08-17 | **Research: unified vs per-app Office MCP bridge + client topology** — ✅ **Complete (2026-08):** one unified bridge (PPT+Excel+Word) behind a single Headscale endpoint + token is feasible, but **needs a thin STDIO→HTTP wrapper** we own — Open WebUI's MCP client is `streamablehttp_client` (remote HTTP), while every native COM server (`ppt-mcp`, `word-mcp-live`, `office-mcp`) is stdio-local. **Recommendation:** unified bridge v1 on the shared family desktop; use mature per-app backends (`ppt-mcp` 1.7.0 + `word-mcp-live` + an added Excel) or adopt immature unified `office-mcp` (0★) for PoC. Excel-live server gap is an open risk. Gates HD-111. · [hd110-office-mcp-research.md](docs/hd110-office-mcp-research.md) |
| HD-51 | 2026-08-16 | *(decision)* **Family users & shared-data ownership (multi-axis identity model)** — ✅ **Decided (2025-08-16):** keep three **separate** ownership axes — ① **persons** = one Authentik identity each (4 family + `guest`); ② **shared bytes** = a neutral `media`/`media` **system** account (dedicated uid/gid, e.g. `1005`, stored once as `storage_uid`/`storage_gid` — **not** `domen`, **not** 1000) owning the shared NAS datasets + local shared files; ③ **processes** = least-privilege containers using `storage_uid`/`storage_gid` (→ HD-94 / KOPS-060). oldsrv: each human = own Linux login (4 + guest), auto-login = primary family account (**not** domen), family users **not** in the `docker` group. **nas: no interactive human logins** — only `ansible-admin` + `ai-debug` (ops) + the neutral system owner. **OpenCloud: Authentik OIDC per-person accounts** (native SSO, not Forward-Auth) so each user gets real per-user file isolation; storage on a NAS dataset owned by the neutral uid. · [services-authentik.md](docs/services-authentik.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-56 | 2026-08-16 | *(decision)* **Host network config-manager: systemd-networkd vs netplan** — ✅ **Decided: systemd-networkd.** Rationale: native Debian server tooling, no Python dependency (netplan pulls python3+libnetplan), Ansible `community.general.systemd_network` module is mature, consistent with existing systemd timer usage (sanoid/syncoid/db-backup). Netplan rejected — Ubuntu desktop default, extra translation layer on Debian server. Blocker cleared for network role's static-IP/VLAN-trunk provisioning. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-92 | 2026-08-16 | *(decision)* **oldsrv stays bare-metal Debian — no local Proxmox / no GPU passthrough on the single Phase-1 box** — ✅ **Decided (2025-08-16):** keep Phase 1 as bare-metal Debian + Docker on oldsrv. Rationale: one shared dGPU (RX 7600) is needed by **both** the family desktop and the AI stack (Ollama/immich-ML) — VM/GPU-passthrough is mutually exclusive and would force choosing one; a single host gains no HA/migration from VMs; hypervisor + ~40 containers is RAM-heavy and fights the "works without Domen" priority. **Proxmox deferred to HD-41/HD-42** (Phase 2) once a real 2nd local node exists. If desktop isolation ever becomes a hard requirement → dedicated 2nd local desktop box, **not** a VM on oldsrv. · [hardware-oldsrv.md](docs/hardware-oldsrv.md), [IaC/README.md](IaC/README.md) |
| HD-93 | 2026-08-16 | *(decision)* **Buy Contabo VPS before go-live; public edge on VPS from day one** — ✅ **Decided (2025-08-16):** purchase the VPS pre-go-live and fold **HD-40A/HD-40B forward into Phase 1** — public Traefik + CrowdSec + Authentik and public apps (Forgejo, Grafana, OpenCloud web) terminate TLS on the VPS (WG S2S → oldsrv backends); oldsrv becomes an internal/LAN box (leaner, less exposed → mitigates the audit's SPOF critique). Does **not** change HD-92 — the VPS is remote/GPU-less, hosts no desktop or AI, and is not a substitute for a 2nd local node. · [services-vps.md](docs/services-vps.md) |

## Implementation & tooling

| ID | Date | Item |
|----|------|------|
| HD-253 | 2026-08-26 | **Session-discipline mechanical enforcement: worktree guard + validate hard-gate + intent-routed start sequence + handoff diff-rule** — ✅ **Done (local tooling/docs; nothing deploy-gated — todo row closed per housekeeping pattern (a)).** Owner decisions locked 2026-08-26: both gates HARD-FAIL (no warn-only), no handoff-shrinkage lint, semantic prior-art routing over todo/changelog/docs incl. `-review`/`-rejected`/`brainstorming/` (NO keyword routing table), branch-per-session explicit checkbox. Implemented: ① `scripts/guard-session.sh` (NEW) — primary ⇔ `git-dir == git-common-dir`; pre-edit mode refuses ANY edit-context while primary sits on `main` (editing there IS the violation, clean or dirty); `--validate-mode` hard-fails only primary+main+DIRTY; clean-main merge-station exempt; worktrees + detached HEAD/CI pass through; refusals print exact live-timestamped remediation (`git worktree add ../homelab-wt-YYYYMMDD-HHMM`) + coordination info (status --short, per-file last-commit subjects); ② wired into `validate-all.sh` near the top + sandboxed `--self-test` fixture over temp repos (fire-on-primary-main, merge-station exemption, dirty-worktree pass, detached pass-through, remediation-text contract) surfacing through the standard gate; ③ README §0 rewritten to the intent-routed start sequence (universal "read README.md" + one free-form intent sentence; unconditional step-0 ritual; depth markers cut analysis depth never safety depth; ambiguous intent → ONE clarifying question); §0–§2 context-mandatory language intact; ④ CONVENTIONS amendments: §6 mechanical-enforcement sentence + "primary = merge station only" definition; §4 Session close-out row gains diff-rule bullet, derived-values ban, branch-per-session checkbox (surgical cell edits only); ⑤ prompt.md updated per its own new diff-rule — traffic-light lane-claim header introduced, #16 open-item detail restored (Zipline compose-header runbook legs ⓪–⑤ incl. vault seeding + guestbin/dropzone gate walk; converge ride-along check ③ open-webui OIDC printenv), stale typed "next free = HD-252" pointer replaced with derive-at-write-time rule; ⑥ source draft process-improvements-draft.md folded and DELETED together with prompt-workflow.md in the closing change (audit-report lifecycle §4). Evidence: `validate-all.sh` green twice (feature commit c13dd44 + close-out); guard dogfooded live — task executed in worktree homelab-wt-20260826-1018 on branch session/hd253-workflow-enforcement, merged back ff-only. · [CONVENTIONS.md](CONVENTIONS.md) §4/§6, [README.md](README.md) §0, [scripts/guard-session.sh](scripts/guard-session.sh), [scripts/validate-all.sh](scripts/validate-all.sh), [scripts/README.md](scripts/README.md), [prompt.md](prompt.md) |
| HD-101 | 2026-08-25 | **Open WebUI OIDC dead-on-arrival root cause FIXED: template shipped invented env names open-webui never reads** — ✅ **IaC done; deploy-gated.** Owner report (local login works while logged out of SSO; NO "Continue with Authentik" button ever rendered on `ai.kogler.si`) verified against open-webui v0.11.0 source (`backend/open_webui/config.py`; pin `openwebui_version 0.11.0`): the generic OIDC provider registers ONLY when `OPENID_PROVIDER_URL` + `OAUTH_CLIENT_ID/SECRET` are set — every name the compose template shipped (`ENABLE_OIDC`, `OIDC_ISSUER`, `OIDC_CLIENT_ID/SECRET`, `OIDC_REDIRECT_URI`) is silently ignored, so SSO never existed on the container since first deploy. Template reworked to the REAL vars (`OPENID_PROVIDER_URL = https://sso.kogler.si/application/o/openwebui/.well-known/openid-configuration`, client id/secret from vault item `openwebui_api`, `OPENID_REDIRECT_URI` matching the ks-oidc.yml strict URI, `OAUTH_PROVIDER_NAME=Authentik`) + posture hardened in the same pass: `ENABLE_SIGNUP=false` CLOSES public local sign-up (upstream default open = first-visitor-becomes-admin race on a crowdsec-only public host; owner claimed local admin first, creds in 1P), `ENABLE_OAUTH_SIGNUP=true` (+ upstream-default `DEFAULT_USER_ROLE=pending`) keeps family self-service per the Authentik-identity model, `OAUTH_MERGE_ACCOUNTS_BY_EMAIL=true` links the existing local admin to its SSO identity instead of duplicating. Authentik side verified correct (provider + strict redirect live since HD-230/231). deployment-ai-stack-secrets.md var names aligned; todo HD-101 tail trimmed to pending items. ⏳ **Deploy-gate:** converge open-webui (rides the in-flight playbook run) → SSO button renders → Authentik round-trip links the admin account by email → LiteLLM completion + RAG verify closes HD-101. · [IaC/ansible/templates/docker_services/open-webui/docker-compose.yml.j2](IaC/ansible/templates/docker_services/open-webui/docker-compose.yml.j2), [docs/services-ai.md](docs/services-ai.md), [docs/deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md), [todo.md](todo.md) |
| HD-246 | 2026-08-25 | *(conventions)* **Secret lifecycle knowledge promoted into CONVENTIONS §2 + owning-spec workflow section** — ✅ **Done (docs/rules).** Four new binding CONVENTIONS §2 rows: **Secret creation path** (exactly one of catalog-generated / manual-value / glue-seeded / externally-coupled), **Seed before converge** (items exist BEFORE the playbook renders their consumer — fail-loud lookups mid-run; check-vault-items.sh = pre-deploy gate), **Rotation propagation** (vault rotation reaches live state only via re-render/converge or an explicit sync task; else NOT_AUTO_ROTATABLE with reason), **Item-reference coverage** (new group_vars key classes land together with scanner support — HD-244 lesson). Mechanics section "Creation & Rotation Workflow" added to deployment-secrets.md (4-path decision table, order-of-operations, rotation contract, coverage contract) — sourced from live sessions 2026-08-24/25 (metabase-forgejo_ro seeding, forgejo drift-guard precedent, scanner blind-spot find). Trigger: owner asked whether secret-creation knowledge was adequately documented; audit showed the lifecycle/decision-tree existed only as changelog precedents + tribal memory. · [CONVENTIONS.md](CONVENTIONS.md) §2, [docs/deployment-secrets.md](docs/deployment-secrets.md) |
| HD-241 | 2026-08-25 | **Metabase SMTP via compose env (smtp2go) + post-setup housekeeping record** — ✅ **IaC done; deploy-gated.** `metabase` compose template gained `MB_EMAIL_SMTP_HOST/PORT/SECURITY(starttls)/USERNAME/PASSWORD` + `MB_FROM_ADDRESS notify@kogler.si`: connection = shared smtp2go SSOT vars (`smtp2go_host/port`, HD-54), creds = shared `smtp_login` item via 1Password lookups in `>-` scalars (CONVENTIONS §2). Env-set settings override and LOCK the Admin-UI fields → IaC is the single SSOT for mail config (owner decision after initially assuming manual UI config). Same change: first-boot wizard walk-through recorded as a runbook in services-admin.md Notes (admin `admin@kogler.si` = Cloudflare alias, vault item `metabase_login` created manually outside the catalog — accepted), interfaces.md stale "Authentik OIDC" access wording fixed to Forward-Auth per HD-148. Validator SSOT-loader extended (whitelists plain `smtp2go_host`/`smtp2go_port` from all/main.yml — grafana had masked their absence with a `default()`). ⏳ Deploy-gate: surgical converge → Send-test-email verify. · [docs/services-admin.md](docs/services-admin.md), [docs/interfaces.md](docs/interfaces.md) |
| HD-242 | 2026-08-25 | **Metabase data sources: CrowdSec SQLite mount + read-only Forgejo Postgres role (`db_ro_sync` mechanism)** — ✅ **IaC done (①②); deploy-gated.** ① CrowdSec SQLite: `/srv/docker/crowdsec/db` host bind mounted into the metabase container RW by design (SQLite WAL/journal recovery must write on first open after unclean crowdsec shutdown; Metabase only SELECTs — official CrowdSec dashboards pattern); stale crowdsec-template header claim that Metabase queries the LAPI corrected to the DB-mount reality. ② Forgejo Postgres: metabase joins `db-internal`; NEW generic opt-in `db_ro_sync/db_ro_item/db_pg_container` in deploy-service.yml (sibling of the HD-220 rotation-drift guard): idempotent CREATE-if-absent of a READ-ONLY login role, then password + schema-public SELECT grants incl. ALTER DEFAULT PRIVILEGES re-applied EVERY converge from the SAME vault item → rotations propagate automatically (no_log, ON_ERROR_STOP, IaC-only per no-hand-SQL rule); wired on the vps.yml metabase entry → forgejo-db with new catalog item `metabase-forgejo_ro` (username `metabase_ro`, rotation-safe by design, NOT in NOT_AUTO_ROTATABLE). Contract documented in deployment-ansible.md. ③ Zipline DB sub-gated on HD-112 first deploy. ⏳ Deploy-gate: owner seeds `metabase-forgejo_ro` BEFORE converge (fail-loud render otherwise) → converge → connect sources → import official CrowdSec dashboard JSONs → write-attempt-must-fail check. HD-243 registered (LDAP auth parked — second-form/no-MFA/HD-186-binding caveats; trigger = second regular user). · [deployment-ansible.md](docs/deployment-ansible.md), [services-admin.md](docs/services-admin.md), [todo.md](todo.md) |
| HD-239 | 2026-08-24 | **Docs liveness sweep: README state-of-the-world refreshed + 8 stale "not yet live" banners corrected to the Phase-1 reality** — ✅ **Done (docs-only).** Evidence base = [deployment-journal.md](deployment-journal.md) Phase 1 (2026-08-22/23 waves): VPS tier live, 32/35 containers Up incl. the observability backend. **README.md §2** rewritten: stale "nothing is deployed live yet" (as of 2026-08-20) → Phase-1-live statement + a three-step live-vs-authored check order (① journal = liveness SSOT → ② `todo.md` §3c / `deployment-tasks.md` ⏳ checklists → ③ doc banners are hints, not proof). **Banner fixes** mirroring services-vps.md's "**Live since 2026-08-22**" wording: observability.md (backend live; Alloy collectors/alerting/SNMP stay ⏳), services-traefik.md (VPS edge live), services-authentik.md (live; OIDC chain proven via HD-230b one-shot), services-matrix.md (containers up; federation HD-47 gated), services-admin.md (Forgejo healed per HD-220a; kopia-agent gated), services-utilities.md (n8n/pairdrop/stirling-pdf live; signal-cli gated), services-office.md (OpenCloud+ONLYOFFICE owner-verified via HD-166 pts.1–6; desktop/MCP parts future), services-ai.md ("Planning — nothing deployed" → VPS platform live; frontmatter `status: planning` → `active`). Home-host-scoped docs verified accurate and untouched (media/downloads/dns/smart-home*/hardware-ups; finance correct post-HD-57). ✅ validate-all green. · [README.md](README.md), [docs/observability.md](docs/observability.md), [docs/services-traefik.md](docs/services-traefik.md), [docs/services-authentik.md](docs/services-authentik.md), [docs/services-matrix.md](docs/services-matrix.md), [docs/services-admin.md](docs/services-admin.md), [docs/services-utilities.md](docs/services-utilities.md), [docs/services-office.md](docs/services-office.md), [docs/services-ai.md](docs/services-ai.md) || HD-57 | 2026-08-24 | **Finance stack (Actual Budget): IaC landed + 4 pre-deploy decisions recorded** — ✅ **IaC done; deploy-gated.** New `docker_services/actual-budget/` template (`actualbudget/actual-server`, stable pin `26.8.1` registry-verified via Docker Hub API 2026-08-24) + `home_servers.yml` registry entry + `versions.yml` pin. **Decisions (2026-08-24, rationale in services-finance.md):** ① placement **oldsrv**, internal-only P+I (financial data on LAN; n8n/LiteLLM stay VPS-side and reach Actual over WG S2S); ② bank sync = **stable image + n8n Enable-Banking bridge** — native EB sync requires the nightly line, rejected under §7 pin law (re-evaluate only if upstream ships it stable); ③ credit card = monthly manual CSV first (email-alert automation investigated post-deploy, SMS last resort); ④ **seed-before-sync**: manual starting balances + IBKR/TR positions entered before enabling sources one-by-one with dedup checks. Doc corrections in the same change: stale `http://ollama:11434` AI path fixed to the LiteLLM spine (llm-backend isolation, HD-59 — nothing targets Ollama directly), categorization re-scoped as an n8n LLM-node loop (no copilot container), backup table moved to kopia-agent scope (`/srv/docker/actual-budget/data`), API leg :5006 published on `oldsrv_home_ip` (immich-ml precedent, HD-184). HD-57 stays open at Stage 8/10 with a ⏳ tail (tokens/EB app, n8n workflows, AllowedIPs scope, first deploy + live verify at Phase 3); Phase-3 ledger wired into deployment-tasks.md. · [docs/services-finance.md](docs/services-finance.md), [deployment-tasks.md](deployment-tasks.md) |
| HD-236 R1 | 2026-08-24 | **HD-236 deploy-gated residual ② CLOSED live; residual ① + HD-230b per-run firing folded into next converge** — ✅ **residual ② Done (read-only live probe).** Effective runtime env of the running `opencloud` container reads `COLLABORATION_APP_INSECURE=false` (= IaC; boolean-only probe output) — confirms the HD-236(a) false-alarm resolution: on-disk yaml `insecure: true` was never authoritative. **HD-230b one-shot:** mechanism proven end-to-end server-side (`headscale` provider carries the HD-233 `/admin/oidc/callback` redirect → `ks-oidc.yml` content applied; `custom BlueprintInstances: 0` is the KNOWN discovery gap, not a failure); direct per-run log evidence inconclusive → decisive check rides along with the next docker_services converge together with **residual ①** (zero spurious restarts on unchanged extras / exactly one per changed extra). · [deployment-journal.md](deployment-journal.md), [docs/services-authentik.md](docs/services-authentik.md), [docs/deployment-ansible.md](docs/deployment-ansible.md) |
| HD-237 | 2026-08-24 | **Tagged-handler hygiene sweep: EVERY role handler carries `tags: always` — filtered runs can no longer silently skip notifications** — ✅ **Done (IaC-only; no deploy needed — handler definitions live controller-side and apply at the next notify).** Closes the latent gap FLAGGED in HD-236c: handlers are tag-filtered exactly like tasks, so under a surgical run (e.g. `--tags "docker_services,<service>"` on `vps.yml`) an UNTAGGED notified handler was skipped even though its notifying task ran — `docker_services`'s `reload systemd` was the live-risk instance (unit-file change → notification dropped → stale systemd units). Sweep across all 9 roles: 21 previously-untagged handlers now carry `tags: always` with a one-per-file rationale comment (`docker_services`, `docker`, `vps-hardening` ×4, `wireguard`, `amd_rocm` ×2, `home_assistant` ×2, `nut` ×3, `storage` ×6); `monitoring` was already correct (HD-220). Verified programmatically: 22/22 handlers tagged, YAML-parsed green. Owning doc updated: the deployment-ansible.md gotcha bullet now states the fixed invariant + rule for new handlers. · [docs/deployment-ansible.md](docs/deployment-ansible.md), [deployment-journal.md](deployment-journal.md) |
| HD-236 | 2026-08-24 | **B-queue small engineering batch: `collaboration.app.insecure` false alarm RESOLVED + docker_services restart-on-config-change guard + `--tags` union semantics documented** — ✅ **Done (offline; orchestrated as parallel isolated worktree workers).** **(a)** HD-166-pt.3's secondary open (`collaboration.app.insecure: true` despite env `false`) is a FALSE ALARM: upstream source at tag v7.4.0 (`services/collaboration/pkg/config/app.go`) confirms `COLLABORATION_APP_INSECURE` IS mapped to `collaboration.app.insecure`; parse order is file → defaults → env LAST (`parser/parse.go`) and nothing couples `OC_INSECURE` into `App.Insecure` (`defaults/defaultconfig.go`) → an on-disk yaml snapshot showing `true` was never the effective runtime value (stale scaffolded bind-mount content / pre-env-line container / HD-212-class stale read). Value stays `"false"` (= verify the valid LE cert on the WOPI leg toward office.kogler.si); compose TODO rewritten as verified explanation + services-office.md verified bullet. **(b)** Rendered extra configs are BIND-MOUNTED and `docker compose up -d` recreates on SPEC changes only → csp.yaml/tuwunel.toml/litellm/prometheus-web-config content changes sat unapplied until an unrelated restart; `deploy-service.yml` now registers the extra-render result and fires ONE guarded per-service `state: restarted` post-up ONLY when a non-`.env` extra actually changed (idempotent converges stay green, first-boot safe; `.env.j2` excluded — interpolation already recreates; traefik/traefik-ha excluded — file-provider hot reload; prometheus restart accepted deliberately); mechanism documented in deployment-compose.md. **(c)** Surgical-run `--tags` union semantics documented in deployment-manual.md + deployment-ansible.md: selection is a UNION (narrower tags never narrow broader ones), role tags do NOT cascade through dynamic `include_tasks` → per-service filtering needs BOTH tags (`--tags "docker_services,<service>"`; service-tag alone = SILENT NO-OP), handlers are tag-filtered too (`reload systemd` currently untagged — latent gap FLAGGED, not fixed), untagged playbooks only ever run `always` tasks under filters; canonical invocation tables added; stale pre-HD-220 manual parenthetical fixed. ⏳ Deploy-gated residuals: first converge must show zero spurious restarts on unchanged extras; one-time live probe of the EFFECTIVE runtime `COLLABORATION_APP_INSECURE`. · [deployment-journal.md](deployment-journal.md), [docs/services-office.md](docs/services-office.md), [docs/deployment-compose.md](docs/deployment-compose.md), [deployment-manual.md](deployment-manual.md), [docs/deployment-ansible.md](docs/deployment-ansible.md) |
| HD-220 | 2026-08-23 | **Phase-1 post-convergence incident batch: rotation-drift guard + grafana↔prometheus auth gap + renovate internal endpoint** — 🔨 **Done (IaC, session branch; ⏳ deploy-gated until the surgical converge lands green).** Overnight convergence (ok=264) re-rendered compose with the HD-211-rotated vault values and exposed three live gaps: **(a)** postgres images apply `POSTGRES_PASSWORD` only at first cluster init → rotated `forgejo_db` left the persisted cluster on the OLD role password; forgejo crash-looped (1049 restarts by 09:04Z, SASL 28P01 — hash-proven read-only: both container envs carry the NEW value, cluster role has OLD); db-backup DB02/DB04 dumps would fail identically once its own startup breakage is fixed. Fix: opt-in `db_role_sync/db_item/db_pg_container` service metadata (authentik, immich-app, forgejo, pgvector) + `deploy-service.yml` post-up idempotent `ALTER ROLE` via `docker exec psql` from the SAME vault item (no_log; IaC-only per the no-hand-SQL rule). **(b)** prometheus enforces basic auth (HD-59 web-config) but the provisioned grafana Prometheus datasource shipped with NO credentials — live-proven 401 from inside the grafana container; dashboards/alerts dead since first deploy. Datasource now renders `basicAuthUser` + `secureJsonData.basicAuthPassword` from `prometheus-internal_api`. **(c)** renovate targeted `https://git.kogler.si`, which sits behind authentik-forward-auth (SSO redirect ≠ token check) while forgejo was down anyway → `RENOVATE_ENDPOINT` switched to internal `http://forgejo:3000` (shared services-internal network; pattern noted for the Phase-5 Actions runner). Same change: role-level tags in `playbooks/vps.yml` enabling surgical runs (deployment-manual How-to-use gap), services-vps.md vsock known-noise note (**decision A** — accepted benign console noise, silencing declined), stale "not yet live" services-vps banner fixed. Diagnostics evidence (read-only, parallel child run): crowdsec clean w/ 4 valid bouncers; sshd 3/no/no incl. ansible-admin context; fail2ban sshd jail active (751 failed/73 lifetime bans); docker daemon hardened; `/` 12% used; wildcard LE cert valid to 2026-11-20; CIFS mounted rw but round-trip as ansible-admin denied by design (`forceuid/gid=1005`) — sudo-level round-trip pending at converge verify; authentik-ldap = 403 outpost-token loop (vault item vs server token drift — separate ak-shell flow, NOT in this batch); kopia seed still pending (config dir empty). · [deployment-journal.md](deployment-journal.md), [deployment-ansible.md](docs/deployment-ansible.md), [services-vps.md](docs/services-vps.md), [todo.md](todo.md) |
| HD-219 | 2026-08-22 | **Edge forward-auth wiring — `ks-forward-auth.yml` blueprint + headscale tier correction** — ✅ **Done (IaC + applied live).** Fleet-wide route 404s root-caused to the authentik outpost having zero applications mapped (only OIDC providers existed; the authentik side of `authentik-forward-auth@file` was never authored while the chain was dead). New blueprint: 9 proxy providers (forward_single — never forward_domain, which would swallow sibling subdomains) + edge applications + embedded-outpost binding for git/stats/sec/auto/pairdrop/pdf/kogler.si/home.kogler.si/traefik-dash. Headscale middleware corrected to `crowdsec-only` (native OIDC + unauthenticated client control traffic; owner-approved anomaly fix). Verified live: all 9 routes 302 → sso authorize with correct per-app client ids; vpn + ai tiers unchanged. · [network-vpn.md](docs/network-vpn.md), [services-traefik.md](docs/services-traefik.md), [deployment-journal.md](deployment-journal.md) |
| HD-218 | 2026-08-22 | **Phase-1 crash-loop triage — Waves 1+2: 18/18 containers diagnosed (parallel read-only workers), fixes batched in one orchestrator pass** — ✅ **Done (IaC).** Wave pattern per owner approval: one read-only diagnostic worker per crashing service (docker ps/inspect/logs via script-file WSL ssh only), merged by the orchestrator, then a single serial editing pass. Three shared classes found and fixed once, not N times: **Class A** root-owned bind dirs vs non-root image uids → `bind_owner_uid`/`bind_dirs` service metadata + deploy-service.yml pre-create/chown task; **Class B** HD-202 blanket hardening vs image init systems (s6 `/run` tmpfs ×2, forgejo/metabase/stirling relaxations with validator allowlist entry + comments, element-web nginx tmpfs); **Class C** config drift one-liners (tuwunel `[global]`, immich `DB_HOSTNAME`, headscale `serve`+schema, renovate `gitea`, prometheus bare targets). Plus: crowdsec collection fix, opencloud rw+csp mount, kopia entrypoint fix (.env rework verified good), certs-dumper wrapper script + wildcard request labels, check-vault-items scan-scope fix. Owner action landed same day: Cloudflare token IP filter widened to the VPS egress (was 403-ing all ACME attempts). ⏳ Deploy-gated on Wave-3 combined run; HD-211 rotation additions recorded in deployment-journal. · [deployment-journal.md](deployment-journal.md), [todo.md](todo.md) |
| HD-214 | 2026-08-23 | **Collapsible-sections convention + first precedent** — ✅ **Done.** Evaluated `<details>`/`<summary>` folding for the deploy runbook and ruled: collapsible blocks stay **out of agent-facing docs** (primary readers are AI agents on raw text — folds hurt grep-ability, add noise, fight the linear execute-in-order flow) and are allowed **only** in human/family-facing browser-rendered prose as optional asides. Precedent added: Slovenian "Kaj, če nekaj ne deluje?" troubleshooting fold in readme-humans.md §Za družino. New CONVENTIONS §6 rule encodes both directions (+ the blank-lines-around-inner-Markdown requirement without which renderers output raw fences). ✅ validate-all green. · [CONVENTIONS.md](CONVENTIONS.md) §6, [readme-humans.md](readme-humans.md) |
| HD-213 | 2026-08-23 | **New `deployment-manual.md` — redeploy runbook (Phase 0 runner bootstrap + Phase 0.5 VPS provisioning)** — ✅ **Done.** Owner decision: the first deploy legitimately filled tasks/journal with fix-narrative, so a clean imperative runbook was missing for redeploys. New root doc = phase-by-phase **procedure SSOT** (exact commands, panel settings, per-step ✔ evidence; no progress markers, no history — complements ledger + journal, which CONVENTIONS §4 now names together). Content: Phase 0 true-zero runner recipe consolidated from the 2026-08-22 journal entries (WSL Debian reinstall → `bootstrap.sh` w/ `op_api.credential` → `restore-runner-key.sh` canonical key → `test-1password.yml` → Windows ssh aliases + agent allowlist) + 9P staleness / partial-rerun (`--start-at-task`) guidance; Phase 0.5 VPS re-provisioning (`gen-custom-script.sh` flow, **netcup SCP field-by-field settings MOVED here from deployment-preseed.md §netcup** — single home for the executable procedure; preseed.md keeps authoring rules + warnings + pointer; table gained the previously untabulated create-additional-user=false row), first-boot TOFU/sshd/sudoers/authorized_keys verification, break-glass note (`netcup-vps_login` console path); Phase 1 section deliberately stubbed until the first green Verify block (fill-in tracked on the prompt.md handoff). Wiring: docs/index.md dispatch row, deployment.md hub Document Map row, deployment-tasks ×2 pointers, prompt.md hygiene reminder. ✅ validate-all green. · [deployment-manual.md](deployment-manual.md), [docs/deployment-preseed.md](docs/deployment-preseed.md), [scripts/README.md](scripts/README.md) |
| HD-210 | 2026-08-22 | **`group_vars/all.yml` silently shadowed by the `group_vars/all/` directory** — ✅ **Done.** Found live at the Phase 1 dry-run (first real playbook run ever): `ansible_admin_users` / `op_vault` / `timezone` undefined on ANY host while `all/versions.yml` pins resolved fine — with BOTH `group_vars/all.yml` and `group_vars/all/` present, Ansible loads only the directory; the 247-line file had been dead since HD-156 created `all/` (latent: syntax-checks and doc renders never needed those vars). Fix: `git mv group_vars/all.yml → group_vars/all/main.yml`; code consumers re-pointed (`render_all.py`, `render_network_addresses.py`, `validate-docker-services.py`); path mentions swept (deployment-ansible tree+§heading, deployment-secrets ×2, scripts/README ×2, IaC/router/README, deployment-tasks ×2). `test-1password.yml` green without any override. · [deployment-ansible.md](docs/deployment-ansible.md), deployment-journal.md Phase 1 |
| HD-209 | 2026-08-22 | **Fix post_install pubkey injection (doubled algorithm prefix) + remediate live VPS** — ✅ **Done.** Found live at the Phase 1 re-provision first-boot verify: template echo lines hardcoded `ssh-ed25519 ` while the injected public-key placeholders already carry full keys ⇒ every `authorized_keys` line malformed (`ssh-ed25519 ssh-ed25519 AAAA…`, owner-console-verified) ⇒ both identities refused ⇒ reinstall looked dead though user/drop-in/sudoers were all correctly in place. Fixed BOTH `post_install.sh` variants (vps + shared, incl. the shared ai-debug options-line); `gen-custom-script.sh` gained a fail-loud guard (doubled-prefix + exact-line checks; regression-tested green). Owner remediated the live box via netcup SCP console (authorized_keys overwritten with correct lines); verified: `sshd -T` = no/no/3 from the drop-in alone, sudoers NOPASSWD, SSH via canonical `ansible-admin_ssh` identity OK (transient op-read key, shredded after — Win32 OpenSSH 9.5p2 pub-hint loader broken on the laptop, quirk recorded in deployment-journal). Same change: vault-record alignment per owner decision — all `netcup-*_login` items live in the **Homelab (human)** vault (Table B break-glass-vault wording corrected in deployment-tasks ×3 + deployment-secrets §exception). Minimal-image-hook suspicion retracted (script ran fully under it). · [deployment-preseed.md](docs/deployment-preseed.md), deployment-journal.md Phase 1.0 |
| HD-20 | 2026-08-21 | **Confirm full Supervisor add-on list (live HAOS harvest)** — ✅ **Done.** `ha addons list` (admin CLI, Advanced SSH & Web Terminal): exactly **3 add-ons** — File editor 6.1.0 (`core_configurator`) · Studio Code Server 6.0.1 (`a0d7b954_vscode`) · Advanced SSH & Web Terminal 24.0.1 — all official dev/ops tooling, **no community add-on stores**; no MQTT/Z2M/media add-ons. Same pass: HAOS 18.2 rpi4-64, 128 GB SD, NIC `end0`, `lsusb` hub-only (no user USB devices). home-assistant-current.md §7.2 + §9 updated; storage.md SD-size rows corrected 32–64 GB → 128 GB. · [home-assistant-current.md](docs/home-assistant-current.md) |
| HD-15 | 2026-08-21 | **Confirm HACS custom-component versions/repos (live HAOS harvest)** — ✅ **Done.** `/config/custom_components/` read via SSH: contains **exactly one directory, `hacs`** — the components the 2026-08-07 REST pass attributed to HACS/custom (OneDrive, go2rtc, card-mod, motion, ai_task) are NOT present in `custom_components/` (since-removed or misattributed: frontend resources load from `www/`, `ai_task` plausibly a 2026 core integration). Consequence for the HD-04 redo: **nothing to port except HACS itself** (+ any `www/` frontend resources); re-adding OneDrive/go2rtc etc. = fresh-deploy decision. home-assistant-current.md §7.1 table corrected + §9 checkbox closed. · [home-assistant-current.md](docs/home-assistant-current.md) |
| HD-208 | 2026-08-21 | **Fix post_install sshd hardening shadow (VPS image)** — ✅ **Done (IaC).** Found live during the Phase 1 console capture: netcup's image ships explicit `PasswordAuthentication yes` + `PermitRootLogin yes` ABOVE the Homelab block appended by the Custom Script — sshd first-obtained-value-wins made the appended `no/no` dead (`sshd -T`: yes/yes/6 ⇒ password-login window until Ansible). Both `IaC/host/vps/post_install.sh` AND the shared `IaC/host/post_install.sh` now write a `/etc/ssh/sshd_config.d/00-homelab-hardening.conf` **drop-in** (stock Debian puts `Include /etc/ssh/sshd_config.d/*.conf` at the TOP of sshd_config → drop-in directives always win first-match) with idempotent file-existence guard; VPS variant keeps `AllowUsers ansible-admin` only, shared keeps `+ ai-debug`; `bash -n` verified. `vps-hardening` role lineinfile enforcement unchanged (compatible — main-file directives become dead text either way; drop-in decides). Discovered via the new deployment-journal flow. · [deployment-preseed.md](docs/deployment-preseed.md), [security.md](docs/security.md) §8, deployment-journal.md Phase 1.0 |
| HD-206 | 2026-08-21 | **NAS disk by-id SSOT + preseed serials + pool-creation runbook** — ✅ **Done.** (a) `docs/hardware-nas.md`: all three drive tables re-keyed from unstable boot-specific `sdX` letters to the stable `/dev/disk/by-id/` SSOT (captured 2026-08-21 on the pre-reinstall install; letters had already reshuffled once — HGST now sdh, IronWolf sde, MX300 sdg); per-disk hours for the three Toshibas marked unattributable (~6.5–8.2 k range) — old readings were recorded against sdX names. (b) `IaC/host/nas/preseed.cfg` both placeholders filled with real by-ids: `partman-auto/disk` → `ata-Crucial_CT525MX300SSD4_173818D02FF0`, `grub-installer/bootdev` → `usb-Generic_Flash_Disk_C3EB7FE7-0:0`. (c) New **Pool-Creation Runbook** in hardware-nas.md: one-time pre-reinstall bootstrap (storage role is import-only for tank/bulk) — bulk RAIDZ2 first (4× 3 TB, ashift=12, `-O` props per storage.md), legacy single-disk pool migrated off the IronWolf via rename-on-import + `zfs send -R` → `bulk/migrate` landing zone, then tank mirror (2× 4 TB; raidz1 explicitly ruled out for 2 disks), export both before the installer boots; datasets beyond the landing zone stay role-owned. deployment-tasks.md Phase 2 step 4 re-pointed at the runbook. ⏳ Runbook execution + migrated-data final-home decision = open **HD-207**. · [hardware-nas.md](docs/hardware-nas.md), [deployment-tasks.md](deployment-tasks.md), IaC/host/nas/preseed.cfg |
| HD-203 | 2026-08-21 | **Fold round-2 audit reports into canonical docs, then delete them (A3 lifecycle)** — ✅ **Done.** Gate met: all HD-181…202 closed. Every finding in the 8 root reports classified against the merged remediation waves; still-durable content folded into owning docs; still-unfixed verification items fixed in their owning docs in this same change; rejections recorded with reasons. Per-report outcome — **`docs-vs-iac.md` (47 findings): 31 implemented-via-HD** (A3–A7→HD-195/196/183 · B1→HD-196+198 · B2→HD-196 · C1→HD-178/181 · D1–D3→HD-192/189 · E1/E3→HD-189/140 · G1–G4→HD-196 · H2→HD-200 · I1/I2/I4→CONVENTIONS §1 + HD-195 · J1–J7,J9,J10,J12→HD-184/185/181/187/191/182/188/183/196); **14 fixed-in-this-change** (A1/A2 stale group_vars samples → SSOT pointers; A8 stale obs-backend comments in `playbooks/storage.yml` + `group_vars/all.yml`; E2 `router_login`; E4/J11 `privado-vpn_api` + `opencloud_login` master-list rows added and hand-entered "44 items" count dropped; F1 role lists/counts incl. deployment.md "12 roles"; F2 RouterOS scripts row; F3 file-layout tree deduped + missing playbooks/roles/group_vars entries; F4 guard whitelist; F5 leftover duplicate lines incl. deployment-compose Grafana bullet ×2; H3 raw-`.rsc` import wording; I3 leftover §2.6 blank line un-hiding HD-14…27; J13 storage.md nvme snapshot/service-list claims corrected to the storage-role reality); **2 rejected/tracked** (H1 CAPsMAN status — already ⏳ via the HD-03 row + Phase 1.5 verify checklist, no doc change needed; J8/S20 `trusted-admin`-vs-`trusted-ha` Home→IoT gating — live-firewall ACL decision, tracked on HD-03 cutover instead of silently changed). **`docs-changes.md` (25 items): 19 implemented-via-HD** (§1 sweeps→HD-195/196 · §2 public-record mirror / cert-issuer / compose OIDC split / office+ai trim→HD-196/178/199 · §3 structure checks already true · §4 index/security/network-dns/deploy-gate→HD-196/192/199 · §5 legacy compression→HD-196; `-review` files kept by design), **3 applied-here** (§1 IaC/README defects — see above; §4 `render_all.py` Windows path added to interfaces.md pipeline; §3 stale "10 files" derived count dropped from the todo HD-32 row), **3 rejected** (backup-placement-table dedup — backup.md answers "what backs up X", storage.md owns "where X lives", content aligned by HD-196; media/*arr conventions stay in deployment-compose — compose-layer rules, not catalog content; home-assistant-current reverify — process reminder, doc already links the open HD-15/20/21 confirm rows). **`iac-changes.md` (29 items): 22 implemented-via-HD** (§2 VLAN map + §8 AllowedIPs/home_ip→HD-200 · §3 pins→HD-192 · §5 vps guard + §6 bootstrap bindings→HD-193 open row · §7 preseeds→HD-201 · §9 syntax-check gate→HD-197 · §11 D1–D12→HD-184/185/181/187/183/188/182/189/202/200/196 · §8 requirements.yml verified no-change-needed), **1 folded-here** (§1 concrete README defects fixed; full pointer-shrink not needed beyond that), **5 rejected** (shared template fragments — per-service readability is a feature per architecture §4, drift now validator-gated · site.yml phase-mapping comment block + render-docs.yml host-pattern comment — comment-only ergonomics · docker_services prepass naming — single special case today, fine per report · ansible.cfg `host_key_checking` — TOFU accepted for bootstrap, = S8). Monitoring-role split (§8) folded as an observability Deferred note (W6). **`conventions-sugestions.md`: 8 implemented** (A1→CONVENTIONS §2 pointers · A3→§4 audit-report lifecycle · A4→§6 readme-humans Slovenian · A5→§7 pin-no-default · B1→§1 cert issuer + HD-178/181 · B2→§2 + HD-196/198 · B5→HD-201 · B6→§6 ISO dates + HD-195 sweep), **1 applied-here** (B4 bootstrap-window binding floor → network-ops.md Service Binding section), **2 rejected** (A2 standing ≤5-line superseded-block rule — one-off compression applied by HD-196; a standing line-count rule invites cosmetic churn, the §4 mention-sweep rule covers the substance · B3 mandatory cross-host catalog-row format — deps already declared where they belong: deployment-compose sibling-auth map VPS→oldsrv WG rows, services-ai depends-on lines, observability placement SPOF note; a required format adds upkeep with no failure mode; C-candidates rejected in-report, D diffs superseded by actual adoptions). **`tracking-sugestions.md`: 4 implemented** (§2 phase sweep→HD-195 · §3.3 lifecycle codified in CONVENTIONS §4 deploy-gated row · §3.4 next-free-ID pointer in CONVENTIONS §1 · §6 consistency rules = A1/A3/B6), **3 fixed-or-applied-here** (§3.1 todo.md §2.6 blank line · §3.2 stale §4 example list dropped per derived-pointer rule · §4 HD-53 naming footnote under the Decisions header), **6 rejected** (editing 2025 years inside changelog rows — append-only law wins, recorded by HD-195 · explicit sort-order statement + future year-split — meta, no defect · generating readme-humans service list — hand-maintained 4 rows accepted per report itself · archiving prompt.md — outside this task's 8-report scope, orchestrator call · 1Password-prerequisite dedup across deployment-tasks phases — phases deliberately self-contained, accuracy restored by HD-195). §1 verified-good file assessments stand as recorded here. **`architecture.md`: 10 implemented-via-HD** (W2/W11→HD-178/181 · W3→HD-200 · W4→HD-195/196/199+CONVENTIONS §2 · W5→HD-192 · W7→HD-198 · W8→HD-197 · W9→HD-180/183 · W10→HD-193+201 · W12→HD-191 · W1 coupling accepted and documented via the observability placement SPOF note), **2 folded-here** (W6 monitoring-monolith split option → observability.md Deferred · S14 home-side tunnel check → observability.md Deferred), **2 rejected** (#10 ML-path decoupling built preemptively — report's own bar: only if upload-latency pain materializes · #12 desktop-client config management — not needed at current scope). Digest/verdict/not-a-problem list affirmed here, not as a doc. **root `security.md` (S1–S22): 18 implemented-via-HD** (S1→HD-186 incl. verify plan → services-vps firewall checklist row 7 · S2→HD-192 · S3/S4/S9→HD-193 open row + HD-165 acceptance · S5→HD-62 + HD-199 marker pass · S6→HD-72 · S7→HD-178/181 · S10→HD-201 · S11 caveat documented · S12→HD-189 · S15/S16→HD-190 · S17→HD-194/188 · S18→HD-191 · S19→HD-182 · S21/S22→HD-202), **2 folded-here** (S13 Kopia-repo immutability residual-risk sentence → backup.md · S14 home-side tunnel check → observability.md Deferred), **2 rejected** (S8 known_hosts pinning — TOFU accepted until hosts stable, fingerprints recorded in deployment-ansible.md · S20 = J8 above). §3 contradictions: three fixed by HD-192/186/195, the secrets-master-list gap fixed here (E4/J11). Posture digest + both verified-good rounds stand as recorded by the closing HD rows. **`scripts.md` (F1–F11): 14 implemented-via-HD-189/197** (F1.1–F1.3/F1.5/F1.6/F1.8, F2, F3, F5, F6, F7, F9 vault-name, F10, F11 — README staleness verified gone), **1 folded as new backlog row: HD-205** (provision-secrets nits, F8), **2 rejected** (F1.4 mock_default fallback-path testing — obsolete post-HD-192: pins are fail-loud, no `default('latest')` paths remain to test · F1.7 template_dir default-name path — the HD-157 count-lint forces explicit `template_dir`, so the unreachable default stays). §5 gate additions all landed (syntax-check HD-197 · vault-name lint HD-189 · pin-existence via fail-loud renders HD-192; rendered-output drift check N/A per report). Deletions: the 8 reports deleted — root `security.md` name-collision resolved by fold+delete, NEVER renamed (HD-153 precedent); git history preserves full text; no leftover `prompt-hd*.md` existed. Dead-link sweep: report links in changelog/todo rows → backticked filenames; stale allowlist entries removed from `check_doc_ips.py` EPHEMERAL_AUDIT_REPORTS + `check_placeholders.py` ALLOWLIST. ✅ `validate-all.sh` green (9 checkers, zero dead links). · CONVENTIONS §4 (Audit reports), [HD-205](todo.md) |
| HD-199 | 2026-08-21 | **Docs structure pass (docs-changes §2/§4)** — ✅ **Done.** (a) **`deployment-compose.md` split:** the whole Authentik OIDC-provisioning block (~80 lines: Blueprint `ks-oidc.yml` + secret-egress-glue contract, deploy ordering incl. the HD-162 pre-pass mapping, and the OpenCloud/Immich/Forgejo/Metabase native-OIDC recipes) moved verbatim to new **`docs/deployment-oidc.md`** (`role: design-spec`, domain deployment, ★ authoring spec, Role/Linked-from headers per convention); a one-line pointer stub remains in deployment-compose; cross-references re-pointed — services-authentik.md (Links-to + deploy-ordering paragraph), deployment-ansible.md §docker_services, deployment-secrets.md `authentik-ldap_bind` row, deployment.md hub Document Map + Related, index.md Document Map row + "Provision Authentik OIDC clients" dispatch row now starts at the new doc, and the IaC header comment in `roles/docker_services/tasks/prepass-authentik.yml` (comment-only change). One stale claim fixed in transit: the moved Metabase note no longer cites `metabase/metabase:latest` (pinned in versions.yml per HD-192). (b) Single-public-record-mirror sub-item **deferred** — gated on open HD-198 (incremental publication); interim alignment of network-dns to the services.md mirror already landed with HD-196. (c) **services-office/services-ai boundary trim** per docs-changes §2: office keeps ONLYOFFICE/WOPI + collaboration, MS fonts, family device/client matrix, MCP topology; the local-model recommendations table moved into services-ai §4 LLM-routing (platform SSOT) with a pointer left in office; office Strategy de-duplicated to link out for routing/models; stale "AI queries go directly to local Ollama API" corrected to the LiteLLM-spine path (HD-100 consumers never call Ollama directly). (d) **Deploy-gate phrasing standardized** to the CONVENTIONS §3 two-sided wording `🟢 **IaC done, not yet live — ⏳ deploy-gated.**` across 15 docs (observability; services-admin/authentik/dns/downloads/media/matrix/traefik/utilities/office/vps — including merging finance's two duplicate status blocks into one; smart-home; smart-home-failover; hardware-ups; services-vps), each keeping its specific detail sentence. ✅ `validate-all.sh` green (doc map 73 docs incl. deployment-oidc.md, all links resolve). · `docs-changes.md` (fold+deleted, HD-203), [deployment-oidc.md](docs/deployment-oidc.md) |
| HD-196 | 2026-08-21 | **Stale-docs sweep (VPS era; G1–G4 + docs-changes §1)** — ✅ **Done.** Each listed stale verified against current content before editing; already fixed by earlier merges and skipped: date typos 2025→2026 in the five evidence docs (**HD-195** swept them), security.md §2 "currently latest" + "42 templates" (**HD-192**), index.md VPN + UPS dispatch rows (already present). Fixed: `backup.md` What-Gets-Backed-Up rows → VPS-era locations (DBs = VPS bundled Postgres per `db-backup` DB01–04; service state split VPS edge/GitOps vs oldsrv *arr; configs = host `/opt/*` VPS+oldsrv) + TSDB-exclusion note → VPS NVMe; `observability.md` Pi-SD section Loki "(14d, oldsrv NVMe)" → VPS NVMe + the two decided rows (Alloy per-host instance label HD-116, SNMP community HD-53 — both verified in `alloy.river.j2`/`snmp.yml.j2`) moved from the ⚠ TODO table into a Decided note in Microservice Notes; `services-matrix.md` placement → VPS (Goals/Container/Storage/RAM/WAN-443 sentences, authoring-spec group_vars pointer → vps.yml, "homeserver Postgres DB" backup claim corrected to the RocksDB store); `hardware-nas.md` stale "TODO (IaC)" box deleted (storage role implemented, `playbooks/storage.yml`); `storage.md` "Proposed IaC (stub)" section deleted (incl. the "3879 mounts" typo) + legacy Immich section compressed to ≤5 lines (decision + changelog link); `security.md` §9 duplicated fragment line removed; `index.md` storage-triage dispatch row added, ★ re-starred by actual role (de-starred the 4 `-generated` views + services-finance, starred storage.md, legend reworded so ★ = authoring spec ≠ generated view); `network-dns.md` ghost `bck` subdomain removed (and the origin claim fixed: services-admin Kopia row subdomain `bck` → `—`, WG-only :51515 per HD-191), both public-record lists aligned to the services.md mirror (root+home, sso, foto, file, office, ai, git, ha, vpn, matrix, chat), Pi-hole Configuration section merged into `services-dns.md` (pointer stub left here); legacy superseded blocks compressed to ≤5 lines in `deployment-compose.md` (Immich hybrid storage) + `hardware-oldsrv.md` (Proxmox VM table); `interfaces.md` Auto-Generation Pipeline step 5 reworded control-plane-manual (Ansible never commits — closes D11). ✅ `validate-all.sh` green. · `docs-changes.md` (fold+deleted, HD-203) |
| HD-181 | 2026-08-21 | **Single cert issuer: `acme_issuer` flag** — ✅ **Done (IaC).** Exactly one host runs ACME for `*.kogler.si`: the VPS (`traefik_acme_issuer: true` in group_vars/vps.yml; role default false in docker_services defaults). The traefik compose template renders both roles from the flag — issuer: CF_DNS_API_TOKEN env + 4 letsencrypt resolver flags + ./acme volume + certs-dumper sidecar; consumer: read-only `./certs:/etc/traefik/certs` mount and NO resolver. All 30 `traefik.http.routers.*.tls.certresolver` label references wrapped in `{% if traefik_acme_issuer is defined and traefik_acme_issuer %}` (Undefined-safe) across 29 compose templates, so consumer routers fall back to the DEFAULT store. New `traefik/dynamic/tls.yml.j2` (registered in `_extra_templates`) declares consumer `tls.certificates` + `stores.default.defaultCertificate` from the synced pair; renders comment-only on the issuer. File-provider routes de-latent-bugged: traefik ha route conditional certResolver vs `tls: {}`; cockpit routes dropped their dead `certResolver: letsencrypt` (no resolver exists on a consumer); traefik-ha already consumer-style — comments corrected to the new sync source. Cert-sync retarget per decided HD-204 (direct pull, no relay): Pi `ha-cert-sync.sh.j2` SRC → `hostvars['vps.kogler.si'].ansible_host:/opt/traefik/certs/`; NEW oldsrv-side puller (`roles/docker_services/templates/traefik-cert-pull.sh.j2` + systemd units, gated to non-issuer traefik hosts). Validator BASE_CTX pins `traefik_acme_issuer: true` (issuer path render-tested); BOTH paths hand-verified (consumer renders valid YAML with no resolver/acme/dumper; issuer keeps all four flags). Docs de-staled: services-traefik, services-vps, smart-home-failover, IaC/README, deployment-tasks Phase 1/3, all.yml wildcard-cert comments. ⏳ **Deploy-gated:** VPS first issuance · authorize Pi ha-sync + oldsrv traefik-cert-sync pubkeys on the VPS · verify both consumer timers deliver a valid pair. Closes audit J3/C1/W11. · [services-traefik.md](docs/services-traefik.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-194 | 2026-08-21 | **`sso` route edge middleware (S17a)** — ✅ **Done.** `crowdsec-only@file` enforced on the public Authentik router (`sso.kogler.si`) — the most brute-force-attractive route no longer sits behind zero edge protection (§1 law exception closed). Decided HD-204: enforce unless outpost/OIDC callbacks break; reasoned through the config and they do NOT break — the CrowdSec bouncer filters by SOURCE IP only (path-agnostic); every callback path (OIDC `/application/o/<slug>/callback/`, forward-auth `/outpost.goauthentik.io/*`) arrives as an ordinary browser request from a user IP that is not blocklisted; the embedded outpost talks to authentik-server container-direct on services-internal and never traverses the edge router. Reasoning documented in-template; security.md §1 middleware map + traefik middlewares.yml.j2 comment updated; fail2ban `http-auth` jail remains as the layered brute-force compensating control. ⏳ **Deploy-gated:** end-to-end login + one OIDC callback at VPS deploy. Closes audit S17(a). · [security.md](docs/security.md), [services-traefik.md](docs/services-traefik.md) |
| HD-195 | 2026-08-21 | **deployment-tasks phase sweep (I2/A3/A4/A7 + tracking §2)** — ✅ **Done.** Mechanical text sweep, every changed claim cross-checked against the playbook/group_vars file it names: (1) header decision date 2025-08-16 → 2026; (2) Phase 2 Ansible chain now includes the `storage` role — `common → ai_diag → network → storage → nut(master) → cockpit`, matching `playbooks/storage.yml` exactly; (3) Phase 3 service list replaced with the real oldsrv subset from `group_vars/home_servers.yml` (ollama, immich-ml, technitium-primary, pihole, homepage [→VPS per HD-180/183], dozzle, signal-cli-rest-api, sunshine desktop-gated, home-assistant-standby, jellyfin + seerr/sonarr/radarr/lidarr/prowlarr/bazarr/sabnzbd/qbittorrent/profilarr/recyclarr); stale "remove/downgrade per HD-135" sentence deleted (split already applied); Phase 3 verify no longer claims Grafana/Forgejo local reachability (VPS-edge services — public URLs, Phase 1); (4) Phase 4 chain fixed to KOPS-063 order (`docker_services` BEFORE `home_assistant`) and Pi service set corrected to home-assistant-primary / technitium-secondary / traefik-ha (no pihole/raspberrymatic); Homematic step marked ⏳ parked (HD-13, dropped from the Pi loop 2026-08-18); (5) Phase 6 "TileBoard" → "HA Dashboard lovelace"; (6) Phase 7 item 7 → Office MCP path per HD-108/111 (AnythingLLM/LocPilot retired); (7) Phase 9 resolved rows HD-35 (link fixed) and HD-39 (watchtower decided) deleted; (8) Host→Playbook table chains match the playbook files byte-for-byte — nas += storage, vps += vps-hardening/cifs/wireguard(cond.), pi reordered with ai_diag; (9) remaining 2025-08-16 typos swept in all five evidence-named docs (security ×4, services-ai ×10, services-vps ×1, hardware-oldsrv ×1; changelog rows left as written per append-only law). · [deployment-tasks.md](deployment-tasks.md), `tracking-sugestions.md` §2 (fold+deleted, HD-203) |
| HD-188 | 2026-08-21 | **Cockpit routes: derive IPs + add edge middleware (J9/D6/S17b)** — ✅ **Done.** `roles/cockpit/templates/cockpit-routes.yml.j2` no longer hardcodes the nas/oldsrv Home-IP literals: backends derive from the address SSOT via `network_static_hosts` name+vlan lookups (`(network_static_hosts | selectattr('name','equalto',…) | selectattr('vlan','equalto',10) | first).ip` — hand-rendered against real group_vars produces exactly the previous literals, so the change is provably behavior-neutral on addressing). Both routers now carry `crowdsec-only@file` — the §1 law (never zero edge protection) no longer has an undocumented exception here; Forward-Auth stays deliberately absent (own-login management surface). TLS serves from the consumer default store (HD-181). services-traefik.md cockpit section updated to match. · [services-traefik.md](docs/services-traefik.md) |
| HD-186 | 2026-08-21 | **Close the published-port firewall bypass (S1)** — ✅ **Done (IaC).** The authentik LDAP outpost no longer publishes `3389` on all interfaces of the public VPS: the port binds ONLY the WireGuard S2S address (`{{ wg_s2s_vps.ip }}…`, peer-pubkey-conditional, loopback fallback — the prometheus/loki precedent), so Samba (nas, the client) targets the VPS's WG-side address over the tunnel and the WAN path refuses. `storage_samba_ldap.host` retargeted from the oldsrv Home-IP literal lookup to `wg_s2s_vps.ip`; stale "outpost runs on oldsrv"/"restrict src at firewall" comments corrected (authentik template + storage defaults). Option B (**DOCKER-USER filter chain**) documented in security.md §8 as future hardening only if a public publish is ever required; the S1 verification plan moved into the services-vps §VPS-Specific Firewall checklist as verify row 7 (external nc/ldapsearch refuse · WG-side connects · nft ruleset shape); HD-132 "firewall 3389→nas" wording fixed in todo + deployment-tasks (direction: nas is the client, nothing published publicly). ⏳ **Deploy-gated:** run checklist row 7 at first VPS deploy. Closes audit S1. · [security.md](docs/security.md), [services-vps.md](docs/services-vps.md) |
| HD-190 | 2026-08-21 | **Overlay header-trust hardening (S15/S16)** — ✅ **Done (IaC).** (a) New group_var `traefik_edge_ips` (`group_vars/all.yml`, placed beside `ha_trusted_proxies`, SAME contract: fill the per-host traefik-public bridge default container IP `.2` at deploy, verify with `docker inspect`) — authentik now renders `AUTHENTIK_TRUSTED_PROXIES: {{ traefik_edge_ips | join(',') }}` instead of the whole traefik-public /16 (spoofed X-Forwarded-For from a compromised sibling can no longer poison client-IP decisions; HD-81/KOPS-039 precedent). Validator SSOT-loader (`validate-docker-services.py`) loads the new plain var — context cannot drift. (b) Grafana auth-proxy hardened on two axes: `GF_AUTH_PROXY_WHITELIST = traefik_edge_ips` (auth-proxy endpoint answers ONLY the edge container IPs → direct overlay forgery of X-authentik-* is rejected by source IP) and `AUTO_SIGN_UP: false` (no account auto-created from a header; explicit admin/API/provisioning mapping). Residual risk documented in-template + todo row: Grafana OSS `[auth.proxy]` performs NO header signature validation in any released version — true signed-header validation requires the separate `[auth.jwt]` module (`jwks_url` against the Authentik OIDC app); deferred to deploy-time verification rather than a blind mechanism switch that could lock out observability. ⏳ **Deploy-gated:** verify real edge IPs; forged-header sibling request must 401/403. Closes audit S15/S16. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-183 | 2026-08-21 | **Homepage → VPS move (J10/A6/W9; decision HD-180)** — ✅ **Done (IaC).** `homepage` moved from `group_vars/home_servers.yml` to the `vps.yml` edge tier (right after authentik — Forward-Auth consumer, not a native-OIDC client, so ordering constraints unaffected). **Route = the move itself:** the compose's existing Docker-provider labels (`Host(\`kogler.si\`) || Host(\`home.kogler.si\`)`, certresolver, forward-auth) register on the VPS traefik over traefik-public once homepage deploys there — exactly audit J10's stated mechanism; `traefik/dynamic/routes.yml.j2` deliberately untouched (file provider stays reserved for non-container backends per repo convention; a parallel file route would duplicate the routers), interpretation supervisor-approved 2026-08-21. `HOMEPAGE_ALLOWED_HOSTS` now lists apex + `home.` alias (Host-header rejection fixed); the read-only docker.sock mount is GONE → reachability/health status is widget/probe-based (siteMonitor probes + blackbox `probe_success` widgets — HD-180 consequence: on the VPS the socket would only ever see VPS containers); homepage_services.yaml.j2 header re-pointed at `vps.yml`; services.md + interfaces.md wording aligned; services-inventory-generated.md re-rendered (homepage now in the VPS table). ⏳ **Deploy-gated:** first VPS deploy renders homepage there and serves `kogler.si`/`home.kogler.si` through the public edge. Closes audit J10/A6/W9. · [services.md](docs/services.md), [interfaces.md](docs/interfaces.md) |
| HD-191 | 2026-08-21 | **oldsrv Kopia agent (S18/J5/W12)** — ✅ **Done (IaC; decided HD-204: containerized).** New `kopia-agent` service (`group_vars/home_servers.yml` + `templates/docker_services/kopia-agent/`): daily snapshot loop connecting to `kopia-server` (VPS) over the WG S2S tunnel at `{{ wg_s2s_vps.ip }}:51515` (SSOT-derived, never a literal), auth = the existing `kopia-server-internal_api` item, fail-loud (no new 1Password item). Sources read-only + NAS-independent per backup.md §Layer 2: `/opt/*` configs, `/srv/dumps` scratch, immich upload/thumb dir, signal-cli state volume (external `name:` pin — project rename breaks the mount, caught at deploy). Same change: kopia-server :51515 published ONLY on the VPS WG address with the HD-186 fail-loud guard idiom (unprovisioned router pubkey → loopback-only — supersedes prompt-hd191's "not published" note per supervisor decision); shared `kopia_version` pin annotated server+agent; validator `wg_s2s_vps` BASE_CTX mock; backup.md §Layer 2 transport paragraph aligned. Implemented directly by the orchestrator after repeated worker infra failures. ⏳ **Deploy-gated:** first snapshot from oldsrv lands in the repo; restore drill picks it up. Closes audit S18/J5/W12. · [backup.md](docs/backup.md), [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-184 | 2026-08-21 | **immich-app → immich-ml cross-host URL (J1/D1)** — ✅ **Done (IaC).** Two-part fix per handoff: (1) `IMMICH_MACHINE_LEARNING_URL` pointed at the container name `immich-ml`, but immich-app deploys on the **VPS** while immich-ml runs on **oldsrv** — Docker DNS cannot resolve across hosts ⇒ new derived vars in `group_vars/all.yml`: `immich_ml_bind = {{ oldsrv_home_ip }}` + `immich_ml_url = http://{{ immich_ml_bind }}:3003` (SSOT-derived, alloy_backend_host idiom, no literal IPs). (2) immich-ml published no ports (overlay-only) ⇒ compose now publishes `{{ immich_ml_bind }}:3003:3003` — bound to oldsrv's Home-VLAN IP, never 0.0.0.0, reach scoped by WG AllowedIPs + RouterOS forward ACL (HD-155); `IMMICH_HOST: 0.0.0.0` inside the container confirmed so the publish forwards. Validator BASE_CTX mocks added for both vars. ML API-key auth env untouched (HD-160 owns live verification); docs checked — services/storage wording already said "VPS → oldsrv (WG)", no stale `immich-ml:3003`. Handoff prompt-hd184.md deleted (A3 lifecycle). ⏳ **Deploy-gated:** live-verify ML round-trip from the VPS over WG (smart-search job completes). Closes audit J1/D1. · [services-ai.md](docs/services-ai.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-187 | 2026-08-21 | **Pi-hole conditional forwarding → Technitium primary (J4/D4)** — ✅ **Done (IaC).** `templates/docker_services/pihole/docker-compose.yml.j2`: `CONDITIONAL_FORWARDING_IP` was the HA VIP (`ha_vip`, serves HA/edge — not DNS) → now `dns_primary_ip` (Technitium primary, oldsrv Home IP from host_vars SSOT), so Pi-hole can forward `kogler.si` lookups and show real hostnames. Validator render context gained a `dns_primary_ip` mock (same instance-specific class as the existing `technitium_secondary_ip` stand-in). ✅ render check green. Closes audit J4/D4. · [network-dns.md](docs/network-dns.md) |
| HD-182 | 2026-08-21 | **Kids VLAN firewall (J6/J7/S19; decision HD-179)** — ✅ **Done (IaC).** The `debug:` placeholder in `roles/router/tasks/main.yml` replaced by the real Kids-VLAN controls: (1) **bedtime WAN block** 22:00–07:00 for VLAN 40 as a RouterOS native time-based filter rule (`time=22:00:00-07:00:00`) — NOT a `/system scheduler` toggle, since a declarative `api_modify` re-run resets a toggled rule's `disabled` flag (23:00 run ⇒ block silently lifted until 07:00) and the state wouldn't survive a reboot mid-window; (2) **forced filtered DNS**: Kids `:53` udp/tcp `dst-nat` to the Technitium primary (`dns_primary_ip`, Kids-Group/Cloudflare-Families policy per network-dns.md) so hardcoded-resolver devices are caught too, plus a DoT `853` tcp/udp bypass drop (DoH/443 residual accepted); (3) explicit **Kids → Home drop** row matching the network-vlans matrix. **DNS parity** (network-dns.md): forward accept for the Pi **secondary resolver** mirroring the primary, DHCP `dns-server` now primary → secondary → **router tertiary**, input-chain accept for LAN :53 + `ip dns allow-remote-requests=yes` (settings-path `api_modify`; needs community.routeros ≥ 2.17 — fails loud at deploy otherwise). **Structural fix bundled:** the default-deny inter-VLAN rule merged into the single ordered `chain=forward` block — the previous second block with the same restrict and `handle_absent_entries: remove` would have deleted all allowed-flow rules (incl. the new Kids rules) on every live run. ⏳ **Deploy-gated:** Phase 1.5 verification of the overnight window, hijack and drops. Closes audit J6/J7/S19 before Phase 1.5 verification. · [network-vlans.md](docs/network-vlans.md), [network-dns.md](docs/network-dns.md) |
| HD-72 | 2026-08-21 | **HA primary privileged → targeted caps (KOPS-014)** — ✅ **Done (IaC).** `home-assistant-primary` drops `privileged: true` + `network_mode: host`: HA runs bridged on `services-internal` with `cap_drop: ALL` + a plain `:8123` publish (VIP contract intact — `traefik-ha` + Prometheus still target VIP:8123; the Pi is single-homed on Home VLAN 10 so exposure is unchanged vs host networking). No `devices:` needed — no local RF hardware on the Pi (HD-13 parked, HmIP-HAP cloud). keepalived sidecar stays host-net (VRRP multicast = the one documented security.md §4 exemption) with caps cut to `NET_ADMIN`+`NET_RAW`. Trade-off accepted (same as standby): mDNS/SSDP auto-discovery no longer traverses into the container — integrations must target device IPs explicitly. Validator backstop updated (primary removed from `ALLOWED_NO_CAP_DROP`). Patch recovered from a failed parallel-worker run and committed by the orchestrator. ⏳ live-verify at Phase 4 (first Pi deploy). · [smart-home-failover.md](docs/smart-home-failover.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-185 | 2026-08-21 | **Fix Pi first-deploy ordering (J2/D2)** — ✅ **Done (IaC).** Render-first reorder per HD-204 option A (supersedes KOPS-063/HD-117 on this host): `playbooks/raspberry_pi.yml` now runs `home_assistant` BEFORE `docker_services`, so `config/configuration.yaml`, `keepalived.conf` (+ `secrets.yaml` once its renderer lands) exist as regular files before the first `docker compose up` — the old order made Docker auto-create each bind-mount source as an empty directory, the HA role's `copy → keepalived.conf` failed, and HA ran default config silently. Belt-and-braces regular-file guard in `roles/docker_services/tasks/deploy-service.yml`: stat + assert all three bind-mount sources under `/opt/home-assistant-primary/` are regular files before every `compose up` (gated to home-assistant-primary), fail-loud with the empty-directory remediation path. Supersession recorded in the playbook + role-header comments; deployment-ansible.md §Implementation Order step 7 aligned. ⏳ **Deploy-gated:** first real Pi deploy (Phase 4) renders configs before `compose up` and passes the guard; note `secrets.yaml` has no render task yet — the guard fails loud until it lands. Closes audit J2/D2. · [smart-home-failover.md](docs/smart-home-failover.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-200 | 2026-08-21 | **SSOT dedup bundle (W3/H2)** — ✅ **Done.** (1) Triple VLAN map killed: `group_vars/router.yml` `vlans` is now a derived view of the SSOT `network_vlans` (`[{'id': 1, 'name': 'Blackhole', …, 'unused': True}] + network_vlans` — quoted Jinja dict keys; equivalence with the old literal proven against real group_vars, consumer loop `selectattr('unused','undefined')` keeps 10/20/21/30/40/50/99); `switch_vlans` in `group_vars/switch.yml` + `roles/switch/defaults/main.yml` derives as `{{ network_vlans }}` (consumers use id+name only). One literal VLAN list remains (all.yml); a VLAN edit touches one file. (2) WG AllowedIPs derived, not cross-linked: router-side `wireguard_s2s_vps.allowed_ips` = `{{ wg_s2s_vps.allowed_ips }}` — one list, two consumers (VPS wg interface + RouterOS peer allowed-address), drift impossible; all.yml comment marks itself SSOT for both sides. Resolved list verified identical to the old 7 entries. (3) `host_vars/oldsrv` `home_ip` literal dropped → `{{ oldsrv_home_ip }}` (network_static_hosts lookup; deployment-ansible.md host-var sample aligned). (4) Technitium `/opt/technitium/config` + Prometheus named-volume `prometheus-data` DOCUMENTED as exceptions in deployment-compose.md §Volume Strategy (technitium template renders oldsrv primary + Pi secondary — no single /srv/docker layout fits both; Kopia covers /opt/*; prometheus TSDB regenerable + not backed up + 30d retention = ephemeral side of the rule) + pointer comments in both templates. ⏳ **Deploy-gated:** derived vars resolve at the next router/switch/WG Ansible run (Phase 1.5). Closes audit W3/H2 + iac-changes §2/§8/§11 D10. · [deployment-ansible.md](docs/deployment-ansible.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-201 | 2026-08-21 | **Preseed placeholder assertions (B5/H2/H3)** — ✅ **Done.** (a) Runtime assertions: `IaC/host/post_install.sh` now greps the produced config (both users' `authorized_keys`, `sshd_config`, `fstab`) for the greppable placeholder tokens (`REPLACE_ME_*`, `<SERIAL>`, the nas serial stubs, `_FROM_1PASSWORD>` pubkeys) and exits non-zero BEFORE cleanup/reboot — an install that would boot into a locked-out host (no root password, KOPS-044) fails loudly on the installer console/log; produced-config files only, never the script itself (pattern definition would self-match). Same rule + same token set wired into `IaC/host/pi/first-boot-config.sh`: the written `user-data`/`firstboot.sh` are asserted before the card is declared ready (hit → loud fix instructions, files stay on the card). Red/green tested. (b) New 9th checker `scripts/check_placeholders.py`: fails on committed placeholder tokens outside designated files (bootstrap artifacts + the spec/audit docs that quote them: deployment-preseed.md, iac-changes.md §7, conventions-sugestions.md B5, changelog.md); scans all repo text except .git/brainstorming/reports/prompt-*; red/green verified; wired into `validate-all.sh` (+ header count, scripts/README row). (c) `docs/deployment-preseed.md`: wrong-script risk noted — the vps preseed late_command copies `/cdrom/preseed/post_install.sh`, the SAME media path the shared nas/oldsrv preseeds use, so a media assembled with the shared script silently installs ai-debug + the LAN-only AI key on the public box (rule lives only in a preseed comment); verify-the-media-before-boot guidance added; post_install skeleton + assertion rule documented in the owning spec. ⏳ **Deploy-gated:** assertions fire at the next real preseed/Pi install. Closes audit H2/H3/B5. · [deployment-preseed.md](docs/deployment-preseed.md), [scripts/README.md](scripts/README.md) |
| HD-197 | 2026-08-21 | **Scripts hygiene pass (F2/F3/F5/F6/F10 + gate)** — ✅ **Done.** (F2) `validate_doc_templates.py` now parses `network_vlans`/`network_static_hosts`/`network_ranges` (+ docker_services) from real group_vars with PyYAML — the hardcoded copies are gone, so template↔SSOT drift fails the gate; canonical `# Ansible managed` mock; `Path(__file__)` root; phantom `ha.kogler.si` hostvars key removed (per-host DNS IPs derived from `network_static_hosts` like the playbook); explicit exit codes + friendly failure. (F3) `render_rack_connections.py` emits the canonical `# Ansible managed` header first and writes LF explicitly; `docs/network-rack-generated.md` regenerated (+1 line). (F5) `check_doc_ips.py` scans ALL root *.md (minus prompt-* + the 8 ephemeral audit reports, fold+deleted by HD-203) + docs/** + IaC/**; changelog.md exempt (append-only); Doco-CD docstring tail removed; cp1252-safe violation echo; **9 real IP literals fixed**: IaC/README nut/keepalived/VIP prose → SSOT references, router/README AP table IPs → `network_static_hosts` pointer, todo HD-03/HD-09 rows → hostname/role wording. (F6) `check_doc_map.py` ROOT_SCAN allowlist → all root *.md except prompt-* (84→93 files scanned); dead `ROOT_DOCS` dropped. (F10) SMART snapshots moved to `reports/`; scripts/README pointer updated; collect-smart.ps1 "Windows 10"→11. (Gate) `ansible-playbook --syntax-check` across site.yml + all playbooks wired into validate-all.sh — WSL/CI-gated, skips with a note when ansible is absent or natively broken (WinError 87). ✅ `validate-all.sh` green. Closes audit F2/F3/F5/F6/F10/F11(rest). · `scripts.md` (fold+deleted, HD-203), [scripts/README.md](scripts/README.md) |
| HD-202 | 2026-08-21 | **Container-hardening rollout (S22/S21)** — ✅ **Done.** `cap_drop: [ALL]` + `read_only: true` + `tmpfs: /tmp` rolled into **42 templates / 51 services** (everything not exempt); minimal `cap_add: [NET_BIND_SERVICE]` only where a container binds <1024 (traefik :80/:443, traefik-ha VIP :443, pihole + technitium :53 — DHCP not used, so no NET_ADMIN). Exemptions per HD-204 decision, encoded in the validator backstop allowlist (`ALLOWED_NO_CAP_DROP`, each commented): GPU/device services (ollama, immich-ml, jellyfin transcode, sunshine), VPN gluetun sidecar (NET_ADMIN + /dev/net/tun), HA primary/standby (privileged/host-net rework = **HD-72**, separate task), raspberrymatic (parked HD-13). New validator backstop check: any rendered service without `cap_drop` fails unless allowlisted with justification (red/green verified). The one intentional `default('', true)` documented in place: signal-cli `SIGNAL_CLI_CAPTCHA` (optional anti-bot token — must not abort the render; fail-loud law applies to REQUIRED secrets only). All state paths verified volume-mounted, so `read_only` is structurally sound. ⏳ **Deploy-gated:** live-verify per-service writability at first deploy (apps that write outside volumes/tmpfs would crash on start — e.g. exotic cache dirs); adjust tmpfs mounts then. Closes audit S21/S22. · [deployment-compose.md](docs/deployment-compose.md) §Container Security |
| HD-192 | 2026-08-21 | **Version-pin completion (S2/D1)** — ✅ **Done.** 24 services pinned into `group_vars/all/versions.yml` (registry-verified via Docker Hub/GHCR API, 2026-08-21): bazarr 1.6.0, dozzle v10.7.3, element-web v1.11.96, homepage v1.4.5, jellyfin 10.11.11, lidarr 3.1.0.4875-ls39, metabase v0.63.14.1, pihole 2026.07.2, prowlarr 2.5.2.5491-ls156, qbittorrent 5.2.3, radarr 6.3.0, raspberrymatic 3.79.6.20250203-bba7b73, recyclarr 7.4.0, renovate 35.10.0, sabnzbd 5.1.1, seerr v3.4.1, signal-cli-rest-api 0.202-dev, sonarr 4.0.19.2979-ls321, sunshine v2026.821.30050-ubuntu-24.04, technitium 15.4.0, ollama 0.32.15-rocm, gluetun v3.41.3, HA 2026.8.1 (host-local var, raspberry_pi.yml). All `\| default('latest')`/`default('release')`/`default('stable')` fallbacks dropped (fail-loud per CONVENTIONS §7). **Findings fixed in the same change:** (1) `qm12/gluetun` fork NO LONGER EXISTS on Docker Hub → sidecar re-pointed to upstream `qmcgaw/gluetun:v3.41.3`; (2) `vectorim/element-web` org is gone (GHCR DENIED) → template re-pointed to `ghcr.io/element-hq/element-web`. Fluid exceptions documented + allowlisted with MUST-pin justification: tuwunel (HD-121 precedent), profilarr (upstream publishes no versioned tags — probe verified). Validator `ALLOWED_LATEST` inverted: 17 entries → 2 justified. ⏳ **Deploy-gated:** Renovate PRs arrive for the new pins; first deploy pulls the pinned digests successfully; registry-reverify `renovate_version` (GHCR tag list truncated), `signal_cli_rest_api_version` (-dev scheme), `home_assistant_version` (calver guess) at first deploy. Closes audit S2/D1/F1.6. · [deployment-compose.md](docs/deployment-compose.md), [security.md](docs/security.md) §2 |
| HD-189 | 2026-08-21 | **Gate hardening bundle** — ✅ **Done.** (a) `validate-secrets.py` scan scope widened to `templates/**/*.j2` + `roles/*/vars/*.yml` + `roles/*/files/*` (a literal credential in a compose template was caught by nothing); the widened scan surfaced 7 hits — all false positives (env-var indirections `os.environ/…`, boolean/numeric policy flags) → resolved as value-shape exemptions, not key allowlists. (b) `validate-docker-services.py`: fail-open group_vars loader → aborts with file+error; dead `NETWORK_MAP` DELETED (decided HD-204 — unfireable `defined_nets` escape, stale vs HD-59; enforcement = external-networks rule + docs); `WEB_SERVICES`/`_EXTRA_TEMPLATES` duplicates deduped; `_extra_templates` now loaded from `roles/docker_services/defaults/main.yml` (SSOT — adds the missed `headscale/policy.hujson.j2`, drops the never-rendered `home-assistant-standby/keepalived.conf.j2` expectation); `BASE_CTX` drift-proofed by loading `versions.yml` + plain `all.yml` vars + storage-role `storage_uid/gid` from SSOT (`op_vault` → `Homelab-ansible`, GPU gids → 104/44, full `crowdsec_collections`, real `network_ranges` — validator-rendered bytes change accordingly, intended); CWD-independent `Path(__file__)` root; stray no-op `sys` line + stale "41 templates" docstring removed (count now derived). (c) New 8th checker `scripts/check_vault_name.py`: flags a bare `Homelab` vault reference (vault-context lines) across canonical .md + IaC yml/j2 + scripts/*.py; changelog.md exempt (append-only history); red/green verified; 30 stale references fixed across 22 files — incl. two FUNCTIONAL bugs: `sync-authentik-users.sh.j2` read `op://Homelab/…` (would fail at runtime) and `authentik-secret-egress.sh.j2` defaulted `OP_VAULT` to the wrong vault. Red/green tested. ✅ `validate-all.sh` green (8 checkers). Closes audit F1.1/F1.2/F1.3/F1.5/F1.8/F7/F11(partial). · [scripts/README.md](scripts/README.md), `scripts.md` (fold+deleted, HD-203) |
| HD-175 | 2026-08-21 | **Audit round 2 — docs/IaC contradiction + architecture/security review (prompt.md)** — ✅ **Done.** Full-repo audit (all docs/*.md + IaC) producing 7 root reports: `docs-vs-iac.md` (contradiction report: HD-135 split not propagated to IaC/README + deployment-ansible sample + deployment-tasks phases; 4 divergent public-record lists vs cloudflare_dns SSOT; wildcard-cert issuer ambiguity VPS-vs-oldsrv; 21 bare-`:latest` templates vs §7 law; vault-name drift; stale counts), `docs-changes.md`, `iac-changes.md`, `conventions-sugestions.md` (derived-pointer rule, audit-report lifecycle, single-cert-issuer, bootstrap security floor), `tracking-sugestions.md` (deployment-tasks phase sweep, todo.md broken tables, date typos), `architecture.md`, `security.md` (**top finding:** nftables forward `oifname docker*` accept lets published ports bypass input default-deny — authentik LDAP `3389` publish on the public VPS; fix before first deploy). Reports are **ephemeral per the A3 lifecycle proposal** (HD-153 precedent): fold actionable items into owning docs + HD rows, then delete. Adoption of individual findings = human triage; no canonical doc changed in this task. ✅ `validate-all.sh` green. · [prompt.md](prompt.md) |
| HD-176 | 2026-08-21 | **Audit round 2b — `scripts/` folder review (`scripts.md`)** — ✅ **Done.** Line-by-line audit of all 14 scripts + data files → root report `scripts.md`. Headline findings: `validate-docker-services.py` duplicate dict keys (NETWORK_MAP immich-app/immich-ml/sunshine last-wins), NETWORK_MAP dead code (defined_nets escape makes it unfireable), BASE_CTX mock drift (`op_vault: Homelab`, GPU gids 123/124 vs SSOT 104/44), fail-open group_vars loader; `validate_doc_templates.py` validates hardcoded copies not real group_vars + retired ansible_managed header + CWD-dependent root; `render_rack_connections.py` emits a non-canonical header (§8.2 violation); `check_doc_ips.py`/`check_doc_map.py` scan scopes miss CONVENTIONS/todo/changelog/IaC/new root docs (hand-maintained ROOT_SCAN); `validate-secrets.py` blind spot — templates/*.j2 and roles/*/vars not scanned for literal credentials; provisioner nits (bcrypt via argv, dead gen_wg_key). Also proposed: vault-name lint gate + Ansible syntax-check in the gate. Ephemeral per the A3 lifecycle like the other round-2 reports. ✅ `validate-all.sh` green. · [scripts/README.md](scripts/README.md), `scripts.md` (fold+deleted, HD-203) |
| HD-177 | 2026-08-21 | **Audit round 2c — deep-dive verification pass (P1–P7), reports expanded** — ✅ **Done.** Line-by-line verification of roles/router + roles/switch (715+360 lines), the docker_services deploy path, all 49 compose templates, storage/monitoring/home_assistant role-vs-doc parity, secrets matrix (45 items), db-backup blocks, Blueprint providers, stale-date sweep. **New deploy-blocking findings:** immich-app ML URL points at a container name that cannot resolve cross-host (ML dead on VPS); Pi first-deploy ordering bug (docker_services before home_assistant auto-creates keepalived.conf/config/secrets.yaml as directories → HA role copy fails); traefik ACME template deployed unmodified on BOTH oldsrv+VPS = dual wildcard issuers (escalates cert-issuer ambiguity to an IaC bug). **New M/L:** Authentik trusts whole traefik-public /16 (HD-81 anti-pattern); Grafana auth-proxy raw-header trust + auto-signup; sso + cockpit routes with zero edge middleware; pihole conditional-forwarding points at the HA VIP (not DNS); oldsrv Kopia agent missing from SSOT; Kids VLAN firewall = debug placeholder vs documented matrix; DNS secondary unreachable cross-VLAN; trusted-ha list unused; homepage ALLOWED_HOSTS missing alias; cockpit hardcoded IPs; privado-vpn_api + opencloud_login missing from master list; validator/role _extra_templates drift. **Verified-good:** HD-155 RouterOS ACL exact, INPUT chain exact, WG fail-closed, storage-role parity, db-backup DB01–04, Blueprint 8 providers, middleware law on labelled routes, Alloy instance label. Reports expanded in place: docs-vs-iac.md §J, security.md §5 (+S1 verify plan, S15–S22), iac-changes.md §11 (D1–D11), architecture.md W11/W12, tracking-sugestions.md date sweep, scripts.md F1.8. Ephemeral per A3. ✅ `validate-all.sh` green. · `docs-vs-iac.md`, `security.md`, `iac-changes.md` (all fold+deleted by HD-203) |
| HD-161 | 2026-08-20 | **Router/switch: assert state before `api_modify` + decide router API TLS** — ✅ **Done (Part A + decision).** Added `community.routeros.api_facts` **assert-before-mutate** to both `roles/router` and `roles/switch`: read `/system identity` **once per role run** and `assert` the target matches the per-gear `routeros_expected_identity` (`group_vars/router.yml` = `router.kogler.si`, `group_vars/switch.yml` = `switch.kogler.si`, both derived from the bootstrap `*.rsc.j2` `/system identity set name=` values); fail-loud guard (`''`) in both role defaults aborts on a blank identity. Decided + documented **API TLS (Part B):** management API moves to **`api-ssl` (:8729) with a self-signed device cert** + `validate_certs: false` in Ansible — TLS on a private Mgmt-VLAN link, **no public CA / no Let's Encrypt dependency**; plaintext `api` (:8728) retired by a manual WinBox cutover. Part B enable tasks remain **deploy-gated** (router must be reset with `rb4011_initial.rsc.j2` first; switch not yet reachable — no VLAN 99 live). Identity values verified live (router `/system identity` = `router` pre-reset; switch = `switch` via WinBox — both will become the `*.kogler.si` names after bootstrap reset). · [deployment-ansible.md](docs/deployment-ansible.md), [network-ops.md](docs/network-ops.md) |
| HD-164 | 2026-08-20 | **Remove the orphaned `kopia` Ansible role** — ✅ **Done.** `roles/kopia/` deleted — it was an **unused stub** (only the admin-guard assert; Kopia runs containerized via `docker_services` `kopia-server`). Verified **completely orphaned**: not referenced in `site.yml`, any playbook, `inventory.ini`, or group_vars. Stripped the `kopia` row from the role table + the kopia note bullet + the `roles/` tree line + the `kopia` role-catalog section in `IaC/README.md`, and the `kopia` tree line + role-catalog section in `docs/deployment-ansible.md`. Repaired a duplicate `# router` heading in both files. README `(18)` in-use role count unchanged (kopia was never counted — it sat in the `proxmox (1, TODO) — kopia intentionally unused` note). Kopia backup itself is unaffected. · [IaC/README.md](IaC/README.md), [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-162 | 2026-08-20 | **Move Authentik glue to an explicit pre-pass + compose-validate inside the service loop** — ✅ **Done (5 commits: `7ba48f3`, `f3241e5`, `910f6e1`, `13176fc`, `67280bd`; final `67280bd`).** The `docker_services` role no longer runs the Authentik secret-egress glue as a side-iteration on the `authentik` loop entry. New `roles/docker_services/tasks/prepass-authentik.yml` is included from `main.yml` **BEFORE** the deploy loop, gated on `'authentik' in docker_services` (VPS edge only; home hosts skip — the assert is a safety net). Each service iteration now runs `docker compose -f <svc>/docker-compose.yml validate` before `docker_compose_v2 up -d` (catches a bad render before deploy). Docs synced: deployment-ansible.md Role Catalog, deployment-compose.md Deploy-ordering (steps 2-4 name the pre-pass), group_vars/vps.yml HD-146 comment (glue runs in the pre-pass, `authentik` must stay in the list). Deploy-gated live-verify stays under HD-147 (Phase 1). · [deployment-ansible.md](docs/deployment-ansible.md), [deployment-compose.md](docs/deployment-compose.md) |
| HD-174 | 2026-08-20 | **Bootstrap per-domain service triage (`-review` + `-rejected`)** — ✅ **Done.** Seeded the §8.3 pattern across 4 big domains: `docs/{services,deployment,network,storage,smart-home}-{review,rejected}.md` (8 files). `-rejected` logs are append-only, sorted by name, each row = `| tool | status | date | why + link |` and mirror changelog `*(decision)*` rows. Seeded decisions: **services** (AnythingLLM/LocPilot dropped-superseded HD-108, GoCardless superseded + Ghostfolio/TradeSight rejected via services-finance, Pangolin dropped via services.md) · **deployment** (Contabo superseded HD-93B, Doco-CD dropped HD-150, Proxmox Phase-1 rejected/deferred HD-92, watchtower rejected HD-39) · **storage** (iDrive dropped HD-29, MinIO dropped HD-139) · **network** (netplan rejected HD-56) · **smart-home** (TileBoard dropped HD-24). All linked from owning docs (Related) + `docs/index.md` Document Map + a new "Triage a candidate" dispatch row. `-review` queues left near-empty with the 30-day stale rule stated. ✅ `validate-all.sh` green. · CONVENTIONS.md §8.3, [services.md](docs/services.md), [index.md](docs/index.md) |
| HD-01 | 2026-08-15 | **Create remaining `docker_services` compose templates** — 24 created ✅ (pre-existing), 17 TODO stubs implemented ✅ (bazarr, dozzle, immich-app, immich-ml, jellyfin, lidarr, ollama, pihole, profilarr, prowlarr, qbittorrent, radarr, recyclarr, sabnzbd, seerr, sonarr, sunshine). 18+ compliance fixes applied across all 43 templates. Immich v3 updated (microservices merged, postgres/valkey images). All validators pass. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-07 | 2026-08-15 | **NUT clients on `oldsrv` + `pi`** — upsmon slave, per-host shutdown delay 60/0/0. ✅ **IaC done:** host_vars wired (oldsrv 60 / pi 0), client upsmon + secret-free upssched-cmd deployed on clients, **fixed deferred-shutdown bug** (`shutdown -h +60` = minutes not seconds → now owned by the upssched ONBATT timer `AT onbatt-delay * EXECUTE powerdown`; LOWBATT stays immediate). ⏳ Not deployed (hosts unprovisioned). · [hardware-ups.md](docs/hardware-ups.md) |
| HD-10 | 2026-08-15 | **Generate `oldsrv/preseed.cfg`** — created: ext4 root on 960 EVO 500 GB, 970 EVO 1 TB left raw for ZFS pool `nvme`, XFCE desktop, GRUB on NVMe. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-11 | 2026-08-15 | **Create Pi first-boot config** — `first-boot-config.sh` writes cloud-init `user-data` + fallback SSH enable on boot partition for raspi.debian.net images. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-12 | 2026-08-15 | **Implement `inventory.md` render pipeline** — enhanced `inventory.md.j2` template (proper frontmatter, 5-column table, render instructions), created initial `docs/inventory.md` from group_vars data. Render hook already wired in `render-docs.yml` + `docker_services` post-deploy. · [interfaces.md](docs/interfaces.md) |
| HD-16 | 2026-08-15 | **Implement Authentik compose template + Traefik Forward-Auth middleware** — server+worker+postgres+redis compose (115 lines), `authentik-forward-auth@file` middleware (42 lines), `subdomain: sso` entry. All implemented in `db51854`. · [services-authentik.md](docs/services-authentik.md), [services-traefik.md](docs/services-traefik.md) |
| HD-19 | 2026-08-15 | **Pi SD-card wear: trim HA recorder + log strategy** — recorder `purge_keep_days: 2` / `commit_interval: 5` / `exclude` (not disabled: keeps Logbook/Energy-LTS/history_stats) + Docker log driver `local` (10m×2) + `journald Storage=volatile` + `/var/log` tmpfs. Implemented as HA config template + docker role daemon.json + home_assistant role tasks (all gated to Pi only). · [observability.md](docs/observability.md) |
| HD-35 | 2026-08-15 | **Fix broken `network-devices.md` reference** — link was already removed from network-vlans.md (resolved). The content lives in `assets/Network-Devices.canvas`. · [network-vlans.md](docs/network-vlans.md) |
| HD-50 | 2026-08-15 | **Implement `docker_services` Ansible role** — ✅ **Done (`eadf9d4`).** Role creates external Docker networks, copies templates, renders Jinja2 with 1Password lookups, runs `docker compose up -d`. Added: systemd auto-start per service (`docker-compose@.service` template unit + `systemctl enable`), post-deploy Homepage reload + `inventory.md` render. Fixed pre-existing path bugs (templates at playbook level, not in role). All 43 templates validated. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-61 | 2026-08-18 | **Pin image tags — Traefik first** — ✅ **Done (`85d387c`).** `traefik_version: latest` → `v3.5.2` in `group_vars/all.yml` (shared by oldsrv edge + Pi `traefik-ha`); matches existing `v`-prefixed pin style (`certs_dumper_version: v2.8.3`); Renovate docker datasource follows. Other mutable tags tracked separately. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-64 | 2026-08-18 | **Fix Loki schema `from:` date** — ✅ **Done (`85d387c`).** `2026-01-01` → `2025-01-01` in `loki.yaml.j2` so the TSDB schema is active for any deployment from Jan 2025 (also covers KOPS-065; avoids silently dropping logs). · [observability.md](docs/observability.md) |
| HD-114 | 2026-08-18 | **Drop dead Alertmanager scrape + phantom alerting block** — ✅ **Done (`85d387c`).** Removed obsolete `alertmanager` scrape job and `alerting.alertmanagers` block from `prometheus.yml.j2` (`alertmanager:9093` not deployed — Grafana Alerting covers Phase 1 per observability.md). Comment notes Phase-2 re-add. · [observability.md](docs/observability.md) |
| HD-62 | 2026-08-18 | **Remove / unbind host ports** — ✅ **Done.** Dropped signal `8080` host binding (n8n via services-internal overlay); loopback-bound prometheus `9090` + loki `3100` (host Alloy agent reaches via localhost); dropped redundant technitium `5380` (web UI via Traefik overlay), kept `53` (network-wide DNS by design); sunshine streaming ports kept w/ justification (manual-start gaming, LAN/VPN clients). · [deployment-compose.md](docs/deployment-compose.md) |
| HD-65 | 2026-08-18 | **Fail-loud on missing secrets** — ✅ **Done.** Removed `default('')` from pihole `WEBPASSWORD` so a failed 1Password lookup aborts the render instead of deploying an unprotected Pi-hole; added a comment documenting the fail-loud policy. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-91 | 2026-08-18 | **Fail-closed guards on missing secrets** — ✅ **Done.** Removed `default=''` on nut SMTP creds (`nut-smtp_login`); added a fail-closed `assert` guard in `docker_services` role that aborts with a clear `fail_msg` if `op_vault` is unset or the 1Password CLI is missing before any compose render. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-82 | 2026-08-18 | **Grafana single auth path** — ✅ **Done.** Added `GF_AUTH_DISABLE_LOGIN_FORM: "true"` to grafana compose so every login goes through Authentik Forward-Auth; the admin account stays reachable for provisioning via API/basic auth (break-glass). · [observability.md](docs/observability.md) |
| HD-116 | 2026-08-18 | **Alloy/Prometheus probe correctness** — ✅ **Done.** Added per-host `instance` label (`{{ inventory_hostname }}`) to the Alloy host-metrics scrape so series don't collide across hosts (KOPS-036); removed 401/403 from blackbox `http_2xx` valid_status_codes so auth-walled/broken endpoints are flagged, not counted up (KOPS-035). · [observability.md](docs/observability.md) |
| HD-117 | 2026-08-18 | **Playbook role-order fixes** — ✅ **Done.** Documented KOPS-050 dependency (network→storage→docker_services) in `home_servers.yml` (already correctly ordered); reordered `raspberry_pi.yml` so `docker_services` runs before `home_assistant` (KOPS-063). · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-60 | 2026-08-18 | **crowdsec-only middleware chain** — ✅ **Done.** Added `crowdsec-only@file` (chain = crowdsec-bouncer only) to `middlewares.yml.j2`; applied it to every self-auth'd route that can't use Forward-Auth: ha (oldsrv route + standby), jellyfin, matrix, seerr, chat. · [services-traefik.md](docs/services-traefik.md) |
| HD-118 | 2026-08-18 | **Apply `crowdsec-only` to HA route** — ✅ **Done (KOPS-004).** oldsrv `traefik` ha route + `home-assistant-standby` compose label get `crowdsec-only@file`; Pi `traefik-ha` edge documented as intentionally CrowdSec-free (CrowdSec is on oldsrv, down on failover — bouncer would fail the very route it must survive). · [services-traefik.md](docs/services-traefik.md) |
| HD-119 | 2026-08-18 | **Seerr behind CrowdSec** — ✅ **Done (KOPS-047).** Applied `crowdsec-only@file` (own login; KOPS-047 recommends CrowdSec-only, not Forward-Auth, since family non-admins submit requests directly). · [services-traefik.md](docs/services-traefik.md) |
| HD-120 | 2026-08-18 | **Element Web `chat.kogler.si` middleware gap** — ✅ **Done (KOPS-025/054).** Applied `crowdsec-only@file`; Matrix-native SSO means no Forward-Auth. · [services-traefik.md](docs/services-traefik.md) |
| HD-121 | 2026-08-18 | **Matrix homeserver `:latest` → pin** — ✅ **Done (KOPS-030).** Added `tuwunel_version` group_var + template reference (matches traefik/certs convention); kept `latest` default with an explicit MUST-pin comment since the obscure single-maintainer tag can't be safely blind-pinned before a registry check. · [services-matrix.md](docs/services-matrix.md) |
| HD-79 | 2026-08-18 | **Pin RaspberryMatic USB path per host** — ✅ **Done (KOPS-040).** Replaced the non-resolving wildcard `/dev/serial/by-id/*` with a per-host `homematic_usb_by_id` host_var (Pi + oldsrv) referenced by the template; placeholder filled at deploy. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-123 | 2026-08-18 | **RaspberryMatic compose hygiene** — ✅ **Done (KOPS-038/049).** Remapped CCU admin UI host port off `80` (Traefik conflict) to per-instance `8085`/`8086` primary/standby via `rmat_ui_port`; duplicates/conflicts resolved. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-77 | 2026-08-18 | **Split `n8n_password`** — ✅ **Done (KOPS-031).** `n8n_password` stays `N8N_ENCRYPTION_KEY` (immutable); new `n8n-webhook_api` → `N8N_BASIC_AUTH_PASSWORD` so webhook/API auth is independently rotatable; Grafana contact point sends basic auth (`grafana`/token). Updated deployment-secrets.md master list. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-125 | 2026-08-18 | **Signal CLI REST API auth** — ✅ **Done (KOPS-002).** Added `SIGNAL_CLI_API_TOKEN` (from `signal-internal_api`) requiring an `X-Api-Key` header, so no container on services-internal can send Signal as Domen's number; host port already unbound in HD-62. n8n sends the token. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-126 | 2026-08-18 | **OpenCloud edge-terminated TLS decision** — ✅ **Done (KOPS-006).** Documented that `OC_INSECURE: true` is CORRECT behind the Traefik edge (forcing false would break the HTTP backend); plaintext confined to the isolated traefik-public Docker network; internal mTLS deliberately not adopted (no gain vs edge TLS + CrowdSec for a single trusted host). · [deployment-compose.md](docs/deployment-compose.md) |
| HD-63 | 2026-08-18 | **Uncomment immich DB backup + OpenCloud tar note** — ✅ **Done (KOPS-026).** Enabled DB03 block for `immich-postgres` (fixed stale `immich-db` name, on db-internal); added note that OpenCloud uses an embedded DB → its backup is a data-dir tar (covered by service-state/Kopia scope), not a DB dump. · [backup.md](docs/backup.md) |
| HD-80 | 2026-08-18 | **Disable root login in preseed** — ✅ **Done (KOPS-044).** `d-i passwd/root-login` set to `false` on nas + oldsrv (removes the identical placeholder-hash + password-root attack surface; `ansible-admin` has key-only SSH + NOPASSWD sudo). deployment-preseed.md updated. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-86 | 2026-08-18 | **Stop persisting OP token in `~/.bashrc`** — ✅ **Done (KOPS-011).** `bootstrap.sh` now stores `OP_SERVICE_ACCOUNT_TOKEN` in `~/.config/op/homelab-sa-token` (chmod 600) and sources it, instead of embedding the plaintext secret inline in the world-readable `~/.bashrc`. · [deployment-secrets.md](docs/deployment-secrets.md) |
| HD-87 | 2026-08-18 | **Pin CrowdSec bouncer plugin version** — ✅ **Done (KOPS-029).** Added `crowdsec_bouncer_plugin_version: v0.4.0` to group_vars/all.yml; removed the hardcoded `default('v0.4.0')` from the traefik compose. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-88 | 2026-08-18 | **Dedup sshd_config append** — ✅ **Done (KOPS-012).** `post_install.sh` now guards the sshd hardening append with a marker check so a re-run never accumulates duplicate directives. · [deployment-preseed.md](docs/deployment-preseed.md) |
| HD-127 | 2026-08-18 | **Deploy scripts not root on live** — ✅ **Done (KOPS-056).** Added a dedicated non-root `svc-backup` system user (in docker group, owns the staging dir) and set `User=`/`Group=` on the `push-services` systemd unit so docker exec backups don't run as root; added post-backup integrity checks (`unzip -t` on the Forgejo dump, `PRAGMA quick_check` on the n8n sqlite) so corruption is caught at backup time, not restore. · [deployment-compose.md](docs/deployment-compose.md) |
| HD-81 | 2026-08-18 | **Shrink HA `trusted_proxies`** — ✅ **Done (KOPS-039).** Replaced the blanket traefik-public `/16` trust with an explicit `ha_trusted_proxies` group_var list of specific Traefik edge container IPs (both primary + standby render the same config). Prevents a compromised sibling container from spoofing client IPs. · [smart-home-failover.md](docs/smart-home-failover.md) |
| HD-78 | 2026-08-18 | **Router INPUT-chain firewall** — ✅ **Done (KOPS-003/009).** Added `chain: input` rules to the router role: management service ports (`22,8728,8729,8291,80,443`) reachable only from the Management VLAN (99) and `trusted-admin` (nas/oldsrv/ha-vip), dropped from all other sources; established/related + DHCP input accepted so the control plane and clients keep working. · [network-ops.md](docs/network-ops.md), [network-vlans.md](docs/network-vlans.md) |
| HD-83 | 2026-08-18 | **Restrict router API in bootstrap .rsc** — ✅ **Done (KOPS-003/042).** `rb4011_initial.rsc.j2` binds `api`/`www-ssl`/`ssh` to the Management VLAN interface (`vlan{mgmt}-mgmt`) so none listens on WAN/other VLANs in the bootstrap window; plain `www` and `api-ssl` (no TLS cert) left disabled. · [network-ops.md](docs/network-ops.md) |
| HD-84 | 2026-08-18 | **Headscale ACL policy** — ✅ **Done (KOPS-022).** Added a real `policy.hujson` (mount + `_extra_templates` + compose volume); OIDC auto-approved nodes need the admin-applied `tag:kogler` to reach the mesh (`tag:kogler:*`), admin plane always reachable; corrected the misleading "requires admin approval" comment. · [network-vpn.md](docs/network-vpn.md) |
| HD-90 | 2026-08-18 | **Renovate managers: ansible-galaxy + pip** — ✅ **Done (KOPS-062).** Added `ansible-galaxy` + `pip_requirements` managers (with the same 3-day stability delay as Docker) so Renovate tracks `IaC/ansible/requirements.yml` collections (community.general/routeros/docker) and `client/office-bridge/requirements.txt`, not just Docker images. · [deployment-renovate.md](docs/deployment-renovate.md) |
| HD-94 | 2026-08-18 | **Centralize shared-data owner + Samba + MinIO (HD-131 D1/D2/D4/D5)** — ✅ **Done (KOPS-060).** `storage_uid`/`storage_gid`=`1005` (`media`), neutral owner provisioned on nas+oldsrv; 9 filesystem/SMB templates (sonarr, radarr, lidarr, prowlarr, bazarr, sabnzbd, qbittorrent, jellyfin, opencloud) centralized to the vars; Samba shared `media` + per-user shares with idempotent provisioning; MinIO S3 service + Immich→S3 originals; Authentik→NAS/OpenCloud glue (`sync-authentik-users.sh`, D5). Remaining: populating `storage_samba_users` once family accounts exist (HD-51). · [deployment-compose.md](docs/deployment-compose.md) |
| HD-85 | 2026-08-18 | **Add CrowdSec collections** — ✅ **Done (KOPS-041).** Moved the hardcoded `COLLECTIONS` env into a `crowdsec_collections` group_var (`all.yml`) and extended it to home-assistant, matrix, grafana parsers (on top of traefik + linux) so the exposed auth-surfaces are parsed by the WAF. Added the var to the render validator mock. · [observability.md](docs/observability.md), [services-traefik.md](docs/services-traefik.md) |
| HD-137 | 2026-08-18 | **Rewrite router/switch roles off nonexistent `community.routeros_*` modules** — ✅ **Done.** `roles/router` (40 calls) + `roles/switch` (15) used declarative schematic modules that don't exist in `community.routeros` (which only ships `api`/`api_facts`/`api_find_and_modify`/`api_info`/`api_modify`/`command`/`facts`), so both playbooks failed to resolve them. Rewrote all ~55 call-sites onto idempotent `community.routeros.api_modify` (path-based), consolidating firewall filter per-chain (forward + input) as ordered `data` lists with `handle_absent_entries: remove` + `ensure_order` for deterministic rule order. Added `routeros_api_host`/`_user`/`_tls` connection vars to both role defaults; 1Password auth unchanged. **Verified:** both playbooks pass `--syntax-check` (24 + 19 coherent tasks); all 11 repo playbooks pass. ⏳ deploy-gated live-verify. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-138 | 2026-08-18 | **Fix removed `stdout_callback = yaml` in `ansible.cfg`** — ✅ **Done.** `ansible.cfg` set `stdout_callback = yaml`, referencing the `community.general.yaml` callback **removed in community.general v12.0.0** (installed 13.1.0) — this blocked **every** playbook run with `The 'community.general.yaml' callback plugin has been removed.` Changed to the modern replacement: `stdout_callback = default` + `result_format = yaml` (built into ansible-core ≥2.13). **Verified:** `ansible-playbook playbooks/dns.yml` now parses and reaches task 1 (stops only on the expected local `op` sign-in). · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-136 | 2026-08-18 | **`cloudflare_dns` FQCN → `community.general` fix** — ✅ **Done.** The Cloudflare DNS role called `community.dns.cloudflare_dns`, but that module lives in **`community.general`** (community.dns ships no Cloudflare module — only nameserver/hetzner/hosttech/infomaniak/etc.), so the DNS playbook couldn't resolve it. Changed `roles/cloudflare_dns/tasks/main.yml` to `community.general.cloudflare_dns` and corrected the stale "Requires community.dns" comments in `requirements.yml`, `tasks/main.yml`, and `playbooks/dns.yml`. No install needed — `community.general` 13.1.0 already present in the venv. **Verified:** `ansible-playbook playbooks/dns.yml --syntax-check --list-tasks` resolves both role tasks. · [deployment-ansible.md](docs/deployment-ansible.md) |
| HD-115 | 2026-08-18 | **Loki auth/RBAC** — ✅ **Done (KOPS-023/051).** `auth_enabled: false` → `true` (Loki multi-tenant): pushes + queries now require the `logs` tenant ID — wired through Alloy (`tenant_id = "logs"`) and the Grafana datasource (`jsonData.tenantId`). Network already restricted to trusted infra: Alloy writes loopback-only (`127.0.0.1`), Grafana reads on db-internal, no LAN/public bind. Documented the tenant-isolation caveat in observability.md (re-evaluate if db-internal grows). · [observability.md](docs/observability.md) |
| HD-152 | 2026-08-19 | **Reconcile `docs/index.md` document map with on-disk `.md`** — ✅ **Done.** Added the missing files to the map (`automation-renewals.md`, `deployment-ai-stack-secrets.md`, `inventory.md` ★, `rack-connections.md` ★, `subscriptions-table.md` ★) + the 2 missing manual guides (`chat.md`, `office-ai.md`); corrected the rack-connections annotation (generated from JSON, not IaC) + added `deployment-ai-stack-secrets.md` to the AI-stack dispatch row. Map now covers all 46 on-disk docs (verified). · [index.md](docs/index.md) |
| HD-153 | 2026-08-19 | **Consolidate audit deliverables into canonical docs** — ✅ **Done.** The 7 root audit files (`docs-vs-iac.md`, `docs-changes.md`, `iac-changes.md`, `conventions-proposal.md`, `lifecycle-docs-proposal.md`, `architecture.md`, `security.md`, commit `4b20b59`) were temporary; their durable content is now folded into canonical `docs/*` and they are **deleted**. Folded: MinIO/S3→live-Box-CIFS bullets (services.md, deployment-compose.md, storage-zfs.md, subscription.md); observability backend→VPS + SPOF rewrite (hardware-oldsrv.md, storage-zfs.md, observability.md); alerting de-duped into observability.md (interfaces.md); template/role counts corrected + role-catalog owner = deployment-ansible.md (IaC/README.md, deployment-tasks.md); index.md map reconciled (with HD-152); README interface-count + key decisions (HD-135/150); two-sided deploy-gate/derived-counts/data-location/version-pin conventions (CONVENTIONS.md); **new backlog rows** from audit P0/P1 actionables: **HD-154** (VPS host hardening checklist), **HD-155** (tunnel least-access ACL + fail-loud WG gate), **HD-156** (all.yml version-pin split → versions.yml), **HD-157** (secrets-lint + doc-map/count lint), **HD-158** (network-addresses IP-SSOT until switch map verified), **HD-159** (blackbox home↔VPS liveness), **HD-160** (services-internal sibling auth), **HD-161** (router/switch assert-before-mutate + API TLS), **HD-162** (docker_services glue pre-pass), **HD-163** (render-script consolidation), **HD-164** (kopia stub expiry), **HD-165** (per-gear routeros creds); security gaps → docs/security.md §8–§9. Git history preserves the full audit for reference. · [security.md](docs/security.md), [services.md](docs/services.md), [services-vps.md](docs/services-vps.md) |
| HD-156 | 2026-08-19 | **Split `group_vars/all.yml` version pins → `versions.yml`** — ✅ **Done.** all.yml was a grab-bag (timezone/NTP/GPU gids + every `*_version` pin + WG + IP + livebox + address SSOT + observability). Moved all **25 `*_version` image pins** out of all.yml into the new **`group_vars/all/versions.yml`** — loaded for every host via the implicit `all` Ansible group, so Renovate docker datasource updates + version review are now a **single file** (HD-134/KOPS-030 goal). `all.yml` keeps infra only + a pointer comment. Updated: IaC/README.md + docs/deployment-ansible.md group_vars trees, CONVENTIONS.md §7 (path now live, not proposed), validator `BASE_CTX` note (mock values unchanged — names/values identical), `raspberry_pi.yml` + litellm compose comments. `home_assistant_version` deliberately stays host-local in `raspberry_pi.yml` (host-scoped, not global). Guard: `bash scripts/validate-all.sh` **green — 48 compose templates valid** (YAML parse + full structural checks). · [deployment-compose.md](docs/deployment-compose.md) |
| HD-157 | 2026-08-19 | **Add secrets-lint + doc-map/count lint to `validate-all.sh`** — ✅ **Done.** (a) **`validate-secrets.py`** — flags literal credential values (`*_password`, `*_secret`, `*_token`, `*_api_key`, `secret:`, PEM/private keys) in `group_vars`/`host_vars`/role defaults; 1Password item references (bare `secret:` values), Jinja/`lookup()`, placeholders exempt. (b) **`check_doc_map.py`** — every `.md` under `docs/` (excl. `assets/`, `manual/*`) must appear in the `## Document Map` block; every `[..](..md)` link resolves (no dead links). (c) **count-lint** — built into `validate-docker-services.py`: every dir under `templates/docker_services/` must have a `template_dir:` in a group_vars `docker_services` list; `RETAINED_TEMPLATE_DIRS` allowlist keeps intentionally-parked dirs (e.g. `raspberrymatic`, HD-13). **Wired all into `validate-all.sh`** (now 6 checkers). **Fixes found by the new linters:** stale “48 templates” → **49** in `IaC/README.md` + `docs/index.md` (HD-166 ONLYOFFICE added the 49th). ✅ `validate-all.sh` green. · [scripts/](scripts/), [docs/index.md](docs/index.md) |
| HD-171 | 2026-08-20 | **Cross-cutting docs resolution** — ✅ **Done** (final cross-cutting task after HD-167→170). Initial pass marked all 6 cross-cutting docs (`cross_cutting: true` + `(cross-cutting)` Role header): `security.md`, `backup.md`, `storage.md`, `observability.md`, `llm-office.md`, `hardware-gpu.md`. **Revised (2026-08-20, owner review):** `observability.md` **reclassified to a domain** — it owns a cohesive service set (Prometheus/Loki/Grafana/n8n/Alloy/blackbox/Dozzle/nut_exporter) with a **primary backend home on the VPS** (HD-135), so per §8.4's own test it is not cross-cutting; removed `cross_cutting: true`, set `domain: observability`, header no longer marked cross-cutting. **CONVENTIONS §8.4 updated** to encode the rule: a doc whose services deploy as a cohesive set with a primary home on one host is a *domain* even if its agents/collectors fan out (observability = the precedent). Final cross-cutting set (4): `security`, `backup`, `storage`, `llm-office` + `hardware-gpu`. No sections moved; SSOT direction preserved (§8.5). · `CONVENTIONS.md` §8.4 |
| HD-167 | 2026-08-20 | **Services docs stack refactor — `services.md` as the central stack index** — ✅ **Done.** `services.md` is now the **central stack index** (`role: index`): catalog legend + Docker Networks codes, Domain & Subdomain Plan (public subset + admin-dashboards decision), Service Accessibility/URL→backend SSOT, and a cluster table linking each `services-<x>.md` stack doc. The ~260-line flat catalog was split into **5 new per-domain stack docs** (each owns its rows + full descriptions): `services-media.md` (Jellyfin, Seerr, Immich, *arr + storage/import), `services-downloads.md` (SABnzbd, qBittorrent, gluetun — ingress + VPN), `services-dns.md` (Technitium, Pi-hole), `services-utilities.md` (n8n, signal-cli, PairDrop, Stirling), `services-admin.md` (Forgejo, Renovate, CrowdSec, Metabase, Headscale, Kopia, DB Backup). AI/Office/Matrix/Finance/Traefik/Authentik/Observability rows folded into existing owning docs (already rich SSOTs). **No `services-dashboards.md`** (dissolved: HA→smart-home, Grafana→observability, Homepage+Sunshine→standalone index entries, CrowdSec/Metabase/Forgejo→admin). Shared SSOT facts kept in exactly one doc (networks/subdomains/URL→backend stay in `services.md`); no `*-generated` docs touched; cross-cutting docs (observability etc.) out of scope per HD-171. All 5 new docs registered in `docs/index.md` Document Map + dispatch table. ✅ `validate-all.sh` green (72 docs/84 canonical scanned). · [services.md](docs/services.md), [services-media.md](docs/services-media.md), [services-downloads.md](docs/services-downloads.md), [services-dns.md](docs/services-dns.md), [services-utilities.md](docs/services-utilities.md), [services-admin.md](docs/services-admin.md), [docs/index.md](docs/index.md) |
| HD-170 | 2026-08-20 | **Smart-home docs refactor — `smart-home.md` as the domain index** — ✅ **Done.** `smart-home.md` is now the smart-home **index** (`role: index`; header/Document Map/Related now link all `smart-home-{voice,audio,failover}.md` + `home-assistant-current.md` + `interfaces.md`). **`home-assistant-current.md` stays a sibling detail doc** (live-HAOS snapshot, domain: smart-home) — added to smart-home.md's Document Map + Related, and its `Linked from` updated to `smart-home.md, index.md`. Sub-docs were already aligned (`role: detail`, `Linked from: smart-home.md`). Doc-map annotation updated (`smart-home.md` → Index). No config/IaC moved — docs only, preserved smart-home ↔ interfaces/network-dns/deployment-ansible cross-links. · [smart-home.md](docs/smart-home.md), [home-assistant-current.md](docs/home-assistant-current.md) |
| HD-169 | 2026-08-20 | **Hardware docs refactor — `hardware.md` as the domain index** — ✅ **Done.** `hardware.md` is now the hardware **index** (`role: index`; header/Document Map/Related now link every `hardware-*.md`, incl. the previously-missing `hardware-ups.md` in Related). Sub-docs verified aligned (`role: detail`, `Linked from: hardware.md`); `hardware-phase2.md` stays `status: deferred` (Phase-2 marker is correct). `hardware-gpu.md` is `role: cross-cutting`; the `cross_cutting: true` frontmatter marker belongs to HD-171 (cross-cutting resolution) — not touched here. Doc-map annotation updated (`hardware.md` → Index). · [hardware.md](docs/hardware.md) |
| HD-168 | 2026-08-20 | **Network docs refactor — `network.md` as the domain index** — ✅ **Done.** `network.md` is now the network **index** (`role: index`, header links to every `network-*.md` incl. both rack docs); the `network-*` sub-docs were already `role: detail` with `Linked from: network.md`. **Rack duplication resolved (via HD-172's rename, confirmed here):** the old `rack-connections.md` wiring doc is now `network-rack-generated.md`; hand-authored `network-rack.md` (18U layout, room maps, images) and the generated wiring (per-port cable map) are **complementary** — kept both, cross-linked both ways, no merge/delete needed. Doc map annotation updated (`network.md` → Index). · [network.md](docs/network.md), [network-rack.md](docs/network-rack.md), [network-rack-generated.md](docs/network-rack-generated.md) |
| HD-173 | 2026-08-20 | **Extend `check_doc_map.py` to scan links beyond `docs/index.md`** — ✅ **Done.** The doc-map linter now resolves every `[x](*.md)` target in all canonical repo `.md` (docs/**, root docs README/CONVENTIONS/todo/changelog/deployment-tasks/readme-humans, IaC/**) so renames can never silently break in-doc links; the original document-map coverage check (every docs/ .md must appear in the `## Document Map` block) is preserved. False-positive guards: URLs/anchors/mailto, glob/wildcard link prose, links inside inline-code spans + fenced blocks (the linter's own `[..](..md)` examples), `manual/*` (resolution-only), symlinks + `docs/assets/` excluded; `brainstorming/` + `prompt-*.md` handoffs skipped. **Dead links found + fixed in the same change:** `docs/storage-zfs.md` → `docs/storage.md` ×2 (changelog HD-131/132 rows, link target only), `docs/observability.md` → `observability.md` ×2 (docs/smart-home.md + docs/storage.md — double-path), `../deployment-tasks.md` → `deployment-tasks.md` (todo HD-40A row). ✅ `validate-all.sh` green (red/green verified: unlisted doc + dangling link both fail). **Prerequisite to HD-172 (renames).** · [scripts/check_doc_map.py](scripts/check_doc_map.py) |
| HD-172 | 2026-08-20 | **`-generated` filename suffix for generated docs + name linter** — ✅ **Done.** Renamed 4 machine-produced docs to carry the suffix per CONVENTIONS.md §8.2: `inventory.md` → `services-inventory-generated.md`, `network-addresses.md` → `network-addresses-generated.md`, `subscriptions-table.md` → `subscriptions-table-generated.md`, `rack-connections.md` → `network-rack-generated.md` (resolves the rack-name collision; HD-168 network merge still pending separately). Updated every reference in the same self-contained change: renderers (`render_network_addresses.py`, `render_rack_connections.py`), `render-docs.yml`, `docker_services` role, `check_doc_ips.py` SSOT path, `docs/index.md` Document Map + prose, CONVENTIONS.md, IaC/README + router/README, and active-doc prose (deployment-ansible/preseed/deployment, hardware-nas/oldsrv/ups, network.md/rack/vlans/vpn/dns, services-traefik/services.md, interfaces, subscription, smart-home-failover, deployment-tasks, todo). Template names (`*.md.j2`) unchanged — only outputs renamed. **Added `scripts/check_generated_suffix.py`** wired into `validate-all.sh` (now 7 checkers): fails on a missing/unsuffixed expected generated doc OR a hand-authored doc wrongly carrying `-generated`. Red/green verified (deleting a suffixed doc fails; both restore green). ℹ️ Coordinated with HD-168: the hand-authored `network-rack.md` + generated `network-rack-generated.md` merge/rename belongs to that task. · [scripts/check_generated_suffix.py](scripts/check_generated_suffix.py) |
| HD-163 | 2026-08-20 | **Consolidate render scripts behind one entry (`render_all.py`)** — ✅ **Done.** **Audit first:** none of the render scripts are obsolete — `render_rack_connections.py` is a **different SSOT** (`rack-connections.json`, no Ansible counterpart), `render_network_addresses.py` is the **Windows fallback** (Ansible crashes natively, WinError 87), `gen-htpasswd.py` is a standalone bcrypt util (HD-59); `render-docs.yml` is not a superset. **New `scripts/render_all.py`** — pure-Python umbrella over all 5 generated-doc renders (`network-addresses`, `services-inventory`, `subscriptions-table`, `network-rack`, `rack-layout`) with **no Ansible/WSL/1Password dependency**; **decision B**, the two standalone renderers are imported (authoritative), while the inventory+subscriptions renders (previously Ansible-only hostvars/logic) are implemented inline; `--check` drift mode renders then git-diffs vs index state and restores the tree (no mutation); `render-routeros.yml` (secrets) stays Ansible. **Standardized** the `# Ansible managed` canonical header across all generated docs + CONVENTIONS §8.2 (equal to Ansible's built-in `ansible_managed` so Python+Ansible stay byte-identical). **Backfilled** stale generated docs in the same change (inventory added `onlyoffice-docs` HD-166; network-addresses added HD-13 parking notes; subscriptions rows refreshed). **Docs wired:** `scripts/README.md` (+ index table + render section), `docs/index.md` dispatch, `CONVENTIONS.md` §8.2, `deployment-ansible.md` render note. ✅ `validate-all.sh` **green** (7 checkers). · [scripts/render_all.py](scripts/render_all.py), [scripts/README.md](scripts/README.md), [CONVENTIONS.md](CONVENTIONS.md), [docs/index.md](docs/index.md), [docs/deployment-ansible.md](docs/deployment-ansible.md) |
| HD-151 | 2026-08-19 | **Trim orphan oldsrv/nas datasets post-HD-135** — ✅ **IaC done.** Trimmed `storage_datasets` to the VPS-era set: dropped `nvme/tsdb` + `nvme/docker/postgres` (oldsrv) and `tank/data/immich` / `tank/data/documents` / `bulk/data/immich` / `bulk/data/documents` (nas) from the `storage` role create-set + their sanoid/syncoid entries (incl. the 5-min `documents` tier); **kept** `nvme/docker/immich` (immich-ml reads thumbs local) + `bulk/data/immich-thumbs` (nightly face-thumb push target). Docs aligned in the same change: `storage.md` (tree, properties table, sanoid note, nvme pool tree, capacity), `backup.md`, `services.md`, `hardware-nas.md`, `deployment-compose.md` (marked legacy section stale). The role is create-only → **no destroy on live hosts**. **Closed 2026-08-20:** hosts (oldsrv/nas) are **not live yet** — they will be reinstalled, so no `zfs destroy` step remains; the trimmed datasets simply never get created on the fresh installs. · [storage.md](docs/storage.md) |

> Decisions were moved into their owning docs; the changelog row keeps the decision summary for audit trail. Items marked `*(decision)*` are `Decided` rather than `Implemented`.

