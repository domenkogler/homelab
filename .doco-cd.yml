# Doco-CD deployment configuration
# Real schema: https://doco.cd/latest/Configuration/Deploy-Settings/
# ⚠️ WIP — Doco-CD is NOT activated (HD-02). This is a schema-correct skeleton:
# the concrete deploy path (per-service compose vs auto_discovery) and any
# external_secrets: / SECRET_PROVIDER wiring are finalized when Doco-CD is enabled.
# Ansible still performs deployment until then (docs/deployment.md).

name: homelab
reference: main
working_dir: .
compose_files:
  - docker-compose.yml
