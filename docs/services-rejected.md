---
title: Services — Rejected / Dropped Decision Log
role: log
domain: services
status: active
tags: [services, rejected, decision-log]
---
# Services — Rejected / Dropped

> **Role:** Append-only decision log — services the `services` domain evaluated and declined. Sorted by service name. This log is the per-domain **decision-log SSOT**.
> **Links to:** `services.md`, `CONVENTIONS.md` (§8.3)
> **Linked from:** `index.md`, `services.md`

> ⚠️ **Append-only.** Never edit or reorder an entry after it lands. A changed decision is a **new appended entry**, left alongside the old one — never strike/replace. Each row: `| <service> | <rejected|dropped|superseded> | <date> | <why, 1–2 lines + evidence link> |`.
> ⚠️ **Evidence = the owning doc + this decision log.** The link below each row points to where the decision is recorded (SSOT). Dates are the decision dates in the owning doc / git-attribution dates (advisory).

## Decisions

| Service | Status | Date | Why |
|---------|--------|------|-----|
| AnythingLLM | dropped | 2026-08-17 | Replaced by the MS Office MCP path (HD-108): Open WebUI is the family web AI layer, so AnythingLLM is removed from the web UI. · [services-ai.md](services-ai.md) |
| Metabase LDAP auth (HD-243) | rejected | 2026-09-09 | **Owner decision (2026-09-09):** Metabase LDAP/SSO is **rejected** — the owner is the **sole Metabase user**, so the revisit trigger ("second regular human user needs Metabase access") will never fire. Metabase stays behind Forward-Auth (the real SSO) + local admin-only. Note: Metabase OSS LDAP would not be real SSO anyway (second login form, no MFA passthrough). · [services-admin.md](services-admin.md) Metabase section |
| Matrix bridges — WhatsApp/Messenger/Signal (HD-48) | rejected | 2026-09-09 | **Owner decision (2026-09-09):** requested-only Matrix bridges are **rejected** — owner is the sole user, native Signal already works, and Matrix is not even federated yet (HD-46/47). Every bridge risks a real external account (ban/isolation). Re-decide cheaply if WhatsApp-in-Matrix is ever wanted. · [services-matrix.md](services-matrix.md) §Bridges — Deferred
| Ghostfolio | rejected | 2026-08-14 | Portfolio tracking is covered by Actual's tracking account (invested vs current value); XIRR/benchmarks/dividend calendar add a full service + Postgres + maintenance for no need. · [services-finance.md](services-finance.md) |
| GoCardless (Nordigen) | superseded | 2026-08-14 | Free personal sign-ups discontinued; replaced by Enable Banking (native Actual sync, account already held, UniCredit SI AISP confirmed). · [services-finance.md](services-finance.md) |
| LocPilot | superseded | 2026-08-17 | Superseded by the Office MCP path for the AI office stack; kept only as the Windows-Word inline helper, no longer a whole-family UI. · [services-ai.md](services-ai.md) |
| Pangolin | dropped | 2026-08-01 | Removed as reverse-proxy layer — Traefik handles all reverse proxy + auto-SSL (single edge). · [services.md](services.md) |
| Edge model — Option A: "one public + one internal all-app edge" | decided → **superseded** | 2026-09-04 → **2026-09-09** | **HD-331 (owner + AI, 2026-09-04):** long-term reverse-proxy architecture = **one PUBLIC edge (WAN-only, public apps) + one INTERNAL all-app edge (serves EVERY app, public + internal) reached over Headscale tailnet AND WireGuard S2S**. The internal all-app edge is implemented as the growth of the existing `traefik-tailnet` VPS instance (file-provider routes; no docker-provider to avoid router conflicts) + a second listener on the `wg-s2s` VPS side + tailnet/WG ACL allow. The public edge stays Docker-labels + single ACME issuer. Same-zone split-horizon DNS resolves the same names everywhere; `traefik-ha` remains the physical HA-VIP failover edge (not the internal all-app edge). **→ SUPERSEDED 2026-09-09 by HD-349** (owner + AI): the VPS-only internal edge leaves **WAN-out = all internal apps dead on the LAN** (every internal app's DNS resolves to the VPS public IP, yet all backends live on home hosts). The new model adds a **home-LAN internal all-app edge on oldsrv** (`traefik-internal`, VIP-bound, mirrored on the Pi) so `*.kogler.si` stays reachable when WAN/VPS is down (LAN + tailnet still work when the VPS is up; the VPS `traefik-tailnet` stays as the tailnet/WAN path). Decision + topology SSOT: [services-traefik.md](services-traefik.md) §Edge model (re-decided 2026-09-09). Alternatives declined in the original decision — (B) home-host internal edge as base: better home latency but needs router hairpin + oldsrv live + complicates certs/one-instance; (C) keep 3 edges as-is: leaves `ha` broken with Tailscale on — (B)'s objections are now addressed by the VIP-binding + split-horizon design in HD-349. Implementation HDs (HD-331): HD-332 (catalog `public:` flag + internal-edge growth), HD-333 (WG reach + ACL), HD-334 (per-device DNS via Pi-first VLAN-10 + seed `vpn`/`home`/`dns`). Owning doc: [services-traefik.md](services-traefik.md) §Edge model; decision log cross-ref: [network-dns.md](network-dns.md) (DNS unchanged), [todo.md](../todo.md) HD-331. |
| Edge model — home-LAN internal all-app edge (HD-349) | decided | 2026-09-09 | **HD-349 (owner + AI, 2026-09-09):** supersedes the HD-331 VPS-only-internal-edge model. Topology per [services-traefik.md](services-traefik.md) §Edge model: **VPS `traefik-tailnet` = tailnet/WAN path** (unchanged), **new home-LAN internal all-app edge on oldsrv (`traefik-internal`, VIP-bound :443)** = LAN-resilience path (all internal apps + `ha` when it holds the VIP), **Pi `traefik-ha` = ha/dns-pi survival when oldsrv is down** (unchanged). LAN DNS points internal app names at the **home VIP** (which rides the active home host); tailnet DNS stays on the VPS. Both `ha` edges carry `ha` so the Companion app works over any live path. Scaffolded by the live 2026-09-09 failover drill: `ha.kogler.si` went dark on Pi-down because no home edge served it. Implemented via HD-350 (home edge IaC) + HD-351 (failover wiring) + HD-352 (DNS re-point). Owning doc: [services-traefik.md](services-traefik.md) §Edge model. |
| TradeSight | rejected | 2026-08-14 | Unnecessary — Trade Republic offers a native structured CSV/Excel export, so no PDF-visual Ollama converter is needed. · [services-finance.md](services-finance.md) |
| LiteLLM → spark: `os.environ/` key reference in a DB model row | rejected (technically impossible) | 2026-09-17 | **HD-382 (2026-09-17):** with `STORE_MODEL_IN_DB=true`, `api_key: os.environ/SPARK_LLM_API` inside `litellm_params` is forwarded to the upstream **literally** — LiteLLM v1.83.10 does not expand `os.environ/` in DB-stored model params (proved twice: through the proxy AND in-process `litellm.completion`, both engine 401; the same call with the resolved value = 200). Chosen instead: deliver the 1P `spark-llm_api` value as the container env the provider prefix already reads (`openai/…` → `OPENAI_API_KEY`) and leave `api_key` **unset** in the row — keeps the bearer out of the Kopia-backed Postgres (CONVENTIONS §secrets), which was the owner's actual requirement. Rejected alternative: paste the literal into the Admin UI (works, but the key then lives in two DB backups). ⚠ Accepted caveat: a future bare `openai/…` entry with no `api_key` silently inherits this key. · [deployment-secrets.md](deployment-secrets.md), [services-ai.md](services-ai.md) §3 |
| `spark/*` granted to the dev-harness scoped keys (side-effect of registering the model) | NOT decided (parked) | 2026-09-17 | **HD-382/HD-384 (2026-09-17):** `spark/qwen3.8-flash-next` is now a DB entry in both LiteLLMs, but `litellm_scoped_keys` stay `ollama/*,openrouter/*` on both instances, so dsh/pi-dev/OWUI cannot reach spark through the gateway. Deliberately not granted as a side-effect: `group_vars/home_servers.yml` already states spark access for the dev harnesses is "a later decision, NOT decided here", and the model-catalog-sync doctrine requires allowlist + key cleanup to move in the SAME change. Gate = owner call (HD-384), with the HD-376 parallel-lane rule in mind (one shared ~262k KV pool; incident #6 = a single 162k-token session took 56 % of it). · [services-ai.md](services-ai.md) §4, [hardware-spark.md](hardware-spark.md) §Model-catalog sync |
| LiteLLM tailnet reach via `tailnet-apps` overlay (HD-372) | accepted | 2026-09-15 | **HD-372 tailnet reach via `tailnet-apps` overlay (HD-372) | accepted | 2026-09-15 |  tailnet reach via `tailnet-apps` overlay (HD-372) | accepted | 2026-09-15 | **HD-372 (2026-09-15, litellm.kogler.si 502):** see the network-rejected.md row — the VPS litellm joins the `tailnet-apps` app→edge overlay (NOT `traefik-public`) so the tailnet edge's `litellm-backend` route resolves it via Docker DNS. Keeps the key-holding spine off the flat public bridge (HD-59). · [services-ai.md](services-ai.md), [network-vpn.md](network-vpn.md) |
| LiteLLM → spark name resolution via `extra_hosts` glue (HD-382) | accepted | 2026-09-17 | **HD-382 (2026-09-17):** neither LiteLLM container could resolve `llm.kogler.si` (VPS + oldsrv host resolv.conf are both public-only — oldsrv by WAN-out-survival design — and `/etc/hosts` is NOT inherited by containers). Accepted = `extra_hosts` mapping the name to `spark_home_ip` on the two services (SSOT-derived; `openclaw` precedent). Rejected: seed the name on the VPS Technitium primary (breaks the LAN-only-names invariant + fails `check_dns_seed_drift.py`); point container `dns:` at the home Technitium (one leg only + fights the WAN-out-survival intent); IP-based `api_base` (no IP SAN → TLS verify fails; the spark edge serves the real Let's Encrypt wildcard, so connecting BY NAME validates). · [services-ai.md](services-ai.md) §3, [network-dns.md](network-dns.md) §Per-Instance Split-Horizon |

| `antheas/spark_hwmon` (DGX Spark SPBM hwmon DKMS driver) | deferred (owner risk call) | 2026-09-16 | **HD-378:** the only source of the GB10 per-rail power split (CPU-P / CPU-E / SoC / DRAM / system, 14 channels + 8 temp zones) — NVIDIA itself says there is "no method to monitor CPU power" on Spark, so nothing else can. **Not adopted:** an out-of-tree DKMS kernel module, self-described upstream as "vibe coded", whose own README requires a `fwupdmgr` BIOS update because older firmware misreports the CPU channels, and with no license file — loaded on the host with the HD-375 OOM history that is the owner's risk decision, not a monitoring one. Its `gpu` channel also only matches `nvidia-smi`, which DCGM already delivers. **Wiring is pre-made:** it lands in `/sys/class/hwmon`, and the Alloy `hwmon` collector enabled in HD-378 picks it up with no further change. · [observability.md](observability.md) §Host sensors and disk I/O |
| `zy84338719/dgx-spark-exporter` (Go, `:9876/metrics`) | rejected | 2026-09-16 | **HD-378:** ~50 `dgx_spark_*` metrics that duplicate the Alloy `prometheus.exporter.unix` collector set ALREADY running on every host (cpu/mem/disk-IO/filesystem/network/load/filefd/swap), with the GPU trio taken from `nvidia-smi` — which DCGM supplies with more fields — and no VRAM and no profiling either (unified memory + DCGM profiling unsupported on GB10). Adds a 0★/no-release always-on daemon for zero new signal. Its unique 3 power channels are the `spark_hwmon` surface above. · [observability.md](observability.md) §Host sensors and disk I/O |
| `dgxtop` (GigCoder-ai Python / DennySORA Rust NVML TUIs) | rejected | 2026-09-16 | **HD-378:** both are terminal monitors with **no metrics endpoint** — nothing for Grafana/VM to consume. The Rust one is a genuinely good `ssh spark` spot-check tool, and both advertise VRAM/BAR1 panels that return N/A on the GB10 (unified memory). Not infrastructure. · [observability.md](observability.md) §Host sensors and disk I/O |
| `text-embeddings-inference` (`cpu-1.9.4`) as the **primary** rerank engine | rejected (kept as the fallback rung) | 2026-09-18 | **HD-385/#27:** measured **5.3 s** for a 20-doc Slovenian rerank at **~790 % CPU** on the i7-7700K (0.34–0.50 s on the RX 7600), plus a warmup memory landmine — default flags reached **10.8 GiB RSS** and were OOMKilled under a 6 GiB cap; it needs `--max-batch-tokens 2048 --max-client-batch-size 32` (client-batch 8 ⇒ HTTP 422 on real RAG requests) and `OMP_NUM_THREADS` must stay **unset** (4 or 8 → never ready in 240–400 s). · [services-ai-bench.md](services-ai-bench.md) §5 |
| **HD 630 iGPU** (ANV/Vulkan) as a rerank/embed/STT device | rejected | 2026-09-18 | **HD-391:** measured **5.8 / 11.4 / 22.8 s** for 5/10/20-doc reranks — **slower than the CPU path** (~46× slower than the dGPU), holds ~798 MiB of **host RAM**, advertises "35911 MiB free" because it *is* host RAM, needs the **frozen `legacy1`** compute-runtime packages (Gen 8/9/11 dropped from regular `intel/compute-runtime` since 24.35.30872.22), and the same GPU is the family **Xorg primary + Jellyfin QSV** device. · [services-ai-bench.md](services-ai-bench.md) §4 |
| `whisper.cpp` **GGML_HIP / ROCm** build for STT | rejected | 2026-09-18 | **HD-391 (re-decides #25(c)):** **no published ROCm artifact exists** — the upstream CI matrix publishes `main, -musa, -intel, -cuda, -vulkan, -arm64, -vulkan-arm64` and Docker Hub `ggmlorg/whisper.cpp` is 404, so the HIP path is a **self-build with no Renovate trail** (CONVENTIONS §7). Vulkan uses native RADV on RDNA3 with no ROCm userspace and no ISA override. · [services-ai-bench.md](services-ai-bench.md) §2 |
| **FP16** GGUF for the pinned embed/rerank legs | rejected (**Q8_0** chosen) | 2026-09-18 | **HD-391:** measured on the box — FP16 buys cos **0.99996 vs 0.99960** against the live Ollama vectors for **+269 MiB VRAM per leg** and **+18 %** embed latency (19 vs 16 ms), and changes **no visible rerank ranking** (top-5 identical, mean \|Δscore\| 0.031, only deep-tail ties permute). · [services-ai-bench.md](services-ai-bench.md) §3c |
| `kftof/bge-reranker-v2-m3-onnx-int8-avx2` (ORT INT8 on CPU) as primary | rejected | 2026-09-18 | **HD-391:** the INT8 path gave only **~1.8×** over candle fp32 — the 7700K has AVX2/FMA but **no AVX-512/VNNI**, so the quantization can't pay for itself; and it is still the same rejected CPU placement. Also: its conventional layout is `onnx/model*.onnx`, only the root `model.onnx` loads. · [services-ai-bench.md](services-ai-bench.md) §3/§5 |
| `Theroxenes/local-reranker-rocm` (TEI ROCm fork) | rejected | 2026-09-18 | **HD-385 (vetted 2026-09-17, mooted by #27):** MIT but **0 stars / single maintainer / no release trail**, and it exists to solve a GPU placement the tier no longer wants — rerank now runs on the published `llama.cpp:server-vulkan` image instead. · [services-ai.md](services-ai.md) §9c |

| Vision-LLM leg on the oldsrv RX 7600 (Qwen3-VL 2B/4B on the pinned tier's card) | dropped (deferred) | 2026-09-20 | **Decision #28 (2026-09-20):** the card stays vision-free. Not a perf judgement — the ledger is closed: pinned 2.4 GiB + VL 2B/4B 3–4.5 GiB + immich-ML 3–5 GiB = **8.4–11.9 GiB > 8 GiB**, `pause` does not free VRAM, and there is no arbiter for two on-demand consumers (graceful-failure unknown on `gfx1102`). Vision for judgment moved to the workstation ([hardware-workstation.md](hardware-workstation.md)). **Revisit only after** both gates are measured: Qwen3-VL `mmproj` runs on Vulkan (not CPU fallback) and `--sleep-idle-seconds` verifiably returns VRAM. · [hardware-gpu.md](hardware-gpu.md) §Vision-LLM leg, [services-ai.md](services-ai.md) §9 #28 |
> **Not a services-domain decision:** hypervisor / deploy / storage / network / smart-home UI rejections (Proxmox, Doco-CD, iDrive, MinIO, TileBoard, netplan…) live in their own `<domain>-rejected.md` files — see [`deployment-rejected.md`](deployment-rejected.md), [`storage-rejected.md`](storage-rejected.md), [`network-rejected.md`](network-rejected.md), [`smart-home-rejected.md`](smart-home-rejected.md).
> **SSOT note:** this log is the decision-log SSOT for the services domain.