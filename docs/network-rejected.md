---
title: Network — Rejected / Dropped Decision Log
role: log
domain: network
status: active
tags: [network, rejected, decision-log]
---
# Network — Rejected / Dropped

> **Role:** Append-only decision log — network tooling options the homelab evaluated and declined. Sorted by tool name. This log is the per-domain **decision-log SSOT**.
> **Links to:** `network.md`
> **Linked from:** `index.md`, `network.md`

> ⚠️ **Append-only.** Never edit or reorder an entry after it lands. A changed decision is a new appended entry (do not strike/replace). Each row: `| <tool> | <rejected|dropped|superseded> | <date> | <why + evidence link> |`.
> ⚠️ **Evidence = the owning doc + this decision log.** Dates are the decision dates in the owning doc / git-attribution dates (advisory).

## Decisions

| Tool | Status | Date | Why |
|------|--------|------|-----|
| UPS web-UI firewall rule (HD-09) | **void / rejected** | 2026-09-22 | The backlog asked for 80/443 Home→Mgmt scoped to the UPS management address. It is **not implementable and not wanted**: the UPS management NIC left the Mgmt VLAN with HD-338 and sits on the no-WAN IoT segment, and the plan of record is that its **web UI and Modbus TCP stay unused** with NUT over USB/serial only ([hardware-ups.md](hardware-ups.md)). Measured while closing it: the rule never existed in IaC or on the device, although both the ledger and the backlog row asserted "IaC done". Do not re-add. · [network-vlans.md](network-vlans.md) §Inter-VLAN · [hardware-ups.md](hardware-ups.md) |
| internal AAAA records (HD-36) | rejected | 2026-09-09 | **Owner decision (2026-09-09):** internal IPv6 AAAA records are **rejected for now** — they need stable per-host global addressing **and** a full mirror of the inter-VLAN isolation in the IPv6 firewall (IPv6 is currently WAN-only). No current IPv6 need; everything is IPv4-first internally. Revisit only if a concrete internal-IPv6 requirement appears. · [network-dns.md](network-dns.md) §A/AAAA policy |
| mDNS reflection (RouterOS mDNS repeater / Avahi across VLANs) | rejected | 2026-08-23 | Cross-VLAN mDNS re-couples the broadcast domains the VLAN plan exists to separate; no integration needs it — cloud appliances (LG ThinQ / Bosch Home Connect) pair via vendor apps + cloud APIs with zero cross-VLAN rules ([smart-home.md](smart-home.md) §Cloud Appliances), local devices (Shelly Gen1 CoAP, KNX gateway, ESPHome) are added by static host/IP with DHCP reservations. Decision context HD-228. · [smart-home.md](smart-home.md), [network-vlans.md](network-vlans.md) |
| DNS primary placement (oldsrv vs VPS) | superseded | 2026-09-03 | **HD-317 / HD-299 executed (2026-09-03):** the DNS primary moves from **oldsrv to the VPS** — 3-instance DNS HA (**VPS primary / oldsrv secondary / Pi tertiary**, separate physical hosts). Rationale (owner + AI, 2026-09-03): the VPS is always-on + WAN-reachable; a DNS primary there keeps LAN + tailnet resolution independent of home infrastructure (oldsrv was offline at decision time; the plain `*.kogler.si` tailnet names resolve via the VPS primary). **Option A (supervisor decision):** single resolver address `dns_primary_ip` = **VPS public IP 159.195.111.66** (LAN hairpins via the HD-309 WAN egress; tailnet resolves it too; survives a WG-tunnel drop). Abuse surface (public UDP 53) is mitigated by strict **VPS nftables source-allow** (tailnet headscale CGNAT + a `dns-allow-home` set refreshed by an Ansible-managed systemd timer that re-resolves the home DDNS `s.kogler.si` every 5 min) — the home-WAN source is DDNS, so drift is self-healing. Alternatives declined: **Option B** (LAN uses the WG S2S bind `wg_s2s_vps.ip:53` ([network-addresses-generated.md](network-addresses-generated.md) §Infrastructure networks) + tailnet uses the public IP) — split resolver addresses + dies with the tunnel; **Option C** (keep primary on home) — contradicts the goal. oldsrv/Pi keep the home-LAN failure-domain split (WAN-out / VPS-down). · [network-dns.md](network-dns.md), [todo.md](../todo.md) HD-317 |
| per-purpose SSID per VLAN (5 SSIDs: Kogler/IOT/IOT-WAN/guest/kids) | superseded | 2026-09-03 | **HD-312 decision (2026-09-03):** the per-purpose-SSID wireless model is superseded by a **3-SSID consolidation — Kogler (family + kids, VLAN 10), IOT (VLAN 20 + per-device WAN via MAC-based list, client-isolation), Kogler guest (VLAN 30, 5GHz, client-isolation)**. Rationale (owner + AI, 2026-09-03): the 2.4GHz band had 5 SSIDs (airtime waste — an AI audit of the live state flagged this); every device has an all-static MAC in `network_static_hosts`, so **per-MAC control** (CAPsMAN `access-list` vlan-id + firewall MAC-address-lists) is strictly better than SSID-per-purpose. Merges: IoT + IoT-WAN → one IOT SSID (cloud-IoT = permanent `iot-wan-allow` MAC list; a firmware window = temporary list add/remove — enables the n8n monthly automation); Kids → into Kogler (same network, family sees each other; kids controls become firewall MAC-address-list, not VLAN 40); Guest stays a separate SSID (unknown guests can't be MAC-mapped). `access-list` on wifi-qcom-ac 7.24.1 supports per-MAC `vlan-id` (live-verified) but cannot move a client to another SSID; requires the CAP bridge access-port pvid model (already in place from HD-229/310). n8n firmware automation (owner idea) toggles the temporary WAN via the router API `iot-wan-allow` list over the wg-s2s tunnel. · [network-vlans.md](network-vlans.md) §3-SSID, [todo.md](../todo.md) HD-312 |
| netplan | rejected | 2026-08-16 | Host network config-manager — Ubuntu-desktop default + extra python3/libnetplan translation layer; rejected in favor of native `systemd-networkd`. HD-56. · [network.md](network.md) |
| systemd-networkd (on RPi Pi) | superseded | 2026-09-01 | The 2026-08-16 netplan-rejection recorded `systemd-networkd` as the repo's config-manager, but the live Pi (Raspberry Pi OS / Debian trixie) ships **NetworkManager** (cloud-init `renderers/activators: netplan, network-manager`; NM 1.52.1 active; no systemd-networkd). Implementing the networkd unit as written would disable/fight NM and risk severing the remote session. **Pi host-side dual-home is therefore done via two NetworkManager keyfiles** (HD-311 refined 2026-09-02): `pi-eth0.nmconnection` = untagged Home on the parent (`ansible_host`, default via Home — single gateway, no `never-default`; DNS via `bootstrap_dns_servers`) + `pi-mgmt.nmconnection` = tagged Mgmt on the `eth0.99` sub-interface (`mgmt_ip`, never-default, route via the tagged leg). The Mgmt IP lives ONLY on the tagged sub-interface — the untagged parent cannot reach the Mgmt plane (ether10 pvid=10), and its stale connected route hijacked `mgmt_subnet` lookups away from the tagged leg (`router99` 'No route to host' live 2026-09-02, fixed + verified). desktop/VPS hosts keep systemd-networkd where that is their manager. HD-307/HD-311. · [network-vlans.md](network-vlans.md), [roles/network](…/IaC/ansible/roles/network/) |
| systemd-networkd (nas/oldsrv netd units) | accepted | 2026-09-02 | **nas + oldsrv (desktop/VPS class) use systemd-networkd** for static dual-home: physical `.network` (untagged Home 10 + default route), `.netdev` VLAN-99 tagged sub-interface, and its `.network` (Mgmt IP, connected-only). Rendered by the `network` role (HD-311). Pi remains the NetworkManager exception (2026-09-01 row). · [network-vlans.md](network-vlans.md), [roles/network](…/IaC/ansible/roles/network/) |
| CAPsMAN per-MAC vlan-id access-list (HD-312 2b) | rejected | 2026-09-04 | **Not a capability of modern wifi-qcom-ac.** The HD-312 3-SSID rationale (2026-09-03) claimed `access-list` supports per-MAC vlan-id override (live-verified) — that capability belongs to the **legacy** `/interface wireless access-list` (`vlan-mode=use-tag` + `vlan-id` per rule). The **modern wifi** stack (`/interface wifi access-list`) cannot set a per-client vlan: the official MikroTik WiFi wiki (`vlan-id` property) states `802.11ac chipsets do not support this type of VLAN tagging, but they can be configured as VLAN access ports in bridge settings` — i.e. wifi-qcom-ac (the whole fleet, HD-232) does VLAN assignment only via the **CAP bridge access-port pvid model** (already live from HD-229/310), never a per-MAC override. Per-client VLAN isolation on wifi-qcom-ac is therefore not implementable; the per-device control that DOES exist is the **firewall MAC-address-list / `src-mac-address` forward-accept** model (iot-wan-allow, kids-* — HD-312 phase 3, live). The empty `routeros_capsman_access_list` SSOT/rsc scaffold drafted 2026-09-04 was reverted (would ship an inoperative `vlan-id` action). · [network-vlans.md](network-vlans.md) §3-SSID, [todo.md](../todo.md) HD-312 |
| n8n firmware workflow — temp `iot-wan-allow` toggles (HD-312(4)) | superseded | 2026-09-09 | **Owner decision (2026-09-09):** the temp-toggle n8n firmware automation is **not needed** — the per-device `wan_allow: true` flag (HD-325, live 2026-09-08) already gives the cloud-IoT appliances (LG/Bosch/HAP) **permanent WAN**, and firmware updates happen over that standing egress. The dedicated `n8n` read-only RouterOS API user (`mikrotik-n8n_api`, provisioned 2026-09-08) stays provisioned for future admin uses, but no flow authoring will be done for temporary WAN toggles. HD-312(4) closed/superseded. · [network-vlans.md](network-vlans.md) §3-SSID, [todo.md](../todo.md) HD-312 |
| VLAN 21 (IoT-Internet) as a Wi-Fi-reachable network | removed | 2026-09-04 | **HD-325 (2026-09-04):** the dedicated **VLAN 21 / IoT-Internet** is DELETED. Rationale: it was the SSOT-prescribed network for the cloud-IoT devices (2×LG, 3×Bosch, HAP) but those Wi-Fi appliances join the **IOT SSID (VLAN 20)** — and wifi-qcom-ac cannot do per-client VLAN tagging (see the HD-312 2b row above), so **VLAN 21 was never actually reachable by the clients SSOT assigned to it** (only a phantom DHCP-21 server + reservations existed; the HAP is wired to router ether9). The live per-MAC firewall model (HD-312 phase 3) already gave these devices WAN; HD-325 made SSOT match reality: `network_vlans` row 21 removed, the six cloud-IoT rows moved to `vlan: 20` with new `.20.20-.25` reserved IPs + an explicit **`wan_allow: true`** flag, and all VLAN-21 consumers (router/switch tasks + `rb4011_converge`/`rb4011_dns_resolver_delta` rsc) removed. Net effect: same cloud behavior, no phantom VLAN, SSOT = live. · [network-vlans.md](network-vlans.md), [todo.md](../todo.md) HD-325 |
| Tailnet DNS servers — MagicDNS loop first (headscale `dns.nameservers`) | accepted | 2026-09-15 | **HD-371 (2026-09-15, live 2026-09-15 laptop-on-hotspot):** headscale's `dns.nameservers` now lists **`100.100.100.100` (headscale MagicDNS loop) FIRST**, then the Technitium VPS primary + oldsrv + Pi. Rationale: a tailnet client resolves `*.kogler.si` + `*.ts.kogler.si` **locally via MagicDNS on any network** (hotspot/travel), instead of depending on the Technitium VPS primary (reachable only from home-WAN/tailnet-CGNAT, source-allow HD-299). The 2026-09-15 live case: laptop on hotspot + Tailscale connected → `stats`/`llm`/`litellm` `ERR_NAME_NOT_RESOLVED` (fell to system resolver) while the phone (client-side MagicDNS extra_records) worked. Alternatives: split-DNS routes / client `--accept-dns` toggles (Windows falls back to system DNS on an unreachable pushed nameserver) — a client-side stopgap; the headscale change is the durable fix. · [network-vpn.md](network-vpn.md), [network-dns.md](network-dns.md), [todo.md](../todo.md) HD-371 |
| LiteLLM → tailnet edge via `tailnet-apps` overlay (not `traefik-public`) | accepted | 2026-09-15 | **HD-372 (2026-09-15, litellm.kogler.si 502):** the VPS litellm compose joins the **`tailnet-apps`** app→edge overlay (the dedicated bridge the `traefik-tailnet` edge already shares with dsh/pi-dev, HD-296/HD-268c) so the edge's file-provider `litellm-backend → http://litellm:4000` resolves the spine via Docker DNS. NOT the flat `traefik-public` bridge: deployment-compose.md HD-59 keeps the key-holding spine isolated from the public-route apps. Alternative (edge joins `services-internal`) declined: would let the edge reach every app on the flat internal bridge (defense-in-depth violation, the ISO-from-services-internal stance). · [services-ai.md](services-ai.md), [network-vpn.md](network-vpn.md), [todo.md](../todo.md) HD-372 |
| Mgmt VLAN 99 reachable from the VPS site-to-site tunnel (HD-398 option B — widen `wg_s2s_vps.allowed_ips` + the router `available-from`) | rejected | 2026-09-19 | **Owner decision A on HD-398 (2026-09-19): the seal stays — VLAN 99 admin is a same-site / physical-presence plane.** Measured twice by two sessions the same day, from the VPS over `wg-s2s`: ICMP to the VLAN-99 gateway answers, TCP/22 to the router's own mgmt address, the Pi's and oldsrv's mgmt legs all die in banner exchange, while a Home-VLAN host answers and the tunnel is up. Cause is TWO deliberate boundaries, not a fault: (1) `wg_s2s_vps.allowed_ips` (`IaC/ansible/group_vars/all/main.yml`) is an explicit /32 **least-access** list (HD-155) naming the five VLAN-10 node addresses + router + switch mgmt + the `wg-vps-services` range — no other mgmt address — and systemd-networkd routes only what AllowedIPs name (`ip route get <oldsrv mgmt>` on the VPS = `via <default gw> dev eth0`, i.e. out to the internet); (2) `/ip service set ssh … available-from={{ router_services_available_from }}` is **the VLAN-99 subnet only** (HD-310 admin-plane scoping), so even a routed mgmt address refuses a tunnel-sourced SSH. **Option B declined** because it spends both boundaries to fix a symptom the Home leg already solves: a compromised VPS would gain admin-plane reach toward router/switch/oldsrv. Rejected alternative **C** (join LAN hosts to the tailnet) was already decided against 2026-09-10 (§Tailnet boundary). **Consequence implemented in the same change (HD-397):** off-LAN admin = the **Home leg (VLAN 10)** for every node — `ansible_ssh_common_args: "-o ProxyJump=vps"` now carried in `group_vars/{home_servers,storage,raspberry_pi,spark}.yml`, `host_vars/oldsrv.kogler.si.yml` `ansible_host` = the Home address, and the laptop alias contract documented (mgmt aliases stay jump-less and are labelled on-site-only; `pi99` lost a `ProxyJump` that could never work). Evidence: 2026-09-19 hotspot pass in [network-vpn.md](network-vpn.md) §Reaching LAN nodes when away; netdev mtime 2026-09-03 rules out a same-day network change; the "it worked earlier today" datum was `Host oldsrv` gaining a `ProxyJump` on the laptop (workstation-side). · [network-vpn.md](network-vpn.md), [todo.md](../todo.md) HD-397/398 |

| `ether1` `/32` host route on the Comtrend modem LAN | superseded | 2026-09-02 | A `/32` gives the router **no connected route** to the modem subnet, so forwarded laptop traffic to the modem web UI hits the PPPoE default instead of `ether1`. The model is a **static `/24`** on the modem's LAN + the srcnat masquerade. · [network.md](network.md) §Comtrend modem management path |
| CAPsMAN **manager datapath `vlan-id`** for per-SSID VLANs | rejected (not a capability) | 2026-09-02 | wifi-qcom-ac CAPs reject it (`does not support assigning vlans`). Per-SSID VLAN rides the CAP's **bridge** instead: pvid on the provisioned slave + tagged `ether1` uplink. · [network-vlans.md](network-vlans.md) §CAPsMAN, [network-ops.md](network-ops.md) §CAPsMAN contract |
| DFS channels on the CAPsMAN 5GHz band / one shared master config for both bands | rejected | 2026-09-03 | Auto-selected DFS (5500/5580) breaks association for many phones (esp. Samsung) → 5GHz is pinned to **channel 36 (5180, non-DFS)**. The pin lives on a **per-band** master config (`cfg-kogler-5`) because a shared config with the pin disturbed the 2.4GHz IOT slaves. · [network-vlans.md](network-vlans.md) §CAPsMAN |
| Temporary **Home→Mgmt forward** as the laptop's mgmt-plane access path | superseded | 2026-09-02 | Reverted. The Mgmt plane is reached by **tagged VLAN 99** on the station (Windows Mgmt99 vNIC) — not by widening Home→Mgmt, which would break the defense-in-depth rule that Home never reaches core infra by default. · [network-vlans.md](network-vlans.md) §Port model |
| "Mgmt-access + single-VLAN" port model | superseded | 2026-09-02 | Superseded by **untagged = primary/access VLAN, tagged = secondary/admin (Mgmt 99)** on the same port (dual-home). It changed L2 membership only — every device kept its `10.10.x`/`10.10.99.x` reservation. It also cost a dead Pi Home leg (HD-307/308) before the model was settled. · [network-vlans.md](network-vlans.md) §Port model |
| WSL bridged Mgmt99 route-through (`wsl-vlan-trunk.ps1` + static `10-eth0.network`) as the default | superseded | 2026-09-07 | Now an **opt-in `-EnableMgmt99` extra for on-site work only**. Durable WSL state = **NAT + auto-generated resolv.conf** (`scripts/wsl-nat-resolv.ps1` + `.wslconfig networkingMode=Nat`), which keeps Debian working on wired/WiFi/hotspot alike. · [network-vlans.md](network-vlans.md) §Port model, [network-vpn.md](network-vpn.md) §alias contract |
| Laptop → Mgmt plane via **ProxyJump `pi`** | superseded | 2026-09-08 | The laptop's own Windows Mgmt99 vNIC reaches `.99.x` directly on-site, so the hop aliases were removed. Off-LAN admin is a different plane entirely (Home leg via the VPS jump, HD-397/398 A). · [network-vpn.md](network-vpn.md) §The laptop alias contract |
| Home→IoT new-connection gating via **`trusted-admin`** | superseded | 2026-09-04 | Owner decision narrowed it to **`trusted-ha`** (`oldsrv` + `ha-vip`) — **`nas` excluded**. Applies to the KNX/Shelly exceptions and the converge template alike; a template still emitting `trusted-admin` silently re-broadens the rule. · [network-vlans.md](network-vlans.md) §Inter-VLAN firewall |
| Full `router.yml` **converge** for mgmt-plane-sensitive changes | rejected | 2026-09-03 | Two live mgmt-plane outages traced to re-asserting the bridge: VLAN-99 tagged memberships went missing / flipped to untagged, and because `available-from` is Mgmt-only the router became unreachable from every mgmt client at once. Use **surgical deltas** + keep `rb4011_pi_delta.rsc.j2` as the recovery. · [network-ops.md](network-ops.md) §Mgmt-plane caution |
| Technitium `:53` exposure gated by the **nftables INPUT source-allow** or the app's **`recursionNetworkACL`** alone | rejected | 2026-09-08 | Neither is the control: Docker's published-port `53:53` DNAT happens in PREROUTING and is then **FORWARDED** to the container bridge, so it never traverses INPUT, and the ACL did not enforce from an external source (open resolver confirmed live). The authoritative gate is the **FORWARD-chain source restriction** of the published port. · [network-dns.md](network-dns.md) §Recursion + exposure controls |
| Rename the `converge` rsc tier to `full` | rejected | 2026-09-01 | Churn across ~25 files + tooling for no semantic gain; the **concept** (converge = the full idempotent apply) is documented instead. · [network-ops.md](network-ops.md) §The three tiers |
| LAN unknowns `deblab` (`00:15:5D:01:67:1E`) / `truenas` (`92:47:15:04:EB:49`) | disposed | 2026-08-24 | Owner confirmed both were VMs (Hyper-V NIC + a locally-administered MAC never owned); the VMs are deleted, so the MACs left the inventory — kept here so a future lease scan that mentions them resolves to a decision instead of a re-investigation. · [network-migration-inventory.md](network-migration-inventory.md) |
| **Single shared WireGuard keypair** on both ends of `wg-s2s` | rejected (cannot work) | 2026-09-02 | One key on both sides makes the interface pubkey and its own peer pubkey identical — **WireGuard refuses to handshake with your own key**, silently. Design = distinct per-side keypairs (`wg_password` / `wg_password_vps`), each peer = the other side's pubkey. · [network-vpn.md](network-vpn.md) §Layer 1 |
| Port-based tailnet sidecar skeleton (a `tailscale-sidecar` dir with fixed `:8080–8085` ports) | superseded | 2026-08-28 | Replaced by the `traefik-tailnet` edge (consumer-mode Traefik + a userspace sidecar sharing its netns, `tailscale serve --tcp=443`) → clean subdomain URLs on 443 with wildcard certs instead of port numbers. · [network-vpn.md](network-vpn.md) §Tailnet-exposed services |

| oldsrv on the tailnet as a home-host node (remote-dev reach) | accepted | 2026-09-20 | **Re-evaluating the [network-vpn.md](network-vpn.md) §Tailnet-boundary stance ("no home host runs a tailnet node") because the fact behind it changed:** the owner's coding plane is moving to oldsrv, and every home-hosted UI reaches a phone only through the VPS `traefik-tailnet` edge + WG S2S — so a VPS outage kills remote dev while the home link is perfectly healthy. **Decided:** ONE host (oldsrv) joins headscale as a node, P2P-first over the static public home IPv4 ([network.md](network.md) §WAN), with **no advertised routes and no LAN bridge** — the stance's actual purpose (Shelly/KNX/IoT/guest never tailnet-reachable) survives intact. ⛔ This does **not** re-open the VLAN-99 seal above, and widens nothing in `wg_s2s_vps.allowed_ips` / the router `available-from`. Gates: declare `tagOwners` in `policy.hujson` before a second tailnet user joins (today the ACL is email-only, so a new node is reachable by everyone on the tailnet). Implementation + full scope: HD-405. ⏳ decided, not live · [network-vpn.md](network-vpn.md) |
| WireGuard road-warrior peer on the RB4011 (dev reach + geo egress, VPS-independent) | accepted | 2026-09-20 | **Re-evaluating the "WireGuard is site-to-site only, user devices use Headscale" strategy because the reason for it does not apply to one admin device:** the family path needs app-toggle simplicity, which is why Headscale won; a single owner phone importing one peer config is a different cost, and it is the only path that depends on neither the VPS nor headscale. The router is already a WG server (site-to-site) on a DDNS endpoint with kernel-fast WG, and a full-tunnel variant serves the Slovenian-egress need **without** an exit node. Declined alternative: lean on the Pi exit node alone (the Pi 4 is the documented throughput ceiling, and the same thread retires the "oldsrv = disposable tier" premise that put the exit node there). Gates: known-peer-only, allowed-IPs scoped to the one host, no advertisement into any other VLAN range, rate-limit + log on the router. Implementation: HD-406. ⏳ decided, not live · [network-vpn.md](network-vpn.md) |
| Exit-node host = oldsrv (replacing the Pi) | superseded (re-decision) | 2026-09-20 | [network-vpn.md](network-vpn.md) §Exit-node rejected oldsrv as the *disposable tier* — "the exit node must be available exactly when travelling". The 2026-09-20 coding-plane decision changes that premise: oldsrv becomes the always-on box the owner actively protects, and it carries a far better pipe than the Pi 4. **Accepted as an option, not as a placement:** the exit node stays client-toggled, the Pi leg is not retired until the router WG peer (HD-405) proves the geo-egress need is met without it, and the owner picks the host at implementation (HD-408). ⏳ open owner call · [network-vpn.md](network-vpn.md) |
| **Exit-node host** — move it from the Pi to oldsrv | **decided: stays on the Pi** | 2026-09-21 | **HD-408 CLOSED by owner decision, no implementation.** The 2026-09-20 row above re-opened the placement because the coding-plane thread removed the *"disposable tier"* premise; the owner weighed the better pipe on oldsrv against widening the tailnet boundary (that thread's exception permits exactly ONE home host with **no advertised routes and no exit node**) and kept the Pi. Honest cost accepted: the Pi's exit throughput stays the constraint. **Re-open trigger:** the HD-406 full-tunnel WG profile — it answers the geo-egress need with no exit node at all, which retires this question instead of answering it. · [network-vpn.md](network-vpn.md) §Exit-node |
| Tailnet resolver chain — leave the chain, or trim/reorder it so the away case is cheap | **re-decided the same day: neither** | 2026-09-21 | **HD-415.** Both were offered and both were rejected once the tradeoff was written down. Leaving the chain makes an away tailnet device burn timeouts on two resolvers it cannot reach; trimming them buys the away case by selling the WAN-out case. The owner chose the third path — make the resolver **reachable from wherever the device is** instead of sorting resolvers by where the device usually is: **oldsrv's tailnet node address** as a nameserver (reachable direct-over-LAN at home with the WAN pulled, reachable away while the home link is up), the **VPS public** instance kept as the fallback so a dead node never costs away-side resolution, and the two LAN-address entries dropped. Case (d) — away while the home WAN is down — is accepted as unresolvable: *"it is ok that I will not get dns for whole domain, because it is not reachable from the internet."* The design's load-bearing assumption (direct-over-LAN to the node with the WAN pulled) is what the three-case drill must prove. · [network-dns.md](network-dns.md) §The resolution requirement · [network-vpn.md](network-vpn.md) |
| HD-312 observation-only close: accept "both kids tablets offline at the bedtime hour" as the sighting for **both** the bedtime WAN block **and** the Kids filtered-DNS binding, or keep the row open for a separate DNS-leg sighting | **accept and close** | 2026-09-21 | **Owner decision.** The two tails shared one mechanism — the same `kids-*` rule set in `rb4011_converge.rsc.j2` matching the same two static MACs (`tablet-valentina`, `tablet-ipad`) — and the symptom observed (no internet on **both** tablets at the observation hour) is that mechanism firing. A device with DNS but no WAN would still load nothing useful, so the absence of a separate DNS-leg observation is not evidence of a gap; a dedicated re-watch buys nothing a later real fault would not surface itself. HD-312 row deleted from `todo.md`; re-open on a kid device seen bypassing the filter. · [network-vlans.md](network-vlans.md) §Firewall policy + §Kids controls |
| `dns.nameservers.split` (split-DNS for `kogler.si` over the tailnet) | rejected | 2026-09-22 | **HD-432, owner decision.** Away from home it answers internal names with LAN addresses — correct answers, unreachable — and a split domain is **never** retried against the OS resolver, so it removes the fallback while adding nothing routable. What the tailnet needs is reachable answers, which `extra_records` provide from the client’s own netmap with no resolver dependency. Re-open only if a client must resolve the whole zone away and can consume LAN answers. · [network-dns.md](network-dns.md) §The answer-plane model |
| whole internal zone as MagicDNS `extra_records` | rejected | 2026-09-22 | Publishes a **frozen copy** of the zone into every device: MagicDNS-local names are answered *instead of* asking the live resolver, so per-subnet views, VIP semantics and offline hosts stop being visible; it invites attaching every router to the tailnet listener (turning the tailnet into a second LAN for the whole catalogue); and it copies the entire internal namespace onto every device. Rejected for the mechanism it would replace, not the outcome — **HD-436**’s `zone_kogler_si` reaches the same place as a derived list carrying a per-name exposure flag, so widening stays a data edit. · [network-dns.md](network-dns.md) §`zone_kogler_si` |
| advertised routes to user nodes (incl. a tailnet-routable keepalived VIP) | rejected | 2026-09-22 | The only design where the *answer itself* floats with HA, and the only one that ends per-name records forever — at the cost of the invariant that user nodes are given no routes: it makes the home subnet reachable from every network a phone joins, moves ACLs from per-address to per-subnet, and is the largest deviation available from the tailnet boundary. Re-open trigger: a decision to treat the tailnet as an extension of the LAN, not another missing record. · [network-vpn.md](network-vpn.md) §Tailnet boundary |
| `dns.override_local_dns: true` | rejected | 2026-09-22 | Would force clients onto the tailnet chain and thereby fix away-resolution for **all** names — by routing every query from every device through home/Internet resolvers, i.e. a DNS exit. Kept `false`; the advisory-chain consequence is handled with reachable answers instead. Measured (2026-09-22) with it `false`: a Windows client resolved the internal zone from the LAN resolver while the tailnet was connected, and a phone on cellular asked its carrier — so nothing in this design may assume a client was told to use the chain. · [network-dns.md](network-dns.md) §The resolution requirement |
| the VPS public address as the tailnet’s resolver | rejected | 2026-09-22 | Same zone, different **views by source**: measured `NOERROR → VPS edge` from the home WAN and `NXDOMAIN` from an away source for the *same name on the same instance*. A co-equal chain entry therefore answers NXDOMAIN for names the home node serves — the poisoning shape the old chain was infamous for. Addressing that instance over its own tailnet node avoids the view problem but there is no listener there (the sidecar shares `traefik-tailnet`’s netns and serves only what `serve.json` maps), so it would need new machine-made wiring. · [network-dns.md](network-dns.md) §The resolution requirement |
| hosts-file aliases as the answer mechanism | rejected | 2026-09-22 | **Owner decision (a), and generate-or-disown.** Hand-maintained entries sit **above** DNS per machine, propagate to nothing that is not a managed Linux box (never to a phone, a TV, a guest laptop), carry no validator — and destroy the instrument: an override on the admin workstation hides a wrong zone answer exactly where it would otherwise be noticed. Kept only for **unqualified** short names on admin boxes, and then **generated** from `zone_kogler_si`, never typed. The live workstation file cited `scripts/tailnet-hosts.txt` + `scripts/tailscale-dns-fix.ps1`, which have never existed in git. · [network-dns.md](network-dns.md) §`zone_kogler_si` |

| a SECOND home tailnet node — the Pi (`tag:home-edge`) | accepted — **amends the 2026-09-20 "ONE host" boundary row above** | 2026-09-25 | **Owner authorises two home nodes on the HD-435 call: `oldsrv` + the Pi.** The reason is HA, not convenience: while the tailnet's answer for `ha.kogler.si` lives on one box, the house's most critical service is unreachable from away *while it is up*, which the 2026-09-22 takeover drill proved from the other side. **What did NOT move:** neither node advertises routes, neither bridges a LAN, and the VLAN-99 seal is untouched — the 2026-09-20 row's purpose (Shelly / KNX / IoT / guest never tailnet-reachable) is exactly what survives. **The second node joins on its OWN tag, deliberately not `tag:dev`:** an ACL grants by TAG, so a shared tag hands the Pi the dev seat's ports and an untagged key leaves it reachable by nobody. Its reach is `tcp/443` to its own node address from the tailnet only, and it must **not** inherit the HD-415 address-scoped `udp 53` resolver grant — that rule is address-scoped precisely so a second node cannot pick up a resolver grant by accident, and the Pi already runs Technitium tertiary. **Re-open trigger:** any proposal to advertise routes, bridge a VLAN, or share a tag between the two home nodes. Current state + measured reads: [network-vpn.md](network-vpn.md) §Tailnet boundary |

> **Not a network-domain decision:** services / deploy / storage / smart-home rejections live in their own `<domain>-rejected.md`. See [`services-rejected.md`](services-rejected.md), [`deployment-rejected.md`](deployment-rejected.md), [`storage-rejected.md`](storage-rejected.md), [`smart-home-rejected.md`](smart-home-rejected.md).
> **SSOT note:** this log is the decision-log SSOT for the network domain.