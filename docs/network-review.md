---
title: Network — Review Queue
role: queue
domain: network
status: active
tags: [network, review, queue]
---
# Network Review Queue

> **Role:** Intake queue — network instrumentation / config-manager options heard of but not yet researched. Near-empty by design: this is a queue, not a backlog.
> **Links to:** `network.md`
> **Linked from:** `index.md`, `network.md`

> ⚠️ **Planning phase — nothing live yet.**

> **Lifecycle (per CONVENTIONS.md §8.3):**
> 1. **Before adding**, check [`network-rejected.md`](network-rejected.md) first (consulted, not auto-blocking; re-review only with an exception note).
> 2. **Promote** → move the row to `todo.md` as an HD-XXX (pointer back here), then delete it from this file.
> 3. **Stale** — any row untouched for **30 days** must be promoted to `todo.md` or moved to `network-rejected.md`. Review is a queue, not a backlog.

---

## Queue (intake)

| Tool | URL | Why (3 words) |
|---------|-----|---------------|
| — | — | — |

> *(Deliberately near-empty. Add a row only when a tool is heard of but not yet reviewed.)*