#!/usr/bin/env bash
# SYNTHETIC FIXTURE for scripts/testdata/check-vault-items/ (HD-247) - NOT real IaC.
# Minimal stand-in for the LiteLLM bootstrap-keys glue map: "<service>:<auto-seeded item>".
KEYS=(
    # GLUE-SEEDED items (hd247) - "<service>:<item>" pairs consumed by check-vault-items.sh:
    #   "litellm:lk_demo_chat_api"
    #   "litellm:lk_demo_rag_api"
)
