# SYNTHETIC FIXTURE for scripts/testdata/check-vault-items/ (HD-244/245) - NOT real IaC.
# Per-service compose template carrying a literal onepassword() lookup.
secrets:
  metabase_password: "{{ lookup('community.general.onepassword', 'demo_plain_api', field='password', vault=op_vault) }}"
