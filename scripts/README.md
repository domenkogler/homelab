# scripts/ — Homelab automation, linters & renderers

> **Role:** Reference map for the scripts in this directory — validation gate, SSOT renderers,
> and small utilities. Read it to pick the right script for a task (or avoid re-implementing one).
> **Linked from:** `CONVENTIONS.md` (§8) · `docs/index.md` (Conventions) · `README.md`
> **Authoritative specs:** owning docs named inline per script — concrete values always live in IaC
> (`group_vars/*.yml`, `host_vars/*.yml`, `rack-connections.json`), never in a generated doc.

Every script here is part of one of three groups: **validation** (the fail-closed repo gate), **render** (IaC/SSOT → generated docs), or a **standalone utility**. The single entry point for validation is
[`validate-all.sh`](validate-all.sh). The single unified render entry is [`render_all.py`](render_all.py) (HD-163) — a pure-Python umbrella over the 5 generated-doc renders below, with **no Ansible / WSL / 1Password CLI dependency**.

---

## Validation gate

**Never commit without it. `validate-all.sh` runs every linter below and must end green.**

| Script | What it checks | Owning spec |
|--------|----------------|-------------|
| [`validate-all.sh`](validate-all.sh) | **Repo gate** — runs all validators in order (`set -e`, stops at first failure), incl. the HD-253 session-discipline hard-gate + self-test. Run `bash scripts/validate-all.sh`. | `CONVENTIONS.md` §4, `docs/index.md` Conventions |
| [`validate-docker-services.py`](validate-docker-services.py) | **Compose templates** — every `docker_services` template referenced by a group_vars list (count derived, orphan-dir lint): file exists, Jinja renders (mocked 1Password; context loaded from `versions.yml` + `all.yml` SSOT so it cannot drift, HD-189), YAML parses, structural checks (external networks, Traefik labels), source-level bug scan (`default=` param, host-net vs `network_mode: host`). Fail-loud on malformed group_vars YAML. | `docs/deployment-compose.md`, `docs/services.md` |
| [`validate_blueprints.py`](validate_blueprints.py) | **Authentik Blueprint shape** — local loader for custom YAML tags (`!KeyOf`, `!Find`, …) + structural checks (flow slugs `default-` prefixed, provider↔app binding). HD-149. | `docs/services-authentik.md` |
| [`check_doc_ips.py`](check_doc_ips.py) | **No IP literals outside the SSOT** — internal IPv4 ranges live only in `docs/network-addresses-generated.md` + IaC; every other doc refers by hostname/role. HD-152. | `docs/index.md` Conventions, `docs/network-addresses-generated.md` |
| [`validate_doc_templates.py`](validate_doc_templates.py) | **SSOT templates render** — `network-addresses.md.j2` + `inventory.md.j2` render with real `group_vars` context (no missing strict-undefined). | `docs/deployment-ansible.md` |
| [`validate-secrets.py`](validate-secrets.py) | **No literal secrets** — flags PEM/OpenSSH blocks + literal `*_password/_secret/_token/_api_key` values in group_vars/host_vars/role defaults+tasks+vars, `roles/*/files/*` and every rendered template (`templates/**/*.j2`) (HD-189); exempts Jinja `{{ ... }}` / `lookup(...)` values, env-var indirections and boolean/numeric policy flags (integrated `default('')`) | `CONVENTIONS.md` §Secrets, `docs/deployment-secrets.md` |
| [`check_doc_map.py`](check_doc_map.py) | **Docs reachable + links valid** — every doc under `docs/` reachable from `docs/index.md` (and vice-versa) + repo-wide `.md` link resolution. HD-152 + HD-173. | `docs/index.md` |
| [`check_generated_suffix.py`](check_generated_suffix.py) | **`-generated` suffix discipline** — every machine-produced doc carries `-generated.md`, no hand-authored doc carries it. Keeps `EXPECTED_GENERATED` in sync with `docs/index.md` map. | `CONVENTIONS.md` §8.2 |
| [`check_vault_name.py`](check_vault_name.py) | **Vault name = `Homelab-ansible`** — flags a bare `Homelab` vault reference (prose/comments/op:// URIs) in canonical docs, IaC yml/j2 and scripts/*.py; the frozen `reports/changelog.md`/`reports/deployment-journal.md` archives are exempt. HD-189. | `docs/deployment-secrets.md`, `CONVENTIONS.md` §1 |
| [`check_placeholders.py`](check_placeholders.py) | **Placeholder discipline (B5)** — greppable bootstrap placeholder tokens (the B5 set: `REPLACE_ME`-style markers, disk-serial stubs, 1Password pubkey stubs — full list in the script) may appear only in the designated bootstrap artifacts + the owning spec that quotes them (`docs/deployment-preseed.md`; the round-2 audit reports that also quoted the set were fold+deleted by HD-203); anywhere else fails the gate. Mirrors the runtime assertions in `IaC/host/post_install.sh` + `pi/first-boot-config.sh`. HD-201. | `docs/deployment-preseed.md` |
| [`check_todo_done.py`](check_todo_done.py) | **`todo.md` §4(a) done-row sweep + `prompt.md` handoff check** — flags todo rows marked DONE/✅/✔/LIVE that were **not deleted** (a fully-done row must be removed — record lives in owning docs + git; only deploy-gated rows with a real `⏳` tail stay). Also scans `prompt.md` (the next-session handoff): an HD presented as **open work** there whose todo row is gone (stale claim) or claimed **whole-row done** while todo still has it open (contradiction) → exit 1; sub-IDs (HD-318a) inherit parent open-ness; phase-done prose on an open row is not a contradiction. Caught HD-198 + 5 stale prompt claims in the 2026-09-08 sweep. Wired into `validate-all.sh` as item 14. ⚠ **Marker grammar (fixed + self-tested):** a completion marker is a `✅`/`✔` glyph or an **UPPERCASE** marker word at a word boundary — `python3 scripts/check_todo_done.py --self-test` pins it, and `validate-all.sh` runs that self-test. It used to be a case-**insensitive substring** test, so ordinary prose became completion claims: "the live headscale config", "deliver the record", "olive oil", "unresolved drift" and the variable name `OLLAMA_KEEP_ALIVE` all matched — and because the §2 window is only −60/+90 characters around an HD id, a word that was never about the HD failed the gate with "prompt.md marks it done but the row is still open". Two consequences worth knowing: the row-level and prompt-level vocabularies are deliberately **not** identical (`REJECTED` beside an id does not mean the row is finished — that misread HD-376 once), and a done-claim is forgiven if the same HD is presented as open work anywhere else in `prompt.md`, which is what lets phase-done prose like "IaC merged ⏳ deploy" through. | `CONVENTIONS.md` §4, `todo.md` §0, `prompt.md` |
| [`check_dns_seed_drift.py`](check_dns_seed_drift.py) | **Technitium split-horizon seed contract (static, HD-341 parity guard)** — renders every record row of the `technitium-seed` loop against the SSOT (`group_vars/all/main.yml`) for all three instances (VPS primary, oldsrv secondary, Pi tertiary) and enforces the per-instance split + the LAN-only `when`-gate (modem/spark/llogs never on the VPS primary). Fails the gate on a record/gate drift BEFORE a converge ships it — the static form of closing the recurring "scoped converge skipped the seed" NXDOMAIN class (live 2026-09-15 llogs). Wired into `validate-all.sh` as item 15. | `docs/network-dns.md` §Per-Instance Split-Horizon, §Convergence & drift |
| [`check_merge_markers.py`](check_merge_markers.py) | **No merge-conflict marker may reach a commit (HD-453)** — scans every file `git ls-files` reports, in the **working tree** (so it fires before the commit that would ship the marker), for `<<<<<<<` / `>>>>>>>` / `\|\|\|\|\|\|\|` and git's exact-width `=======` divider. Born from a real miss: a `>>>>>>>` line survived a bad rebase into a rebased commit of a root view file and every gate stayed green. The divider is matched at exactly 7 and only there — the repo's tracked raw evidence rules sections with 50–60-char `=` banners, and a `^={7,}` draft printed 78 false positives at first glance (a muted gate is no gate); in `.md` a bare `=` underline directly under text is a legal setext H1 and stays green. `--self-test` builds a throwaway repo and asserts both halves |
| [`check_md_tables.py`](check_md_tables.py) | **No markdown table row is WIDER than its own header (HD-417)** — splits every table row on unescaped pipes and compares the cell count to that table's header. Chose WIDER-only over equality after measuring: 14 narrower rows are legitimate (a 2-cell row inside a 3-column decision table, a struck-through PARKED row that dropped its last column), so an equality assert printed **41 findings across 12 files** and would have been muted in a week; wider-only printed the 11 lines that were actually broken. Two real classes caught: a swallowed newline that merged two registry rows (identity silently lost — and it is caught at the row level, which the merge-marker gate cannot be, because a merged row is valid markdown), and a stray pipe inside a cell, **including inside code spans**, where GFM still cuts the cell (this very row is the demo: an unescaped pipe here shifted the Owning-spec column into the description and the width check could not see it, because the count stayed legal) — that is the trap the escape convention documents. Excludes `reports/**`, `docs/assets/**` and `brainstorming/**`: raw evidence and archived material are not reformatted to satisfy a gate. `--self-test` plants both classes and asserts escaped pipes and short rows stay green | HD-417, [CONVENTIONS.md](../CONVENTIONS.md) §6 · [prompt-417.md](../prompt-417.md) |
| [`check_iac_backend_strings.py`](check_iac_backend_strings.py) | **Retired-backend names may not regrow (HD-404)** — a **ratchet**, not a lie-detector: counts `Prometheus|Loki` mentions under `IaC/ansible/{playbooks,group_vars}` and refuses growth past `FLOOR` (HD-342 replaced that pair with VictoriaMetrics + VictoriaLogs, but IaC headers kept naming the dead backend, and a header is what a converging agent reads first). Why not smarter, measured here: an assertion-shaped regex (`backend` / `scraped by` / `lives on`) caught 2 of the 4 real violations HD-404 fixed and missed `… fall back to 127.0.0.1` + `forwards telemetry to the VPS Prometheus/Loki`; an allowlist over the 28 survivors (Alloy's `prometheus.scrape` module, the `alloy_ha_exporter` var, the retired-pin notes) would be muted in a week — the HD-417 failure mode. `--show` re-derives the count; `--self-test` counts growth, counts removal, and refuses a floor red at rest or >20 above the corpus |
| [`check_secrets.py`](check_secrets.py) | **Secret-shape scanner over what would be committed next** (added 2026-09-18 after a live `spark-llm_api` bearer reached `origin/main` inside stability bench logs — `validate-secrets.py` only covers `group_vars`/roles, so logs and archives were ungated). Scans the git INDEX by working-copy content + untracked non-ignored files, **descends into tracked `.tar`/`.tar.gz`/`.zip` members**, prints hits MASKED only (kind/length/salted hash/4-char head, never the value), downgrades prose (`sk-master-key-here`) + `os.environ/…`/code refs to non-blocking notes, and treats an **unreadable archive as a failure** (an artifact you cannot read is not trustworthy). Wired into `validate-all.sh` as #15. Negative-tested: a planted bearer fails it as a plain file AND inside a `.tar.gz`. | `CONVENTIONS.md` §2, `deployment-ai-stack-secrets.md` §4a |
| [`check_self_converge_guard.py`](check_self_converge_guard.py) | **HD-413 self-converge guardrail — static completeness** — asserts every playbook that applies a lockout-capable role (`network`/`storage`/`wireguard`/`vps-hardening`) imports `playbooks/tasks/self-converge-guard.yml` and imports it in **pre_tasks** (the only place that can refuse before the damage); every lockout role has its own guard task; each guard task's `tags:` equals its `homelab_guard_tags`; that shared list covers the role's complete selectable vocabulary **recomputed from the role dirs** (role task tags + playbook role-level tags + `untagged` where tasks are untagged — storage 43/56, vps-hardening 16/17), so adding a tag inside a role tightens the guard instead of opening it; the guard carries no `default()`/`failed_when` (HD-399 rule); a role writing under `--check` (`check_mode: false`) may not claim `check_safe`. Wired into `validate-all.sh` as item 16. Cannot catch a wrong predicate — see the runtime fixture below. | `deployment-ansible.md` §Self-converge guardrail **Also derives the set (2026-09-21):** classifies every role from its tasks (unit restarts on sshd, networkd, tailscaled, wg-quick, nftables, raw nft, iptables, ip, wg, tailscale, nmcli, netplan and systemctl-restart commands, writes to `/etc/ssh`, `/etc/systemd/network`, `/etc/netplan`, `/etc/nftables`, `/etc/wireguard`, `/etc/fstab`, `/etc/sudoers`, `/etc/pam.d`, `/root/.ssh`, `authorized_keys`, and mount/filesystem state) and fails when such a role is neither guarded nor exempt via `homelab_lockout_exempt_reason` in its own defaults/main.yml (under 40 chars = not an argument). Carries `self_test()`, so the detector cannot silently stop detecting; a canary role that restarts sshd turns it red. Limits it declares: declared limits: container-level damage, network_cli device config, revoking authorized_keys, variable-resolved dest paths (hinted, not rendered). |
| [`rotate-spark-llm-key.sh`](rotate-spark-llm-key.sh) | **Atomic rotation window for `spark-llm-key`… (`spark-llm_api`) — plan-only by default.** Needed because ONE vault item is FOUR coupled bearers (vLLM `--api-key`, `OPENAI_API_KEY` in both LiteLLM instances, and laptop `~/.pi/agent/models.json → providers.spark.apiKey`, which Ansible does not own): a vault write followed by later converges opens a half-applied window where a re-rendered consumer 401s against a stale engine — and another session's converge can open it too. Preflights for a running `ansible-playbook` + prints worktree state; rotates the vault with the **write-scoped** token (root-only `/etc/op/provision-token` on vps; the runner's exported SA token is READ-scoped and 1P answers `Couldn't update the item.`); re-renders spark-ai / litellm / lan-litellm **detached**; waits for `/health`; patches `models.json`; verifies **both directions** (new→200 AND old→401 — the 401 is the only proof the window closed). Values move via variables + a 0600 temp; only lengths/hashes are printed. Cost: ~25 min engine cold load. `--run --yes` to execute; `--skip-rotate` when the owner already rotated the vault; pass the superseded value in the env as `OLD_SPARK_LLM_KEY` so the "old bearer must 401" proof can run; `OLDSRV_SSH="<ssh target>"` is the **escape hatch** for a station that lacks the laptop alias contract — since HD-397 (2026-09-19) the plain `ssh oldsrv` alias reaches oldsrv's Home leg through the VPS jump from any network, so the default needs no override (an unreadable leg still prints `UNREADABLE`, never a verdict). | `deployment-ai-stack-secrets.md` §4a |
| [`todo-table.md`](../todo-table.md) | **Planning view of the backlog** — hand-curated split of `todo.md`'s open rows into **Table AI (AI-runnable, no AI-side blocker; a deploy-gated chapter lists rows waiting on a Table Human prerequisite)** and **Table Human (needs an owner step)**, grouped by why each is blocked, for "what to do next" planning. `todo.md` remains the registry SSOT (row deleted when fully done, §4(a)); this table only cross-links owning-doc status blocks. Keep in sync with todo.md on every backlog change. | [`todo.md`](todo.md), `CONVENTIONS.md` §4 |
| [`guard-session.sh`](guard-session.sh) | **Session-discipline gate (HD-253)** — pre-edit guard refuses ANY edit-context while the PRIMARY checkout (`git-dir == git-common-dir`) sits on `main`; `--validate-mode` (wired into `validate-all.sh`) hard-fails only on primary+main+DIRTY; clean main merge-station exempt; session worktrees + detached HEAD/CI pass through. Refusals print the exact `git worktree add ../homelab-wt-<date>-<HHMM>` remediation with live timestamp + coordination info (status, per-file last-commit). `--self-test` runs a sandboxed fixture (temp repos) inside the gate. Owning rule: CONVENTIONS §6. | `CONVENTIONS.md` §6 |
| [`sync-skills.sh`](sync-skills.sh) | **Skill drift gate (HD-254)** — keeps `skills/` ↔ `~/.pi/agent/skills/` drift-free, repo = SSOT. `--check` (default) reports drift; `--check --strict` exits nonzero on any drift OR encoding violation (wired into `validate-all.sh` as item 13; guarded to SKIP when no `~/.pi/agent/skills` exists, e.g. bare CI / non-pi laptop); `--push` deploys repo → deployed (the canonical direction, blocks on a CRLF/UTF-8-BOM encoding violation); `--pull` copies deployed → repo working tree only (never auto-commits/deletes — captures a post-session self-learn for the normal worktree+commit flow, CONVENTIONS §6). Byte-compare is exact so CRLF/BOM reads as a content change; artifact ignore-list (`net.json`, `__pycache__/**`, zero-byte skill-name markers) never counts as drift and is pruned on push — mirrors `install-pi-wsl.sh`. | [skills/](../skills), CONVENTIONS §6 |
| [`check_ledger_state.py`](check_ledger_state.py) | **The ledger stays a ledger** (CONVENTIONS: `deployment-tasks.md` = checkboxes, `- [x]` + date when done). Three checks: an HD line inside a `**Deploy-gated verification**` block written as **status prose** instead of a checkbox (the failure mode that let six HD-3xx narratives live in the middle of the vault table); an OPEN item naming an HD with **no `todo.md` row** (closed/deleted/renamed rows read as open work forever) — bundled ids like `**HD-391 / HD-393**` are validated **per id**, because a closed row paired with an open one used to keep the whole line alive; and a **`- [x]` with no date**, so "done" carries no evidence time. It caught three of its own author's edits on first run. |
| [`check_runbook_purity.py`](check_runbook_purity.py) | **The runbook stays imperative procedure** (CONVENTIONS: `deployment-manual.md` carries no progress markers, no history, no knowledge). Rejects status glyphs and status words in headings (`⚠` stays — a caution is procedure, a tick is not), execution narrative ("live lesson", "as executed", "this session", "close-out", "recap"), any `20YY-MM-DD` outside a code fence and outside a `decision #NN` citation, **duplicate headings** (the file shipped two §5.5, which hid a section), and enforces **phase alignment**: every `## Phase X` in the runbook must exist in the ledger, so "Phase 5" cannot mean the deploy button in one file and a GPU box in the other (the runbook's `Phase 0.5` reinstall is the one documented exception — the ledger carries it as a Phase 1 step). |
| [`check_vault_docs.py`](check_vault_docs.py) | **Every vault item the IaC reads is written down.** Derives the required set from the IaC itself — every `onepassword` lookup in playbooks/roles/templates plus every item named in `_service_vault_items` (declared-but-unconsumed counts too: a scanner-required item must still be documented, and the doc row is where that oddity gets explained) — and requires a row in `docs/deployment-secrets.md` §Master Secret List for each. `check-vault-items.sh` answers "does the vault HAVE it"; nothing answered "is it documented", which is how `technitium_login` — a hard gate whose absence fails the first DNS seed of a true-zero rebuild — was missing from the catalog. |
| [`check_traefik_host_rules.py`](check_traefik_host_rules.py) | **No whitespace inside a traefik `Host()` matcher or a quoted certificate path.** A `Host()` rule is compared byte-for-byte, so a template space — `Host(\`ha. {{ base }} \`)` — yields a router that can never match a request while every converge reports success: that is how `ha.ts.kogler.si` served 404 for two days behind a healthy entrypoint, ACL, extra_record and VIP backend (HD-432, 2026-09-22). Renders nothing, needs no inventory, and carries a 7-case self-test; and by the cert-path twin found the same evening (`certFile: "…/certs/ {{ … }} "` → traefik answers `TRAEFIK DEFAULT CERT` and `curl -k` cannot see it). 11 self-test cases, both shipped instances mutation-killed; mutation-killing it by re-injecting either space is in the docstring. |
| portability sweep (in `validate-all.sh`) | **Debian/WSL portability gate (HD-256)** — `bash -n` every `scripts/*.sh` (+ the testdata runner) and `python3 -m py_compile` every `scripts/*.py`, so nothing can regress on the WSL ext4 primary. bash absent on Windows → sweep silently skipped there (exercised under WSL/CI). Byte-compiles to gitignored `__pycache__/`; never dirties the tree. | HD-256 |
| `ansible-playbook --syntax-check` (in `validate-all.sh`) | **Playbook syntax gate** — every playbook must parse + resolve modules. WSL/CI-gated: skipped with a note when ansible is absent or broken natively on Windows (WinError 87). HD-197. | `docs/deployment-ansible.md` |

---

## Renderers (IaC / SSOT → generated docs)

> Direction of truth is **IaC → generated MD** (never the reverse). These write `*-generated.md` files
> that must not be hand-edited. Re-render and `git diff --exit-code` to detect drift.

| Script | Renders | SSOT → outputs | Why it's needed (vs Ansible) |
|--------|--------|-----------------|----------------------------|
| [`render_network_addresses.py`](render_network_addresses.py) | `docs/network-addresses-generated.md` | `group_vars/all/main.yml` + host_vars (`oldsrv`/`pi` DNS) → generated doc | **Windows fallback** — Ansible crashes natively on this host (`os.get_blocking` → WinError 87); this refreshes the doc without WSL/Ansible |
| [`render_rack_connections.py`](render_rack_connections.py) | `docs/network-rack-generated.md` + `docs/rack-layout.mmd` | `docs/rack-connections.json` (parsed from `docs/assets/Rack.canvas`) → generated docs | **Different SSOT** (rack JSON, not group_vars) — not covered by `render-docs.yml` |
| [`knx-hass-gen.py`](knx-hass-gen.py) | `IaC/ansible/roles/home_assistant/files/knx-entities.yaml` (deployed into HA) | the ETS export `docs/assets/references/knx/StanovanjeKogler_v1_0.knxproj` (parsed with `xknxproject`) → the HA `knx:` entity block | **Not docs, same direction**: the `.knxproj` is the KNX SSOT and this is the only thing allowed to translate it. `--check` re-renders and REFUSES if any emitted address is not published in the project — the hand-typed 19-address sensor appendix is the one place an address can be invented here, and HA pays per-entity (`Did not respond to GroupValueRead`). Measured 2026-09-25: 246 published, 154 emitted, 0 outside; canary: retyping one appendix address to `12/1/99` turns it red and names it as hand-typed. `--self-test` (stdlib-only) is wired into `validate-all.sh`; `--check` is not, because the runner has no `xknxproject` and **no file in this repo declares that dependency** |

> Ansible equivalents: `IaC/ansible/playbooks/render-docs.yml` renders the same *group_vars*-derived
> docs on Linux/CI or in WSL (`wsl.exe -d Debian`). `render_all.py` makes the Python path the
> primary one (including the inventory + subscriptions renders that previously existed only as
> Ansible tasks). `render_rack_connections.py` has **no** Ansible
> counterpart. See [`docs/deployment-ansible.md`](../docs/deployment-ansible.md).

---

## Utilities

| Script | What it does | Owning spec |
|--------|--------------|-------------|
| [`next-hd.sh`](next-hd.sh) | **Derive the next free HD id** (CONVENTIONS §1 / README §0-4) — implements the HD-253 "derived-values ban" mechanically: reads the SSOT files (`todo.md` + `docs/**/*.md`; changelog archive excluded, + git-history fallback `git log -S 'HD-'` for closed rows) and prints `max(HD)+1`. Parser is lenient on the historical tokens: zero-padded (`HD-03`), letter-suffixed (`HD-259B`), en-dash ranges (`HD-247–251`), lowercase compounds (`hd244+hd245`) — largest integer wins, numeric (never lexical) sort, dedup. `--max`/`--max-raw` print the current max; `--raw` prints the bare next number for scripting; `--next`/default prints `HD-<n>`. Fails loud (rc 2) when no HD parses. Replaces always-hand-typed next-HD literals in handoffs/backlog. | `CONVENTIONS.md` §1 (derived-values ban), README §0/§4 |
| [`adapt-vllm-dashboards.py`](adapt-vllm-dashboards.py) | **vLLM/SGLang Grafana dashboards — upstream → homelab adapter (HD-368)** — turns a grafana.com export (`/tmp/vllm-dash/<gnet>.json`, via `grafana.com/api/dashboards/<id>/revisions/latest/download`) into the file-provisioned JSON the monitoring role copies to the VPS Grafana. Default mode adapts from `/tmp/vllm-dash`; **`--inplace`** re-runs the **live-data drift layer** over the repo files (byte-idempotent). Beyond scaffolding stripping, `${DS_*}` → the literal `prometheus` uid and `homelab-*` uids/tags, the layer carries `EXPR_REWRITES` (queries that can never resolve here — V0 `vllm:num_requests_swapped`, production-stack `vllm:current_qps`/`healthy_pods_total`/`router_*_usage_percent`, the V1 `gpu_prefix_cache_*` rename, unscoped `process_*`), `PANEL_OVERRIDES` (title/unit/tooltip/legend following an expr fix), `VAR_OVERRIDES` (reset an upstream-baked `current:` model name), `TEXT_TRANSLATE` (operator-visible Chinese/Korean → English) and `STALE_METRICS`/`LIVE_VERIFIED_SET` (fail-loud guards + a populated-vs-empty-by-design coverage line). **Never hand-edit the three `homelab-*vllm*` JSONs — add a rule here.** | [`docs/observability.md`](../docs/observability.md) §Dashboards (vLLM block) |
| [`build-llm-dashboard.py`](build-llm-dashboard.py) | **Unified LLM dashboard — the three HD-368 vLLM boards → ONE `homelab-llm.json` (HD-377)** — merges the adapted exports into a single row-based board (Health & Readiness · spark CPU/RAM/GPU + disk capacity/IOPS/throughput/saturation · scheduling · latency · throughput · cache · reliability · request shape · per-engine · forward-look). Chain: grafana.com → `adapt-vllm-dashboards.py` → the three JSONs → **this** script → `files/dashboards/homelab-llm.json`, so the merged file is derived and **never hand-edited** (fix a query in the adapter or in `EXPR_REWRITES`/`NEW_PANELS`). Merging ≠ concatenating: `DROPPED` records every cut + reason (gnet 25043 is cut 100 %), and every carried query is re-joined `by (job, instance)` + scoped to `$instance` so a second engine (SGLang, RX 7600) adds a series instead of lying. Five fail-loud guards: reference resolution, empty/misplaced-variable queries, `$instance` coverage (host panels pinned to spark are the documented exemption — the list is `node_titles`, so a new `node_*` panel must be added there), layout-vs-`DROPPED` consistency, non-English strings + HD-368 stale-metric creep into queries. Live-series allowlists (`LIVE_VLLM_METRICS`, `LIVE_NODE_METRICS`, `LIVE_PROCESS_METRICS`) are what a panel must touch to render without an "empty by design" note — re-derive them from VM after any exporter/engine change. `--check` = CI drift gate (byte-idempotent), `--report` = print the carry/cut map. | [`docs/observability.md`](../docs/observability.md) §LLM Dashboard |
| [`gen-htpasswd.py`](gen-htpasswd.py) | **bcrypt htpasswd line** — `gen-htpasswd.py USERNAME [PASSWORD]`; cost 12. (Prometheus `basic_auth_users` use-case retired with `prometheus-internal_api`, HD-341/342; kept as a general bcrypt-line utility.) | `docs/deployment-compose.md`, `docs/deployment-secrets.md` |
| [`provision-secrets.py`](provision-secrets.py) | **1Password item create/rotate helper** for the `Homelab-ansible` vault — **safe-by-default** (bare run is a no-op help; every write needs an explicit flag + `--yes`). `--list` shows the generated-item catalog; `--create` seeds missing generated items (never overwrites); `--rotate ITEM` / `--rotate-all` regenerate values in place, refusing externally-coupled items (`wg_password`, DB passwords, `authentik_password`, `kopia_password`, `matrix_password`). Rotation + Ansible: 1Password is the SSOT, so after rotating a 1P item re-run the affected Ansible role to re-render compose/config with the new value. Needs a write-scoped `OP_SERVICE_ACCOUNT_TOKEN`; fails closed without it. | `docs/deployment-secrets.md` |
| [`ansible_query.py`](ansible_query.py) | **Read-only Ansible fact queries — answers instead of trees (HD-456)** — the context-cheap way to ask the recurring questions about `IaC/ansible` (or `--root spark`), so an agent never has to `grep` 118 YAML + 183 `.j2` files or read a 68 KB task file: `--var NAME` (every definition in **precedence order** with the winner marked, plus every usage site — including **bare** Jinja in `when:` / `assert: that:` and `{% %}` inside `.j2`, which a brace-only scan and the Ansible language server both miss), `--handler NAME` / `--handlers --unresolved` (dangling-notify verdict), `--role NAME` / `--module NAME`, `--vault` (1Password item/field **names** + file:line only — never a value, CONVENTIONS §6), `--jinja REGEX` (expressions only, YAML scaffolding excluded), `--list-vars`, `--context`. Output is `--limit`-bounded per section. Needs PyYAML + Jinja2 → re-execs under `~/ansible-venv/bin/python3` when the runner python lacks them. Ships `--self-test` (16 sandboxed assertions, incl. two §6 masking canaries and a precedence canary). **Deliberately NOT wired into `validate-all.sh`**: a query has no pass/fail and a read tool must not be able to fail a commit — only `--handlers --unresolved` exits non-zero (1), so it can be gated later unchanged. Why not ast-grep + a tree-sitter Jinja grammar: `languageInjections` is experimental, ast-grep's LSP reports nothing for injected-language rules (upstream #2522), and Ansible's un-braced `when:` conditions are not parseable as Jinja at all — full rationale in the module docstring. | `CONVENTIONS.md` §6 (secrets: names/lengths only), [docs/deployment-ansible.md](../docs/deployment-ansible.md) |
| [`ansible-run.sh`](ansible-run.sh) | **Playbook runner for whichever machine is the control node (HD-407)** — wraps venv activation, the read-scope SA token and explicit `ANSIBLE_CONFIG`/`ANSIBLE_ROLES_PATH` exports (Ansible ignores cwd config on world-writable `/mnt` drives). Repository root is self-derived (`$REPO` from the script's own path); on this machine it is the WSL ext4 primary `/home/domen/source/homelab` (HD-259). Usage: `bash scripts/ansible-run.sh playbooks/vps.yml [--check]`. **It fast-forwards its own clone before converging** (owner decision 2026-09-23, reversing the original "NOTHING HERE PULLS" stance — the oldsrv runner clone had silently drifted two commits behind `main` and no run said so): `git pull --ff-only` on the clone that hosts `$REPO`, then it prints the commit it is about to converge. `--no-pull` converges this working tree as-is; a dirty tree is a loud refusal rather than a stash, and a branch with no upstream (a session worktree) is reported and skipped, never guessed at. Fails loud when Phase 0 bootstrap is missing. Machine-agnostic by construction: every path derives from the script's own location, so it behaves identically on the WSL primary, in a session worktree and on oldsrv's clone. HD-413 refuses the lockout-capable roles when the target IS this runner and `--check` legs stay open (docs/deployment-ansible.md §Self-converge guardrail) — the refusal is the design. | `deployment-tasks.md` Phase 0/1 |
| [`guarded-converge.sh`](guarded-converge.sh) | **Converge or restart-prove ONE compose service behind an independent auto-re-enable (HD-415).** `--action converge`: self-test the watchdog → copy it to the target + arm it `setsid`/`nohup` (survives an SSH drop, a laptop death, a killed ansible) → **assert the arm is live** (`pgrep` + its own `armed:` line) → run the converge through a detached wrapper whose **trap** brings the service up on any exit → **sample liveness** (default 6 × 10 s: `.State.Running` true **AND** `RestartCount` flat across samples **AND** no `unhealthy`/`exited` health, plus an optional `--probe '<cmd>'` that must succeed on the target on EVERY sample — for headscale `--probe 'docker exec headscale headscale nodes list >/dev/null'`) → disarm only on a GREEN verdict. ⚠ **Run any `--probe` on the target by hand and see it succeed BEFORE you arm**: a probe that does not answer turns a HEALTHY control plane RED and fires your own trap. The HTTP form `curl -fsS localhost:8080/` does not answer on the VPS at all (measured 2026-09-25: connection refused), so it is not a probe here. `--action prove`: same asserts, then `docker stop` the real container and wait for the watchdog to bring it back — GREEN demands a changed `StartedAt` **and** a `RE-ENABLING` log line, because a container that came back for some other reason proves nothing. Defaults differ per action (converge grace 240 s so a legitimate restart is never raced; prove 20 s). ⛔ **`--project` is not optional when the compose dir is named after the SERVICE but the guarded container is not:** `--container authentik-server` sends the watchdog to `/opt/authentik-server`, which does not exist, so it refuses to arm — the correct value is `--project /opt/authentik`. A refusal to arm is a refusal to converge, which is the point. ⛔ **It cannot guard a host where docker needs sudo:** on the Pi every `docker` call in the arm/verify path dies with `permission denied … docker.sock`, so Pi compose services run unguarded and `--action prove` can never pass there → **HD-463**. ⛔ **`Running` is not liveness (HD-436, 2026-09-25):** a container that exits at every start under `restart: unless-stopped` still reports `Running=true` in the window BETWEEN crashes, so a one-shot inspect passes a crash loop — which is exactly what the pinned headscale does when `dns.extra_records` and `dns.extra_records_path` are both set. `--self-test` (7 cases: flat-green, crash-loop-red, app-probe-fails-red, down-red, unhealthy-red, no-samples-red, single-sample-green) runs inside `validate-all.sh`, so the verdict cannot silently stop refusing the shape it was written for. ⛔ **Assert and act in the SAME run:** an arm asserted in one command and acted on in another can fail in between and leave the service down unattended — measured cost: the tailnet control plane sat down for 2m20s. Use it for anything the remote path rides (headscale); never hand-restart those. | [network-dns.md](../docs/network-dns.md) §The resolution requirement, [deployment-manual.md](../deployment-manual.md) §1.4e |
| [`restart-watchdog.sh`](restart-watchdog.sh) | **The independent timed re-enable + disarm marker that `guarded-converge.sh` arms on the target.** `arm` waits a grace period, then for a window re-runs `docker compose up -d` once the container has missed `--consecutive` checks; a marker file (`disarm`) makes it stand down at its next check. Runs detached with no dependency on the session that armed it; always exits 0 (a rescue net must never break a converge's status). Carries a four-case `self-test` (down→re-enabled / disarmed mid-window→silent / pre-disarmed→never opens the window / up→left alone) against a stubbed `docker`; mutant-tested 2026-09-22 — removing the recovery call, the running-check or either marker branch turns it RED. Declared limit: it can only ever bring a service UP. | HD-415 (the owner's safety condition on the authorized headscale restart) |
| [`install-pi-wsl.sh`](install-pi-wsl.sh) | **pi.dev bootstrap for the WSL Debian runner** — installs pi natively inside Debian WSL (a standalone Node 22.19+ via the official [`pi.dev/install.sh`](https://pi.dev/install.sh), since apt's nodejs is the too-old 20.x), then deploys the **repo SSOT** to `~/.pi/agent`: `skills/` (artifact-ignore per HD-254: `net.json`, `__pycache__/**`, zero-byte skill-name markers), `pi-agent/` (`AGENTS.md` + `prompts/start.md`), tracked pi packages (`@ogulcancelik/pi-ssh-tools`, `pi-subagents`, `@season179/pi-worktree`, `pi-deepseek-optimized`) via `pi install`, and portable extensions from `pi-agent/extensions/`. **`remote-bash.ts` is intentionally not copied** — it hardcodes Windows `sshpass.exe`/`C:\…` paths (see the local file) and is not WSL-portable. Modes: `--pi-only` / `--config-only` / `--reload`. Run inside Debian (`wsl -d Debian -- bash …/scripts/install-pi-wsl.sh`). Companion to `git-bootstrap.sh` (ext4 primary = pi.dev + Ansible + editing). Owner: user (HD-259 pi.in-WSL companion). · [deployment-manual.md](../deployment-manual.md) §0.1 (ext4 primary + git-bootstrap) · [skills/platform-env/references/debian.md](../skills/platform-env/references/debian.md) |
| [`git-bootstrap.sh`](git-bootstrap.sh) | **Debian/WSL-side primary-repo setup** — makes WSL ext4 (`~/source/homelab`) the primary SSOT clone (pi.dev + Ansible + editing) instead of the slow `/mnt/d` drvfs mount. Idempotent: `clone` on first run, `update`/`pull` do `git fetch` + `git pull --ff-only origin/main`; `--reload` is informational. Env-overridable: `SRC`, `REPO`, `REMOTE`, `GITUSER`, `GITEMAIL`. **`--ssh-auth` (HD-265, op CLI-only):** idempotent SSH auth + commit-signing setup — human `op` sign-in (SA can't read `Private`), pulls `GitHub sign`/`GitHub auth` ssh keys from the private vault into `~/.ssh` + `ssh-add`, flips `origin` HTTPS→SSH, writes `~/.ssh/config` (github.com → auth key), and sets `user.signingkey`/`gpg.format=ssh`/`gpgsign=true`. Nudges the session-worktree discipline (CONVENTIONS §6). Rationale + caveats: CONVENTIONS §6 (worktree/merge-station discipline) + `deployment-manual.md` §0.1 (ext4 primary). | HD-265, CONVENTIONS §6 |
| [`git-bootstrap-win11.sh`](git-bootstrap-win11.sh) | **Windows 11 / git-bash mirror of git-bootstrap.sh.** Same clone/update/pull/`--reload` modes + `SRC`/`REPO`/`REMOTE`/`GITUSER`/`GITEMAIL` env. **`--ssh-auth` (HD-265, desktop-app path):** instead of `op`-CLI key-pull, relies on the **1Password desktop app SSH agent** on the named pipe `\\.\\pipe\\openssh-ssh-agent` — sets `core.sshCommand` to Windows OpenSSH in `.gitconfig-windows` (so git reaches the pipe), ensures `gpg.ssh.program = op-ssh-sign.exe`, flips `origin` HTTPS→SSH (fires the `.gitconfig-github` signing includeIf), and reports agent reachability. Requires the desktop app's SSH agent toggled ON. **No `~/.ssh/config` `Host github.com` block needed** (transport + signing both bypass file keys). | `deployment-manual.md` §0.4b (HD-265 win companion) |
| [`install-pi-wsl.sh`](install-pi-wsl.sh) | **pi.dev bootstrap for the WSL Debian runner** — installs pi natively inside Debian WSL (a standalone Node 22.19+ via the official [`pi.dev/install.sh`](https://pi.dev/install.sh), since apt's nodejs is the too-old 20.x), then deploys the **repo SSOT** to `~/.pi/agent`: `skills/` (artifact-ignore per HD-254: `net.json`, `__pycache__/**`, zero-byte skill-name markers), `pi-agent/` (`AGENTS.md` + `prompts/start.md`), tracked pi packages (`@ogulcancelik/pi-ssh-tools`, `pi-subagents`, `@season179/pi-worktree`, `pi-deepseek-optimized`) via `pi install`, and portable extensions from `pi-agent/extensions/`. **`remote-bash.ts` is intentionally not copied** — it hardcodes Windows `sshpass.exe`/`C:\…` paths (see the local file) and is not WSL-portable. Modes: `--pi-only` / `--config-only` / `--reload`. Run inside Debian (`wsl -d Debian -- bash …/scripts/install-pi-wsl.sh`). Companion to `git-bootstrap.sh` (ext4 primary = pi.dev + Ansible + editing). Owner: user (HD-259 pi.in-WSL companion). · [skills/platform-env/references/debian.md](../skills/platform-env/references/debian.md) |
| [`git-bootstrap.sh`](git-bootstrap.sh) | **Debian/WSL-side primary-repo setup** — makes WSL ext4 (`~/source/homelab`) the primary SSOT clone (pi.dev + Ansible + editing) instead of the slow `/mnt/d` drvfs mount. Idempotent: `clone` on first run, `update`/`pull` do `git fetch` + `git pull --ff-only origin main`; `--reload` is informational. Env-overridable: `SRC`, `REPO`, `REMOTE`, `GITUSER`, `GITEMAIL`. **`--ssh-auth` (HD-265, op CLI-only):** idempotent SSH auth + commit-signing setup — human `op` sign-in (SA can't read `Private`), pulls `GitHub sign`/`GitHub auth` SSH keys from the private vault into `~/.ssh` + `ssh-add`, flips `origin` HTTPS→SSH, writes `~/.ssh/config` (github.com → auth key), and sets `user.signingkey`/`gpg.format=ssh`/`gpgsign=true`. Nudges the session-worktree discipline (CONVENTIONS §6). Rationale + caveats: see the role's source (`IaC/ansible/roles/wireguard/`) + `docs/deployment-manual.md`. **Auto-load (HD-300):** once `--ssh-auth` has run, `bootstrap-runner.sh` installs a guarded `~/.bashrc` block that re-adds the keys to the agent on every shell start — no per-session `ssh-add` workarounds required. Verify with `git log -1 --format='%G?'` → `G`. | HD-265 (see also HD-300 + CONVENTIONS §6) |
| [`provision-vault.sh`](provision-vault.sh) | **Vault item seeder wrapper** — pulls the write-scoped SA token transiently from the VPS (`/etc/op/provision-token`, HD-143) over SSH and runs `provision-secrets.py --create --yes`. `--dry-run` lists the catalog. Run from the WSL runner. | `docs/deployment-secrets.md` |
| [`op-vault-export.py`](op-vault-export.py) | **HD-258 bulk 1Password pre-pass helper** — reads item NAMES (one per line on stdin) and emits `{name: {username,password,credential,bcrypt_hash}}` JSON via ONE `op item get --reveal` per item, resolved by field label/id (the `onepassword` lookup semantics). Replaces ~160 per-template `op` spawns with ~55 one-per-item. Fail-closed (rc!=0 + stderr on any unfetchable item). Consumed by `roles/docker_services/tasks/fetch-vault-pass.yml`. Never writes secrets to disk. | `docs/deployment-ansible.md` (HD-258) |
| [`restore-runner-key.sh`](restore-runner-key.sh) | **Canonical runner-key restore (any Debian runner)** — replaces bootstrap-runner.sh's throwaway `~/.ssh/id_ed25519` with the vault-canonical `ansible-admin_ssh` key (1Password = source of truth) and verifies the pair + fingerprint (public half only, CONVENTIONS §6). **Refuses to overwrite an existing, different key** — it idempotently no-ops when the canonical key is already in place, and fails loud otherwise with both fingerprints, because on a long-lived runner `~/.ssh/id_ed25519` is not necessarily the throwaway (measured on oldsrv: it held `oldsrv-rsync`, a hand-made key that is in no vault — destroying that silently breaks whatever on the far end authorizes it). **Superseded by the live 2026-09-22 oldsrv run:** that key is gone (HD-416 retired it), so what fires is a different refusal — "no `oldsrv-rsync` key to restore" — and the real path is `--force-throwaway`, which displaced the box's existing pair to `id_ed25519.pre-restore-<stamp>` (backup kept, still on the box). `--force-throwaway` is the deliberate override for the Phase-0 rebuild case and keeps a timestamped backup. Needs only the read-scope token file, so it works on a headless runner — this is the step that gives oldsrv (HD-407) the key the managed hosts actually authorize, since bootstrap-runner.sh's generated key is throwaway. | `docs/1password.md`, `deployment-tasks.md` Phase 0 |
| [`render-pi-config.py`](render-pi-config.py) + [`pi-config/models-spec.yml`](pi-config/models-spec.yml) | **Render the client model contract from ONE spec (HD-388)** — the spec is the SSOT for pi's `~/.pi/agent/models.json` **and** `~/.pi/agent/auth.json` (vendors `pi` / `pi_auth`), plus a Continue.dev `models:` block (`continue`, opt-in). Replaces the two hand-kept laptop files that `install-pi-wsl.sh` refuses to sync and that [pi-harness.md](../docs/pi-harness.md) §1 had to call its own "reference copy" because they carry bearer keys. `--check` is the drift gate (exit 1; structural diff; secret-bearing paths masked) and is proven **byte-identical against both live files**; renders are 0600 with a timestamped backup; `--vendor pi\|pi_auth\|continue\|all`, and `--out` with `--vendor all` refuses (that is how a test render lands in a live agent dir). **Credentials resolve at render time from 1Password** (`spark-llm_api`, `entrim_api`, `openrouter_api`, `opencode-go`); a provider whose item cannot be read is an ERROR naming the item, `--keep-legacy-credentials` applies only to providers explicitly marked `unmanaged:` (none today), and `auth.json` providers the spec does not name are **preserved with a printed NOTE** — a silent drop in a credential file is a lockout. Spec order == render order; the two `contextWindow` numbers (engine 262144 vs the gateway row's 245760 = window − reserve) sit side by side so nobody harmonises them. Rationale + limits: [pi-harness.md](../docs/pi-harness.md) §4/§4b. | HD-388, [pi-harness.md](../docs/pi-harness.md) |
| [`seed-runner-ssh.sh`](seed-runner-ssh.sh) | **Give a fresh runner the jump it needs (HD-407)** — every behind-NAT host is reached with `-o ProxyJump=vps` via `ansible_ssh_common_args`, and a newly seeded runner has no `Host vps` alias, so every jumped leg dies (`Could not resolve hostname`). Idempotently writes a marker-delimited `Host vps` block (`HostName`/`User`/`IdentityFile`/`IdentitiesOnly`/**`AddressFamily inet`**) into `~/.ssh/config`, TOFU-captures the jump host key, then **proves** the jump authenticates instead of declaring it. Never rewrites a block it did not write (a human's alias contract is left alone); a block it DID write is replaced in place when it predates the `AddressFamily` pin, because a stale block silently keeps a broken jump. `--check` reports without writing. The `AddressFamily inet` line is not decoration — a dual-stack runner prefers the VPS's AAAA, TCP/22 to it times out, and every jumped leg (including the runner's own address) reads `Connection timed out during banner exchange / UNKNOWN port 65535`. Rationale + the VPS-side ICMPv6 defect it works around: `docs/deployment-ansible.md` §Runner placement. | HD-407, `docs/deployment-ansible.md` |
| [`check_ssh_grants.py`](check_ssh_grants.py) | **SSH grant inventory gate (HD-416)** — read-only. Sweeps every `*/.ssh/authorized_keys*` + `/root/.ssh/authorized_keys` on the managed hosts (`ssh` + `ssh-keygen -lf`; a file stores the base64 blob so grepping a fingerprint matches nothing), derives the named set from the vault public halves (`domen_ssh`/`ansible-admin_ssh`/`ai_ssh`) + the `docs/deployment-secrets.md` grant table (short-prefix tolerant, `~~struck~~` = retired ⇒ must be ABSENT), and exits 1 unless named ≡ live. Reds on `UNKNOWN`, `RETIRED-BUT-PRESENT`, `WRONG-ACCOUNT` (named key under an account the table doesn't name — the exact shape of the `oldsrv-rsync` find), `UNPLACED` and `AMBIGUOUS` (reported, never guessed away). `--self-test` scores 9 fixtures offline (including the false readings the first live run caught in the checker itself); `--dump` keeps a dated capture; `--offline FILE` re-scores one. **Revokes nothing, ever** — revocation is the reversible hand sequence in `docs/deployment-secrets.md`. Scope hole it exposes: the HA guests are not in the sweep set, so they are unaudited. | HD-416, `docs/deployment-secrets.md` §Who is authorized where |
| [`check-vault-items.sh`](check-vault-items.sh) | **Vault coverage diff** — items required by the enabled services vs the vault, excluding glue-auto-seeded OIDC items. Scans per-service template dirs PLUS shared top-level templates and the VPS group_vars context (blind-spot fix 2026-08-22 — `ha-failover_api`, `cloudflare_api` class), AND the `*_item:`-class registry keys (`db_item`, `db_ro_item`, ...) parsed per-entry from ENABLED entries in `group_vars/vps.yml` + `home_servers.yml` — deploy-service.yml consumes those dynamically (`svc.<key>` lookups), so a literal-lookup grep can never see them (HD-244, live-found while seeding `metabase-forgejo_ro`). Items minted by deploy-time glues are EXCLUDED from MISSING: OIDC client items via literal name pairs in `authentik-secret-egress.sh.j2`; LiteLLM scoped keys via their group_vars SSOT — every spec record inside a top-level `*_scoped_keys:` list routes its `vault_item:` into the GLUE set, while `db_item`/`db_ro_item` on service entries stay NEEDED (owner-seeded catalog items; the litellm glue template is Jinja-rendered and carries no greppable literals; gap live-found 2026-08-26). Non-VPS role items (router/nut/Pi) deliberately out of scope. Flags: `--strict` exits non-zero when the MISSING-and-not-glue list is non-empty (default informational); `--root DIR` + `--fake-vault FILE` drive the committed self-test (`scripts/testdata/check-vault-items/run.sh`, wired into validate-all.sh — proves the registry-key catch AND the no-false-positive/glue-exclusion paths without touching the real vault). Run before a deploy to get the exact "create these" list. | `deployment-tasks.md` Phase 1, `scripts/testdata/check-vault-items/` |
| [`bootstrap-runner.sh`](bootstrap-runner.sh) | **Runner bootstrap for any Debian host (laptop today; oldsrv as the on-site control node, HD-407)** — relocated here from `IaC/bootstrap-ansible-client/` (HD-256). Idempotent: system update + prereqs, 1Password CLI install, `~/ansible-venv` creation + collection install from `IaC/ansible/requirements.yml` (self-derived `$REPO`), SSH key gen, write-scoped SA token file, **auto-load `github_signing` + `github_auth` into the ssh-agent on every shell start** (HD-300, guarded `ssh-add -l` grep + on-disk existence checks; appended next to the existing `SSH_AUTH_SOCK` block — the agent socket is already wired, keys were the missing half), passwordless sudo. Usage: `bash scripts/bootstrap-runner.sh` (then `source ~/.bashrc`). Referenced by `ansible-run.sh` / `restore-runner-key.sh` / `git-bootstrap.sh` as the Phase-0 prerequisite. **Opt-in flags (bare invocation = the historical laptop path, unchanged):** `--no-upgrade` skips the distro upgrade and installs prereqs only ("set up my runner" must not become a 59-package upgrade under a production Docker host); `--token-stdin` seeds the SA token from stdin so it never lands in shell history or on a readable prompt (same 0600 file, never printed); `--no-sudoers` writes nothing and instead verifies the existing passwordless-sudo grant, failing loud without it. Unknown arguments now REFUSE — live 2026-09-20: the script had no argument parsing at all, so an unexpected `--help` was ignored and the FULL bootstrap executed on the laptop. | `deployment-tasks.md` Phase 0, `deployment-manual.md` §Phase 0 |
| [`testdata/self-converge-guard/run.sh`](testdata/self-converge-guard/run.sh) | **HD-413 guardrail — runtime verdict matrix** — executes the REAL guard (`import_tasks` of the IaC file) against a throwaway inventory holding exactly two hosts: this machine via `ansible_connection=local` (so *controller == target* is genuinely true) and a decoy that cannot be, with **inert stub roles** so nothing can converge anything; asserts 15 verdicts (unfiltered self-run, `--tags network/netd/base/hosts/storage/untagged/zfs_exporter/wireguard/hardening` → refused; `--check` of check-safe roles and `--tags docker_services,<svc>` → allowed; the off-box target → always allowed). Exists because the first draft passed every static check while allowing an unfiltered self-converge of the netdev role: `ansible_run_tags` is a **tuple**, so `ansible_run_tags == ['all']` is silently always false (membership is the only correct form). SKIPs itself where ansible or `uname` is absent (native Windows). Wired into `validate-all.sh` as item 17. | `deployment-ansible.md` §Self-converge guardrail |
| [`gen-custom-script.sh`](gen-custom-script.sh) | **VPS post-install generator** — relocated from `IaC/host/vps/` (HD-256). Injects the two real 1Password public keys (`domen_ssh`, `ansible-admin_ssh`) into a copy of `IaC/host/vps/post_install.sh` → git-ignored `post_install_with_secrets.sh` for the netcup SCP Custom-Script field. Self-derives `$REPO`; fail-loud guards (placeholders replaced, doubled-prefix, exact lines) per HD-209; output in `IaC/host/vps/`. Usage: `bash scripts/gen-custom-script.sh`. | `deployment-preseed.md` §netcup, `deployment-manual.md` §Phase 0.5 |
| [`gen-media-post-install.sh`](gen-media-post-install.sh) | **Homelab USB-media post-install generator** — relocated from `IaC/host/` (HD-256). Injects three real 1Password public keys (`domen_ssh`, `ansible-admin_ssh`, `ai_ssh`) into a copy of `IaC/host/post_install.sh` → git-ignored `post_install_with_secrets.sh`; optional mountpoint arg copies to `<media>/preseed/post_install.sh`. fail-closed HD-209 guards. Usage: `bash scripts/gen-media-post-install.sh [/media/usb]`. | `docs/deployment-preseed.md` §media-layout, `deployment-manual.md` §Phase 1a |
| [`get-bootstrap-keys.sh`](get-bootstrap-keys.sh) | **MikroTik Phase 1.5 bootstrap .pub materializer** — pulls the two SSH public keys the rendered RouterOS .rsc files import on every device (`admin.pub` from `domen_ssh`, `ansible.pub` from `ansible-admin_ssh`, both in vault `Homelab-ansible`) and writes them to the gitignored `IaC/router/rendered/` dir alongside the .rsc files. Asserts the expected SHA256 fingerprint before writing (fail-loud on vault rotation drift). Public-key only — never reveals the private half. Usage: `bash scripts/get-bootstrap-keys.sh` (or with a custom output dir). | `deployment-manual.md` §Phase 1.5 |
| [`apply-converge.yml`](../IaC/ansible/playbooks/apply-converge.yml) | **Ansible-path RouterOS apply** — the playbook twin of [`routeros-apply-delta.sh`](routeros-apply-delta.sh): uploads a rendered `*_converge.rsc` (or any delta) to a device via SCP (ansible SSH identity), then `/import`s it over the RouterOS API. Pulls the `ansible-admin_ssh` private key via the `onepassword` lookup (direct field value — no shell wrapper), writes it to a `0600` temp file, **verifies it loads with `ssh-keygen -y` before SCP** (HD-309 hardening — same fail-loud guard as the `.sh`; a mangled vault field surfaces as `error in libcrypto` at SCP otherwise), pins the device host key (TOFU), and reports the import. Compatible with `ansible-run.sh`: `bash scripts/ansible-run.sh playbooks/apply-converge.yml -e converge_rsc=rb4011_converge.rsc` (or `-e target=switch`). Note: the final API `/import` step requires `librouteros` in the runner interpreter — if that is missing use the SSH-import path (`routeros-apply-delta.sh`). | `docs/network-ops.md` §Apply workflow |
| [`routeros-apply-delta.sh`](routeros-apply-delta.sh) | **MikroTik delta-`.rsc` apply helper (HD-309)** — automates the manual SCP→`/import` loop from [network-ops.md](docs/network-ops.md) §Apply workflow: pulls the `ansible-admin_ssh` private key from 1Password via **`op read`** (canonical clean PEM — `op item get --reveal` pipes a quote/newline wrapper that corrupts the file into OpenSSH's `error in libcrypto`, a self-inflicted trap hit 2026-09-01), **verifies the key loads** (ssh-keygen), pins the device's current SSH host key (TOFU, rotates at reset), SCPs the rendered delta from `IaC/router/rendered/`, then `/import`s it over SSH. Never prints key material. Usage: `bash scripts/routeros-apply-delta.sh <device-host> <delta-file>` (e.g. `10.10.99.1 rb4011_wan_delta.rsc`). | `docs/network-ops.md`, `deployment-manual.md` §1.5.3c |
| [`ansible-network-hop.sh`](ansible-network-hop.sh) | **⚠️ OBSOLETE (2026-09-08) — RouterOS playbook via Pi-99 hop, superseded by direct Mgmt-99.** Kept (not deleted) for history. The laptop now reaches the Mgmt plane **directly** via the Windows Mgmt99 vNIC (`wsl-nat-resolv.ps1 -EnableMgmt99`), so playbooks run **directly** against the `.99` IPs — no SSH local-forward through `pi`. If the hop is ever needed again (laptop off-Mgmt), it still works: SSH-local-forwards the device API through `pi` (traffic originates mgmt-sourced, passes router/switch lockdown) and execs `ansible-run.sh` against the loopback host/port. Usage: `bash scripts/ansible-network-hop.sh <router\|switch> <playbook> [--check --diff]`. | `docs/network-ops.md` §Apply workflow, `deployment-manual.md` §Phase 1.5 |
| [`ak-shell.sh`](ak-shell.sh) | **Authentik container shell** — runs a Python snippet inside `authentik-worker` (VPS) over the runner's key-file SSH, base64-safe (no multi-layer quoting). Call INSIDE WSL via script-file indirection; **arg mode is the reliable path** — stdin (`-`) mode fails intermittently through the cmd→wsl chain (Wave-3 R5). Used for token minting / model queries / one-shot fixes — see `docs/services-authentik.md` live-deploy findings. | `docs/services-authentik.md` |
| [`collect-smart-live.sh`](collect-smart-live.sh) | **SMART report** from a SystemRescue live ISO on the NAS (HP MicroServer Gen8) — `smartctl --scan` across internal + SilverStone miniSAS bays → `/tmp/smart-report-*.txt` (host, disk map, SMART attrs). | `docs/hardware-nas.md` |
| [`collect-smart.ps1`](collect-smart.ps1) | **Windows PowerShell sibling of `collect-smart-live.sh`** — SMART report from a Windows host (laptop / Windows PE / regular Windows session): `Get-PhysicalDisk` + `Get-Disk`/`Get-Volume` context, `smartctl` invocation when smartmontools is on PATH (or skips with a friendly hint), writes `smart-report-<hostname>-<ts>.txt` to the script's directory. **Windows-only by design** (intentional sibling, NOT a migration target — see classification table line below). Cross-platform matrix lives in the prose block immediately following this registry. | `docs/hardware-oldsrv.md`, `docs/hardware-nas.md` |
| [`wsl-nat-resolv.ps1`](wsl-nat-resolv.ps1) | **Idempotent WSL2 network fix — NAT mode + auto-resolv (admin; replaces `wsl-vlan-trunk.ps1`)** — makes WSL Debian work on ANY network the Windows host can reach (wired LAN, WiFi, phone hotspot) without manual per-network edits: (1) `.wslconfig` → `networkingMode=Nat` (removes any `Bridged`/`vmSwitch` line — the old bridged setup pinned `eth0` to the homelab static and died on WiFi/hotspot with NO-CARRIER → no IP → `Network is unreachable`); (2) `/etc/wsl.conf` → drop `generateResolvConf=false` (keeps `systemd=true`) so WSL regenerates `/etc/resolv.conf` every boot from the Windows/default-switch resolver (Option A — DNS follows Windows: homelab DHCP chain at home, hotspot DNS when away); (3) disable `/etc/systemd/network/10-eth0.network` (the homelab static `10.10.1.81/24` + gw + mgmt route is NAT-irrelevant and was the off-LAN breaker); (4) `wsl --shutdown` + restart; (5) verify eth0 has an address + default route + auto-resolv + DNS + HTTPS. **Idempotent / safe re-run** — reads-before-writes, takes `.bak-nat`/`.disabled-homelab` backups, tracks a `$changed` flag and **skips the WSL restart when nothing changed**. Optional `-EnableMgmt99` creates the Windows **Mgmt99 vNIC** (`10.10.99.80` + IP forwarding ON) — **this is what makes the Mgmt VLAN 99 reachable from the laptop/WSL (2026-09-08: the direct-`.99` SSH aliases `router`/`switch`/`pi99` replace the old ProxyJump-`pi` hop)** — **on-site use only**: HD-398 owner decision A (2026-09-19) settled that the Mgmt plane is a same-site plane (no tunnel route, no `ProxyJump`), and `ssh oldsrv` moved to the Home leg at the same time; alias SSOT = `docs/network-vpn.md` §The laptop alias contract. It is the current standard on-site path, not a legacy extra. Encoding: ASCII-only, LF, no BOM (PowerShell misparses BOM-less UTF-8 `.ps1` with non-ASCII). | `docs/network-vlans.md` (Laptop row), `deployment-manual.md` §0.4b |
| [`collect-disk-facts.sh`](collect-disk-facts.sh) | **Phase 1a read-only disk-facts collector** for a Debian installer/live USB (pre-reinstall facts on oldsrv/nas; generalized sibling of `collect-smart-live.sh` that writes to the stick instead of /tmp): `/dev/disk/by-id/` ↔ device reverse map (HD-128 NVMe by-id capture — the doc values are Windows-derived EUI64, unverified on Linux), model/serial/size/transport per disk, SMART key attrs + full `smartctl -x` dump when smartmontools is present, `lsblk`/`blkid`/`nvme list` context (existing-FS awareness before the Pool-Creation Runbook's wipefs step). Writes `disk-facts-<label>-<ts>.txt` to the root of the writable USB stick (auto-detects mounted removable media or auto-mounts a boot-stick partition; loud fallback if none — a plain dd'd ISO9660 stick is read-only by design). POSIX sh — runs in the d-i busybox console (`Ctrl+Alt+F2`) and live sessions alike. Usage: `sudo sh collect-disk-facts.sh [oldsrv\|nas]`. Strictly read-only toward disks. | `deployment-tasks.md` Phase 1a, `docs/hardware-oldsrv.md`, `docs/hardware-nas.md` (HD-207 runbook pre-flight), `todo.md` HD-128/HD-207 |

The raw SMART/drive data snapshots live in [`reports/`](../reports/) (`laptop-sda.txt`,
`laptop-sdb.txt`, `oldsrv-sda.txt`, `oldsrv-sdb.txt`, `smart-report-*.txt`,
`disk-facts-oldsrv-*.txt` from collect-disk-facts.sh, `DOMENPC-cpuz.txt`) — CI / audit
inputs, not tools. `collect-smart.ps1` is the Windows PowerShell sibling of
`collect-smart-live.sh` (same purpose, runs from Windows instead of a live ISO).

---

## Notes & conventions

- **Never edit a `-generated` doc directly** — change the SSOT (`group_vars/*.yml`, `rack-connections.json`) and re-render, then `git diff --exit-code` to confirm.
- **Judge `validate-all.sh` by its EXIT CODE, never by a filtered view of its output.** Live
  2026-09-25: `bash scripts/validate-all.sh 2>&1 | grep -iE "FAIL|OK: all validators" && git commit … &&
  git push` — the pipeline's status is GREP's, and grep exits 0 when it finds something, so a run that
  printed `FAIL: 2 internal IP literal(s)` happily committed and pushed to `main`. Run the gate, check
  `$?` (or `&&` on the bare command), and only then filter the log for reading: `bash
  scripts/validate-all.sh >/tmp/v.log 2>&1; echo "exit=$?"; grep -iE 'FAIL|OK: all' /tmp/v.log`.
- **No secrets outside 1Password `Homelab-ansible`.** Render scripts that need one (e.g. `render-routeros.yml` → device `.rsc`) resolve via `lookup(...)`; the pure-Python renderers here intentionally touch **no** secrets (they read only the YAML/JSON SSOT).
- **Windows vs Linux:** the Python scripts run cross-platform (PyYAML + Jinja2). Ansible playbooks (`render-docs.yml`, `render-routeros.yml`) require WSL/CI on this machine. Since the WSL ext4 move (HD-259) the Debian/WSL primary is the canonical runner, so scripts must not assume Windows-only launchers/paths; the gate enforces this (see §Portability). Like `ansible-run.sh`, the `validate-all.sh` `--syntax-check` gate exports `ANSIBLE_CONFIG`/`ANSIBLE_ROLES_PATH` from the repo root so roles resolve (HD-256).
- **Live converges run ASYNC (HD-370 live lesson 2026-09-15):** a full/`--tags docker_services` converge takes **10–30+ min** (Bulk 1P pre-pass, authentik glue, per-service compose up, restart guards, Technitium seed). Running it in the foreground of a tool/terminal shell (or behind the container host's own timeout) lets an outer timeout kill it **mid-restart** — live evidence: a `timeout 600` killed a VPS converge right at `docker compose restart traefik-tailnet`, leaving `tailscale-sidecar Exited (128)` and a `cannot join network namespace of a non running container` failure on the *next* run's restart guard. Always launch converges **detached** and poll:
  ```bash
  # from the WSL runner (same env exports as ansible-run.sh)
  nohup ansible-playbook -i IaC/ansible/inventory.ini playbooks/…yml \
      --limit hosts --tags … > /tmp/converge-<host>-<ts>.log 2>&1 &
  # then poll: tail -f /tmp/converge-<host>-<ts>.log → PLAY RECAP
  ```
  `--check` is the only safe foreground form (fast, read-only). If a converge is killed mid-restart, the fix is idempotent: re-run the same playbook (the restart-on-config-change guard restarts the stack cleanly once the sibling containers are up).
- **Scoped service deploys need BOTH tags (live 2026-09-17, HD-382):** `-e docker_services_scope=<svc>` alone does **not** deploy the service. `deploy-service.yml` tags every task `"{{ svc.name }}"`, so a run with only `--tags docker_services` iterates the loop (the include even prints `included: … item=<svc>`) and then tag-filters **every inner task** — a silent no-op that reports `changed=0/1, failed=0` while the rendered compose stays stale on the host. Correct form: `--tags docker_services,<svc>` (the HD-220 intent as the task comments state). When a scoped run looks suspiciously cheap, verify with `docker inspect` / the rendered file rather than trusting the RECAP.
- **Never run a `docker_services` converge with `--diff` (secret hygiene, live 2026-09-17):** the 1P bulk pre-pass is `no_log`, but the **template diff of a rendered `docker-compose.yml` is not** — `--check --diff` printed live `LITELLM_MASTER_KEY` / `OPENROUTER_API_KEY` values onto stdout into the run log (CONVENTIONS §6: never print a secret VALUE). `--check` alone stays safe; the *diff* is what leaks. Verify renders from the template or diff the rendered host file with secret lines filtered; purge any log that caught values (`shred -u`) instead of grepping it.
- **Validator wiring:** anything added to `validate-all.sh` must be listed here; keep the comment block in `validate-all.sh` in sync.
- **Config / "what reads what":** `CONVENTIONS.md` §8 is the canonical index of rules these scripts enforce; this file is a *map* — the owning specs remain authoritative.

---

## Portability (Debian/WSL ext4 primary, HD-256)

Every script must run natively in WSL Debian; paths use `$HOME`, `$SRC`, `$REPO` or the script's own self-derived `SCRIPT_DIR` rather than drive-letter/temp-var literals. `bash -n` (for `*sh` shebangs) + `python3 -m py_compile` (for `*.py`) are enforced in `validate-all.sh` — future scripts that regress portability fail the gate.

| Script | Shell/PY | Windows-only assumption? | Status |
|--------|----------|--------------------------|--------|
| `ak-shell.sh` | bash | none (self-derived path comment only) | 🟢 portable |
| `ansible-run.sh` | bash | none (paths from `$REPO`/`$HOME`) | 🟢 portable |
| `ansible_query.py` | python3 | none (tree root self-derived from the script's own path; re-execs into `~/ansible-venv/bin/python3`, no Windows idiom — needs PyYAML+Jinja2 from that venv) | 🟢 portable |
| `check-vault-items.sh` | bash | none (`$HOME`, self-derived `REPO`) | 🟢 portable |
| `collect-disk-facts.sh` | POSIX sh | none (live/Linux only; `/tmp` + `mktemp`) | 🟢 portable |
| `collect-smart-live.sh` | bash | none (live SystemRescue/Linux only; `/tmp`) | 🟢 portable |
| `collect-smart.ps1` | PowerShell | **Windows-only by design** (SMART from a Windows host; intentional sibling of `collect-smart-live.sh`) | ⬛ exception (not a migration target) |
| `wsl-nat-resolv.ps1` | PowerShell | **Windows-only by design** (WSL2 NAT + auto-resolv network fix; replaces the bridged `wsl-vlan-trunk.ps1`) | ⬛ exception (not a migration target) |
| `gen-htpasswd.py` | python3 | none | 🟢 portable |
| `next-hd.sh` | bash | none (paths from `$REPO`; no Windows idioms) | 🟢 portable |
| `git-bootstrap.sh` | bash | none (paths from `$SRC`/`$REPOPATH`/`$HOME`; `/mnt/d` mention is a comment) | 🟢 portable |
| `git-bootstrap-win11.sh` | bash | none (paths from `$USERPROFILE`/`$SRC`/`$REPOPATH`; Windows OpenSSH via `core.sshCommand`) | 🟢 portable |
| `guard-session.sh` | bash | none (`mktemp`/`${TMPDIR:-/tmp}`; Windows comment only) | 🟢 portable |
| `install-pi-wsl.sh` | bash | none (paths from `$SCRIPT_DIR`/`$HOME`; `/mnt/c` is a comment) | 🟢 portable |
| `op-vault-export.py` | python3 | none | 🟢 portable |
| `provision-secrets.py` | python3 | **`py -3` default** → now auto-detects `python3`/`py -3`/`sys.executable` and probes `import bcrypt` (falls back on a broken `BCRYPT_PY` override); HD-205 | 🟢 portable (HD-256 fix) |
| `check_dns_seed_drift.py` | python3 | none | 🟢 portable |
| `check_secrets.py` | python3 | git + stdlib | 🟢 portable |
| `check_merge_markers.py` | python3 | git (`ls-files`) + stdlib; `--self-test` needs a working `git init`/`commit` | 🟢 portable |
| `check_md_tables.py` | python3 | stdlib + `git ls-files` | 🟢 portable |
| `check_iac_backend_strings.py` | python3 | stdlib only (reads the two IaC trees) | 🟢 portable |
| `check_iac_backend_strings.py` | python3 | stdlib only (reads the two IaC trees) | 🟢 portable |
| `check_traefik_host_rules.py` | python3 | none | 🟢 portable |
| `adapt-vllm-dashboards.py` | python3 | reads `/tmp/vllm-dash/*.json` in adapt mode (write the downloads there, or use `--inplace`); dashboard dir self-derived from the script path | 🟢 portable |
| `build-llm-dashboard.py` | python3 | reads the three repo dashboard JSONs, writes `homelab-llm.json` in the same dir; no network, no `/tmp` dependency | 🟢 portable |
| `provision-vault.sh` | bash | none (paths from `$REPO`; `python3` already) | 🟢 portable |
| `render_all.py` | python3 | none | 🟢 portable |
| `render_network_addresses.py` | python3 | none | 🟢 portable |
| `knx-hass-gen.py` | python3 | `xknxproject` for generation and `--check`; nothing for `--self-test` — ⚠ the dep is declared nowhere in the repo (measured 2026-09-25) | 🟡 dep missing |
| `render_rack_connections.py` | python3 | none | 🟢 portable |
| `restore-runner-key.sh` | bash | none (paths from `$HOME`) | 🟢 portable |
| `seed-runner-ssh.sh` | bash | none (`$HOME`, `ssh`/`ssh-keygen`) | 🟢 portable |
| `check_ssh_grants.py` | python3 | `op` CLI + SA token file (doc-only fallback), `ssh` | 🟢 portable |
| `routeros-apply-delta.sh` | bash | none (self-derived `$REPO`, `mktemp`, python3 verify) | 🟢 portable |
| `sync-skills.sh` | bash | none (paths from `$HOME`/`$REPO`; POSIX `tr`/`od`, no Windows idioms) | 🟢 portable |
| `bootstrap-runner.sh` | bash | none (self-derived `$REPO`; `$HOME`/`~` for venv/token/ssh) | 🟢 portable |
| `gen-custom-script.sh` | bash | none (self-derived `$REPO`; output to `IaC/host/vps/`) | 🟢 portable |
| `gen-media-post-install.sh` | bash | none (self-derived `$REPO`; output to `IaC/host/`) | 🟢 portable |
| `testdata/check-vault-items/run.sh` | bash | none | 🟢 portable |
| `testdata/check-vault-items/run.sh` | bash | none | 🟢 portable |
| `validate-all.sh` | bash | `py -3` fallback is fine (Windows path); new sweep uses `python3` | 🟢 portable |
| all other `*.py` (check_doc_*, validate_*, render_*) | python3 | none | 🟢 portable |

> Portability discipline is owned by `CONVENTIONS.md`; this section is the per-script map — the owning specs remain authoritative.


---

## Scripts elsewhere in the repo (IaC-deployed payloads — NOT runner tools)

These run **on target hosts** (or are Ansible-deployed) and are consumed by path from Ansible roles, so they stay in `IaC/` and are **not** relocated to `scripts/` (HD-256). They are script-like artifacts, kept here as a complete registry; their own owning docs remain authoritative.

| Script | Purpose / runs on | Deployed by | Owning spec |
|--------|-------------------|-------------|-------------|
| [`client/office-bridge/`](../client/office-bridge/) | **Native Windows/COM Office MCP bridge** (Word/Excel/PPT live-edit tools into Open WebUI) — NOT a runner tool; a first-class tracked repo component (HD-106). Version-pinned (`requirements.txt` + `excelcli.exe` SHA256), Renovate-tracked (`pip_requirements`). Runs on each **Windows 11 client** next to the running Office app. | per-client `install.ps1`/`update.ps1` over the Headscale tunnel — **not Ansible** (by design; Ansible manages server Docker) | `docs/services-office.md` (HD-106–111), folder README |
| [`IaC/host/post_install.sh`](../IaC/host/post_install.sh) | shared host bootstrap (ansible-admin + ai-debug + sshd); placeholder-only | preseed `late_command` / media | `docs/deployment-preseed.md` |
| `IaC/host/vps/post_install.sh` | VPS bootstrap (two keys, no ai-debug) | netcup SCP Custom-Script | `docs/deployment-preseed.md` §netcup |
| `IaC/host/pi/first-boot-config.sh` | Pi 4 first-boot SSH enable + key stub — **fallback path**: the normal route is Raspberry Pi Imager's advanced settings (docs say so) | Raspberry Pi OS Lite 64-bit image — ⚠ NOT the raspi.debian.net rebuild, which boots this Pi 4 to a rainbow screen | `docs/deployment-preseed.md`, HD-201 |
| `ai-diag` (`roles/ai_diag/files/ai-diag`) | restricted diagnostic dispatcher (ai-debug user) | ai_diag role | `docs/security.md` |
| `ha-failover-api.py`, `ha-sync-rsync` (`roles/home_assistant/files/`) | HA failover HTTP trigger + restricted rsync wrapper | home_assistant role | `docs/smart-home-failover.md` |
| `roles/*/templates/*.j2` (`authentik-secret-egress`, `litellm-bootstrap-keys`, `traefik-cert-pull`, `ha-cert-sync`, `ha-config-sync`, `ha-failover*`, `upssched-cmd`, `push-services`, `sync-authentik-users`, `syncoid-run`, `certs-rename`) | Jinja-rendered payloads run on hosts | Ansible converge / docker-compose | respective owning docs |
| [`authentik-secret-egress.sh.j2`](../IaC/ansible/roles/docker_services/templates/authentik-secret-egress.sh.j2) | secret-egress **sink** glue — reads OIDC client creds from the Authentik API and writes them down into 1Password items (write-only-if-changed); per-provider fetch+compare+write parallelized via `xargs -P` (each worker owns one distinct item) | docker_services role (`prepass-authentik.yml`) | `docs/deployment-ansible.md` (§Tags & surgical, HD-146/162), `docs/services-authentik.md` |
| [`litellm-bootstrap-keys.sh.j2`](../IaC/ansible/roles/docker_services/templates/litellm-bootstrap-keys.sh.j2) | LiteLLM virtual-key **source** glue — vault-state-first: reads each 1P item, probes the live LiteLLM key, mints+stores only when the vault value is absent. **SERIAL main loop** (2026-08-28 revert: the parallel probe read via a `docker exec -i` heredoc that breaks under `xargs -P` — see §Parallel-1P live lesson) | docker_services role (litellm service deploy pass) | `docs/deployment-ansible.md` (§Tags & secrets), `docs/services-ai.md` (LiteLLM spine) |
### Parallel 1Password operations — the concurrency contract (HD-269)

Three distinct mechanisms touch 1Password. They are NOT alternatives — they are a **pipeline across runtime layers**. Two of them parallelize a different direction each; the LiteLLM source glue is **serial** (its probe reads instructions via a `docker exec -i` heredoc that does not survive `xargs -P` — reverted 2026-08-28, see the live lesson below). They share one invariant: the **1Password rate-limit budget**. Keep them three; if a THIRD CONTROL/egress parallel loop ever appears, copy the bounded fan-out pattern below (not merge the scripts).

| Mechanism | Layer | Direction | Concurrency | Rate/limit knob | Purpose |
|---|---|---|---|---|---|
| `scripts/op-vault-export.py` | **control-node** (Python, `ThreadPoolExecutor`) | bulk **read** of static vault items into the `vault` dict | parallel per-item `op item get --reveal` | `op_vault_workers` (default 6) | HD-257/258 bulk pre-pass — ONE pass replaces ~160 per-template `op` lookups |
| `authentik-secret-egress.sh.j2` | **host** (bash, `xargs -P`) | **write** (sink): Authentik API → 1Password OIDC items | per-provider fetch+compare+write; **each worker owns a DISTINCT item** so parallel item-writes are safe | `OP_PARALLEL` (default 6) | HD-146/162 secret-egress OIDC cred seeding (write-only-if-changed) |
| `litellm-bootstrap-keys.sh.j2` | **host** (bash, **serial** — reverted 2026-08-28) | **read + mint** (source): 1Password → LiteLLM probe → mint back to 1Password | **SERIAL main loop** (`probe_key` reads its probe via a `docker exec -i` heredoc, which does not survive `xargs -P`; a parallel attempt failed live rc 3 and was reverted). mint/store serial (no alias race) | n/a (serial) | HD-247 LiteLLM scoped virtual-key bootstrap (vault-state-first) |

**The shared budget (persisted lesson, HD-268 15→6):** keep concurrent `op` calls ≤ **6 reads** (default), ≤ **4–6** when a run also mutates; only `curl`/`docker exec` HTTP **probes** may fan out wider. Never exceed this on a live converge — hosted 1P rate-limits can block for **15min+**.

**Extensibility rule:** to add a THIRD service/glue that touches 1Password, copy the fan-out harness (bounded `xargs -P "${OP_PARALLEL:-6}"`, `export -f <worker>`, worker writes an outcome **line** to a shared temp file, aggregate AFTER all workers finish — never read exit codes for the rc taxonomy, carry it in the lines). Do NOT extract a shared code file: these three render to self-contained `/usr/local/sbin/*.sh` on hosts where a lib dependency is a recovery risk, and each has a different write-concurrency/safety model. Duplication buys host-resilience; ossify the pattern HERE instead.

**⚠️ Live lesson 2026-08-28 (HD-269 Step-1 re-baseline):** a parallel loop whose worker bodies **read stdin via heredoc into an external CLI** does NOT survive `xargs -P` fan-out. The LiteLLM glue's `probe_key` calls `docker exec -i ... <<'PYEOF'` (python reads instructions via stdin); under `xargs -P` the worker's stdin is a broken pipe, so the probe returns 000 / hangs → the glue failed rc 3 in the live converge (155s / 12 retries) and was **reverted to the serial loop** (the authentik glue, whose workers only `curl`+`op read` and never feed stdin, parallelizes cleanly: 21-22s → 11.94s). **Rule:** a fan-out worker may use `curl`/`op`/plain subprocess, but if its body pipes a heredoc into `docker exec -i`/`ssh`/etc, run that probe SERIALLY (or give it a stdin-neutral invocation). Confirm with `op service-account ratelimit` to identify a healthy `token read` budget (the current homelab SA sits at ~6 reads / 1000 well below the cap; the account `read_write` level was 230/1000 — the 15→6 lesson keeps us off that wall).


---

*Last updated 2026-08-28 · scripts/ has no separate CI hook beyond `validate-all.sh`.*