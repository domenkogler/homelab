# T9 — Full validation + stale-TODO comment cleanup

**Objective:** Run the complete acceptance suite, confirm all 8 new templates +
31 entries validate, and remove/point the now-stale "TODO (create templates)"
comments so the repo no longer claims the templates are missing.

**File scope (ONLY these):**
- `IaC/ansible/group_vars/home_servers.yml` (remove the `# TODO (create templates): …` comment line only)
- `docs/deployment-ansible.md` (the `# TODO (create templates): homepage, …` line only)
- `IaC/README.md` (the `# TODO (create): homepage/, …` line only)
- No other files.

**Subtasks (do in order; tick as you go):**
- [ ] 1. Remove the `# TODO (create templates): homepage, renew…, metabase` comment in `group_vars/home_servers.yml` (all 8 entries now exist in the list)
- [ ] 2. Remove the `# TODO (create templates): homepage, renovate, doco-cd, prometheus, loki, blackbox-exporter, signal-cli-rest-api` line in `docs/deployment-ansible.md` (section: docker_services list in group vars)
- [ ] 3. Remove the `# TODO (create): homepage/, renovate/, doco-cd/, prometheus/, loki/, blackbox-exporter/, signal-cli-rest-api/` line in `IaC/README.md` (file-layout tree) and, if present in the same doc, the "Missing templates (homepage, …)" sentence in the Note paragraph — leave everything else verbatim
- [ ] 4. Run the full validator (must PASS all 31) and the docs-template regression script
- [ ] 5. Run the secret-hygiene grep (must find nothing)
- [ ] 6. Run the global acceptance git checks
- [ ] 7. Do NOT touch anything outside File scope

**Difficulty:** 1

**Model:** openrouter/free

**Capabilities:** image/text->text ·T·J·R·V — mechanical cleanup + verification.

**Validation:**
```
py -3 scripts/validate-docker-services.py
# pass = prints "PASS: 27 docker_services templates valid" (19 existing + 8 new in home_servers.yml) and exits 0
py -3 scripts/validate_doc_templates.py
# pass = prints "network-addresses.md.j2 rendered OK" and exits 0
rg -n "<secret|PASSWORD=.*[A-Za-z0-9]{8}" IaC/ansible/templates/docker_services
# pass = no output (exit 1 is OK — nothing found)
```

**Executor prompt (paste to the worker):**
> Environment: windows11/git-bash — pin CWD to repo root, use `py -3`, UTF-8 no-BOM/LF, forward-slash paths; see index (`## Environment`).
>
> You are executing task T9. Read THIS file. Remove ONLY the three stale TODO-comment lines listed (verify each still matches before deleting; if the line text drifted, report it instead of guessing). Then run the exact Validation commands and report PASS/FAIL for each. On FAIL, stay `in-progress`, paste the output, and STOP.