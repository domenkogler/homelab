# T5 — homepage template + list entry

**Objective:** Create `IaC/ansible/templates/docker_services/homepage/docker-compose.yml.j2`
(family launchpad at `kogler.si` + alias `home.kogler.si`, Authentik Forward-Auth,
Docker-socket health checks) per `docs/services.md` + `docs/interfaces.md` and add
the `homepage` entry to `docker_services` in `group_vars/home_servers.yml`.

**File scope (ONLY these):**
- `IaC/ansible/templates/docker_services/homepage/docker-compose.yml.j2` (new)
- `IaC/ansible/group_vars/home_servers.yml` (add one list entry only)

**Exact template content:**
```
# homepage docker-compose.yml — family launchpad (kogler.si root + home.kogler.si alias)
#   traefik-public behind Authentik Forward-Auth (no anonymous access).
#   Health checks via the Docker socket; services.yaml + widgets.yaml are
#   auto-generated by the docker_services post-deploy hook (homepage_*.yaml.j2).
#   See docs/services.md + docs/interfaces.md. Secrets via
#   {{ lookup('community.general.onepassword', '<name>', vault=op_vault) }} — none needed here.
services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    restart: unless-stopped
    environment:
      HOMEPAGE_ALLOWED_HOSTS: "{{ domain_local | default('kogler.si') }}"
      TZ: "{{ timezone | default('Europe/Ljubljana') }}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro   # health-check status via Docker API
      - ./config:/app/config                            # services.yaml + widgets.yaml (auto-generated)
    networks:
      - traefik-public
    labels:
      traefik.enable: "true"
      traefik.http.routers.homepage.rule: "Host(`kogler.si`) || Host(`home.kogler.si`)"
      traefik.http.routers.homepage.entrypoints: websecure
      traefik.http.routers.homepage.tls.certresolver: letsencrypt
      traefik.http.routers.homepage.middlewares: authentik-forward-auth@file
      traefik.http.services.homepage.loadbalancer.server.port: "3000"
networks:
  traefik-public:
    external: true
```

**List entry to add** (dashboard group, near grafana/n8n):
```yaml
  - { name: homepage,        template_dir: homepage }
```
> No `subdomain` override: the root `kogler.si` + alias `home.kogler.si` are the
> Traefik labels inside the template (SSOT `docs/services.md`). The `subdomain`
> convention applies only to `*.kogler.si` names.

**Subtasks (do in order; tick as you go):**
- [ ] 1. Create the template dir + docker-compose.yml.j2 (skip if already valid)
- [ ] 2. Add the homepage entry; do NOT change any existing lines
- [ ] 3. Run the Validation command (must PASS)
- [ ] 4. Do NOT touch anything outside File scope

**Difficulty:** 2

**Model:** openrouter/free

**Capabilities:** image/text->text ·T·J·R·V — fully specified config.

**Validation:**
```
py -3 scripts/validate-docker-services.py --only homepage
# pass = prints single-service PASS for homepage and exits 0
```

**Executor prompt (paste to the worker):**
> Environment: windows11/git-bash — pin CWD to repo root, use `py -3`, UTF-8 no-BOM/LF, forward-slash paths; see index (`## Environment`).
>
> You are executing task T5. Read THIS file for the exact template content and list entry. Touch ONLY the two files in File scope. Keep `lookup(...)` expressions intact. Then run the exact Validation command and report PASS/FAIL. On FAIL, stay `in-progress`, paste the output, and STOP.