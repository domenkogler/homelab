# T7 — doco-cd template + list entry ⚠️ awaiting-verification

**Objective:** Create `IaC/ansible/templates/docker_services/doco-cd/docker-compose.yml.j2`
for the pinned upstream image **`ghcr.io/kimdre/doco-cd:latest`** (GitOps CD:
watches the repo, deploys compose on merge, post-deploy hooks per `docs/deployment.md`),
add the `doco-cd` entry to `docker_services` in `group_vars/home_servers.yml`, and
align the repo-root `.doco-cd.yml` config skeleton with the project's real schema
**only if** the project's documented format contradicts the current comment block.

**File scope (ONLY these):**
- `IaC/ansible/templates/docker_services/doco-cd/docker-compose.yml.j2` (new)
- `IaC/ansible/group_vars/home_servers.yml` (add one list entry only)
- `.doco-cd.yml` (repo root — ONLY rewrite the skeletal comments into the project's real schema; do not invent keys)

**Required research (worker must fetch the upstream project docs):**
- `ghcr.io/kimdre/doco-cd` — its GitHub repo, the **image tag**, **config file schema** for
  `doco-cd.yml`/`.doco-cd.yml` (watch paths, deploy command, post-deploy hooks), the
  **webhook endpoint/path** and its auth (secret header), and how it authenticates to the
  Git remote (deploy key / token) and to 1Password (Service Account token, env var name).
  If docs are unreachable, encode the repo's own documented expectations
  (`docs/deployment.md` + current `.doco-cd.yml` comments) and clearly flag what is unverified
  in your report.

**Design decisions (from the SSOT — encode these):**
- Container runs on the **host network** with **docker.sock** rw (docs/deployment-compose.md:
  "CD (Doco-CD) | host network (needs docker.sock)"). Non-root, `cap_drop: [ALL]`
  where the image allows it.
- Secrets, all via 1Password lookups (naming per `docs/deployment-secrets.md`):
  - `forgejo_token` — repo access (read)
  - `doco_cd_webhook_secret` — webhook auth
  - `doco_cd_op_service_account` — 1Password Service Account (minimum-scope vault)
  - `doco_cd_github_webhook_token` if the project needs a distinct value (only if the docs say so)
- The container watches the repo and deploys on merge; **T7 does NOT activate it**
  (activation = HD-02, out of scope). The compose + config must be deployable but
  the plan does not enable any scheduler on first run.
- The compose template must render with only validator-provided context vars
  (`op_vault`, `timezone`, `domain_local`, `inventory_hostname`, …) + the mock `lookup`.

**List entry to add** (CD group, after renovate):
```yaml
  - { name: doco-cd,         template_dir: doco-cd }
```
> `subdomain`: none — Doco-CD has a REST API consumed by Prometheus, no user-facing
> Traefik route (docs/services.md "internal" table has no entry for it).

**Subtasks (do in order; tick as you go):**
- [ ] 1. Fetch upstream docs for `ghcr.io/kimdre/doco-cd`; note image tag, config schema, webhook + auth details in your report (skip if already verified)
- [ ] 2. Create the template dir + docker-compose.yml.j2 battery per the design decisions (skip if already valid)
- [ ] 3. Align `.doco-cd.yml` root config to the real schema (only if it contradicts current comments)
- [ ] 4. Add the doco-cd list entry; do NOT change any existing lines
- [ ] 5. Run the Validation command (must PASS)
- [ ] 6. Write a 3–6 line summary of what you pinned (image tag, schema keys, webhook path) at the top of your PASS report — human verifies at the gate
- [ ] 7. Do NOT touch anything outside File scope

**Difficulty:** 4 (cross-cutting: upstream research + repo config + compose + verification; human gate)

**Model:** ~deepseek/deepseek-v4-flash-latest (user pick — falls back to `deepseek/deepseek-v4-flash`)

**Capabilities:** text->text ·T·J·R — research + config design; needs tools/reasoning, no vision.

**Validation:**
```
py -3 scripts/validate-docker-services.py --only doco-cd
# pass = prints single-service PASS for doco-cd and exits 0
```

**Executor prompt (paste to the worker):**
> Environment: windows11/git-bash — pin CWD to repo root, use `py -3`, UTF-8 no-BOM/LF, forward-slash paths; see index (`## Environment`).
>
> You are executing task T7 — a difficulty-4 task with a human gate. Read THIS file carefully. First research the pinned upstream project (`ghcr.io/kimdre/doco-cd`) — image tag, config schema, webhook/auth, 1Password Service Account env vars. Then create the compose template and align `.doco-cd.yml` only where the real schema contradicts the current skeleton. Keep `lookup(...)` expressions intact. Run the exact Validation command AND include your 3–6 line summary (what you pinned, what is unverified). Report PASS/FAIL with the summary. On FAIL, stay `in-progress`, paste the output, and STOP — the human reviews this task explicitly.