# T2 — blackbox-exporter template + list entry

**Objective:** Create `IaC/ansible/templates/docker_services/blackbox-exporter/docker-compose.yml.j2`
(external reachability, `probe_success`) per `docs/observability.md` and add the
`blackbox-exporter` entry to `docker_services` in `group_vars/home_servers.yml`.

**File scope (ONLY these):**
- `IaC/ansible/templates/docker_services/blackbox-exporter/docker-compose.yml.j2` (new)
- `IaC/ansible/group_vars/home_servers.yml` (add one list entry only)

**Exact template content** (transcribe; same header/secret style):
```
# blackbox-exporter docker-compose.yml — external reachability (probe_success)
#   Scraped by Prometheus (services-internal, :9115); no web UI, no Traefik labels.
#   Probe modules: http_2xx (TLS cert expiry + status), icmp, tcp, dns.
#   Scrape targets are defined in Prometheus' prometheus.yml (see T6), NOT here.
#   See docs/observability.md. Secrets via
#   {{ lookup('community.general.onepassword', '<name>', vault=op_vault) }} — none needed here.
services:
  blackbox-exporter:
    image: prom/blackbox-exporter:latest
    container_name: blackbox-exporter
    restart: unless-stopped
    networks:
      - services-internal
    volumes:
      - ./blackbox.yml:/etc/blackbox_exporter/blackbox.yml:ro
    command:
      - "--config.file=/etc/blackbox_exporter/blackbox.yml"
networks:
  services-internal:
    external: true
```
Also create the probe-module config (same dir) — exact content:
```
# blackbox-exporter probe modules — prometheus/blackbox-exporter
modules:
  http_2xx:
    prober: http
    timeout: 10s
    http:
      valid_status_codes: [200, 301, 302, 401, 403]
      follow_redirects: true
  icmp:
    prober: icmp
    timeout: 5s
  tcp_connect:
    prober: tcp
    timeout: 5s
  dns_udp:
    prober: dns
    dns:
      query_name: kogler.si
      query_type: A
```

**List entry to add** (observe/alerting group area in `group_vars/home_servers.yml`, keep grouping tidy — append after `n8n`):
```yaml
  # Observability
  - { name: blackbox-exporter, template_dir: blackbox-exporter }
```

**Subtasks (do in order; tick as you go):**
- [ ] 1. Create the template dir + docker-compose.yml.j2 + blackbox.yml (skip if already valid)
- [ ] 2. Add the blackbox-exporter entry; do NOT change any existing lines
- [ ] 3. Run the Validation command (must PASS)
- [ ] 4. Do NOT touch anything outside File scope

**Difficulty:** 2

**Model:** openrouter/free

**Capabilities:** image/text->text ·T·J·R·V — small, fully specified config.

**Validation:**
```
py -3 scripts/validate-docker-services.py --only blackbox-exporter
# pass = prints single-service PASS for blackbox-exporter and exits 0
```

**Executor prompt (paste to the worker):**
> Environment: windows11/git-bash — pin CWD to repo root, use `py -3`, UTF-8 no-BOM/LF, forward-slash paths; see index (`## Environment`).
>
> You are executing task T2. Read THIS file for the exact template + probe-module content and the list entry. Touch ONLY the two files in File scope (plus the new `blackbox.yml` beside the compose template). Keep `lookup(...)` expressions intact. Then run the exact Validation command and report PASS/FAIL. On FAIL, stay `in-progress`, paste the output, and STOP.