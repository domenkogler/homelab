# T1 — renovate template + list entry

**Objective:** Create `IaC/ansible/templates/docker_services/renovate/docker-compose.yml.j2`
per `docs/deployment-renovate.md` (exact compose is specified there) and add the
`renovate` entry to the `docker_services` list in `group_vars/home_servers.yml`.

**File scope (ONLY these):**
- `IaC/ansible/templates/docker_services/renovate/docker-compose.yml.j2` (new)
- `IaC/ansible/group_vars/home_servers.yml` (add one list entry; replace the TODO comment only if all 8 are added — it is NOT in this task)

**Exact template content** (transcribe; header comment style follows technitium/traefik templates):
```
# renovate docker-compose.yml — upstream version tracking (3-day stability hold)
#   Renovate container scanning the homelab repo on Forgejo — no web UI, no Traefik labels.
#   Secret: forgejo_token (1Password « Homelab).
#   See docs/deployment-renovate.md. Secrets via
#   {{ lookup('community.general.onepassword', '<name>', vault=op_vault) }}
services:
  renovate:
    image: ghcr.io/renovatebot/renovate:latest
    container_name: renovate
    restart: unless-stopped
    environment:
      RENOVATE_PLATFORM: forgejo
      RENOVATE_ENDPOINT: https://git.kogler.si
      RENOVATE_TOKEN: "{{ lookup('community.general.onepassword', 'forgejo_token', vault=op_vault) }}"
      RENOVATE_REPOSITORIES: "domen/homelab"
      RENOVATE_ONBOARDING: "false"
      RENOVATE_DEPENDENCY_DASHBOARD: "true"
      RENOVATE_DEPENDENCY_DASHBOARD_TITLE: "Homelab Dependency Dashboard"
      RENOVATE_STABILITY_DAYS: "3"
      TZ: "{{ timezone | default('Europe/Ljubljana') }}"
    networks:
      - services-internal
    volumes:
      - /opt/renovate/config:/config
networks:
  services-internal:
    external: true
```
(Values mirror `deployment-renovate.md`; `RENOVATE_STABILITY_DAYS` matches
`renovate.json` packageRules stabilityDays=3. If the https://git.kogler.si
endpoint FQDN is used elsewhere verbatim, keep it verbatim.)

**List entry to add** (after the `sunshine` entry in `group_vars/home_servers.yml`):
```yaml
  # Update
  - { name: renovate,        template_dir: renovate }
```

**Subtasks (do in order; tick as you go):**
- [ ] 1. Create the template dir + file with the exact content above (skip if already valid)
- [ ] 2. Add the renovate entry to the docker_services list; do NOT change any existing lines
- [ ] 3. Run the Validation command (must PASS)
- [ ] 4. Do NOT touch anything outside File scope

**Difficulty:** 1

**Model:** openrouter/free

**Capabilities:** image/text->text ·T·J·R·V — verbatim transcription task.

**Validation:**
```
py -3 scripts/validate-docker-services.py --only renovate
# pass = prints "PASS: renovate" (or the same single-service PASS line) and exits 0
```

**Executor prompt (paste to the worker):**
> Environment: windows11/git-bash — pin CWD to repo root (`D:/source/domenkogler/homelab`), use `py -3`, UTF-8 no-BOM/LF, forward-slash paths; see index (`## Environment`).
>
> You are executing task T1. Read THIS file (`plan/2026-08-08-hd01-docker-templates/T1.md`) for the exact template content and list entry. Touch ONLY the two files in File scope. Transcribe the compose content verbatim (it is rendered by T0's validator with a mocked 1Password lookup — the `lookup(...)` expressions must stay intact, not be replaced). Then run the exact Validation command and report PASS/FAIL. On FAIL, stay `in-progress`, paste the output, and STOP.