# T1 — P0: Clean stale Obsidian references in workspace.json

**Objective:** Remove phantom/dead paths from `.obsidian/workspace.json`
`lastOpenFiles` so Obsidian's graph/search stops pointing at deleted files
(from the prior removed `docs/technical/` structure and old names).

**File scope (ONLY these):**
- .obsidian/workspace.json

**Exact values to insert** (remove exactly these entries from the
`lastOpenFiles` array only; change nothing else):
| Entry to remove |
|-----------------|
| `docs/technical/Rack.canvas` |
| `docs/technical/Network Devices.canvas` |
| `docs/technical/assets/wAP ac.png` |
| `docs/technical/WiFi devices 1.canvas` |
| `docs/technical/assets/HMIP-HAP.png` |
| `docs/02-home-server-hardware.md` |
| `docs/manual/travel-router.md` |
| `Pasted image 20260727131335.png` |

**Subtasks (do in order; tick as you go):**
- [ ] 1. Read `.obsidian/workspace.json`; parse the `lastOpenFiles` list. (skip if already clean)
- [ ] 2. Remove exactly the 8 entries listed above from `lastOpenFiles`. Do NOT reorder remaining entries or touch any other key.
- [ ] 3. Write back as valid JSON. Do NOT touch any file outside File scope.

**Difficulty:** 1

**Model:** openrouter/free

**Capabilities:** image/text->text ·T·J·R·V

**Validation:**
```
cd /d/source/domenkogler/homelab && if grep -qE 'docs/technical|02-home-server|manual/travel-router|Pasted image' .obsidian/workspace.json; then echo STALE_PRESENT; exit 1; else echo P0_OK; exit 0; fi
# pass = prints P0_OK and exits 0
```

**Executor prompt (paste to the worker):**
> You are executing task T1. Read THIS file (`plan/2026-08-06-docs-obsidian-reorg/T1.md`).
> Touch ONLY `.obsidian/workspace.json`. Remove exactly the 8 named stale entries
> from the `lastOpenFiles` array; reorder nothing; change no other keys. Write
> valid JSON. Then run the exact Validation command. Report PASS or FAIL. On PASS
> your driver (run-task) marks the task `done` in `index.md` and advances. On FAIL
> stay `in-progress`, paste validator output, STOP.
