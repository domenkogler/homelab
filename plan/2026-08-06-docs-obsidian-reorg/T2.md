# T2 — A2: Add Obsidian excluded-folder choke to app.json

**Objective:** Add a `userIgnoreFilters` list to `.obsidian/app.json` so
Obsidian excludes non-doc folders from the Graph/backlinks view (keeps the
vault focused on `docs/`). Only Obsidian reads this — zero portability impact.

**File scope (ONLY these):**
- .obsidian/app.json

**Exact values to insert** (replace the current JSON body):
```json
{
  "promptDelete": false,
  "alwaysUpdateLinks": true,
  "userIgnoreFilters": [
    "IaC/",
    "brainstorming/",
    "skills/",
    "scripts/",
    "reports/",
    "obsolete/"
  ]
}
```

**Subtasks (do in order; tick as you go):**
- [ ] 1. Read `.obsidian/app.json`. (skip if a valid `userIgnoreFilters` list already exists)
- [ ] 2. Preserve `promptDelete` and `alwaysUpdateLinks` values exactly; add the `userIgnoreFilters` key with the 6 entries above.
- [ ] 3. Write valid JSON. Do not modify any other file.

**Difficulty:** 1

**Model:** openrouter/free

**Capabilities:** image/text->text ·T·J·R·V

**Validation:**
```
cd /d/source/domenkogler/homelab && grep -q '"userIgnoreFilters"' .obsidian/app.json && python3 -c "import json;json.load(open('.obsidian/app.json'))" && echo A2_OK || { echo A2_MISSING; exit 1; }
# pass = prints A2_OK and exits 0
```

**Executor prompt (paste to the worker):**
> You are executing task T2. Read THIS file (`plan/2026-08-06-docs-obsidian-reorg/T2.md`).
> Touch ONLY `.obsidian/app.json`. Add the `userIgnoreFilters` array with the 6
> folders listed above, preserving the existing keys exactly. Write valid JSON.
> Run the exact Validation command. Report PASS or FAIL. On PASS your driver
> (run-task) marks the task `done` in `index.md` and advances. On FAIL stay
> `in-progress`, paste output, STOP.
