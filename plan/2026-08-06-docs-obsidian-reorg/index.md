# Execution Plan: Make docs/ Obsidian-friendly with minimal compatibility loss

> Generated by `plan-task`. **Review and edit before running `run-task`.**
> Index: `plan/2026-08-06-docs-obsidian-reorg/index.md` · Tasks: `plan/2026-08-06-docs-obsidian-reorg/T1.md` … `T4.md`

## Goal
Make the `docs/` tree fully Obsidian-friendly (Properties pane, Dataview,
Graph color-groups, tag navigation) by adding YAML frontmatter, cleaning stale
Obsidian references, adding a Graph-scope choke, and adding reciprocal
`## Related` markdown-link blocks — with **zero** loss to GitHub / Doco-CD /
raw-Markdown / AI-dispatcher rendering, and **no file renames or moves**.

## How to execute (runbook — read by run-task)
1. Present the Model-assignment table below and block until the human approves
   or edits it (one-time gate).
2. Take the first task not marked `done`; read its task file (`T<ID>.md`); run
   it via a subagent at its resolved model; run its exact Validation command.
3. Pass -> mark `done` in the Task summary, advance to the next task. Fail ->
   mark `in-progress`, log error, propose a model upgrade, and STOP until
   approved.
4. Pause at every `awaiting-verification` task (difficulty 4-5) for human
   sign-off. *(No tasks here reach d4-5, so no human gates are inserted by
   default.)*
5. After the last task, run the Global acceptance suite, write the closing
   report, then ask the human before deleting this plan directory.

## Global invariants (never-do)
- **No file renames or moves** — every existing filename stays byte-identical
  (protects `git log --follow`, existing links, and Doco-CD watches).
- **No `[[wikilinks]]` conversion** anywhere — markdown relative links only.
- **Only ADD**: frontmatter blocks, `app.json` keys, `## Related` sections.
  Never edit, reorder, or delete existing body lines, headings, or blockquotes.
- **No `.llm-wiki/`**, no Dataview code blocks, no plugin/theme config changes
  (Dataview is already installed; do not touch its settings).
- **No secrets.** Frontmatter carries only metadata, never credentials.
- Scope is **only** `docs/*.md`, `docs/manual/*.md`, `.obsidian/app.json`,
  `.obsidian/workspace.json`. Nothing else (IaC/, README.md, scripts, skills).

## Models (review/edit here)
All tasks difficulty 1–2 → `openrouter/free` per user instruction (use cached
tier table; **no** OpenRouter refresh).

| Task | Difficulty | Selected model | price_in/M | price_out/M | caps |
|------|-----------|----------------|-----------|-------------|------|
| T1    | 1         | openrouter/free | $0         | $0          | image/text->text ·T·J·R·V |
| T2    | 1         | openrouter/free | $0         | $0          | image/text->text ·T·J·R·V |
| T3    | 2         | openrouter/free | $0         | $0          | image/text->text ·T·J·R·V |
| T4    | 2         | openrouter/free | $0         | $0          | image/text->text ·T·J·R·V |

## Task summary (the progress marker — single source of Status)
| ID | Title | Difficulty | Status | Validation | Task file |
|----|-------|-----------|--------|------------|-----------|
| T1 | P0 — clean stale `.obsidian/workspace.json` references | 1 | todo | grep finds no stale paths | T1.md |
| T2 | A2 — add Obsidian excluded-folder choke to `app.json` | 1 | todo | `app.json` has `userIgnoreFilters` | T2.md |
| T3 | A1+A4 — add YAML frontmatter (role/domain/status/tags) to all 40 docs | 2 | todo | all 40 files carry 5 required keys | T3.md |
| T4 | A3 — add `## Related` markdown blocks to 5 broad docs | 2 | todo | `## Related` present in 5 files | T4.md |

## CURRENT_TASK: T1

---

## Global acceptance (final)

Run after the last task:
```
cd /d/source/domenkogler/homelab && test ! -d .llm-wiki && echo NO_LLM_WIKI && git status --short docs .obsidian/app.json .obsidian/workspace.json
# pass = prints NO_LLM_WIKI, and git status shows ONLY modifications (no renames/deletions) within docs/, .obsidian/app.json, .obsidian/workspace.json
```
```
cd /d/source/domenkogler/homelab && git diff --stat -- docs .obsidian/app.json .obsidian/workspace.json
# pass = exit 0; diff touches ONLY *.md (frontmatter/Related additions), app.json, workspace.json — no renames, no binary/config churn, no files deleted
```

## Out of scope
- ❌ B: Dataview plugin/index work — Dataview is already installed and is left
  untouched; no Dataview code blocks are added.
- ❌ C: Physical folder-grouping of files (`docs/<domain>/`) — deferred; would
  require link churn and is not justified at current file count.
- ❌ `[[wikilink]]` conversion, `.llm-wiki/` adoption, plugin settings, themes.
- ❌ Any edit to `IaC/`, `README.md`, `scripts/`, `skills/`, `brainstorming/`,
  `reports/`, `obsolete/`.
- ❌ Content rewrites of any doc body — only additive frontmatter/`## Related`.
