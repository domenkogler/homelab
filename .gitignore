__pycache__/
*.py[cod]

# pi-subagents run artifacts
.pi-subagents/

# Rendered RouterOS bootstrap scripts (secrets + static IPs injected at render time)
IaC/router/rendered/
.tmp_*

# Office bridge secrets (HD-109): token/env never committed
client/office-bridge/bridge.env

# Netcup VPS post-install script with REAL SSH keys injected (never commit; delete after use)
IaC/host/vps/post_install_with_secrets.sh

# Homelab USB media post-install with REAL SSH keys injected (never commit; delete after copy to stick)
IaC/host/post_install_with_secrets.sh