> **Role:** entry point for the next session. This handoff is a **pointer index** — every item's owning doc/row holds the detail. Start with README.md §0 (intent routing), then §1 context, then §3 execution order. Read [todo.md](todo.md) for the full row bodies.

> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 · [scripts/guard-session.sh](scripts/guard-session.sh) · [todo.md](todo.md) · archive: [reports/changelog.md](reports/changelog.md), [reports/deployment-journal.md](reports/deployment-journal.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — §Tags & surgical runs (`docker_services_scope`, `base` tier) + §Deploy Timing Runbook.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — compose/secret conventions + fail-loud rule.
4. [docs/network-vpn.md](docs/network-vpn.md) — §Pattern A tailnet sidecars + current tailnet edge state + §MagicDNS (extra_records design).
5. Owning `docs/*.md` (per task) — the decision-logs + spec SSOT; read before re-deciding anything. Historical changelog/journal rows live in the frozen `reports/changelog.md` / `reports/deployment-journal.md` (archive-only).

## 1. Environment (WSL Debian primary, ext4) — as of #67, the repo runs from the WSL Debian primary on ext4 (not Windows)

Adapted to the WSL Debian primary (repo moved to ext4; the Windows-runner/9P-gate era is legacy — see "9P gate: N/A" in §4). `python3`/bash/LF/UTF-8 no-BOM. Secrets → 1Password item.field only, `>-` for YAML renders. **Self-learned (2026-08-26):** do NOT use multi-line bash heredocs with backslashes/backticks (mangled through `bash -c`) — write patch scripts to a temp file and run them. **Signed-commit gotcha (still valid):** keys in `~/.ssh/github_signing`/`github_auth`; `Couldn't find key in agent` → `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.

## 2. State snapshot (start of next session)
- **SESSION 2026-09-08 (open-resolver exposure FIXED + LIVE, branch `homelab-wt-20260908-1135` → main `810e8a7`) — the VPS DNS primary was an open recursive resolver; closed at the nftables FORWARD chain.** Owner asked “why external clients in dns.kogler.si?” → diagnostics proved the 6 low-count external IPs were internet scanners (`reposify.net`/`pfcloud.network`/AWS/GCP) probing the resolver, and it answered recursion from non-ACL sources. **Root cause:** Docker's published-port `53:53` DNAT happens in PREROUTING and is then FORWARDED to the container bridge — it never traverses the host INPUT chain's `ip saddr @dns-allow-home … dport 53 accept`, so it was matched by the generic `oifname "br-*" accept` and ANY external source reached the resolver. **Fix:** source-restrict the `:53 → {{ tchnitium_dns_overlay_ip }}` forward path in `vps-hardening/templates/nftables.conf.j2` (allow tailnet CGNAT `100.64/10` + home-WAN `@dns-allow-home` set, drop the rest) — a FORWARD drop is authoritative over Docker's accept (2026-08-23 incident precedent). **Applied live** (`playbooks/vps.yml --tags hardening`); **verified:** external scanner drops incrementing (4→19 UDP/45s), home-WAN resolution intact (`dig @159.195.111.66` from home LAN → NOERROR/2 answers). **SSOT docs updated:** network-dns.md (corrected the false “input source-allow is the control” claim + noted the recursion-ACL gap), security.md §3/§8, services-vps.md firewall checklist row 7, README state, todo HD-299. validate-all green; no new HD (owner: just fix it). · [todo.md HD-299](todo.md) · [network-dns.md](docs/network-dns.md) · [security.md](docs/security.md)
- **SESSION 2026-09-08 (WAN-out DNS failover VERIFIED, branch `session/failover-verify-20260908`) — HD-317/HD-299 last owner-verifiable item DONE.** Owner disabled router PPPoE (homelab WAN down): `dig @159.195.111.66` (VPS primary) timed out while the **LAN-local Pi answered the full `*.kogler.si` set** + HA/dns-pi 200 throughout; clean immediate recovery on PPPoE re-enable (no negative caching). Proved the **Pi leg** (VLAN-10 Pi-first design holds — LAN resolution survives WAN-out). ⏳ Only remaining: **oldsrv secondary seed = HD-340 (gated on HD-318)** — full 3-instance HA completes then. · [todo.md](todo.md) · [network-dns.md](docs/network-dns.md)
- **SESSION 2026-09-08 (SMTP-587 fix + DNS-UI correction + HD-265 close, branch `session/smtp-dns-close-20260908`) — owner reported 3 findings, all handled.** ① **HD-241 BUG: smtp2go port 587 is BLOCKED from the VPS egress** — owner's Metabase *Send test email* → `POST /api/email/test 500` (~134s); probes confirmed only **2525/8025** open from the VPS (LAN egress reaches both; 2525 answers an SMTP/Exim banner). Fix = SSOT `smtp2go_port: 587 → 2525` (group_vars/all/main.yml) + NUT upssched fallback 587→2525 + doc mentions (observability.md, automation-renewals.md, home_servers.yml). This propagates to Metabase/Grafana (VPS) + NUT (nas). ⏳ VPS surgical converge (`docker_services_scope=metabase`) + owner retest. ② **dns.kogler.si has its OWN login, NOT SSO — CORRECTED (not a bug):** forward-auth only gates the edge (302→sso then into the app), but Technitium keeps its own `admin`/1P `technitium_login` login — SSO never logs you into Technitium. Docs/todo/prompt wording corrected (was "browser SSO login"). ③ **HD-265 FULLY CLOSED + row DELETED:** owner ran `git-bootstrap-win11.sh --ssh-auth` successfully → both deploy-gates pass. validate-all green. · [todo.md](todo.md) · owning docs
- **SESSION 2026-09-08 (quick closes: HD-307/HD-317/HD-299/HD-241/HD-265/HD-292, branch `session/hd-close-quick-20260908`) — live-verified + todo/SSOT updated.** ① **HD-307 CLOSED + row DELETED** (fully provisioned + LIVE; residual verifies resolved elsewhere — KNX config-flow done, `ha.kogler.si` cutover→VIP done, Authentik-OIDC explicitly NOT wanted (owner), failover runbook = HD-04 umbrella; live re-verify 2026-09-08: keepalived MASTER, HA :8123 200, traefik-ha :443 200, dual-home NM connections active). ② **HD-317 + HD-299 TRIMMED to owner-only tails** (VPS/Pi legs LIVE-VERIFIED 2026-09-08: VPS primary seeded + answering, `dns.kogler.si` 302→sso→200, Pi tertiary seeded + `dns-pi:5380` 200, router DHCP 3-chain + `/ip dns` = VPS→oldsrv→Pi live; remaining = owner browser SSO login + WAN-out failover test; oldsrv seed = HD-340/HD-318-gated). ③ **HD-241 TRIMMED** — SMTP env LIVE-APPLIED on the VPS (`MB_EMAIL_SMTP_*` set); remaining = owner *Send test email* in Metabase. ④ **HD-265 Debian/WSL side LIVE + VERIFIED** (keys in agent, origin SSH, signed commits `%G?=G`, `ssh -T git@github.com` → `Hi domenkogler!`); Win11 mirror = owner. ⑤ **HD-292 CLOSED + row DELETED** (no prune needed — the stale `traefik:v3.5.2` is already gone; live `v3.7.11` per versions.yml SSOT; 0 dangling, 0% reclaimable; stale `v3.5.2` mention fixed in security.md). validate-all green; worktree branch = session. · [todo.md](todo.md) · owning docs
- **SESSION 2026-09-08 (close-out, branch `session/hd313-closeout`) — done-items deleted + decisions recorded + wg AllowedIPs auto-apply.** ① **HD-313 row DELETED** (closed; record lives in network-ops.md §Central log shipping) and the **wireguard role now auto-reapplies the peer** when `wg_s2s_vps.allowed_ips` changes (`Render wg-s2s.conf` → notify `ensure wg-s2s peer` → oneshot restart) — the manual `systemctl restart wg-ensure-s2s-peer` step is obsolete and was removed from deployment-manual (owner-steps only). ② **HD-252 row DELETED** (done — tailnet devices re-enrolled per owner). ③ **HD-18 REJECTED** (stick will not be purchased) + **HD-13 row updated to REJECTED**: local-RF Homematic closed out; decision row added to [smart-home-rejected.md](docs/smart-home-rejected.md); smart-home.md + smart-home-failover.md docs updated (IP-only failover path). · [network-ops.md](docs/network-ops.md) · [smart-home-rejected.md](docs/smart-home-rejected.md)
- **SESSION 2026-09-08 (HD-326 + HD-312(1)(3) LIVE + SSOT parity, branch `session/hd326-kids-live`) — HD-326 CLOSED; cloud-IoT WAN un-blocked (was a rule-ORDERING bug, not lease turnover).** **Root cause:** the role's forward-chain `api_modify` tasks (HD-326 kids bedtime/DoT drops + HD-312/325 `iot-wan-allow` accepts) APPEND at the END of the forward chain — BELOW the `Home->WAN accept` and the `IoT->WAN drop` — so first-match-wins shadowed them: kids controls were inert, cloud-IoT (LG/Bosch/HAP) WAN was blocked. **Live apply (`rb4011_hd326_fix_delta.rsc` via routeros-apply-delta.sh):** removed duplicates (4 extra bedtime + 2 DoT), re-added the SSOT set ABOVE the WAN accepts (kids now ind 13-16, iot-wan-allow ind 21-26, all verified above the accept/drop), added the MISSING `tablet-valentina` filtered-DNS NAT rows (was iPad-only), cleaned a double iPad NAT. **Live-verified:** Bosch/HAP/LG **established WAN conns to AWS/Azure** (`.190→62.113.x`, `.192→3.74.x`, `.23/.24→20.105.x`); iPad fixed-MAC (owner step, 2026-09-08) on Kogler/cap-wifi2 with `F6:9B:E2:B9:26:F9`, lease binds the `tablet-ipad` static reservation. **SSOT parity (apply-of-record):** `rb4011_converge.rsc.j2` was MISSING all `src-mac-address` rules (a converge import would have silently dropped the live set) — added the full iot-wan-allow + kids forward set + per-MAC kids NAT via `network_static_hosts` loops in the correct order; **role forward-chain tasks GATED OFF (`when: false`)** — forward-chain state now owned by the converge template ONLY (network-ops apply-model: role api_modify append-at-end was the live-shadowed mechanism); NAT kids task kept (idempotent dstnat). New `rb4011_hd326_fix_delta.rsc.j2` (transient, folded) + render-converge task. HD-326 row **deleted** from todo.md (CONVENTIONS §4(a)); HD-312 row trimmed to (4) n8n only; Pkg A marked done; IP-literals in docs/todo fixed (check_doc_ips). validate-all green. · [todo.md](todo.md) HD-312 · [network-vlans.md](docs/network-vlans.md) §3-SSID · [rb4011_converge.rsc.j2](IaC/router/templates/rb4011_converge.rsc.j2)
- **SESSION 2026-09-08 (HD-313 RESOLVED + LIVE, HD-321 rotation re-run, branch `session/hd313-fixsyslog`) — the "zero UDP/514 syslog" blocker was NOT RouterOS logging: it was the VPS WireGuard peer AllowedIPs missing the router's own wg address.** Diagnosis (synchronized router-sniffer + VPS-listener test): router WAS emitting syslog on wg-s2s; the VPS wg endpoint silently dropped it because the source (the router's wg addr) was absent from the peer `AllowedIPs` (HD-155 least-access list). Fix = SSOT `wg_s2s_vps.allowed_ips` + `{{ wg_s2s_vps.router_ip }}/32` (group_vars/all.yml; single SSOT, both tunnel sides per HD-200) + re-render VPS wg-s2s conf + re-run the `wg-ensure-s2s-peer` oneshot (`wg setconf`, live). **Verified end-to-end:** RB4011 → wg-s2s → rsyslog(wg addr:514) → `/var/log/remote-syslog/routeros.log` → CrowdSec acquis (`type: mikrotik`) + Loki. **Retained safety net:** `fixsyslog` boot script+scheduler on the RB4011 (toggle centralsyslog target 10s post-boot) — documented as NOT the fix, harmless; folded into the converge template + a render-converge task. **MikroTik admin password rotated on all 4 devices** (HD-321 flow; vault `mikrotik-admin_login` `old-password`/`password`; old fails / new authenticates verified per device; ⏳ next-reset `flash/` re-upload of the re-rendered initial `.rsc`). **New deltas added to render-converge.yml:** `rb4011_fixsyslog_delta.rsc.j2`, `mikrotik_admin_rotate_delta.rsc.j2`, `rb4011_egress_probe_delta.rsc.j2`. ⏳ Remaining: switch/AP syslog forwarding (CRS328 no wg — future), n8n `logpipe` reuse (HD-312d). · [todo.md](todo.md) HD-313 · [network-ops.md](docs/network-ops.md) §Central log shipping
- **WSL runner networking made durable / network-type independent (2026-09-07, this session):** the bridged `.wslconfig` (`networkingMode=Bridged` + `vmSwitch=VLAN-Switch`) pinned `eth0` to the homelab static via systemd-networkd and broke on WiFi/hotspot with NO-CARRIER → no IP → `Network is unreachable`. Fixed live + scripted: `scripts/wsl-nat-resolv.ps1` (admin, idempotent) sets `.wslconfig` → `networkingMode=Nat`, drops `generateResolvConf=false` from `/etc/wsl.conf` (auto-resolv follows Windows' resolver on any network), and disables the static `10-eth0.network`. Replaces the deleted `scripts/wsl-vlan-trunk.ps1`. Re-run it any time to restore the same state; the old route-through-Windows Mgmt-99 extras are opt-in via `-EnableMgmt99` for home only. Details: this session's entry + [deployment-manual.md](deployment-manual.md) §0.4b + [scripts/README.md](scripts/README.md).
- **SESSION 2026-09-07 (HD-343, commit `1a206a8`/`2b66fe3`, branch `session/hd343-network-clients`) — Grafana “Network Clients” dashboard design AUTHORED into the SSOT; new HD-343 registered.** Owner request: “grafana dashboard on stats.kogler.si with all network clients, ideally grouped per vlan” — described read-only first, then written into `docs/observability.md` §Network Clients Dashboard (design SSOT: 5-source join — DHCP leases / ARP / bridge FDB / wifi-reg / SSOT `network_static_hosts` — with per-source VLAN classifiers) + `todo.md` HD-343 row (data-path decision: small RouterOS-API collector on `oldsrv` using the read-only API / `mikrotik-read.py` → Prometheus → oldsrv Alloy remote-write over wg-s2s → VPS; VPS→router direct, Loki-derivation, and SNMP-as-primary rejected — SNMP kept as backstop only). ⏳ **Remaining (HD-343 tail): implement the exporter role + `homelab-network-clients` dashboard JSON in `IaC/ansible/roles/monitoring/`; verify live wifi path (`/interface/wifi/registration-table` vs legacy); oldsrv converge gated on Phase-3/HD-318; owner render-verify.** Also fixed two pre-existing `check_doc_ips` violations (HD-342's hardcoded Pi DNS IP → `dns_tertiary_ip` in `todo.md` + `prompt.md`) so main is green again. · [observability.md](docs/observability.md) §Network Clients Dashboard · [todo.md](todo.md) HD-343
- **HD-312 3-SSID WiFi — phases 1 + 2a DONE + LIVE (2026-09-03).** Live SSIDs = **Kogler (VLAN 10) + Kogler IOT (VLAN 20) + Kogler guest (VLAN 30, 5GHz)**; IOT-WAN + Kids SSID configs **deleted** (configs + security profiles removed live + from SSOT; items kept in 1P, unreferenced). Phase 1 static-IP sweep: 4× Shelly RGBW2 + 4 family WiFi clients (valentina/phone-domen/phone-martina/tablet-ipad) + UPS + iLO now have MAC + static reservations in `network_static_hosts` (router converge applied them live — UPS already bound its `.99.9`; iLO/phones/iPad bind at next lease renewal/reconnect). Phase 2a: guest turned up via idempotent deltas (`rb4011_guest_delta`/`crs328_guest_delta`/`ap_guest_delta` .rsc.j2, no secrets, folded into render-converge.yml); CAPsMAN provisioning now band-split by SSID `band:` (2.4GHz = Kogler+IOT, 5GHz = Kogler+guest). **⚠ LIVE GAP → RESOLVED (2026-09-03, phase 3 live): the cloud-IoT appliances (2× LG AC, 3× Bosch, HAP) were on the deleted `Kogler IOT WAN` SSID (VLAN 21, WAN allowed) — moved to `Kogler IOT` (VLAN 20 = no internet).** HD-312 phase 3 (`src-mac-address` WAN accepts) **LIVE 2026-09-03** — the per-MAC forward-accept is on the router; the appliances regain WAN as their DHCP leases turn over to the reserved VLAN 20 pool (verify). ⏳ Remaining: (2b) CAPsMAN per-MAC `access-list` vlan-id; (3b) `kids-*` MAC-lists active + kids-control off VLAN 40; (4) n8n workflow + scoped router API user. Full spec [network-vlans.md](docs/network-vlans.md) §3-SSID + [network-rejected.md](docs/network-rejected.md) + [todo.md](todo.md) HD-312.
- **SESSION 2026-09-07 (HD-339, commits `255ccf4`+`85b2e99`, branch `session/hd339-wifi-ap-vlan`) — WiFi + KNX fixed (two independent root causes).** ① **WiFi dead / no DHCP** (phones + 4× Shelly associated but lease-less; HA→Shelly HTTP 000; KNX lagged on shared VLAN-20): HD-338's converge rewrite dropped `ether11`/`ether12` from the CRS328 **tagged** SSID-VLAN membership (10/20/30/40) — only untagged-99 left → tagged client frames dropped at switch ingress. Fixed live (delta: VLAN 10/20/30/40 `tagged=bridge,sfp-sfpplus1,ether11,ether12` via `routeros-apply-delta.sh`; verified 4× Shelly ARP dynamic + HTTP 200) + **switch-role parity task** `roles/switch/tasks/main.yml` (HD-339, mirrors `crs328_converge.rsc.j2`'s `wifi_ports` clause — HD-338 edit-both-or-neither rule). ② **KNX can't turn OFF** (light.jedilnica): generator `emit_light()` used `DimmingControl` (DPT 3.7 relative) as `brightness_address`; HA writes absolute 0-255 (0=OFF) → ignored. Fixed to `DimmingValue` (DPT 5.1); regenerated all 16 DIMM lights (Jedilnica `2/1/2`→`2/1/4`), re-deployed to Pi. **Plus `route_back: true`** on the Pi KNX tunneling entry (was false → state/telegram pushes blocked) → light state now tracks live (off→on→off verified) + rekuperator humidity reads (only **Room Temp = −10°C is a genuine ComfoConnect probe fault**, GA 12/1/14 = −7.68°C on the bus — owner: check probe; filter `4320 h`=180d correct). Full HD-339 row + docs: [todo.md](todo.md) HD-339, [network-vlans.md](docs/network-vlans.md), [home-assistant-current.md](docs/home-assistant-current.md). **HD-339 CLOSED 2026-09-07 (owner): the −10°C rekuperator room temp is a physical ComfoConnect probe fault** — no config action; HD-339 row trimmed + docs updated. **SSOT parity commit pending merge → main** (session close-out).
- **SESSION 2026-09-07 (HD-330 + HA mobile_app, branch `session/hd339-close-ha-oidc`) — Pi Technitium admin-align + seed DONE + LIVE + mobile_app integration enabled.** ① **HD-330:** recreated the Pi Technitium admin to the 1P `technitium_login` value (still default `admin`/`admin`) → `raspberry_pi.yml -e docker_services_scope=technitium-secondary` converge **failed=0** → `kogler.si.zone` + split-horizon A records live (`getent hosts ha.kogler.si` → `ha_vip`; `dig @<Pi Home IP per SSOT>`/`@<VPS primary>` both answer). **Fixed 2 IaC bugs en route:** seed container-IP discovery emitted literal `\n` → `_tech_api` glued 2 IPs (`http://<ctr-ip-1>\n<ctr-ip-2>\n:5380`) → login failed (fixed: space-delim + `tr` + `awk NF`); records loop used VPS-only `tailnet_sidecar_ip` → undefined on Pi (fixed: `| default(tailnet_sidecar_ip, true)`). ② **HA Android companion:** rendered HA config had **no `mobile_app:`** (and no `default_config:`) → `/api/mobile_app/registrations` 404 → app aborted at "enable mobile app integration". Added `mobile_app:` to `configuration.yaml.j2` (NOT `default_config:` and NOT host networking — compose is fine) → HA restart → **endpoint now 401** (integration live). Android app should register now.
- **HD-310 CLOSED 2026-09-07** (row deleted, CONVENTIONS §4) — port model fully done + live (Pi/oldsrv/laptop untagged Home 10 + tagged Mgmt 99). Owner decisions this session: **`external_url` = https://ha.kogler.si SET**; **no Authentik with HA** (native-OIDC NOT wanted — HA stays local-auth, WAN-independent). Remaining HA cutover items folded under HD-04; the oldsrv Technitium secondary DNS leg is a new row **HD-340** (deploy-gated on HD-318).
- **HD-310 FINAL port model = LIVE + SSOT**: `router_port_map`: **old-srv ether2 = pvid10 + tagged[99] · laptop-domen ether3 = pvid10 + tagged[99] (dual-home since HD-338 2026-09-07 — untagged Home 10 + tagged Mgmt 99; Windows stays Home-untagged-only) · rpi ether10 = pvid10 + tagged[99]** (nas-ilo/homematic-ap stay pvid99 — homematic-ap→pvid20 IoT since HD-338). Live router verified: VLAN10 untagged=ether2,3,10 · VLAN99 tagged=bridge-lan,sfp-sfpplus1,ether2,ether3,ether10, untagged=ether7 · VLAN20 untagged=ether9 (HAP). Services `available-from` = **Mgmt subnet only** STRICT again ([network-addresses-generated.md](docs/network-addresses-generated.md)) (Home→Mgmt forward removed — **Home must never reach core infra**; Pi `eth0.99` tagged leg is the only mgmt-plane client besides the laptop's tagged-99 port). SSH: `~/.ssh/config` `pi99`→mgmt `pi` / `router99`→`router` mgmt via ProxyJump `pi` (IPs in [network-addresses-generated.md](docs/network-addresses-generated.md)) — verified. **Winbox GUI tunnels (2026-09-02, SSOT [network-vpn.md](docs/network-vpn.md)):** `ssh -N switch-wb`/`ap-spalnica-wb`/`ap-dnevna-wb` → `127.0.0.1:8292/8294/8295` (forward runs ON the Pi — the Mgmt-leg holder; a ProxyJump-to-device final hop leaves RouterOS SSH hosting the forward, which breaks Winbox auth — live lesson). Cable moves: ✅ **DONE 2026-09-02** — Pi → **ether10**, laptop → **ether3** (owner moved the cables; mini-switch freed); live end-to-end re-verified (Pi Home leg, `router99` via tagged-99 hop, router bridge ports pvid=10 on ether2/3/10 + pvid=99 on ether7/9).
- **HD-302 Comtrend modem mgmt = LIVE + SSOT (2026-09-02, commit `22abb8a`)** — live fix: `router_on_modem` `/32`→`/24` on ether1 (connected route to the modem subnet) + **srcnat masquerade** out ether1 (dst=`comtrend_modem.modem_mgmt_ip`:80/443) because the consumer ONT only answers its web UI to its directly-connected peer (router on the modem LAN) — forwarded HTTP from deeper subnets is silently ignored. **Live-verified:** laptop → modem UI returns 401 (auth page); router can ping the modem. SSOT: `comtrend_modem` (router.yml), `rb4011_converge.rsc.j2` SNAT rule, generated SSOT re-rendered. ⏳ **Remaining:** private DNS `modem.kogler.si` → needs the first Technitium (oldsrv offline).
- **Phase 2 (nas) DONE + LIVE (2026-09-03, HD-314; row closed — record in owning docs).** `storage.yml` failed=0. ZFS pools tank (mirror) + bulk (RAIDZ2) IMPORTED + datasets/props; NFS exports → oldsrv (`/tank/data`, `/bulk/media`, `/bulk/data/immich-thumbs`, SSOT oldsrv Home IP `/32`, root_squash anonuid/gid=1005) — `exportfs` verified; sanoid+syncoid timers; Samba; **NUT master LIVE** (PowerWalker `upsc` battery 100% / Innova Unity; `upsd` :3493 + `nut_exporter` :9199 + `zfs_exporter` :9198 active); cockpit-storaged (trixie has no cockpit-zfs). nas's DHCP client-id = MAC (`dhcpcd.conf clientid mac`) → binds its **reserved Home static** (SSOT) instead of the dynamic `.100`. Host state + full fix list: [hardware-nas.md](docs/hardware-nas.md) + [hardware-ups.md](docs/hardware-ups.md) + [deployment-manual.md](deployment-manual.md) §Phase 2. ⏳ **Remaining:** battery-pull test (owner, HD-06), `bulk/migrate` landing-zone redistribution (HD-207).
- **⚠ Live-ops flag (2026-09-03) — RESOLVED same day:** the **router mgmt-plane (tagged-99 → the router's SSOT Mgmt IP) was intermittently unreachable** during the nas session (ARP FAILED from the Pi; Home-plane ICMP OK). Root cause (confirmed via WinBox this session): a converge had flipped the VLAN-99 bridge tagged memberships on `ether2`/`ether10` to UNTAGGED → Pi tagged Mgmt frames dropped. Fixed with the Winbox one-liner (tagged=ether2,ether10 restore) → `router99`, API over the Pi hop, and every mgmt client now ARP-REACHABLE. Recorded RESOLVED in [network-ops.md](docs/network-ops.md).
- **HD-335 — spark node planned + SSOT (2026-09-06, commit `909ee31`).** The old Phase-2 Ryzen/R9700/Proxmox build (**HD-42**) is **superseded** by the purchased **Lenovo ThinkStation PGX** (`spark.kogler.si`, NVIDIA GB10, 128 GB unified, 1 PFLOP FP4). New [`docs/hardware-spark.md`](docs/hardware-spark.md) + `hardware.md`/`hardware-gpu.md`/`index.md`/`readme-humans.md`/`README.md` updated; old build archived in `deployment-rejected.md`. **AI plan recorded** in `services-ai.md` (§Components Triton/Mem0/OpenHands, local-model table, decision-log 22, §5c-D Mem0 OWUI/Qdrant `user_id` scoping with the exact snippet): **Triton Inference Server** + NVFP4 set (nemotron-30B, qwen3-next-80B, llama-3.3-70B, qwen3-coder-next-80B, bge-m3/reranker, whisper-large-v3 + turbo, XTTS-v2/Piper) + **Mem0** (OWUI long-term memory on Qdrant) + **OpenHands** (agentic coding). ⏳ **Remaining (node bring-up under HD-335):** DGX OS install, Ansible role + IP reservation/placement (add to `network_static_hosts` at provision), Triton serve, Mem0/OpenHands onboarding (10-step). No IaC written yet.
- **DNS: oldsrv/Technitium (primary DNS) — oldsrv back on `.101` (DHCP lease, 2026-09-03)** — it's powered on + pingable at its transient lease; still needs the Phase 3 provision (networkd units + DHCP client-id=MAC) before it runs Technitium at its reserved Home static. Laptop DNS stays `1.1.1.1`/`1.0.0.1` until then.
- **sync-skills resolved (2026-09-06, HD-335-era):** `plan-task`/`run-task` skills **removed as obsolete** (repo `skills/` + `sync-skills.sh`/`update_pi.cmd`/`pi-agent/AGENTS.md`/`readme-humans.md`/`platform-env` swept). Repo `skills/` now == deployed `~/.pi/agent/skills` ({mikrotik, platform-env, shelly}) — the previous `sync-skills.sh --check --strict` drift at gate item 13 is gone.

- **Phase 1 (VPS edge) is LIVE** — full enabled `docker_services` set converges green; observability + Authentik + headscale + traefik-tailnet all up. **HD-298 (pi-dev pty.node) DONE + LIVE-VERIFIED** (#63-era; row closed — record in owning docs): `pi-dev:8080` 200 via sidecar; 4-layer fix (ignore-scripts removal + glibc node:24 + npm allowScripts + cap_add CHOWN/FOWNER/DAC_OVERRIDE).
- **HD-297 tailnet clean URLs: LIVE.** `https://dsh.ts.kogler.si` / `https://pi-dev.ts.kogler.si` → **200**; plain `*.kogler.si` → **302 → sso** — root cause was a comment-brace render crash (`fc2b0a1`), middlewares.yml renders + loads (detail §3 item 6). **⚠ `stats.kogler.si` plain still blocked from the laptop — VPS primary never seeded (see §3 item 2).**
- **Phase 4 (Pi): FULLY PROVISIONED + LIVE (2026-09-03, HD-307/310/04).** Pi OS Lite installed (hostname `pi`, user `admin` + `ansible-admin`, SSH key `ansible-admin_ssh`); port model + physical final (Pi on **ether10** untagged Home 10 + tagged Mgmt 99; laptop on **ether3** untagged Home only); host-side dual-home = two NM keyfiles (`pi-eth0.nmconnection` Home + `pi-mgmt.nmconnection` tagged-99, verified). **Full `raspberry_pi.yml` provision run `failed=0`** — **home-assistant-primary** (HA 2026.8.1 + keepalived MASTER on VIP `ha_vip`), **technitium-secondary** (DNS :53 alive), **traefik-ha** (TLS `*.kogler.si` valid) ALL UP + verified; `https://ha.kogler.si/` → **302**; HA boots **0 config errors**. Live-fixed: docker_services enabled-set, first-boot guard loop_var, technitium read_only/cap_add, knx-entities `knx:` wrapper, meteoblue-not-in-2026.8 (disabled), trusted_proxies + stale `.storage/http`, ha-cert-sync `dump/` exclusion + VPS ha-sync key auth/rsync. **Session #65 completion:** `ha.kogler.si` DNS cutover → VIP DONE (verified), **KNX LIVE** (HD-319 firewall rule + HA config-flow UDP v1 → entities load), **Lovelace dashboard fixed** (relative includes + Pregled card fixes), logged in as `admin`. ⏳ **Remaining (owner-gated):** failover runbook (blocked on oldsrv/HD-318); `dns-pi` 502 decision. **DONE 2026-09-07:** `external_url` = https://ha.kogler.si set; Authentik-OIDC explicitly NOT wanted (owner). **KNX fully working since HD-339 (2026-09-07): route_back=true + DimmingValue brightness fix** — only known rekuperator issue = Room Temp −10°C (ComfoConnect probe fault). Runbook: [deployment-pi-provision.md](docs/deployment-pi-provision.md) (status = fully provisioned).
- **RouterOS apply convention (decided 2026-09-01, [network-ops.md](docs/network-ops.md)):** `initial`(manual bootstrap) → `converge`(=full, SSOT, idempotent, universal apply) → `delta`(transient only, fold back). API = verify/idempotent only. **Apply = render → /import (SCP over SSH, pinned host key, `ansible` identity).** **⚠ Ordering pitfall (live 2026-09-01):** a FORWARD accept appended at the tail lands BELOW the repo's mid-list "Default deny inter-VLAN" and is shadowed (HD-307 hit this: rule #33 below deny #25 → dead). Fix pattern (in network-ops.md): dedup → re-add → `move … destination=$deny` (RouterOS 7 = places BEFORE) → always verify position. Idempotent deltas: `rb4011_pi_delta.rsc.j2`, `rb4011_hd307_delta.rsc.j2` (both committed, rendered via render-converge.yml).
- **HD-309 (Shield Media→WAN) DONE + LIVE-VERIFIED (2026-09-01; row closed — record in network-ops.md + git).** Root cause was the router DNS tertiary (SERVFAIL, no upstreams) — NOT the forward-ACCEPT. Fixed: `/ip dns servers=1.1.1.1,1.0.0.1` + row-73 WAN accepts. Live: Shield has 6 established Media→WAN conns. Tooling: `scripts/routeros-apply-delta.sh`.
- **Phase 1.5 network redo DONE except close-out (2026-09-02):** switch + APs reset + `.pub`/`.rsc` re-rendered (flash/ fix, HD-304); router on the new design; **wg-s2s handshake RESOLVED + LIVE (HD-285, closed — two-key fix, commit `bc4eb95`, record in owning docs)**; owner MikroTik password rotation pending.
- **wg-s2s (HD-285, row closed) RESOLVED + LIVE 2026-09-02 (two-key design fix).** Root cause: shared `wg_password` on both ends → identical pubkeys → router handshaking with itself. Now: router `wg_password` + VPS **NEW `wg_password_vps`** (distinct), oneshot owns the VPS iface (networkd create-only + Unmanaged), router HD-155 accept moved above the Default deny, router endpoint = `vps.kogler.si` FQDN. Live: handshake fresh (keepalive), VPS→router-mgmt 0% loss. Commit `bc4eb95`. · owning docs + git history
- **Vault state:** `check-vault-items.sh --strict` → **all needed items present, MISSING list empty** (exit 0; includes `technitium_login`/`technitium_api` created 2026-09-03 with the seed role — count re-derive at write time per derived-values ban).
- **ID registry:** next free = max(HD)+1 in [todo.md](todo.md) — always re-derived at write time (CONVENTIONS §1/§4); NEVER type a literal (newest registered: **HD-343**, 2026-09-07 — derive next via `bash scripts/next-hd.sh`).
- **Coordination:** headplane/headscale = SEPARATE lane (D) — coordinate, never fold into other converges.
- **SESSION 2026-09-07 — laptop-domen tagged-99 leg (commit `90a897a`, pushed main):** `router_port_map.laptop-domen`
  → `tagged: [99]` (dual-home: untagged Home 10 + tagged Mgmt 99); `rb4011_converge.rsc.j2` VLAN-99 tagged set now includes **ether3** (add + set branches) + port comment updated;
  `all/main.yml` role text + re-rendered `network-addresses-generated.md` + `network-vlans.md` (port-model note +
  port-type table) + `todo.md` (HD-310/HD-311 rows) updated. Validated green, signed, merged fast-forward, pushed.
  ✅ router converge applied in HD-338 (`9b69911`) — tagged-99 live on the device. ✅ **WSL host-step DONE 2026-09-07 (route-through-Windows):** WSL2 cannot tag its own 99 leg (vNIC trunking impossible; mirrored = view-only ARP-FAIL) → **Debian routes the Mgmt subnet (`mgmt_subnet`) via Windows**: Windows Home `laptop-domen` Home IP (DHCP) + Mgmt99 static `laptop-domen` Mgmt IP (Access 99, no gw) + **IP forwarding ON**; Debian `eth0` static `laptop-wsl` Home IP (systemd-networkd) with route Mgmt-net → Windows Home IP. Reusable: `scripts/wsl-nat-resolv.ps1` (idempotent, admin; NAT + auto-resolv, replaces the bridged `wsl-vlan-trunk.ps1`). SSOT `laptop-domen`/`laptop-wsl` rows.
- **SESSION 2026-09-07 — WSL runner networking + Windows signing fixed (host-local, not repo):** ① WSL `mirrored`
  mode was wedged (ARP `FAILED` / `No route to host` while Windows healthy) — `.wslconfig` switched to
  `networkingMode=Nat` → eth0 = a Default-Switch NAT IP, LAN/WAN healthy; `/etc/resolv.conf` = homelab chain
  (Pi→oldsrv→router); `generateResolvConf=false` keeps it durable (procedures now in deployment-manual §0.4b).
  ② Windows commit signing: repo-local `user.signingkey` was a private-key FILE path → 1Password `op-ssh-sign`
  failed (`invalid ssh public key`); fixed by setting `signingkey` to the **pub-key string** (same as
  `.gitconfig-github`). Verified: signed commit works from git-bash via 1Password.
- **SESSION 2026-09-07 (HD-338, commit `9b69911`, pushed main) — IoT devices OFF the Mgmt VLAN + laptop tagged-99 leg LIVE; role↔.rsc parity enforced; WSL 99 access via route-through-Windows (DONE 2026-09-07 — see -a).**
  - **UPS NIC → IoT VLAN 20** (new `ups` SSOT row — see [network-addresses-generated.md](docs/network-addresses-generated.md); switch ether4 pvid20, untagged-20, **NO wan_allow** — NUT/USB is the monitoring path): trusted-admin→UPS web 80/443 forward rule + `ups_management` list REMOVED; stale dhcp-mgmt lease removed, dhcp-20 reservation added.
  - **HMIP-HAP → router ether9 pvid20** untagged-20 (was pvid99 Mgmt; SSOT VLAN 20 + wan_allow already) — port map/template/role trunk task were stale at pvid-99.
  - **WSL laptop tagged-99 leg LIVE on the router** — VLAN-99 tagged now `bridge-lan,sfp-sfpplus1,ether2,ether3,ether10`, untagged `ether7`; ether3 pvid10 + tagged99.
  - **Switch**: VLAN-99 untagged `ether11,ether12` (APs only), VLAN-20 untagged `ether4,ether2,ether21,ether22` (UPS+camera+KNX), ether4 pvid20 — all live-verified via Pi-99 hop.
  - **Pre-existing main bugs fixed en route:** (a) router role kids/`wan_allow` loops used unguarded `selectattr('attr','equalto',true)` → EVERY router converge died (`object of type 'dict' has no attribute 'kids'`) — added `selectattr('attr','defined')` guards (3 kids + 1 wan_allow); (b) crs328 + rb4011 converge `.rsc` templates put `,untagged=` INSIDE `tagged=` (RouterOS reads it as a tagged member) — now separate params.
  - **Role↔converge parity rule documented** (network-ops.md + router role header + the 2 L2 tasks): any role `api_modify` mirroring converge state must stay byte-identical to the template — edit BOTH or NEITHER (drift here regressed VLAN-99 backplane pre-fix).
  - **⚠ WSL eth0.99 host-step NOT DONE — BLOCKED:** tested live — tagged-99 frames do NOT egress the WSL2 NAT Default Switch (router ARP never saw the laptop's `laptop-domen` mgmt IP; ping of the router's mgmt IP 100% loss). Owner set `[wsl2] networkingMode=mirrored` in `.wslconfig` — **next session after `wsl --shutdown`/reboot: retry the host-step** (bring up `eth0.99` sub-interface + Mgmt address + never-default route; verify router99/pi99 direct from WSL; if mirrored still wedges per the 2026-09-07 NAT note above, revert to Nat + keep the Pi-hop pattern).
  - **Housekeeping:** transient delta `rb4011_iot_off_mgmt_delta.rsc.j2` (comitted, folded into converge) · `check_doc_ips` pre-existing fails (deployment-manual/prompt IP literals) FIXED → gate fully green.
  - **Owner note:** Pkg-F worktree (`homelab-wt-20260904-0950`) remains parked untouched.
- **DNS still Pkg-F gated:** the `vpn/home/dns.kogler.si` NXDOMAIN class + per-device DNS visibility remain
  blocked on **HD-330** (Pi Technitium admin-align) → **Pkg F (HD-331→334)**. The homelab chain works for
  general resolution now; the split-horizon records complete under Pkg F. Tailscale `vpn.kogler.si` login
  still needs the Pkg-F seed (this session merely restored basic WSL DNS).

## 3. Next-session execution order (pointers — row bodies in todo.md)

### 3-N. NEXT (priority order)

**-0z. DNS open-resolver exposure CLOSED + LIVE (2026-09-08) — no action needed.** The VPS `:53` publish is now source-restricted at the nftables FORWARD chain (tailnet CGNAT + home-WAN only; external scanners dropped, verified live). **Optional follow-up (defense-in-depth, NOT required — firewall is the authoritative control):** tighten the Technitium `recursionNetworkACL` in `technitium-seed.yml` so the app layer also refuses non-allowed sources, instead of the current “allow all networks” (the seed intentionally sets it that way). · [todo.md HD-299](todo.md) · [network-dns.md](docs/network-dns.md)

**-0a. HD-307 CLOSED + row deleted (2026-09-08).** Pi Phase-4 fully provisioned + live (dual-home NM active, HA/keepalived MASTER/traefik-ha/technitium-pi all up); residual verifies resolved elsewhere (KNX done, HA cutover→VIP done, OIDC rejected). Only true remainder = failover runbook under HD-04 (owner-gated, oldsrv). No action.

**-0b. HD-317 + HD-299 — VPS/Pi DNS legs + WAN-out failover ALL VERIFIED (2026-09-08); ⏳ oldsrv leg only.** VPS primary + Pi tertiary seeded + answering; `dns.kogler.si` live (forward-auth then Technitium's own login — NOT SSO); router DHCP 3-chain + `/ip dns` = VPS→oldsrv→Pi live; **WAN-out failover test PASSED** (PPPoE disabled → Pi carried full set, HA 200, clean recovery). Remaining: oldsrv secondary seed = HD-340 (deploy-gated on HD-318). · [todo.md HD-317](todo.md) · [todo.md HD-299](todo.md) · [network-dns.md](docs/network-dns.md)

**-0c. HD-241 — Metabase SMTP: 587 BLOCKED from VPS egress → SSOT switched to 2525 (IaC done 2026-09-08); ⏳ VPS converge + owner retest.** Owner's *Send test email* → `POST /api/email/test 500` (~134s); only 2525/8025 open from VPS. `smtp2go_port` 587→2525 in group_vars/all/main.yml + NUT template fallback + docs. Next: surgical VPS converge (`docker_services_scope=metabase`, also re-renders grafana SMTP) → owner *Send test email* → invite flow. · [todo.md HD-241](todo.md)

**-0d. HD-265 FULLY CLOSED (row deleted 2026-09-08) — Debian/WSL + Win11 deploy-gates both pass** (`git-bootstrap-win11.sh --ssh-auth` ran and works per owner). No action.

**-0e. HD-343 — Grafana “Network Clients” dashboard — DESIGN AUTHORED (this session); ⏳ implement.** Authoring spec in `docs/observability.md` §Network Clients Dashboard + `todo.md` HD-343 row. Next: implement the RouterOS-API collector exporter (on oldsrv, read-only API) + `homelab-network-clients` dashboard JSON in `IaC/ansible/roles/monitoring/`; verify live wifi-reg path (`/interface/wifi/registration-table`); oldsrv converge needed (Phase-3/HD-318 gated); owner render-verify. · [todo.md HD-343](todo.md) · [observability.md](docs/observability.md) §Network Clients Dashboard

**0a. HD-335 — spark node bring-up (NEW, 2026-09-06).** Hardware purchased, SSOT spec done (docs only). ⏳ Next: DGX OS install on the ThinkStation PGX → Ansible role + placement/IP reservation (`network_static_hosts`, never hardcoded) → Triton + NVFP4 model serve → Mem0 (OWUI/Qdrant, §5c-D) + OpenHands onboarding (10-step). · [todo.md HD-335](todo.md) · [hardware-spark.md](docs/hardware-spark.md) · [services-ai.md](docs/services-ai.md) · [deployment-tasks.md](deployment-tasks.md)

0. **oldsrv Phase 3 — ⏳ in progress (HD-318, HEAVILY UNBLOCKED 2026-09-07).** oldsrv first Ansible contact reached `office`; mgmt-plane RESOLVED; docker/network/storage (nvme ZFS pool + NFS)/nut/cockpit/desktop live. ✅ **Unblocked:** office key (Ascensio), amd_rocm Debian-native (noble repo removed), ollama disabled, `pihole_password`/`sonarr_api`/`radarr_api` 1P items created (sonarr/radarr overwritten with real config.xml keys), media-library layout, pihole `DNSMASQ_USER`, seerr bind-owner, *arr s6 fixes (`/run:exec` + caps + drop read_only) → **sonarr/radarr/lidarr/prowlarr/bazarr/sabnzbd + recyclarr UP on oldsrv**. ⏳ **Remaining:** (a) re-converge oldsrv so the running *arr adopt the final compose (read_only removed) + recyclarr re-renders with real keys; (b) HD-340 (oldsrv Technitium admin-align + seed — the converge's `failed=1`); (c) qbit gluetun needs owner PrivadoVPN endpoint (`privado` provider dropped → `custom`); (d) seerr/jellyfin/actual-budget/kopia-agent RO-path fixes; (e) HD-341 re-seed VPS+Pi ✅ DONE (this session, 2026-09-07). oldsrv becomes the DNS secondary (HD-317/340) + heavy services. · [todo.md HD-318](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 3

1. **HD-312 — 3-SSID WiFi + per-MAC control (phases 1+2a+3 + (1)(3) LIVE + VERIFIED 2026-09-08; HD-326 CLOSED).** ⏳ Remaining: (4) n8n firmware workflow + scoped router API user (`logpipe` reuse). · [todo.md HD-312](todo.md) · [network-vlans.md](docs/network-vlans.md) §3-SSID

2. **HD-317 — Technitium DNS-primary on VPS — VPS/Pi legs + WAN-out failover VERIFIED 2026-09-08; ⏳ oldsrv leg only.** VPS primary seeded (HD-324) + Pi tertiary seeded (HD-330); `dns.kogler.si` live (forward-auth → Technitium's own login, NOT SSO); `dns-pi:5380` 200; router DHCP 3-chain + `/ip dns` = VPS→oldsrv→Pi live; WAN-out test PASSED (Pi carried full set). Remaining: oldsrv seed = HD-340 (HD-318-gated). · [todo.md HD-317](todo.md) · [network-dns.md](docs/network-dns.md)

3. **HD-313 deploy-gate — ✅ CLOSED + row deleted 2026-09-08 (was: IaC DONE 2026-09-03, BLOCKED on "zero UDP/514").** Root cause was the VPS wg peer AllowedIPs missing the router's own wg-tunnel address — NOT RouterOS logging; fixed in the SSOT (`wg_s2s_vps.allowed_ips`), applied, verified end-to-end (RB4011 → wg-s2s → rsyslog → routeros.log → CrowdSec/Loki). The wireguard role now auto-reapplies the peer on any allowed_ips change (notify → oneshot restart), so no manual step remains. `fixsyslog` boot toggle retained as a harmless safety net. Record: [network-ops.md](docs/network-ops.md) §Central log shipping. Switch/AP syslog forwarding still future (CRS328 no wg).

4. **Phase 1.5 cutover remainder (operator + AI).** switch reachability + CAPsMAN steady-state imported; router + switch roles re-converged LIVE via the Pi-99 hop (ansible-network-hop.sh); live switch/router bugs fixed in roles. ⏳ **Remaining:** APs re-join CAP; owner MikroTik password rotation; parked `validate-routeros7-syntax` linter stays parked. · [deployment-manual.md](deployment-manual.md) §1.5 · [todo.md HD-304](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 1.5

5. **HD-299 — Technitium primary → VPS** (new `dns-servers` overlay, 3-instance DNS HA, split-horizon static A records for plain `*.kogler.si` on tailnet). ✅ VPS + Pi legs + WAN-out failover verified (2026-09-08); ⏳ oldsrv leg = HD-340. · [todo.md HD-299](todo.md) · [docs/network-dns.md](docs/network-dns.md) line 18/67

6. **HD-296 + HD-297 — tailnet subdomains + traefik-tailnet clean URLs (dsh/pi).** (a) IaC CLOSED; (b) DEPLOY done — `dsh.ts`/`pi-dev.ts` 200; **(c) RESOLVED at the EDGE + LIVE (2026-09-03)** — plain `*.kogler.si` → 302 → sso. Root cause: template comment brace `{{ vault['…'] }}` crashed the render (`dict has no attribute '…'`, fix `fc2b0a1`) → middlewares.yml never landed; rendered + loaded since the 12:11 converge. **✅ OWNER-FACING plain `stats.kogler.si` RESOLVED (SESSION #74):** the VPS primary was seeded (admin recreated + seed role fixed, HD-324) → plain `stats.kogler.si` now resolves + SSO → Grafana. ⏳ owner browser verify. · [todo.md HD-296](todo.md) + [todo.md HD-297](todo.md) · owning docs

7. **HD-268c owner enable-flow (tailnet sidecars live).** Mint preauth keys → seed 1P `tailscale_dsh_api`/`tailscale_pi_dev_api` → flip `*_tailnet_sidecar_enabled` → scoped converge → verify UIs. 4-step flow in `group_vars/vps.yml`. · [todo.md HD-268](todo.md)

8. **HD-264 Renovate sandbox (owner, parked).** (a) commit versioned manifest + `renovate.json` to `domen/test`; (b) external re-launch timer; (c) verify PR lands; (d) flip `RENOVATE_REPOSITORIES` → the internal Forgejo homelab repo (NOT GitHub — superseded note). · [todo.md HD-264](todo.md) · [docs/deployment-renovate.md](docs/deployment-renovate.md)

9. **Audit-fold owner actions (from the merged audit):** HD-280 fail2ban (add traefik accesslog + filter, or drop http-auth jail) · HD-288 sunshine owner-decision + port binding (HD-289 deleted — in-template) · HD-292 VPS `docker image prune` · HD-286 vault-escape template comments. Row bodies carry owning docs. · [todo.md](todo.md) §§2.4/2.8

10. **Metabase gates (PARTIAL DONE):** HD-241 SMTP *Send test email* + invite flow; HD-242 connect both sources + import dashboards + SELECT-only proof. · [todo.md HD-241](todo.md) + [todo.md HD-242](todo.md)

11. **AI stack pre-deploy gates:** HD-105 secrets + OIDC providers (blocks HD-100/101/103/104); then HD-101 SSO verify ride-along, HD-247 (scoped keys — HD-102 PGVector superseded by HD-267/268 Qdrant), HD-248 OWUI split (depends HD-247), HD-103 docling, HD-104 openclaw onboard. · [todo.md HD-105](todo.md) · [docs/deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md)

12. **HD-112 Zipline go-live legs** (first deploy human-gated; post-up seeding runbook in compose header; family drop script). · [todo.md HD-112](todo.md)

> **Handoff diff-rule (CONVENTIONS §4):** this handoff is edited from #74's — every prior §2/§3 open item either appears or is explicitly resolved (closed items moved to the owning docs + git history only; frozen changelog/journal are archive). **Derived-values ban:** next-free HD quoted from todo.md, never typed. Next free HD: derive via `bash scripts/next-hd.sh` (HD-336+ after HD-335).
## 4. Working rules (binding)

Fresh worktree per session BEFORE any edit — mechanically enforced (`bash scripts/guard-session.sh`; `validate-all.sh` hard-fails primary+main+DIRTY); merge back only committed+green, ff-only; primary = merge station only. **9P gate: N/A on the WSL Debian primary (repo moved to ext4 — no /mnt/d drvfs staleness; the gate is legacy from the Windows-runner era).** Surgical `--tags docker_services -e docker_services_scope=<svc>`; secrets 1P-item.field only + `>-` for YAML; persisted Authentik tokens always `expiring=False` (HD-216); owning doc + todo update in same change; English; relative links; Authentik blueprint pin array attrs (HD-231); don't touch headplane/headscale unless asked. No multi-line bash heredocs with backslashes/backticks — write+run temp script files instead. (changelog/journal frozen → `reports/`, archive-only.)

## 4. Session close-out — 2026-09-06 (inference/sunshine/agent-memory brainstorm + SSOT)

Decisions locked this session (recorded in owning docs + commits; see prior §2/§3 pointers):

- **Inference consolidated on spark (2026-09-06)** — spark = sole local-inference tier (Triton/GB10):
  gen/embeddings/rerank/voice. Voice pinned resident (whisper-turbo + XTTS/Piper), no fallback.
  No Ollama/ROCm on oldsrv (amd_rocm role targeted for removal, HD-318 unblocks); oldsrv GPU =
  Sunshine gaming-encode FIRST + immich-ML as pause-able GPU batch consumer (prep-commands
  `docker pause/unpause immich-ml`; `release-rocm` image). Cohere retired → bge-m3/1024 +
  bge-reranker (Qdrant 1536→1024 free: nothing RAG'd yet). DGX bring-up guide in SSOT.
- **HD-336 (coding plane)**: agent-memory.dev per-project on oldsrv (MCP + LiteLLM key, opt-in
  cross-project); skills stay git SSOT; ZeroClaw = system-mgmt runner (laptop+oldsrv, VPS rejected);
  CrewAI epic orchestration pilot gated on homelab-finished; OpenHands on spark (HD-335).
  Agent-memory pi wiring left for later (outside repo domain); OpenClaw stays for office work.
- **HD-337 (spark bring-up)**: execute HD-335; spec in hardware-spark.md §Bring-up (incl. HD-155
  spark leg). Embedding cutover + Cohere retirement gated on it.
- **Pkg-F WIP worktree (`homelab-wt-20260904-0950`)**: left as-is per owner (5 uncommitted files:
  public: field dead, routes.yml duplicate middlewares key, Pi-first DNS, WG 4443). Not merged.

SSOT changes committed on `session/inference-sunshine-20260906` (3 commits: ad34cac, 1408003, + this
closeout): `services-ai.md` (§9b + decision row 23 ×2), `hardware-spark.md` (+bring-up +guide),
`hardware-gpu.md`, `hardware-oldsrv.md`, `services.md`, `deployment-ansible.md`, `todo.md` (HD-336/337),
`deployment-tasks.md` (ledger), `prompt.md` (this block).

## 4. Session close-out — 2026-09-07 (HD-339 closed + HD-330 done + HA mobile_app fixed)

**Decisions locked this session (recorded in owning docs + commits `2e9610a`, `7e49aea`, `5011051`):**

- **HD-339 CLOSED (deleted row per CONVENTIONS §4).** WiFi AP-tagged regression + KNX dimmer
  `brightness_address` (DPT 3.7→5.1) + `route_back: true` — all fixed live + SSOT parity (previous
  session's commits `255ccf4`/`85b2e99`). Owner confirmed the rekuperator room-temp −10°C is a
  **physical ComfoConnect probe fault** — no config action. Record: `docs/home-assistant-current.md`,
  `docs/network-vlans.md`.
- **HA = WAN-independent + local-auth; Authentik is never a gate (owner decision).** `ha` route has NO
  Forward-Auth on either edge (HD-118/KOPS-004); native-OIDC (Authentik *inside* HA) is optional later
  **alongside** `type: homeassistant`, never replacing it. Access matrix: home WiFi (local login) +
  tailnet (local login; path not built yet) + browser SSO optional. `auth_providers` invariant: always
  keep `homeassistant` — dropping it causes "Enable mobile clients" for any untrusted network.
  Record: `docs/smart-home-failover.md` + `docs/home-assistant-current.md`.
- **HD-330 DONE + LIVE (Pi Technitium admin-align + seed).** Admin recreated to the 1P
  `technitium_login` value (was default `admin`/`admin`) → `raspberry_pi.yml -e
  docker_services_scope=technitium-secondary` failed=0 → `kogler.si.zone` + split-horizon records
  (`ha`/`dns-pi` → VIP, dashboards → `tailnet_sidecar_ip`, `sso` → VPS). Verified `getent hosts
  ha.kogler.si` → VIP; `dig @<Pi>`, `dig @<VPS>` both answer. **Fixed 2 seed IaC bugs en route:**
  ① container-IP discovery emitted literal `\n` from docker `--format` (Go template) → `_tech_api` glued
  both IPs → login failed; fixed with space-delim + `tr` + `awk NF` first-line. ② records loop used
  VPS-only `tailnet_sidecar_ip` → undefined on Pi; fixed `default(tailnet_sidecar_ip, true)`.
- **HA Android Companion app FIXED.** Rendered `configuration.yaml` had **no `mobile_app:`** (and no
  `default_config:`) → `/api/mobile_app/registrations` 404 → app aborted at "enable the mobile app
  integration". Added `mobile_app:` to `configuration.yaml.j2` (minimal explicit include — NOT
  `default_config:`, NOT host networking; compose is correct) → HA restart → endpoint now **401**
  (integration live) → **app registers and works (owner confirmed)**.

**Handoff state for next session:**
- HD-310 row remains open but its tail is the HD-04 umbrella (Authentik OIDC on `ha` optional,
  `external_url` still null, failover runbook). `external_url` set = recommended next owner action
  (Robustness for the App: notifications + reconnect use the right URL) — UI: Settings → System →
  Network → URL.
- **Mobile-over-Tailscale (away-from-home) path is NOT built** (mesh is CGNAT-only, no subnet route /
  `ha.ts.kogler.si`). Small separate change (can ride Pkg F). This is the only remaining gap for
  "phone works everywhere".
- oldsrv Phase-3 still blocked (HD-318: ONLYOFFICE repo/key, ROCm pins, 3 1P items).
- Pkg F / HD-334 now **unblocked on the Pi leg** (Pi DNS seeded).
- Session branch `session/hd339-close-ha-oidc` merged to main (3 commits); worktree removed.
- **HD-310 CLOSED (row deleted):** owner set `external_url` = https://ha.kogler.si and decided **no Authentik with HA**. New **HD-340** = oldsrv Technitium secondary admin-align + seed (mirror of HD-330), deploy-gated on HD-318 — closes the DNS 3-instance seed battery (Pkg C). HD-04 umbrella remains (failover runbook etc.).

---

## 4. Session close-out — 2026-09-07 (Pkg F orchestrated authoring + live-deploy WIP; VPS SSH incident)

**Branch / worktree:** `session/pkg-f-20260904-0950` (@ `/home/domen/source/homelab-wt-20260904-0950`), 4 commits ahead of `main`, validate-all **green**, working tree **clean**, NOT merged to main.

**This session (orchestrated with `deepseek-v4-flash` subagent lanes):**
1. **Rebased the parked Pkg-F WIP onto current main** (23-commit gap bridged; HD-330/338 fixes preserved — semantic-clean).
2. **Commit `3e9385d`** — base: `public:` flags (vps.yml), internal-edge routes (routes.yml.j2), Pi-first VLAN-10 DNS (router role + rb4011 converge), `vpn/home/dns` seed records (technitium-seed).
3. **Commit `50a7336`** (HD-332, Lane A + verify): `public: false` wired into edge labels — grafana/dozzle/metabase/crowdsec-web-ui render `traefik.enable:false` (internal-only; was **latent live exposure** but DNS records already absent → cleanup not a cut); collapsed duplicate `middlewares:` (file-provider schema defect); added missing `sso-int-kogler` internal router + `authentik-backend`. Validator updated.
4. **Commit `9fbe58e`** (HD-333, Lane B + **reviewer APPROVE**, no P0/P1): WG-S2S reach — `traefik-tailnet` compose publishes `:4443` bound to wg-s2s VPS addr (`wg_s2s_vps.ip`) → container :443; `vps-hardening` nftables `iifname "wg-s2s" ip saddr router_ip tcp dport 4443 accept`; SSOT vars; validator mocks.
5. **Commit `23fc4fa`** (Lane C, docs): services-traefik §Edge-model + network-vpn §HD-333 + todo.md marked **IAC-AUTHORED / deploy-gated** (HD-332/333 IAC DONE-deploy-gated, HD-334 authored, HD-331 locked). HD-334 seed records authored (vpn/home/dns → dns_primary_ip).

### ⚠ VPS live-deploy STATUS — SSH to VPS currently unreachable from home
**Do NOT treat the VPS as converged-live.** Partial applies DID land, but **VPS SSH `:22` is unreachable from the entire home network right now** (port 22 "Connection refused" from WSL AND from the Pi; VPS answers ICMP). Almost certainly **fail2ban on the home WAN IP (`193.77.156.222`)**, triggered by my own auth-failure probing (`root@`/`admin@` attempts this session; sshd jail `maxretry=5, bantime=10m`). **The VPS is healthy (ICMP 8.6ms); this is a temporary ban that needs ~10 min of no SSH-auth attempts to clear.**

**What partially applied already (before the ban):**
- VPS `docker_services` converge: **ok=70 changed=10 failed=0** — applied technitium seed records (HD-334), traefik-tailnet restart (HD-333 :4443 + HD-332 routes), 4 label-gate services restarted.
- VPS `hardening` converge: **ok=20 changed=0 failed=0** — nftables ruleset deployed (the nftables.conf was already byte-identical incl. HD-333 4443 line, so `notify: reload` was a no-op-content change; HD-333 rule verified in template).

### NEXT SESSION (in priority order)
1. **Wait for fail2ban ban to lapse (~10m of no auth attempts), then VERIFY the VPS deployed state before any further change.** Clean approach (no auth-failure probing): use the SA-token ansible path via `bash scripts/ansible-run.sh playbooks/vps.yml --tags docker_services -e docker_services_scope=...` (this succeeded earlier, so it works once the ban clears) OR take `ansible-admin` SSH via `ProxyJump=pi` from the **home router's** unbanned path. Confirm: `ss -ltnp | grep 4443` bound to the wg-s2s VPS addr (SSOT `wg_s2s_vps.ip`); `nft list chain inet filter input | grep 4443`; `docker inspect grafana/dozzle/metabase/crowdsec-web-ui --format ...traefik.enable` → `false`; `curl -k -I https://<wg-s2s VPS addr>:4443` → 302/200.
2. **Router converge LAST (per owner):** `rb4011_converge.rsc` render → `/import` to land HD-334 Pi-first VLAN-10 DHCP DNS. (Do not run until VPS + Pi legs verified; router last to avoid losing steering connection.)
3. **Pi converge:** `raspberry_pi.yml -e docker_services_scope=technitium-secondary` to land the seed records + Pi DNS.
4. **Final live verify:** public edge serves public-only, internal edge serves all (`stats/logs/sec/csui` → internal-only), home (router WG) + tailnet reach internal edge, `dig @<pi> vpn/home/dns`.
5. **Close out:** update todo.md (HD-332/333 → LIVE-DONE; HD-334 → LIVE; mark Pkg F complete), owning docs status lines ✅, merge `session/pkg-f-20260904-0950` → main (ff-only, validate-green), delete worktree.

**Operational notes for next session:**
- VPS public IP `159.195.111.66`, ansible via `host_vars/vps.kogler.si.yml`: `ansible_user=ansible-admin`, SSH identity via 1Password SA-token/agent (NOT `~/.ssh/id_ed25519` — that only has github keys). Interactive `ssh vps` uses `HostName vps.kogler.si` which **does not resolve from WSL** (WSL DNS = the tailscale forwards to the homelab chain) — use the SA-token ansible path or the raw IP.
- **Never probe VPS SSH with wrong users/`root`/`admin`** — it instantly triggers the fail2ban sshd jail (`maxretry=5, bantime=10m`) and bans the whole home WAN IP (WSL + Pi egress the same `193.77.156.222`).
- HD-333 reviewer's top note: rely on Docker PREROUTING DNAT + the wg-s2s-address-bound publish as enforcement; the nftables rule is defense-in-depth. Deploy check = `ss -ltnp | grep 4443` bound to wg addr.
- VPS `hardening`/`vps-hardening` role tag is **`hardening`** (not `vps-hardening`).
- The 4 admin apps (`stats/logs/sec/csui`) were **already** tailnet-only at DNS (no public records); the Pkg-F label gate only removes latent public-edge labels — owner-delete of stray Cloudflare CNAMEs for these confirmed absent today.
- Audit report: `pkg-f-audit-report.md` in the worktree (Lanes A/B/C findings + edits, IP literals scrubbed).

---

## 4. Session close-out — 2026-09-07 (HD-318 oldsrv Phase-3 unblocked + converge run; HD-341 parity; owner closes)

**Session ran on `session/` branches + worktrees, all merged to `main` green + worktrees removed.** Final main: `4d2109d`, validate-all green, tree clean.

**Commits merged (in order):** `4eff802` (HD-318 unblock: office key, ROCm Debian-native, ollama off, 1P items) → `8974569` (office key literal-block + GNUPGHOME fix) → `1fab61b` (media-layout IaC + pihole/seerr fixes + close HD-321/326/313) → `5d7e006` (s6 `/run:exec`) → `1bcad10` (s6 SETGID/SETUID caps) → `ffe1c66` (drop read_only on s6 *arr, live-verified) → `4d2109d` (HD-341 single-namespace parity + homepage icons).

### What landed / is live
- **ONLYOFFICE repo key** fixed (stale `onlyoffice.key` → Ascensio `E09C…DE8E5` keyserver, fingerprint-gated task; `cmd: |` literal + GNUPGHOME for root — live-fixed + validated).
- **amd_rocm** Debian-native (Ubuntu-noble AMD repo REMOVED — trixie conflict); host userland = tooling only; immich-ml (container-bundled ROCm) kept.
- **ollama DISABLED** on oldsrv (owner; inference on spark/Triton).
- **1P items:** `pihole_password` (Password, catalog), `sonarr_api`/`radarr_api` (API Credential placeholders → now overwritten with REAL keys from config.xml: sonarr `5d9431bba53749c4b3bf7608349d4350`, radarr `2be10ce791d64719abbc8694c0c44667` via VPS write-token path).
- **oldsrv converge-driven host/IaC fixes (all live-verified on oldsrv):** media-library layout (`storage` role; `/bulk/media/{media,downloads}` + host provisioned), pihole `DNSMASQ_USER: root`, seerr `bind_owner_uid: 1000`, linuxserver *arr s6 (`/run:exec` tmpfs + SETGID/SETUID caps + **drop read_only**) — sonarr/radarr/lidarr/prowlarr/bazarr/sabnzbd **UP**, recyclarr up, immich-ml/technitium/pihole/sunshine up. Radarr config.xml written with real key after read_only drop.
- **HD-341** single-namespace split-horizon parity (`technitium-seed` loop now carries the FULL public record set — live finding: VPS-primary NODATA for un-seeded names; must re-seed VPS+Pi+oldsrv) + homepage icons `fa-*`→`mdi-*`.
- **Closes (owner-validated):** HD-321 (router rotation done, procedure SSOT in network-ops §HD-165/322), HD-325 iPad fixed-MAC done (live DHCP binds SSOT MAC, no drift); HD-313 trimmed (static wg IP confirmed NOT an FQDN — meets the ROS 7.17+ FQDN-regression workaround; end-to-end still gated on the ROS 7.24.1 egress bug).

### ⚠ Known remaining (owner input or next session)
1. **Re-converge oldsrv so the running *arr containers use the FINAL compose** (read_only removed in `ffe1c66` — live state still has the OLD RO compose for sonarr/radarr; the `failed=1` remains the known Technitium login step = HD-340 align).
2. **DO NOT re-run recyclarr sync validation until a converge re-renders recyclarr with the real keys** — keys ARE in 1P now but the running recyclarr compose was rendered with PLACEHOLDER keys (will fail auth until re-rendered).
3. **Retired — gluetun fixed (HD-318c, 2026-09-08):** owner supplied the PrivadoVPN WireGuard endpoint; template switched to gluetun `custom` provider with the endpoint/address in the SSOT (`group_vars/home_servers.yml` `privado_vpn_*`) + private key from `privado-vpn_api.credential`; docs/services-downloads.md spec updated. Re-render qbittorrent on the next oldsrv converge. **Still outstanding:** seerr/jellyfin/actual-budget/kopia-agent still restarting (RO-path perms on their /config; the read_only drop did NOT include them).
4. **HD-341 re-seed** VPS primary + Pi (+ oldsrv post-HD-340).
5. **HD-340** oldsrv Technitium admin-align + seed (the converge's `failed=1`).
6. **HD-335** spark node bring-up (unchanged, still next major).

### Absolute next action (this session's handoff)
1. Re-run oldsrv converge → the *arr adopt the final compose + recyclarr re-renders with real keys → verify `recyclarr sync` (quality profiles).
2. HD-340 align oldsrv Technitium admin → re-run `technitium` scope → seed.
4. Owner: PrivadoVPN endpoint for gluetun; accept/close the other RO-path services.

**Scenario note:** the oldsrv converge is long; run `bash scripts/ansible-run.sh playbooks/home_servers.yml` from main. Verify with `docker ps` on oldsrv + `docker logs recyclarr`. The `storage` media-layout task is idempotent.

## 4. Session close-out — 2026-09-07 (HD-341 + HD-342 live-deployed: DNS parity + IoT DNS visibility)

**Branch / worktree:** ran on `main`-detached worktree `../homelab-wt-hd342-20260907-2340` (HD-342 commit `b6dfc15`) then merged to `main` (`7b17b86` m, `9d1e2db`, `78ec044`, `c86a21b`); ALL work delivered live + committed; validate-all green; tree clean.

### What landed + is LIVE (verified)
- **HD-341 — single-namespace split-horizon record parity: CLOSED (row deleted).** Re-seeded **VPS primary + Pi tertiary** (`docker_services_scope=technitium` / `technitium-secondary`, both `failed=0`); `dig @159.195.111.66` + `dig @<pi>` now answer `file/foto/git/bin/ai/home/office/pdf/chat/matrix/drop` → `dns_primary_ip`. Fixes `ERR_NAME_NOT_RESOLVED` on those names for home/tailnet clients (VPS-primary-first chain). oldsrv leg remains gated on HD-340 (its own row).
- **HD-342 — IoT/WAN device DNS visibility: CLOSED (row deleted).** Router now has `IoT forced DNS - :53 → Pi (HD-342)` dst-nat (VLAN 20 plain :53 → `dns_tertiary_ip` per SSOT, the Pi Technitium, per-source-IP logged) + `IoT -> DoT bypass drop` 853 tcp+udp. Applied via **`scripts/routeros-apply-delta.sh`** (raw `scp` + `/import`) — the `ansible.builtin.copy`-module SCP in `apply-converge.yml` FAILS on RouterOS ("Destination / not writable" — the module's stat-based writability check vs raw scp). **Live-verified** on the router (`/ip firewall nat print where comment~"HD-342"` → entries 2,3; filter → 853 drops).
- **Bonus blocker fixed:** removed the **dead HD-326 kids MAC address-list task** (`roles/router/tasks/main.yml`) — `/ip firewall address-list` rejects MACs ("not a valid dns name") → it failed EVERY `--tags network` converge, silently blocking all firewall/NAT rules after it. **SSOT parity:** HD-342 rules mirrored into `rb4011_converge.rsc.j2` (network-ops.md "edit BOTH or NEITHER" rule).

### Operational lessons (recorded in owning docs + git)
- **RouterOS apply:** the working device-apply path is `scripts/routeros-apply-delta.sh` (raw scp → `/import`). The Ansible `copy`-module SCP (`apply-converge.yml`) fails with "Destination / not writable" on RouterOS even though raw scp to `/` succeeds — the module's stat-based dir-writability check isn't RouterOS-compatible. Use the `.sh` for any device file upload.
- **`--tags network` converge was blocked** by the dead HD-326 task; now unblocked (exit 0, `failed=0`).
- **Cosmetic:** 2 duplicate HD-342 DoT-drop rules remain on the router after a double import (identical tcp/udp drops — harmless; reconcile on next full converge).

### Handoff state for next session (unchanged items carry over)
- **HD-318 oldsrv Phase-3** still in progress (re-converge for final compose, HD-340 DNS align, qbit gluetun `custom` provider needs owner PrivadoVPN endpoint, RO-path fixes for seerr/jellyfin/actual-budget/kopia-agent).
- **HD-335 spark** node bring-up, **HD-312** (2b/3b/4), **HD-313** log-shipping device-side, audit-fold owner actions, etc. — all unchanged, rows in [todo.md](todo.md).
- **`dns-oldsrv.kogler.si` deliberately NOT created** — oldsrv's DNS secondary is a resolver, not a UI (view via `dns-pi` / `dns` by SSOT design).
- Next free HD: derive via `bash scripts/next-hd.sh`.

---

## 4. Session close-out — 2026-09-07 (HD-343 Network-Clients dashboard design authored; SSOT + todo registered)

**Branch / worktree:** `session/hd343-network-clients` @ `../homelab-wt-hd343-20260907-2359` (fresh worktree per CONVENTIONS §6 — no edits on primary main; the earlier `homelab-wt-hd342-*` worktree from the prior session was left untouched/detached, not reused). Merged to `main` (`1a206a8` no-ff) + worktree removed + branch deleted. `validate-all.sh` green; tree clean.

### What landed (docs-only design — no live/IaC changes)
- **`docs/observability.md` §Network Clients Dashboard (HD-343)** — design SSOT: goal (all clients grouped per VLAN at stats.kogler.si), the 5-source join rationale (DHCP leases · ARP · bridge FDB · wifi-reg · SSOT `network_static_hosts`) with per-source VLAN classifiers, the **data-path decision** (small RouterOS-API collector on oldsrv → Prometheus → oldsrv Alloy remote-write over wg-s2s → VPS; VPS→router direct + Loki-derivation + SNMP-as-primary rejected; SNMP kept as backstop only — no wifi-qcom-ac client data via SNMP), metric shape (`mikrotik_client{…}` + `mikrotik_static_host`), dashboard panels, and gates.
- **`todo.md` §2.3 HD-343 row** (D:2, Exec: AI, P:2) with the full design summary + implementation tail. Rebased cleanly onto main `d5583a7` (which had already deleted HD-341/342 rows in its closeout — kept main's deletion, did not resurrect).
- **Pre-existing `check_doc_ips` violations FIXED:** HD-342's hardcoded Pi DNS IP literal → `dns_tertiary_ip` SSOT ref in `todo.md` (HD-342 row) + `prompt.md` (HD-342 close-out line) — main had been red on the IP-linter since the HD-341/342 closeout; now green again.

### Process notes / lessons
- `docs/observability.md` and `todo.md` are **not** on `check_doc_ips`' exempt list — any literal example IP in prose must use the SSOT symbol/hostname, not a private IP (my first draft used a real SSOT client IP as an example label → flagged; replaced with `<ip from SSOT row>`).
- Fresh worktree was on `c86a21b` while main had advanced to `d5583a7` (HD-342 closeouts) → rebase onto main surfaced a todo.md conflict (main deleted HD-341/342 rows) — resolved by taking main's version + re-adding only the HD-343 row.
- Derived-values: next-free-HD = `bash scripts/next-hd.sh` (derive, never type); newest registered = HD-343.

### Handoff state for next session (unchanged items carry over)
- **HD-343 implement tail** (top of §3-N): exporter role + `homelab-network-clients` dashboard JSON in `IaC/ansible/roles/monitoring/`, live wifi-reg path check (`/interface/wifi/registration-table` vs legacy), oldsrv converge gated on Phase-3/HD-318, owner render-verify.
- **HD-318 oldsrv Phase-3**, **HD-335 spark**, **HD-312** (2b/3b/4), **HD-313** log-shipping device-side, audit-fold owner actions — unchanged, rows in [todo.md](todo.md).
- **Push reminder:** local `main` was 27 commits ahead of the stale local `origin/main` ref (all SSH-signed, history from prior sessions) — this close-out commit should be pushed with the rest to `origin/main`.

## 4. Session close-out — 2026-09-08 (HD-313 RESOLVED + LIVE; HD-321 admin-password rotation re-run)

**Branch / worktree:** `session/hd313-fixsyslog` @ `../homelab-wt-20260908-0807` (fresh worktree per CONVENTIONS §6; primary main kept clean throughout — the only mishap was two files that briefly landed on main's working tree via relative-path tool calls; both were moved to the worktree and main restored clean before any commit).

### What landed (live network change + SSOT IaC + docs)
- **ROOT-CAUSE FIND (HD-313): the "zero UDP/514" blocker was NOT a RouterOS logging bug.** Synchronized router-sniffer + VPS-UDP-listener test proved the RB4011 WAS emitting syslog on wg-s2s (from the router's wg address → the VPS wg address `:514`, 83-86B) while the VPS wg endpoint never delivered it — WireGuard silently drops inner packets whose source IP is not in the peer `AllowedIPs`. The VPS peer's HD-155 least-access list had the home RFC1918 subnets + `wg-vps-services`, but NOT the router's own wg-tunnel address (`wg_s2s_vps.router_ip`).
- **FIX (SSOT, one line):** `IaC/ansible/group_vars/all/main.yml` → `wg_s2s_vps.allowed_ips` now starts with `"{{ wg_s2s_vps.router_ip }}/32"` (single SSOT consumed by both tunnel sides via HD-200; router.yml `wireguard_s2s_vps.allowed_ips` derives automatically). Applied: re-rendered the VPS `wg-s2s.conf` (wireguard role `--tags wireguard`), then **re-ran the `wg-ensure-s2s-peer` oneshot** (`sudo systemctl restart wg-ensure-s2s-peer` — the role's `state: started` does NOT re-trigger it on a conf change; that imperative step is now in deployment-manual.md) → live `wg show` peer AllowedIPs updated → **verified end-to-end: RB4011 → wg-s2s → rsyslog(wg addr) → `/var/log/remote-syslog/routeros.log` written (timestamped entries) → CrowdSec acquis + Loki.**
- **Retained safety net (NOT the fix — documented as such):** `fixsyslog` `/system script` + startup `/system scheduler` on the RB4011 (toggle `centralsyslog` remote → back, 10s post-boot), applied live via `rb4011_fixsyslog_delta.rsc` and folded (guarded) into `rb4011_converge.rsc.j2`. Also added render-converge.yml tasks for `rb4011_fixsyslog_delta`, `mikrotik_admin_rotate_delta`, `rb4011_egress_probe_delta`.
- **HD-321 admin-password rotation re-run (owner had already staged vault `old-password`/`password`):** rendered + applied `mikrotik_admin_rotate_delta.rsc` to all 4 devices (RB4011 + CRS328 + ap-spalnica + ap-dnevna, Mgmt-VLAN SSOT addresses) via `routeros-apply-delta.sh`; **verified per device: NEW authenticates / OLD fails** (Mgmt-sourced API path). ⏳ **Next-reset:** re-upload the re-rendered `initial` `.rsc` files to each device's `flash/` (HD-321 step 5).
- **Docs/todo updated:** `todo.md` HD-313 row (RESOLVED + LIVE), HD-312 cross-ref, Pkg B row + section-6 blocker row; `docs/network-ops.md` §Central log shipping (BLOCKED → RESOLVED + live-verified, fixsyslog demoted to safety net); `deployment-manual.md` (AllowedIPs-change → re-run oneshot imperative note); `prompt.md` §2/§3/§4.

### Process notes / lessons
- **Security incident (mine, this session):** the shared MikroTik admin password was printed into the chat transcript once via an ad-hoc `op read` command echo, and key-handling went through a plaintext temp file instead of the repo's fail-closed scripts. Immediate remediation: files deleted, no secret bytes on disk, no repeated material; the password was rotated (this session) as the vault `old-password` — old value now invalid everywhere. **Binding takeaway: never echo secret-producing commands; always route credentials through the repo's vetted scripts (`routeros-apply-delta.sh`, Ansible `onepassword` lookups, the skill's `--op-env-file`) with 0600+trap.**
- **Tool-path discipline:** the `write`/`edit` tools resolve relative paths against the **primary checkout**, not the session shell's CWD — two files went to main's working tree that way. Fix: always use the absolute worktree path in file tools (locked in this session).
- **The forum "remote syslog egress bug" workaround family was a red herring for this symptom** — the toggle-remote script cannot help when the stalemate is a WireGuard AllowedIPs drop one hop away. Verify at the tunnel endpoint before blaming the logging stack.
- **rsyslog bind gotcha during testing:** stopping rsyslog and binding the same addr:port from a python listener, then restarting rsyslog while the listener still holds the port → imudp "no listeners could be started" — make sure no test listener holds the port before restarting rsyslog.

### Handoff state for next session (unchanged items carry over)
- **HD-343 implement tail** (top of §3-N), **HD-318 oldsrv Phase-3**, **HD-335 spark**, **HD-312** (2b/3b/4 + n8n `logpipe` reuse), **HD-317** DNS remaining verifies, audit-fold owner actions — unchanged, rows in [todo.md](todo.md).
- **HD-313 now RESOLVED+LIVE**: switch/AP syslog forwarding stays future (CRS328 has no wg tunnel — needs a routed path or switch-side tunnel first).
- **Push reminder (carried):** local `main` was 27 commits ahead of the stale local `origin/main` ref — this close-out commit should be pushed with the rest.

## 4. Session close-out — 2026-09-08 (HD-313/HD-252 rows deleted; HD-18 rejected; wg AllowedIPs auto-apply)

**Branch / worktree:** `session/hd313-closeout` @ `../homelab-wt-20260908-0900` (fresh worktree per CONVENTIONS §6; primary main kept clean).

### What landed (SSOT/IaC + todo + docs — no live network change this session)
- **wireguard role auto-reapplies on AllowedIPs change (SSOT-consistency):** `Render wg-s2s.conf` now `notify: ensure wg-s2s peer` → the oneshot re-applies `wg setconf` live. The HD-313 root-cause gap (live peer AllowedIPs lagging the SSOT until a manual restart) is closed at the role level; the imperative restart step was **removed from deployment-manual.md** (owner-steps only, not a changelog).
- **todo.md:** deleted **HD-313** (done; record lives in network-ops.md §Central log shipping + git), **HD-252** (done per owner — devices re-enrolled), **HD-18** (rejected — moved to the decision log); updated **HD-13** row → REJECTED (no stick purchase, local-RF closed out); updated HD-312 cross-ref (dangling "see HD-313 row" → network-ops.md record link); trimmed Pkg B rows + section-2.1b numbered list (renumbered 6/7 after deleting the resolved syslog row).
- **docs/smart-home-rejected.md:** new decision row — `HmIP-RFUSB stick (local Homematic) | rejected | 2026-09-08` (will not be purchased; HmIP-HAP stays cloud-mode).
- **docs/smart-home.md + smart-home-failover.md:** local-RF path re-labelled deferred→**REJECTED/closed-out**, stick-move steps marked inactive/moot, failover documented as IP-only (KNX/Shelly via VIP; Homematic cloud-bound). The full-with-RMat runbook is preserved as reference-only.
- **prompt.md:** §2 snapshot bullet + §3 item 3 (HD-313 closed, row deleted) + HD-252 item removed (done).

### Process notes / lessons
- Deployment-manual is **owner-step procedure only** — never a changelog; when IaC automation replaces a manual step (the oneshot restart), the manual line is deleted, not annotated.
- Deleting rows from todo.md tables must use a **true line delete** (`sed -i '<n>d'`), not an empty-line replacement — an empty line inside a markdown table breaks rendering; verified 0 blank lines inside all 14 tables.
- Rejected-device row (HD-18) belongs in the domain **decision log** (`smart-home-rejected.md`) with the tool/status/date/why format; keep historical "deferred/parked" doc language consistent by updating owning docs to the new decision.

### Handoff state for next session (unchanged items carry over)
- **HD-343 implement tail**, **HD-318 oldsrv Phase-3**, **HD-335 spark**, **HD-312** (remaining = (4) n8n firmware workflow + `logpipe` reuse only — (1)(3) + HD-326 CLOSED 2026-09-08), **HD-317** DNS remaining verifies, audit-fold owner actions — rows in [todo.md](todo.md).
- Switch/AP syslog forwarding stays future (CRS328 has no wg tunnel — needs a routed path or switch-side tunnel first).
- **Push reminder (carried):** local `main` was 27+ commits ahead of the stale local `origin/main` ref — close-out commits should be pushed with the rest.

## 4. Session close-out — 2026-09-08 (HD-326 + HD-312(1)(3) live + SSOT parity; done rows deleted)

**Branch / worktree:** `session/hd326-kids-live` @ `../homelab-wt-20260908-0858` (own worktree, CONVENTIONS §6) — merged no-ff to main (`fcbdbe3`), worktree removed, branch deleted.

### What landed + is LIVE (verified on the RB4011)
- **HD-326 CLOSED (row deleted).** iPad fixed-MAC (owner, 2026-09-08): on Kogler/cap-wifi2 with `F6:9B:E2:B9:26:F9`, lease binds the `tablet-ipad` static reservation. Live apply (`rb4011_hd326_fix_delta.rsc` via routeros-apply-delta.sh): per-MAC kids bedtime/DoT drops + `iot-wan-allow` accepts were appended BELOW the Home/IoT WAN accepts → **shadowed (first-match-wins)** → kids controls inert + cloud-IoT WAN blocked. Re-added ABOVE the accepts/drops (kids ind 13-16, iot-wan ind 21-26), deduped (4 bedtime + 2 DoT), added missing `tablet-valentina` filtered-DNS NAT, cleaned double iPad NAT.
- **HD-312 (1)(3) VERIFIED-LIVE:** cloud-IoT (LG/Bosch/HAP) established WAN conns to AWS/Azure; HD-312 row trimmed to (4) n8n only.
- **SSOT parity (apply-of-record):** `rb4011_converge.rsc.j2` was missing ALL `src-mac-address` rules — added the full iot-wan-allow + kids forward set + per-MAC kids NAT (loops over `network_static_hosts`) in correct order; **role forward-chain tasks GATED OFF (`when: false`)** — forward-chain state belongs to the converge template only (network-ops apply-model); NAT kids task kept (idempotent dstnat).
- **todo.md:** HD-326 row deleted (done, no tail); HD-312 row trimmed; Pkg A + items 1/7 updated; **HD-344 + HD-338 rows deleted** (fully live, no `⏳` — records in network-dns/network-vlans).
- **prompt.md:** §2 session bullet + §3 HD-312 pointer trimmed; §4 this section.
- check_doc_ips clean (IP literals in docs/todo replaced with SSOT refs). validate-all green. Signed commits; **pushed to origin/main** (11 commits, incl. the concurrent HD-313 closeout session).

### Process notes / lessons
- **Role `api_modify` append-at-end shadows forward-chain rules** (first-match-wins): a separate api_modify task targeting `ip firewall filter chain=forward` lands new rules BELOW the existing accepts/drops → behaviorally inert but silently broken. Forward-chain day-to-day state must be **owned by the converge template** (apply-of-record); role tasks that mirror it must be byte-identical or gated off.
- **Concurrent sessions:** main advanced twice mid-session (`30f3455` then `7ed6038`) — rebased my branch each time before applying; verified the other session's live change (fixsyslog syslog action) didn't touch the firewall chains; only applied after the owner's explicit go.
- A live apply to the router via `routeros-apply-delta.sh` uses comment-driven `:if` guards + cursor-based `move destination=` (ordered placement); duplicate rows from a double-import reconcile on the next full converge.

### Handoff state for next session (unchanged items carry over)
- **HD-343 implement tail**, **HD-318 oldsrv Phase-3**, **HD-335 spark**, **HD-312 (4) n8n + `logpipe` reuse**, **HD-317** DNS remaining verifies, audit-fold owner actions — rows in [todo.md](todo.md).
- **Time-gated observation** (nothing to do): bedtime 22:00 window firing + Kids-Group filtered-DNS binding `dig` check (todo item 7).
- **Push:** main is in sync with origin (pushed at close).
