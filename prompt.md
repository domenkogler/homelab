> **SESSION #69 (2026-09-03, primary checkout `main` clean → commits signed + validate-all green):** **two work streams:** ① **HD-320 Shelly-on-Pi (earlier this session, commit `683b6ff`):** narrow `Home Pi (HA) -> Shelly RGBW2 REST tcp/80` forward rule in the router role (HD-319 KNX-exception pattern — the Pi node is not `trusted-admin`), + Shelly RGBW2 LED strips added to `lovelace-stanovanje` (`view_luci.yaml`/`view_pregled.yaml`: `light.kuhinja`, `light.wc_4_channel_1..4`, `light.orhideje`, `light.kopalnica_2`), + `skills/shelly/.env.op` vault-ref fix (broken `op://Private` removed — fleet is `auth:false` open; future cred → `Homelab-ansible`), + HD-320 row + owning-doc updates. **Owner/deploy-gated:** converge router (rule live), **add the 4× Shelly in the HA UI by IP** (`shelly-rgbw2-*` on IoT VLAN 20 — SSOT, config-flow only, no mDNS across VLANs) → verify light entities bind + dashboard renders. ② **HD-321 RouterOS admin-password rotation (this close-out):** rotated the shared `admin` password (`mikrotik-admin_login` `old-password`→`password`) on **RB4011 (`router` mgmt), CRS328 (`switch` mgmt), `ap-spalnica`, `ap-dnevna` (mgmt plane — SSOT `network-addresses-generated.md`)** — **verified per device** (old FAILS / new authenticates via the Pi's tagged-99 mgmt-sourced API path; the laptop's direct Home-IP API is INPUT-dropped → any password reads as a false lockout — the valid probe MUST be Mgmt-sourced). **Artifacts refreshed:** re-rendered `render-converge.yml` + `render-routeros.yml` so every `IaC/router/rendered/*.rsc` embeds the NEW password (hash-verified vs vault); **deleted stale** `ap_initial.rsc` (legacy universal AP script with OLD pw — per-AP `ap_initial-*.rsc` are current) and removed a stale `rb4011_converge.rsc` a partial `/import` had left on the router (would re-assert OLD on a reset). **Process notes (self-learn):** rotate ONLY via the repo-native **render → `/import`** (`scripts/routeros-apply-delta.sh` / `apply-converge.yml`, SSH `ansible` identity, pinned host key) — the manual Pi venv + python path is deprecated; the API `/import` step needs `librouteros` in the runner (use the SSH-import fallback when missing); `ansible` is a key-only user and unaffected. **⏳ Remaining (owner, next reset):** re-upload the re-rendered `initial` `.rsc` files to each device's `flash/` before any bootstrap reset so a flash-bootstrap sets the NEW password (live `flash/` not refreshed this session). Rotation runbook recorded in [network-ops.md](docs/network-ops.md) §Rotating the shared admin password. **Handoff edited from #68 (diff-rule).**

# prompt.md — Deployment Execution Handoff #69b — **router DNS-primary ordering VPS→oldsrv→Pi DHCP + /ip dns applied LIVE via API (HD-317 ⏳ item 2); full-converge apply infra fixed (recursion/cmd/SCP-path + ROS_SSH_PORT); dns CNAME + dns-pi 5380 publish in SSOT; HD-322 opened (full-converge idempotency); merged concurrent HD-320 Shelly firewall; next free HD: HD-323**
 (docs(handoff): prompt.md → #69 — router DNS-primary VPS→oldsrv→Pi LIVE (API-apply, 3-server cap), converge-apply infra, dns CNAME + dns-pi 5380, HD-321)
 (fix(ids): renumber full-converge-idempotency row HD-321->HD-322 — concurrent session took HD-321 for the RouterOS admin-password rotation (live, done))

> **SESSION #69 (2026-09-03, worktree `homelab-wt-20260903-1524` → branch `session/dns-primary-router`, 5 signed commits + validate green):** **Router DNS-primary on the MikroTik RB4011 — HD-317 ⏳ item 2 (DHCP/resolver ordering VPS→oldsrv→Pi) DONE + LIVE.** IaC + live: DHCP dns-server chain + `/ip dns` upstream now **VPS public IP (159.195.111.66) → oldsrv → Pi (all per SSOT dns_primary_ip/dns_secondary_ip/dns_tertiary_ip)** (+1.1.1.1,1.0.0.1 for /ip dns) on **all 7 VLAN networks** (incl. Mgmt 99, previously stale literal) — **applied via the RouterOS API** (KEY LIVE FINDING: the rsc-import path **mangles** multi-value `dns-server` silently — drops the Pi / substitutes the gateway; the API `set dns_server=<csv>` sticks exactly; the `router` role `api_modify` is the authoritative steady-state apply). **RouterOS caps `dhcp-server network` at 3 dns-servers** — aligned roles/router/tasks/main.yml + rb4011_converge.rsc.j2 + new rb4011_dns_resolver_delta.rsc.j2 to the 3-resolver chain (dropped the gateway-as-4th). **Converge-apply infra fixed** (all committed): apply-converge.yml recursion fix + `/import` cmd form (`/import file=<rsc>` — module splits on spaces) + **SCP to absolute `/` RouterOS path** (a relative dest leaked a rendered rsc with secrets into IaC/ansible/playbooks/ — deleted from primary + `.gitignore` only covers IaC/router/rendered/, hardened note); ansible-network-hop.sh owns the non-recursive converge_rsc default; routeros-apply-delta.sh gained **ROS_SSH_PORT** (apply via Pi-99 SSH tunnel); pppoe-client add guarded idempotent. **SSOT additions:** `dns` → vps.kogler.si CNAME (live-added by owner; now in cloudflare_dns/vars — apply via playbooks/dns.yml from home control plane) + technitium compose **5380 publish on the Pi tertiary only** (fixes the dns-pi web-UI 502; needs a Pi docker_services converge to land live). **Merged concurrent session's HD-320** (Shelly REST firewall in roles/router) via rebase onto main — no conflict, validate green. **HD-322 opened** (make the full converge fully idempotent on re-import — it still aborts on `/user add` line 44). Rebased onto main's HD-320. **Live applied + verified:** router DHCP networks + /ip dns. **Deferred/next (owner gates):** Pi converge (5380/dns-pi), VPS admin recreate via dns.kogler.si UI → seed, Cloudflare `dns` apply. **Handoff edited from #68 (diff-rule).** BRANCH NOT MERGED: concurrent session has uncommitted WIP on the primary (prompt.md + todo.md, their HD-321 admin-pw rotation) — merge `session/dns-primary-router` to main after their WIP commits; HD-321 = their rotation (done/live), HD-322 = my full-converge idempotency (open).

> **SESSION #66 (2026-09-03, session branches `session/oldsrv-zfs-headers` + `session/storage-zfs-create-fix` + `session/storage-zfs-create-fix2` + `session/hosts-ssot-fix` + `session/cockpit-dynamic-dir` → all merged to main):** **oldsrv Phase-3 first Ansible contact — provision reached the `office` role.** ✅ **mgmt-plane outage fully RESOLVED:** re-verified the tagged-99 plane was dark again (vlan-99 bridge entry had `ether2`/`ether10` in **UNTAGGED** — same class as #65's Winbox fix); fixed via render + `/import rb4011_pi_delta.rsc` (sets `vlan-99 tagged=bridge-lan,sfp-sfpplus1,ether2,ether10 untagged=ether7,ether9`) — oldsrv→router99/pi99 now 0% loss, ARP REACHABLE on the tagged leg. ✅ **`home_servers.yml` on oldsrv (Home leg) reached `office`:** docker ✅ (installed+enabled) · network ✅ (netd units, /etc/hosts) · **storage ✅ (`nvme` ZFS pool CREATED** — owner OK'd wiping the 970 EVO NTFS, backed up on nas; datasets + NFS mounts to nas live) · nut ✅ (client) · cockpit ✅ (+ `/opt/traefik/dynamic/cockpit.yml`) · desktop ✅ · office ⚠. **5 IaC bugs fixed + committed (all signed `G`, merged + pushed):** ① `linux-headers-amd64` in storage role — DKMS couldn't build zfs for the running trixie kernel (`7a084ef`); ②+③ fresh-build pool create never reached — import task aborted on missing pool; refactored to iterate `_pool_list.results` + skip import when `allow_create` (`5c3c1f5`, `6ee6b76`); ④ `/etc/hosts` rendered from `network_static_hosts` SSOT, not `hostvars.ansible_host` — the `-e ansible_host` override leaked into every entry (nas→oldsrv's own IP → NFS 'Connection refused') (`66dd629`); ⑤ cockpit creates `/opt/traefik/dynamic` before writing the route (`4e79bbe`). ⚠ **BLOCKED (exact stop-points for next session):** ① `office` role — ONLYOFFICE apt repo `squeeze` suite + GPG key mismatch (`E09C…DE8E5` missing) — needs repo/key fix; ② `amd_rocm` — ROCm noble-on-trixie dep conflict (distro `rocm-cmake`/`hipcc` vs ROCm 7.2.4 pins) — needs pinning/removal decision; ③ `pihole_password`/`sonarr_api`/`radarr_api` 1P items still MISSING (owner-created; `privado-vpn_api`/`signal_api`/`ha_api` now present) — `docker_services` (big service deploy) gated on them. ⚠ **`switch_port_map` gap is MOOT:** oldsrv is wired to **router ether2** (verified live — MAC on ether2, NOT the switch), so no oldsrv switch entry needed. · [todo.md HD-318](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 3. **Handoff edited from #65 (diff-rule).**
> **SESSION #65 (2026-09-03, session branches `session/knx-fw-rule` + `session/lovelace-fix` + `session/router-fix` + `session/dashboard-fix` + `session/closeout`):** **HA is now reachable at `https://ha.kogler.si/` (DNS cutover → VIP done), KNX is live (integration loads real entities), and the whole tagged-99 mgmt plane is BACK.** Root cause of the mgmt outage (resolved): the vlan-99 bridge entry had `ether10`/`ether2` in **UNTAGGED** instead of **TAGGED** (a prior full `router.yml` converge flipped them) → Pi's tagged Mgmt frames dropped → ARP FAILED cluster-wide. Fixed via Winbox one-liner (`/interface bridge vlan set … tagged=bridge-lan,sfp-sfpplus1,ether2,ether10 untagged=ether7,ether9`) + re-configure + verified. **3 router converge blockers fixed + committed (all were working-HD bugs):** ① HD-312 `iot-wan-allow` MAC-address-list task put MACs into `/ip firewall address-list` (IP/CIDR-only → live 'not a valid dns name') → removed (per-MAC mechanism is the `src-mac-address` forward-accept, which is already correct); ② HD-155 `vps_scoped_home` duplicate block (oldsrv/pi/router/switch twice) → dedup; ③ HD-313 logging action name `central-syslog`/`central_syslog` (hyphen/underscore rejected — RouterOS action names letters+numbers only) → renamed `centralsyslog`; `mikrotik-logpipe_api` seeded. **KNX (HD-319) LIVE:** narrow forward-accept `udp/3671 → knx-ip` (verified via router API, count 1); Pi reaches `knx-ip:3671`; KNX integration set up in HA (UDP Tunneling v1, NAT off, local IP blank) — entities load, lights/sensors visible. **Dashboard fixes:** `view_pregled.yaml` — removed the dead `weather.meteoblue…` card (meteoblue disabled, HD-22) + fixed `binary_sensor.rekuperator_filter_dirty` → `sensor.rekuperator_filter_replace`; also added the 2 missing rekuperator sensors (supply_temperature, room_humidity) per SSOT. Dashboard → HTTP 200, no config errors. **HA login:** created `admin`; OAuth/IndieAuth callback error traced to missing `external_url` (still unset — see owner step); login works on retry. **Lovelace include-path bug fixed** (`lovelace/view_*` → `view_*` relative; deployed).
> **Remaining (owner/next):** (1) KNX config-flow DONE — verify the 3 rekuperator GAs (12/1/*) answer on the bus (some GroupValueRead warnings); (2) set `external_url: https://ha.kogler.si` in HA (`core.config` null) to prevent future callback errors (add to `configuration.yaml.j2` + converge Pi); (3) phase-4 owner steps rd: Authentik OIDC on `ha` route, `ha.kogler.si` → VIP cutover DONE, failover runbook exercise, `dns-pi` web UI still 502 (secondary has NO 5380 port published — compose comment says 'NO host 5380' but the traefik-ha route expects it; needs a decision); (4) HD-312 phase 3 live: cloud-IoT regain WAN after lease turn-over — verify. **Handoff edited from #64 (diff-rule).**

> **SESSION #64 (2026-09-03, worktree `homelab-wt-20260903-1145` → branch `session/vps-converge`, commits `089ab7e`→`5ae21fb` merged + pushed):** **VPS converge DEPLOYED** — the HD-317/315/316 deploy-gate (owned by this session) landed.
> ① **Full `vps.yml` converge** `failed=0` up to the fragile layer; **surgical finishing runs** (`--tags monitoring` + `--tags docker_services -e docker_services_scope=crowdsec`) green → **Technitium primary UP** (`:53/udp+tcp` published; container on the `dns-servers` overlay pin (`tchnitium_dns_overlay_ip`, SSOT) + `traefik-public`; web UI serves on the overlay), **5 HD-315 Grafana dashboards** copied to `/srv/docker/grafana/provisioning/dashboards/` (file-provider hot-reload) + **homepage** tiles rendered + restarted → `https://home.kogler.si/` → **302 Authentik SSO**, **rsyslog** RouterOS receiver installed+restarted, **crowdsec** healthy with `a1ad/mikrotik` enabled.
> ② **4 live-fixes (pre-existing bugs fixed + committed, validate green):**
>    - `network` role dhcp-client MAC assert fired on **static hosts** (VPS) — HD-314 added `dhcp_client` to `network/vars/main.yml` for every host (fallback `none`), so `when: dhcp_client is defined` became a tautology → gated block on new `dhcp_client_optin` (`089ab7e`).
>    - **Literal `{{ vault['…'] }}` inside .j2 comments** crashed the render (`dict has no attribute '…'`; Jinja renders braces even in `#` comments) — swept 8 templates (`b21c835`).
>    - **rsyslog not installed** on the netcup minimal image → monitoring role now `apt install rsyslog` (the old comment claimed stock rsyslog exists — wrong on this image) (`f9321ad`).
>    - **Phantom `crowdsecurity/syslog-logs` collection** (not in the hub) crash-looped crowdsec on boot — dropped from `crowdsec_collections` + both owning docs (`5ae21fb`).
> ③ **traefik-tailnet sidecar** recovered from a compose-restart netns race (`docker compose up -d` on the project; manual §1.4b precedent).
> **⏳ Remaining DNS deploy-gate (owner + next steps):** (1) **seed the split-horizon static A records on all 3 Technitium instances** — `ha`→VIP, `dns-pi`→VIP, tailnet dashboards→`tailnet_sidecar_ip` — **BLOCKED: no `technitium_*` admin credential in 1Password `Homelab-ansible` (verified absent); the Technitium API seed needs it → create the 1P item + wire an Ansible seed role (currently a manual/API step per network-dns.md)**; `dns.kogler.si` will NXDOMAIN until then (no public record by design + empty zone on the fresh instance); (2) **router `rb4011` step** (DHCP/resolver ordering VPS→oldsrv→Pi — still gated on the tagged-99 mgmt-plane outage); (3) owner verify: `dig @159.195.111.66 dns.kogler.si`, tailnet `stats.kogler.si`, homepage family vs Domen technical. Handoff edited from #63 (diff-rule).

> **SESSION #61 (2026-09-03, worktree `homelab-wt-20260903-0941` → branch `session/ha-pi-provision`, commits `f6a6114` + `55b2a9d` merged + pushed):** **Phase-4 Pi HA-primary provision LANDED + LIVE.** Full `raspberry_pi.yml` run `failed=0` on `pi.kogler.si` (Debian trixie + Docker): **home-assistant-primary** (HA 2026.8.1 + keepalived **MASTER** on VIP `ha_vip`) · **technitium-secondary** (DNS :53 alive) · **traefik-ha** (TLS `*.kogler.si` valid, Verify 0) — all up + verified; `https://ha.kogler.si/` → **302**. HA boots **0 config errors**. Live-fixed this session: docker_services vault pre-pass crashed on entries without `enabled:` (Pi primary/secondary omit it) → defined+truthy ∪ missing-key = default(true); deploy-service first-boot guard stat used `{{ item }}` with `loop_var: ha_src_file` (undefined) + fail_msg referenced undefined `ha_standby_path` on the Pi → fixed to loop var + registered results; **technitium compose** duplicate `cap_add` + `read_only: true` crash-looped (`Read-only /etc/dns, /var/log/technitium`) → merged caps + relaxed read_only (forgejo precedent) + tmpfs `/etc/dns`; **knx-hass-gen.py emitted a `knx:` wrapper** inside knx-entities.yaml (included via `knx: !include`) → nested `knx:` invalid — removed wrapper, regenerated (legacy rekuperator + appliance sensors preserved); **meteoblue NOT in HA 2026.8.1** → platform failed setup + blocked HA (incl. proxy trust) — weather block disabled until available/HACS; **`configuration.yaml` trusted_proxies** needed the Pi traefik-ha host IP (`network_mode: host` → source = the Pi's own Home IP, SSOT `pi` row), AND a **stale `.storage/http` on the Pi** held `use_x_forwarded_for` absent (HA 400 behind proxy) → deleted it so HA re-derives from YAML; **ha-cert-sync.sh** excludes `dump/` (ACME private keys never leave the issuer) — plus a **VPS-side operator step that was missing**: `ha-sync` key was never authorized on the VPS `ansible-admin` (rsync-only wrapper + key now added; rsync installed on the VPS). **Close-out (same session):** `docs/deployment-pi-provision.md` status banner + §6 success criteria marked LIVE; todo.md HD-307/HD-310 trimmed to owner-gated remainders. **Owner follow-ups:** KNX UI config-flow import (tunneling `knx_router_ip`), Authentik OIDC on the `ha` route (no Forward-Auth), `ha.kogler.si` DNS cutover → VIP (`smart-home-failover.md`), failover runbook exercise. **Handoff edited from #60 (diff-rule).**

> **Role:** entry point for the next session. This handoff is a **pointer index** — every item's owning doc/row holds the detail. Start with README.md §0 (intent routing), then §1 context, then §3 execution order. Read [todo.md](todo.md) for the full row bodies.

> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 · [scripts/guard-session.sh](scripts/guard-session.sh) · [todo.md](todo.md) · archive: [reports/changelog.md](reports/changelog.md), [reports/deployment-journal.md](reports/deployment-journal.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — §Tags & surgical runs (`docker_services_scope`, `base` tier) + §Deploy Timing Runbook.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — compose/secret conventions + fail-loud rule.
4. [docs/network-vpn.md](docs/network-vpn.md) — §Pattern A tailnet sidecars + current tailnet edge state + §MagicDNS (extra_records design).
5. Owning `docs/*.md` (per task) — the decision-logs + spec SSOT; read before re-deciding anything. Historical changelog/journal rows live in the frozen `reports/changelog.md` / `reports/deployment-journal.md` (archive-only).

## 1. Environment (WSL Debian primary, ext4) — as of #67, the repo runs from the WSL Debian primary on ext4 (not Windows)

Adapted to the WSL Debian primary (repo moved to ext4; the Windows-runner/9P-gate era is legacy — see "9P gate: N/A" in §4). `python3`/bash/LF/UTF-8 no-BOM. Secrets → 1Password item.field only, `>-` for YAML renders. **Self-learned (2026-08-26):** do NOT use multi-line bash heredocs with backslashes/backticks (mangled through `bash -c`) — write patch scripts to a temp file and run them. **Signed-commit gotcha (still valid):** keys in `~/.ssh/github_signing`/`github_auth`; `Couldn't find key in agent` → `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.

## 2. State snapshot (start of next session)
- **HD-312 3-SSID WiFi — phases 1 + 2a DONE + LIVE (2026-09-03).** Live SSIDs = **Kogler (VLAN 10) + Kogler IOT (VLAN 20) + Kogler guest (VLAN 30, 5GHz)**; IOT-WAN + Kids SSID configs **deleted** (configs + security profiles removed live + from SSOT; items kept in 1P, unreferenced). Phase 1 static-IP sweep: 4× Shelly RGBW2 + 4 family WiFi clients (valentina/phone-domen/phone-martina/tablet-ipad) + UPS + iLO now have MAC + static reservations in `network_static_hosts` (router converge applied them live — UPS already bound its `.99.9`; iLO/phones/iPad bind at next lease renewal/reconnect). Phase 2a: guest turned up via idempotent deltas (`rb4011_guest_delta`/`crs328_guest_delta`/`ap_guest_delta` .rsc.j2, no secrets, folded into render-converge.yml); CAPsMAN provisioning now band-split by SSID `band:` (2.4GHz = Kogler+IOT, 5GHz = Kogler+guest). **⚠ LIVE GAP → RESOLVED (2026-09-03, phase 3 live): the cloud-IoT appliances (2× LG AC, 3× Bosch, HAP) were on the deleted `Kogler IOT WAN` SSID (VLAN 21, WAN allowed) — moved to `Kogler IOT` (VLAN 20 = no internet).** HD-312 phase 3 (`src-mac-address` WAN accepts) **LIVE 2026-09-03** — the per-MAC forward-accept is on the router; the appliances regain WAN as their DHCP leases turn over to the reserved VLAN 20 pool (verify). ⏳ Remaining: (2b) CAPsMAN per-MAC `access-list` vlan-id; (3b) `kids-*` MAC-lists active + kids-control off VLAN 40; (4) n8n workflow + scoped router API user. Full spec [network-vlans.md](docs/network-vlans.md) §3-SSID + [network-rejected.md](docs/network-rejected.md) + [todo.md](todo.md) HD-312.
- **HD-310 FINAL port model = LIVE + SSOT (2026-09-02)** — `router_port_map`: **old-srv ether2 = pvid10 + tagged[99] · laptop-domen ether3 = pvid10 + tagged[] (untagged Home only — mgmt via Pi hop) · rpi ether10 = pvid10 + tagged[99]** (nas-ilo/homematic-ap stay pvid99). Live router verified: VLAN10 untagged=ether2,3,10 · VLAN99 tagged=ether2,10 (+trunk/self), untagged=ether7,9. Services `available-from` = **Mgmt subnet only** STRICT again ([network-addresses-generated.md](docs/network-addresses-generated.md)) (Home→Mgmt forward removed — **Home must never reach core infra**; Pi `eth0.99` tagged leg is the only mgmt-plane client). SSH: `~/.ssh/config` `pi99`→mgmt `pi` / `router99`→`router` mgmt via ProxyJump `pi` (IPs in [network-addresses-generated.md](docs/network-addresses-generated.md)) — verified. **Winbox GUI tunnels (2026-09-02, SSOT [network-vpn.md](docs/network-vpn.md)):** `ssh -N switch-wb`/`ap-spalnica-wb`/`ap-dnevna-wb` → `127.0.0.1:8292/8294/8295` (forward runs ON the Pi — the Mgmt-leg holder; a ProxyJump-to-device final hop leaves RouterOS SSH hosting the forward, which breaks Winbox auth — live lesson). Cable moves: ✅ **DONE 2026-09-02** — Pi → **ether10**, laptop → **ether3** (owner moved the cables; mini-switch freed); live end-to-end re-verified (Pi Home leg, `router99` via tagged-99 hop, router bridge ports pvid=10 on ether2/3/10 + pvid=99 on ether7/9).
- **HD-302 Comtrend modem mgmt = LIVE + SSOT (2026-09-02, commit `22abb8a`)** — live fix: `router_on_modem` `/32`→`/24` on ether1 (connected route to the modem subnet) + **srcnat masquerade** out ether1 (dst=`comtrend_modem.modem_mgmt_ip`:80/443) because the consumer ONT only answers its web UI to its directly-connected peer (router on the modem LAN) — forwarded HTTP from deeper subnets is silently ignored. **Live-verified:** laptop → modem UI returns 401 (auth page); router can ping the modem. SSOT: `comtrend_modem` (router.yml), `rb4011_converge.rsc.j2` SNAT rule, generated SSOT re-rendered. ⏳ **Remaining:** private DNS `modem.kogler.si` → needs the first Technitium (oldsrv offline).
- **Phase 2 (nas) DONE + LIVE (2026-09-03, HD-314; row closed — record in owning docs).** `storage.yml` failed=0. ZFS pools tank (mirror) + bulk (RAIDZ2) IMPORTED + datasets/props; NFS exports → oldsrv (`/tank/data`, `/bulk/media`, `/bulk/data/immich-thumbs`, SSOT oldsrv Home IP `/32`, root_squash anonuid/gid=1005) — `exportfs` verified; sanoid+syncoid timers; Samba; **NUT master LIVE** (PowerWalker `upsc` battery 100% / Innova Unity; `upsd` :3493 + `nut_exporter` :9199 + `zfs_exporter` :9198 active); cockpit-storaged (trixie has no cockpit-zfs). nas's DHCP client-id = MAC (`dhcpcd.conf clientid mac`) → binds its **reserved Home static** (SSOT) instead of the dynamic `.100`. Host state + full fix list: [hardware-nas.md](docs/hardware-nas.md) + [hardware-ups.md](docs/hardware-ups.md) + [deployment-manual.md](deployment-manual.md) §Phase 2. ⏳ **Remaining:** battery-pull test (owner, HD-06), `bulk/migrate` landing-zone redistribution (HD-207).
- **⚠ Live-ops flag (2026-09-03) — RESOLVED same day:** the **router mgmt-plane (tagged-99 → the router's SSOT Mgmt IP) was intermittently unreachable** during the nas session (ARP FAILED from the Pi; Home-plane ICMP OK). Root cause (confirmed via WinBox this session): a converge had flipped the VLAN-99 bridge tagged memberships on `ether2`/`ether10` to UNTAGGED → Pi tagged Mgmt frames dropped. Fixed with the Winbox one-liner (tagged=ether2,ether10 restore) → `router99`, API over the Pi hop, and every mgmt client now ARP-REACHABLE. Recorded RESOLVED in [network-ops.md](docs/network-ops.md).
- **DNS: oldsrv/Technitium (primary DNS) — oldsrv back on `.101` (DHCP lease, 2026-09-03)** — it's powered on + pingable at its transient lease; still needs the Phase 3 provision (networkd units + DHCP client-id=MAC) before it runs Technitium at its reserved Home static. Laptop DNS stays `1.1.1.1`/`1.0.0.1` until then.
- **Pre-existing (not from this session, unaddressed):** `sync-skills.sh` drift — deployed `~/.pi/agent/skills` (`plan-task`, `run-task`) differ from repo `skills/` (repo = SSOT). Fails gate item 13 at HEAD too. Owner decision: `bash scripts/sync-skills.sh --push` (deploy repo→deployed) or `--pull` (capture deployed→repo).

- **Phase 1 (VPS edge) is LIVE** — full enabled `docker_services` set converges green; observability + Authentik + headscale + traefik-tailnet all up. **HD-298 (pi-dev pty.node) DONE + LIVE-VERIFIED** (#63-era; row closed — record in owning docs): `pi-dev:8080` 200 via sidecar; 4-layer fix (ignore-scripts removal + glibc node:24 + npm allowScripts + cap_add CHOWN/FOWNER/DAC_OVERRIDE).
- **HD-297 tailnet clean URLs: LIVE.** `https://dsh.ts.kogler.si` / `https://pi-dev.ts.kogler.si` → **200**; plain `*.kogler.si` → **302 → sso** — root cause was a comment-brace render crash (`fc2b0a1`), middlewares.yml now renders + loads (detail §3 item 6).
- **Phase 4 (Pi): FULLY PROVISIONED + LIVE (2026-09-03, HD-307/310/04).** Pi OS Lite installed (hostname `pi`, user `admin` + `ansible-admin`, SSH key `ansible-admin_ssh`); port model + physical final (Pi on **ether10** untagged Home 10 + tagged Mgmt 99; laptop on **ether3** untagged Home only); host-side dual-home = two NM keyfiles (`pi-eth0.nmconnection` Home + `pi-mgmt.nmconnection` tagged-99, verified). **Full `raspberry_pi.yml` provision run `failed=0`** — **home-assistant-primary** (HA 2026.8.1 + keepalived MASTER on VIP `ha_vip`), **technitium-secondary** (DNS :53 alive), **traefik-ha** (TLS `*.kogler.si` valid) ALL UP + verified; `https://ha.kogler.si/` → **302**; HA boots **0 config errors**. Live-fixed: docker_services enabled-set, first-boot guard loop_var, technitium read_only/cap_add, knx-entities `knx:` wrapper, meteoblue-not-in-2026.8 (disabled), trusted_proxies + stale `.storage/http`, ha-cert-sync `dump/` exclusion + VPS ha-sync key auth/rsync. **Session #65 completion:** `ha.kogler.si` DNS cutover → VIP DONE (verified), **KNX LIVE** (HD-319 firewall rule + HA config-flow UDP v1 → entities load), **Lovelace dashboard fixed** (relative includes + Pregled card fixes), logged in as `admin`. ⏳ **Remaining (owner-gated):** Authentik OIDC on `ha` route, `external_url` set, `dns-pi` 502 decision, failover runbook. Runbook: [deployment-pi-provision.md](docs/deployment-pi-provision.md) (status = fully provisioned).
- **RouterOS apply convention (decided 2026-09-01, [network-ops.md](docs/network-ops.md)):** `initial`(manual bootstrap) → `converge`(=full, SSOT, idempotent, universal apply) → `delta`(transient only, fold back). API = verify/idempotent only. **Apply = render → /import (SCP over SSH, pinned host key, `ansible` identity).** **⚠ Ordering pitfall (live 2026-09-01):** a FORWARD accept appended at the tail lands BELOW the repo's mid-list "Default deny inter-VLAN" and is shadowed (HD-307 hit this: rule #33 below deny #25 → dead). Fix pattern (in network-ops.md): dedup → re-add → `move … destination=$deny` (RouterOS 7 = places BEFORE) → always verify position. Idempotent deltas: `rb4011_pi_delta.rsc.j2`, `rb4011_hd307_delta.rsc.j2` (both committed, rendered via render-converge.yml).
- **HD-309 (Shield Media→WAN) DONE + LIVE-VERIFIED (2026-09-01; row closed — record in network-ops.md + git).** Root cause was the router DNS tertiary (SERVFAIL, no upstreams) — NOT the forward-ACCEPT. Fixed: `/ip dns servers=1.1.1.1,1.0.0.1` + row-73 WAN accepts. Live: Shield has 6 established Media→WAN conns. Tooling: `scripts/routeros-apply-delta.sh`.
- **Phase 1.5 network redo DONE except close-out (2026-09-02):** switch + APs reset + `.pub`/`.rsc` re-rendered (flash/ fix, HD-304); router on the new design; **wg-s2s handshake RESOLVED + LIVE (HD-285, closed — two-key fix, commit `bc4eb95`, record in owning docs)**; owner MikroTik password rotation pending.
- **wg-s2s (HD-285, row closed) RESOLVED + LIVE 2026-09-02 (two-key design fix).** Root cause: shared `wg_password` on both ends → identical pubkeys → router handshaking with itself. Now: router `wg_password` + VPS **NEW `wg_password_vps`** (distinct), oneshot owns the VPS iface (networkd create-only + Unmanaged), router HD-155 accept moved above the Default deny, router endpoint = `vps.kogler.si` FQDN. Live: handshake fresh (keepalive), VPS→router-mgmt 0% loss. Commit `bc4eb95`. · owning docs + git history
- **Vault state:** `check-vault-items.sh --strict` → **all needed items present, MISSING list empty** (exit 0; includes `technitium_login`/`technitium_api` created 2026-09-03 with the seed role — count re-derive at write time per derived-values ban).
- **ID registry:** next free = max(HD)+1 in [todo.md](todo.md) — always re-derived at write time (CONVENTIONS §1/§4); NEVER type a literal (newest rows: HD-310, HD-311, registered 2026-09-01).
- **Coordination:** headplane/headscale = SEPARATE lane (D) — coordinate, never fold into other converges.

## 3. Next-session execution order (pointers — row bodies in todo.md)


### 3-N. NEXT (priority order)

0. **oldsrv Phase 3 — ⚠ BLOCKED (HD-318, carryover from #66).** oldsrv first Ansible contact reached the `office` role; mgmt-plane outage RESOLVED; docker/network/storage (nvme ZFS pool + NFS)/nut/cockpit/desktop live. ⚠ **BLOCKED (exact stop-points):** ① `office` role — ONLYOFFICE apt repo `squeeze` suite + GPG key mismatch (`E09C…DE8E5` missing) — needs repo/key fix; ② `amd_rocm` — ROCm noble-on-trixie dep conflict (distro `rocm-cmake`/`hipcc` vs ROCm pins) — needs pinning/removal decision; ③ `pihole_password`/`sonarr_api`/`radarr_api` 1P items still MISSING (owner-created) — `docker_services` big-service deploy gated on them. oldsrv becomes the DNS secondary (Technitium, HD-317) + NUT client + heavy services. · [todo.md HD-318](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 3

1. **HD-312 — 3-SSID WiFi + per-MAC control (phases 1+2a+3 DONE + LIVE; 2b/3b/4 next).** ⏳ Remaining: (2b) CAPsMAN per-MAC `access-list` vlan-id; (3b) `kids-*` lists active + kids-control off VLAN 40 (owner: define kids device set); (4) n8n firmware workflow + scoped router API user. · [todo.md HD-312](todo.md) · [network-vlans.md](docs/network-vlans.md) §3-SSID

2. **HD-317 — Technitium DNS-primary on VPS — VPS converge DONE + LIVE; seed role DONE; ⏳ remaining.** (1a) **DURABILITY + SEED ROLE DONE 2026-09-03 (`6457522`):** compose binds `/etc/dns` → `/opt/technitium/dns` (was tmpfs — wiped admin/zones on restart, the VPS REFUSING cause); `technitium_login`/`technitium_api` 1P items created; new `technitium-seed` role bootstraps admin + seeds kogler.si zone + split-horizon A records on every `docker_services` converge. **Pi instance SEEDED + verified live** (`dig @pi ha.kogler.si` → VIP; dashboards → `tailnet_sidecar_ip`). ✅ **RB4011 router step DONE + LIVE (SESSION #69):** DHCP dns-server chain + `/ip dns` upstream = **VPS→oldsrv→Pi** on all 7 networks (incl. Mgmt 99) — applied via the RouterOS API (live finding: rsc-import mangles multi-value `dns-server` silently drops the Pi; the role's api_modify is authoritative); RouterOS caps `dhcp-server network` at 3 dns-servers → aligned roles/router + rb4011_converge.rsc.j2 + new rb4011_dns_resolver_delta.rsc.j2 to the 3-chain. **ALSO:** `dns` public CNAME added to the IaC SSOT (owner added it live; needs `playbooks/dns.yml` from home control plane) + **dns-pi 5380 publish (Pi tertiary)** in the technitium template ← fixes dns-pi web-UI 502 — **needs a Pi docker_services converge to land live**. **VPS primary admin recreate pending** — its auth.config admin pw ≠ 1P item (owner recreates via `dns.kogler.si` UI once the Traefik @file-middleware issue is fixed) → then re-converge seeds VPS+oldsrv automatically; (3) owner verify `dig @159.195.111.66 dns.kogler.si` + tailnet `stats.kogler.si` + LAN resolv.conf ordering + WAN-out failover. **NEW HD-322:** make the full `rb4011_converge.rsc` fully idempotent on re-import (aborts on `/user add`). · [todo.md HD-317](todo.md) + [todo.md HD-322](todo.md) · [network-dns.md](docs/network-dns.md) · [docs/services-dns.md](docs/services-dns.md)

3. **HD-313 deploy-gate (IaC DONE 2026-09-03).** RouterOS → VPS CrowdSec + Loki log shipping — seed 1P `mikrotik-logpipe_api` (ISH-seeded per #65) → next **router.yml converge** (system/logging action + topics + logpipe user) → **VPS monitoring + crowdsec** deploy → live-verify `cscli alerts list` + Loki search find RouterOS lines. · [todo.md HD-313](todo.md) · [network-ops.md](docs/network-ops.md) §Central log shipping

4. **Phase 1.5 cutover remainder (operator + AI).** switch reachability + CAPsMAN steady-state imported; router + switch roles re-converged LIVE via the Pi-99 hop (ansible-network-hop.sh); live switch/router bugs fixed in roles. ⏳ **Remaining:** APs re-join CAP; owner MikroTik password rotation; parked `validate-routeros7-syntax` linter stays parked. · [deployment-manual.md](deployment-manual.md) §1.5 · [todo.md HD-304](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 1.5

5. **HD-299 — Technitium primary → VPS** (new `dns-servers` overlay, 3-instance DNS HA, split-horizon static A records for plain `*.kogler.si` on tailnet). ⏳ IaC done (via HD-317) + seed role done (Pi seeded live; VPS+oldsrv pending on VPS admin recreate); owner RB4011 forwarder step. · [todo.md HD-299](todo.md) · [docs/network-dns.md](docs/network-dns.md) line 18/67

6. **HD-296 + HD-297 — tailnet subdomains + traefik-tailnet clean URLs (dsh/pi).** (a) IaC CLOSED; (b) DEPLOY done — `dsh.ts`/`pi-dev.ts` 200; **(c) RESOLVED + LIVE (2026-09-03)** — plain `*.kogler.si` → 302 → sso. Root cause: template comment brace `{{ vault['…'] }}` crashed the render (`dict has no attribute '…'`, fix `fc2b0a1`) → middlewares.yml never landed; rendered + loaded since the 12:11 converge. ⏳ owner browser verify. · [todo.md HD-296](todo.md) + [todo.md HD-297](todo.md) · owning docs

7. **HD-268c owner enable-flow (tailnet sidecars live).** Mint preauth keys → seed 1P `tailscale_dsh_api`/`tailscale_pi_dev_api` → flip `*_tailnet_sidecar_enabled` → scoped converge → verify UIs. 4-step flow in `group_vars/vps.yml`. · [todo.md HD-268](todo.md)

8. **HD-264 Renovate sandbox (owner, parked).** (a) commit versioned manifest + `renovate.json` to `domen/test`; (b) external re-launch timer; (c) verify PR lands; (d) flip `RENOVATE_REPOSITORIES` → the internal Forgejo homelab repo (NOT GitHub — superseded note). · [todo.md HD-264](todo.md) · [docs/deployment-renovate.md](docs/deployment-renovate.md)

9. **Audit-fold owner actions (from the merged audit):** HD-280 fail2ban (add traefik accesslog + filter, or drop http-auth jail) · HD-288 sunshine owner-decision + port binding (HD-289 deleted — in-template) · HD-292 VPS `docker image prune` · HD-286 vault-escape template comments. Row bodies carry owning docs. · [todo.md](todo.md) §§2.4/2.8

10. **Metabase gates (PARTIAL DONE):** HD-241 SMTP *Send test email* + invite flow; HD-242 connect both sources + import dashboards + SELECT-only proof. · [todo.md HD-241](todo.md) + [todo.md HD-242](todo.md)

11. **AI stack pre-deploy gates:** HD-105 secrets + OIDC providers (blocks HD-100/101/103/104); then HD-101 SSO verify ride-along, HD-247 (scoped keys — HD-102 PGVector superseded by HD-267/268 Qdrant), HD-248 OWUI split (depends HD-247), HD-103 docling, HD-104 openclaw onboard. · [todo.md HD-105](todo.md) · [docs/deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md)

12. **HD-112 Zipline go-live legs** (first deploy human-gated; post-up seeding runbook in compose header; family drop script). · [todo.md HD-112](todo.md)

13. **HD-252 pending cleanup (owner):** nodes list EMPTY — re-enroll laptop + phone via the now-working OIDC flow under `domen@kogler.si`. · [todo.md HD-252](todo.md)

> **Handoff diff-rule (CONVENTIONS §4):** this handoff is edited from #66's — every prior §2/§3 open item either appears or is explicitly resolved (closed items moved to the owning docs + git history only; frozen changelog/journal are archive). **Derived-values ban:** next-free HD quoted from todo.md, never typed. Next free HD: derive via `bash scripts/next-hd.sh` (HD-315+).
## 4. Working rules (binding)

Fresh worktree per session BEFORE any edit — mechanically enforced (`bash scripts/guard-session.sh`; `validate-all.sh` hard-fails primary+main+DIRTY); merge back only committed+green, ff-only; primary = merge station only. **9P gate: N/A on the WSL Debian primary (repo moved to ext4 — no /mnt/d drvfs staleness; the gate is legacy from the Windows-runner era).** Surgical `--tags docker_services -e docker_services_scope=<svc>`; secrets 1P-item.field only + `>-` for YAML; persisted Authentik tokens always `expiring=False` (HD-216); owning doc + todo update in same change; English; relative links; Authentik blueprint pin array attrs (HD-231); don't touch headplane/headscale unless asked. No multi-line bash heredocs with backslashes/backticks — write+run temp script files instead. (changelog/journal frozen → `reports/`, archive-only.)