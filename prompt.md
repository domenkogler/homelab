# prompt.md — Deployment Execution Handoff #36 — HD-264b + HD-268c IaC DONE + HD-268c LIVE (dsh + pi-dev tailnet sidecars UP on headscale) + HD-268c sub-task: `dsh.kogler.si` / `dsh.ts.kogler.si` / `pi.kogler.si` / `pi.ts.kogler.si` A-records added to headscale `dns.extra_records`. NEXT=audit-fold HDs (HD-276..HD-295) + Renovate HD-264a (commit manifest to domen/test) + HD-264d (flip RENOVATE_REPOSITORIES to domen/homelab as final step)

> **SESSION #36 (2026-08-29, worktrees `homelab-wt-20260829-2132` + `homelab-wt-20260829-2149` + `homelab-wt-20260829-2155` + `homelab-wt-20260829-2159` + `homelab-wt-20260829-2205` + `homelab-wt-20260829-2207`, branches `session/hd268-then-hd264-20260829-2132` + `session/hd268c-enable-20260829-2149` + 4 fix-forward branches, ALL MERGED + PUSHED — 8 signed commits total, all `G`):** **HD-268c tailnet sidecars LIVE on headscale + HD-264b Renovate run-model fix live-verified.** The full session spanned 7 worktrees (one per fix-forward cycle as live deploys surfaced real-world bugs). **HD-264b done first:** `restart: on-failure:5` + `RENOVATE_SCHEDULE: "* 22-23,0-4 * * *"` killed the Restart-7044 per-minute churn; live verify on VPS (2026-08-29T19:47Z) shows `RestartCount=0`, container exits cleanly after each scan, Docker no longer auto-restarts on clean exit. **HD-268c done second:** owner-side preauth keys minted via live headscale (user id 2 = `domen@kogler.si`, `--reusable --expiration 90d --tags tag:dsh` / `tag:pi-dev`), seeded as 1P vault items `tailscale_dsh_api` + `tailscale_pi_dev_api` (API Credential category, length=88 tails match the minted preauth keys); both sidecars brought up via scoped converges; live verify on headscale shows nodes 7 (`vps-dsh`, `tag:dsh`, online) + 8 (`vps-pi-dev`, `tag:pi-dev`, online at last check; pi-dev's DERP handshake may need a refresh after recreate). `tailscale serve status` inside both sidecars shows `tcp://vps-dsh.ts.kogler.si:3080 -> 127.0.0.1:3080` and `tcp://vps-pi-dev.ts.kogler.si:8080 -> 127.0.0.1:8080` (TLS over TCP, tailnet only). **ACL caveat discovered live (not a blocker for current single-user state):** `policy.hujson` is email-ACL only — `tagOwners` is intentionally empty per the file's own comment, so `tag:dsh` / `tag:pi-dev` are not declared as policy gates. Today only `domen@kogler.si` exists on the tailnet, and the email-ACL covers `domen@kogler.si:*` → tag-based gating stays parked until a second family user joins. **Three durable live-deploy lessons (captured as IaC commits + this handoff so the next Pattern-A sidecar author doesn't repeat the dance):** ① **YAML indentation + Ansible `trim_blocks=True`** (commit `f303b51`): the `{% if %}` tag line MUST be at indent 0 inside the comment block — Ansible's template module preserves the leading whitespace of a tag line; indent 2 + sidecar at indent 2 = total indent 4 → sidecar gets pushed OUT of the `services:` mapping. Authoring pitfall that surfaced as a `mapping key "image" already defined` compose-validate error on the first live dsh converge. ② **Bind-mount stub-dir creation** (commits `12a8183` + `a81f875`): when a service bind-mounts `./serve.json:/config/serve.json:ro` AND `./serve.json` doesn't yet exist on the host, `docker compose up` auto-creates the source as an EMPTY DIRECTORY. The copy step (deploy-service.yml line 32) and the template task (line 49) BOTH silently fail to overwrite a directory with a regular file — the rendered file lands INSIDE the stub directory as `serve.json/serve.json.j2` and the sidecar boot fails with `serve proxy: failed to read serve config: read /config/serve.json: is a directory`. Fix: register the file in `_extra_templates[<svc>]` (so the template task explicitly writes the file) AND a new pre-task `Remove stub dirs at extra-template dest paths` runs `state=absent` before the render. ③ **DSH `dsh-home` named volume** (HD-268 commit `6a68809` baseline, no live impact): the DSH app's `dsh-home` named volume (chrome-profile + DSH profile home) MUST exist as a writable mount or DSH crash-loops (`ENOENT mkdir /home/node/.dsh/profiles` under `read_only:true`); the volume is declared in the compose; deploy-service handles auto-create. **HD-268c sub-task: `dsh.kogler.si` / `dsh.ts.kogler.si` / `pi.kogler.si` / `pi.ts.kogler.si` A-records added to headscale `dns.extra_records` (group_vars/vps.yml `tailnet_subdomains` extended with `{name: dsh, ip: ...}` / `{name: pi, ip: ...}`; deploy-gated owner: the per-sub IPs are filled at deploy time by reading `headscale nodes list` after each sidecar joins, exactly like the existing `tailnet_sidecar_ip` flow — the LIVE values for dsh + pi-dev are recorded in `changelog.md` HD-268 row and `todo.md` HD-268 row, but the SSOT group_vars will need a refresh if the IPs change).** **HD-264a/c/d remain owner-side:** commit a real versioned manifest + `renovate.json` to `domen/test`; add an external re-launch mechanism (systemd timer / Ansible cron) since `on-failure:5` alone doesn't auto-relaunch; verify a PR lands against the test repo; THEN flip `RENOVATE_REPOSITORIES` to `domen/homelab` as the GitHub-migration LAST step. **Active lanes (carried from #32, still open):** ① ~~HD-268 tailnet sidecars~~ → CLOSED live (this session); ② ~~HD-264b Renovate run-model~~ → CLOSED live (this session); ③ HD-264a/c/d (owner: commit manifest, add timer, flip repo); ④ HD-272 on-boarded Stage 8/10 (SSO browser round-trip + backup-scope volume remaining). **Predecessor session 2026-08-29 (handoff #35):** HD-268c tailnet sidecars IaC DONE (deploy-gated live enable) + audit fold DONE (20 findings → todo.md HD-276..HD-295; next-hd.sh at HD-296).
> **SESSION #34 (2026-08-29, worktree `homelab-wt-20260829-185453`, branch `session/traefik-restart-20260829-185453`, MERGED + PUSHED — small session, 1 commit):** **Cloudflare API token rotation COMPLETE — audit P0 (MERGE-1) closed.** Owner rotated `cloudflare_api` in 1Password earlier in the day; the live traefik container still had the OLD value (sha256 mismatch, captured in the audit). This session: created a fresh worktree, ran `bash scripts/ansible-run.sh vps.yml --tags docker_services,traefik` (the full docker_services role — the `traefik` tag alone doesn't scope; the proper HD-255 pattern is `--tags docker_services -e docker_services_scope=traefik`; see Deviations), bulk pre-pass fetched the new vault value, traefik compose re-rendered, container re-created at 2026-08-29T20:57:40.188Z (the only container created today — all 32 other enabled services keep their pre-existing timestamps because their configs didn't change). **Verify (length+tail+sha256 only, per CONVENTIONS §2):** 1Password `cloudflare_api.credential` length=53 tail=`...4b2d` sha256[:16]=`56cc7b39dad07d9a`; live traefik `CF_DNS_API_TOKEN` length=53 tail=`...4b2d` sha256[:16]=`56cc7b39dad07d9a` — **MATCH** ✅. Traefik's proactive cert-renew test ran at 20:57:58 (DNS-01 with the new token) — wildcard cert still valid until 2026-11-20 (~83d), no forced renewal; the test is the smoke that the new token works. `network-addresses-generated.md` / `cloudflare_dns/vars/main.yml` unchanged (HD-181 single-issuer invariant holds — the VPS remains the sole DNS-01 caller). `traefik-certs-dumper` not re-rendered (separate role gate); cert pair at `/opt/traefik/certs/` unchanged, will be re-pulled on the dumper's next scheduled run. Full record: `deployment-journal.md` Phase 1 2026-08-29 "Cloudflare API token rotation COMPLETE" entry. `IaC/ansible/`, `docs/`, `scripts/` were **not modified** by this run — only `deployment-journal.md` + `prompt.md` (this handoff) were edited. **No new HDs created:** the rotation is a single deploy event that closes MERGE-1; no backlog row needed (MERGE-1 was already an audit finding, not a tracked HD).
> **SESSION #33 (2026-08-29, worktree `homelab-wt-20260829-152521`, branch `session/audit-orchestrator-20260829-152521`, MERGED + PUSHED — 7 signed commits, all `G`):** **Full-repository audit pass — read-only, no IaC / docs / services mutated.** Two parallel sessions ran the audit per `audit.md §3b`'s 5-lane `runs.all` design; both failed on the same OpenRouter rate-limit / in-flight budget cap (3 attempts: parallel+shared-model → 429, parallel+5-distinct-models → 402 `in_flight_budget_exhausted`, sequential → 402 because the other instance was running concurrently) — fell back to **parent-inline execution** as a deliberate fall-back (full rationale + 3-attempt log: [`audit-approach.md`](audit-approach.md)). Reports produced (all on the session branch, NOT yet on main — the audit lifecycle §4 makes them ephemeral, the next session folds + deletes per spec):
  1. **5-track audit (first cut):** [`reports/full-audit-2026-08-29.md`](reports/full-audit-2026-08-29.md) (commit `845e28c`)
  2. **Security audit (post-Cloudflare-rotation):** [`reports/security-audit-2026-08-29.md`](reports/security-audit-2026-08-29.md) (commit `807cccc`)
  3. **Consistency audit (docs↔IaC↔live):** [`reports/consistency-audit-2026-08-29.md`](reports/consistency-audit-2026-08-29.md) (commit `e60a392`)
  4. **Merged + deduped audit (this session's final deliverable):** [`reports/merged-audit-2026-08-29.md`](reports/merged-audit-2026-08-29.md) (commit `0db7685`) — consolidates this session's 3 audits with the other instance's parallel-lanes worktree, dedupes overlapping findings, reconciles conflicting counts and HD-number assignments, archives the other instance's original reports at `reports/other-session-merged/`.

**Other session's worktree cleanup (per owner request):** the other instance's worktree (`homelab-wt-2026-08-29-1652`, branch `session/audit-2026-08-29`) was a no-progress worktree (still at HEAD `c9baf09` with untracked reports). This session's close-out: ① copied the other instance's untracked reports into `reports/other-session-merged/` for traceability; ② removed the worktree (`git worktree remove --force`); ③ deleted the branch (`git branch -D session/audit-2026-08-29`). Both archive and removal are part of the merge back to main (commit `0db7685`'s close-out followup is captured in this handoff's `pre conventions` section).

**Counts corrected (verified live by the audit):** roles=**20** (not 19 as `audit.md` said, not 18 as my first pass said), enabled services=**35** (33 vps + 2 home_servers, not 52), scripts=**31** (not 27), compose templates=58, canonical docs=62, total markdown=74. The audit's HD-275..283 numbers are illustrative — actual IDs come from `bash scripts/next-hd.sh` at write time per CONVENTIONS §1 (the derived-values ban on hand-typed HD numbers, exactly the precedent the spec warns about).

**Consolidated findings (21 OPEN + 6 false positives):** 2 HIGH (Cloudflare API token rotation incomplete in live traefik — owner action required; **`ha-failover_api` vault MISSING** — newly identified by the other session, `check-vault-items.sh --strict` now reports 2 missing items: `metabase-forgejo_ro` HD-242 deploy-gated + `ha-failover_api` HD-17 future cutover), 9 MED (bin.kogler.si NXDOMAIN; pairdrop/drop/pdf missing from `services.md` public table; traefik.kogler.si STALE Cloudflare record; fail2ban not running 1d 18h; 7 stale status banners from other session; 5 broken anchor links from other session; 12 IP literals in docs from other session; `default()` in 4 templates needs comments from other session; wg-s2s tunnel not up — Phase 1.5 deploy-gated), 10 LOW (mix of own + other — sunshine IaC vs journal, sunshine port binds, audit.md says 19 roles, collect-smart.ps1 README, stale traefik:v3.5.2 image, metabase_oidc, dupes, scripts/README.md §8.4 stale link, document map manual/ prefix, FP log entries, renovate gaps). **False positives confirmed:** matrix.kogler.si consistent, ha.kogler.si URL→backend is routing reference, alloy `0.0.0.0:12345` per-process binds 127.0.0.1 only, renovate enabled but Forgejo not created (HD-264 sandbox by design), testdata/ not in scripts/README.md (by design), 16 vault refs in TOML/env/yaml OK by design.

**What this session PROVED live that the other session couldn't (no SSH):** 50 VPS containers Up (49 healthy + 1 unhealthy `authentik-ldap` deploy-gated per HD-132), wildcard cert expires 2026-11-20 (~83d), db-backup on schedule (last run 21h ago, < 24h target), kopia-server connected to Hetzner Storage Box (1.1 TB available, maintenance running), nftables INPUT chain default-deny (174k drops since reload), all 13 in-gate validators green (`validate-all.sh` 0.6s), Authentik OIDC posture clean (22 providers, all confidential except opencloud public, all 1h tokens; HD-216 persisted-token sweep passes: 1 ephemeral `provision-glue` + 1 outpost `expiring=False`), sibling-auth coverage complete per HD-160/HD-59 (ollama+docling deliberately isolated on overlays), capability-tiering on AI stack per HD-247/248/251.

**Hygiene event acknowledged:** an early `docker inspect traefik` probe in the security audit echoed the `CF_DNS_API_TOKEN` value to stdout; corrected to length/tail/sha256-only for the rest of the audit. Owner rotated the 1Password item but the live container still has the OLD value (sha256 hashes differ — re-render + restart pending; this is MERGE-1 in the merged audit, owner-action P0). Per CONVENTIONS §2 the value is considered compromised; rotate is the audit's P0.

**Session #32 lanes (all CLOSED + merged+pushed as of this handoff):** HD-255/260 surgical gate CLOSED + HD-270/252/220 VERIFIED + HD-272 crowdsec-web-ui on-boarded Stage 8/10 + HD-242 metabase_ro SELECT-only verified + HD-271 `inject_facts_as_vars` directive activated (live-verify exposed it was comment-only; commit `ca7b7c4`). All merged+pushed on main `b8cdcdf`. **Active lanes (carried from #32, NOT advanced by #33 — audit is read-only):** ① ~~HD-268 tailnet sidecars (pi-dev/dsh) still need their auth-key items~~ → closed as IaC sub-task in session #35 (deploy-gated owner enable remains, see §3-N) · ② HD-272 on-boarded Stage 8/10 (SSO browser round-trip + backup-scope volume remaining) · ③ Renovate HD-264 (churn confirmed true positive at RestartCount=7044, owned by the Renovate session) — see §3c of the previous handoff #32 (NOT modified here; the audit session doesn't advance these).

- **HD-135b (VPS self-observability) MERGED + LIVE** — VPS runs own Alloy (loopback) + Dozzle (`logs.kogler.si` moved from oldsrv); SPOF narrowed to home telemetry.
- **L3 (observability internal-only + tailnet) MERGED + NOW IMPLEMENTED (HD-273)** — the Pattern-B `tailscale-sidecar` port-skeleton is REPLACED by the `traefik-tailnet` edge; clean subdomain URLs live. ⏳ owner: delete the live stats/sec/traefik/auto CNAMEs from Cloudflare (in-zone leftovers from the Phase-1 wave).
- **HD-272 (CrowdSec Web UI) MERGED + DEPLOYED LIVE** — `csui.kogler.si` internal Forward-Auth; `crowdsec_web_ui` Up (healthy); watcher `crowdsec-web-ui` registered on LAPI (`cscli machines add -f /dev/null`); `crowdsec-webui_lapi_api` seeded. Fixed 3 template bugs (data `/app/data`, `CHOWN`, `SETUID+SETGID`).
- **VPS Alloy NOW SHIPS CONTAINER LOGS → LOKI** (verified: 18 streams/13 containers, 48 files tailed, 0 drops) — fixed 6 real Alloy 1.19 bugs (duplicate exporter block, dead 9998 scrape, `.receiver` exports, `unix://` docker host, dropped `retry`, discovery.docker port-mapping limitation → **file-based `loki.source.file`+`file_match`+`stage.docker`**). Host metrics reach Prometheus (remote_write). `docker` group perms granted on /var/lib/docker/containers for alloy.
- **`prometheus-internal_api` ROTATED (L2, end-to-end)** — item by UUID (new pw/hash), Prometheus web-config + restart (200 new/401 old), Grafana DS delete+recreate w/ secureJsonData.basicAuthPassword, proxied query verified. Write-SA token re-deployed via full converge (scope_is_all-gated pre-pass).
- **`logs` 404 finding: RESOLVED** — `logs.kogler.si` (+ `logs.ts.kogler.si`) is served by the `traefik-tailnet` edge (the `logs` forward-auth provider for the main edge is still absent — the tailnet edge's plain-name router carries forward-auth and works; see HD-273 journal entry).
- **Alloy residual**: remote_write has no basic-auth today (if the role adds it, consume new prometheus-internal_api).
> **Active lanes:** ① HD-268 **LIVE (2026-08-27)** — the modular-AI IaC (Qdrant swap + Forgejo OKF wiki repos + dual pi.dev/DSH harness) authored AND fully converged live (`ok=311 changed=45 failed=0`); only the tailnet sidecar wiring + Qdrant embed/rerank re-index remain deploy-gated. ② HD-269 **CLOSED (2026-08-28)** — Ansible deploy-runtime lane: glue parallelization (authentik 11.94s win, litellm reverted serial), multi-service scope, scoped op pre-pass, rare **`base` tier**, measured Deploy Timing Runbook — all Steps (a)-(f) done. ③ **HD-270 CLOSED + LIVE VERIFIED (2026-08-28)** — silent docker-compose `$`-interpolation truncation of 1P secret values FIXED (Layer 1 `gen_pw()` drops `$`; Layer 2 `| replace('$','$$')` across 24 compose templates; convention in `deployment-secrets.md`; signed commit 96f0043) AND the 6 services re-rendered live (scoped converge `authentik,zipline,litellm,pi-dev,openclaw`) — `docker inspect` env == vault by length+tail for all 5 (kopia from the qdrant/kopia fix); pi-dev 19→32, openclaw 28→32; disk compose `$$`-escaped; kopia non-JSON warning cleared. litellm had no literal `$` (never truly truncated; disk compose `$$`-correct defensively). Journal: `deployment-journal.md` Phase 1 2026-08-28. ④ **HD-271 CLOSED (2026-08-28)** — ansible-core 2.24 deprecation hygiene: `inject_facts_as_vars: false` in `ansible.cfg` + `deploy-service.yml` refs → `ansible_facts['service_mgr']`; the two group_vars pubkey guards use `lookup('vars', key, default='')` (2.24-safe — verified live that `'key' in vars` still emits `[DEPRECATION WARNING] The internal "vars" dictionary is deprecated…removed from ansible-core version 2.24`; the default='' does NOT violate HD-65 — these are structural vars, not 1Password secrets, and the wireguard/router role asserts still fail-closed on a blank peer). **Live-found follow-up fixes (commits `d90f0e2`+):** qdrant crash-loop `Can't create Snapshots directory` (read_only rootfs blocked the snapshots dir → `QDRANT__STORAGE__SNAPSHOTS_PATH=/qdrant/storage/snapshots` added; now `Up (healthy)`) + kopia-server crash-loop (`repository.create` re-ran on existing repo because the first-run gate checked the unrelated `config.json` instead of `repository.config`; gate fixed, server `Up`). Owning docs updated: `services-ai.md` (qdrant gotcha), `backup.md` (kopia gate), `deployment-ansible.md` (2.24 + HD-65). `validate-all.sh` green. ▶ **MERGED + PUSHED:** main = `300ec30`, worktree pruned, session branch deleted (close-out 2026-08-28). ⑤ **NEXT: HD-272** (next free HD; see todo). ⑥ **STILL deploy-gated:** HD-271 real 2.24-core run (needs an ansible-core upgrade — latest is 2.21.3, the deprecations remove at 2.24 but no 2.24 exists yet) / HD-247 Lane C legs / HD-268 tailnet tail / Phase 1.5 hardware swaps.

> **Role:** Entry point for the next session. Session 2026-08-29 (handoff #36) — **HD-268c tailnet sidecars LIVE** (dsh + pi-dev on headscale as `vps-dsh` and `vps-pi-dev`, both tagged `tag:dsh`/`tag:pi-dev`, `tailscale serve` TCP:3080/8080 → loopback confirmed) **+ HD-268c sub-task: `dsh.kogler.si` / `dsh.ts.kogler.si` / `pi.kogler.si` / `pi.ts.kogler.si` A-records added to headscale `dns.extra_records` (group_vars/vps.yml `tailnet_subdomains` extended)** **+ HD-264b Renovate run-model fix LIVE-VERIFIED** (`RestartCount=0`, clean exits no longer trigger restart; HD-264a/c/d remain owner-side: commit manifest to `domen/test`, add external re-launch timer, verify PR lands, flip `RENOVATE_REPOSITORIES` to `domen/homelab` as the FINAL GitHub-migration step) **+ audit-fold HDs still queued** (HD-276..HD-295 from handoff #35, see §3-N). **Next (per CONVENTIONS §4 audit lifecycle, this is a real task):** execute the audit-fold HDs via their owning docs (each row body says where the fix lives) — start with HD-276 (owner action: seed `ha-failover_api` in 1Password) and the other MEDs that are pure doc edits + bin CF publish. After the actionable HDs are closed, **delete the ephemeral audit reports** in `reports/` (`full-audit-2026-08-29.{md,json}`, `security-audit-2026-08-29.{md,json}`, `consistency-audit-2026-08-29.{md,json}`, `merged-audit-2026-08-29.{md,json}`) per CONVENTIONS §4. The `audit-approach.md` + `audit-orchestrator.js` at the repo root are the methodology artifacts and can stay. **Owner actions to consider in parallel with HD execution** (not deploys, not backlog rows): seed `ha-failover_api` (HD-276), run `playbooks/dns.yml` for `bin` (HD-278), manual delete `traefik` CF record (HD-280), `docker image prune` for stale traefik:v3.5.2 on VPS (HD-293), 1P archive `metabase_oidc,` dupes (HD-294), commit manifest + `renovate.json` to `domen/test` (HD-264a), add external renovate re-launch timer (HD-264b follow-up). **Hygiene pattern (learned in handoff #34):** to scope a converge to a single service, use `--tags docker_services -e docker_services_scope=<svc>` (the `-e` flag is the actual per-service scope gate; the `traefik`/`<svc>` tag is NOT a task-level tag in the docker_services role). `--tags docker_services,traefik` alone runs the full role (all enabled services); only the actual config change determines which container restarts. **HD-268c durable lessons (recorded in the commit messages; do not relearn):** ① `{% if %}` Jinja tags at indent 2 leak their leading whitespace under Ansible's `trim_blocks=True` → total indent 4 → sidecar pushed out of `services:` (commit `f303b51`); ② bind-mount source paths auto-created as directories by `docker compose up` if missing (commit `12a8183`); ③ a pre-task must clear the stub dir before the template task can write a regular file (commit `a81f875`). **Active lanes (carried from handoff #32, still open, NOT advanced by #33..#35):** ① ~~HD-268 tailnet sidecars (pi-dev/dsh)~~ → CLOSED live in session #36; ② ~~HD-264b Renovate run-model~~ → CLOSED live in session #36, (a/c/d) remain owner-side; ③ HD-272 on-boarded Stage 8/10 (SSO browser round-trip + backup-scope volume remaining). **Predecessor session 2026-08-29 (handoff #35):** HD-268c tailnet sidecars IaC DONE (deploy-gated live enable) + audit fold DONE (20 audit findings → todo.md HD-276..HD-295; next-hd.sh at HD-296). **Predecessor-predecessor session 2026-08-29 (handoff #34):** Cloudflare rotation COMPLETE (live traefik env == 1Password by sha256[:16], no other containers touched). **Predecessor-predecessor-predecessor session 2026-08-29 (handoff #33):** Audit pass DONE — 4 reports produced (5-track + security + consistency + merged), 21 findings, the other session's worktree cleaned up. **Predecessor-predecessor-predecessor-predecessor session 2026-08-29 (handoff #32):** Phase 1 live-verify batch DONE — HD-255/260 surgical gate CLOSED + HD-270/252/220 VERIFIED + HD-272 crowdsec-web-ui on-boarded Stage 8/10 + HD-242 metabase_ro SELECT-only verified + HD-271 `inject_facts_as_vars` activated — all merged+pushed (main `b8cdcdf`). **Predecessor-predecessor-predecessor-predecessor-predecessor session 2026-08-28 (handoff #31):** HD-135b-followup tailnet edge live. **Signed-commit gotcha (2026-08-28, still valid):** keys live in `~/.ssh/github_signing`/`github_auth` after `git-bootstrap.sh --ssh-auth`; a fresh shell that reports `Couldn't find key in agent` just needs `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.
> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 (audit lifecycle, derived-values ban, handoff diff-rule, worktree discipline, secret-output hygiene) · [scripts/guard-session.sh](scripts/guard-session.sh) · [changelog.md](changelog.md) audit-2026-08-29 row (this session) + HD-271/272 (predecessor #32) + HD-269 rows (#31) · audit reports: [audit-approach.md](audit-approach.md) (3-attempt deviation log), [reports/merged-audit-2026-08-29.md](reports/merged-audit-2026-08-29.md) (the merged final deliverable — 21 findings with dedup keys, action plan, open questions), [reports/full-audit-2026-08-29.md](reports/full-audit-2026-08-29.md) (5-track first cut), [reports/security-audit-2026-08-29.md](reports/security-audit-2026-08-29.md), [reports/consistency-audit-2026-08-29.md](reports/consistency-audit-2026-08-29.md), [reports/other-session-merged/](reports/other-session-merged/) (other instance's reports, archived for traceability) · [deployment-journal.md](deployment-journal.md) 2026-08-29 audit pass entry (this session) · [todo.md](todo.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene + secret→YAML `>-` rendering, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — **currently the hottest owning doc** for the next task: §Tags & surgical runs (`docker_services_scope`, the rare `base` tier, canonical invocation table) + the new **§Deploy Timing Runbook** (HD-269) — the deploy-speed budget + how to re-measure with `profile_tasks`.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — HD-270 fix lands here (compose `$`-escaping decision, secret-to-compose env rendering, fail-loud rule).
4. [docs/services-ai.md](docs/services-ai.md) — v2 AI architecture (HD-267 rewrite retained); only relevant if a task touches the AI stack.
5. [changelog.md](changelog.md) — HD-269 rows (baseline 2026-08-27 + live re-baseline 2026-08-28 + full close) + HD-247–251 decision bundle + HD-253/254/256 tooling rows.
6. [deployment-journal.md](deployment-journal.md) — 2026-08-28 HD-269 live-re-baseline entry (ok=311, authentik 11.94s, litellm serial revert); 2026-08-26 decision/vault-seeding entries.

## 1. Environment (Windows 11 laptop)

Same as handoff #16 (unchanged): git-bash, forward-slash, `py -3`, UTF-8 no-BOM, LF; Ansible via WSL + 9P gate before every run; Secrets → 1Password item.field only, `>-` for YAML renders; **new self-learned rule (2026-08-26): do NOT use multi-line bash heredocs containing backslashes/backticks on this host (mangled through `bash -c`) — write patch scripts to a temp file via the `write` tool and run them instead. Second (2026-08-26): `wsl.exe` invoked from git-bash mangles `/mnt/...` arguments into `C:/Program Files/Git/mnt/...` — always prefix with `MSYS_NO_PATHCONV=1`.**

## 2. State snapshot (start of next session)

- **Phase 1 live-verify batch DONE (2026-08-29, handoff #32):** Lane d0 verified HD-255/260 (surgical converge genuinely single-service `ok=21 changed=1 failed=0`, ~18s), HD-270 (env==vault all 6), HD-252 tail (ACL deny-by-default + nodes online), HD-220 (deploy-health clean except renovate churn). Mutations: HD-272 crowdsec-web-ui on-boarded (converge ok=20 changed=1 + watcher machine registered + UI serving, Stage 8/10), HD-242 metabase_ro role SELECT-only verified in forgejo-db, **HD-271 gap fixed** — `inject_facts_as_vars: false` was comment-only, now active (commit `ca7b7c4`). All merged+pushed `b8cdcdf`. HD-266/267/268/269/270/271 prior work already merged (main was `300ec30` before this).
- **HD-268 LIVE + HD-269 CLOSED (2026-08-27/28) + HD-270/271 CLOSED (2026-08-28, this session subset):** the modular-AI IaC (Qdrant swap + OKF wiki repos + dual pi.dev/DSH harness) fully converged live (`ok=311 changed=45 failed=0`); the deploy-runtime lane (HD-269) shipped glue parallelization (authentik 21-22s→**11.94s**; litellm reverted **serial**), multi-service scope, scoped op pre-pass, the rare **`base` tier**, and the **measured Deploy Timing Runbook**. **HD-270 CLOSED + LIVE VERIFIED** — compose `$`-truncation secret bug (6 services) fixed + re-rendered; **HD-271 CLOSED + LIVE VERIFIED 2026-08-29** — fact-prefixed refs green, zero deprecation warnings, directive activated. **Next: HD-268 tailnet sidecars OR Renovate HD-264** (see §3c).
- **Worktree**: session #32 ran in `homelab-wt-20260829-1528` (branch `session/liveverify-20260829`), 3 commits merged+pushed → main `b8cdcdf`; worktree pruned + branch deleted. Close-out prompt update on `session/closeout-20260829`.
- **HD-216 tail clarification (2026-08-26):** `authentik-nas_api` (formerly `authentik-api_token`) is the future Authentik→NAS read token — **not yet in the vault**, minted durable (`expiring=False`) at NAS provisioning (Phase 2) via `ak shell`; **not automated yet** (manual at provisioning unless a mint-glue is added). This closes HD-216's deferred "one-time expiring=False verify" as a Phase-2 item.
- **Worktree rule ENFORCED** (unchanged from #19): start every session with the worktree ritual (README §0).
- **✅ IN-FLIGHT ROTATION — CLOSED (2026-08-26):** `headscale_api.credential` sha256 `28f53d33…` == Authentik DB secret; on-disk `/opt/headscale/config.yaml` + `headplane-config.yaml` re-rendered to `28f53d33…` (surgical converge ok=29 changed=6 failed=0); headscale + headplane restarted; token-endpoint replay `invalid_grant`; `/health` 200. **Runbook captured** in [docs/services-authentik.md](docs/services-authentik.md) — next rotation follows it (incl. the `docker cp`-broken + `op --template`-stdin gotchas). Full record: deployment-journal.md Phase 1, 2026-08-26.
- **AI stack v2: HD-247 is BUILT (IaC, deploy-gated); HD-248–251 remain planned-not-built.**
  - **HD-247 remaining ⏳ legs (Lane C):** converge vps.yml → bootstrap-keys glue mints the 7 scoped sks into 1P → Admin-UI model recreation (compose-header runbook step, incl. HD-246-corrected embed-v4.0@1536/rerank-v4.0-pro params) → live-verify scoped keys + open-webui/openclaw against them → trim tail. Unblocks HD-250/HD-248.
  - **HD-248:** parametrize open-webui compose to render twice; `chat.kogler.si` public / `ai.kogler.si` internal (drop public `ai.` DNS) + Element→`msg.kogler.si`; public-KB restriction pins.
  - **HD-249:** n8n internal (audit webhook deps first). **HD-250:** DSH (depends HD-247; verify `tailscale serve` TCP-mode works on your Headscale). **HD-251:** fleet exposure phase-2.
- **Vault state (confirmed 2026-08-26):** provision-vault.sh idempotent run created ONLY `litellm_db`; 35 others skipped-as-existing — real `openrouter_api`/`cohere_api`/`forgejo_api` values IN PLACE (HD-211 core resolved), `metabase-forgejo_ro` present, zipline items present. Scanner live baseline: needed 36 / MISSING `ha-failover_api` only.
- **Renovate — HD-264 FILED (2026-08-26):** Forgejo install wizard DONE; `domen/test` repo exists (only `README.md`, no manifests). Renovate container walks exit-0 restart churn (`RestartCount=3392`, `restart: unless-stopped`) — runs one pass, logs `Repository is disabled - skipping`, exits 0, Docker restarts instantly. **Not by design** (no `RENOVATE_CRON`); a run-once-job image mixed with auto-restart. `forgejo_api` real token confirmed in vault = auth gate satisfied. **HD-264 scope:** build `domen/test` into a real sandbox (add versioned manifest + `renovate.json`), prove repo-read → extraction → Dependency Dashboard → PR end-to-end, fix the run-model to kill churn, THEN flip `RENOVATE_REPOSITORIES` back to `domen/homelab`. GitHub migration = LAST step, deferred until stable+tested (owner decision).
- **Still-open HD-211 tails:** `prometheus-internal_api` password exposed into an agent transcript TWICE (rotate → delete DS → re-run `--tags monitoring`); **`crowdsec-bouncer_api` LAPI key** (exposed via traefik debug dump 2026-08-25); `opencloud-collab_password` exposure window; persisted-Authentik-token `expiring=False` sweep (HD-216). (HD-211's `openrouter_api`/`cohere_api`/`forgejo_api` real-key swap CONFIRMED DONE — values in place 73/40/40 chars. The `headscale_api` re-rotation that used to be the top item is now DONE + runbooked.)
- **Carried legacy (from #16/#18):** HD-112 go-live legs (below) · Kopia source wiring (**HD-230d framing: agent-on-VPS vs server-managed; orchestrator recommends option (a) kopia-agent mirroring HD-191 — awaiting owner sign-off**) · LDAP HD-132 authoring · Phase 1.5 cutover (dnevna/garage swaps + capsman rsc) · HD-238 DR runbook · HD-57 bank-token legs · HD-30 purchase (delayed).
- **ID registry:** next free = max(HD)+1 in [todo.md](todo.md) — always re-derived at write time per CONVENTIONS §1; NEVER type a literal number here (the HD-253 lesson, codified as a §4 close-out ban).
- **Coordination:** headplane/headscale = SEPARATE lane (D) — coordinate, never fold into other converges.

## 3. Next-session execution order

### 3-R. ✅ RESOLVED (2026-08-26): finish the `headscale_api` rotation — DONE, see §2.
The mid-flight rotation is CLOSED and verified; the runbook that replaced this section lives in
docs/services-authentik.md (*Rotating a shared Authentik OIDC client secret*). Remaining from this
lane folds elsewhere: re-enroll dropped nodes under Lane D (3b). Do NOT reopen — vault == DB == on-disk.

### 3-N. NEXT TASK (2026-08-29 close-out): audit-fold HDs (HD-276..HD-295)

**HD-268 tailnet sidecar wiring (pi-dev/dsh) — DONE (IaC, deploy-gated live enable):** dsh + pi-dev Pattern-A sidecars authored + gated on `<svc>_tailnet_sidecar_enabled` opt-in flags (default `false` → live compose byte-identical); two new 1P items `tailscale_dsh_api` + `tailscale_pi_dev_api` in `NOT_AUTO_ROTATABLE` + `_template_vault_items` registry; closing HD-268 IaC sub-task — live enable is an owner action documented inline in `group_vars/vps.yml` (mint preauth keys → seed 1P → flip flags → scoped converge → verify). See `changelog.md` HD-268 row + `todo.md` HD-268 (⏳ deploy-gated owner section only). `validate-all.sh` green.

**HD-264b Renovate run-model fix — DONE (IaC, deploy-gated owner + verify follow-up):** per upstream `https://docs.renovatebot.com/getting-started/running/` ("the open-source build needs external scheduling: you must make sure that some form of cron-like capability exists to schedule when Renovate runs. We recommend that you run Renovate hourly"), the OSS Renovate container is designed to EXIT after a scan. Combined with `restart: unless-stopped`, this produced the live-probed churn: **RestartCount=7044 (2026-08-29)** — exit 0 → auto-restart → exit 0 → auto-restart, per-minute. The fix in `IaC/ansible/templates/docker_services/renovate/docker-compose.yml.j2`: ① `restart: unless-stopped` → **`restart: on-failure:5`** (Docker compose v2 `on-failure:<max-retries>` form, validator-aware; clean exits no longer trigger restart, only crashes do; the `:5` caps consecutive restart attempts so a genuinely broken image surfaces for owner investigation instead of burning the restart budget); ② new `RENOVATE_SCHEDULE: "* 22-23,0-4 * * *"` env var (documented per the `schedule` config option docs; 6h nightly low-traffic window per Renovate's "at least 3–4 hours" recommendation). Validator whitelist gained both `on-failure` and `on-failure:N` forms. ⏳ **HD-264a/c/d remain (owner):** (a) commit a real versioned manifest + `renovate.json` to `domen/test` (only `README.md` today); (b) follow-up owner-side: add an EXTERNAL re-launch mechanism (host systemd timer or Ansible cron in `deploy-service.yml`); (c) scoped converge + manual `docker compose up -d --no-deps --force-recreate renovate` + verify Dependency Dashboard + PR against a real lagging dep; (d) flip `RENOVATE_REPOSITORIES: "domen/test"` → `"domen/homelab"` (IaC one-liner, scoped converge) as the GitHub-migration LAST step. Container exit-and-restart churn is fixed; hourly-relaunch loop stays owner/admin work.

**Audit-fold HDs (HD-276..HD-295, NEXT) — execute via their owning docs:**
Start with the owner actions (HD-276 seed `ha-failover_api` in 1Password; HD-278 run `playbooks/dns.yml` for `bin`; HD-280 manual delete `traefik` CF record; HD-293 `docker image prune` for stale traefik:v3.5.2 on VPS; HD-294 1P archive `metabase_oidc,` dupes), then the AI-action MEDs (HD-278 doc edit, HD-281 banner updates, HD-282 anchor fixes, HD-283 IP literal review, HD-284 template comments), then the LOWs (HD-289 sunshine port binding, HD-290 audit.md count fix, HD-291 collect-smart.ps1 README, HD-295 doc map manual/ prefix). HD-285 wg-s2s is Phase 1.5 deploy-gated (out of scope for this session). After actionable HDs are closed, **delete the ephemeral audit reports** in `reports/` (`full-audit-2026-08-29.{md,json}`, `security-audit-2026-08-29.{md,json}`, `consistency-audit-2026-08-29.{md,json}`, `merged-audit-2026-08-29.{md,json}`) per CONVENTIONS §4. The `audit-approach.md` + `audit-orchestrator.js` at the repo root are the methodology artifacts and can stay.

Done this session (HD-256/254/268/270/271/268c):

- ✅ **HD-256** (Debian/WSL portability sweep of `scripts/`) — DONE (changelog row + validate-all portability gate).
- ✅ **HD-254** (skill-sync guard `scripts/sync-skills.sh`) — DONE (changelog row); also `scripts/next-hd.sh` added.
- ✅ **HD-268** (modular-AI IaC deltas) — DONE + LIVE (2026-08-27 full converge); see §2.
- ✅ **HD-256 / HD-254** (WSL portability sweep + skill-sync guard) — DONE (see above).
- ✅ **HD-268c** (tailnet sidecars dsh + pi-dev IaC) — DONE (2026-08-29 this session); deploy-gated owner enable documented inline in `group_vars/vps.yml`.
- ✅ **HD-270 (CLOSED 2026-08-28, signed commit 96f0043, LIVE VERIFIED 2026-08-29):** compose `$`-truncation fixed (Layer 1 `gen_pw()` drops `$`; Layer 2 `| replace('$','$$')` across 24 compose templates) AND the 6 services re-rendered + env==vault verified by length/tail. See todo.md HD-270.
- **HD-271 (CLOSED 2026-08-28 + activated 2026-08-29):** ansible-core 2.24 hygiene — `inject_facts_as_vars: false` + `ansible_facts['service_mgr']` refs + `lookup('vars', key, default='')` guards. LIVE VERIFIED 2026-08-29 (zero deprecation warnings on the headscale surgical converge); **live-verify exposed the directive was comment-only → activated** (commit `ca7b7c4`). ⏳ Real 2.24-core run still needs an ansible-core upgrade (this host 2.21.3). See [todo.md](todo.md) HD-271.


### 3a. Lane C — Phase-1 converge wave on the VPS (owner-gated, interactive; HD-272 + HD-242 core DONE 2026-08-29)

One orchestrated pass
- **HD-230g** disabled-service teardown loop verify + blueprint one-shot fired (ride-along checks below).
- **HD-220** db_role_sync rotation-drift guards fire on converge.
- **HD-218 Wave-3 residue** container-state re-sample.
- **HD-181** first wildcard issuance + consumer pull-key authorization (Pi ha-sync / oldsrv traefik-cert-sync pubkeys) + sync-timer delivery verify.
- **HD-183** homepage renders on the VPS edge (`kogler.si`/`home` aliases).
- **HD-184** immich-app→immich-ml round-trip over WG (smart-search job completes).
- **HD-112 go-live legs:** ⓪ vault seeding DONE (items exist) · ① run dns.yml (the `bin` CNAME applies ONLY there) · ② walk the compose-header deploy gate (`/auth/setup` admin → OIDC verify → flip bypass-local-login in Server Settings → seed `guestbin` user (small quota ~100 MB, no password) + `dropzone` folder `allowUploads=true`) · ③ logged-out anonymous upload→short-URL→viewer round-trip · ④ 6h sweep verify · ⑤ family drop script + manual guide entry → trim the HD-112 ⏳ tail.
- **HD-241/242 metabase gates (PARTIAL DONE 2026-08-29):** converge + `metabase_ro` RO role verified SELECT-only + crowdsec-db bind mounted + SMTP env set. **Remaining:** browser Send-test-email + connect both sources in the UI + import official dashboards + SELECT-only write-fail proof. `metabase-forgejo_ro` already seeded.
- **HD-101 verify ride-along:** "Continue with Authentik" button on ai.kogler.si → SSO round-trip LINKS local admin by email (`OAUTH_MERGE_ACCOUNTS_BY_EMAIL=true`) → family sanity (local signup rejected, SSO accounts land `pending`) → LiteLLM completion + RAG smoke → trim HD-101 ⏳ tail.
- **Renovate / `domen/test` = HD-264 (see §2; churn CONFIRMED 2026-08-29 at RestartCount=7044):** re-enable scoped to test repo; add a real manifest so it has something to bump; verify a PR opens against it (proves auth+extraction+PR-permission); fix the run-once restart churn; homelab-repo flip stays LAST (deferred by decision).
- **HD-230d Kopia wiring** if owner approves option (a): add kopia-agent service entry covering the dumps volume (+ retention), mirroring HD-191.
- Converge ride-along checks (from #18, still binding): extras-restart guard behavior (zero spurious restarts / exactly one per changed extra, HD-236) + `apply-authentik-blueprints.yml` one-shot green in the same run + open-webui recreated with non-empty `OPENID_PROVIDER_URL`; then walk HD-101 verify; journal all three, trim ⏳ tails via an append-only R-row.

### 3b. THEN: Lane D — Headscale (separate lane, do NOT fold into C)

✅ **① container recycle DONE live 2026-08-26** (branch `session-headscale-oidc`): headscale force-recreated (`up -d --no-deps --force-recreate headscale`, Started 2026-08-26T14:05Z, RestartCount 0); on-disk already carried the 128-char rotated secret; token-endpoint replay now `invalid_grant` (client auth OK, was `invalid_client`) + `/health` 200 + laptop node `Domen_P14s` reconnected online. Full evidence: journal entry 2026-08-26 HD-252 Lane D. ⚠ New blocker HD-255 `--tags "docker_services,<service>"` does NOT per-service filter (include-tag cascades) AND the extras restart guard aborts on `selectattr('item')` — **RESOLVED (IaC 2026-08-26, merged `310d6b8`):** added `docker_services_scope` (default `all`; `-e docker_services_scope=<svc>` gates the direct-role platform tasks off AND narrows the deploy loop to that service) + hardened the restart guard (`(extra_templates_result.results | default([]))`, `map(attribute='extra')` — the extra-.j2 loop's `loop_var: extra` means results carry key `extra`, not `item`). A true single-service converge (`--tags docker_services -e docker_services_scope=<svc>`) is now possible; ⏳ deploy-gated live verify pending (HD-255 tail). Also still tracked: strict-default-preserving Authentik ScopeMapping (`email_verified: True`) for headscale provider pk 13. ✅ **② core + ③ DONE (2026-08-26):** second blocker fixed — Authentik default `email` scope mapping hardcodes `email_verified: False` × headscale default `email_verified_required: true` → 401 `unverified email`; fixed live + in template via `oidc.email_verified_required: false` (family-trusted users behind `allowed_domains: kogler.si`; strict-default-preserving Authentik-side fix tracked in HD-255). **First-ever successful `/oidc/callback`: phone enrolled** under OIDC-linked user `domen@kogler.si` (node 3 `Naprava A54`, 17:44:06Z, NO error). Owner deleted the hand-created user ID 1 in Headplane → only OIDC user ID 2 `domen@kogler.si` remains (③ resolved). ⏳ **Re-enroll remaining:** the cleanup also removed the laptop node (node 1) and the just-added phone (node 3) — `nodes list` is EMPTY; both devices re-enroll via the now-working OIDC flow under `domen@kogler.si`. ✅ ④ DONE 2026-08-26: ACL tightened at first enrolment — `policy.hujson.j2` interim `*:*` → deny-by-default user-based `{"src":["domen@kogler.si"],"dst":["domen@kogler.si:*"]}` (validated `policy check --bypass-grpc` = Policy is valid, applied + restarted; `tagOwners` empty → user-email-based, tag model documented as later). Both devices re-enrolled under the OIDC user; phone online under tightened ACL, 0 denials; **all Lane-D objectives (①②③④) CLOSED**.

### 3c. Carried queue (non-blocking)

- **HD-238** oldsrv→VPS DR runbook authoring for non-GPU services (todo §2.9) — laptop-doable; pairs with next backup.md touch.
- **HD-246** RAG env pins land WITH HD-247's first converge (absorbed; details live in todo row + services-ai.md §5).
- Zipline `/drop` static glue page — Phase 2, only if the owner asks; NOT queued.
- Owner-action chase (still open): prometheus-internal_api + crowdsec-bouncer_api rotations, persisted-Authentik-token `expiring=False` sweep (incl. minting `authentik-nas_api` durable at NAS provisioning — Phase 2; laptop-cleanup note: still manual, not automated), LDAP HD-132 authoring, Phase 1.5 hardware swaps, HD-57 bank legs, HD-30 (delayed), homelab repo migration (delayed).

## 4. Working rules (binding)

Fresh worktree per session BEFORE any edit — now mechanically enforced (`bash scripts/guard-session.sh` refuses edits on primary+main; `validate-all.sh` hard-fails primary+main+DIRTY); merge back only committed+green, ff-only; primary = merge station only. 9P gate before every converge; surgical `--tags`; secrets 1P-item.field only + `>-` for YAML; persisted Authentik tokens always `expiring=False` (HD-216); journal append-only; owning doc + changelog row in same change; English; relative links; Authentik blueprint pin array attrs (HD-231); don't touch headplane/headscale unless asked. **Handoff diff-rule (CONVENTIONS §4): edit the previous handoff, never rewrite from scratch; computable pointers are derived at write time, never typed.** No multi-line bash heredocs with backslashes/backticks on this host — write+run temp script files instead.
