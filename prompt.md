> **SESSION #82 (2026-09-04, worktree `homelab-wt-20260903-2348` → branch `session/dns-ts-edge-option-a`, signed commit `ed00368`, validate-all green):** **DNS/TS edge architecture — Option A DECIDED + SSOT-locked; implementation backlog rows added (HD-331 decision, HD-332/333/334 implement). No functional/LIVE changes.** ① **Decision (owner + AI, 2026-09-04):** long-term reverse-proxy architecture = **one PUBLIC edge (WAN-only, public apps) + one INTERNAL all-app edge (every app, public + internal) reached over the Headscale tailnet AND WireGuard S2S**. The internal edge is the growth of the existing `traefik-tailnet` VPS instance (file-provider routes, no Docker-labels; + a second listener on the `wg-s2s` VPS side + nftables allow). The settled DNS (one `kogler.si` zone, 3 Technitium instances, VPS-primary, split-horizon seed) stays EXACTLY as decided (HD-317/HD-299 + HD-329). `traefik-ha` stays as the physical HA-VIP failover edge. Alternatives considered + rejected: (B) home-host internal edge (Pi/oldsrv) — better home latency but needs router hairpin + oldsrv live + complicates certs/one-instance; (C) keep 3 edges as-is — leaves `ha` broken with Tailscale on (the live symptom that prompted this). ② **SSOT updates (all in one signed commit `ed00368`):** `docs/services-traefik.md` §Edge model (owning decision + rejected alternatives); `docs/services-rejected.md` decision-log row (`Edge model — Option A` … `decided`, 2026-09-04, **re-decide banned** §8.3); `docs/interfaces.md` 6-Tier table updated (Grafana/Traefik/Metabase + All-internal-apps row via the internal all-app edge, no `.ts` needed); `todo.md` **HD-331** (decision) + **HD-332** (catalog `public:` flag + internal-edge growth), **HD-333** (WG second listener + nftables allow + ACL approve), **HD-334** (Pi-first DNS for VLAN-10 per-device visibility + seed `vpn`/`home`/`dns` — the missing records behind the Tailscale `vpn.kogler.si` NXDOMAIN); `README.md` §2 notes the decision. ③ **Context that triggered it (diagnosed live, prior research in this session):** (a) **Tailscale down** — Windows client `failed to resolve "vpn.kogler.si": no DNS fallback candidates remain` because the VPS primary has NO `vpn.kogler.si` A record (split-horizon seed covers ha/dns-pi/stats/logs/csui/sec/traefik/auto/sso but NOT vpn/home/dns) → the missing records are now tracked in **HD-334**; the public Cloudflare + TCP 443 to the VPS both resolve fine, server healthy. (b) **`ha.kogler.si` fails with Tailscale ON on Home Wi-Fi** — root cause is the mesh capturing the Home-LAN route (tailnet has no subnet route to `10.10.1.0/24`; the ACL only allows user-own + `tag:sidecar:443`, nothing to the Home LAN), NOT the DNS (all instances resolve `ha → 10.10.1.200` VIP). The Option A internal edge + Pi-as-tailnet-subnet-router (HD-333) is the fix. (c) **Technitium shows all queries from `193.77.156.222`** (home WAN, hairpin) — expected; per-device visibility lives at the Pi first-hop (HD-334). ④ **Next (unchanged + new):** **HD-330** (Pi Technitium admin rotate → scoped Pi converge seeds zone) remains the highest-value **owner** step and now also gates HD-334's Pi-first DNS; implement **HD-332 → HD-333 → HD-334** (all deploy-gated, VPS + router converges); HD-326 kids controls implement-next-session; HD-312 remaining tails; HD-318 oldsrv blocked (office/ROCM/1P); HD-313 RouterOS syslog-egress workaround pending. **Handoff edited from #81 (diff-rule).**

> **SESSION #81 (2026-09-04, close-out `homelab-wt-closeout-20260903-2314` → branch `session/hd317-hd315-closeout`):** **Session close-out — README §2 corrected, todo HD-317/HD-315 trimmed, prompt → #81; no functional changes.** ① **README §2 (state-of-world) FIXED:** the prior text claimed the split-horizon A-record **seed is DONE + LIVE on the VPS primary AND Pi tertiary** — that **Pi part was false** (live-verified 2026-09-04: the Pi's `/opt/technitium/dns/zones/` is EMPTY; the `technitium-seed` role's `first-login bootstraps the admin` premise only applies to a fresh `auth.config`, and the Pi was seeded 2026-09-03 with the DEFAULT `admin`/`admin`, so the seed login fails). README now states: **VPS primary seeded + live; `dns-pi` 5380 web-UI publish LIVE (2026-09-04, HD-317 — `technitium-pi` publishes `5380:5380/tcp`, `pi:5380` HTTP 200, the 502 root cause gone); Pi tertiary A-record seed BLOCKED on rotating its admin to the 1P `technitium_login` value (HD-330 — same class as the VPS HD-324 fix); oldsrv secondary seeds once its Phase-3 admin is up; HD-315 nut_exporter pinned `v3.3.0`.** ② **todo sweep (CONVENTIONS §4a):** no fully-done rows to delete (HD-320/325 already swept by the prior session; HD-328 by the switch session). **HD-317** trimmed + its stale "Pi instance already SEEDED" claim CORRECTED (Pi NOT yet seeded; zone empty — cross-ref **HD-330** opened by the concurrent #80 session for the admin rotate; the two `technitium-seed` bugs + the `common` apt-sources dup-Signed-By bug fixed 2026-09-04 are in-cell). **HD-315** trimmed to owner-tail only (exporter pin `v3.3.0` done). **HD-330** stays (owner step). ③ **prompt.md → #81.** _No functional/LIVE changes; validate green + signed commit._ **Handoff edited from #80 (diff-rule).** ⏳ **Next (unchanged, cross-ref both sessions):** HD-330 (Pi technitium admin rotate → then scoped Pi converge seeds the zone + records) is the highest-value owner step; HD-326 kids controls implement-next-session; HD-312 remaining tails; HD-318 oldsrv blocked; HD-313 RouterOS syslog-egress workaround pending.

> **SESSION #79 (2026-09-04, worktree `homelab-wt-20260903-2214` → branch `session/hd317-hd315`):** **HD-317 `dns-pi` 5380 publish → LIVE; HD-315 nut_exporter pin → committed; two `technitium-seed` role bugs + one `common` apt-sources bug fixed live.** ① **HD-317 — `dns-pi` 5380 publish LANDED + VERIFIED (2026-09-04):** the Pi `technitium` container (`secondary-pi` instance) was running the OLD compose (5380 exposed-but-unpublished → `dns-pi` route 502'ed at the Pi's Home IP `:5380` (SSOT)). A scoped Pi converge (`raspberry_pi.yml -e docker_services_scope=technitium-secondary`) re-created `technitium-pi` with `5380:5380/tcp` → **`pi:5380` + `127.0.0.1:5380` answer HTTP 200** (verified). Public **`dns` CNAME already live** (dig `dns.kogler.si` → `vps.kogler.si` → the VPS public IP per SSOT; manually added by owner — no apply needed). ② **Two `technitium-seed` role bugs fixed (committed, live):** (a) container-name lookup used `svc.container_name | default('technitium')` but the template names the Pi container `technitium-pi` (instance-derived) → `docker inspect` found nothing → now derives the name from `svc.instance` (`primary`→`technitium`, `secondary`→`technitium-oldsrv`, `secondary-pi`→`technitium-pi` — mirrors the compose template); (b) the `docker inspect --format` range loop glued MULTI-network IPs onto one line built a garbage login URL → now newline-separates + `awk NR==1` picks the first clean IP (the Pi container joins `traefik-public` + `services-internal`). ③ **`common` role apt-sources bug fixed (committed, live):** the "fresh-install gap" task wrote `debian-trixie.sources`/`debian-trixie-security.sources` (Signed-By `debian-archive-keyring.gpg`) unconditionally on trixie → alongside the stock `debian.sources` (`Signed-By …pgp`) APT failed `Conflicting values set for option Signed-By` → blocked the whole Pi converge (live 2026-09-04). Role now **stats `debian.sources` first and skips the fragments when it exists**; the two dup files removed on the Pi (apt update clean). ④ **HD-315 — nut_exporter PINNED:** `roles/nut/defaults/main.yml` `nut_exporter_release: "@latest"` → **`v3.3.0`** (latest tagged release 2026-05-15; the `@latest` drift had produced a `(devel)` build on nas; binary already `3.3.0` live, verified). Renovate trails the tag. ⏳ **Owner step (HD-317 remaining tail, mirror of the VPS HD-324 step):** the Pi's Technitium `auth.config` admin was bootstrapped to a non-1P password on first boot, so the `technitium-seed` **login still fails** (`Invalid username or password`) and the Pi zone is EMPTY (`/opt/technitium/dns/zones/` empty — no `ha`/`dns-pi` records yet). **Recreate the Pi admin to the 1P `technitium_login` value** (deployment-manual §1.4c Pi one-liner against `technitium-pi:5380`, taking the first network IP), then re-run `raspberry_pi.yml -e docker_services_scope=technitium-secondary` → seed completes idempotently (login OK → zone + records). The 5380 fix itself (the 502 root cause) is DONE + live. **Handoff edited from #78 (diff-rule).**


> **SESSION #80 (2026-09-04, worktrees `homelab-wt-20260903-2252`(D:/ mirror, discarded) + WSL `homelab-wt-` → branch `session/vps-dns-home-lan`, signed commits `b7c91f4` + `6a1c5b4` merged ff-only to main, validate-all green, VPS + Pi converged LIVE):** **Technitium recursion policy root-caused + FIXED (fully Ansible-managed) — home WSL/laptop + Windows Tailscale DNS broke with REFUSED; now all resolve via the VPS primary (allow-all policy + least-privilege ACL) + Pi tertiary.**
> ① **Root cause (definitive, via the Technitium API):** the VPS primary's `recursion` was the default **`AllowOnlyForPrivateNetworks`** — refuses recursion for any PUBLIC source. Home WSL queries arrive hairpin-NAT'd with the **home WAN public IP** (`193.77.156.222`) → REFUSED; tailnet CGNAT (100.64/10) is **not RFC1918** → REFUSED (the Windows Tailscale DNS break, `tailscale status` failing on `vpn.kogler.si`). VPS-local/loopback queries (RFC1918) worked — hence the "intermittent" look. The nftables `dns-allow-home` set was correct; Technitium's own policy was the gate.
> ② **The API lesson (fully-manages it):** the recursion policy is NOT the read-only `recursion` string enum — it's the **booleans `allowRecursion` + `allowRecursionOnlyForPrivateNetworks`** (`/api/settings/set`). My first attempt used `recursion=AllowAllNetworks` (ignored); the APIDOCS example line (`...allowRecursion=true&allowRecursionOnlyForPrivateNetworks=true...`) exposed the real fields. **`allowRecursion=true` + `allowRecursionOnlyForPrivateNetworks=false` = "Allow all networks" recursion, API-settable + idempotent** → NO manual UI step needed.
> ③ **IaC change (committed `b7c91f4` + `6a1c5b4`):** `technitium-seed.yml` now sets `allowRecursion=true` + `allowRecursionOnlyForPrivateNetworks=false` + `recursionNetworkACL` = home VLAN subnets (SSOT `network_vlans`) + tailnet CGNAT + loopback — one idempotent task on every `docker_services` converge. `docs/network-dns.md` documents the fully-API-managed posture (dropped the erroneous "must flip in UI" note from the first attempt). **VPS primary converged live** (`ansible-run.sh playbooks/vps.yml --tags docker_services -e docker_services_scope=technitium`, task ran, `failed=0`), **ACL live** (10 entries verified via API), **WSL→VPS `google.com` NOERROR (verified)**.
> ④ **Pi tertiary (also fixed live):** the Pi's technitium admin is still the DEFAULT `admin`/`admin` (verified — never re-seeded to the 1P `technitium_login`; the seed's first-login-bootstrap only applies to a fresh instance). Applied the ACL directly to the Pi (`pi:5380`, `admin`/`admin`) — LAN clients resolve the Pi (RFC1918 source, no allow-all needed). **Opened HD-330** to rotate the Pi admin to the 1P value (same class as the earlier VPS HD-324 admin-recreate flow).
> ⑤ **Close-out:** commits merged ff-only to main; VPS + Pi live-verified (`dig @dns_primary_ip google.com` NOERROR, `dig @<Pi Home IP per SSOT> google.com` NOERROR); `validate-all.sh` green + playbook syntax green. Todo: **HD-330 opened** (Pi admin hardening). Next free HD after this session: derive via `scripts/next-hd.sh` (expect HD-331). Note: the recursive fix is in the SHARED role → oldsrv (Phase 3, not provisioned) gets it automatically on first converge.
> **Handoff edited from #79 (diff-rule).**

> **SESSION #77 (2026-09-04, worktree `homelab-wt-20260903-1812` → branch `homelab-wt-20260903-1812`, commit `fcf3348` signed, validate green, merged to main):** **HD-327 — HA door contacts: invert fix + physical-door map + registry cleanup — LIVE + MERGED.** ① **`invert: true` for the KNX door contacts:** the ETS 1.001 door telegrams read **inverted on the bus** (1 = closed / 0 = open) — live-verified by watching which sensor flipped as each door was closed. `scripts/knx-hass-gen.py` `emit_binary_sensor` now emits `invert: true` (additionally in `render_yaml`); `knx-entities.yaml` **regenerated** (the hand-curated rekuperator + appliance-current sensor block is NOT in the current ETS export and was re-appended by hand — don't regen without re-appending); deployed to the Pi via the `home_assistant` role (`--tags home_assistant,ha_render,knx`) + `docker compose restart home-assistant` → **door open/closed now renders correctly** (closing = `off`, open = `on`, verified live). ② **Physical door map — 9 ETS door contacts are NOT all doors**: house has **5 real doors**, identified interactively (owner closed each door, the sensor that flipped won the mapping, full table in [smart-home.md](docs/smart-home.md) + HD-327 todo row): `0/3/3`→**Soba roza**, `0/3/6`→**WC**, `8/3/4`→**Soba zelena**, `0/3/5`→**Spalnica**, `9/3/0`→**Kopalnica**. ③ **Registry cleanup (UI/registry-level, NOT in `knx-entities.yaml`):** the 5 real doors **renamed** to friendly names (`Vrata soba roza`, `Vrata WC`, `Vrata soba zelena`, `Vrata spalnica`, `Vrata kopalnica`) + the **4 unused/spare contacts** (`0/3/0`/`0/3/1`/`0/3/2`/`9/3/1`) **disabled** (`disabled_by: user`) so they don't clutter the dashboard (they remain on the bus; re-enable in Settings → Devices if ever needed). Also earlier this session: **renamed 53 live HA entities to the old-HA friendly names** (per the `docs/assets/references/old-ha/entity-registry-export.json` SSOT — matched 1:1 by stable unique_id: KNX group addresses + Shelly MAC uids), the old names the owner wanted back on the dashboard. **Convention notes:** the old-HA names came from the pre-redo instance; entity friendly names are **registry state on ha.kogler.si**, not in the IaC generator (the generator emits raw ETS names; the registry carries the friendly names + disable flags — a pristine re-install would need the registry names re-applied via HA UI or WS). ⏳ **Remaining/next:** KNX UI config-flow import was already done (Tunnel UDP live, `connection_established` 16:49 UTC); leftover GroupValueRead timeouts on some GAs (`0/0/2`, `0/1/4`, `0/1/6`, `0/1/9`…) are the bus not answering passive read-requests — push-based contacts still report fine; if the rekuperator GAs (12/1/*) don't answer, that's a KNX-project-export gap (hand-curated block), not the invert fix. **Handoff edited from #76 (diff-rule).**

> **SESSION #79 (2026-09-04, worktree `homelab-wt-20260903-2147` → branch `hd-328-switch-media-vlan50`, HD-328 + close-out; validated green, merged + pushed):** **HD-328 — Switch Media VLAN 50 access ports missing untagged membership (Shield no internet) → FIXED + APPLIED LIVE + Shield ONLINE; stale phantom VLAN 21 cleaned live; HD-325 + HD-328 rows closed.** ① **Diagnosis (live, via RouterOS API through the Pi-99 hop):** the CRS328 bridge **VLAN 50 had NO untagged access members** — only the trunk (`vlan 50 tagged=bridge,sfp-sfpplus1`); `ether14` (console) / `ether20` (Shield) were pvid-50 with `ingress-filtering:true` → access frames **dropped at switch ingress** → the router never learned the Shield MAC → ARP `failed`/`complete:false`, DHCP lease `waiting`, **no WAN** — the exact HD-308 "empty ARP" signature, re-hit. This was the **missing half** of the 2026-09-03 wifi fix (which added the APs as **tagged** `wifi_ports` members but never added any *access* ports to their VLAN memberships). ② **Fix (SSOT + converge template + switch role):** `crs328_converge.rsc.j2` VLAN-loop now adds every `switch_port_map` access port as an **untagged member** of its role VLAN (`VLAN 50 → untagged=ether14,ether20`; Home 10 → printer/nas, IoT 20 → KNX/camera, Mgmt 99 → UPS/APs) — native Jinja (`.items()` filter, NOT Ansible `dict2items` which is unavailable at render); `IaC/ansible/roles/switch/tasks/main.yml` gained the parity task. ③ **Stale phantom VLAN 21 cleanup (HD-325's deploy-gated tail):** `rb4011_converge.rsc.j2` + `crs328_converge.rsc.j2` now remove the dead `vlan21-iot-internet` interface + `dhcp-21` server/pool/network/lease + bridge-vlan entry (idempotent `:if-find` guards). ④ **Applied LIVE (2026-09-04):** rendered both converge + two focused deltas (`crs328_hd328_media_vlan50_delta.rsc`, `rb4011_hd328_vlan21_cleanup_delta.rsc`) via `render-converge.yml` (ok=14) and imported through the Pi-99 SSH tunnels (`ROS_SSH_PORT=22222/22221`, `routeros-apply-delta.sh`: op-read key verify → host-key pin → SCP → `/import` → `Script file loaded and executed successfully`). **Verified live:** switch `vlan 50 untagged=ether14,ether20`, VLAN 21 gone from both switch + router; router `vlan21-iot-internet` gone. ⑤ **Outcome:** Shield now **ARP for its reserved Media IP reachable** (MAC learned), DHCP **`bound`** — **online with internet** (owner confirmed). Deltas deleted (folded into converge); rendered dir cleaned (secrets). ⑥ **Close-out:** HD-325 + HD-328 todo rows **deleted** (fully-done; close-out in owning docs + commit); `docs/network-vlans.md` Port-Type-Reference gained the **access-port-untagged-membership rule (HD-328, LIVE)**; README §2 refreshed. **Handoff edited from #78 (diff-rule).** ⏳ **Deploy-gated/next (unchanged from prior handoffs):** the converge templates now carry the untagged-access membership — next switch/router `render-converge.yml` re-applies it idempotently; HD-326 kids controls implement-next-session; HD-312 remaining tails (n8n firmware workflow, kids off VLAN 40); HD-318 oldsrv blocked on office/ROCM/1P items; HD-313 RouterOS syslog-egress workaround pending.
> **SESSION #78 (2026-09-04, close-out `homelab-wt-closeout2-20260903-1939` → branch `session/readme-closeout`):** **Session close-out — README §2 refreshed, todo sweep confirmed no done rows, HD-325 merged-note placed in-cell.** ① **README §2 (state-of-world):** updated the HD-312 tail to name the **kids device set (HD-326: `tablet-valentina` + `tablet-ipad`)** as the next implement item, alongside HD-325 (VLAN 21 deleted) + HD-317 (DNS-primary live). ② **Todo sweep (CONVENTIONS §4a):** audited every ✅ row — **no fully-done rows remain to delete** (the prior session already swept HD-322/320/323/297/324); the remaining ✅ rows all carry a ⏳/⚠/blocked/deploy-gated or owner tail (HD-325 deploy-gated at next converge, HD-326 open, HD-313/280 live-but-BLOCKED on the ROS syslog-egress bug, HD-318 oldsrv blocked, HD-198 standing-process decision). Fixed the HD-325 `(MERGED to main `a547725`)` marker placement into the cell. ③ **prompt.md → handoff #77.** _No functional/LIVE changes; validate green + signed commit, merged + pushed._ **Handoff edited from #77 (diff-rule).**




> **SESSION #75 (2026-09-04, worktree `homelab-wt-20260903-1855` → branch `session/hd313-hd280`, signed commit `3b1e4e8`, validate green, merged ff-only + pushed):** **HD-313 deploy-gate — router + VPS sides APPLIED + LIVE; end-to-end log flow BLOCKED on a RouterOS 7.24 remote-syslog egress bug. HD-280 fail2ban — DEPLOYED + LIVE (jails http-auth + sshd active).** ① **HD-313 router side LIVE + verified** (`system logging print` rules 6–9 error/firewall/critical/warning → `centralsyslog` action `remote={{ wg_s2s_vps.ip }}:514 src-address={{ wg_s2s_vps.router_ip }}`, `logpipe` read-only user — all on-device via the Pi-99 hop). **VPS side LIVE** (`monitoring` converge: rsyslog active on {{ wg_s2s_vps.ip }}:514, `acquis-routeros.yml` in the CrowdSec config mount, CrowdSec restarted, Alloy re-render with `routeros_syslog`). **Two IaC gaps found + fixed (committed):** (a) router/switch role `routeros_api_password` `set_fact` was **untagged** → any surgical `--tags` converge failed on undefined password (the HD-161 identity assert is `tags: always`); tagged `always` in both roles — without this the surgical router converge for HD-313 could not run at all. (b) **VPS vps-hardening nftables input had NO allow for UDP/514 from the router wg peer** → rsyslog bound {{ wg_s2s_vps.ip }}:514 but the default-deny input policy **silently dropped every syslog packet**; added `iifname "wg-s2s" ip saddr {{ wg_s2s_vps.router_ip }} udp dport 514 accept` (SSOT-derived, live-verified in the ruleset). **⚠ BLOCKED — RouterOS egress bug:** with config correct AND live on BOTH ends (verified via router sniffer `ip-protocol=udp port=514`, live UDP listeners on the VPS wg address across ports 514/1514, multiple action variants incl. the canonical default `remote` action + iso8601 + no src-address, and synthetic `:log info` events all while `/log print` shows the events) the **RB4011 (RouterOS 7.24.1) emits ZERO UDP/514 packets** — matches the reported ROS 7.18–7.24 remote-syslog **egress bug family** (forum.mikrotik.com RB4011/RB5009: “no packets on 514 although syslog reachable”; workaround = re-add/touch the action config or IP-vs-hostname; MikroTik confirmed a 7.17+ regression, fix pending). Router config is correct — the device itself isn't shipping. **⏳ Remaining (RouterOS-side):** upgrade / re-add-action workaround on the RB4011, then re-verify `cscli alerts list` shows RouterOS alerts + Loki `{job="routeros-syslog"}` indexes lines. Switch/AP forwarding stays future expansion (CRS328 has no wg tunnel). ② **HD-280 fail2ban DEPLOYED + LIVE:** Traefik surgical re-render landed `--accesslog` → `/opt/traefik/logs/access.log` created + **streaming CLF** (real requests visible). **Live finding:** fail2ban was **down (crash-loop `Have not found any log file for http-auth jail`)** because the jail's logpath didn't exist until Traefik re-renders (deploy ORDER trap — vps-hardening restarts fail2ban BEFORE the container tier creates the file). Started fail2ban after the re-render → **jails `http-auth` + `sshd` BOTH active** (sshd already counting failed attempts). **Hardening committed:** vps-hardening now **pre-creates `/opt/traefik/logs/access.log`** (touch, 0644) before the fail2ban start task, so any converge order is safe (verified re-run green + jails stay up). ⏳ Remaining (optional): confirm a repeated 401/403 triggers a ban (accesslog so far only 404s). **Owner note (pre-existing, not this session):** the VPS `/etc/hosts` lacks the `vps` entry → every `sudo` invocation warns `unable to resolve host vps` (cosmetic; sudo NOPASSWD still works). **Handoff edited from #74 (diff-rule).**

> **SESSION #76 (2026-09-04, worktrees `homelab-wt-20260903-1853` → branch `hd312-vlan21-removal`, commit `a547725` MERGED to main ff-only + close-out `homelab-wt-closeout-20260903-1932` → branch `session/hd326-closeout`):** **HD-325 — delete VLAN 21 (IoT-Internet), cloud-IoT → VLAN 20 + `wan_allow` flag (SSOT = live) — DONE + MERGED; kids device set DEFINED (HD-326 opened).** ① **HD-325 (VLAN 21 removal):** following the HD-312 2b rejection (per-MAC `access-list` vlan-id is **not** a wifi-qcom-ac capability), the SSOT's `vlan: 21` rows for the cloud-IoT devices (2×LG, 3×Bosch, HAP) were a **phantom** — wifi clients join the IOT SSID (VLAN 20), so VLAN 21 was never reachable; the SSOT rows only created a dead `dhcp-21` reservation + a never-reachable gateway. HD-325 flushes it: `network_vlans` row 21 removed; the six cloud-IoT rows moved to `vlan: 20` with new DHCP-reserved IPs below the IoT pool (see [network-addresses-generated.md](docs/network-addresses-generated.md)) + an explicit **`wan_allow: true`** flag; router role (gateway loop, DHCP comment, two inter-VLAN accepts deleted, `IoT-Internet → Internet allowed` accept deleted, iot-wan loop now keyed on `wan_allow`) + switch role (IoT-Internet port task removed) + `rb4011_converge.rsc.j2` (4 hardcoded VLAN-21 subnet lines) + `rb4011_dns_resolver_delta.rsc.j2` (dhcp-21 line) all cleaned; generated address doc re-rendered; docs updated (network-vlans/smart-home/migration-inventory/deployment-secrets + decision-log row in network-rejected). **Same cloud behavior as before** (per-MAC `src-mac-address` WAN accepts — the mechanism already live), **no phantom VLAN**, SSOT = live. ⏳ **Deploy-gated:** next router/switch converge (removes dhcp-21/vlan21; cloud-IoT bind their reserved VLAN-20 IPs on lease turn-over). ② **Kids set DEFINED (HD-326):** the owner identified the kids devices = **`tablet-valentina` + `tablet-ipad`** (SSOT `network_static_hosts`, both on Kogler/VLAN 10). The legacy kids-control mechanism (VLAN-40 forced filtered DNS + bedtime + Kids→Home drop) is keyed on the **VLAN-40 subnet**, which the kids no longer use (they're on VLAN 10 per HD-312). HD-326 records the decision + the implementation plan (SSOT `kids: true` marker replacing the broken `^(tablet|phone)-` loop heuristic that would snag Domen's/Martina's phones; enable the reserved `kids-*` MAC-list task + per-MAC bedtime/DoT-bypass forward rules on VLAN 10; filtered-DNS dst-nat is the least-tested piece — needs live-verify; **Kids→Home drop intentionally N/A** since kids are ON Home). ⚠ **iPad caveat:** `tablet-ipad` uses an iOS **locally-administered/randomized MAC** (`F6:9B:E2:B9:26:F9`) — per-MAC controls silently fail if the iPad rotates its MAC (decide: disable "Private Wi-Fi Address" for the Kogler SSID). **HD-326 is ⏳ implement-next-session (AI + gate).** **Close-out:** HD-325 merged ff-only to main (+ branch `hd312-vlan21-removal` at `a547725`); HD-326 row + HD-312 tail item (3) updated (device set defined); this handoff → #76 (next free HD: HD-326). **Handoff edited from #75 (diff-rule).**
 **Technitium VPS primary BOOTSTRAPPED END-TO-END: `dns.kogler.si` 502 fixed (Traefik backend-port label) + VPS admin recreated to the 1P value via the documented API + seed role repaired + VPS primary LIVE-SEEDED (all split-horizon records answering).** ① **`dns.kogler.si` 502 after SSO (your Chrome symptom):** root cause = the technitium compose had **no `traefik.http.services.dns.loadbalancer.server.port` label** while exposing BOTH `53/tcp` and `5380/tcp` → Traefik auto-picked the **lowest exposed port (53, the DNS port)** for the `dns` router → after forward-auth it proxied HTTP to port 53 → **502** (the unauth edge still returned 302, so session #72's 302-verify never caught the authenticated leg). Fix: `traefik.http.services.dns.loadbalancer.server.port: "5380"` (every other overlay-served service pins it). ② **VPS primary never seeded:** its `admin` pw ≠ 1P `technitium_login` → `technitium-seed` login failed. The container **seeds a default `admin`/`admin` on first boot** (auth.config/cache.bin regenerate from the image — deleting them does NOT reset; that's why the seed's "empty auth.config → bootstrap" premise only works on a truly-fresh instance). After the 502 fix, the **documented default login `admin`/`admin` worked**, and `POST /api/user/changePassword` (**needs `pass` + `newPass` + `Authorization: Bearer` per [APIDOCS](https://github.com/TechnitiumSoftware/DnsServer/blob/master/APIDOCS.md)** — earlier attempts with only one param failed) set the admin to the 1P value. **Default `admin`/`admin` now FAILS** (verified). ③ **Seed-role idempotency bug (HD-324):** the record-add task referenced `item_resp` with **no `register:`** → `item_resp.json is not defined` → EVERY record-add failed closed (even when the API returned idempotent `status: error` "already exists"). Fixed `register: _tech_record` + tolerate `status: error` (parity with the zone task) → **seed converge `failed=0` idempotently.** ④ **Verified end-to-end:** `dig @159.195.111.66 {ha,dns-pi,stats,logs,csui,sec,traefik,auto}.kogler.si` all answer (`ha`/`dns-pi` → VIP (`ha_vip` SSOT); dashboards → `tailnet_sidecar_ip` (SSOT)); zone `kogler.si` SOA live; `https://dns.kogler.si/` → 302 → sso. **Close-out:** new HD-324 row + HD-317/HD-299/HD-296-297 tails marked RESOLVED (VPS admin recreate + seed DONE); `network-dns.md`/`services-dns.md`/`README.md` status lines updated; **`deployment-manual.md` §1.4c** — imperative admin-bootstrap + seed + the 502-port-label procedure added (repeatable from-scratch). **For owner now:** clear `kogler.si` cookies / use incognito → `https://dns.kogler.si/` → SSO → Technitium console uses the **1P `technitium_login`** password (username `admin`); `https://stats.kogler.si` → SSO → Grafana now resolves. **⏳ Residual:** oldsrv secondary (Phase 3) + `dns` public CNAME apply (`playbooks/dns.yml`) + a Pi `docker_services` converge to land the `dns-pi` 5380 publish. **Handoff edited from #73 (diff-rule).**


> **SESSION #73 (2026-09-04, worktree `homelab-wt-20260903-1744` → branch `session/hd313-312-280`, 3 signed commits `1d8b419` (HD-313), `6898411` (HD-312), `54d1850` (HD-280) + validate green, MERGED to main ff-only):** **HD-313 deploy-gate IaC + HD-312 2b REJECTION + HD-280 fail2ban fix — three backlog items closed (IaC-complete, deploy-gated).** ① **HD-313 (RouterOS → CrowdSec + Loki log shipping) — three IaC gaps that blocked the deploy gate closed:** (a) **`central-syslog` → `centralsyslog`** action-name typo in the router role's `/system logging` rules (RouterOS 7 action names are letters+numbers only — the hyphenated action-ref was the converge rule-creation error); (b) **`acquis-routeros.yml` moved out of the docker_services extra-template set** into the monitoring role, rendered into the CrowdSec **config mount** (`/srv/docker/crowdsec/config/acquis.d/`) with a compose-restart on change — the compose-dir extra-template copy was DEAD (CrowdSec reads acquisitions from `/etc/crowdsec/acquis.d/` only), so RouterOS was never actually ingested; (c) **VPS Alloy now tails `/var/log/remote-syslog/routeros.log` → Loki** (`loki.source.file routeros_syslog` + `loki.process.routeros`, `job=routeros-syslog`) — previously only Docker logs reached Loki, so the documented 'RouterOS logs → Loki' SSOT claim had no live path. **switch/AP forwarding** documented as a future expansion (CRS328 has no wg tunnel; the rsyslog receiver accepts only the router wg peer). ⏳ **Deploy-gated:** router/switch converge (logging action/topics/`logpipe` — verify `system logging print` after re-import), VPS `monitoring` converge (rsyslog live; acquis + Alloy land) + surgical crowdsec re-render, then `cscli alerts list` + Loki `{job="routeros-syslog"}`. ② **HD-312 (3-SSID + per-MAC) — 2b REJECTED:** the CAPsMAN per-MAC `access-list` `vlan-id` ACL is **not a wifi-qcom-ac capability** — verified against the official MikroTik WiFi wiki (`802.11ac chipsets do not support this type of VLAN tagging`; the legacy `/interface wireless access-list` vlan-id does not apply to the modern stack). VLAN assignment on this fleet rides the **CAP bridge access-port pvid model** only; per-device control = firewall MAC-lists (`iot-wan-allow`/`kids-*`, live). Recorded in `docs/network-rejected.md` (append-only) + `network-vlans.md` corrected; todo 2b marked REJECTED. Remaining HD-312: (1) lease-turnover WAN regain live-verify, (3) kids-* lists active (owner: define device set), (4) n8n firmware flow (owner/n8n-side). ③ **HD-280 (fail2ban http-auth jail) — option (a):** Traefik main-edge compose enables `--accesslog` (filepath `/var/log/traefik/access.log`, `bufferingsize=100`, dir-bind `./logs:/var/log/traefik` → host `/opt/traefik/logs/access.log`); vps-hardening ships a **`traefik-http-auth` fail2ban filter** (matches Traefik CLF 401/403 — the forward-auth SSO brute-force signal) + the jail now points at the real accesslog (was a dead `nginx-http-auth` on a non-existent path). ⏳ **Deploy-gated:** VPS `vps.yml` converge (hardening) + traefik surgical re-render → `fail2ban-client status http-auth` active + accesslog has lines + a repeated 401 triggers a ban. **Note:** primary `main` still carries two dirty generated docs (`network-addresses-generated.md`, `services-inventory-generated.md`) from a CONCURRENT session's re-render — left untouched (coordination, not ours). **Handoff edited from #72 (diff-rule).**

> **SESSION #69 (2026-09-03, primary checkout `main` clean → commits signed + validate-all green):** **two work streams:** ① **HD-320 Shelly-on-Pi (earlier this session, commit `683b6ff`):** narrow `Home Pi (HA) -> Shelly RGBW2 REST tcp/80` forward rule in the router role (HD-319 KNX-exception pattern — the Pi node is not `trusted-admin`), + Shelly RGBW2 LED strips added to `lovelace-stanovanje` (`view_luci.yaml`/`view_pregled.yaml`: `light.kuhinja`, `light.wc_4_channel_1..4`, `light.orhideje`, `light.kopalnica_2`), + `skills/shelly/.env.op` vault-ref fix (broken `op://Private` removed — fleet is `auth:false` open; future cred → `Homelab-ansible`), + HD-320 row + owning-doc updates. **Owner/deploy-gated:** converge router (rule live), **add the 4× Shelly in the HA UI by IP** (`shelly-rgbw2-*` on IoT VLAN 20 — SSOT, config-flow only, no mDNS across VLANs) → verify light entities bind + dashboard renders. ② **HD-321 RouterOS admin-password rotation (this close-out):** rotated the shared `admin` password (`mikrotik-admin_login` `old-password`→`password`) on **RB4011 (`router` mgmt), CRS328 (`switch` mgmt), `ap-spalnica`, `ap-dnevna` (mgmt plane — SSOT `network-addresses-generated.md`)** — **verified per device** (old FAILS / new authenticates via the Pi's tagged-99 mgmt-sourced API path; the laptop's direct Home-IP API is INPUT-dropped → any password reads as a false lockout — the valid probe MUST be Mgmt-sourced). **Artifacts refreshed:** re-rendered `render-converge.yml` + `render-routeros.yml` so every `IaC/router/rendered/*.rsc` embeds the NEW password (hash-verified vs vault); **deleted stale** `ap_initial.rsc` (legacy universal AP script with OLD pw — per-AP `ap_initial-*.rsc` are current) and removed a stale `rb4011_converge.rsc` a partial `/import` had left on the router (would re-assert OLD on a reset). **Process notes (self-learn):** rotate ONLY via the repo-native **render → `/import`** (`scripts/routeros-apply-delta.sh` / `apply-converge.yml`, SSH `ansible` identity, pinned host key) — the manual Pi venv + python path is deprecated; the API `/import` step needs `librouteros` in the runner (use the SSH-import fallback when missing); `ansible` is a key-only user and unaffected. **⏳ Remaining (owner, next reset):** re-upload the re-rendered `initial` `.rsc` files to each device's `flash/` before any bootstrap reset so a flash-bootstrap sets the NEW password (live `flash/` not refreshed this session). Rotation runbook recorded in [network-ops.md](docs/network-ops.md) §Rotating the shared admin password. **Handoff edited from #68 (diff-rule).**

> **SESSION #70 (2026-09-03, worktree `homelab-wt-shelly-20260903-1649` → branch `homelab-wt-shelly-20260903-1649`, 1 signed commit `dd7e62f` + validate green):** **HD-320 (Shelly REST firewall) APPLIED LIVE + HD-322 (full converge idempotency) DONE + LIVE-VERIFIED.** ① **HD-320 — Shelly-on-Pi unblocked:** the `Home Pi(HA) -> Shelly RGBW2 REST tcp/80` rule (authored `683b6ff`) was **never deployed live** — the laptop (`trusted-admin`) could reach the Shellys but the **Pi node (Home VLAN, deliberately NOT in `trusted-admin`, HD-310) had NO new-TCP path** (Home→IoT only accepted established/related), so HA's Shelly config-flow times out. **Fix + live-truth:** folded the Shelly tcp/80 **and** KNX udp/3671 rules into `rb4011_converge.rsc.j2` (parity with the router role, which had them but was never converged); applied via render → `/import` through the Pi-99 SSH tunnel (`routeros-apply-delta.sh` + `ROS_SSH_PORT`); **VERIFIED from inside the HA container: all 4 Shellys (`shelly-rgbw2-*`, IoT VLAN 20 — SSOT `network-addresses-generated.md`) answer HTTP 200** (SHRGBW2, auth:false, fw 1.14.0). First-delta residue (duplicate KNX row + below-default-deny placement from a buggy `dst-address` find guard) cleaned by `rb4011_ha_iot_repair.rsc` — both new deltas are idempotent comment-anchored templates. ② **HD-322 — full converge idempotency DONE + LIVE:** guarded every unguarded `/add` in `rb4011_converge.rsc.j2` (user/bridge/bridge-port/bridge-vlan/vlan/ip-address/pool/dhcp-server/network/lease/wg/route/firewall-addr-list) with `:if-find`+`set`-else; **reset the firewall address-lists** (`trusted-admin`/`trusted-ha`/`internal_lan` — they previously STACKED duplicates on re-import); guarded bootstrap-only `/user ssh-keys import` on `/file find` (admin.pub/ansible.pub are gone in steady state — aborted the old re-import); **folded `laptop-domen` into `trusted-admin`** (was live-only via HD-307, missing from SSOT → the address-list reset would have revoked it). **VERIFIED: two consecutive full re-imports, both `Script file loaded and executed successfully`, zero errors; live forwarded-chain = 35 rules clean (was 51 with dupes), Shelly rule + KNX rule both above default-deny, HA→Shelly still 200 after the reset.** **Live lessons recorded in [network-ops.md](docs/network-ops.md) §Rsc authoring conventions (7-10):** RouterOS 7 `find where address=` does NOT match `/ip address`/`/ip dhcp-server network`/`/ip firewall filter dst-address` (use `interface=`/`gateway=`/`comment=`); inline `#` comments inside `:if do={}` blocks break the parser (`expected end of command`); chain-resets (NAT/forward/input `remove [find chain=…]`) are idempotent but address-lists are NOT (need explicit reset). **Merged to main via rebase onto `f5b3b59` (the concurrent HD-297c closeout) — no conflict (todo.md HD-297 vs HD-320/322 rows non-overlapping), fast-forward merge, pushed conceptually-ready.** **⏳ Remaining (owner only, HD-320 deploy-gate tail):** add the 4× Shelly in the HA UI by IP (Settings → Devices → Shelly, config-flow only, no mDNS across VLANs) → verify the light entities (`light.kuhinja`, `light.wc_4_channel_1..4`, `light.orhideje`, `light.kopalnica_2`) bind + the Stanovanje dashboard renders. **Handoff edited from #69 (diff-rule).**

> **SESSION #72 (2026-09-03→04, primary checkout main clean at start → commits `f5b3b59`, `b096af4`, `1ea0005`, `91fa044`, `d485a67` signed + validate green + merged + pushed):** **HD-297c investigation (edge-side fix confirmed live) + `https://dns.kogler.si/` FIXED + LIVE + `stats.kogler.si` root-caused.** ① **HD-297c (plain `*.kogler.si` → 302 SSO on the tailnet edge):** root cause confirmed = a **comment-brace render crash** — `middlewares.yml.j2`'s own comment contained a live `{{ vault['…'] }}` expression, which Jinja evaluates even in `#` comments → `dict has no attribute '…'` → the render task failed → file never landed. Already fixed + committed in `fc2b0a1` (VPS converge, 12:11) — edge live-verified (`stats.kogler.si` → 302 → sso; `dsh.ts` → 200; error gone). ② **`https://dns.kogler.si/` was 404 — FIXED + LIVE:** the Technitium route label + `authentik-forward-auth@file` existed since HD-62, but the **`forward-dns` Authentik ProxyProvider was never added to `ks-forward-auth.yml`** → the embedded outpost returned 404 for `Host: dns.kogler.si` (verified via `wget http://authentik-server:9000/outpost.goauthentik.io/` from the traefik netns → 404; container UI answers `:5380` → 200; laptop+VPS curl both 404). Added `provider_edge_dns` (forward_single, external_host `dns.kogler.si`, internal_host `http://technitium:5380`) + `app_edge_dns` (slug `edge-technitium`) + outpost `providers` list entry. **KEY LIVE FINDING:** blueprint upsert does NOT merge the embedded outpost's `providers` m2m — the provider existed in the DB but the outpost wouldn't serve it until ORM-bound (`ak shell`: `Outpost.first().providers.add(ProxyProvider.objects.get(name='forward-dns')); save()`; outpost controller reloaded). **VERIFIED live:** laptop + VPS `https://dns.kogler.si/` → **302 → sso.kogler.si** → 200 at the SSO login with `redirect_uri=dns.kogler.si` (outpost now 16 providers). Lesson recorded in [services-authentik.md](docs/services-authentik.md). ③ **`stats.kogler.si` root-cause (owner-facing BLOCKED):** `.ts` twin works (MagicDNS extra_records) but plain name fails because **the VPS primary Technitium NEVER SEEDED** — its `auth.config` admin pw ≠ 1P `technitium_login` → `technitium-seed` login errors (`Invalid username or password for user: admin`) → no `stats`/`ha`/… A records on the VPS primary → laptop resolver (router DHCP → VPS primary first, oldsrv down, Pi last) caches the negative. Router resolves it fine (`dig @router stats.kogler.si` → `tailnet_sidecar_ip`); Pi tertiary is seeded. **⏳ Owner (exact):** log into `dns.kogler.si` (now works) → **recreate the VPS Technitium admin with the 1P `technitium_login` password** → re-run `ansible-playbook playbooks/vps.yml -l vps -t docker_services -e docker_services_scope=technitium` (seed role bootstrap) → verify `dig @<dns_primary_ip per SSOT> stats.kogler.si` → `tailnet_sidecar_ip`. **⏳ Owner (HD-297c):** browser-verify `https://stats.kogler.si/` → SSO → Grafana. **⏳ Owner (HD-317 (1b)):** complete the SSO login flow to `dns.kogler.si`. **Handoff edited from #71 (diff-rule).**

> **SESSION #71 (2026-09-04, worktree `homelab-wt-20260903-1657` → branch `session/grafana-ups`, 2 signed commits `849bcd4` + `a5e5028` (rebased → `144c752`+`20833ac`), validate green, MERGED to main):** **HD-315 (Grafana dashboards) Traefik metrics + HD-08 (UPS alerts/dashboard) — IaC DONE + deployed LIVE.** ① **Traefik metrics (HD-315):** enabled `--metrics.prometheus=true` + `--entryPoints.metrics.address=:8082` (overlay-only) on the main edge; **joined `db-internal`** so Prometheus resolves `traefik:8082` (was cross-bridge NXDOMAIN → `up=0`); dashboards fixed to the **v3 name `traefik_entrypoint_requests_total`** (the v2 `traefik_requests_total` didn't exist). Deploy hiccup: `--metrics.prometheus.addr` is NOT a v3 flag (field-not-found crash-loop, caught+recovered same deploy). **LIVE: `up{job="traefik"}=1`, series flowing.** ② **UPS (HD-08) — metric-shape bug found + fixed:** the DRuggeri/nut_exporter v3 (`(devel)` build) emits **`network_ups_tools_*`** over **`/ups_metrics?ups=powerwalker`** (plain `/metrics` empty) with **per-flag `ups_status{flag=OB/RB}`** — NOT the authored `nut_*` bitmask. Fixed coherently: Prometheus scrape (`metrics_path`+`params.ups`), all 4 alert rules, `homelab-ups` dashboard (renamed + status panel + dropped non-existent `input_voltage`), `nut_ups_name` SSOT in group_vars/all. **LIVE: `battery_charge=100`, `OL=1/OB=0/RB=0`, all 4 UPS alert rules + 6 dashboards provisioned in Grafana.** ⏳ **Remaining (owner/next):** pin `nut_exporter_release` to a tagged release (running `(devel)`) + re-converge nas; owner panel-render check on the live dashboards. **Handoff edited from #70 (diff-rule).**

# prompt.md — Deployment Execution Handoff #79 — **HD-328: Switch Media VLAN 50 untagged-access membership (Shield no internet) FIXED + applied live + Shield online; stale VLAN 21 cleaned live; HD-325 + HD-328 rows CLOSED; prev #78 = close-out; next free HD: HD-329** (quote `scripts/next-hd.sh`)
 (docs(handoff): prompt.md → #74 — session 2026-09-04: HD-313 router+VPS sides LIVE, end-to-end blocked on ROS 7.24 syslog-egress bug; HD-280 fail2ban live)
 (docs(handoff): prompt.md → #73 — session 2026-09-04: HD-313/312/280 closed IaC-stage; remaining all deploy-gated/owner)
 (docs(handoff): prompt.md → #69 — router DNS-primary VPS→oldsrv→Pi LIVE (API-apply, 3-server cap), converge-apply infra, dns CNAME + dns-pi 5380, HD-321)
 (fix(ids): renumber full-converge-idempotency row HD-321->HD-322 — concurrent session took HD-321 for the RouterOS admin-password rotation (live, done))

> **SESSION #69 (2026-09-03, worktree `homelab-wt-20260903-1524` → branch `session/dns-primary-router`, 5 signed commits + validate green):** **Router DNS-primary on the MikroTik RB4011 — HD-317 ⏳ item 2 (DHCP/resolver ordering VPS→oldsrv→Pi) DONE + LIVE.** IaC + live: DHCP dns-server chain + `/ip dns` upstream now **VPS public IP (159.195.111.66) → oldsrv → Pi (all per SSOT dns_primary_ip/dns_secondary_ip/dns_tertiary_ip)** (+1.1.1.1,1.0.0.1 for /ip dns) on **all 7 VLAN networks** (incl. Mgmt 99, previously stale literal) — **applied via the RouterOS API** (KEY LIVE FINDING: the rsc-import path **mangles** multi-value `dns-server` silently — drops the Pi / substitutes the gateway; the API `set dns_server=<csv>` sticks exactly; the `router` role `api_modify` is the authoritative steady-state apply). **RouterOS caps `dhcp-server network` at 3 dns-servers** — aligned roles/router/tasks/main.yml + rb4011_converge.rsc.j2 + new rb4011_dns_resolver_delta.rsc.j2 to the 3-resolver chain (dropped the gateway-as-4th). **Converge-apply infra fixed** (all committed): apply-converge.yml recursion fix + `/import` cmd form (`/import file=<rsc>` — module splits on spaces) + **SCP to absolute `/` RouterOS path** (a relative dest leaked a rendered rsc with secrets into IaC/ansible/playbooks/ — deleted from primary + `.gitignore` only covers IaC/router/rendered/, hardened note); ansible-network-hop.sh owns the non-recursive converge_rsc default; routeros-apply-delta.sh gained **ROS_SSH_PORT** (apply via Pi-99 SSH tunnel); pppoe-client add guarded idempotent. **SSOT additions:** `dns` → vps.kogler.si CNAME (live-added by owner; now in cloudflare_dns/vars — apply via playbooks/dns.yml from home control plane) + technitium compose **5380 publish on the Pi tertiary only** (fixes the dns-pi web-UI 502; needs a Pi docker_services converge to land live). **Merged concurrent session's HD-320** (Shelly REST firewall in roles/router) via rebase onto main — no conflict, validate green. **HD-322 opened** (make the full converge fully idempotent on re-import — it still aborts on `/user add` line 44). Rebased onto main's HD-320. **Live applied + verified:** router DHCP networks + /ip dns. **Deferred/next (owner gates):** Pi converge (5380/dns-pi), VPS admin recreate via dns.kogler.si UI → seed, Cloudflare `dns` apply. **Handoff edited from #68 (diff-rule).** BRANCH NOT MERGED: concurrent session has uncommitted WIP on the primary (prompt.md + todo.md, their HD-321 admin-pw rotation) — merge `session/dns-primary-router` to main after their WIP commits; HD-321 = their rotation (done/live), HD-322 = my full-converge idempotency (open).

> **SESSION #66 (2026-09-03, session branches `session/oldsrv-zfs-headers` + `session/storage-zfs-create-fix` + `session/storage-zfs-create-fix2` + `session/hosts-ssot-fix` + `session/cockpit-dynamic-dir` → all merged to main):** **oldsrv Phase-3 first Ansible contact — provision reached the `office` role.** ✅ **mgmt-plane outage fully RESOLVED:** re-verified the tagged-99 plane was dark again (vlan-99 bridge entry had `ether2`/`ether10` in **UNTAGGED** — same class as #65's Winbox fix); fixed via render + `/import rb4011_pi_delta.rsc` (sets `vlan-99 tagged=bridge-lan,sfp-sfpplus1,ether2,ether10 untagged=ether7,ether9`) — oldsrv→router99/pi99 now 0% loss, ARP REACHABLE on the tagged leg. ✅ **`home_servers.yml` on oldsrv (Home leg) reached `office`:** docker ✅ (installed+enabled) · network ✅ (netd units, /etc/hosts) · **storage ✅ (`nvme` ZFS pool CREATED** — owner OK'd wiping the 970 EVO NTFS, backed up on nas; datasets + NFS mounts to nas live) · nut ✅ (client) · cockpit ✅ (+ `/opt/traefik/dynamic/cockpit.yml`) · desktop ✅ · office ⚠. **5 IaC bugs fixed + committed (all signed `G`, merged + pushed):** ① `linux-headers-amd64` in storage role — DKMS couldn't build zfs for the running trixie kernel (`7a084ef`); ②+③ fresh-build pool create never reached — import task aborted on missing pool; refactored to iterate `_pool_list.results` + skip import when `allow_create` (`5c3c1f5`, `6ee6b76`); ④ `/etc/hosts` rendered from `network_static_hosts` SSOT, not `hostvars.ansible_host` — the `-e ansible_host` override leaked into every entry (nas→oldsrv's own IP → NFS 'Connection refused') (`66dd629`); ⑤ cockpit creates `/opt/traefik/dynamic` before writing the route (`4e79bbe`). ⚠ **BLOCKED (exact stop-points for next session):** ① `office` role — ONLYOFFICE apt repo `squeeze` suite + GPG key mismatch (`E09C…DE8E5` missing) — needs repo/key fix; ② `amd_rocm` — ROCm noble-on-trixie dep conflict (distro `rocm-cmake`/`hipcc` vs ROCm 7.2.4 pins) — needs pinning/removal decision; ③ `pihole_password`/`sonarr_api`/`radarr_api` 1P items still MISSING (owner-created; `privado-vpn_api`/`signal_api`/`ha_api` now present) — `docker_services` (big service deploy) gated on them. ⚠ **`switch_port_map` gap is MOOT:** oldsrv is wired to **router ether2** (verified live — MAC on ether2, NOT the switch), so no oldsrv switch entry needed. · [todo.md HD-318](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 3. **Handoff edited from #65 (diff-rule).**
> **SESSION #65 (2026-09-03, session branches `session/knx-fw-rule` + `session/lovelace-fix` + `session/router-fix` + `session/dashboard-fix` + `session/closeout`):** **HA is now reachable at `https://ha.kogler.si/` (DNS cutover → VIP done), KNX is live (integration loads real entities), and the whole tagged-99 mgmt plane is BACK.** Root cause of the mgmt outage (resolved): the vlan-99 bridge entry had `ether10`/`ether2` in **UNTAGGED** instead of **TAGGED** (a prior full `router.yml` converge flipped them) → Pi's tagged Mgmt frames dropped → ARP FAILED cluster-wide. Fixed via Winbox one-liner (`/interface bridge vlan set … tagged=bridge-lan,sfp-sfpplus1,ether2,ether10 untagged=ether7,ether9`) + re-configure + verified. **3 router converge blockers fixed + committed (all were working-HD bugs):** ① HD-312 `iot-wan-allow` MAC-address-list task put MACs into `/ip firewall address-list` (IP/CIDR-only → live 'not a valid dns name') → removed (per-MAC mechanism is the `src-mac-address` forward-accept, which is already correct); ② HD-155 `vps_scoped_home` duplicate block (oldsrv/pi/router/switch twice) → dedup; ③ HD-313 logging action name `central-syslog`/`central_syslog` (hyphen/underscore rejected — RouterOS action names letters+numbers only) → renamed `centralsyslog`; `mikrotik-logpipe_api` seeded. **KNX (HD-319) LIVE:** narrow forward-accept `udp/3671 → knx-ip` (verified via router API, count 1); Pi reaches `knx-ip:3671`; KNX integration set up in HA (UDP Tunneling v1, NAT off, local IP blank) — entities load, lights/sensors visible. **Dashboard fixes:** `view_pregled.yaml` — removed the dead `weather.meteoblue…` card (meteoblue disabled, HD-22) + fixed `binary_sensor.rekuperator_filter_dirty` → `sensor.rekuperator_filter_replace`; also added the 2 missing rekuperator sensors (supply_temperature, room_humidity) per SSOT. Dashboard → HTTP 200, no config errors. **HA login:** created `admin`; OAuth/IndieAuth callback error traced to missing `external_url` (still unset — see owner step); login works on retry. **Lovelace include-path bug fixed** (`lovelace/view_*` → `view_*` relative; deployed).
> **Remaining (owner/next):** (1) KNX config-flow DONE — verify the 3 rekuperator GAs (12/1/*) answer on the bus (some GroupValueRead warnings); (2) set `external_url: https://ha.kogler.si` in HA (`core.config` null) to prevent future callback errors (add to `configuration.yaml.j2` + converge Pi); (3) phase-4 owner steps rd: Authentik OIDC on `ha` route, `ha.kogler.si` → VIP cutover DONE, failover runbook exercise, `dns-pi` web UI still 502 (secondary has NO 5380 port published — compose comment says 'NO host 5380' but the traefik-ha route expects it; needs a decision); (4) HD-312 phase 3 live: cloud-IoT regain WAN after lease turn-over — verify. **Handoff edited from #64 (diff-rule).**

> **SESSION #64 (2026-09-03, worktree `homelab-wt-20260903-1145` → branch `session/vps-converge`, commits `089ab7e`→`5ae21fb` merged + pushed):** **VPS converge DEPLOYED** — the HD-317/315/316 deploy-gate (owned by this session) landed.
> ① **Full `vps.yml` converge** `failed=0` up to the fragile layer; **surgical finishing runs** (`--tags monitoring` + `--tags docker_services -e docker_services_scope=crowdsec`) green → **Technitium primary UP** (`:53/udp+tcp` published; container on the `dns-servers` overlay pin (`tchnitium_dns_overlay_ip`, SSOT) + `traefik-public`; web UI serves on the overlay), **5 HD-315 Grafana dashboards** copied to `/srv/docker/grafana/provisioning/dashboards/` (file-provider hot-reload) + **homepage** tiles rendered + restarted → `https://home.kogler.si/` → **302 Authentik SSO**, **rsyslog** RouterOS receiver installed+restarted, **crowdsec** healthy with `a1ad/mikrotik` enabled.
> ② **4 live-fixes (pre-existing bugs fixed + committed, validate green):**
>    - `network` role dhcp-client MAC assert fired on **static hosts** (VPS) — HD-314 added `dhcp_client` to `network/vars/main.yml` for every host (fallback `none`), so `when: dhcp_client is defined` became a tautology → gated block on new `dhcp_client_optin` (`089ab7e`).
>    - **Literal `{{ vault['…'] }}` inside .j2 comments** crashed the render (`dict has no attribute '…'`; Jinja renders braces even in `#` comments) — swept 8 templates (`b21c835`).
>    - **rsyslog not installed** on the netcup minimal image → monitoring role now `apt install rsyslog` (the old comment claimed stock rsyslog exists — wrong on this image) (`f9321ad`).
>    - **Phantom `crowdsecurity/syslog-logs` collection** (not in the hub) crash-looped crowdsec on boot — dropped from `crowdsec_collections` + both owning docs (`5ae21fb`).
> ③ **traefik-tailnet sidecar** recovered from a compose-restart netns race (`docker compose up -d` on the project; manual §1.4b precedent).
> **⏳ Remaining DNS deploy-gate (owner + next steps):** (1) **seed the split-horizon static A records on all 3 Technitium instances** — `ha`→VIP, `dns-pi`→VIP, tailnet dashboards→`tailnet_sidecar_ip` — **BLOCKED: no `technitium_*` admin credential in 1Password `Homelab-ansible` (verified absent); the Technitium API seed needs it → create the 1P item + wire an Ansible seed role (currently a manual/API step per network-dns.md)**; `dns.kogler.si` will NXDOMAIN until then (no public record by design + empty zone on the fresh instance); (2) **router `rb4011` step** (DHCP/resolver ordering VPS→oldsrv→Pi — still gated on the tagged-99 mgmt-plane outage); (3) owner verify: `dig @159.195.111.66 dns.kogler.si`, tailnet `stats.kogler.si`, homepage family vs Domen technical. Handoff edited from #63 (diff-rule).

> **SESSION #61 (2026-09-03, worktree `homelab-wt-20260903-0941` → branch `session/ha-pi-provision`, commits `f6a6114` + `55b2a9d` merged + pushed):** **Phase-4 Pi HA-primary provision LANDED + LIVE.** Full `raspberry_pi.yml` run `failed=0` on `pi.kogler.si` (Debian trixie + Docker): **home-assistant-primary** (HA 2026.8.1 + keepalived **MASTER** on VIP `ha_vip`) · **technitium-secondary** (DNS :53 alive) · **traefik-ha** (TLS `*.kogler.si` valid, Verify 0) — all up + verified; `https://ha.kogler.si/` → **302**. HA boots **0 config errors**. Live-fixed this session: docker_services vault pre-pass crashed on entries without `enabled:` (Pi primary/secondary omit it) → defined+truthy ∪ missing-key = default(true); deploy-service first-boot guard stat used `{{ item }}` with `loop_var: ha_src_file` (undefined) + fail_msg referenced undefined `ha_standby_path` on the Pi → fixed to loop var + registered results; **technitium compose** duplicate `cap_add` + `read_only: true` crash-looped (`Read-only /etc/dns, /var/log/technitium`) → merged caps + relaxed read_only (forgejo precedent) + tmpfs `/etc/dns`; **knx-hass-gen.py emitted a `knx:` wrapper** inside knx-entities.yaml (included via `knx: !include`) → nested `knx:` invalid — removed wrapper, regenerated (legacy rekuperator + appliance sensors preserved); **meteoblue NOT in HA 2026.8.1** → platform failed setup + blocked HA (incl. proxy trust) — weather block disabled until available/HACS; **`configuration.yaml` trusted_proxies** needed the Pi traefik-ha host IP (`network_mode: host` → source = the Pi's own Home IP, SSOT `pi` row), AND a **stale `.storage/http` on the Pi** held `use_x_forwarded_for` absent (HA 400 behind proxy) → deleted it so HA re-derives from YAML; **ha-cert-sync.sh** excludes `dump/` (ACME private keys never leave the issuer) — plus a **VPS-side operator step that was missing**: `ha-sync` key was never authorized on the VPS `ansible-admin` (rsync-only wrapper + key now added; rsync installed on the VPS). **Close-out (same session):** `docs/deployment-pi-provision.md` status banner + §6 success criteria marked LIVE; todo.md HD-307/HD-310 trimmed to owner-gated remainders. **Owner follow-ups:** KNX UI config-flow import (tunneling `knx_router_ip`), Authentik OIDC on the `ha` route (no Forward-Auth), `ha.kogler.si` DNS cutover → VIP (`smart-home-failover.md`), failover runbook exercise. **Handoff edited from #60 (diff-rule).**

> **Role:** entry point for the next session. This handoff is a **pointer index** — every item's owning doc/row holds the detail. Start with README.md §0 (intent routing), then §1 context, then §3 execution order. Read [todo.md](todo.md) for the full row bodies.

> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 · [scripts/guard-session.sh](scripts/guard-session.sh) · [todo.md](todo.md) · archive: [reports/changelog.md](reports/changelog.md), [reports/deployment-journal.md](reports/deployment-journal.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — §Tags & surgical runs (`docker_services_scope`, `base` tier) + §Deploy Timing Runbook.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — compose/secret conventions + fail-loud rule.
4. [docs/network-vpn.md](docs/network-vpn.md) — §Pattern A tailnet sidecars + current tailnet edge state + §MagicDNS (extra_records design).
5. Owning `docs/*.md` (per task) — the decision-logs + spec SSOT; read before re-deciding anything. Historical changelog/journal rows live in the frozen `reports/changelog.md` / `reports/deployment-journal.md` (archive-only).

## 1. Environment (WSL Debian primary, ext4) — as of #67, the repo runs from the WSL Debian primary on ext4 (not Windows)

Adapted to the WSL Debian primary (repo moved to ext4; the Windows-runner/9P-gate era is legacy — see "9P gate: N/A" in §4). `python3`/bash/LF/UTF-8 no-BOM. Secrets → 1Password item.field only, `>-` for YAML renders. **Self-learned (2026-08-26):** do NOT use multi-line bash heredocs with backslashes/backticks (mangled through `bash -c`) — write patch scripts to a temp file and run them. **Signed-commit gotcha (still valid):** keys in `~/.ssh/github_signing`/`github_auth`; `Couldn't find key in agent` → `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.

## 2. State snapshot (start of next session)
- **HD-312 3-SSID WiFi — phases 1 + 2a DONE + LIVE (2026-09-03).** Live SSIDs = **Kogler (VLAN 10) + Kogler IOT (VLAN 20) + Kogler guest (VLAN 30, 5GHz)**; IOT-WAN + Kids SSID configs **deleted** (configs + security profiles removed live + from SSOT; items kept in 1P, unreferenced). Phase 1 static-IP sweep: 4× Shelly RGBW2 + 4 family WiFi clients (valentina/phone-domen/phone-martina/tablet-ipad) + UPS + iLO now have MAC + static reservations in `network_static_hosts` (router converge applied them live — UPS already bound its `.99.9`; iLO/phones/iPad bind at next lease renewal/reconnect). Phase 2a: guest turned up via idempotent deltas (`rb4011_guest_delta`/`crs328_guest_delta`/`ap_guest_delta` .rsc.j2, no secrets, folded into render-converge.yml); CAPsMAN provisioning now band-split by SSID `band:` (2.4GHz = Kogler+IOT, 5GHz = Kogler+guest). **⚠ LIVE GAP → RESOLVED (2026-09-03, phase 3 live): the cloud-IoT appliances (2× LG AC, 3× Bosch, HAP) were on the deleted `Kogler IOT WAN` SSID (VLAN 21, WAN allowed) — moved to `Kogler IOT` (VLAN 20 = no internet).** HD-312 phase 3 (`src-mac-address` WAN accepts) **LIVE 2026-09-03** — the per-MAC forward-accept is on the router; the appliances regain WAN as their DHCP leases turn over to the reserved VLAN 20 pool (verify). ⏳ Remaining: (2b) CAPsMAN per-MAC `access-list` vlan-id; (3b) `kids-*` MAC-lists active + kids-control off VLAN 40; (4) n8n workflow + scoped router API user. Full spec [network-vlans.md](docs/network-vlans.md) §3-SSID + [network-rejected.md](docs/network-rejected.md) + [todo.md](todo.md) HD-312.
- **HD-310 FINAL port model = LIVE + SSOT (2026-09-02)** — `router_port_map`: **old-srv ether2 = pvid10 + tagged[99] · laptop-domen ether3 = pvid10 + tagged[] (untagged Home only — mgmt via Pi hop) · rpi ether10 = pvid10 + tagged[99]** (nas-ilo/homematic-ap stay pvid99). Live router verified: VLAN10 untagged=ether2,3,10 · VLAN99 tagged=ether2,10 (+trunk/self), untagged=ether7,9. Services `available-from` = **Mgmt subnet only** STRICT again ([network-addresses-generated.md](docs/network-addresses-generated.md)) (Home→Mgmt forward removed — **Home must never reach core infra**; Pi `eth0.99` tagged leg is the only mgmt-plane client). SSH: `~/.ssh/config` `pi99`→mgmt `pi` / `router99`→`router` mgmt via ProxyJump `pi` (IPs in [network-addresses-generated.md](docs/network-addresses-generated.md)) — verified. **Winbox GUI tunnels (2026-09-02, SSOT [network-vpn.md](docs/network-vpn.md)):** `ssh -N switch-wb`/`ap-spalnica-wb`/`ap-dnevna-wb` → `127.0.0.1:8292/8294/8295` (forward runs ON the Pi — the Mgmt-leg holder; a ProxyJump-to-device final hop leaves RouterOS SSH hosting the forward, which breaks Winbox auth — live lesson). Cable moves: ✅ **DONE 2026-09-02** — Pi → **ether10**, laptop → **ether3** (owner moved the cables; mini-switch freed); live end-to-end re-verified (Pi Home leg, `router99` via tagged-99 hop, router bridge ports pvid=10 on ether2/3/10 + pvid=99 on ether7/9).
- **HD-302 Comtrend modem mgmt = LIVE + SSOT (2026-09-02, commit `22abb8a`)** — live fix: `router_on_modem` `/32`→`/24` on ether1 (connected route to the modem subnet) + **srcnat masquerade** out ether1 (dst=`comtrend_modem.modem_mgmt_ip`:80/443) because the consumer ONT only answers its web UI to its directly-connected peer (router on the modem LAN) — forwarded HTTP from deeper subnets is silently ignored. **Live-verified:** laptop → modem UI returns 401 (auth page); router can ping the modem. SSOT: `comtrend_modem` (router.yml), `rb4011_converge.rsc.j2` SNAT rule, generated SSOT re-rendered. ⏳ **Remaining:** private DNS `modem.kogler.si` → needs the first Technitium (oldsrv offline).
- **Phase 2 (nas) DONE + LIVE (2026-09-03, HD-314; row closed — record in owning docs).** `storage.yml` failed=0. ZFS pools tank (mirror) + bulk (RAIDZ2) IMPORTED + datasets/props; NFS exports → oldsrv (`/tank/data`, `/bulk/media`, `/bulk/data/immich-thumbs`, SSOT oldsrv Home IP `/32`, root_squash anonuid/gid=1005) — `exportfs` verified; sanoid+syncoid timers; Samba; **NUT master LIVE** (PowerWalker `upsc` battery 100% / Innova Unity; `upsd` :3493 + `nut_exporter` :9199 + `zfs_exporter` :9198 active); cockpit-storaged (trixie has no cockpit-zfs). nas's DHCP client-id = MAC (`dhcpcd.conf clientid mac`) → binds its **reserved Home static** (SSOT) instead of the dynamic `.100`. Host state + full fix list: [hardware-nas.md](docs/hardware-nas.md) + [hardware-ups.md](docs/hardware-ups.md) + [deployment-manual.md](deployment-manual.md) §Phase 2. ⏳ **Remaining:** battery-pull test (owner, HD-06), `bulk/migrate` landing-zone redistribution (HD-207).
- **⚠ Live-ops flag (2026-09-03) — RESOLVED same day:** the **router mgmt-plane (tagged-99 → the router's SSOT Mgmt IP) was intermittently unreachable** during the nas session (ARP FAILED from the Pi; Home-plane ICMP OK). Root cause (confirmed via WinBox this session): a converge had flipped the VLAN-99 bridge tagged memberships on `ether2`/`ether10` to UNTAGGED → Pi tagged Mgmt frames dropped. Fixed with the Winbox one-liner (tagged=ether2,ether10 restore) → `router99`, API over the Pi hop, and every mgmt client now ARP-REACHABLE. Recorded RESOLVED in [network-ops.md](docs/network-ops.md).
- **DNS: oldsrv/Technitium (primary DNS) — oldsrv back on `.101` (DHCP lease, 2026-09-03)** — it's powered on + pingable at its transient lease; still needs the Phase 3 provision (networkd units + DHCP client-id=MAC) before it runs Technitium at its reserved Home static. Laptop DNS stays `1.1.1.1`/`1.0.0.1` until then.
- **Pre-existing (not from this session, unaddressed):** `sync-skills.sh` drift — deployed `~/.pi/agent/skills` (`plan-task`, `run-task`) differ from repo `skills/` (repo = SSOT). Fails gate item 13 at HEAD too. Owner decision: `bash scripts/sync-skills.sh --push` (deploy repo→deployed) or `--pull` (capture deployed→repo).

- **Phase 1 (VPS edge) is LIVE** — full enabled `docker_services` set converges green; observability + Authentik + headscale + traefik-tailnet all up. **HD-298 (pi-dev pty.node) DONE + LIVE-VERIFIED** (#63-era; row closed — record in owning docs): `pi-dev:8080` 200 via sidecar; 4-layer fix (ignore-scripts removal + glibc node:24 + npm allowScripts + cap_add CHOWN/FOWNER/DAC_OVERRIDE).
- **HD-297 tailnet clean URLs: LIVE.** `https://dsh.ts.kogler.si` / `https://pi-dev.ts.kogler.si` → **200**; plain `*.kogler.si` → **302 → sso** — root cause was a comment-brace render crash (`fc2b0a1`), middlewares.yml renders + loads (detail §3 item 6). **⚠ `stats.kogler.si` plain still blocked from the laptop — VPS primary never seeded (see §3 item 2).**
- **Phase 4 (Pi): FULLY PROVISIONED + LIVE (2026-09-03, HD-307/310/04).** Pi OS Lite installed (hostname `pi`, user `admin` + `ansible-admin`, SSH key `ansible-admin_ssh`); port model + physical final (Pi on **ether10** untagged Home 10 + tagged Mgmt 99; laptop on **ether3** untagged Home only); host-side dual-home = two NM keyfiles (`pi-eth0.nmconnection` Home + `pi-mgmt.nmconnection` tagged-99, verified). **Full `raspberry_pi.yml` provision run `failed=0`** — **home-assistant-primary** (HA 2026.8.1 + keepalived MASTER on VIP `ha_vip`), **technitium-secondary** (DNS :53 alive), **traefik-ha** (TLS `*.kogler.si` valid) ALL UP + verified; `https://ha.kogler.si/` → **302**; HA boots **0 config errors**. Live-fixed: docker_services enabled-set, first-boot guard loop_var, technitium read_only/cap_add, knx-entities `knx:` wrapper, meteoblue-not-in-2026.8 (disabled), trusted_proxies + stale `.storage/http`, ha-cert-sync `dump/` exclusion + VPS ha-sync key auth/rsync. **Session #65 completion:** `ha.kogler.si` DNS cutover → VIP DONE (verified), **KNX LIVE** (HD-319 firewall rule + HA config-flow UDP v1 → entities load), **Lovelace dashboard fixed** (relative includes + Pregled card fixes), logged in as `admin`. ⏳ **Remaining (owner-gated):** Authentik OIDC on `ha` route, `external_url` set, `dns-pi` 502 decision, failover runbook. Runbook: [deployment-pi-provision.md](docs/deployment-pi-provision.md) (status = fully provisioned).
- **RouterOS apply convention (decided 2026-09-01, [network-ops.md](docs/network-ops.md)):** `initial`(manual bootstrap) → `converge`(=full, SSOT, idempotent, universal apply) → `delta`(transient only, fold back). API = verify/idempotent only. **Apply = render → /import (SCP over SSH, pinned host key, `ansible` identity).** **⚠ Ordering pitfall (live 2026-09-01):** a FORWARD accept appended at the tail lands BELOW the repo's mid-list "Default deny inter-VLAN" and is shadowed (HD-307 hit this: rule #33 below deny #25 → dead). Fix pattern (in network-ops.md): dedup → re-add → `move … destination=$deny` (RouterOS 7 = places BEFORE) → always verify position. Idempotent deltas: `rb4011_pi_delta.rsc.j2`, `rb4011_hd307_delta.rsc.j2` (both committed, rendered via render-converge.yml).
- **HD-309 (Shield Media→WAN) DONE + LIVE-VERIFIED (2026-09-01; row closed — record in network-ops.md + git).** Root cause was the router DNS tertiary (SERVFAIL, no upstreams) — NOT the forward-ACCEPT. Fixed: `/ip dns servers=1.1.1.1,1.0.0.1` + row-73 WAN accepts. Live: Shield has 6 established Media→WAN conns. Tooling: `scripts/routeros-apply-delta.sh`.
- **Phase 1.5 network redo DONE except close-out (2026-09-02):** switch + APs reset + `.pub`/`.rsc` re-rendered (flash/ fix, HD-304); router on the new design; **wg-s2s handshake RESOLVED + LIVE (HD-285, closed — two-key fix, commit `bc4eb95`, record in owning docs)**; owner MikroTik password rotation pending.
- **wg-s2s (HD-285, row closed) RESOLVED + LIVE 2026-09-02 (two-key design fix).** Root cause: shared `wg_password` on both ends → identical pubkeys → router handshaking with itself. Now: router `wg_password` + VPS **NEW `wg_password_vps`** (distinct), oneshot owns the VPS iface (networkd create-only + Unmanaged), router HD-155 accept moved above the Default deny, router endpoint = `vps.kogler.si` FQDN. Live: handshake fresh (keepalive), VPS→router-mgmt 0% loss. Commit `bc4eb95`. · owning docs + git history
- **Vault state:** `check-vault-items.sh --strict` → **all needed items present, MISSING list empty** (exit 0; includes `technitium_login`/`technitium_api` created 2026-09-03 with the seed role — count re-derive at write time per derived-values ban).
- **ID registry:** next free = max(HD)+1 in [todo.md](todo.md) — always re-derived at write time (CONVENTIONS §1/§4); NEVER type a literal (newest rows: HD-310, HD-311, registered 2026-09-01).
- **Coordination:** headplane/headscale = SEPARATE lane (D) — coordinate, never fold into other converges.

## 3. Next-session execution order (pointers — row bodies in todo.md)


### 3-N. NEXT (priority order)

0. **oldsrv Phase 3 — ⚠ BLOCKED (HD-318, carryover from #66).** oldsrv first Ansible contact reached the `office` role; mgmt-plane outage RESOLVED; docker/network/storage (nvme ZFS pool + NFS)/nut/cockpit/desktop live. ⚠ **BLOCKED (exact stop-points):** ① `office` role — ONLYOFFICE apt repo `squeeze` suite + GPG key mismatch (`E09C…DE8E5` missing) — needs repo/key fix; ② `amd_rocm` — ROCm noble-on-trixie dep conflict (distro `rocm-cmake`/`hipcc` vs ROCm pins) — needs pinning/removal decision; ③ `pihole_password`/`sonarr_api`/`radarr_api` 1P items still MISSING (owner-created) — `docker_services` big-service deploy gated on them. oldsrv becomes the DNS secondary (Technitium, HD-317) + NUT client + heavy services. · [todo.md HD-318](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 3

1. **HD-312 — 3-SSID WiFi + per-MAC control (phases 1+2a+3 DONE + LIVE; 2b/3b/4 next).** ⏳ Remaining: (2b) CAPsMAN per-MAC `access-list` vlan-id; (3b) `kids-*` lists active + kids-control off VLAN 40 (owner: define kids device set); (4) n8n firmware workflow + scoped router API user. · [todo.md HD-312](todo.md) · [network-vlans.md](docs/network-vlans.md) §3-SSID

2. **HD-317 — Technitium DNS-primary on VPS — VPS converge DONE + LIVE; seed role DONE; ⏳ remaining.** (1a) **DURABILITY + SEED ROLE DONE 2026-09-03 (`6457522`):** compose binds `/etc/dns` → `/opt/technitium/dns` (was tmpfs — wiped admin/zones on restart, the VPS REFUSING cause); `technitium_login`/`technitium_api` 1P items created; new `technitium-seed` role bootstraps admin + seeds kogler.si zone + split-horizon A records on every `docker_services` converge. **Pi instance SEEDED + verified live** (`dig @pi ha.kogler.si` → VIP; dashboards → `tailnet_sidecar_ip`). ✅ **RB4011 router step DONE + LIVE (SESSION #69):** DHCP dns-server chain + `/ip dns` upstream = **VPS→oldsrv→Pi** on all 7 networks (incl. Mgmt 99) — applied via the RouterOS API (live finding: rsc-import mangles multi-value `dns-server` silently drops the Pi; the role's api_modify is authoritative); RouterOS caps `dhcp-server network` at 3 dns-servers → aligned roles/router + rb4011_converge.rsc.j2 + new rb4011_dns_resolver_delta.rsc.j2 to the 3-chain. **✅ (1b) `dns.kogler.si` web UI FIXED + LIVE (SESSION #72):** was 404 — the `forward-dns` Authentik ProxyProvider was missing from `ks-forward-auth.yml` (route + forward-auth label existed since HD-62); added provider + app + **ORM-bound to the embedded outpost** (blueprint upsert does NOT merge the outpost `providers` m2m — lesson in [services-authentik.md](docs/services-authentik.md)); verified `https://dns.kogler.si/` → **302 → sso** → 200 login. **ALSO:** `dns` public CNAME added to the IaC SSOT (owner added it live; needs `playbooks/dns.yml` from home control plane) + **dns-pi 5380 publish (Pi tertiary)** in the technitium template ← fixes dns-pi web-UI 502 — **needs a Pi docker_services converge to land live**. **✅ VPS primary admin RECREATED + SEEDED + LIVE-VERIFIED (SESSION #74, HD-324):** the 502 fix exposed the default `admin`/`admin` bootstrap → documented `POST /api/user/changePassword` set the admin to the 1P `technitium_login` value (default creds now FAIL); the seed role (with the **fixed** `register: _tech_record`) seeded the zone + all records; `dig @159.195.111.66 stats.kogler.si` → `tailnet_sidecar_ip` + all others answer; **`https://stats.kogler.si` → SSO → Grafana now resolves.** + LAN resolv.conf ordering + WAN-out failover still to verify. · [todo.md HD-317](todo.md) + [todo.md HD-322](todo.md) · [network-dns.md](docs/network-dns.md) · [docs/services-dns.md](docs/services-dns.md)

3. **HD-313 deploy-gate (IaC DONE 2026-09-03).** RouterOS → VPS CrowdSec + Loki log shipping — seed 1P `mikrotik-logpipe_api` (ISH-seeded per #65) → next **router.yml converge** (system/logging action + topics + logpipe user) → **VPS monitoring + crowdsec** deploy → live-verify `cscli alerts list` + Loki search find RouterOS lines. · [todo.md HD-313](todo.md) · [network-ops.md](docs/network-ops.md) §Central log shipping

4. **Phase 1.5 cutover remainder (operator + AI).** switch reachability + CAPsMAN steady-state imported; router + switch roles re-converged LIVE via the Pi-99 hop (ansible-network-hop.sh); live switch/router bugs fixed in roles. ⏳ **Remaining:** APs re-join CAP; owner MikroTik password rotation; parked `validate-routeros7-syntax` linter stays parked. · [deployment-manual.md](deployment-manual.md) §1.5 · [todo.md HD-304](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 1.5

5. **HD-299 — Technitium primary → VPS** (new `dns-servers` overlay, 3-instance DNS HA, split-horizon static A records for plain `*.kogler.si` on tailnet). ⏳ IaC done (via HD-317) + seed role done (**VPS + Pi seeded live, HD-324; oldsrv pending Phase 3**); owner RB4011 forwarder step. · [todo.md HD-299](todo.md) · [docs/network-dns.md](docs/network-dns.md) line 18/67

6. **HD-296 + HD-297 — tailnet subdomains + traefik-tailnet clean URLs (dsh/pi).** (a) IaC CLOSED; (b) DEPLOY done — `dsh.ts`/`pi-dev.ts` 200; **(c) RESOLVED at the EDGE + LIVE (2026-09-03)** — plain `*.kogler.si` → 302 → sso. Root cause: template comment brace `{{ vault['…'] }}` crashed the render (`dict has no attribute '…'`, fix `fc2b0a1`) → middlewares.yml never landed; rendered + loaded since the 12:11 converge. **✅ OWNER-FACING plain `stats.kogler.si` RESOLVED (SESSION #74):** the VPS primary was seeded (admin recreated + seed role fixed, HD-324) → plain `stats.kogler.si` now resolves + SSO → Grafana. ⏳ owner browser verify. · [todo.md HD-296](todo.md) + [todo.md HD-297](todo.md) · owning docs

7. **HD-268c owner enable-flow (tailnet sidecars live).** Mint preauth keys → seed 1P `tailscale_dsh_api`/`tailscale_pi_dev_api` → flip `*_tailnet_sidecar_enabled` → scoped converge → verify UIs. 4-step flow in `group_vars/vps.yml`. · [todo.md HD-268](todo.md)

8. **HD-264 Renovate sandbox (owner, parked).** (a) commit versioned manifest + `renovate.json` to `domen/test`; (b) external re-launch timer; (c) verify PR lands; (d) flip `RENOVATE_REPOSITORIES` → the internal Forgejo homelab repo (NOT GitHub — superseded note). · [todo.md HD-264](todo.md) · [docs/deployment-renovate.md](docs/deployment-renovate.md)

9. **Audit-fold owner actions (from the merged audit):** HD-280 fail2ban (add traefik accesslog + filter, or drop http-auth jail) · HD-288 sunshine owner-decision + port binding (HD-289 deleted — in-template) · HD-292 VPS `docker image prune` · HD-286 vault-escape template comments. Row bodies carry owning docs. · [todo.md](todo.md) §§2.4/2.8

10. **Metabase gates (PARTIAL DONE):** HD-241 SMTP *Send test email* + invite flow; HD-242 connect both sources + import dashboards + SELECT-only proof. · [todo.md HD-241](todo.md) + [todo.md HD-242](todo.md)

11. **AI stack pre-deploy gates:** HD-105 secrets + OIDC providers (blocks HD-100/101/103/104); then HD-101 SSO verify ride-along, HD-247 (scoped keys — HD-102 PGVector superseded by HD-267/268 Qdrant), HD-248 OWUI split (depends HD-247), HD-103 docling, HD-104 openclaw onboard. · [todo.md HD-105](todo.md) · [docs/deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md)

12. **HD-112 Zipline go-live legs** (first deploy human-gated; post-up seeding runbook in compose header; family drop script). · [todo.md HD-112](todo.md)

13. **HD-252 pending cleanup (owner):** nodes list EMPTY — re-enroll laptop + phone via the now-working OIDC flow under `domen@kogler.si`. · [todo.md HD-252](todo.md)

> **Handoff diff-rule (CONVENTIONS §4):** this handoff is edited from #66's — every prior §2/§3 open item either appears or is explicitly resolved (closed items moved to the owning docs + git history only; frozen changelog/journal are archive). **Derived-values ban:** next-free HD quoted from todo.md, never typed. Next free HD: derive via `bash scripts/next-hd.sh` (HD-315+).
## 4. Working rules (binding)

Fresh worktree per session BEFORE any edit — mechanically enforced (`bash scripts/guard-session.sh`; `validate-all.sh` hard-fails primary+main+DIRTY); merge back only committed+green, ff-only; primary = merge station only. **9P gate: N/A on the WSL Debian primary (repo moved to ext4 — no /mnt/d drvfs staleness; the gate is legacy from the Windows-runner era).** Surgical `--tags docker_services -e docker_services_scope=<svc>`; secrets 1P-item.field only + `>-` for YAML; persisted Authentik tokens always `expiring=False` (HD-216); owning doc + todo update in same change; English; relative links; Authentik blueprint pin array attrs (HD-231); don't touch headplane/headscale unless asked. No multi-line bash heredocs with backslashes/backticks — write+run temp script files instead. (changelog/journal frozen → `reports/`, archive-only.)