> **Role:** entry point for the next session. This handoff is a **pointer index** — every item's owning doc/row holds the detail. Start with README.md §0 (intent routing), then §1 context, then §3 execution order. Read [todo.md](todo.md) for the full row bodies.

> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 · [scripts/guard-session.sh](scripts/guard-session.sh) · [todo.md](todo.md) · archive: [reports/changelog.md](reports/changelog.md), [reports/deployment-journal.md](reports/deployment-journal.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — §Tags & surgical runs (`docker_services_scope`, `base` tier) + §Deploy Timing Runbook.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — compose/secret conventions + fail-loud rule.
4. [docs/network-vpn.md](docs/network-vpn.md) — §Pattern A tailnet sidecars + current tailnet edge state + §MagicDNS (extra_records design).
5. Owning `docs/*.md` (per task) — the decision-logs + spec SSOT; read before re-deciding anything. Historical changelog/journal rows live in the frozen `reports/changelog.md` / `reports/deployment-journal.md` (archive-only).

## 1. Environment (WSL Debian primary, ext4) — as of #67, the repo runs from the WSL Debian primary on ext4 (not Windows)

Adapted to the WSL Debian primary (repo moved to ext4; the Windows-runner/9P-gate era is legacy — see "9P gate: N/A" in §4). `python3`/bash/LF/UTF-8 no-BOM. Secrets → 1Password item.field only, `>-` for YAML renders. **Self-learned (2026-08-26):** do NOT use multi-line bash heredocs with backslashes/backticks (mangled through `bash -c`) — write patch scripts to a temp file and run them. **Signed-commit gotcha (still valid):** keys in `~/.ssh/github_signing`/`github_auth`; `Couldn't find key in agent` → `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.

## 2. State snapshot (start of next session)
- **SESSION 2026-09-08 (Victoria* migration — orchestrator run, HD-341/342/344 AUTHORED + MERGED to main @ `4ba5ac9`; branch `session/victoria-migration-20260908-1510` + leg branches, 19 signed commits).** Owner decision (2026-09-08): replace **Prometheus + Loki** with a single **Victoria stack** — VictoriaMetrics (metrics, :8428, retention 365d) + VictoriaLogs (logs, :9428, 90d) on the VPS; keep **Grafana + Alloy + Dozzle** (Dozzle drop reverted); **MCP AI-debugging servers on oldsrv** (RAM). Orchestrated as 3 subagent legs + parent integration: **Leg 1 (HD-341 SSOT/docs)** — `victoria_*` vars (group_vars/all/main.yml: ports 8428/9428, retention 365/90, item names), 1P items **`victoria-metrics_api`/`victoria-logs_api`** (HYPHENATED per CONVENTIONS §1 — P1 reviewer fix from the underscore `victoria_metrics_api`; catalog + deployment-secrets rows), `versions.yml` pins (v1.151.0/v1.52.0), obs.md architecture + docs rows. **Leg 2 (HD-342 IaC, 8 commits)** — `victoria-metrics`/`victoria-logs` compose replace prometheus/loki on the VPS; **topology B: Alloy is the SINGLE scrape tier** (VPS host Alloy scrapes loopback-published traefik:8082/crowdsec:6060/n8n:5678/blackbox-exporter:9115/VM-self/VL-self via 127.0.0.1 publishes — Option-1 approval; oldsrv/Pi Alloy scrape home exporters); VM/VL pure storage/query (no `-promscrape.config`); Grafana datasources API-seeded (uid `prometheus`→VM, uid `loki`→VL, `victoriametrics-logs-datasource` plugin via `GF_INSTALL_PLUGINS`); self-monitoring rules/dashboards retargeted (`victoria-metrics-down`/`victoria-logs-down`); retention 365d/90d **Kopia-backed** (reverses the old "TSDB regenerable" doctrine — docs/backup.md + storage.md updated); prometheus/loki compose + pins removed; generated inventory re-rendered. **Leg 3 (HD-344 MCP, 4 commits)** — `mcp-victoriametrics`(:8080)/`mcp-victorialogs`(:8081) compose on oldsrv (pinned v1.18.0/v1.8.0), Basic-auth to the VPS backend via `victoria-metrics_api`/`victoria-logs_api` (`VM_INSTANCE_HEADERS`/`VL_INSTANCE_HEADERS` base64), endpoint var `victoria_backend_host` (**wg-s2s now / tailnet later** — owner's future s2s-wg→tailnet redo re-plumbs one var), Home-VLAN-only exposure, deploy-gated `enabled: false`, oldsrv registry rows, pi/Open WebUI/OpenClaw wiring documented (obs.md §MCP). **Parent:** merged all 3 legs + re-merged main twice (HD-315/105 + HD-318 kopia TLS) with clean seam resolution; **cross-cut repo-wide Prometheus/Loki scrub** (16 docs + IaC comments + HD-37/38 rows — historical records preserved); validate-all GREEN at every stage; final FF merge to main `4ba5ac9` + pushed. **HD-341/HD-37/HD-38 rows DELETED** (SSOT done / retention now default / Alertmanager moot — records in own docs). **⏳ Deploy-gated next (the migration is IaC-authored, NOTHING live yet):** seed `victoria-metrics_api`/`victoria-logs_api` via `provision-vault.sh --create` → converge VPS (`ansible-run.sh vps.yml --tags docker_services,monitoring`; **I own the VPS converge — coordination: other sessions must NOT converge VPS**) → verify datasources/dashboards/alerts → kopia client wiring for `/srv/docker/victoria-*/data` (tracked gap) → oldsrv MCP (HD-344, gated on Phase-3/HD-318) → register MCP in pi/OWUI/OpenClaw. **Fixes the HD-315 'Host Overview empty' root cause** (VPS Alloy `127.0.0.1:9090` vs WG-bound Prometheus) by topology B. · [todo.md §2.12](todo.md) · [observability.md](docs/observability.md) · [brainstorming/recent/Prometheus-Vs-victoriastack.md](brainstorming/recent/Prometheus-Vs-victoriastack.md)
- **SESSION 2026-09-08 (OLD-SESSION close-out: oldsrv/nas/pi converges + HD-318b/c + HD-343 + HD-08, branch `session/hd318-oldsrv-finish-20260908-1632` → merged to main `d476aba`, pushed; this close-out session = `session/hd318-closeout-20260908-1821`).** ① **oldsrv full converge GREEN (`failed=0`, 269 ok, 23 changed)** — all roles (network/storage/NUT/cockpit/**amd_rocm** Debian-native ROCm + GPU plumbing/desktop/**office** onlyoffice key/HA-standby/docker_services 17 svcs/**monitoring**). ② **monitoring-role bugs fixed + live (commit `f389f23`):** CrowdSec acquisition + Grafana provisioning tasks were gated `groups['monitoring']` (spans oldsrv collector + VPS) but run **only on VPS** → on oldsrv `/opt/crowdsec` missing + `docker restart grafana` “No such container” → re-gated `groups['vps']`, dead `/srv/docker/{crowdsec,grafana}` artifacts cleaned. ③ **HD-343 exporter LIVE (3 deploy bugs fixed):** wrong Debian pkg (`python3-routeros-api` doesn't exist on trixie; script imports PyPI `routeros_api`) → pinned `routeros-api==0.21.0` in `/opt/network-clients-exporter` venv; 0700-root broke the unprivileged unit → 0750 root:networkclients; Jinja `.lower` on bool rendered `false` → `NameError` → emit `True/False`. Unit active, `/metrics` serves real `mikrotik_client` union from the RB4011 logpipe user. ④ **HD-318c standby render LIVE:** fixed `/etc/ha-failover` dir prereq + `home_assistant_version` gap on oldsrv (pin added to `home_servers.yml`); `/opt/home-assistant-standby` = compose + BACKUP keepalived + failover variant + secrets + config; ha-failover-api active. ⑤ **nas `storage.yml` GREEN (`failed=0`, 79 ok):** HD-08 tail — `nut_exporter` v3.3.0 tagged LIVE (was `(devel)`), active + battery 100%. ⑥ **pi `raspberry_pi.yml` GREEN (`failed=0`, 104 ok)** — HA primary/technitium-secondary/traefik-ha/keepalived all Up. ⑦ **⚠ Knowns for next session:** kopia-agent `Restarting` = **VPS-gated (HD-318a)** — 1P `kopia-server_fingerprint` unseedable until the VPS kopia-server leg converges (post-Victoria); signal-cli = **manual phone registration** (empty identity volume); **Victoria merged to main** (`4ba5ac9`/HD-341/342/344) — the monitoring role's alloy now looks up `victoria-metrics_api` (not seeded) → **do NOT re-converge oldsrv monitoring or full-scope docker_services** until the VPS Victoria leg seeds it + converges (re-converging full-scope also aborts on the missing `kopia-server_fingerprint`); nas Mgmt-99 `eno1.99` leg = pre-existing ifupdown-vs-netd mismatch (convenience only). · [todo.md HD-318/HD-343/HD-08](todo.md) · [hardware-oldsrv.md](docs/hardware-oldsrv.md) · [observability.md](docs/observability.md) §Network Clients Dashboard · [hardware-ups.md](docs/hardware-ups.md)
- **SESSION 2026-09-08 (backlog sweep + worktree/branch cleanup, branch `session/backlog-sweep-20260908`).** Housekeeping per CONVENTIONS §4/§6: **① Legacy-worktree cleanup** — removed the 4 open session worktrees (`homelab-wt-20260908-1510/-1518-leg2/-1559-leg3/-1824`) and deleted their fully-merged `session/victoria-*` + `homelab-wt-*` branches (19); preserved the orphaned `homelab-wt-hd297-20260903-1700` record (VPS Technitium-never-seeded / HD-317-HD-297c) as commit `2c36b68` (cherry-picked onto main, backup at `refs/backstop/*`); main is now the sole local branch + only worktree, pushed to `origin/main`. ② **Backlog sweep** — **closed rows removed from `todo.md`:** HD-198 (public-record SSOT — its incremental-publication method now codified in `docs/network-dns.md` §Public so no knowledge lost), plus the closed Pkg C row tail + Pkg D note (records in `network-dns.md`); the fully-done rows HD-317/HD-297c/HD-326 had already been deleted by prior close-outs (grep-clean). **Remaining open state = genuinely deploy-gated/backlog rows** (HD-318 kopia-agent VPS-gated, HD-311 oldsrv tagged-99 apply, HD-03 residual audit, HD-302 `modem` record, HD-342 Victoria VPS converge, HD-344 MCP oldsrv — all still carry a real ⏳ tail; **nothing was closed that isn't done**). · [todo.md](todo.md) · [docs/network-dns.md](docs/network-dns.md) · [CONVENTIONS.md](CONVENTIONS.md) §4/§6
