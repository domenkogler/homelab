> **Role:** entry point for the next session. This handoff is a **pointer index** — every item's owning doc/row holds the detail. Start with README.md §0 (intent routing), then §1 context, then §3 execution order. Read [todo.md](todo.md) for the full row bodies.

> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 · [scripts/guard-session.sh](scripts/guard-session.sh) · [todo.md](todo.md) · archive: [reports/changelog.md](reports/changelog.md), [reports/deployment-journal.md](reports/deployment-journal.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — §Tags & surgical runs (`docker_services_scope`, `base` tier) + §Deploy Timing Runbook.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — compose/secret conventions + fail-loud rule.
4. [docs/network-vpn.md](docs/network-vpn.md) — §Pattern A tailnet sidecars + current tailnet edge state + §MagicDNS (extra_records design).
5. Owning `docs/*.md` (per task) — the decision-logs + spec SSOT; read before re-deciding anything. Historical changelog/journal rows live in the frozen `reports/changelog.md` / `reports/deployment-journal.md` (archive-only).

## 1. Environment (WSL Debian primary, ext4) — as of #67, the repo runs from the WSL Debian primary on ext4 (not Windows)

Adapted to the WSL Debian primary (repo moved to ext4; the Windows-runner/9P-gate era is legacy — see "9P gate: N/A" in §4). `python3`/bash/LF/UTF-8 no-BOM. Secrets → 1Password item.field only, `>-` for YAML renders. **Self-learned (2026-08-26):** do NOT use multi-line bash heredocs with backslashes/backticks (mangled through `bash -c`) — write patch scripts to a temp file and run them. **Signed-commit gotcha (still valid):** keys in `~/.ssh/github_signing`/`github_auth`; `Couldn't find key in agent` → `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.

## 2. Current open work (handoff state — next session starts here)

> Pointer index only: each open HD links its owning-doc status block (the SSOT) and its
> [todo.md](todo.md) row (the registry — a row is deleted when fully done, §4(a)).
> For "what to do next" also see [todo-table.md](todo-table.md) (planning view: **Table AI** = AI-runnable, with a deploy-gated chapter for rows waiting on a human prerequisite; **Table Human** = needs an owner step).
>
> **2026-09-08 Mgmt-99 direct cleanup:** the laptop/WSL now reaches the Mgmt VLAN **directly** via the Windows
> Mgmt99 vNIC (`wsl-nat-resolv.ps1 -EnableMgmt99`) — SSH aliases `router`/`switch`/`oldsrv`/`pi99` are **direct
> (no ProxyJump `pi`)**, the hop aliases (`pi99`-via-`pi`, `router99`, `oldsrv99`, `nas99`) were removed + deduped,
> HD-33/26/303/311 closed. `ansible-network-hop.sh` is kept but **obsolete** (scripts/README). `pi`/`pi99` are the
> one dual-leg exception.

| HD | Open item (one line) | Detail SSOT |
|----|----------------------|-------------|
| HD | Open item (one line) | Detail SSOT |
|----|----------------------|-------------|
| HD-342 | Victoria stack deploy (VPS) LIVE; remaining tails: kopia client wiring for Victoria data, oldsrv MCP (HD-344) | [observability.md](docs/observability.md) · [todo.md HD-342](todo.md) |
| HD-344 | MCP AI-debugging servers on oldsrv (register in pi/OWUI/OpenClaw + tailnet redo) | [observability.md](docs/observability.md) §MCP · [todo.md HD-344](todo.md) |
| HD-318 | oldsrv Phase-3 provision — **kopia-agent DONE + LIVE 2026-09-08 (HD-318a):** agent Up over HTTPS+fingerprint, snapshots flowing; remaining = recyclarr @daily verify | [hardware-oldsrv.md](docs/hardware-oldsrv.md) · [todo.md HD-318](todo.md) |
| HD-343 | Network Clients dashboard — owner verify (wifi path, panels, stats.kogler.si) | [observability.md](docs/observability.md) §Network Clients Dashboard · [todo.md HD-343](todo.md) |
| HD-315 | Grafana dashboard render-verify with data (**data now flowing** — all 4 hosts + probes; owner visual verify) | [observability.md](docs/observability.md) §Dashboards · [todo.md HD-315](todo.md) |
| HD-345 | `ifOperStatus` SNMP series still 0 in VM (rules live, noDataState OK; SNMP now enabled — walk not yet arriving) | [observability.md](docs/observability.md) · [todo.md HD-345](todo.md) |
| HD-350 | **Home-LAN internal all-app edge (`traefik-internal`) on oldsrv — IaC IMPLEMENTED 2026-09-10 (edge lock).** Host-net, VIP+LAN-IP-bound :443; serves home-hosted set (13) + VPS-hosted via wg:4443; no middleware (own logins, *arr Forms-auth); mobile bridge = VPS traefik-tailnet edge → WG → `oldsrv_home_ip` (dsh/pi-dev). ⏳ **deploy-gated: converge oldsrv + VPS pubkey authorization** | [services-traefik.md](docs/services-traefik.md) §Edge model · [todo.md HD-350](todo.md) |
| HD-352 | DNS re-point (IMPLEMENTED 2026-09-10): home Technitium instances (oldsrv secondary + Pi tertiary) resolve **home-hosted app names → oldsrv LAN IP**; `ha` → home VIP; VPS primary + tailnet DNS unchanged. ⏳ deploy-gated: seed converges on oldsrv + Pi | [network-dns.md](docs/network-dns.md) · [todo.md HD-352](todo.md) |
| HD-353 | **SeerrNG IaC-IMPLEMENTED 2026-09-10** (`seerrng` template + pin v3.20.0, oldsrv entry). ⏳ deploy-gated: converge oldsrv + own Jellyfin login verify | [services-media.md](docs/services-media.md) · [todo.md HD-353](todo.md) |
| HD-354 | **Navidrome VPS+Box IaC-IMPLEMENTED 2026-09-10** (`navidrome` template + pin 0.63.2, VPS entry, music on Storage Box). ⏳ deploy-gated: converge VPS + Box music dir + Subsonic/ExtAuth verify | [services-media.md](docs/services-media.md) · [todo.md HD-354](todo.md) |

| HD-356 | **LiteLLM split IaC-IMPLEMENTED deploy-gated on spark** (`lan-litellm` authored, `enabled: false`). ⏳ spark: flip + re-point OPENAI_BASE_URL | [services-ai.md](docs/services-ai.md) · [todo.md HD-356](todo.md) |
| HD-359 | **spark (DGX GB10) FULLY PROVISIONED + LIVE 2026-09-14 (HD-363 + bootstrap + converge):** DGX OS wizard done (admin → 1P `spark_login`, analytics off · auto-suspend masked), first-contact bootstrap (key-only `ansible-admin` + sshd hardening), NVMe p2 shrunk → **p3 = 503.4G XFS at `/mnt/spark_nvme`** (fstab-durable, dirs `kv_cache/models/overlays/ples_int4/triton_cache/vllm_cache`), `spark.yml` converge **failed=0** (ok=53: common/network/docker/spark/monitoring; docker role now Ubuntu-noble-aware, XFS mount-opts fixed). New: **artifact store role + STAGED LIVE 2026-09-14** (`roles/spark-artifacts/` — general config-agnostic manifest weights+PLE overlays+INT4 tables on XFS; downloaded on the box: weights 169G + overlays 3×.py + tables 30G/129 files, 296G free; 5 live-run defects fixed: PEP-668, `hf` CLI swap, XFS child-dir perms + HF_HOME, tables-flatten, compose overlay paths). Spark = first-class IaC host. ⏳ **deploy-gated remaining:** bench plan S1–S5 on the live box → flip `spark-ai.enabled: true` → vLLM + llm-d router live → promote winner | [hardware-spark.md](docs/hardware-spark.md) §Benchmark §Bring-up · [BENCHMARK-PLAN.md](spark/BENCHMARK-PLAN.md) · [todo.md HD-359](todo.md) |
| HD-367 | **spark mgmt-leg (single-NIC VLAN-99) IaC DONE + LIVE 2026-09-14 + llm-d REMOVED; S1 bench BLOCKED on torchinductor cache (crash-loop)** — mgmt leg rides the SAME enP7s7 (Home untagged + Mgmt 99 tagged, same MAC); llm-d was never public (K8s-only project) → LiteLLM is the router; `spark-ai.enabled` stays false. ⏳ next: fix `TORCHINDUCTOR_CACHE_DIR` read-only → converge → PLE load → S1 c1/c3/c5 bench → commit results → flip | [hardware-spark.md](docs/hardware-spark.md) §Benchmark · [todo.md HD-367](todo.md) |
| HD-366 | **DGX Dashboard JupyterLab on the LAN (:11002) — IaC IMPLEMENTED 2026-09-20** — the dashboard's integrated JupyterLab (per-user ports in `jupyterlab_ports.yaml`, admin = :11002) exposed via a second socat relay in the same `dgx-dashboard` sidecar → `spark_home_ip:11002` → `127.0.0.1:11002`; reachable at `http://spark.kogler.si:11002` once a lab is Running (spawned on demand from the dashboard JupyterLab panel; healthcheck stays on :11000). ⏳ **deploy-gated:** converge spark → start a lab → `curl http://spark.kogler.si:11002` answers | [hardware-spark.md](docs/hardware-spark.md) §Remote management · [todo.md HD-366](todo.md) |
| HD-357 | **Homepage tiles fix (bug #6):** no working Jellyfin/Seerr tiles; Immich stuck in “Soon” though live — wire layout/widgets to verified endpoints now that the route tables are final | [services.md](docs/services.md) accessibility SSOT · [todo.md HD-357](todo.md) |
| HD-358 | **Seerr → Sonarr/Radarr link (bug #7):** undocumented post-deploy step — record API-key + URL wiring as an owner runbook step once home edge is live | [services-media.md](docs/services-media.md) · [todo.md HD-358](todo.md) |
| HD-362 | **Music pillar IaC-IMPLEMENTED + CORRECTED (2026-09-14):** acquisition ladder 1) usenet (SABnzbd) 2) Soulseek (slskd, gluetun sidecar) 3) YouTube (lidarr-ydl, Angrido) + Aurral discovery + Tube Archivist (3-container) on oldsrv; all P2P via PrivadoVPN, 10 MB/s cap; orpheusdl/Murglar = manual-only. ⏳ **deploy-gated:** owner seeds 1P (`lidarr_api`, `soulseek_api`, `tube_archivist_login`) → converge oldsrv → wire Lidarr clients → Navidrome Box refresh | [services-media.md](docs/services-media.md) §Music Pillar · [deployment-secrets.md](docs/deployment-secrets.md) · [deployment-manual.md](deployment-manual.md) §Phase 3 · [todo.md HD-362](todo.md) |

| HD-352 | DNS re-point (re-scoped 2026-09-10): home Technitium instances (oldsrv secondary + Pi tertiary) resolve **home-hosted app names → oldsrv LAN IP**; `ha` → home VIP; VPS primary + tailnet DNS unchanged | [network-dns.md](docs/network-dns.md) · [todo.md HD-352](todo.md) |
| HD-08 | UPS battery-pull test + NUT guaranteed-notify/shutdown — **drill EXECUTED 2026-09-09 + full fix set MERGED + deployed (nas/oldsrv/pi)**. Fixes: stray ACL on `upssched-cmd`, SMTP creds rendered inline (also rotated 2026-09-09), nut→`poweroff` sudoers, charge-threshold policy (oldsrv ≤75%, nas/pi critical-only), persistent `nut-wake.timer` WoL wake-at-≥80% (BIOS verified on oldsrv Z270 Extreme4). **Open: re-test short pull (owner) to confirm poweroff + WoL wake end-to-end.** | [hardware-ups.md](docs/hardware-ups.md) · [todo.md HD-06/HD-08](todo.md) |
| HD-360 | **Enable Samba Authentik-as-LDAP (VPS-side) — split from HD-132 (2026-09-14).** LDAP provider/outpost/svc_samba never created live; `authentik-ldap_bind` token expired 2026-09-01 (outpost unhealthy) → Samba runs `storage_samba_passdb=tdbsam` now. To enable: add LDAP provider + svc_samba to the `ks-oidc.yml` Blueprint, create outpost, mint fresh bind token (1P), redeploy `authentik-ldap`, flip `storage_samba_passdb: ldapsam` on nas, live-verify a family drive. Do NOT flip before steps 1–3 are live (smbd fails hard if outpost unreachable). | [deployment-compose.md](docs/deployment-compose.md) §Samba↔Authentik-as-LDAP · [todo.md HD-360](todo.md) |

**Recent sessions (history — see owning docs/commits):** 2026-09-14 **spark mgmt-leg + llm-d removal + S1 bench attempt session (`session/spark-s1-20260914` — WORKTREE `../homelab-wt-20260914-2114`, NOT merged; branch kept for the deploy-gated flip):** ✅ **mgmt-leg IaC DONE + LIVE** — the single enP7s7 port carries Home untagged + **Mgmt VLAN-99 tagged** (same NIC, same MAC in both SSOT rows; the old "mgmt NIC not cabled" assumption is obsolete); `roles/network` NM dual-home generalized (`network_nm_dualhome_hosts` = pi+spark, `nm_parent_iface`/per-host keyfiles+ids, **pi byte-identical verified**); spark parent = DHCP (Home IP via the router static-lease, `dhcp-client-id=mac`), tagged `spark-mgmt` = mgmt IP/24 + route1 + never-default; added `nmcli connection reload` + explicit `load` before `up` (fresh-keyfile live hit `unknown connection`); converge failed=0 → **mgmt leg UP, ping to the router mgmt IP 0.5ms OK**, default route stays Home. Router-side `dhcp-mgmt` spark static renders in the converge — standard `render-converge.yml`→/import apply (owner/operator, not yet applied). ✅ **llm-d REMOVED (owner):** the real llm-d is a Kubernetes inference-pool stack (endpoint-picker/disagg-sidecar route Envoy+K8s pools); the `ghcr.io/llm-d/router:latest` ref was **never public** (GHCR 401; LLM-d README = K8s/Helm only). LiteLLM (existing) fronts vLLM :8000. `spark-ai` compose = vLLM engine only (`llm-backend`), `spark-internal` net + `spark_llm_router_*` vars removed, `spark-ai` dropped from validator ALLOWED_LATEST. ⏳ **S1 bench attempted + 2 live engine fixes (BLOCKER):** (a) numactl `--membind=0` → `get_mempolicy: Operation not permitted` under the hardened container → entrypoint now plain `python3 -m vllm...`; (b) **`OSError: [Errno 30] Read-only file system: '/root/.cache/torchinductor'`** — vLLM's torchinductor compile cache points at `/root/.cache/torchinductor` but the rootfs is read-only (read_only:true; compose only mounts `vllm_cache → /root/.cache/vllm`). Engine crash-loops (exit 1). **Branch NOT merged — flip stayed `enabled: false`** (bench never certified). ⏳ **Next per deploy gate (HD-367):** fix the torchinductor cache path (mount `vllm_cache`→/root/.cache entirely or a torchinductor bind+tmpfs) → converge → PLE load (~20 min) → health :8000 → run S1 c1/c3/c5 bench + accuracy gate → commit results → flip `spark-ai.enabled: true`. Earlier: 2026-09-20 **spark DGX Dashboard JupyterLab LAN exposure session (`dc24ac8` + `665e021` on main, branches `session/jupyter-11002` + `session/rendersync`):** JupyterLab is an INTEGRAL part of the DGX Dashboard (`/opt/nvidia/dgx-dashboard-service/jupyterlab_ports.yaml` per-user ports: nobody 11001 / admin 11002 / ansible-admin 11003); NVIDIA's remote path = a second SSH tunnel per port. Extended the `dgx-dashboard` alpine/socat host-net sidecar (HD-364) with a **second relay** `spark_home_ip:11002 → 127.0.0.1:11002` (admin's port), same digest pin, one container — healthcheck intentionally stays on :11000 (a stopped lab would otherwise mark the bridge unhealthy). JupyterLab spawns **on demand** from the dashboard's JupyterLab panel (Start); docs absorbed: hardware-spark.md (banner + §Remote management bullet HD-366), network-dns.md spark row, spark.yml registry comment, validator HOST_NET_CONTAINERS comment, todo.md HD-366 row. Also committed a `validate-all.sh`-surfaced **stale-generated-doc re-render** (`665e021`) — HD-362 music-pillar rows (aurral/slskd/lidarr-ydl/tube-archivist) had landed in `home_servers.yml` (0619609/142de58) without re-rendering `*-generated.md`; pure SSOT re-render, no hand edits. All commits signed, validate-all green, merged to main. ⏳ Next per deploy gate: **converge spark (`--tags dgx-dashboard`) → Start a lab in the dashboard → verify `http://spark.kogler.si:11002` from a LAN client** (HD-366). Earlier: 2026-09-14 **spark artifacts live-download + fixes session (commits `67d4479`/`e8dd814`/`d8b24ce` on main, branches `session/spark-ai-artifacts-20260914-1902` + `session/spark-artifacts-fix-20260914-1912`):** implemented the **artifact-vs-engine split** — new **general config-agnostic `roles/spark-artifacts/`** (manifest `spark_artifacts:` maps HF repo → XFS dir: `wtdcode/Qwen3.8-Flash-Next-AWQ-W4A16` → `models/`, `primitive-ai/Qwen3.8-Flash-Next-PLE-quant` overlays → `overlays/` + INT4 tables → `ples_int4/`; one download per quant-artifact, shared by every engine profile; idempotent; `hf` CLI; runnable via `--tags spark-artifacts`) and **ran the ~199 GB download LIVE on spark** — **STAGED: weights 169G, overlays 3×.py, tables 30G/129 files (296G free)**; the run exposed + fixed **5 real defects**: (1) PEP-668 `--break-system-packages` on Ubuntu 24.04, (2) `huggingface-cli` deprecated → `hf` CLI (abs `~/.local/bin/hf`, not on non-login PATH), (3) `hf` writes `.cache` under `--local-dir` → downloads target writable child dirs (never root-owned mount root) + `HF_HOME` 0777 XFS child, (4) tables `--include` nesting → idempotent flatten task + guard on `ples_int4/META.json`, (5) compose PLE overlay mounts → `overlays/`. LiteLLM stays on oldsrv (HD-356), WAN-independent inference once spark serves; **all commits signed, validate-all green, merged to main** (main moved mid-session via HD-364/365 sessions; recovered dangling commits via `git fsck` + merged cleanly). ⏳ Next per deploy gate: **bench S1–S5 on the live box → flip `spark-ai.enabled: true`**. Earlier: 2026-09-14 **spark bootstrap + first converge session (`354efb8`/`dbced68`/`41de566`/close-out on main, branch `session/hd363-*` + `session/closeout-20260914`):** delivered + ran the first-contact bootstrap on spark via LAN HTTP (bootstrap-spark.sh — `ansible-admin` key-only + sshd hardening + auto-suspend mask; **not committed**, keys from 1P `ansible-admin_ssh`/`laptop-domen_ssh`); operator shrank NVMe p2 to 450G (DGX OS had put root on what the role assumed was the XFS data partition → **HD-363: p3 carve**) and carved p3 manually via `ssh spark` (`parted mkpart 944769024s 100%` + `mkfs.xfs -d agcount=16` + mount); fixed **two live bugs**: docker role was Debian-only (spark is Ubuntu 24.04 noble — repo `/linux/debian` + suite noble 404 → distro-aware `ubuntu`/`debian` family), XFS mount options carried ext4-isms (`logbufs/logbsize/nobarrier` — kernel rejects `nobarrier` → mount failed; now `noatime,nodiratime`); `spark.yml` converge **failed=0** (ok=53) — `/mnt/spark_nvme` XFS 504G fstab-durable + dirs, docker 29.2.1, Alloy active. Spark = first-class IaC host (HD-359 → DEPLOY-GATED on bench S1–S5 / `spark-ai.enabled`). Handoff diff: HD-359 row reflects provisioned+live. Earlier: 2026-09-14 **music pillar session (`0619609` + `142de58` on main, branches `session/music-pillar-20260914` + `session/music-fixes-20260914`):** from the **Media stack redefined** brainstorm — implemented + CORRECTED the music acquisition/discovery tier on oldsrv: **Agreed ladder (owner):** 1) usenet (SABnzbd) 2) Soulseek (slskd on a gluetun WG sidecar — fetch-only, 10 MB/s router cap) 3) YouTube (Lidarr-YouTube-Downloader/Angrido as Newznab+SABnzbd-emulation client + bgutil PO sidecar); **Aurral** (discovery, Last.fm/ListenBrainz) + **Tube Archivist** (3-container: app + bbilly1/tubearchivist-es 8.19.0 + redis 7-alpine; `vm.max_map_count` sysctl in the role; `bulk/media/tube` subdir) all registry-verified + pinned; **orpheusdl + Murglar = manual laptop/phone tools only** (orpheusdl has NO container image — dropped from IaC). Intermediate commit `0619609` shipped wrong registries/shapes (orpheusdl image 404, TA single-container, aurral wrong repo) → corrected in `142de58` (real GHCR/DockerHub tags, real stack shapes, corrected vault derive-maps so fail-closed render stays green). All P2P egress via PrivadoVPN; SABnzbd plain LAN. **Deploy-gated next:** owner seeds 1P (`lidarr_api`, `soulseek_api`, `tube_archivist_login`) → converge oldsrv → wire Lidarr download clients → Navidrome Box refresh (HD-362 ⏳). Earlier: 2026-09-14 **spark first-boot session (`709d399` on main, branch `session/spark-firstboot-20260914-1448`):** owner ran the DGX OS first-boot wizard on the GB10 (EN, Europe/Ljubljana, admin → 1P `spark_login`, analytics off, updates + reboot); node went L2-silent after the update reboot (headless auto-suspend; power-cycle recovered) → runbook `deployment-manual.md` §Phase 5 written incl. the MANDATORY `systemctl mask sleep/suspend/hibernate/hybrid-sleep` step; MAC `38:A7:46:78:13:97` learned from the live lease → `network_static_hosts` spark VLAN-10 row + re-render; hardware-spark.md status → first boot DONE; todo HD-359 updated. Earlier: 2026-09-14 **`/bulk/robi` samba-share removal + Samba LDAP gating (live NAS revert, `9e36cff` + `session/samba-revert` on main):** reverted an out-of-band quick-fix — destroyed ZFS `bulk/robi`, deleted POSIX user+group `robi` (996/985) + Samba account, dropped the `[robi]` share + stale secret-bearing `smb.conf.bak-ldap`. Repaired live before revert verified: real IaC defects — `vfs objects = zfs_core` (TrueNAS-only, NOT in Debian samba-vfs-modules; broke EVERY tree connect) → `acl_xattr`; `passdb` behind `storage_samba_passdb` switch (default `tdbsam` — `ldapsam` is HD-360/deploy-gated: live `authentik-ldap` unhealthy since 2026-09-01, token expired, NO LDAP provider/outpost/svc_samba ever created; smbd fails HARD on startup if ldapsam is enabled while the outpost is dead). NAS re-converged tdbsam+acl_xattr (`smbd` active, media share only). Follow-up session (`session/hd360-ldap-samba`): split HD-360 (VPS-side LDAP enable) from HD-132, updated todo.md/todo-table.md/docs/deployment-compose.md + docs/deployment-oidc.md, added HD-360 to this handoff. Earlier: 2026-09-14 **spark (GB10) research→IaC→deploy-kit session (6 commits `806ac1b`..`4f85d5f` on main, session branches `session/spark-*`):** added the 19 GB10 research artifacts + verdicts + R6 digest (deleted the `summary.md` handoff), rewrote `spark/BENCHMARK-PLAN.md` engine-neutral (vLLM↔SGLang, S1–S5, TRT-LLM future row, post-bench promotion), pinned the vLLM image digest, added `spark-bootstrap` role + playbook (Part A), then **folded spark into the IaC tree** (`inventory` spark group, `host_vars/spark.kogler.si.yml` SSOT statics, `roles/spark` = 1TB NVMe p2→XFS + headroom assert + models/overlays on XFS, `group_vars/spark.yml` registry+vars, `playbooks/spark.yml`, `spark-internal` net) and authored the **dockerized proven Qwen3.8-Flash-Next** (`templates/docker_services/spark-ai/` = B1 AWQ W4A16 + PLE INT4 + MTP3 + llm-d router; validator extended to render `group_vars/spark.yml`). Everything pushed green; only pending = the deploy-gated box bring-up (HD-359 ⏳). Earlier: 2026-09-10 **edge+media+AI redo IaC session (`440b542` on main, session branch `session/edge-redo-20260910-2154`):** implemented the 2026-09-10 architecture redo as IaC across 3 parallel lanes (own worktrees, parent-merged): (1) **edge lock** — `traefik-internal` home-LAN edge (host-net VIP+LAN IP :80/:443, file-provider FULL route table 13 home + VPS-hosted via wg:4443, no Fwd-Auth/CrowdSec, *arr Forms-auth, cert-pull consumer, `ip_nonlocal_bind`); DNS split-horizon per-instance (`technitium-seed.yml`); HD-350/352 implemented (HD-351 deleted as no-op). (2) **media** — `seerrng` + `navidrome` templates/pins, docs (HD-353/354). (3) **AI split** — dsh/pi-dev moved to oldsrv (tailscale sidecars dropped, `oldsrv_home_ip` publish), LAN LiteLLM (HD-355/356); VPS traefik-tailnet backends re-pointed over WG (mobile path = VPS edge → WG → oldsrv_home_ip). Added HD-357 (homepage tiles) + HD-358 (seerr link). Gate: validate-all green on main; pushed. **Deploy-gated next (all rows carry the ⏳):** converge oldsrv + VPS (new services + certs) then the spark-gated LiteLLM flip. Mid-session infra note: the worker subagent provider hit a $10/month billing limit (opencode.ai) — resolution was CHANGING THE PROVIDER; `resume` keeps the child's stored model contract, so blocked children must be relaunched fresh (labeled same-role fallback) once the provider is switched.

**Recent sessions (history continued — see owning docs/commits):** 2026-09-09 **NUT battery-pull drill + fix session (`session/hd06-nut-fix-20260909-1735`, merged `033044e` on main):** live drill exposed 3 latent defects in the guaranteed-notify/shutdown layer (upssched-cmd stray ACL → exec 126; SMTP creds never rendered as shell-vars; no sudoers for nut → polkit-fail shutdown). Fixed + deployed nas/oldsrv/pi: ACL clear+chmod, inline SMTP creds (rotation handled by the sibling secrets session `ae441f8`), `/etc/sudoers.d/nut` NOPASSWD poweroff+shutdown, **all shutdown paths → `/sbin/poweroff`** (halt left the board powered — needed a long-press), `restart upsmon (all modes)` handler (client config reloads were being skipped), charge-threshold policy → **oldsrv ≤75% charge** (owner decision), and **`nut-wake.timer`** (persistent systemd, every 60 s) → WoL at ≥80% recharge on mains-restore. **Merge note:** `upssched-cmd.j2` SMTP rendering was also fixed by the sibling session on main; auto-merged cleanly.** oldsrv BIOS verified 2026-09-09 (Z270 Extreme4: PCIE Devices Power On / I219 LAN Power On / RTC By OS = Enabled, Deep Sleep = Disabled). **Open: re-test (short pull) to confirm poweroff + WoL wake; oldsrv had to be manually long-pressed once because the first wake attempt never fired (transient upssched helper died) — now daemonized.**

2026-09-09 **failover drill + edge-model re-decision session (`a3a11c0` on main + `275c183` on `session/edge-model-20260909`):** live forward-takeover drill executed on oldsrv — fixed the standby HA 8123 publish (standby compose only exposed 5683/udp → added `8123:8123`, `a3a11c0`, standby HA then served at VIP:8123), proved VIP move + standby boot + Shelly control; **discovered two real gaps**: (1) `ha.kogler.si` HTTPS had NO edge on Pi-down (doc said "oldsrv traefik" but HD-331 removed it — stale), (2) GIRA KNX router drops all KNXnet/IP from oldsrv (device-side, unresolved). **Owner re-decided the edge model (HD-349)** — home-LAN internal all-app edge `traefik-internal` on oldsrv + DNS re-point (HD-350/352 implemented 2026-09-10; the failover-wiring sub-item was deleted as no-op). **Reconnect outcome (2026-09-09):** Pi back up, took MASTER automatically (oldsrv was OFF for the UPS battery test), VIP on Pi, `ha.kogler.si` HTTPS 200 — failback complete without a manual trigger. Session branch `session/edge-model-20260909` + worktree `homelab-wt-edge-20260909-1813` hold the docs commit (merged at close-out). **At close-out: oldsrv still OFF (battery test, 90% WoL wake pending) — verify it boots cold (containers `restart: no` → nothing auto-starts) and restore its standby keepalived conf before the next cold boot.**

**HD-318a/kopia + platform/secrets session (2026-09-08, `94f6231` + `98d4720`):** kopia-agent auth fixed end-to-end (htpasswd user@host entries + repo-user provisioned with the repo master password + compose `$$`-escaping) — agent **LIVE + snapshotting**; HD-134 matrix pinned `v1.9.0`; HD-59 `prometheus-internal_api` retired (Victoria); HD-286 vault-escape comments; HD-218 re-sample (zero residue, orphaned loki/prom/renovate removed); HD-211 Authentik expiring=False audit passed. todo.md/todo-table.md updated at close-out.

**Storage/UPS session (2026-09-08, `20c2b1d`/`5e7043f`):** NUT — `gen_pw` excluded `#` (NUT comment char broke every upsmon MONITOR line in 2.8.1); `nut_password`/`nut-exporter_password` rotated #-free; POWERDOWNWAIT dropped; nas/oldsrv/pi `nut-monitor` all active again (`upsc powerwalker@nas.kogler.si` → OL/100%). Kopia — oldsrv agent re-rendered with per-client htpasswd identity + first snapshots landed (opt 1.3GB/5648f, dumps, immich-upload, signal-cli-data); HD-191 first-snapshot phase done (⏳ restore drill + volume-name pin remains open), HD-220 kopia-seed phase resolved (⏳ renovate token check remains owner). Battery-pull test ready for owner (HD-08/HD-06).

**Backlog/session-discipline close-out (2026-09-08, `2dce8e0`/`44e5ccc`/`37d44ad`/`2e18f10`):** doc-only session — no live/manual changes. Re-synced README §2 + todo.md + todo-table.md to the oldsrv **Phase-3** reality (HD-318 blockers cleared; the Victoria cutover replaced Prometheus/Loki on the VPS; kopia tail closed). Restructured todo-table.md into **Table AI** (AI-runnable + a `⏳ Deploy-gated chapter` for rows whose deploy is AI but wait on a Table-Human prerequisite — HD-46/122, HD-230, HD-57) and **Table Human** (owner steps, incl. explicit HD-230/57 owner-parts); removed the now-redundant "blocked/owner" split. HD-211 = done (owner-only on-exposure rotation); HD-160/287 gate conditions met → rows are Table-AI candidates (HD-160's openclaw tail stays open under HD-104). `prompt.md`/README/scripts-README stale Table A/B/C references swept. **Process lesson:** the session's earlier direct-on-main commits predate the HD-253 worktree gate — the edited state is committed + validated, no live impact; all future sessions run on a `session/<slug>` branch in a session worktree (see CONVENTIONS §74(7)/§116).
