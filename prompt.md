# prompt.md — Deployment Execution Handoff #62 — **HD-315/316/317 LANDED (Grafana technical dashboards + Homepage family launchpad + Technitium DNS-primary on the VPS) — all IaC, deploy-gated; next free HD: derive via `scripts/next-hd.sh` (HD-318+)**

> **SESSION #62 (2026-09-03, orchestrator — 3 parallel lanes merged → main `3eba9d0`):** ① **HD-315 Grafana technical dashboards** (`session/grafana-dashboards` → `d877a0a`): 5 provisioned dashboards (`homelab-overview` / `host-overview` / `service-reachability` / `wan-tunnel` / `selfmonitoring`) on the API-seeded `prometheus` datasource; review-fixed (dropped remote-write `alloy` from `up{}`, scoped node panels by instance, documented the traefik `:8082` empty-series + permanent-red consequence). ② **HD-316 Homepage family launchpad** (`session/homepage-family` → `7b544bc`): `homepage_services.yaml.j2`/`widgets.yaml.j2` full layout — **family groups** (Photos/Files/Docs/Chat/Git/AI/Tools/Accounts, siteMonitor on public FQDNs only) + **Domen · technical** group (Grafana/stats, Dozzle/logs, Traefik, CrowdSec, Metabase, n8n, VPN, dsh, pi-dev; gated `homepage_show_technical` default true) + **Soon** roadmap (deploy-gated, `homepage_show_soon` default true); **Grafana stays separate = Domen's technical dashboard, never family** — Renovate is a Forgejo Dependency Dashboard issue (GitOps), not surfaced in Grafana. ③ **HD-317 Technitium DNS-primary → VPS** (`session/dns-primary-vps` → `3eba9d0`, resolved one merge conflict in the technitium compose — kept main's `/etc/dns` tmpfs + my instance naming): **3-instance DNS HA = VPS primary (resolver = VPS PUBLIC IP `dns_primary_ip`, Option A) / oldsrv secondary / Pi tertiary**; `dns-servers` overlay + `tchnitium_dns_overlay_ip` container pin; headscale nameservers VPS-public-first; **VPS nftables source-allow UDP/TCP 53 from tailnet CGNAT + `dns-allow-home` set refreshed by a new `dns-allow-refresh` systemd timer (home DDNS `s.kogler.si`)**; renderer/doc-template/host_vars consistency (removed stale host_vars `dns_*` that shadowed the SSOT; generated doc shows the VPS-primary tier; validator renders the trio). **All three validate-green on main.**

> **⏳ Deploy-gated (owned by the parallel nas/Pi provisioning session + owner — NOT run from the orchestrator):** next **VPS converge** (`vps.yml` + `vps-hardening` + `monitoring` + `docker_services`) brings up (a) Technitium primary + nftables allow + DDNS timer + (b) the 5 Grafana dashboards (file-provider hot-reload) + (c) the homepage family/technical tiles; then **router `rb4011` step** (DHCP/resolver ordering VPS→oldsrv→Pi), **seed the split-horizon static A records on all 3 instances** (ha→VIP, dns-pi→VIP, *arr, dashboard names → `tailnet_sidecar_ip`), and owner verifies (Panel render, `dig @<vps-public> dns.kogler.si`, tailnet `stats.kogler.si`, home.kogler.si family view vs Domen technical).

> **SESSION #61 (2026-09-03, worktree `homelab-wt-20260903-0941` → branch `session/ha-pi-provision`, commits `f6a6114` + `55b2a9d` merged + pushed):** **Phase-4 Pi HA-primary provision LANDED + LIVE.** Full `raspberry_pi.yml` run `failed=0` on `pi.kogler.si` (Debian trixie + Docker): **home-assistant-primary** (HA 2026.8.1 + keepalived **MASTER** on VIP `ha_vip`) · **technitium-secondary** (DNS :53 alive) · **traefik-ha** (TLS `*.kogler.si` valid, Verify 0) — all up + verified; `https://ha.kogler.si/` → **302**. HA boots **0 config errors**. Live-fixed this session: docker_services vault pre-pass crashed on entries without `enabled:` (Pi primary/secondary omit it) → defined+truthy ∪ missing-key = default(true); deploy-service first-boot guard stat used `{{ item }}` with `loop_var: ha_src_file` (undefined) + fail_msg referenced undefined `ha_standby_path` on the Pi → fixed to loop var + registered results; **technitium compose** duplicate `cap_add` + `read_only: true` crash-looped (`Read-only /etc/dns, /var/log/technitium`) → merged caps + relaxed read_only (forgejo precedent) + tmpfs `/etc/dns`; **knx-hass-gen.py emitted a `knx:` wrapper** inside knx-entities.yaml (included via `knx: !include`) → nested `knx:` invalid — removed wrapper, regenerated (legacy rekuperator + appliance sensors preserved); **meteoblue NOT in HA 2026.8.1** → platform failed setup + blocked HA (incl. proxy trust) — weather block disabled until available/HACS; **`configuration.yaml` trusted_proxies** needed the Pi traefik-ha host IP (`network_mode: host` → source = the Pi's own Home IP, SSOT `pi` row), AND a **stale `.storage/http` on the Pi** held `use_x_forwarded_for` absent (HA 400 behind proxy) → deleted it so HA re-derives from YAML; **ha-cert-sync.sh** excludes `dump/` (ACME private keys never leave the issuer) — plus a **VPS-side operator step that was missing**: `ha-sync` key was never authorized on the VPS `ansible-admin` (rsync-only wrapper + key now added; rsync installed on the VPS). **Close-out (same session):** `docs/deployment-pi-provision.md` status banner + §6 success criteria marked LIVE; todo.md HD-307/HD-310 trimmed to owner-gated remainders. **Owner follow-ups:** KNX UI config-flow import (tunneling `knx_router_ip`), Authentik OIDC on the `ha` route (no Forward-Auth), `ha.kogler.si` DNS cutover → VIP (`smart-home-failover.md`), failover runbook exercise. **Handoff edited from #60 (diff-rule).**

> **Role:** entry point for the next session. This handoff is a **pointer index** — every item's owning doc/row holds the detail. Start with README.md §0 (intent routing), then §1 context, then §3 execution order. Read [todo.md](todo.md) for the full row bodies.

> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 · [scripts/guard-session.sh](scripts/guard-session.sh) · [todo.md](todo.md) · archive: [reports/changelog.md](reports/changelog.md), [reports/deployment-journal.md](reports/deployment-journal.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — §Tags & surgical runs (`docker_services_scope`, `base` tier) + §Deploy Timing Runbook.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — compose/secret conventions + fail-loud rule.
4. [docs/network-vpn.md](docs/network-vpn.md) — §Pattern A tailnet sidecars + current tailnet edge state + §MagicDNS (extra_records design).
5. Owning `docs/*.md` (per task) — the decision-logs + spec SSOT; read before re-deciding anything. Historical changelog/journal rows live in the frozen `reports/changelog.md` / `reports/deployment-journal.md` (archive-only).

## 1. Environment (Windows 11 laptop)

Same as handoff #16 (unchanged): git-bash, forward-slash, `py -3`, UTF-8 no-BOM, LF; Ansible via WSL + 9P gate before every run; Secrets → 1Password item.field only, `>-` for YAML renders. **Self-learned (2026-08-26):** do NOT use multi-line bash heredocs with backslashes/backticks (mangled through `bash -c`) — write patch scripts to a temp file and run them. `wsl.exe` from git-bash mangles `/mnt/...` — prefix `MSYS_NO_PATHCONV=1`. **Signed-commit gotcha (still valid):** keys in `~/.ssh/github_signing`/`github_auth`; `Couldn't find key in agent` → `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.

## 2. State snapshot (start of next session)
- **HD-312 3-SSID WiFi — phases 1 + 2a DONE + LIVE (2026-09-03).** Live SSIDs = **Kogler (VLAN 10) + Kogler IOT (VLAN 20) + Kogler guest (VLAN 30, 5GHz)**; IOT-WAN + Kids SSID configs **deleted** (configs + security profiles removed live + from SSOT; items kept in 1P, unreferenced). Phase 1 static-IP sweep: 4× Shelly RGBW2 + 4 family WiFi clients (valentina/phone-domen/phone-martina/tablet-ipad) + UPS + iLO now have MAC + static reservations in `network_static_hosts` (router converge applied them live — UPS already bound its `.99.9`; iLO/phones/iPad bind at next lease renewal/reconnect). Phase 2a: guest turned up via idempotent deltas (`rb4011_guest_delta`/`crs328_guest_delta`/`ap_guest_delta` .rsc.j2, no secrets, folded into render-converge.yml); CAPsMAN provisioning now band-split by SSID `band:` (2.4GHz = Kogler+IOT, 5GHz = Kogler+guest). **⚠ LIVE GAP (next priority): the cloud-IoT appliances (2× LG AC `2C:2B:F9:*`, 3× Bosch `68:A4:0E:*`, HAP `00:1A:22:1E:F7:FD`) were on the deleted `Kogler IOT WAN` SSID (VLAN 21, WAN allowed) — they are now joined to `Kogler IOT` (VLAN 20 = **no internet / WAN drop**). **They need HD-312 phase 3 (`iot-wan-allow` firewall MAC-list) or a manual VLAN-21 path to regain WAN.** Their DHCP leases show dhcp-20 dynamic leases. ⏳ Remaining: (2b) CAPsMAN per-MAC `access-list` vlan-id; (3) firewall MAC-lists (`iot-wan-allow`, `kids-*`) + kids-control off VLAN 40; (4) n8n workflow + scoped router API user. Full spec [network-vlans.md](docs/network-vlans.md) §3-SSID + [network-rejected.md](docs/network-rejected.md) + [todo.md](todo.md) HD-312.
- **HD-310 FINAL port model = LIVE + SSOT (2026-09-02)** — `router_port_map`: **old-srv ether2 = pvid10 + tagged[99] · laptop-domen ether3 = pvid10 + tagged[] (untagged Home only — mgmt via Pi hop) · rpi ether10 = pvid10 + tagged[99]** (nas-ilo/homematic-ap stay pvid99). Live router verified: VLAN10 untagged=ether2,3,10 · VLAN99 tagged=ether2,10 (+trunk/self), untagged=ether7,9. Services `available-from` = **Mgmt subnet only** STRICT again ([network-addresses-generated.md](docs/network-addresses-generated.md)) (Home→Mgmt forward removed — **Home must never reach core infra**; Pi `eth0.99` tagged leg is the only mgmt-plane client). SSH: `~/.ssh/config` `pi99`→mgmt `pi` / `router99`→`router` mgmt via ProxyJump `pi` (IPs in [network-addresses-generated.md](docs/network-addresses-generated.md)) — verified. **Winbox GUI tunnels (2026-09-02, SSOT [network-vpn.md](docs/network-vpn.md)):** `ssh -N switch-wb`/`ap-spalnica-wb`/`ap-dnevna-wb` → `127.0.0.1:8292/8294/8295` (forward runs ON the Pi — the Mgmt-leg holder; a ProxyJump-to-device final hop leaves RouterOS SSH hosting the forward, which breaks Winbox auth — live lesson). Cable moves: ✅ **DONE 2026-09-02** — Pi → **ether10**, laptop → **ether3** (owner moved the cables; mini-switch freed); live end-to-end re-verified (Pi Home leg, `router99` via tagged-99 hop, router bridge ports pvid=10 on ether2/3/10 + pvid=99 on ether7/9).
- **HD-302 Comtrend modem mgmt = LIVE + SSOT (2026-09-02, commit `22abb8a`)** — live fix: `router_on_modem` `/32`→`/24` on ether1 (connected route to the modem subnet) + **srcnat masquerade** out ether1 (dst=`comtrend_modem.modem_mgmt_ip`:80/443) because the consumer ONT only answers its web UI to its directly-connected peer (router on the modem LAN) — forwarded HTTP from deeper subnets is silently ignored. **Live-verified:** laptop → modem UI returns 401 (auth page); router can ping the modem. SSOT: `comtrend_modem` (router.yml), `rb4011_converge.rsc.j2` SNAT rule, generated SSOT re-rendered. ⏳ **Remaining:** private DNS `modem.kogler.si` → needs the first Technitium (oldsrv offline).
- **Phase 2 (nas) DONE + LIVE (2026-09-03, HD-314)** — `storage.yml` failed=0. ZFS pools tank (mirror) + bulk (RAIDZ2) IMPORTED + datasets/props; NFS exports → oldsrv (`/tank/data`, `/bulk/media`, `/bulk/data/immich-thumbs`, SSOT oldsrv Home IP `/32`, root_squash anonuid/gid=1005) — `exportfs` verified; sanoid+syncoid timers; Samba; **NUT master LIVE** (PowerWalker `upsc` battery 100% / Innova Unity; `upsd` :3493 + `nut_exporter` :9199 + `zfs_exporter` :9198 active); cockpit-storaged (trixie has no cockpit-zfs). nas's DHCP client-id = MAC (`dhcpcd.conf clientid mac`) → binds its **reserved Home static** (SSOT) instead of the dynamic `.100`. Host state + full fix list: [hardware-nas.md](docs/hardware-nas.md) + [todo.md HD-314](todo.md) + [deployment-manual.md](deployment-manual.md) §Phase 2. ⏳ **Remaining:** battery-pull test (owner, HD-06), `bulk/migrate` landing-zone redistribution (HD-207).
- **⚠ Live-ops flag (2026-09-03):** the **router mgmt-plane (tagged-99 → the router's SSOT Mgmt IP) was intermittently unreachable** during the nas session (from the Pi: ARP to the router's Mgmt IP FAILED, SSH reset once; Home-plane ICMP OK). Likely pre-existing (the handoff already noted 'router ARP incomplete'). Before Phase 3/4 router-facing convergences, owner should check the tagged-99 leg / `router99` reachability.
- **DNS: oldsrv/Technitium (primary DNS) — oldsrv back on `.101` (DHCP lease, 2026-09-03)** — it's powered on + pingable at its transient lease; still needs the Phase 3 provision (networkd units + DHCP client-id=MAC) before it runs Technitium at its reserved Home static. Laptop DNS stays `1.1.1.1`/`1.0.0.1` until then.
- **Pre-existing (not from this session, unaddressed):** `sync-skills.sh` drift — deployed `~/.pi/agent/skills` (`plan-task`, `run-task`) differ from repo `skills/` (repo = SSOT). Fails gate item 13 at HEAD too. Owner decision: `bash scripts/sync-skills.sh --push` (deploy repo→deployed) or `--pull` (capture deployed→repo).

- **Phase 1 (VPS edge) is LIVE** — full enabled `docker_services` set converges green; observability + Authentik + headscale + traefik-tailnet all up. **HD-298 (pi-dev pty.node) DONE + LIVE-VERIFIED** (`pi-dev:8080` 200 via sidecar; 4-layer fix: ignore-scripts removal + glibc node:24 + npm allowScripts + cap_add CHOWN/FOWNER/DAC_OVERRIDE). Evidence: owning docs + `git log`.
- **HD-297 tailnet clean URLs: MOSTLY LIVE.** `https://dsh.ts.kogler.si` / `https://pi-dev.ts.kogler.si` → **200** (verified at the edge). **BLOCKED piece:** plain `*.kogler.si` → 302-SSO — `middleware "authentik-forward-auth@file" does not exist` (traefik-tailnet; middlewares.yml never rendered — detail §3-1). DN + Authentik providers done (`forward-dsh`/`forward-pi-dev` exist AND bound to embedded outpost).
- **Phase 4 (Pi): FULLY PROVISIONED + LIVE (2026-09-03, HD-307/310/04).** Pi OS Lite installed (hostname `pi`, user `admin` + `ansible-admin`, SSH key `ansible-admin_ssh`); port model + physical final (Pi on **ether10** untagged Home 10 + tagged Mgmt 99; laptop on **ether3** untagged Home only); host-side dual-home = two NM keyfiles (`pi-eth0.nmconnection` Home + `pi-mgmt.nmconnection` tagged-99, verified). **Full `raspberry_pi.yml` provision run `failed=0`** — **home-assistant-primary** (HA 2026.8.1 + keepalived MASTER on VIP `ha_vip`), **technitium-secondary** (DNS :53 alive), **traefik-ha** (TLS `*.kogler.si` valid) ALL UP + verified; `https://ha.kogler.si/` → **302**; HA boots **0 config errors**. Live-fixed: docker_services enabled-set, first-boot guard loop_var, technitium read_only/cap_add, knx-entities `knx:` wrapper, meteoblue-not-in-2026.8 (disabled), trusted_proxies + stale `.storage/http`, ha-cert-sync `dump/` exclusion + VPS ha-sync key auth/rsync. ⏳ **Remaining (owner-gated):** KNX UI config-flow import, Authentik OIDC on `ha` route, `ha.kogler.si` DNS cutover → VIP, failover runbook. Runbook: [deployment-pi-provision.md](docs/deployment-pi-provision.md) (status = fully provisioned).
- **RouterOS apply convention (decided 2026-09-01, [network-ops.md](docs/network-ops.md)):** `initial`(manual bootstrap) → `converge`(=full, SSOT, idempotent, universal apply) → `delta`(transient only, fold back). API = verify/idempotent only. **Apply = render → /import (SCP over SSH, pinned host key, `ansible` identity).** **⚠ Ordering pitfall (live 2026-09-01):** a FORWARD accept appended at the tail lands BELOW the repo's mid-list "Default deny inter-VLAN" and is shadowed (HD-307 hit this: rule #33 below deny #25 → dead). Fix pattern (in network-ops.md): dedup → re-add → `move … destination=$deny` (RouterOS 7 = places BEFORE) → always verify position. Idempotent deltas: `rb4011_pi_delta.rsc.j2`, `rb4011_hd307_delta.rsc.j2` (both committed, rendered via render-converge.yml).
- **HD-309 (Shield Media→WAN) DONE + LIVE-VERIFIED (2026-09-01).** Root cause was the router DNS tertiary (SERVFAIL, no upstreams) — NOT the forward-ACCEPT (default-accept already allowed it). Fixed: `/ip dns servers=1.1.1.1,1.0.0.1` + row-73 WAN accepts for all VLANs (Home/Media/Guest/Mgmt + Kids-out-of-window). Live: the Shield (nvidia-shield, Media VLAN) has 6 established Media→WAN conns; DNS resolves on the Media gateway. Tooling: `scripts/routeros-apply-delta.sh`. · owning docs + git history
- **Phase 1.5 network redo DONE except close-out (2026-09-02):** switch + APs reset + `.pub`/`.rsc` re-rendered (flash/ fix, HD-304); router on the new design; **wg-s2s handshake RESOLVED + LIVE (HD-285 two-key fix, commit `bc4eb95`)**; owner MikroTik password rotation pending.
- **wg-s2s (HD-285) RESOLVED + LIVE 2026-09-02 (two-key design fix).** Root cause: shared `wg_password` on both ends → identical pubkeys → router handshaking with itself. Now: router `wg_password` + VPS **NEW `wg_password_vps`** (distinct), oneshot owns the VPS iface (networkd create-only + Unmanaged), router HD-155 accept moved above the Default deny, router endpoint = `vps.kogler.si` FQDN. Live: handshake fresh (keepalive), VPS→router-mgmt 0% loss. Commit `bc4eb95`.
- **Vault state:** all items present under `check-vault-items.sh --strict` (needed 8, in vault 84), except the tailnet-sidecar preauth items `tailscale_dsh_api`/`tailscale_pi_dev_api` which are **MISSING until the HD-268c owner enable-flow runs** (mint preauth keys → seed 1P → flip flags → scoped converge).
- **ID registry:** next free = max(HD)+1 in [todo.md](todo.md) — always re-derived at write time (CONVENTIONS §1/§4); NEVER type a literal (newest rows: HD-310, HD-311, registered 2026-09-01).
- **Coordination:** headplane/headscale = SEPARATE lane (D) — coordinate, never fold into other converges.

## 3. Next-session execution order (pointers — row bodies in todo.md)


### 3-N. NEXT (priority order)

0. **Phase-4 Pi provision — ✅ DONE + LIVE (2026-09-03, HD-307/310/04).** `raspberry_pi.yml` `failed=0`; **home-assistant-primary** (HA 2026.8.1 + keepalived MASTER on VIP `ha_vip`) · **technitium-secondary** (DNS :53) · **traefik-ha** (TLS valid) all up; `https://ha.kogler.si/` → 302. ⏳ **Remaining (owner-gated):** KNX UI config-flow import, Authentik OIDC on `ha` route, `ha.kogler.si` DNS cutover → VIP, failover runbook. · [deployment-pi-provision.md](docs/deployment-pi-provision.md) · [todo.md HD-307](todo.md)

dc5→
0. **HD-314 — nas Phase 2 — ✅ DONE + LIVE (2026-09-03, commit `326b08f`).** `storage.yml` failed=0; ZFS pools imported, NFS exports → oldsrv live, NUT master live, exporters/cockpit up. ⏳ **Remaining (owner):** battery-pull test (HD-06), `bulk/migrate` landing-zone redistribution (HD-207). · [todo.md HD-314](todo.md) · [hardware-nas.md](docs/hardware-nas.md) · [hardware-ups.md](docs/hardware-ups.md) · [deployment-manual.md](deployment-manual.md) §Phase 2

0. **oldsrv Phase 3 — NOW UNBLOCKED (was: deploy-gated on nas NFS + NUT master).** nas NFS (`/tank/data`, `/bulk/media`, `/bulk/data/immich-thumbs` → oldsrv) + NUT master are live; the DHCP client-id=MAC fix + netd units are in the `network` role (deploy-gated on host provision). oldsrv is up on `.101` (DHCP lease). **First:** owner/agent confirm the router mgmt-plane (⚠ flag in §2), then provision oldsrv (`home_servers.yml`) — it becomes the DNS primary (Technitium, HD-302 closure) + NUT client + heavy services. · [todo.md HD-311](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 3

0. **HD-312 — 3-SSID WiFi + per-MAC control (phases 1+2a+3 DONE; 2b/3b/4 next).** ⏳ ***Deploy-gate (HIGHEST VALUE): next `router.yml` converge — UN-BLOCKS the cloud-IoT appliances* (iot-wan-allow MAC-list regains their WAN).** Remaining: (2b) CAPsMAN per-MAC `access-list` vlan-id; (3b) `kids-*` lists active + kids-control off VLAN 40 (owner: define kids device set); (4) n8n workflow + scoped router API user. · [todo.md HD-312](todo.md) · [network-vlans.md](docs/network-vlans.md) §3-SSID

0. **HD-313 deploy-gate (IaC DONE 2026-09-03).** RouterOS → VPS CrowdSec + Loki log shipping — deploy-gated on: seed 1P `mikrotik-logpipe_api` (provision-vault.sh) → next **router.yml converge** (system/logging action + topics + logpipe user) → **VPS monitoring + crowdsec** deploy → live-verify `cscli alerts list` + Loki search find RouterOS lines. · [todo.md HD-313](todo.md) · [network-ops.md](docs/network-ops.md) §Central log shipping

> **SESSION #59 (2026-09-03, worktree `homelab-wt-20260903-0936` → branch `session/iot-wan-allow`, commits `8442be7` + `1e6c2cd` merged + pushed):** **HD-312 phase 3 IA-C DONE (deploy-gated)** — per-MAC `iot-wan-allow` firewall MAC-list + WAN egress accepts in the router role. Details in the §3-N.0 pointer above. validate-all green. **Deploy-gated tail in [todo.md HD-312](todo.md):** next router converge → verify `/ip firewall address-list print` shows the 6 iot-wan-allow MACs + LG/Bosch/HAP regain WAN. · docs: [network-vlans.md](docs/network-vlans.md) §3-SSID/P3 · [network-dns.md](docs/network-dns.md) §Per-Subnet DNS Policy. **Handoff edited from #58 (diff-rule).**
1. ~~NEW follow-ups from HD-309~~ — **DONE 2026-09-01 (no HDs registered):** ① bedtime `time=` rule was a **false alarm** (`invalid=true` = normal out-of-window RouterOS display; overnight 22h-7h span confirmed working — no code change); ② **NAT duplicate rows FIXED** (converge reset-first + role reconcile, live-deduped to 3). See prompt.md §2. Done/omit.
1. **HD-310 — DONE + LIVE-APPLIED + VERIFIED (2026-09-02, final port model).** ✅ SSOT (`router_port_map`) + converge template = ether2 10/t99 · ether3 10-only · ether10 10/t99; live applied + verified. ✅ **Physical cable move DONE 2026-09-02:** Pi → ether10, laptop → ether3. ✅ **Phase 4 provision DONE + LIVE (2026-09-03, HD-307):** `raspberry_pi.yml` `failed=0`; `ha.kogler.si`→VIP + keepalived MASTER verified; TLS Verify 0. ⏳ **Remaining (owner-gated):** KNX UI import, Authentik OIDC, DNS cutover, failover runbook (HD-04 umbrella). · [todo.md HD-310](todo.md) +
2. **HD-311 — host-side dual-home IaC (PARTIAL DONE, Pi side DONE + VERIFIED + oldsrv/nas netd IaC DONE 2026-09-02).** ✅ Pi:Mgmt IP moved OFF the untagged parent ONTO the **tagged `eth0.99`** sub-interface — new `pi-mgmt.nmconnection.j2` (never-default, route via tagged leg) + `pi-eth0.nmconnection.j2` is now Home-only; role renders + activates both; router role enforces bridge-port pvid/tagged from `router_port_map` (HD-311 a/b/d). Live-applied + verified: `router99` → Pi tagged-99 hop → router mgmt WORKS; live state = eth0 Home only, eth0.99 Mgmt, `mgmt_subnet` routes via eth0.99 only. ⏳ **Remaining:** apply the oldsrv/laptop tagged-99 sub-interfaces once their OS stacks are provisioned (laptop stays untagged-Home only per decision — no tagged-99 on Windows). **Note (parallel lane `session/hd311-netd`, commit `8c496fa`, merged into this main):** the oldsrv/nas IaC is ALREADY DONE — the `network` role renders systemd-networkd units (`netd-phys.network.j2`/`netd-vlan.netdev.j2`/`netd-vlan.network.j2`) for nas+oldsrv; Pi keeps the NM keyfile path. So the remaining piece is **deploy-gated only** (first Ansible contact applies the netd units). ⏳ Deploy-gated on host provision. · [todo.md HD-311](todo.md)
3. **HD-285 — wg-s2s handshake — ✅ RESOLVED + LIVE 2026-09-02 (two-key fix).** Root cause: ONE shared `wg_password` on both ends → router interface pubkey == VPS peer pubkey → WireGuard refuses to handshake with its own key. Fix: distinct per-side keys (`wg_password` router + NEW `wg_password_vps` VPS), oneshot-owned VPS iface (networkd create-only), router HD-155 forward accept moved above Default deny, router endpoint → `vps.kogler.si` FQDN. Live: fresh handshake + rx/tx both sides, VPS→router-mgmt 0% loss. Commit `bc4eb95`. · [todo.md HD-285](todo.md) · [roles/wireguard/](IaC/ansible/roles/wireguard/)
4. **Phase 1.5 cutover remainder (operator + AI).** ✅ 2026-09-02: switch reachability re-verified + CAPsMAN steady-state imported (§1.5.4, commit `b0bae72`). ✅ 2026-09-02 (later): **runner now reaches the API via the Pi-99 hop** — new `scripts/ansible-network-hop.sh` (commit `cebd312`); **router + switch roles re-converged LIVE through it** (router `ok=34 changed=5`, switch `ok=23 changed=4`, both `failed=0`) — §1.5.3 switch role take-over DONE; live switch/router bugs fixed in roles (poe-out auto-on, no poe-priority, protocol tcp on INPUT; router_port_map dict2items). ⏳ **Remaining:** APs re-join CAP (registration-table fills as they power up); owner MikroTik password rotation; parked `validate-routeros7-syntax` linter stays parked. · [deployment-manual.md](deployment-manual.md) §1.5 · [todo.md HD-304](todo.md) · [deployment-tasks.md](deployment-tasks.md) §Phase 1.5
5. **HD-299 — Technitium primary → VPS** (new `dns-servers` overlay, 3-instance DNS HA, split-horizon static A records for plain `*.kogler.si` on tailnet). ⏳ IaC work + owner RB4011 forwarder step. · [todo.md HD-299](todo.md) · [docs/network-dns.md](docs/network-dns.md) line 18/67
6. **HD-296 + HD-297 — tailnet subdomains + traefik-tailnet clean URLs (dsh/pi).** (a) IaC CLOSED; (b) DEPLOY + (c) DEPLOY-VERIFY owner-side (scoped headscale converge, then full vps.yml converge for `tailnet-apps` overlay; `dig`/`curl` checks). · [todo.md HD-296](todo.md) + [todo.md HD-297](todo.md) · owning docs
7. **HD-268c owner enable-flow (tailnet sidecars live).** Mint preauth keys → seed 1P `tailscale_dsh_api`/`tailscale_pi_dev_api` → flip `*_tailnet_sidecar_enabled` → scoped converge → verify UIs. 4-step flow documented in `group_vars/vps.yml`. · [todo.md HD-268](todo.md)
8. **HD-264 Renovate sandbox (owner).** (a) commit versioned manifest + `renovate.json` to `domen/test`; (b) external re-launch timer; (c) verify PR lands; (d) flip `RENOVATE_REPOSITORIES` → `domen/homelab` (LAST). · [todo.md HD-264](todo.md) · [docs/deployment-renovate.md](docs/deployment-renovate.md)
9. **Audit-fold owner actions (from the merged audit):** HD-280 fail2ban (add traefik accesslog + filter, or drop http-auth jail) · HD-288/289 sunshine owner-decision + port binding · HD-292 VPS `docker image prune` · HD-294 drop §8.4 refs when HD-263 closes · HD-286 vault-escape template comments. Row bodies carry owning docs. · [todo.md](todo.md) §§2.4/2.8
10. **Metabase gates (PARTIAL DONE):** HD-241 SMTP *Send test email* + invite flow; HD-242 connect both sources + import dashboards + SELECT-only proof. · [todo.md HD-241](todo.md) + [todo.md HD-242](todo.md)
11. **AI stack pre-deploy gates:** HD-105 secrets + OIDC providers (blocks HD-100..104); then HD-101 SSO verify ride-along, HD-102 PGVector db item + extension, HD-103 docling, HD-104 openclaw onboard. · [todo.md HD-100..105](todo.md) · [docs/deployment-ai-stack-secrets.md](docs/deployment-ai-stack-secrets.md)
12. **HD-112 Zipline go-live legs** (first deploy human-gated; post-up seeding runbook in compose header; family drop script). · [todo.md HD-112](todo.md)
13. **HD-252 pending cleanup (owner):** nodes list EMPTY — re-enroll laptop + phone via the now-working OIDC flow under `domen@kogler.si`. · [todo.md HD-252](todo.md)

> **Handoff diff-rule (CONVENTIONS §4):** this handoff is edited from #60's — every prior §2/§3 open item above either appears or is explicitly resolved (closed items moved to the owning docs + git history only; frozen changelog/journal are archive). **Derived-values ban:** next-free HD quoted from todo.md, never typed. Next free HD: derive via `bash scripts/next-hd.sh` (HD-315+).

### 3-N. NEXT (priority order)

0. **HD-313 deploy-gate (new, IaC DONE 2026-09-03).** RouterOS → VPS CrowdSec + Loki log shipping — deploy-gated on: seed 1P `mikrotik-logpipe_api` (provision-vault.sh) → next **router.yml converge** (system/logging action + topics + logpipe user) → **VPS monitoring + crowdsec** deploy (rsyslog receiver + acquis + a1ad/mikrotik collection) → live-verify `cscli alerts list` + Loki search find RouterOS lines. · [todo.md HD-313](todo.md) · [network-ops.md](docs/network-ops.md) §Central log shipping

> **SESSION #57 close (2026-09-03, worktree `homelab-wt-20260903-0105-wifiarch` → branch `session/wifi-arch-decision`):** **HD-312 WiFi architecture DECISION documented** — 3-SSID consolidation (Kogler family+kids / IOT isolated / guest) + per-MAC control (CAPsMAN access-list vlan-id + firewall MAC-lists) + static-IP prerequisite + n8n firmware automation. Decision row in [network-rejected.md](docs/network-rejected.md), HD-312 row + spec in [todo.md](todo.md) + [network-vlans.md](docs/network-vlans.md) §3-SSID. **Not implemented** (live config still Kogler + Kogler IOT). ⏳ Next: static-IP sweep in SSOT → CAPsMAN 3-SSID IaC → firewall MAC-lists → n8n. Also live-fixed this session (commits `0ac0222`/`0099a6c`/`98cd981`): CAPsMAN manager-enable + datapath-VLAN→bridge model, switch AP-ports tagged VLAN 10/20 (phone 5GHz disconnects), per-band configs + 5GHz non-DFS ch36. Handoff edited from #56 (diff-rule).

## 4. Working rules (binding)

Fresh worktree per session BEFORE any edit — mechanically enforced (`bash scripts/guard-session.sh`; `validate-all.sh` hard-fails primary+main+DIRTY); merge back only committed+green, ff-only; primary = merge station only. **9P gate: N/A on the WSL Debian primary (repo moved to ext4 — no /mnt/d drvfs staleness; the gate is legacy from the Windows-runner era).** Surgical `--tags docker_services -e docker_services_scope=<svc>`; secrets 1P-item.field only + `>-` for YAML; persisted Authentik tokens always `expiring=False` (HD-216); owning doc + todo update in same change; English; relative links; Authentik blueprint pin array attrs (HD-231); don't touch headplane/headscale unless asked. No multi-line bash heredocs with backslashes/backticks — write+run temp script files instead. (changelog/journal frozen → `reports/`, archive-only.)