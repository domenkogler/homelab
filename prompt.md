# prompt.md — Deployment Execution Handoff #46 — **HD-297 (b) done, (c) owner-verify + blocked plain-URL 302 (middlewares.yml render gap) · HD-306 still open · Pi Phase 4 (HD-307) in progress**

> **SESSION #45 (2026-09-01, worktree `homelab-wt-20260831-hd297`, branch `session/hd297-20260831`, MERGED → main `a955bc2` + session `homelab-wt-20260901-hd297close` for this handoff):** Advanced **HD-297** (clean tailnet URLs for dsh + pi-dev web UIs via `traefik-tailnet`) well beyond the IaC-close of #45. **Completed + live-verified:** ① pi-dev web-UI subdomain **renamed `pi.kogler.si` → `pi-dev.kogler.si`** (pi.kogler.si is the Raspberry Pi DEVICE hostname — the docker UI must not claim it); ② headscale `extra_records` for `dsh`/`pi-dev` now point at the **traefik-tailnet EDGE** (`tailnet_sidecar_ip`) — the Pattern-A sidecars only serve plain HTTP :3080/:8080, no TLS/routing; ③ Authentik **`forward-dsh` + `forward-pi-dev` ProxyProviders created AND bound to the embedded outpost** (`outpost_embedded.providers` list in `ks-forward-auth.yml`) — the outpost DOESN'T load providers that aren't bound (observed live; restart alone didn't help, binding did). **Live-verified:** `https://pi-dev.ts.kogler.si` → **200** (Express), `https://dsh.ts.kogler.si` → **200** (DSH HTML) via the edge (`tailscale serve` TCP:443 → 127.0.0.1:443 on vps-obs). **🔴 BLOCKED (remaining HD-297 (c)):** plain `*.kogler.si` → 302-SSO does NOT work — traefik-tailnet logs `middleware "authentik-forward-auth@file" does not exist` because **`/opt/traefik-tailnet/dynamic/middlewares.yml` is never rendered** (only `middlewares.yml.j2` exists; `routes.yml`/`tls.yml` DO render — the "Template extra .j2 files" loop renders routes+tls but NOT middlewares for `traefik-tailnet`). **Root-cause candidates + fix directions in [todo.md HD-297](todo.md)** and the `§3-1` detail below. Also removed a stray tracked `skills/mikrotik/.env.op` happened upstream already. **HD-298 closed** earlier (4-layer pi-web-ui crash-loop fix, live-verified 200 — changelog + journal). **Predecessors #33–#44** closed history (journal + changelog).

> **Role:** entry point for the next session. This handoff is a **pointer index** — every item's owning doc/row holds the detail. Start with README.md §0 (intent routing), then §1 context, then §3 execution order. Read [todo.md](todo.md) for the full row bodies.

> **Linked from:** [README.md](README.md) §0/§2 · [CONVENTIONS.md](CONVENTIONS.md) §4/§6 · [scripts/guard-session.sh](scripts/guard-session.sh) · [changelog.md](changelog.md) · [deployment-journal.md](deployment-journal.md) · [todo.md](todo.md).

---

## 0. Mandatory context (read in this order)

1. [CONVENTIONS.md](CONVENTIONS.md) — §6 worktree discipline (**MECHANICALLY** enforced by `scripts/guard-session.sh` + validate-all hard-gate), §4 journal loop + close-out items, §2 secret-output hygiene, §5 service-onboarding checklist.
2. [docs/deployment-ansible.md](docs/deployment-ansible.md) — §Tags & surgical runs (`docker_services_scope`, `base` tier) + §Deploy Timing Runbook.
3. [docs/deployment-compose.md](docs/deployment-compose.md) + [docs/deployment-secrets.md](docs/deployment-secrets.md) — compose/secret conventions + fail-loud rule.
4. [docs/network-vpn.md](docs/network-vpn.md) — §Pattern A tailnet sidecars + current tailnet edge state + §MagicDNS (extra_records design).
5. [changelog.md](changelog.md) — decision log SSOT; read before re-deciding anything.
6. [deployment-journal.md](deployment-journal.md) — append-only as-built record; latest entries = Phase 1.5 + HD-298 (2026-08-31).

## 1. Environment (Windows 11 laptop)

Same as handoff #16 (unchanged): git-bash, forward-slash, `py -3`, UTF-8 no-BOM, LF; Ansible via WSL + 9P gate before every run; Secrets → 1Password item.field only, `>-` for YAML renders. **Self-learned (2026-08-26):** do NOT use multi-line bash heredocs with backslashes/backticks (mangled through `bash -c`) — write patch scripts to a temp file and run them. `wsl.exe` from git-bash mangles `/mnt/...` — prefix `MSYS_NO_PATHCONV=1`. **Signed-commit gotcha (still valid):** keys in `~/.ssh/github_signing`/`github_auth`; `Couldn't find key in agent` → `ssh-add ~/.ssh/github_signing ~/.ssh/github_auth`.

## 2. State snapshot (start of next session)

- **Phase 1 (VPS edge) is LIVE** — full enabled `docker_services` set converges green; observability + Authentik + headscale + traefik-tailnet all up. **HD-298 (pi-dev pty.node) DONE + LIVE-VERIFIED** (`pi-dev:8080` 200 via sidecar; 4-layer fix: ignore-scripts removal + glibc node:24 + npm allowScripts + cap_add CHOWN/FOWNER/DAC_OVERRIDE). See [deployment-journal.md](deployment-journal.md) + [changelog.md](changelog.md) HD-298.
- **HD-297 tailnet clean URLs: MOSTLY LIVE.** `https://dsh.ts.kogler.si` / `https://pi-dev.ts.kogler.si` → **200** (verified at the edge). **BLOCKED piece:** plain `*.kogler.si` → 302-SSO — `middleware "authentik-forward-auth@file" does not exist` (traefik-tailnet; middlewares.yml never rendered — detail §3-1). DN + Authentik providers done (`forward-dsh`/`forward-pi-dev` exist AND bound to embedded outpost).
- **Phase 4 (Pi) is IN PROGRESS (2026-09-01):** Pi OS Lite installed (hostname `pi`, user `admin`, SSH key `ansible-admin_ssh`), online on mgmt (dynamic `.97`, will move to static `.20` on renew). Router **`ether10` dual-homed** (VLAN-10 access + mgmt-99 tagged), `pi` static reservations applied (idempotent delta rsc). **Next:** [todo.md HD-307](todo.md) — Pi network dual-home + Docker/HA/Technitium provision.
- **RouterOS apply convention (decided 2026-09-01, [network-ops.md](docs/network-ops.md)):** `initial`(manual bootstrap) → `converge`(=full, SSOT, idempotent, universal apply) → `delta`(transient only, fold back). API = verify/idempotent only. **Apply = render → /import (SCP over SSH, pinned host key, `ansible` identity).**
- **Phase 1.5 network redo is MID-CUTOVER (2026-08-31):** switch + APs reset + `.pub`/`.rsc` re-rendered (flash/ fix, HD-304); router on new design (HD-301/303 fixes committed); **switch reachability re-verify + CAPsMAN import (§1.5.4) + wg-s2s handshake (§1.5.5) still pending**.
- **wg-s2s BLOCKED on HD-306** (VPS systemd-networkd peer never binds) — the current top blocker; research candidates + fallback in [todo.md HD-306](todo.md).
- **Vault state:** all items present under `check-vault-items.sh --strict` except the tailnet-sidecar preauth items `tailscale_dsh_api`/`tailscale_pi_dev_api` — **MISSING until the HD-268c owner enable-flow runs** (mint preauth keys → seed 1P → flip flags → scoped converge). (The sidecars themselves are live — they were brought up with an earlier key per HD-268c; only the preauth-item rotation is pending.)
- **ID registry:** next free = max(HD)+1 in [todo.md](todo.md) — always re-derived at write time (CONVENTIONS §1/§4); NEVER type a literal.
- **Coordination:** headplane/headscale = SEPARATE lane (D) — coordinate, never fold into other converges.

## 3. Next-session execution order (pointers — row bodies in todo.md)

### 3-N. NEXT (priority order)

1. **HD-297 (c) — finish the plain-URL 302-SSO (PARTIALLY BLOCKED, high value, AI-doable).** Goal: `https://dsh.kogler.si` / `https://pi-dev.kogler.si` → 302 to Authentik SSO (the `.ts` clean URLs already 200 — this is the last HD-297 piece). 🔴 **Current blocker — `middleware "authentik-forward-auth@file" does not exist`:** `/opt/traefik-tailnet/dynamic/middlewares.yml` is **NOT rendered** (only `middlewares.yml.j2`; `routes.yml` + `tls.yml` DO render). The "Template extra .j2 files for traefik-tailnet" task loops `_extra_templates['traefik-tailnet']` which includes `dynamic/middlewares.yml.j2` — yet the rendered file never appears. **Investigate:** why the render skipped/failed for middlewares (check `extra_templates_result` in a verbose converge; possible: the `.j2` render task deletes a bind-mount-source stub via "Remove stub dirs" then writes — verify the mount interplay; the file is bind-mounted `./dynamic:/etc/traefik/dynamic:ro` — a write to `middlewares.yml` while the container holds `:ro` should still work host-side). **Likely fixes to try in order:** ① re-render manually to confirm the .j2 is valid → if valid, find why Ansible's loop skipped it (dest path, template_dir mismatch, loop label); ② if the render genuinely can't land, render `middlewares.yml` as a static file (drop `.j2` content that has no Jinja or use a separate template) or add it to the compose entrypoint `entrypoint.sh`-style render; ③ confirm after render: `grep -c forwardAuth /opt/traefik-tailnet/dynamic/middlewares.yml` > 0, restart traefik-tailnet, then `wget -S --header="Host: dsh.kogler.si" https://127.0.0.1/` from the tailscale-sidecar netns → expect **302 Location: sso.kogler.si/...** (the forward-auth middleware routes unauthenticated to SSO). NOTE: from the sidecar netns plain DNS doesn't resolve `dsh.kogler.si` — use the Host-header trick; a real tailnet client (owner laptop) is the final check. **Also:** after the middleware works, re-verify plain `stats.kogler.si` (should 302 like before — regression check). · [todo.md HD-297](todo.md) · [IaC/ansible/templates/docker_services/traefik-tailnet/dynamic/middlewares.yml.j2](IaC/ansible/templates/docker_services/traefik-tailnet/dynamic/middlewares.yml.j2) · `_extra_templates` in [roles/docker_services/defaults/main.yml](IaC/ansible/roles/docker_services/defaults/main.yml) lines 74-82 · [docs/network-vpn.md](docs/network-vpn.md) §Pattern A
2. **HD-297 (c) owner-verify (after the middleware fix):** from the laptop on the tailnet: `curl -I https://dsh.ts.kogler.si` + `https://pi-dev.ts.kogler.si` → **200**; `curl -I https://dsh.kogler.si` + `https://pi-dev.kogler.si` → **302 to sso.kogler.si**; `dig dsh.kogler.si pi-dev.kogler.si @<headscale>` → `tailnet_sidecar_ip` (the tailnet edge). · [todo.md HD-297](todo.md)
3. **HD-306 — VPS wireguard peer-bind research (blocker for wg-s2s).** Root-cause why the netdev doesn't bind (`ip link add` test, `journalctl -b -u systemd-networkd`, kernel quirk, fallback `wg setconf` oneshot); then `--tags wireguard` + verify handshake both sides + ping wg-s2s peer IPs. · [todo.md HD-306](todo.md) · [roles/wireguard/](IaC/ansible/roles/wireguard/)
4. **HD-307 — Pi Phase 4 provisioning continuation.** Configure Pi network dual-home (mgmt + Home per SSOT), create `ansible-admin` (NOPASSWD + key), run `raspberry_pi.yml` (Docker + HA-Container + technitium-secondary + traefik-ha, render-first HD-185), verify `ha.kogler.si`→VIP + failover. Pi at `.97` now (→`.20` on renew). · [todo.md HD-307](todo.md) · [deployment-manual.md](deployment-manual.md) §Phase 4
5. **Phase 1.5 cutover remainder (operator + AI):** switch reachability re-verify; CAPsMAN `/import capsman_steady-state.rsc` (§1.5.4); ride `router.yml`/`switch.yml` re-converge; parked `validate-routeros7-syntax` linter stays parked. · [deployment-manual.md](deployment-manual.md) §1.5 · [todo.md HD-304](todo.md)
6. **HD-299 — Technitium primary → VPS** (new `dns-servers` overlay, 3-instance DNS HA, split-horizon static A records for plain `*.kogler.si` on tailnet). ⏳ IaC work + owner RB4011 forwarder step. · [todo.md HD-299](todo.md) · [docs/network-dns.md](docs/network-dns.md) line 18/67
7. **HD-268c owner enable-flow (tailnet sidecars preauth rotation).** Mint preauth keys → seed 1P `tailscale_dsh_api`/`tailscale_pi_dev_api` → flip `*_tailnet_sidecar_enabled` → scoped converge → verify UIs. 4-step flow in `group_vars/vps.yml`. · [todo.md HD-268](todo.md)
8. **HD-264 Renovate sandbox (owner).** (a) commit manifest + `renovate.json` to `domen/test`; (b) external re-launch timer; (c) verify PR; (d) flip `RENOVATE_REPOSITORIES` → `domen/homelab` (LAST). · [todo.md HD-264](todo.md)
9. **Audit-fold owner actions:** HD-280 fail2ban · HD-288/289 sunshine · HD-292 VPS `docker image prune` · HD-294 drop §8.4 refs · HD-286 vault-escape template comments. · [todo.md](todo.md) §§2.4/2.8
10. **Metabase gates (PARTIAL):** HD-241 SMTP test + invite; HD-242 connect sources + dashboards + SELECT-only proof. · [todo.md HD-241](todo.md) + [todo.md HD-242](todo.md)
11. **AI stack pre-deploy gates:** HD-105 secrets + OIDC (blocks HD-100..104); then HD-101 SSO, HD-102 PGVector, HD-103 docling, HD-104 openclaw. · [todo.md HD-100..105](todo.md)
12. **HD-112 Zipline go-live legs** (human-gated first deploy; seeding runbook; family drop script). · [todo.md HD-112](todo.md)
13. **HD-252 pending cleanup (owner):** re-enroll laptop + phone via OIDC. · [todo.md HD-252](todo.md)

> **Handoff diff-rule (CONVENTIONS §4):** this handoff is edited from #45's — every prior §2/§3 open item above either appears or is explicitly resolved (closed items moved to changelog/journal only). **Derived-values ban:** next-free HD quoted from todo.md, never typed.

## 4. Working rules (binding)

Fresh worktree per session BEFORE any edit — mechanically enforced (`bash scripts/guard-session.sh`; `validate-all.sh` hard-fails primary+main+DIRTY); merge back only committed+green, ff-only; primary = merge station only. 9P gate before every converge; surgical `--tags docker_services -e docker_services_scope=<svc>`; secrets 1P-item.field only + `>-` for YAML; persisted Authentik tokens always `expiring=False` (HD-216); journal append-only; owning doc + changelog row in same change; English; relative links; Authentik blueprint pin array attrs (HD-231); don't touch headplane/headscale unless asked. No multi-line bash heredocs with backslashes/backticks — write+run temp script files instead.