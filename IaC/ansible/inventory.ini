[router]
router.kogler.si

[vps]
vps.kogler.si

[home_servers]
oldsrv.kogler.si              # Phase 1: bare-metal Debian desktop + Docker host
nas.kogler.si                 # HP MicroServer Gen8 — ZFS storage server
# <proxmox-host>.kogler.si    # Phase 2: future Proxmox host (name TBD)

[raspberry_pi]
ha.kogler.si

[docker_hosts:children]
vps
home_servers

[localhost]
localhost ansible_connection=local
