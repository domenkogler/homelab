---
- name: Test 1Password Integration
  hosts: localhost
  tasks:
    - name: Fetch test secret from 1Password
      debug:
        msg: "Successfully read password from 1Password: {{ lookup('community.general.onepassword', 'kopia_password', field='password', vault=op_vault) }}"
