# Hetzner Storage Box live — CIFS credentials (0600). Rendered by Ansible from 1Password.
# Do NOT commit; referenced from fstab via `credentials=` only.
username={{ lookup('community.general.onepassword', livebox_op_item, field='username', vault=op_vault) }}
password={{ lookup('community.general.onepassword', livebox_op_item, field='password', vault=op_vault) }}
domain=