# Lovelace dashboard — Stanovanje (YAML mode). Modernized from the old
# dashboard_stanovanje.yaml (docs/assets/references/old-ha/), room structure kept,
# upgraded to HA sections/tiles. Rendered by the home_assistant role.
title: Stanovanje
views:
  - !include view_pregled.yaml
  - !include view_luci.yaml
  - !include view_zaluzije.yaml
  - !include view_ogrevanje.yaml