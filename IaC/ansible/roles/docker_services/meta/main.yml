---
dependencies: []

galaxy_info:
  role_name: docker_services
  author: domen
  description: Deploy Docker Compose services from Jinja2 templates — creates external networks, copies templates, renders .j2 files, runs docker compose up -d.
  company: Kogler Homelab
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
  galaxy_tags:
    - docker
    - compose
    - homelab