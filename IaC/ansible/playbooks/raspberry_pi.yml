---
- hosts: raspberry_pi
  become: true
  roles:
    - common
    - network
    - nut             # UPS client (Pi) — after network
    - docker
    - home_assistant
    - docker_services        # raspberrymatic + technitium-secondary on the Pi
    - monitoring