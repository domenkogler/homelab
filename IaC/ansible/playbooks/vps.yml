---
- hosts: vps
  become: true
  roles:
    - common
    - docker
    - network
    - wireguard          # VPS side of wg-s2s (home router tunnel) — before services bind its address
    - docker_services
    - monitoring