# profilarr docker-compose.yml — TODO: define service
#   Quality-profile UI on top of Sonarr/Radarr, profilarr.kogler.si — Forward-Auth.
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}