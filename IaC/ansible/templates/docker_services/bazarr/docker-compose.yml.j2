# bazarr docker-compose.yml — TODO: define service
#   Subtitles, bazarr.kogler.si — Forward-Auth; writes .srt next to media (library rw).
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}