# jellyfin docker-compose.yml — TODO: define service
#   Media server, media.kogler.si — OWN LOGIN (no Forward-Auth, client apps),
#   iGPU transcode via /dev/dri, user: "1000:1000", /mnt/nas/data/media ro.
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}