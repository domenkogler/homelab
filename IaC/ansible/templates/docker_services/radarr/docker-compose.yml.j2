# radarr docker-compose.yml — TODO: define service
#   Movies, radarr.kogler.si — Forward-Auth. TRaSH hardlink import:
#   /mnt/nas/data/downloads/complete/movies → /mnt/nas/data/media/movies (Use Hardlinks ON).
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}