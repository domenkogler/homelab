# ks-oidc.yml — Authentik OIDC providers + applications for homelab services
#
# Blueprint (config-as-code, idempotent). Applied by Authentik at startup / on demand
# from the mounted `./blueprints:/blueprints` volume (authentik compose).
#
# Model: each OIDC consumer is a provider (oauth2provider) + an application bound to it.
#
# A+mini-glue design (services-authentik.md, deployment-compose.md):
#   - This blueprint declares the SHAPE only: providers, applications, flows, outposts.
#   - Client credentials are NOT declared here. Authentik AUTO-GENERATES a client_id +
#     client_secret for each confidential oauth2provider on creation.
#   - The secret-egress glue (HD-143) runs after blueprint apply: GET each provider via
#     the Authentik API (/api/v3/core/providers/oauth2/) and writes the generated
#     client_id / client_secret into the 1Password items the compose templates lookup()
#     (openwebui_api, headscale_api, matrix_api, openclaw_api, opencloud_oidc).
#   Do NOT add client_id / client_secret lookup() fields to this file — that would try to
#   read credentials before Authentik has generated them (chicken-and-egg) and is the
#   exact error the A+mini-glue design removes.
#
# Deploy-verify (HD-147): oauth2provider attrs (authentication_flow/authorization_flow
# names, signing_key name, sub_mode, scope/property mappings) are Authentik-version-specific.
# Validate against the running Authentik on first apply; defaults referenced (self-signed
# cert, implicit-consent flows) ship with a stock Authentik install.
#
# Scope (2026-08-19): HD-142 — OpenWebUI, Headscale, Matrix(Tuwunel), OpenClaw, OpenCloud.
#   Immich / Forgejo / Metabase are HD-148 (follow-on, separate commit).
---
version: 1
entries:
  # ==================================================================================
  # Open WebUI (ai.kogler.si)  ->  1Password `openwebui_api` (seeded by glue)
  # ==================================================================================
  - model: authentik_providers_oauth2.oauth2provider
    id: provider_openwebui
    attrs:
      name: openwebui
      authentication_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      authorization_flow: !Find [authentik_flows.flow, [slug, provider-authorization-implicit-consent]]
      client_type: confidential
      redirect_uris: |
        https://ai.kogler.si/oauth2/callback
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      sub_mode: hashed_user_id
  - model: authentik_core.application
    id: app_openwebui
    attrs:
      name: Open WebUI
      slug: openwebui
      meta_launch_url: https://ai.kogler.si
  - model: authentik_providers_oauth2.application
    attrs:
      application: !Key app_openwebui
      provider: !Key provider_openwebui

  # ==================================================================================
  # Headscale (vpn.kogler.si)  ->  1Password `headscale_api` (seeded by glue)
  # ==================================================================================
  - model: authentik_providers_oauth2.oauth2provider
    id: provider_headscale
    attrs:
      name: headscale
      authentication_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      authorization_flow: !Find [authentik_flows.flow, [slug, provider-authorization-implicit-consent]]
      client_type: confidential
      redirect_uris: |
        https://vpn.kogler.si/oidc/callback
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      sub_mode: hashed_user_id
  - model: authentik_core.application
    id: app_headscale
    attrs:
      name: Headscale
      slug: headscale
      meta_launch_url: https://vpn.kogler.si
  - model: authentik_providers_oauth2.application
    attrs:
      application: !Key app_headscale
      provider: !Key provider_headscale

  # ==================================================================================
  # Matrix / Tuwunel (matrix.kogler.si)  ->  1Password `matrix_api` (seeded by glue)
  #   native homeserver SSO. Matrix's callback is client_id-scoped
  #   (https://matrix.kogler.si/_matrix/client/unstable/login/sso/callback/<client_id>),
  #   so register the prefix form and verify the resolved callback at deploy.
  # ==================================================================================
  - model: authentik_providers_oauth2.oauth2provider
    id: provider_matrix
    attrs:
      name: matrix
      authentication_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      authorization_flow: !Find [authentik_flows.flow, [slug, provider-authorization-implicit-consent]]
      client_type: confidential
      redirect_uris: |
        https://matrix.kogler.si/_matrix/client/unstable/login/sso/callback/*
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      sub_mode: hashed_user_id
  - model: authentik_core.application
    id: app_matrix
    attrs:
      name: Matrix
      slug: matrix
      meta_launch_url: https://matrix.kogler.si
  - model: authentik_providers_oauth2.application
    attrs:
      application: !Key app_matrix
      provider: !Key provider_matrix

  # ==================================================================================
  # OpenClaw (internal gateway, services-internal — NO public subdomain)
  #   ->  1Password `openclaw_api` (seeded by glue)
  #   OpenClaw is an internal AI-agent backend with no public edge. Its Control-UI OIDC
  #   redirect is deploy-time, generated by `openclaw onboard` (deployment-ai-stack-secrets.md
  #   §2b, HD-104). The provider is declared here; append the real redirect_uri(s) from the
  #   `openclaw onboard` output at deploy (headscale/internal callback — NOT a public
  #   openclaw.kogler.si redirect).
  # ==================================================================================
  - model: authentik_providers_oauth2.oauth2provider
    id: provider_openclaw
    attrs:
      name: openclaw
      authentication_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      authorization_flow: !Find [authentik_flows.flow, [slug, provider-authorization-implicit-consent]]
      client_type: confidential
      # redirect_uris filled from `openclaw onboard` output at deploy (HD-104 / HD-143)
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      sub_mode: hashed_user_id
  - model: authentik_core.application
    id: app_openclaw
    attrs:
      name: OpenClaw
      slug: openclaw
  - model: authentik_providers_oauth2.application
    attrs:
      application: !Key app_openclaw
      provider: !Key provider_openclaw

  # ==================================================================================
  # OpenCloud (file.kogler.si)  ->  1Password `opencloud_oidc` (seeded by glue)
  #   native OIDC, multi-redirect web + desktop + mobile (HD-52). Unified provider with
  #   multiple redirect URIs (Option A). Web UI callback is canonical; desktop/mobile
  #   callback suffixes are OpenCloud-client-defined — add any exact URIs on separate
  #   lines here after verifying against the deployed OpenCloud client (HD-147).
  # ==================================================================================
  - model: authentik_providers_oauth2.oauth2provider
    id: provider_opencloud
    attrs:
      name: opencloud
      authentication_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      authorization_flow: !Find [authentik_flows.flow, [slug, provider-authorization-implicit-consent]]
      client_type: confidential
      redirect_uris: |
        https://file.kogler.si/oauth2/callback
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      sub_mode: hashed_user_id
  - model: authentik_core.application
    id: app_opencloud
    attrs:
      name: OpenCloud
      slug: opencloud
      meta_launch_url: https://file.kogler.si
  - model: authentik_providers_oauth2.application
    attrs:
      application: !Key app_opencloud
      provider: !Key provider_opencloud