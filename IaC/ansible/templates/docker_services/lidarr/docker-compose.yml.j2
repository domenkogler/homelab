# lidarr docker-compose.yml — TODO: define service
#   Music, lidarr.kogler.si — Forward-Auth. TRaSH hardlink import:
#   /mnt/nas/data/downloads/complete/music → /mnt/nas/data/media/music (Use Hardlinks ON).
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}