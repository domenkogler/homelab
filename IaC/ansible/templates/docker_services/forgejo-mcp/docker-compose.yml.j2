# TODO: define service — forgejo-mcp (HD-268b MCP marker stub) — placeholder for the
#   Forgejo MCP bridge that lets agents read/write OKF `.md` wiki notes + open PRs
#   (docs/services-ai.md §3/§7). STUB FOR, not implemented:
#     - Network: services-internal (reach Forgejo; proposal via PR against protected
#       `main`, NO merge rights — docs/services-ai.md §7).
#     - Secret: Forgejo service-account PAT, PR-only scope (reuse the dsh/pi-dev
#       Forgejo PR-token seam when implemented).
#     - Data: reads/writes the Forgejo OKF wiki repos (created by owner manually,
#       HD-268b — not scaffolded here).
# TODO: define service