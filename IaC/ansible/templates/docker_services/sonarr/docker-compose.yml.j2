# sonarr docker-compose.yml — TODO: define service
#   TV series, sonarr.kogler.si — Forward-Auth. TRaSH hardlink import:
#   /mnt/nas/data/downloads/complete/tv → /mnt/nas/data/media/tv (Use Hardlinks ON).
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}