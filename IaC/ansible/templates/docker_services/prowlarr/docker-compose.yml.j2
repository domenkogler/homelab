# prowlarr docker-compose.yml — TODO: define service
#   Indexer registry, prowlarr.kogler.si — Forward-Auth; syncs to Sonarr/Radarr/Lidarr.
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}