# sabnzbd docker-compose.yml — TODO: define service
#   Usenet downloader (Eweka, NL), sab.kogler.si — Forward-Auth.
#   Categories movies/tv/music → /mnt/nas/data/downloads/complete/<cat>. Plain LAN (no VPN).
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}