# seerr docker-compose.yml — TODO: define service
#   Request portal (seerr.dev, image seerr/seerr), seerr.kogler.si — OWN LOGIN,
#   SQLite config volume. Internal-only.
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}