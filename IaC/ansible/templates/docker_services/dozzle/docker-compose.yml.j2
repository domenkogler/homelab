# dozzle docker-compose.yml — TODO: define service
#   Live container log viewer, logs.kogler.si — Forward-Auth. Read-only docker.sock,
#   ALL containers, viewer only (never a log store — Loki stays the backend).
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}