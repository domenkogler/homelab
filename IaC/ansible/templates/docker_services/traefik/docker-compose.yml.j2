# traefik docker-compose.yml — TODO: define service
#
# This is the SINGLE ACME issuer for the wildcard *.kogler.si cert (Cloudflare DNS-01).
# The Pi's `traefik-ha` edge syncs its cert pair from here (see home_assistant role, and
# docs/smart-home-failover.md — Edge accessibility). CONTRACT:
#   - The ACME storage/checkout must emit / present the cert pair as exactly
#     `{{ wildcard_cert_file }}` and `{{ wildcard_cert_key_file }}`
#     (vars in group_vars/all.yml — shared with traefik-ha), e.g. at
#     /opt/traefik/certs/{{ wildcard_cert_file }} + {{ wildcard_cert_key_file }}.
#   - ha route: Host(`ha.kogler.si`) -> VIP {{ ha_vip }}:8123, NO Authentik Forward-Auth
#     (Companion WebSocket/token flow), tls.certresolver: letsencrypt.
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}
