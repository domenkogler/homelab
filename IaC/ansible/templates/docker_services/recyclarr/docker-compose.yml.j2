# recyclarr docker-compose.yml — TODO: define service
#   TRaSH custom formats + quality profiles sync → Sonarr/Radarr. Scheduled worker,
#   NO UI / no subdomain (like renovate/doco-cd). Config: recyclarr.yml (this repo).
# Secrets via {{ lookup('community.general.onepassword', '...', vault=op_vault) }}